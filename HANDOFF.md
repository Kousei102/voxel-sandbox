# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05）

- `npm test` **3029 件すべて成功**（前の周 3014 から **+15 件**）・`npm run typecheck` 緑・
  **`npm run build` 緑**（`src/**` を触った周なので走らせました）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていない周です）
- **音の一群は 2 回とも緑**でした（羊毛 0.552 < 雪 0.716 < 砂 2.235・低い声 0.162 < 高い声 0.329。
  落ち着いた値そのままです）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1443 行**（上限 1500・**止まる目安 1450 まで残り 7 行**。この周で 1428 から +15）
- **アイテム 103 種・`MAX_ITEM_ID` 138・111..255 の空きは 117・1..63 の空きは 9**
- **立方体 39 / 非立方体 69**・**クラフト 51 本 / 精錬 7 本 / 食べ物 8 種**・**モブ 9 種類**（変わらず）

## この周でやったこと（AUTODEV 31 の C・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が 7 件（3 件以上）・`AUTODEV-SPEC.md` が **`状態: 未着手`** だったので、
判定表を上から当てて **C（実装）の周**。**15b ミルクバケツ（ID 138）を実装しました。**

**空のバケツで手前の牛を右クリック → ミルクバケツ。右クリックで飲むと毒が消えて空のバケツへ戻る。**
**空腹も満腹度も体力も 1 も動きません**（本家と同じ）。判断の置き場は仕様書のとおり:

- 「誰から搾れるか」は **`mobs.ts` の `MobDef.milkable`**（`kind === "cow"` と書かない。
  **時計も回数も持たないので、`Mob` にもセーブにも 1 バイトも増えていません**）
- 「毒が消えるか」は **`vitals.ts` の `drinkMilk()`**（消せたかを返すだけ。`eat()` も `heal()` も呼ばない）
- 並び順は **`use.ts`**（**搾るのは刈るの隣・器より前 / 飲むのはバケツの直後・器より後ろ**）
- **`main.ts` は 15 行ちょうど**（事実 1 つ・注文 2 つ・入れ替えの `swapBucket()` 6 行）
- **`FOODS` にも `FILLED_BUCKETS` にも 1 行も足していません**（仕様書 §6 の 2 つの罠）

**点検（C-2）は親が `git diff` を全部読む代替**で閉じました（`AUTODEV.md` が認めている形）。
**サブエージェントは立てていません** —— 触るのが 5 ファイルで仕様書が細かく、
同じ番の中で結果が返る形を確かめるより自分で書くほうが安いと判断しました。

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手が 6 件（3 件以上）・`AUTODEV-SPEC.md` が `済` なので
→ 次は B（仕様）の周**です。**キューの先頭は 16 番（サボテンに触るとダメージ・ID 0 個）。**

- **16 番は `main.ts` が 1 行で済む見込み**ですが、**そのあとが足りません**
  （1443 行 + 1 = 1444 で、止まる目安 1450 まで 6 行）。**17 番以降を仕様にする周は、
  先に `main.ts` の行数を見てから割ること。**
- **次に取る ID は 139 から**（`ROADMAP.md` の予約表「139..255 予備 117 個」）

## ブラウザで見てほしいところ

**この周の見た目の変化は、クリエイティブ一覧の枠が 1 つ増えることだけ**です（新しい形も
新しい音も 0 個）。**`node tools/browsershot.mjs` の 5 枚を撮って `Read` で開きました** ——
**console のエラー・例外 0 件 / WebGL 2.0 / ポインタロック効いた**で、**地形・HUD・
インベントリはどれも前の周と同じ**でした。一覧の枠そのものは DOM なので絵に出ず、
`test/craftscreen.test.ts` の「一覧 103 種類 / 末尾 ミルクバケツ」が足場です。

- **今回いちばん見てほしいのは牛です**: 空のバケツを持って牛に寄って右クリック →
  **ミルクバケツになるか**、飲んで**空のバケツに戻るか**、**腐った肉を食べてから飲むと
  毒が止まるか**（画面に「毒が消えた」が出ます）。**間合いは殴れる距離と同じ**です
- **ミルクバケツの色（`0xeaf2f8`）が、一覧で羽根・卵・糸・雪玉と見分けられるか**
  （白っぽいものがこれで 5 つ並びました。`TUNING.md`）
- 持ち越し: **かまど・チェスト・作業台を開いて閉じる経路**（15a の持ち越し）/ 雪玉を投げても
  何も起きない / 鉄・金・ダイヤのブロックが材料の山と見分けられるか / **一覧の枠の名前が
  2 行に折れてはみ出す**（「ミルクバケツ」もこれに当たりそうです。`style.css` の `.slot .label`）/
  クモを 3 匹倒すまで弓が作れない道のり / クモの速さ 6.0 と朝に燃えてよいか /
  投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか / 産卵の間隔 300〜600 秒

## この周で分かったこと

**決まりごと 3 件**（どれも `Edit` で `rules/` に据えました。`RULES-INBOX.md` は 0 件）:

- **`rules/items-survival.md`**: **`MAX_ITEM_ID` を新しいアイテムへ移すと、それまで
  指していたブロックの import が余って `npm run typecheck` が落ちます**（実際に落ちました。
  ブロック側のアイテムは for が作るので、あの import は `MAX_ITEM_ID` 1 か所でしか使われていない）
- **`rules/use.md`**: 搾る・飲むの並び順と、**`FOODS` / `FILLED_BUCKETS` に足したときの
  2 つの壊れ方**（満腹だと毒を消せない / ミルクを地面に流せる）
- **`rules/mobs.md` と `rules/vitals.md`**: `MobDef.milkable`（刈るとの違いは
  **時計を持たないことと、落とし物が要らないこと**）と `drinkMilk()` の作法

## 人がやること

- **設計の判断を 1 つお願いします（3 周持ち越し・そろそろ効きます）: `main.ts` を 2 つに
  割ってよいか。** いま **1443 行で、止まる目安 1450 まで残り 7 行**です。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 13 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
