# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-08・AUTODEV 56 のあと）

- `npm test` **3273 件すべて成功**（**+23**。3250 → 3273）・`npm run typecheck` 緑・
  `npm run build` 緑（`src/**` を触ったので走らせました）
- **`npm run bench` は走らせていません** —— `worldgen.ts` も `mesher.ts` も 1 行も
  触っていないためです（足したのは `def()` 1 つと、`player.update()` の
  `bodyTouches()` 1 回。生成にもメッシュ化にも掛かりません）
- **音の一群は跳ねませんでした**（この周は 3 回走らせて 3 回とも緑）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行（±0）**（`wc -l`。テストの数え方では 1450。上限 1500・止まる目安 1450）
- **アイテム 116 種・`MAX_ITEM_ID` 154・111..255 の空きは 101・1..63 の空きは 9**
  （**この周で取ったのはブロック 154 の 1 個だけ**。予約表の次の空きは **155**）
- **立方体 40（±0）/ 非立方体 77**（76 → 77）・**モブ 9 種類**・**レシピ 59 本**・
  **精錬 7 本**・**燃料 8 種**・**食べ物 11 種**・**バイオーム 10**・**構造物 2**・
  **次元 3**・**`DamageCause` 9 種**（レシピ以下はどれも ±0）
- **`AUTODEV-SPEC.md` 119 行（`状態: 済`）**・**`AUTODEV-QUEUE.md` 109 行**（未着手 6 件）

## この周でやったこと（AUTODEV 56 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**キューの先頭 1 件 = 23（クモの巣・ブロック 154）**を実装し、コミット 1 つで push。

**サブエージェントは 1 つも立てていません。** 実装も C-2 の点検も親が自分でやり、
`git diff` を読んで確かめました（`AUTODEV.md` の「返らないなら自分で実装すること」）。
**差し戻し 0 回。**

**足したもの**: ブロック 154（十字・硬さ 1.2・`sound: "wool"`・色 `0xc8c8dc`）と、
**旗 2 つ**（`sticky` = 中に居ると鈍る / `bladed` = 刃物でだけ落ちる）。
壊すと**糸が 1 個**、中では**横 0.25 倍・上下 1.0 m/s 止まり**、
**絡まっているあいだは落下ダメージが積まれません**。**自然生成もレシピも 0 行**です
（本家にレシピは無く、廃坑・要塞に湧かせるのは別の周）。

**`main.ts` は ±0 行で通りました**（B の周の予告どおり）—— 直したのは
`updateVitals()` に渡す 1 行の**名前だけ**（`onLadder:` → `clinging:`）で、
**「落ちたぶんを積まないのはどれか」の判断は `player.ts` の `clinging` ゲッター 1 か所**。
`vitals.ts` は「はしごか巣か」を知りません（`VitalsContext.onLadder` を
`clinging` に改名しただけで、中身は 1 文字も変えていません）。

**旗を 2 つに割ったのがこの周でいちばん効いた判断です。** 1 本にまとめると、
氷（鈍るだけ）とツタ（刃物だけ）を足す周に必ず片方を巻き添えにします。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので判定表の 2 行目です
（未着手が 6 件あるので A ではありません）。**キューの先頭 1 件 = 24（ケーキ）**を
`AUTODEV-SPEC.md` に書き直すこと。**書く前にその 1 件だけコードで数え直すこと**（B の決まり）。

**24 は `add-stateful-block` スキルを名指しで渡すこと** —— 置いて 7 回かじるので
`furnaces.ts` と同じ「位置ごとの状態」になります。**ID は 155 から**（予約表）。
**`main.ts` に何行要るかは B の周が数えること** —— 位置ごとの状態はセーブに乗るので、
**`currentSave()` にキーが 1 本増えると `main.ts` に 1 行**要ります（27b と同じ話で、
いま 1449 行）。**そこが 1 行でも増えるなら、`AUTODEV.md` の停止条件 2 に当たります。**

## ブラウザで見てほしいところ

**この周は撮りました**（`tools/shot.ts` に **`cobweb` の場面を足しました** ——
本棚・はしごと同じで自然に生えないので、既存の場面には 1 枚も写りません）。
**絵で確かめたのは 3 つ**: 十字の板 2 枚が草むらと同じ形で組まれている /
**宙に浮いた 1 個が面を欠かさず出る**（支えが要らないことの足場）/
**色 `0xc8c8dc` が石の灰色とも草の緑とも見分けが付く**。
**`terrain` は 1 画素も変わっていません**（自然生成 0 行なので当然。変わっていたら
生成に漏れがある合図です）。本物のブラウザ（Chromium 141 / SwiftShader / WebGL 2.0）も
5 枚とも例外なしでした。

**人に見てほしいのは 3 つ（クモの巣）**: **中の歩きが 1/4（1.3 m/s）で「絡まった」と
感じられるか、それとも待たされるだけか** / **素手 6.0 秒・刃物 1.8 秒という比が
「剣を持ち替える理由」になるか**（本家の「素手 20 秒 / 剣 0.4 秒」は**剣の掘る速さを
1 のままにしている以上ここでは作れません**。`TUNING.md`）/ **一覧で `0xc8c8dc` が
雪・ミルク・紙・糸と見分けられるか**（いちばん近いのは鉄インゴットで 27.5）。

**持ち越し（前の周から）**: **金のリンゴの色 `0xf0a800` が一覧で金インゴット・
ブレイズロッド・ブレイズパウダーと見分けられるか** / **金インゴット 8 個 + リンゴ 1 個に
「体力 4 回復」が見合うか** / **27b（革の防具）を実装したら: 防具点 7 で 8 割ぶんしか
減らない（10 → 7.2）のが「着た手ごたえ」として分かるか**。

**持ち越し（まだ誰も見ていません）**: **本棚が「本の詰まった棚」に見えるか**
（側面 `0x9c5064` がかなり赤紫）/ **紙 `0xd4e0ec` が雪玉と 25.5 で見分けられるか** /
**本棚 1 個に材料 9 個が見合うか** / **リンゴの色 `0xe0342c` が赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か** / **地面に落ちた
リンゴが赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか** /
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか** /
**はしごの上端から地上へ出る手触り** / **はしごの色（`0xc9a063` / `0xa8823f`）が
板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に見えるか** /
**`CANE_GROW_SECONDS = 180` が待てる長さか** / **自然に生えたサトウキビは伸びません**
（理由は `TUNING.md`）/ **⚠ 前から遊んでいる世界だけの見え方**: 前に浜の
サトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて見えることがあります /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度 / サボテンに
当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件**。どちらも `rules/` へ据えました）

1. **`rules/items-survival.md`** —— **「剣でだけ落ちるブロック」の書き方。**
   既存の「どのブロックにも `tool: "sword"` を付けないこと」の**裏側**（では
   どう書くのか）が今まで無く、`BlockDef.tool` に書きたくなる罠がそのままでした。
   正解は**旗 1 本（`bladed` / `isBladed()`）+ `items.ts` の `isBlade()` +
   `canHarvest()` の 1 行**で、**`toolSpeed()` には 1 文字も足しません**
   （足すと剣が掘る道具に戻ります）。**`sticky` と 1 本にまとめないこと**も一緒に。
2. **`rules/testing.md`** —— **対照は、比べたいものが 1 つも無い試験場で走らせること。**
   「巣の中では 1 秒で 1.0 m しか落ちない」に自由落下の対照を付けるとき、
   **同じ塔で落とすと 1.5 秒で 31 m 落ちて巣の帯（y=20..40）に突っ込みます** ——
   対照のほうも絡まって、両方とも巣の中を測ることになります。**測る区間ぜんぶで
   どこまで動くかを計算してから的を置くこと**（書く前に気付いて床だけの試験場に移しました）。

## 人がやること

- **設計の判断（29 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**で、
  上限 1500 まで 51 行です。**23 も ±0 行で通りました**（4 周続けて動いていません）。
  **次の 24（ケーキ）は「位置ごとの状態」＝セーブにキーが 1 本増える**ので、
  **そこで 1 行要る見込みです** —— `AUTODEV.md` の停止条件 2 に当たるかどうかは
  B の周が数えて判断します。`test/ui.test.ts` の 40 件の見張りが動かせない事情は
  変わっていません（**判定の意味が変わるので無人の周では触れません**）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 39 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
