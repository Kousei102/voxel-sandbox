# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は B（仕様）の周**でした。書いたのは `AUTODEV-QUEUE.md` の先頭
  **7c-2「卵を投げる側」**の仕様書 1 枚だけで、**`src/**` と `test/**` は 1 行も
  触っていません**（`git show --stat` で確かめられます）
- `npm test` **2904 件すべて成功**（前の周と同じ数。**B の周なので増えません**）。
  `npm run typecheck` も緑。**`npm run build` と `npm run bench` は走らせていません**
  （`src/**` に差分が無いため）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 126**（**この周は 0 個使用**）
- **アイテムは 94 種**、`MAX_ITEM_ID` は **129**。**モブは 7 種類**。`SaveData` は `version` 1
- **`main.ts` は 1449 行のまま**（上限 1500）
- `REVIEW.md` の未着手は 0 件。`RULES-INBOX.md` は層 3（スキル）専用です
- **踏んだ落とし穴（決まりごと）は 0 件**でした（数え直しと文章だけの周のため）

## この周でやったこと — 仕様「卵を投げる」（`AUTODEV-SPEC.md` 119 行）

**取る前にコードで数え直しました**（`AUTODEV.md` の B）。`src/**` の `EGG` は
**`items.ts` の 3 行と `mobs.ts` の 2 行だけ**で、`projectiles.ts` / `use.ts` / `main.ts` には
卵が 1 つもありません。**7c-2 はまだ入っていない**ので、そのまま仕様にしました。

決めた線（**実装の周は勝手に変えないこと**。変えたくなったら仕様書ごと直す）:

- **飛び道具の表に 1 行足すだけ**（`PROJECTILE_KINDS` の 5 行目）。速さ 20・寿命 30・
  重力あり・当たったら消える・**ダメージ 0**（本家の卵も 0 なので、`mobs.ts` は 0 行 ——
  `hitByProjectile()` は `shot.damage <= 0` で戻ります）
- **何を投げると何が飛ぶかは `items.ts` の表 1 本**（`THROWN` / `thrownProjectile()`）。
  `use.ts` にも `main.ts` にも **`held === EGG` を書かせません**（`isBucket` と同じ作法）
- **`use.ts` の並びは「器 → … → アイを投げる → 投げる → 火種」**。器より前に出すと
  **作業台の上で卵を持っているあいだ作業台が開かなくなります**（`rules/use.md` の 2 番目）
- **ID は 0 個**（卵は 129 にもうある。`"egg"` は文字列で番号ではない）。
  **ヒヨコは孵しません**（子モブの仕組みが無いので別件）

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は絵を撮っていません**（`src/**` に差分が無く、画面に出るものが 1 つも
変わっていないため。`AUTODEV.md` の C-3 は実装の周の決まりです）。

持ち越しは 4 つ:

1. **卵（`0xf7f0e0`）と羽根（`0xe8e4dc`）が一覧で隣り合っていて、どちらもほぼ白**
   （`E` を押して末尾へ）。2 つまとめて決め直すのが早いと思います
2. **産卵の間隔 300〜600 秒は本家のままですが、ここでは長すぎるかもしれません**
   （モブは 72m で消え保存もされないので、実際にはほとんど産みません。`TUNING.md`）
3. **矢が 2x2 で作れなくなってよいか**（AUTODEV 17）。戻すなら `crafting.ts` の 1 行
4. **苗の緑が草原に紛れる**件と **`GROW_SECONDS` 180 秒**、**`#status` の位置**

## 人がやること

- **`loop/endgame` にはまだ層 2 の設置の仕組み（`npm run rules:install`）が届いていません。**
  行き渡らせ方（`master` 経由でマージするか、cherry-pick するか）は決めてください
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手が **5 件**（3 件以上）で、`AUTODEV-SPEC.md` が
**`状態: 未着手`** なので → **次は C（実装）の周**です。

- **`AUTODEV-SPEC.md` を全文サブエージェントに渡すこと**（要約すると「触らないファイル」と
  「このタスク固有の禁じ手」が真っ先に落ちます）。**背景（async）で起動しないこと** ——
  同じ番の中で結果が返らないなら**自分で実装する**ほうが安いです
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）。
  **2904 件から増えていること**が完了の判定です
- 仕様が済んだら `AUTODEV-QUEUE.md` の 7c-2 を消し、仕様書を `済` に。
  残りは 8 牛 / 9 クモと糸 / 10 スケルトンと骨 / 11 刈られた羊が見て分かること
- **ID は `ROADMAP.md` の予約表から**（**次は 130。111 以降はブロックとアイテムで
  1 本の番号列**）。**この 7c-2 は 0 個です**

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に 1 件だけ赤 →
走らせ直したら緑。**この周は緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL が動かない」は開発者の手元の devcontainer の話です）。
**クリエイティブの一覧を撮る手**は `docs/browser-shots/README.md` の 12・14・15 枚目。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
