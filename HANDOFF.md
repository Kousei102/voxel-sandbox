# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-22・AUTODEV 116（B の周）のあと）

**この周は B（仕様）の周**でした（判定表の 2 行目 —— `AUTODEV-QUEUE.md` の未着手が
**3 件（3 件未満ではない）**で A は当たらず、`AUTODEV-SPEC.md` が **`状態: 済`** だったため）。
**キューの先頭 43「投げた卵からヒヨコが湧く」を `AUTODEV-SPEC.md` に書き、`master` へ push しました。**
**`src/**` は 1 行も触っていません**（B の周の決まり）。

### ⚠ `main.ts` の判断待ちを、±0 行の設計で回避しました

**43 は「`onHitBlock` に 1〜3 行増える」ので判断待ちに並んでいた 3 件の 1 つ**でしたが、
**実際に数えたら 1 行で足りる**ので、**その 1 行を真上のコメント（7 行 → 6 行）で相殺する
設計**にしました。**`main.ts` は 1450 行のまま ±0** で、**`AUTODEV.md` の停止条件 2
（1450 行を超える・判断を出しても収まらない）には当たりません。**
**`main.ts` を 2 つに割ってよいかの人の判断は、待ったまま持ち越し**です（下の「人がやること」）。

**仕様の骨子**（全文は `AUTODEV-SPEC.md`）:

- **判断は全部 `mobs.ts`**（表 `HATCHES` 1 本 + `HATCH_CHANCE` = 1/8 + `hatch()` 1 本）。
  `main.ts` は `mobs.hatch(shot, world, mobContext())` の 1 行だけ
- **`projectiles.ts` は 1 行も動きません**（`rules/projectiles.md` の 36 行が
  「入れるなら `mobs.ts` の話で、表は 1 行も動かない」と前もって書いてありました）
- **ID は 0 個**（次に取るのは 189 のまま）。**`SaveData` も ±0**
- **本家と違うところが 2 つ**: **湧くのは大人の鶏**（子モブの仕組みが 1 つも無い）と、
  **モブに当てたときは孵らない**（`hitByProjectile()` に `world` を足すと配線とテスト
  5 か所が動く）。**どちらも C の周に `TUNING.md` へ 1 行**

## 数字（2026-09-22・この周で走らせて確かめたもの）

- `npm test` **3897 件すべて成功**（**±0。`src/**` も `test/**` も触っていない周**）/
  `npm run typecheck` 緑 / **`build` と `bench` は走らせていません**（`src/**` に差分が無いため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1450 行 / 上限 1500**（±0）
- **1..63 の空き 7**（±0）・**111..255 の空き 67**（±0。**次に取るのは 189**）
- `CLAUDE.md` 197 / `LOOP.md` 199 / `AUTODEV.md` 198（上限 200）・`TASKS.md` 104 / 150・
  `REVIEW.md` 84 / 200・`AUTODEV-QUEUE.md` **105 行 / 150・未着手 3 件**・
  **`AUTODEV-SPEC.md` 120 行 / 上限 120**（`wc -l` では 119。ちょうど上限です）

## 次にやること

**次は C（実装）の周** —— 判定表を上から当てると、未着手は **3 件**で A は当たらず、
**`AUTODEV-SPEC.md` が `状態: 未着手`** なので C です。

- **`AUTODEV-SPEC.md` の全文をそのまま実装役に渡すこと**（要約しないこと。要約すると
  「触らないファイル」と「このタスク固有の禁じ手」が真っ先に落ちます）。
  **当たる `rules/*.md` も引いて渡すこと**（仕様書の 2 に一覧があります）
- **仕様の肝は「`main.ts` を ±0 行で閉じる」**です。**+1 行で済ませたつもりが
  コメントを足して 3 行増えた、が起きやすい形**なので、**C-2 の点検で `wc -l src/main.ts`
  が 1449 のままか**を必ず見ること（テストの数え方では 1450）
- **見た目は 0 個**（湧くのは今までどおりの鶏）ですが、**`npm run shot -- mobs --size 900x900`
  を 1 枚だけ撮って `Read` で見ること**（1〜2 秒。ブラウザは要りません）

## ブラウザで見てほしいところ

**この周（116・B）は絵を 1 枚も撮っていません**（`src/**` に差分が無いため）。
**下は 115（C）の周に撮って確かめたぶんです**:

**`shots/oreblocks.png` を撮って `Read` で見ました**（新しい場面。
**鉄・金・ダイヤの立方体 135..137 も、これで初めて絵に写りました**）。
**絵で確かめたのは 4 つ**: 石炭ブロックの立方体の形が AO と陰影で読めること /
石炭鉱石(14)・黒曜石(43) と並べて見分けられること / 鉄・金・ダイヤと並べて
「しまった立方体の仲間」に見えること / 面の欠けと裏返りが無いこと。
**下は人にしか決められないところ**です。

- **⚠ 42 石炭ブロック**: **ほぼ真っ黒（上面 `0x100f0f`）が「石炭ブロック」に見えるか**
  （**側面を `0x070707` まで下げて形が読めるようにしましたが、本家は 6 面とも同じ
  テクスチャです** —— 暗すぎないか・のっぺりしないか）/
  **かまどに入れて 800 秒燃えるのが長すぎないか**（**炎ゲージは `burnTotal` が
  分母なので、1 個入れると炎がほとんど減りません**）/
  **石炭 9 個をしまう理由が伝わるか**（10 個ぶん燃えるので 1 個ぶん得）
- **⚠ 41 ネザーレンガのフェンス**: **暗い赤紫 `0x6e3746` が「ネザーレンガ」に見えるか** /
  **木のフェンスと繋がるのは違和感が無いか**（**本家では繋がりません**）/
  **素手で壊せないのが分かるか**（**木のフェンスは素手で壊せます**）
- **⚠ 40 エンダーマンが水で痛む**: **浅い池をうろつく個体が削られていくのが分かるか**
  （毎秒 2・20 秒で死ぬ）/ **跳んで逃げるのは半分だけ** / **水で倒れても真珠は落ちません**
- **⚠ 39 昼のクモ**: **そばを通れるのが安心か拍子抜けか** / **松明（光量 14）のそばでは
  クモだけが襲ってこず、ゾンビは襲ってくる**のが分かりにくくないか
- **⚠ 38 クモが壁を登る**: 2 段・4 段の壁を登ってくるのが怖すぎないか / **登る速さ 3.0 m/s** /
  **囲い（家の壁・柵）がクモには効きません**
- **⚠ 37 サボテン**: **180 秒に 1 段・3 段で止まる**のは待てる長さか /
  **自然に生えたサボテンは伸びません**
- **⚠ 12 刈られた羊**: **地肌の桃色 `0xe3b09c`** / **60 秒で戻るのは待てるか**
- **⚠ 34c ツタ**: **森の木の 25%** は多いか少ないか / **1 列 0〜4 マス**
- **⚠ 33b 金・ダイヤの防具**: **金の上着 `0xefd714` とズボン `0xf8b317` は近い** /
  **ダイヤ一式 80% 減が強すぎないか**（**防具は壊れません**）/ **金を作る理由があるか**
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**57 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**（余裕 0.0117。**187 も同じ当たり判定です**）/
  **⚠ 氷と雪の浜がほぼ同じ白**
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: レンガ（32b）/ 粘土は海底の 7.2%（32a）/ ハーフ 2 材質（31）/
  苗木の 180 秒（30b）/ 木炭（29）/ 修理（28）/ スケルトンの矢（2 秒に 1 発）/
  クモの巣の歩き 1/4 / リンゴ 200 枚に 1 個 / はしご 2.35 m/s / クモの速さ 6.0

## 分かったこと（**この周で据えた決まりごとは 1 件**）

- **`rules/blocks-shapes.md`** の「`def()` の色は `top` → `side` → `bottom`」の節に、
  **「真っ黒に近いブロックは `top` 1 色だけで書くと絵で立方体に見えない」**を 1 段
  据えました（**目安は上面と側面の画素の差 10 以上**・**確かめ方は PNG の画素を
  直に読むこと**・**明るいほうへ寄せると一覧の隔たりを割るので側面を下げること**）

**⚠ もう 1 つ踏みましたが、こちらは据えていません**（型でも出力でも止まる安全な罠のため）:
**`mining.ts` の `breakTime()` は、落ちない道具のとき `HARVEST_FACTOR`(1.5) に 5 を
掛けるのではなく `MISMATCH_FACTOR`(5) へ丸ごと差し替えます。** 素手の秒数を
`5 × 1.5 × 5` = 37.5 と書いて落ちましたが、**テストが「値を出してから判定する」形
だったので出力に `25.000s` と出て、1 回で割れました**（`rules/testing.md` の形が効いた例）。

**⚠ レシピ名の長さは 41 で上限ちょうどのままです** —— 「ネザーレンガのフェンス x6」で
**全角 12.5 文字ぶん / 上限 12.5**（`test/ui.test.ts`）。**42 の「石炭ブロック x1」は
7.5 文字ぶん・「石炭 x9」は 4 文字ぶんなので、どちらも通りました。**

**⚠ 音のテストが赤いのは「揺れ」ではなく退行です**（2026-09-19 に原因ごと消えました）。
9 つの値はビット同一で出るはずなので、**1 つでも動いたら退行**として扱ってください。

## 人がやること

- **⚠ 定期実行のプロンプトに古い行が 2 か所あります**（人がプロンプトを直すまで、
  次の周は `rules/testing.md` と `npm test` の出力のほうを信じること）:
  - **「`test/audio.test.ts` の羊毛は 15 回に 2 回ほど跳ねる」** —— 2026-09-19 に
    原因ごと消えました。**いまは 1 つでも動いたら退行**です
  - **「いま 2934 件」** —— 実際は **3897 件**です
- **⚠ 42 で本家と違えたところが 1 つ**: **石炭ブロックの側面と下面を暗くしました**
  （本家は 6 面とも同じテクスチャ）。**上の「ブラウザで見てほしいところ」の 1 つ目**で、
  **戻すなら絵で立方体に見えなくなるのと引き換え**です
- **⚠ 41 で本家と違えたところが 1 つ**: **本家では木のフェンスと
  ネザーレンガのフェンスは繋がりません**が、**ここでは繋がります。**
  `fenceConnects()` は「フェンスならどれでも」の表 1 本で、材質の分岐を入れると
  **`mesher.ts` とテストの表の 2 か所に材質が漏れます**。**直すならそれだけで 1 周**です
- **⚠ 41 で見送ったものが 1 つ**: **要塞がネザーレンガのフェンスを建てること**
  （`fortress.ts` を触る話。**取るなら 1 周**）
- **⚠ 設計の判断（85 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 3 件で、いまキューの未着手 3 件はこの 3 件だけです**
  （**43 卵のヒヨコ** / **35 骨粉**（`use.ts` の 19 通り目）/ **24b ケーキをかじる**（最低 7 行））。
  **⚠ 43 は「1 行足してコメントを 1 行削る」で ±0 に収めたので、この周は止まりませんでした**
  （上の「いまの状態」）。**同じ手は何度も使えません** —— **残る 35 と 24b は
  どちらも行が増える側**なので、**次の B の周（118 周目）はこの判断に当たります。**
  **下りていなければ、判断の要らないものをキューへ足す A の周を先に回すほうが安全**
  かもしれません（**人の判断**）
- **⚠ 40 で見送ったものが 2 つ**: **溺れ（酸素・息継ぎ）**と **雨で痛むこと（天候が無い）**。
  **⚠ 39 で見送ったもの: 「殴られたら明るくても追い返す」**（敵対モブは反撃の状態を
  1 つも持っていません）。**⚠ 37 で見送ったもの 2 つ**: **「砂の上にしか置けない」**と
  **「横に固いブロックがあると壊れる」**。**どれも取るならそれぞれ 1 周**です
- **⚠ 明かりの意味が種類ごとに変わりました。** 松明を 1 本立てた所では、
  **クモは襲ってこないのにゾンビは襲ってきます**（本家どおり。`TUNING.md`）
- **⚠ クモの「登れる高さに上限が無い」件** / **⚠ クモは水中では登りません**（本家は登ります）
- **⚠ 落ちたアイテムを溶岩で燃やしてよいか**（`drops.ts` の 347 行）
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **この周は ID を 1 つも取っていません**（B の周なので。**次に取るのは 189**）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 116 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

## 絵の画素を直に読む（**115 の周に書いた。そのまま使えます・1 分**）

**暗いブロックの「形が読めるか」は、目で見ると「見えている気がする」ほうへ転びます。**
**PNG を展開して画素を直に読むこと**（スクラッチパッドに入れて `node` で走らせるだけ。
`zlib` と `node:fs` だけなので依存は 0 個です）:

```js
const buf = readFileSync(png);                       // IHDR で幅・高さ、IDAT を集める
const raw = zlib.inflateSync(Buffer.concat(idat));   // 行ごとに filter(0..4) を戻す
// out[y * width * 3 + x * 3] が R。上面と側面の同じ色の面を 1 点ずつ拾って引き算する
```

**目安は上面と側面の差が 10 以上**（**この周の実測: 鉄 29 / 石炭鉱石 11 / 黒曜石 12 /
石炭ブロックは直す前 3・直したあと 11**）。**`FACE_SHADE` は上 1.0 / ±Z 0.86 / ±X 0.74 の
掛け算**なので、**暗いブロックほど差が縮みます**（`rules/blocks-shapes.md`）。

## 色を総当たりで測る（そのまま使えます・3 分）

**入口をスクラッチパッドに書いて `esbuild` で束ねるだけ**です（判定は `top` だけ）:

```ts
import { allItemIds, itemColor, itemName } from "/home/user/voxel-sandbox/src/items";
const dist = (a: number, b: number): number =>
  Math.hypot(((a >> 16) & 255) - ((b >> 16) & 255), ((a >> 8) & 255) - ((b >> 8) & 255), (a & 255) - (b & 255));
for (const c of [0x100f0f]) {
  const near = allItemIds().map((o) => ({ o, g: dist(c, itemColor(o)) })).sort((a, b) => a.g - b.g);
  console.log(near.slice(0, 5).map((x) => `${itemName(x.o)}(${x.o}) ${x.g.toFixed(1)}`).join(" / "));
}
```

```bash
npx esbuild <その .ts> --bundle --platform=node --format=esm --outfile=node_modules/.cache/color.mjs
node node_modules/.cache/color.mjs
```

**帯ごと総当たりするなら 3 重ループで 2 刻み**。**いちばん遠い色を選ばないこと** ——
上位は真っ黒（`0x000000`・隔たり 67.1）です。**「材質らしく見える範囲でいちばん遠いもの」を
選ぶこと。** **42 では本家の写し（`0x100f0f`）がそのまま 40.9 で通りました** ——
**本家の値でも通ることがあるので、ずらす前に必ず素直な値を測ること。**
**⚠ ただし「一覧で見分けられる色」と「絵で立方体に見える色」は別の話です**
（上の「絵の画素を直に読む」。**一覧は `top` だけ・絵は 6 面ぜんぶ**）。

## 音のテストだけを束ねて回す（そのまま使えます）

**30〜50 回連続で回すなら全体（1 回 12 秒）ではなくこちらを**（1 回 0.30 秒）:

```ts
import { run as audio } from "/home/user/voxel-sandbox/test/audio.test";
import { summary } from "/home/user/voxel-sandbox/test/harness";
await audio(); summary();
```

```bash
npx esbuild <その .ts> --bundle --platform=node --format=esm \
  --external:node-web-audio-api --outfile=node_modules/.cache/voxel-audioonly.mjs
node node_modules/.cache/voxel-audioonly.mjs      # 「12 件すべて成功」
```

**⚠ 出力先が `node_modules/.cache/` でないと `node-web-audio-api` を見つけられず、
まるごと `skip` になります**（緑に見えて 1 件も確かめていない形になります）。

## 絵を撮るときの注意（前の周までに実測で分かっているもの）

**⚠ 前の周と絵が変わっていないことを確かめるなら `md5sum` で足ります**
（**`shots/` は `.gitignore` に入っているので、大きさは自由に変えてよい**）。
**md5 がこの引き継ぎに残っていれば `git worktree` は要りません**（**`git stash` は
使わないこと**。`rules/testing.md` の点検役の事故）。

**⚠ 既にある場面へ 2 材質目を足すなら、カメラ寄り（`z` が大きい側）の空いた所へ**
（2026-09-22 に実測。`rules/meshing-render.md`）。**カメラのほうを動かさないこと**
（前の周が詰めた通りが画面から外れます）。**新しい場面を足すときは自由に決めてよい** ——
**42 の `oreblocks` は `(2.4, y+3.4, 8.2)` から `(-0.6, y+0.4, 2)` を見下ろして、
4 つ並べた列と黒どうしの列が 1 枚に入りました**（**既定の 640x400 でも読めます**）。

**⚠ `npm run shot -- mobs` は既定の 640x400 では読めません** —— **`--size 900x900`**
（正方形）にすると水平画角が 131° → 70° に狭まり、中ほどの数体が読める大きさになります。
**⚠ その画の手前（下・中央）に毎回写る暗い穴は地形です**（モブ 0 体でも同じ絵が出ます）。

**⚠ `npm run shot` のカメラを目の高さ（`y + 1.7`）まで下げて 4 マスまで寄らないこと** ——
**画の下半分の地面が 1 枚も描かれず空色に抜けます**。**遠すぎても失敗します**
（8.5 マス引くと画の下半分が台の外の草で埋まります）。**撮り直しは 1 秒なので、
まず撮って `Read` で見ること。**

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（`sheep` / `cactus` / **`oreblocks`**
がその形）。**鉄・金・ダイヤの立方体（135..137）は、42 の `oreblocks` でようやく
絵に写りました**（それまでどの場面にも 1 個も写っていませんでした）。
**時間が経たないと見えないもの**（育つ・燃える・溶ける・刈られる）は、**その場で
`update()` を回すか、その操作を呼んでから撮ること**。**手で積んで撮らないこと。**
**⚠ 防具を着た姿は撮れません**（一人称視点）。**⚠ 動き（登る・跳ぶ・追う・水で痛んで
跳ぶ）も撮れません** —— あれは人に遊んでもらうぶんです。
