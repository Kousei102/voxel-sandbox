# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-20・AUTODEV 105（C の周）のあと）

**この周は C（実装）の周**でした（判定表の 3 行目 —— `AUTODEV-SPEC.md` が
`状態: 未着手` で、キューの未着手が 9 件あったため）。**取ったのはキューの先頭 37
「サボテンが育つ」1 件だけで、ID は 0 個・`main.ts` は 1 行も触っていません。**

- **`src/blocks.ts`**: `CACTUS` の def に **`stacksOnSelf: true` の 1 行** /
  **`CACTUS_HEIGHT_MAX = 3`** を `CANE_HEIGHT_MAX` の隣に export
- **`src/treeshape.ts`**: `grownTreeHeight()` の `cactus` の**リテラル 3 を
  `CACTUS_HEIGHT_MAX` に差し替え**（範囲 1..3 は 1 文字も変えていません）
- **`src/crops.ts`**: `CACTUS_GROW_SECONDS = 180` / `notePlaced()` と `update()` に
  1 分岐ずつ / **`growCane()` を `growStack(..., self, maxHeight, seconds, ...)` に
  一般化**（**サトウキビとサボテンが同じ 1 本を通ります** —— 写して 2 本にしていません）
- **テスト 8 件**（`test/crops.test.ts` 6 件・`test/blocks.test.ts` の新しい節
  「サボテンを積む」2 件）/ **`tools/shot.ts` に場面 `cactus` を 1 つ**

### ⚠ この周の要（次に「積み上がるもの」を足す周が必ず踏みます）

**`crops.ts` だけ直しても本物の世界では 1 段も伸びません。** `World.setVoxel()` は
`canPlaceAt()` を通すので、`blocks.ts` に `stacksOnSelf` が無いと**書き込みが黙って
落ち続けます**。しかも **`test/crops.test.ts` の偽の `Field` は `canPlaceAt` を
持たないので、テストだけが緑**になります —— だから**本物の `World` で 3 段積んで
下を壊す件**が `test/blocks.test.ts` に要ります。**`rules/stateful-blocks.md` に据えました。**

## 数字（2026-09-20・この周で走らせて確かめたもの）

- `npm test` **3846 件すべて成功**（この周で 4 回走らせて 4 回とも緑。**3838 → +8**）。
  内訳は**「育つ苗（crops.ts）」94 → 100 件**・**新しい節「サボテンを積む」2 件**
- **音は 12 件で、9 つの値はビット同一のまま**（羊毛 0.552 / 雪 0.716 / 砂 2.235 ほか）
- `npm run typecheck` 緑 / **`npm run build` 緑**（`src/**` を触ったので走らせました）/
  **`npm run bench` は走らせていません**（生成もメッシュ化も触っていないため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）・
  **`main.ts` 1450 行 / 上限 1500**（**±0。1 行も触っていません**）
- **1..63 の空き 7**（±0）・**111..255 の空き 69**（**±0。次に取るのは 187**）
- `CLAUDE.md` 197 / `LOOP.md` 199 / `AUTODEV.md` 198（上限 200）・`TASKS.md` 104 / 150・
  `REVIEW.md` 84 / 200
- `AUTODEV-QUEUE.md` **108 行 / 上限 150・未着手 9 件**・
  `AUTODEV-SPEC.md` **118 行 / 上限 120・`状態: 済`**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` を **`状態: 済`** にしたので判定表の
2 行目です（キューの未着手は 9 件あるので A には落ちません）。

- 書くのは**キューの先頭 38「クモが壁を登る」**（**ID 0 個・`main.ts` 0 行の見込み**・
  判断は `mobs.ts` だけ）。**取る前にその 1 件だけコードで数え直すこと**（B の決まり）
- **当たる `rules/` を自分で引くこと**: `grep -l '"src/mobs.ts"' rules/*.md` と、
  **書いているファイルそのもの**（`grep -l '"AUTODEV-SPEC.md"' rules/*.md` →
  `rules/testing.md`。**`wc -l` を 119 以下に収めること** —— 上限は `split("\n")` で
  数えるので 1 多く出ます。**これで 4 回踏んでいます**）
- **B の周は `src/**` も `test/**` も触りません。**

## ブラウザで見てほしいところ

**この周で撮ったもの**（撮って `Read` で開いて見ています）:
`shots/cactus.png`（`npm run shot -- cactus`）と `docs/browser-shots/` の 5 枚
（`node tools/browsershot.mjs`。**本物の Chromium で WebGL 2.0・ポインタロックとも変わりなし**）。

- **⚠ サボテンの伸び方（37）**: **置いたサボテンが 180 秒に 1 段・3 段で止まる**のは
  待てる長さか / **自然に生えたサボテンは伸びません**（サトウキビと同じ線。`TUNING.md`）/
  **⚠ 伸びた先がプレイヤーの居るマスでも特別扱いはありません**（物理が押し出し、
  `spiky` が毎フレーム刺さります）。**絵では 3 段・継ぎ目の空き無し・砂岩より細い**
  ことまで確かめました —— **残るのは手ざわりだけです**
- **⚠ 刈られた羊の手ざわり（12）**: **地肌の桃色 `0xe3b09c` が「刈られた羊」に見えるか** /
  **細さ（体が 0.625 → 0.438 幅）が足りているか** / **60 秒で戻るのは待てる長さか**
- **⚠ ツタの生え方（34c）**: **森の木の 25%** は多いか少ないか / **1 列 0〜4 マス**の
  垂れ方は「ツタ」に見えるか / **苗木から育てた木には掛かりません** /
  **針葉樹林には 1 本も生えません**（`BiomeDef.vine`）/ **登る速さははしごと同じ・硬さ 0.2**
- **⚠ 金・ダイヤの防具の色（33b）**: **金の上着 `0xefd714` とズボン `0xf8b317` は絵の上で
  やや近い** / **⚠ ダイヤ一式 80% 減が強すぎないか**（**防具は壊れません**）/
  **⚠ 金を作る理由があるか** / **⚠ 鉄の防具の「鋼の青」が鉄に見えるか**（33a・鉄一式 60% 減）
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**（余裕 0.0117）/ **⚠ 氷と雪の浜がほぼ同じ白**
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: レンガ（32b）/ 粘土は海底の 7.2%（32a）/ ハーフ 2 材質（31）/
  苗木の 180 秒（30b）/ 木炭（29）/ 修理（28）/ スケルトンの矢（2 秒に 1 発）/
  クモの巣の歩き 1/4 / リンゴ 200 枚に 1 個 / はしご 2.35 m/s /
  **自然に生えたサトウキビは伸びません** / クモの速さ 6.0 / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 2 件**）

- **`rules/stateful-blocks.md`** —— 「積み上がるものを足す周は `blocks.ts` の
  `stacksOnSelf` も要る（無いと `canPlaceAt()` に黙って落とされる）／偽の `Field` は
  それを見逃すので本物の `World` の件が要る／上限の段数は `blocks.ts`・秒数と条件は
  `crops.ts`／2 本目を写さず `growStack()` に一般化する」の 4 つ
- **`rules/testing.md`** —— 「**そのとき何回だったかを数える値は、その場で控えてから
  次のフレームを回すこと**」。**この周で自分が 1 度赤を出しました** ——
  `field.writes` を「石をどけたあと」に読んだので、**塞がっている間の 0 回に
  次のフレームの 1 回が乗って**落ちました（実装は正しい。判定はゆるめていません）

**⚠ 音のテストが赤いのは「揺れ」ではなく退行です**（2026-09-19 に原因ごと消えました）。
9 つの値はビット同一で出るはずなので、**1 つでも動いたら退行**として扱ってください。

## 人がやること

- **⚠ 定期実行のプロンプトに古い行が 2 か所あります**（人がプロンプトを直すまで、
  次の周は `rules/testing.md` と `npm test` の出力のほうを信じること）:
  - **「`test/audio.test.ts` の羊毛は 15 回に 2 回ほど跳ねる。赤が出たらまずもう一度
    走らせること」** —— 2026-09-19 の C の周で原因ごと消えました
  - **「いま 2934 件」** —— 実際は **3846 件**です
- **⚠ 設計の判断（75 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 3 件**（**43 卵のヒヨコ**（`onHitBlock` に 1〜3 行）/ **35 骨粉**
  （`use.ts` の 19 通り目）/ **24b ケーキをかじる**（最低 7 行））。
  **キューの 38..42 の 5 件は 0〜1 行の見込み**なので、**判断が下りなくても 5 周は進みます**
- **⚠ サボテンの本家の性質を 2 つ見送りました**（37 の仕様の外。
  理由は `docs/autodev-log.md`）: **「砂の上にしか置けない」**（`needsSoil` の形が要り、
  既存の置ける場所が狭まる）と**「横に固いブロックがあると壊れる」**（置くたびに
  隣 4 マスを見る新しい規則）。**取るならそれぞれ 1 周**にしてください
- **⚠ 落ちたアイテムを溶岩で燃やしてよいか**（`drops.ts` の 347 行の
  「液体にはどれでも浮かせる」は人が選んだ判断です）
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **⚠ 刈られた羊は倒しても羊毛が出ません**（`dropFor()`。**本家も同じ**です）
- **⚠ `ROADMAP.md` の 21 行目「サボテンに触れたときのダメージ」はもう済んでいます**
  （AUTODEV 32）。**この周は ID を 1 つも取っていないので予約表は 1 行も触りませんでした**
  —— 直すなら人の手で（`AUTODEV-SPEC.md` が「予約表を変えないこと」と書いていました）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 105 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

## 音のテストだけを束ねて回す（そのまま使えます）

**30〜50 回連続で回すなら全体（1 回 12 秒）ではなくこちらを**（1 回 0.30 秒）。
入口を**スクラッチパッドに**書いて `esbuild` で束ねるだけです:

```ts
import { run as audio } from "/home/user/voxel-sandbox/test/audio.test";
import { summary } from "/home/user/voxel-sandbox/test/harness";
await audio(); summary();
```

```bash
npx esbuild <その .ts> --bundle --platform=node --format=esm \
  --external:node-web-audio-api --outfile=node_modules/.cache/voxel-audioonly.mjs
node node_modules/.cache/voxel-audioonly.mjs      # 「12 件すべて成功」
```

**⚠ 出力先が `node_modules/.cache/` でないと `node-web-audio-api` を見つけられず、
まるごと `skip` になります**（緑に見えて 1 件も確かめていない形になります）。

## 絵を撮るときの注意（実測で分かっているもの）

**⚠ `npm run shot` のカメラを目の高さ（`y + 1.7`）まで下げて 4 マスまで寄らないこと** ——
**画の下半分の地面が 1 枚も描かれず空色に抜けます**。谷の暗がりに見えるので地形のせいだと
思い込み、**`setVoxel` を 1365 件たたいて足場を埋め、`primeAround()` を 3 回流しました**が、
**三角形の数が 1 枚も増えませんでした**。**原因はカメラが低くて近いことだけで、
`y + 2.2` まで上げて 6.5 マス引いたら一発で消えました。**
**モブを撮る場面は `mobs` の構え方を写すこと。**

**⚠ 遠すぎても失敗します**（2026-09-20・`cactus`）。8.5 マス引いて `y + 3.4` から撮ったら
**画の下半分が台の外の草で埋まり**、見たかった継ぎ目と 1/16 の痩せ方が数画素に潰れました。
**6.8 マス・`y + 2.9` に寄せ直して一発**。**撮り直しは 1 秒なので、まず撮って `Read` で見ること。**

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと**。
**⚠ 枠を数えるときは `#creative .slot` を使うこと**（素の `.slot` はホットバーと
インベントリの 36 枠まで拾います）。**⚠ `/opt/pw-browsers` には
`chromium_headless_shell-1194` も居ます** —— `startsWith("chromium-")` で弾くこと
（`tools/browsershot.mjs` の `findChrome()`）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**`sheep` と `cactus` がその形です。**
**時間が経たないと見えないもの**（育つ・燃える・溶ける・**刈られる**）は、
**その場で `update()` を回すか、その操作を呼んでから撮ること**
（`sheep` は `mobs.shear()`、**`cactus` と `grown` は `crops.update()` を通しています**）。
**手で積んで／手で並べて撮らないこと** —— **育つ道が本当に書けるかを 1 つも確かめないまま
緑の絵になります**（`cactus` は `stacksOnSelf` を足し忘れていれば 1 段のまま写ります）。
**⚠ 防具を着た姿は撮れません**（一人称視点なので自分の体が写りません）。
