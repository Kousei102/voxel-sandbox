# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-09・AUTODEV 64 のあと）

- `npm test` **3356 件すべて成功**（**+24**）・`npm run typecheck` 緑・`npm run build` 緑
- **音の一群が 1 度だけ跳ねました**（「cutoff が低い材質ほど…」）。**次の 1 回で緑**に
  戻り、そのあと 3 回続けて緑です（`rules/testing.md` のとおり同じ件が続けて落ちたときだけ退行）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていないので）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（1 バイトも増えていません）
- **`main.ts` 1450 行（±0。この周は 1 度も開いていません）**・**`mesher.ts` も ±0**
- **アイテム 119 種・`MAX_ITEM_ID` 157・111..255 の空きは 98・1..63 の空きは 9**
- **立方体 41（±0）/ 非立方体 79（+1）**・**モブ 9 種類**・**レシピ 61 本（+1）**・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 123 行**（未着手 **5 件**）

## この周でやったこと（AUTODEV 64 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**26a（フェンス・ブロック兼アイテム 157）を実装しました。**

**サブエージェントは立てず、自分で実装して自分で `git diff` を読みました**
（`AUTODEV.md` の C-1 / C-2 の「同じ番の中で返らないなら自分でやる」の側）。**差し戻し 0 回。**

- **`BlockDef.collision`（当たり判定だけの形）を必須の項目に。** `def()` の既定は
  `boxes` そのものなので、**書かないブロックは今までどおり 3 つの用途が同じ形**です。
  **`collisionBoxes()` だけが `collision` を引き**、`shapeBoxes()` / `shapeBounds()` は `boxes` のまま
- **`isTallCollision()`** は `collision` の最大 y > 1 から表 1 本を立てました（手で旗は書いていません）
- **`collides()` の走査を 1 段下へ**（**その 1 層だけ `isTallCollision()` が偽のマスを飛ばす**）。
  **押し戻しの式・EPS・評価順は 1 文字も動かしていません** —— 軌跡もチェックサムもそのまま通りました

## ⚠ この周に分かった、仕様書の見積もりが外れた 1 件

**跳躍の余裕は 0.09 ではなく 0.0117 で、およそ 52fps を割るとフェンスを跳び越せます。**

B の周の 1.41 m は**連続の式**のものでした。実装は違い、`player.ts` は**跳ぶフレームだけ
重力のあとに `velocity.y = JUMP_SPEED` を入れる**ので、そのフレームは `9.2 × dt` を
まるごと進みます。**到達は `9.2²/(2×30) + 9.2×dt/2`**、実測は
**60fps 1.4883 / 30fps 1.5667 / 刻みの上限 0.05 で 1.6450**（`main.ts` の刻みは 0.05 で頭打ち）。

**1.5 のままにしました** —— 仕様書が `[[0,0,0,1,1.5,1]]` を名指ししていて本家の値でもあり、
直すなら**跳躍の積分**か**1.5 の側**で、**どちらも手ざわりの判断なので人が決める話**だからです。
**代わりに 3 つの刻みの値を毎回出すテストを 2 件足しました**（`test/physics.test.ts`）——
式と実測が 0.01 以内で一致することと、「60fps では越えられないが 30fps では越える」を
数値で固定してあります。**ゆるめて黙らせないこと。**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので判定表の 2 行目です。
**`AUTODEV-QUEUE.md` の先頭は 26b（フェンスが隣と繋がって見える）** で、`mesher.ts` だけの周です。

- **取る前に、その 1 件だけコードで数え直すこと**（`AUTODEV.md` の B）。**ID は 0 個**の見込み
- **26b は `mesher.ts` の `buildProps()` だけ**。`collision` にも `physics.ts` にも触りません
- **未着手 5 件。3 件を切ったら次は A の周**です

## ブラウザで見てほしいところ

**この周は撮って見ました**（`npm run shot -- fence` の 1 枚と `browsershot` の 5 枚）。
**フェンスは自然生成しないので `browsershot` には 1 マスも写りません** ——
`tools/shot.ts` に `fence` の場面を足したのが唯一の経路です（直線 5 本 / 角 / 1 本だけ /
石の上 / 比べる板ハーフ）。**絵で確かめたのは 4 つ**: 腕が 4 方向とも出ている /
柱の上端がマスの 1.0 で止まっている（当たり判定の 1.5 は絵に出ていない）/
面の欠けも裏返りも無い / 板ハーフと色で見分けが付く。**`browsershot` の 5 枚では、
`def()` に項目を 1 つ足した影響が地形に出ていないことを見ました。**

**それでも人に見てほしいところ**:

- **⚠ 上の「52fps を割ると跳び越せる」をどうするか。** 数値では決められません ——
  **跳躍の積分を直す**（全ブロックの段差の登り方が変わり、`test/physics.test.ts` の
  軌跡を引き直す周になります）か、**1.5 を上げる**（本家の値から離れます）か
- **柱 + 腕 4 方向を常に描く形が、1 本だけ置いたときに変に見えないか**（26b で直せます）
- **当たり判定がマスいっぱいなので、柱の脇の「見えない壁」に気付くか**
- **色 `0x988a5e` が「木の柵」に見えるか**（板 `0xb18a56` からは 26.2 離れています）
- **棒 6 本で 2 個が、牧場 1 区画（数十本）に見合う値段か**（本家の古い値です）

**持ち越し（凍った海 25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の
境目が読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
いちばん近いガラスから 34.3 しか余裕がありません）。**気になるなら人が色を決めてください** /
**氷の板の上を歩けることが見て分かるか** / **氷ごしに海の底が見えるか**（`alpha` 0.6）。

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
（理由は `TUNING.md`）。**凍った海が入ったので実地で試せます。**

**持ち越し（ケーキ）**: 上面 `0xffd0e4` が「ケーキ」に見えるか / 側面 `0xe8c9a0` と
下面 `0xd9b98a` の 3 色がケーキに読めるか。**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: 中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか /
素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / 森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ /
牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
**一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件**）

**どちらも `rules/blocks-shapes.md` に据えました**（`Edit` で普通に直しています）:

1. **「形は 3 つの用途を兼ねる」の唯一の例外 `BlockDef.collision`** —— 既定を `boxes`
   そのものにすること・`?:` にしないこと・`boxes` のほうを高くして済ませないこと・
   **1 マスより高い箱は `collides()` が 1 段下も見ないと拾えない**（無いと上に立って
   ガタつく）・**その 1 層だけ `isTallCollision()` で弾くこと**
2. **跳んで越えられるかは 1 フレームの刻み次第**（上の ⚠。式と実測を書いてあります）

**もう 1 つ、書いてあったのに 2 度踏んだ罠**（`rules/` は直していません。氷の節に既に
同じ注意書きがあります）: **`minTier` を書かないブロックは素手でも「適正」なので、
採掘は「硬さ × 5」ではなく「硬さ × 1.5」です**（フェンスは素手 3.0 秒）。

## 人がやること

- **⚠ 新: フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **設計の判断（37 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1450 行**で、
  上限 1500 まで 50 行です。**26b には `main.ts` は要りません**。待っているのは
  **24b（ケーキをかじる。最低 7 行）** と **27b（革の防具。セーブのキーで 1 行）** の 2 件です。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
  （**判定の意味が変わるので無人の周では触れません**）
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 47 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
**自然生成しないブロックは browsershot には 1 マスも写りません** ——
氷・ケーキ・クモの巣・本棚・**フェンス**は `npm run shot -- <名前>` が唯一の経路です。
**`tools/shot.ts` に場面が無いものを近くから見たいときは、scratchpad で
`tools/raster.ts` を直に叩けます**（AUTODEV 62 で実際にやりました。差分は残りません）。

**`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
**`wc -l` を 119 以下に収めること**（`rules/testing.md`）。
