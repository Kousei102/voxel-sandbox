# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-16・AUTODEV 90（B の周）のあと）

**この周は B（仕様）の周**で、**キューの先頭 33b「金・ダイヤの防具 8 部位」の仕様書を
書きました**（判定表の 2 行目 —— `AUTODEV-SPEC.md` が `状態: 済` で、未着手が 5 件あったため）。
**`src/**` は 1 文字も触っていません。**

- **`AUTODEV-SPEC.md` を丸ごと書き直しました**（**120 行ちょうど = 上限**・7 項目そろい・
  `状態: 未着手` / `差し戻し: 0 回`）。**この周で差分があるのは
  `AUTODEV-SPEC.md` と `rules/testing.md` と `HANDOFF.md` の 3 本だけ**です
- **ID は 8 個・175..182**（金 175..178 / ダイヤ 179..182）。**`ROADMAP.md` の予約表で
  次の空きが 175 であることを確かめました**（取ると **111..255 の空きが 81 → 73**。
  停止条件の 10 個にはまだ遠い）。**予約表への書き込みは C の周の仕事**なので、
  この周は 1 行も足していません
- **⚠ `ARMOR_CAP`(20) の件は「触らない」で畳みました。** **ダイヤ一式 20 点が上限に
  ちょうど当たるのは本家がそういう設計**で（20 点 = 8 割減が上限そのもの）、
  「早く頭打ちになった」のではなく**上限に届く一式が初めて入る**だけです。
  `test/vitals.test.ts` の「防具点 20 で 2（8 割減で頭打ち）」は**もう緑**なので、
  C の周が足すのは「**その 20 点がダイヤ一式で届く**」1 件だけになります
- **色は 8 つとも測り直して、そのまま通りました**（一覧 136 色に対して）。
  金 **26.3 / 38.7 / 26.7 / 32.7**・ダイヤ **28.4 / 34.9 / 27.4 / 39.6**（判定は 20）・
  **8 つ互いは最小 31.2**・**既存の防具 8 部位とは最小 47.1**。**ずらさずに済みました**
- **⚠ 仕様書が 1 度 143 行になり、120 行まで削りました**（下の「分かったこと」）。
  **7 項目も ⚠ も禁じ手も 1 つも落としていません** —— 削ったのは繰り返しと 33a の由来です
- **サブエージェントは立てていません**（B の周なので C-1 / C-2 がそもそも要りません）

## 数字（2026-09-16・この周で測ったもの）

- `npm test` **3656 件すべて成功**（±0。**`src/**` も `test/**` も 1 文字も触っていません**）。
  `npm run typecheck` 緑。**`npm run build` と `npm run bench` は走らせていません**
  （`src/**` に差分が無いため）
  - **⚠ 音が 1 回だけ跳ねました**（**6 回走らせて 1 回**）。落ちたのは足音ではなく
    **「声色（音程の倍率）が波形に出る」で、低い声が 0.162 → 4.161** と高い声 0.329 を
    追い抜いた形です。**`rules/testing.md` に載っている既知の揺れそのもの**（前に
    測られた 4.159 とほぼ同じ値）で、**走り直して 3 回続けて緑。判定はゆるめていません**
  - **⚠ 1 度だけ赤くしました** —— `AUTODEV-SPEC.md` が 124 行で
    `test/docs.test.ts` に落とされたものです。**削って緑に戻しました。判定はゆるめていません**
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1450 行 / 上限 1500**（**この周は 1 文字も開いていません**）
- **アイテム 136 種**・**`MAX_ITEM_ID` 174**・**レシピ 74 本**（どれも ±0）
- **1..63 の空きは 7**（57..63）・**111..255 の空きは 81**（**次に取るのは 175**）
- **`AUTODEV-SPEC.md` は `状態: 未着手`・120 行**（`wc -l` では 119）・
  **`AUTODEV-QUEUE.md` 未着手 5 件・117 行**

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。
**`AUTODEV-SPEC.md` を全文読んで、そのとおりに実装すること**（要約して渡さない）。要点だけ:

- **`src/items.ts` に ID 8 つ ＋ `item()` 8 行 ＋ `ARMORS` 8 行 ＋ `MAX_ITEM_ID`**、
  **`src/crafting.ts` は `armorRecipes()` の呼び出し 2 行だけ**（33a で関数に畳み済み。
  **関数の中身は 1 行も変えないこと**）。**`main.ts` は 0 行の見込み**
- **テストは「材質の列を 2 つ足す」だけ** —— `armors.length === 8` → **16**・
  `bySlot` の `n === 2` → **4**・`MAX_ITEM_ID === IRON_BOOTS` → **`DIAMOND_BOOTS`**
  （**`blocks.test.ts` と `items.test.ts` の 2 か所。古いまま残すと `tsc` が TS2367**）・
  共有帯 **56 → 64 個**・空き **81 → 73**・レシピ **74 → 82 本**
- **⚠ ダイヤは `SMELTING` にありません。** 一式の道は**鉄・金とは違う形**で、
  **ダイヤ鉱石を掘ると `DROPS` でダイヤ 1 個**です（`smeltResultOf(DIAMOND_ORE)` は null）
- **色は取るときにもう一度測ること**（この周の値がそのまま通る見込みですが、
  アイテムが増えるたびに隔たりは縮みます）
- **防具に耐久値を足さないこと**・**金の道具を足さないこと**（どちらも別件）

## ブラウザで見てほしいところ

**この周は撮っていません**（B の周で `src/**` に差分が 0 だからです。`AUTODEV.md` の C-3 は
「見た目に出るものを触った周だけ」）。**33b を実装する C の周は 1 枚撮ること** ——
**クリエイティブ一覧の末尾に 8 枠増えます**（175 以降なので末尾に出ます）。

**人に見てほしいのはここです**（上が新しい分）:

- **⚠ 鉄の防具の色（33a・入っています）**: **「鋼の青」が鉄に見えるか。**
  **素直な銀灰は測って通らないと分かっています**（一覧の無彩色は既存 46 個で埋まって
  いて、いちばん遠くて 20.1〜22.1。判定は 20）。**帽子 `0xcfcfff` はとくに淡い青紫寄り**です
- **⚠ 鉄一式で 60% 減が「固くなった」と感じられるか。** **防具は壊れない**ので、
  本家より気前がよい状態です。**鉄一式 24 枚**が渋すぎ／安すぎないかも一緒に
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **5 文字から折れます。** **一覧全体でもう 51 枠が折れていて**、最長は 9 文字の
  「ネザーレンガハーフ」で 39px = 3 行です。**33b で 5 枠増える見込み**
  （金のズボン・ダイヤの 4 部位。金の帽子・上着・靴は 4 文字以下）
- **レンガ（32b）**: **`0xbc5d39` が「焼いたレンガ」に見えるか**と、**一覧で
  レンガブロック（`0xa4553f`）と並んだときに 2 つあると分かるか**（隔たり 26.0）
- **粘土（32a）**: **海に潜って粘土を見つけられるか**（海底の列の 7.2%・4x4 の塊）と、
  **水越しに砂と見分けられるか**。**粘土玉がやや青紫寄り**（`0xa7b4da`）
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）/ **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）/
  **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 1 件**）

- **`rules/testing.md`**: **仕様書が 120 行を越えたときに割るかどうかは、中身の大きさで
  決めること。** あの上限は「1 周で閉じない」の信号であると同時に**書きすぎの信号でもあります** ——
  **前の周が関数と数え方を畳んだ「続きの周」は、言葉を削れば収まります**（33b は 143 → 120 行。
  `crafting.ts` は呼び出し 2 行・テストは表に列を 2 つ）。**削ってよいのは繰り返しと前の周の
  由来だけで、7 項目・⚠・禁じ手を 1 つでも落として縮めたなら、それは割る周です。**
  あわせて **4 回目の「`AUTODEV-SPEC.md` で `rules/` を引き忘れた」** も 2 行足しました。

**この周で踏んだ罠**（どちらも既に `rules/testing.md` にある件を踏み直しただけです）:

- **`test/docs.test.ts` は `wc -l` より 1 多く数えます**（`split("\n").length`）。
  **119 行（`wc -l`）＝ 120 行（テスト）が上限ちょうど**です
- **触るファイルで `rules/` を引くとき、`src` の側だけを見ました。** B の周が書くのは
  `AUTODEV-SPEC.md` そのもので、**`grep -l '"AUTODEV-SPEC.md"' rules/*.md` を先に
  引いていれば、143 行を書く前に上限を知っていました**

## 人がやること

- **⚠ 設計の判断（61 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**・
  最低 7 行）。**キューの上 2 件（33b・34 ツタ）はどちらも `main.ts` 0 行の見込み**なので、
  判断が下りなくても 2 周は止まりません
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も壊れません**（足すなら
  `vitals.ts` → `inventory.ts` の配線になるので**それだけで 1 周**）。**33b が入ると
  「ダイヤ一式 80% 減が永久に持つ」になるので、周を追うごとに効いてきます**
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、**この周の仕様書が
  「本家どおりなので `ARMOR_CAP` を触らない」で畳みました。** 頭打ちの手ざわりが
  嫌なら人の判断です（`vitals.ts` の `ARMOR_CAP` / `ARMOR_DENOM`）
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままにしてあります**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**もう 51 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は 6 回中 1 回赤**
  （足音ではなく**声色**のほう。`rules/testing.md` の既知の揺れ）。
  **直すなら測り方**（窓を長く取る・複数回鳴らして中央値）で、**判定はゆるめないこと**。
  **キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 90 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押してから撮ること**
（`docs/browser-shots/README.md` の末尾）。**⚠ 送り先は末尾とは限りません** ——
**低帯（1..63）の番号を取ったものは一覧の真ん中に出る**ので、`scrollTop = scrollHeight`
では 1 枠も写りません。**`.label` の文字列で枠を探して
`scrollIntoView({ block: "center" })` を呼ぶこと。**
**⚠ 枠を数えるときは `#creative .slot` を使うこと** —— 素の `.slot` は
**ホットバーとインベントリの 36 枠まで拾って 252 個**になり、名前の重複も嘘の数が出ます。
**33b の 8 部位は 175 以降なので末尾に出ます。**

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**海底の粘土（32a）も場面 `clay` を足しました** —— ただしあれは**原点のまわりを
撮りません**（種 4242 は ±128 の海が全部凍っています。`rules/meshing-render.md`）。
**33a も 33b も新しいブロックが 0 個なので、`npm run shot` は要りません**（防具はアイテムです）。
**⚠ 防具を着た姿は撮れません** —— **一人称視点なので自分の体が写らない**ためで、
装備したときの手ざわりは人に見てもらうしかありません。
