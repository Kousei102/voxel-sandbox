# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-08・AUTODEV 57 のあと）

- `npm test` **3273 件すべて成功**（**±0**。この周は `src/**` を 1 行も触っていません）・
  `npm run typecheck` 緑
- **`npm run build` も `npm run bench` も走らせていません**（B の周なので `src/**` に差分なし）
- **音の一群は跳ねませんでした**（2 回走らせて 2 回とも緑）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行（±0）**（`wc -l`。テストの数え方では 1450。上限 1500・止まる目安 1450）
- **アイテム 116 種・`MAX_ITEM_ID` 154・111..255 の空きは 101・1..63 の空きは 9**（どれも ±0）
- **立方体 40 / 非立方体 77**・**モブ 9 種類**・**レシピ 59 本**・**精錬 7 本**・**燃料 8 種**・
  **食べ物 11 種**・**バイオーム 10**・**構造物 2**・**次元 3**・**`DamageCause` 9 種**（全部 ±0）
- **`AUTODEV-SPEC.md` 114 行（`状態: 未着手`）**・**`AUTODEV-QUEUE.md` 119 行**（未着手 7 件）

## この周でやったこと（AUTODEV 57 の B・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 済`」で 2 行目 = B（仕様）の周**でした
（未着手が 6 件あったので A ではありません）。**キューの先頭 1 件 = 24（ケーキ）**の
仕様書を書き、**サブエージェントは 1 つも立てていません。**

**この周のいちばんの仕事は「24 を 2 件に割ったこと」です。** 前の周の予告どおり
`main.ts` に何行要るかを数えたところ:

- **「かじる（7 回）」は `main.ts` に最低 7 行** —— `useOrPlace()` の `switch` に
  `case "cake":` の 1 行と、効果（かじった回数を減らす・書き込む・腹を満たす・音・
  `saveDirty`）を貼る助けが `tillAt` / `plantAt` と同じ形で 6〜8 行。
  **1449 行なので停止条件 2 に当たります**
- **「置けるところまで」は ±0 行** —— ブロック 1 つとレシピ 1 本で、置く経路も
  落とす経路も既にあるものを通ります

なので **`AUTODEV-SPEC.md` は前半（24a）だけ**にし、**24b（かじる）はキューの
いちばん後ろへ**回しました（`AUTODEV.md` の A-2 の 4 番。**取りやすさで並べ替えた
のではなく、依存が 1 つ解けていないから**です）。

**もう 1 つ分かったこと**: **クラフトに「残りかす」の道が 1 本もありません。**
`consumeGrid()` は 1 枠 1 個減らして空にするだけなので、そのままだとケーキ 1 個で
**バケツ 3 個（鉄 9 個）が消えます。** 24a に **`items.ts` の `LEFTOVERS` /
`leftoverOf()`** を入れてあります（`EMPTIES` と同じ形の表 1 本で、`crafting.ts` は
聞くだけ。**`Recipe` にキーは足しません** —— 残りかすはアイテムの性質です）。

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が **`状態: 未着手`** なので判定表の 3 行目です。
**`AUTODEV-SPEC.md`（24a・ケーキを置けるところまで）を全文どおり実装すること。**

- **ID は 155 ひとつだけ**（ブロックとアイテムで 1 本）。`MAX_ITEM_ID` を手で 155 へ
- **`main.ts` を 1 行も開かないこと**（この周の目的の半分がそれです）
- **見た目に出るので撮ること** —— `tools/shot.ts` に `cake` の場面を足して
  `npm run shot -- cake`、**撮ったら `Read` で開いて見る**（自然生成しないので
  既存の場面には 1 枚も写りません。クモの巣の周と同じ）

## ブラウザで見てほしいところ

**この周は `src/**` を 1 行も触っていないので、新しく見てもらうものはありません。**

**持ち越し（クモの巣・前の周から）**: **中の歩きが 1/4（1.3 m/s）で「絡まった」と
感じられるか** / **素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか** /
**一覧で `0xc8c8dc` が雪・ミルク・紙・糸と見分けられるか**（いちばん近いのは鉄インゴットで 27.5）。

**持ち越し（金のリンゴ）**: **色 `0xf0a800` が金インゴット・ブレイズロッド・
ブレイズパウダーと見分けられるか** / **金インゴット 8 個 + リンゴ 1 個に「体力 4 回復」が
見合うか** / **27b（革の防具）を実装したら: 防具点 7 で 8 割ぶんしか減らない
（10 → 7.2）のが「着た手ごたえ」として分かるか**。

**持ち越し（まだ誰も見ていません）**: **本棚が「本の詰まった棚」に見えるか**
（側面 `0x9c5064` がかなり赤紫）/ **紙 `0xd4e0ec` が雪玉と 25.5 で見分けられるか** /
**本棚 1 個に材料 9 個が見合うか** / **リンゴの色 `0xe0342c` が赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か** / **地面に落ちた
リンゴが赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか** /
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか** /
**はしごの上端から地上へ出る手触り** / **はしごの色（`0xc9a063` / `0xa8823f`）が
板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に見えるか** /
**`CANE_GROW_SECONDS = 180` が待てる長さか** / **自然に生えたサトウキビは伸びません**
（理由は `TUNING.md`）/ **⚠ 前から遊んでいる世界だけの見え方**: 前に浜の
サトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて見えることがあります /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度 / サボテンに
当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` に据えるものはありませんでした。** 踏んだ落とし穴が
「クラフトに残りかすの道が無い」1 つだけで、**それは決まりごとではなく
これから作るもの**だからです（24a の仕様書に入れてあります）。**書けるようになるのは
`leftoverOf()` が入ったあと** —— そのとき `rules/items-survival.md` に
「**食べ終わりに戻る器（`EMPTIES`）と、クラフトの残りかす（`LEFTOVERS`）は別の表**」を
1 節足すこと（C の周の C-4 でやること）。

## 人がやること

- **設計の判断（30 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**で、
  上限 1500 まで 51 行です。**この周でとうとう「割らないと入らないもの」が出ました** ——
  **24b（ケーキをかじる）に最低 7 行**要ります（上の数え方）。24a は ±0 行で通せるので
  ループは止まりませんが、**24b・スケルトン（11）と進むうちに必ずここへ戻ります。**
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
  （**判定の意味が変わるので無人の周では触れません**）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 40 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
