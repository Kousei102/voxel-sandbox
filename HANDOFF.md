# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-18・AUTODEV 98（B の周）のあと）

**この周は B（仕様）の周**でした（判定表の 2 行目 —— `AUTODEV-SPEC.md` が
`状態: 済` で、`AUTODEV-QUEUE.md` の未着手が 3 件あったため）。
**キューの先頭 12「刈られた羊が見て分かること」を `AUTODEV-SPEC.md` に書きました。**

- **`src/**` と `test/**` に 1 行も差分がありません**（B の周の完了条件）。
  触ったのは **`AUTODEV-SPEC.md`（110 行 / 上限 120）** と **`rules/testing.md`**、
  この `HANDOFF.md` の 3 本だけです
- **取る前にコードで数え直しました**: `Mob.woolTimer` は立つ（`mobs.ts` の `shear()`）のに、
  **`mobrender.ts` は `shapes` を `MobKind` だけで引いて形を使い回す**ので、
  刈っても見た目が 1 画素も変わりません。**12 は未実装で正しい**と確かめました

### この周で仕様のために測ったこと（次の C がそのまま使えます）

- **`test/mobs.test.ts` の見張りには `shearing` と `woolTimer` がもう入っています**
  （`decisions` の並び。「見た目は別の周だが」というコメント付き）。**次の周がその周です** ——
  だから `mobrender.ts` は**刈られたかどうかを知ってはいけません**。仕様は
  `mobs.ts` に `mobVariant(mob)` を置き、**描画が見るのは数 1 つだけ**の形にしてあります
- **刈られた羊の箱を実際に計算して、当たり判定に収まることを確かめました**
  （前と横 0.444 ≤ 0.45 / 高さ 1.125 ≤ 1.2 / 後ろ 1.000 は `longBody` の例外のまま）
- **⚠ モデル全体の外形は刈っても変わりません**（**0.625 x 1.125 x 1.444 のまま** ——
  脚と頭が外形を決めているため）。**細くなったことを見るなら体（`group 0`）で比べること**
  （**0.625 x 0.563 x 1.250 → 0.438 x 0.438 x 1.063**）。仕様に書いてあります
- **色は総当たりで選びました**: 地肌 `0xe3b09c` は**羊毛 `0xe8e4dc` から 82.6・
  顔と脚 `0xd6c8b4` から 36.3**（素直な `0xe0b6a4` だと顔から 26.1 しか離れません）

## 数字（2026-09-18・この周で測ったもの）

- `npm test` **3802 件**（±0。`src/**` も `test/**` も触っていないので当然です）
- **⚠ 音の跳ねがこれまでで一番荒れました**: **13 回走らせて 8 緑 / 5 赤**。
  **5 回とも音のグループ**で、**4 回が同じ「声色（音程の倍率）が波形に出る」**
  （低い声が 0.162 → 4.638 / 3.918 / 4.161 に跳ぶ。**高い声は 13 回とも 0.329**）。
  **⚠ そのうち 3 回は続けて落ちました** —— `rules/testing.md` の
  **「同じ件が続けて落ちたことは一度もありません」は嘘だった**ので据え直しました（下）。
  **判定は 1 文字もゆるめていません。** 最後は **8 回中 7 回緑**まで落ち着きました
- `npm run typecheck` / `npm run build` / `npm run bench` は**走らせていません**
  （`src/**` に 1 行も差分が無い周なので、`AUTODEV.md` の検証の対象外）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1450 行 / 上限 1500**
- **アイテム 145 種・`MAX_ITEM_ID` 186**（±0）・**立方体 42 / 非立方体 89**（±0）
- **1..63 の空きは 7**・**111..255 の空きは 69**（±0。**次に取るのは 187**）
- `AUTODEV-QUEUE.md` **116 行 / 上限 150・未着手 3 件**（±0）・
  `AUTODEV-SPEC.md` **110 行 / 上限 120・`状態: 未着手`**

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。
**`AUTODEV-SPEC.md` を全文そのままサブエージェントに渡すこと**（要約しないこと）。

- **一緒に渡す `rules/`**: `grep -l '"src/mobs.ts"' rules/*.md` → **`rules/mobs.md`**
  と **`rules/meshing-render.md`**、`test/**` を触るので **`rules/testing.md`** も
- **ID は 0 個**・**`main.ts` は 0 行**の見込みです（`MobRenderer.sync()` の呼び方は変わりません）
- **見た目そのものが本題の周なので、C-3 は飛ばせません。**
  `npm run shot -- sheep mobs`（場面 `sheep` は仕様で足すもの）と `node tools/browsershot.mjs`
- **12 を実装すると未着手 2 件**になるので、**その次の周が A（棚卸し）**です。
  **そのときキューに「音の揺れを測り方で直す」を足すこと**（下の「人がやること」）

## ブラウザで見てほしいところ

**この周は `src/**` を 1 行も触っていないので、絵は撮っていません**（撮るものが
1 つも変わっていません）。前の周までに撮ったものは `shots/` と `docs/browser-shots/`。
人に見てほしいのは（上ほど新しい）:

- **⚠ ツタの生え方の手ざわり（34c）**: **森の木の 25%** に掛かるのは多いか少ないか /
  **1 列 0〜4 マス**の垂れ方は「ツタ」に見えるか（`TUNING.md` の 34c の節）/
  **苗木から育てた木には掛かりません**（意図した違い）/ **針葉樹林には 1 本も
  生えません**（形はもう出来ているので、`BiomeDef.vine` を変えれば生えます）
- **⚠ 垂れたツタの手ざわり（34b）**: **宙にぶら下がった 1/16 の板が「ツタ」に見えるか**
- **⚠ 薄い板の横・真上に石があると置ける件（34b で開いた道。人の判断）**
- **⚠ 石の天井の下のツタ**: **手では置けないのに、あれば落ちません**
  （**34c はこの性質に寄りかかっています** —— 狭めるなら、生成側の切れ端も一緒に見ること）
- **⚠ ツタ（34a）の手ざわり 3 つ**: 登る速さがはしごと同じでよいか / 硬さ 0.2 /
  壁に貼った緑が「ツタ」に見えるか
- **⚠ 金・ダイヤの防具の色（33b）**: **金の上着 `0xefd714` とズボン `0xf8b317` は絵の上でやや近い**
- **⚠ ダイヤ一式 80% 減が強すぎないか**（**防具は壊れません**）/ **⚠ 金を作る理由があるか**
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ 鉄の防具の色（33a）**: 「鋼の青」が鉄に見えるか / 鉄一式 60% 減の手ざわり
- **レンガ（32b）**: `0xbc5d39` が「焼いたレンガ」に見えるか / **粘土（32a）**: 海底の 7.2%
- **ハーフ 2 材質（31）** / **苗木（30b）**: 180 秒が待てる長さか / **木炭（29）** / **修理（28）**
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / **防具（27b）**: 革一式 28% 減
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白** / **氷（25a）**: 滑りの手ざわり
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 1 件**）

- **`rules/testing.md`**: **音の揺れは「同じ件が続けて落ちたら退行」で見分けられません**
  （2026-09-18 に**同じ声色の件が 3 回続けて**落ちました。それまでの記述は
  「続けて落ちたことは一度もない」でした）。**退行かどうかは値で見ること** ——
  数回走らせて落ち着いた値（低い声 0.162 / 高い声 0.329）に戻れば揺れ、
  **毎回同じ跳ねた値なら退行**。**先に `src/audio.ts` / `src/sfx.ts` /
  `test/audio.test.ts` の 3 本に差分があるかを見ること**（無ければその周の実装が原因ではありえない）。

**`RULES-INBOX.md` は ±0 行**（層 3・スキルに足すことはありませんでした）。

## 人がやること

- **⚠ 音の揺れを測り方で直すか（この周でいちばん高くつきました）。** **13 回中 5 回赤**で、
  **`npm test` が通るまで 13 回走らせる**ことになりました。**赤い周は公開サイトに
  載らない**ので、**直さないとデプロイが 3 回に 1 回落ちます**。
  **直すなら測り方**（窓を長く取る・複数回鳴らして中央値）で、**判定はゆるめないこと**。
  **キューに足すのは A の周の仕事**で、**次の次の周が A です**
- **⚠ 設計の判断（69 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**）。
  **次の 12（刈られた羊）は `main.ts` 0 行の見込み**なので、判断が下りなくてもあと 1 周は
  進みます。**その次の 35 と 24b で当たります**
- **⚠ 石の天井の下のツタの非対称**（手では置けないのに落ちない）。**34c の生成は
  この性質を支えの保険に使っています** —— 狭めるなら、**森のツタの切れ端**も
  一緒に見てください（`test/worldgen.test.ts` の「支えを持っている」が先に落ちます）
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 98 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと**。
**⚠ 枠を数えるときは `#creative .slot` を使うこと**（素の `.slot` はホットバーと
インベントリの 36 枠まで拾います）。**⚠ `/opt/pw-browsers` には
`chromium_headless_shell-1194` も居ます** —— `startsWith("chromium-")` で弾くこと
（`tools/browsershot.mjs` の `findChrome()`）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**次の周の「刈られた羊」も同じで、
場面 `sheep` を足すことが仕様に入っています**（`mobs` の場面には**もこもこの羊しか
出ません** —— `Mobs.spawn()` したてなので `woolTimer` が 0 です）。
**時間が経たないと見えないもの**（育つ・燃える・溶ける）は、**その場で
`update()` を回してから撮ること**。
**⚠ 防具を着た姿は撮れません**（一人称視点なので自分の体が写りません）。
