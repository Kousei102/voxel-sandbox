# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-26）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。無人で回すうえで
  実際に踏んだものが 3 つあります:
  **(1)** **`.claude/rules/**` を編集すると「sensitive file」の確認で止まります。**
  → **`RULES-INBOX.md` に書いて、人が居る周が取り込む**形にしました（`LOOP.md` の「4.5」）。
  **`.claude/settings.json` に許可を書いても消えません**（2026-08-25 に 2 周とも実測）。
  **(2)** クラウドから `origin` へ push できず 403 → ユーザーが GitHub 側で権限を付け直し済み。
  **(3)** **5 時間の利用制限。** 当たった周は実装を終えた直後に止まり、次の定期実行も
  同じセッションに投げられて即終了しました（**新しい周にならず、1 回ぶんの枠が消えます**）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1868 件すべて成功**（1842 → 1868。この周の 26 件はエンドポータルの起動）
- **進行（クリア導線）達成 11 / 13**（11 件とも本物。**仮は残り 2 件**）
- ブロック ID **1..63 の空きは 11 個**（この周で 52 を使いました）。
  向き違いは **110 まで**（増えていません）。アイテム ID は **92 まで**（増えていません）
- `src/main.ts` は **1441 行 / 上限 1500**（余白 59 行。この周で 28 行使いました）

## 直前にやったこと — TASKS 2-9（エンドポータルの起動）

**枠 12 個すべてにエンダーアイを嵌めると、輪の内側 3x3 がポータルの面になります。**
**まだエンドへは行けません**（面を踏んでも何も起きない。移動は 2-10）。

| どこ | 何が入ったか |
| --- | --- |
| `endportal.ts`（新規） | **判断は全部ここ。** `ringAt` / `activate` / `fitEye` / `eyeMessage` |
| `blocks.ts` | **エンドポータル 52**（寝た薄い面・壊せない・通り抜ける・`emission` 15）と `END_PORTAL_LIGHT` |
| `stronghold.ts` | 輪の形を **`RING_OFFSETS`（12 個）/ `RING_INSIDE`（3x3）**として export。`ring()` はそれを回すだけになった。`facingToCentre()` も export |
| `main.ts` | 枠を狙ってアイを持っていたら `fitEye()`（+28 行） |
| `test/endportal.test.ts`（新規） | 26 件 |
| `test/progression.test.ts` | 「エンドポータルが 12 個の枠で起動する」を**本物**に差し替え、`ACHIEVED_BASELINE` を 11 へ |
| `test/ui.test.ts` | routed に `fitEye(` と `eyeMessage(` の 2 行 |

- **`portals.ts` に混ぜていません。** あちらは「黒曜石の枠を探して火を点ける」、
  こちらは「もう建っている輪の状態を進める」で、枠の探し方も点き方も共通点がありません。
  1 ファイルに混ぜると `findFrame()` がどちらのポータルの話か読めなくなります
- **この周のいちばんの罠: 輪の中心は枠の向きからは決まりません。**
  輪の 1 辺に並ぶ 3 個は**同じ向き**を向いているので、「向きへ 2 マス進んだ先が中心」と
  書くと**角寄りの 8 個が別の場所を指します**（真ん中の 4 個だけ当たるので、
  試し方によっては動いているように見えます）。いまは
  **「自分が輪のどの位置に居るか」を `RING_OFFSETS` の 12 通りとも試して**中心を出します
- **輪の形の出どころは `stronghold.ts` の 1 か所**（写していない）。
  `test/endportal.test.ts` は**手で輪を並べず、`STRONGHOLD.build()` が実際に建てた輪**に
  アイを嵌めます —— 手で並べると、建てる側と起動する側が食い違っていても両方とも緑になります。
  枠を数えるときも `RING_OFFSETS` を読まずに 13x13 を舐めています
- **もう嵌まっている枠を叩いてもアイは減りません**（`already`）。それでも輪は見ます ——
  未読み込みの列で 3x3 の書き込みが落ちたときに、もう一度叩けば続きが埋まります
- **輪の内側にブロックが置いてあっても上書きして起動します。** 枠が壊せない以上、
  輪の中にブロックを置いて起動を止められると、アイを 12 個使い切ったあとで詰みます
- **画面の 1 行は `eyeMessage()`**（`vitals.ts` の `deathMessage()` と同じ形）。
  枠は地下 18 マスにしか無いので、文言の分岐が `main.ts` に居ると掘り当てるまで直せません。
  **残り個数を必ず出すこと**（12 個は 5x5 の縁なので、1 個飛ばしていても目では気付けません）
- **わざと壊して 6 通りとも赤くなることを確かめました**: 中心を軸上の枠だけから探す →
  10 件 / 11 個で起動 → 4 件 + 進行の後戻り / 嵌め直しを許す → 1 件 /
  輪の内側を 5x5 に → 4 件 / 嵌めるついでに向きを決め直す → 1 件 /
  `main.ts` の配線を外す → 進行の後戻り + 「出した判断を呼び直している」
- `stronghold.ts` はチャンク生成の中なので `npm run bench` を 3 回:
  `generateChunk` **1.08 / 0.99 / 1.00ms**（中央 1.00。退行なし）
- 決まりごとは **`RULES-INBOX.md` に 2 件**（`rules/worldgen.md` へ 1 行と、
  `rules/dimensions.md` の新しい節「エンドポータル（`endportal.ts`）」）、
  数値は **`TUNING.md` の「エンドポータルの起動（2-9）」（7 行）**、
  確認項目は `REVIEW.md` の「2-9」に 7 個

### 前の周（2-1 〜 2-8）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージは書かず、撃った側が
  `Shot.damage` に載せる**ので、2-12 の弓矢は撃つ側を足すだけで当たる側が要らない）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ。
  **クリア導線の 2 本がここで合流する**）
- **2-7**: 投げたエンダーアイが要塞の方を向く（`stronghold.ts` 新規。
  `structures.ts` から `SiteRule` と `siteAt()` を切り出した）
- **2-8**: 要塞（石レンガ 53 + エンドポータルの部屋 + 枠 51。地面から 18 マス下）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 /
  2-8 / 2-9 の 13 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（これまで 7 度）。
   **この周でもう 1 度踏みかけました** —— 「エンドポータルが 12 個の枠で起動する」の
   仮の判定は `block("エンドポータル")` の存在だけを見ていたので、
   **ブロックを 1 行足した時点で達成 11 / 13 に化けました**（起動の実装より前に、です）。
   同じ周のうちに本物へ差し替えてあります。**残る仮 2 件**（「エンドへ行ける」と
   「エンダードラゴンを倒せる」）も、`block("エンドストーン")` /
   `sourceHas("src/mobs.ts", "dragon")` なので**同じ形で化けます**
2. **同じ向きが並ぶ列から中心を逆算しないこと**（この周）。輪の 1 辺に並ぶ枠は
   全部同じ向きを向いているので、向きから中心を出すと角寄りが別の場所を指します。
   **「自分がその形のどこに居るか」を全通り試すほうが、形を変えても付いてきます**
3. **手で並べた試験場は、建てる側との食い違いを見つけられません**（この周）。
   `test/endportal.test.ts` は `STRONGHOLD.build()` が実際に建てた輪を使います
4. **構造物の `extent` は「どこに掛かるか」の唯一の材料です**（2-8）。
   **`build()` が `extent` の外へ書くと端が黙って欠けます**
5. **地面から潜る構造物の深さは、地形の最低高さで頭打ちになります**（2-8）。
   `STRONGHOLD_DEPTH` を 30 にしたら、海の底の要塞の床が y=0（岩盤）に刺さりました
6. **1 チャンクに収まった標本だけを見ていると、端の欠けを見逃します**（2-8）
7. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（2-7）
8. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字まで
9. **探す半径は「正方形」であって「円」ではありません**（2-5）
10. **試験場のプレイヤーは、跳ぶモブでは `HOSTILE_SIGHT`(18) より遠くに置くこと**（2-5）
11. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）
12. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の火球で焼け死にます
13. **相手を先に、ブロックをあとに見ること**（2-4b）。逆だと壁を背にした相手に当たりません
14. **飛ぶモブには `ACCEL_GROUND` を使うこと**（空中の加速 4 では 1 フレームに 0.07 しか戻らない）
15. **`world.seed` をセーブや場所の計算に使わないこと**（ネザーでは塩を混ぜたあとの値）。
    種は `worldSeed`
16. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）
17. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）
18. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）
19. **わざと壊して、テストが赤くなるか確かめること。** この周は 6 通りとも確認済みです
20. **`test/audio.test.ts` の「声色（音程の倍率）が波形に出る」が 1 度だけ落ちました**
    （2-8 の周の途中で 1 回。`0.329 > 4.161` という出方）。**この周は 12 回まわして再現せず**、
    実装は 1 行も触っていません。**もう一度出たら、そのときは本気で見てください**
    （`node-web-audio-api` のレンダリングが揺れているのだと思われます）

## 次に取るもの

1. **2-10（エンド次元。エンドストーン 50 の島）**。**ポータルの面はもう建ちます。**
   - **`ROADMAP.md` の予約表から `END_STONE` 50 を取ること。**
   - 生成器は `nethergen.ts` が手本（`ChunkSource` を 1 つ足して `DIMENSIONS` に 1 行）。
     **`salt` は 0 以外で、オーバーワールドともネザーとも別の値**にすること
   - **空の行はもう置いてあります**（`daynight.ts` の `SKY_STYLES` の `end`。2-3b）。
     表を触らずに済むはずです
   - 移動は `portaltravel.ts` に 1 行（`portalTarget()` / `linkScale()`）。
     **`main.ts` に `to === END ? ...` を書かないこと**（`test/ui.test.ts` が
     次元の分岐を数えていて、上限は 2 です）
   - **踏んだら移る判定はネザーポータルと形が違います** —— あちらは面の中に立つと
     `PortalGate` が 0.9 秒数えます。エンドの面は寝ているので、素直に流用できるか
     見てから決めてください
   - **進行テストの仮の判定「エンドへ行ける」は `block("エンドストーン")` の存在だけ**を
     見ています。**ブロックを足した瞬間に達成へ化けます** ——
     その周のうちに本物（実際に移って、立てる場所に出るところまで）へ差し替えること
2. そのあとは並びどおり 2-11 → 2-12 → 2-13（ドラゴン）。
   **2-12 の弓矢は、撃つ側（弓とアイテム）を足すだけで当たる側が要りません**
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 12 件**）/ `REVIEW.md` の一括確認
   （**いま 13 節**）/ `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **起動したポータルの面を踏んでも何も起きません**（2-10 で移動が付きます）
- **起動したことはセーブに残ります**（面は `edits` に入る普通のブロックなので）。
  枠のアイも同じく `edits` です
- **本家の「星空」の見た目は出せません**（テクスチャが無いので暗い紫の無地）
- **要塞は「部屋 1 つ」だけです**（本家は通路・図書室・階段・シルバーフィッシュ）。
  肉付けするなら `stronghold.ts` の `build()` に足す形ですが、
  **クリア導線が 13 / 13 になってからにしてください**
- **要塞の中に入る道がありません**（掘って降りる前提）
- **投げたエンダーアイは、近づいても落ちません**（本家は近いと真下へ落ちる）。
  だから「その辺り」までしか案内してくれず、**部屋を 13 マス四方と大きめ**にしてあります
- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  `eyeShot()` に次元を渡して「オーバーワールド以外では null」にするのが素直です
- **投げたアイは地面に落ちません**（`projectiles.onExpire` で `drops` に渡すところから）
- **アイを投げても減りません**（本家は 20% で壊れる。`TUNING.md` の 2-7）
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒。`smelting.ts` に 1 行）
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方を全部見直すことになるので、**独立したタスクとして決めてください**
- **エンダーマンは日陰へ逃げません**し、**ブロックを持ち運びません**
- **ネザーでも寝られます**（本家はベッドが爆発する）
- **寝ても時間は「進みません」**（時計だけ飛びます）
- **火球は火を点けません**（火そのものが無いので、ブロックとして足すところから）
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）
- **`F3` の `biome` はネザーでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）
- **要塞の部屋の中にも敵対モブは湧きます**（石レンガは `spawnOn` を持たないため）。
  松明 4 本では隅まで照らしきれないので、**掘り当てた部屋にゾンビが居ることがあります。**
  本家の要塞も同じなので、いまは直していません
