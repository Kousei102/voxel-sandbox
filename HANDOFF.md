# 引き継ぎメモ（2026-08-05 時点）

このファイルは**使い捨て**です。読んで頭に入れたら消してかまいません。
恒久的な決まりごとは全部 `CLAUDE.md` に書いてあるので、**まず `CLAUDE.md` を読んでください。**
ここにはそこに書けない「いまの状況」だけを書きます。

---

## 1. いまの状態

**モブは完了しました。** 豚・羊・ゾンビが入り、ユーザーのブラウザ確認も全部通っています。

```
285703a モブ: 水の中では遅くなる（プレイヤーと同じ 0.6 倍）
b9bcb10 モブ: 1 ブロックの段差を跳び越えられるようにする（豚・羊・ゾンビ共通）
9a55a10 モブに殺されても死亡画面が出なかったのを直す
a1d81bd モブ: 敵対（ゾンビ）。暗所の湧き・追跡・接触ダメージ・日光で燃える
aa811aa モブ: 殴れるようにして、羊毛と生豚肉を落とす
6d01476 モブ: 徘徊 AI・自然な湧き・デスポーンと、2 種類目の羊
b66316c モブ: 描画の土台（豚が地面に立つところまで）
2441454 物理を physics.ts へ切り出す（挙動は変えない）
bcb7883 テスト: プレイヤーの移動の軌跡を固定する
1011f62 フェーズ3: 音（WebAudio で合成）                  ← origin/master はここ
```

- `npm test` **653 件成功** / `npm run typecheck` / `npm run build` 通過。作業ツリーはきれい
  （未追跡はこの `HANDOFF.md` だけ）
- **上の 9 コミットはすべてユーザーがブラウザで確認済み**
- **`origin/master` は 9 コミット遅れています。** モブはまだ公開サイトに出ていません
- モブの計画書（`/home/node/.claude/plans/handoff-md-eventual-tiger.md`）は**使い切りました。**
  読む必要はもうありません（残す価値のあることは `CLAUDE.md` の「モブ」の節に移してあります）

### 次にやること

**インベントリまわりの課題**をユーザーが別セッションで出します。**内容はまだ聞いていません。**
それ以外の見取り図（テクスチャ・空腹・かまど・環境音・川と湖）は `CLAUDE.md` の
「これから入れるもの」を見てください。**着手する順はユーザーと決めること。**

インベントリを触るときの下ごしらえ:

- 判定は `inventory.ts` / `crafting.ts`（DOM に触らない。ヘッドレスでテストできる）、
  描画は `inventoryui.ts`。**判定を UI 側に書かないこと**（`CLAUDE.md`「アイテムとサバイバル」）
- `test/inventory.test.ts` と `test/crafting.test.ts` が既にあります
- インベントリはセーブに入ります（`storage.ts`）。**アイテム ID を振り直さないこと**

### push について

**ユーザーに確認を取ってから push すること。** 公開サイトが差し替わる外向きの操作です。
push すると `.github/workflows/deploy.yml` が走り、`npm ci` → `npm test` → `npm run build`
を通ってから GitHub Pages（https://kousei102.github.io/voxel-sandbox/）に出ます。
**テストが落ちればデプロイされません。**

押したら `gh run watch <id> --exit-status` で Actions を見届けること。
そのあと配信中の JS のハッシュを `dist/assets/` と突き合わせると、実際に出たかまで確かめられます。

**フレーム時間の p99 判定（`world.test.ts`）はマシンが混むと落ちます。**
実測: テストを 2 本同時に走らせると p99 が 170ms まで跳ねました（上限 39ms）。
CI ランナーが混んでいると同じ落ち方をするので、落ちたら**中身の退行と決めつけず
再実行を試すこと**（フェーズ1 では実際にこれで止まりました）。

ブランチは切らず `master` に直接積んでいます（デプロイのワークフローが
`master` への push で走る設定のため）。

---

## 2. モブで実際に踏んだ落とし穴

同じ所でまた転ばないように残しておきます。**上 4 つはテストが捕まえたもの、
下 4 つはユーザーがブラウザで見つけたもの**（＝テストでは捕まらなかったもの）です。

- **`>>` を float 座標に使うと負の側で 1 列ずれる**（`-0.5 >> 4` が 0 になる）。`columnOf()` を通すこと
- **崖回避は向きだけ変えても効かない。** 勢いも殺し、さらに「目標と向きがずれているあいだは
  前へ出さない」（`aligned = max(0, cos(...))`）が要る
- **モデルを当たり判定からはみ出させない**（壁の向こうへ鼻が突き抜けて見える）。
  **振る部位の箱は `y1 === 0`**（0 でないと足首・肘で回る）
- **テスト自身が偽陽性になりうる。** 「湧きを止めるつもりでプレイヤーを 300 ブロック先に置いた」ら
  モブがデスポーンして「更新されていないから落ちない」で通っていた。
  **先に「ちゃんと動いた」ことを確かめる判定を置くこと**（歩いた距離・個体数）。
  同じ筋で、確率つきドロップの検証を **1 体ごとに `seeded(...)` を作って**やったら、
  線形合同法の 1 個目が種に引きずられて 200 回全部同じ側に転び「確率 0.6 なのに 100%」で通った。
  **乱数は 1 本を回し続けること**
- **フレームの前後で体力を比べる形にしないこと。** モブのダメージは `updateVitals()` より
  前に入るので、控えを取った時点で差分が済んでいて、**ゾンビに殺されても死亡画面が出なかった。**
  いまは `vitals.takeDamage()` で 1 回だけ拾う形（順番に依存しない）
- **跳ぶだけでは壁を越えられない。** 壁に当たった時点で `moveBody` が横の速度を 0 にしていて、
  空中の加速では戻らないので、**跳ぶ瞬間に前へ出す速さを入れ直す**必要がある（`hopTimer`）
- **プレイヤーに掛かっている補正はモブにも要る。** 水中の 0.6 倍が抜けていて、
  ゾンビが水中でも陸と同じ速さで追ってきた。`player.ts` を触ったときは
  `mobs.ts` の同じ箇所も見ること（重力・終端速度・水の抵抗）
- **敵対モブのテストでプレイヤーを目の前に置かないこと。** 「殴る距離だから止まる」判定が
  毎フレーム効いて、モブが 1 歩も動かないまま通る
- **暗くしただけでは湧きは止まらない**（暗所はゾンビの湧き条件）。テストの試験場を静かにするには
  `quiet()`（sky 0 + block 15）を使うこと

---

## 3. 進め方（このプロジェクト特有）

**この環境では WebGL が動かず、音も鳴らせません**（詳細は `CLAUDE.md`）。なので:

- **見た目・音を変える変更は、1 まとまりごとに止めてユーザーに見て（聞いて）もらうこと。**
  未確認の変更を積み上げないでください。落ちたときに切り分けられなくなります
- 確認をお願いするときは、**具体的な確認項目を 4〜6 個の箇条書き**で出すと話が早いです。
  番号を振っておくと「5 だけ違う」と返ってきて、すぐ切り分けられます
- **確かめられないものは、確かめられるものから切り離す。** これがこのプロジェクトの背骨です。
  いまの切り分け: GLSL は `terrainshader.ts` / `sky.ts` だけ、WebAudio は `audio.ts` だけ、
  DOM は `ui.ts` / `inventoryui.ts` だけ、モブの three は `mobrender.ts` だけ。
  残りは全部ヘッドレスで動きます。**新しく「確かめられないもの」を足すときは、必ず同じ形にしてください**
- 画面や耳でしか気付けない類は、数値に落としてテストにしています
  （面の裏返り → 発散定理で体積、置き方 → `placeSpot()` を直接呼ぶ、音 → 間隔と数値の妥当性、
  通知 → 要素の置き場所、モブ → 上のリスト）

### デバッグに使えるもの

- `M` キー: 照準の先にモブを 1 体（**湧きの条件に関わらず**出せるので、
  「湧かない場所なのか、描けていないのか」を切り分けられます）。種類はランダムです
- `F3`: デバッグ表示にモブ数（`mobs N`）が出ます
- 開発サーバーは `npm run dev`（5173 が埋まっていると 5174 になります）

### 計測するとき

**`npm run bench` は 1 回では当てになりません。** 同じコードで `generateChunk` が
0.93〜1.46ms に振れます。疑わしいときは `git worktree add <絶対パス> HEAD` で比較対象を
用意し、`node_modules` を symlink して**同じ条件で交互に測ってください**
（終わったら `git worktree remove`）。

このセッション終盤の実測:

```
world.update    中央 3.4ms / 最悪 10〜17ms（GC 込み）
モブ 40 体      中央 0.042ms / p99 0.14ms   ← world.update の 9ms 予算に対して誤差
npm test        11.7 秒（653 項目）
```

**モブの費用は `world.update()` の外に置いてあります**（意図的）。中に入れると
`test/world.test.ts` の p99 に乗って、モブの退行とストリーミングの退行が区別できません。

---

## 4. 触るときに気をつける場所

`CLAUDE.md` に全部書いてありますが、特に壊しやすいのはこの 6 つです。

1. **`terrainshader.ts` の GLSL と `audio.ts` の WebAudio**。どちらもこの環境では
   確かめられません。**判断を持ち込まないこと。**
   壊れると「画面が真っ黒」「音が鳴らない」という形でしか出ません
2. **ブロック ID の 2 段の枠**。立方体を 64 以降に置く／64 以降にアイテムを持たせると、
   面の統合が壊れるか持てないブロックになります。**既存の ID を振り直すのも厳禁**
   （`localStorage` の差分が別のブロックに化けます）。
   **1..63 の空きは 26 個**、アイテムは 82（腐った肉）まで使用済み
3. **`Builder.quad()` は法線の成分の和で頂点の巡回順を決めています。** 斜めの法線は
   和が 0 になって面が裏返ります。**箱は軸に平行のまま作って `Object3D` で回すこと**
4. **`canPlaceAt()` と `breakUnsupported()` は対**（`world.ts`）。
   片方だけ直すと、壁の松明が空中に残ります
5. **`BlockDef` の既定値**。`sound` は `"stone"`、`blocksSky` は `opaque` と同じ、
   `boxes` は立方体。**書き忘れが「それらしく間違った状態」になる**ので、
   ブロックを足したらテストが出す一覧（ID の空き・音の材質）を必ず見てください
6. **`mobrender.ts` の `matrixAutoUpdate`**。チャンクの真似をして `false` にすると
   モブが全部原点で固まります（コメントを残してあります）

いずれもテストが見ています。壊してみて落ちることは確認済みです。
