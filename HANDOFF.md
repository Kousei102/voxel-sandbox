# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05・AUTODEV 37 のあと）

- `npm test` **3090 件すべて成功**（この周は **+11**）・`npm run typecheck` 緑・
  **`npm run build` 緑**（`src/**` を 5 ファイル触った周）
- **音の一群は 1 回も赤くなりませんでした**（3 回走らせて 3 回とも緑。落ち着いた値は
  前の周と一致 —— 羊毛 0.552 < 雪 0.716 < 砂 2.235 / 低い声 0.162 < 高い声 0.329）
- **`npm run bench` は走らせていません**（生成の連鎖に 1 段足しただけで、
  ハッシュ 1 本ぶんです。**代わりに `npm test` の前後を同じ箱で測って 21.5 → 22.4 秒**。
  増えたぶんは**足したテストの走査**で、生成そのものではありません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（1 バイトも増えていません）
- **`main.ts` 1448 行**（上限 1500・**止まる目安 1450 まで残り 2 行。この周で ±0**）
- **アイテム 109 種・`MAX_ITEM_ID` 144・111..255 の空きは 111・1..63 の空きは 9**
- **立方体 39 / 非立方体 72**・**モブ 9 種類**・**レシピ 54 本**・**食べ物 9 種**

## この周でやったこと（AUTODEV 37 の C・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 未着手` だったので判定表の 3 行目に当たって **C（実装）の周**。
**サトウキビ（ブロック 143）と砂糖（アイテム 144）を足しました**（キューの 18a）。
**差し戻し 0 回**・**サブエージェントは使わず自分で書きました**（`AUTODEV.md` の C-1 の
「返らないなら自分で実装する」。点検 C-2 も自分で `git diff` を読みました）。

- **サトウキビはキノコ（139 / 140）の定義を写したもの**で、違うのは**色・生える場所・
  箱の上端**の 3 つだけ。**浜（`BEACH`）にだけ 0.12 で立ちます**（`biomes.ts` の
  `BiomeDef.cane`。**`worldgen.ts` に「浜なら」とは 1 行も書いていません**）
- **箱は `CANE_BOX`（上端 1）**。`CROSS_BOX` の 0.8 のままだと **18b で積んだときに
  継ぎ目が 0.2 マス空く**ので、**先に上端だけ伸ばしてあります**（あとから変えると
  セーブ済みの世界で見た目が動くため）
- **砂糖はサトウキビ 1 個の形なし**（2x2 で作れます）。置けず・道具でもなく・食べ物でもない
- **`main.ts` は 1 行も触っていません**（仕様書の予告どおり。置くのも掘るのも生えるのも
  既存の経路に乗ります）。`placing.ts` / `canSupport()` / `canPlaceAt()` も 0 行

**前の周（B）が据えた決まりごとが 2 つとも効きました。** 水際の判定も
`patchOf()` も**実装で 1 度も踏んでいません**（`docs/autodev-log.md` の AUTODEV 37）。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` を `状態: 済` にし、`AUTODEV-QUEUE.md` の
未着手が 4 件なので判定表の 2 行目です。

- **キューの先頭は 18b（サトウキビが積める・上へ伸びる）**。**ここは `main.ts` の配線が
  要ります** —— 下の「人がやること」の判断が返っていなければ、**仕様の周のうちに
  「`main.ts` が 0 行で済むか」を数え、済まないなら 19 番以降へ回すか、
  さらに割ること**（18a を 0 行で通したのと同じやり方）
- **次に取る ID は 145 から**（`ROADMAP.md` の予約表）

## ブラウザで見てほしいところ

**この周は撮って `Read` で開きました**（`npm run shot -- beach` = **`tools/shot.ts` に
足した新しい場面**。既存の 8 場面はどれも浜を近くから写しません。あわせて
`node tools/browsershot.mjs` の 5 枚 = 本物のブラウザ）。
**数値では言えないところだけ残します:**

- **浜のサトウキビの密度 0.12 が歩いていて気持ちいいか。** 撮った絵では
  **「まばらな竹林」**に見えます（浜の生える列の 12.2%）。**薄くすると 1 本ずつ探し歩く
  手触り**に、**濃くすると浜が緑で埋まります**（`TUNING.md`）
- **1 マスぶんの背丈が寸詰まりに見えないか。** 本家は 3 段まで生えます。
  **積めるようにするのは 18b** ですが、**「1 本だけ」が不自然に見えるなら順番を上げる**
  判断が要ります
- **一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか**（色は DOM なので
  `npm run shot` に写りません。**雪からの隔たりは 22.3 しかありません**）と、
  **サトウキビ（`0x9ad14f`）が小麦の種（42.4）と並んだとき**

**持ち越し（まだ誰も見ていません）**: ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け /
シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか / 食べ終わりにボウルが手に戻る手ごたえ /
森を歩いたときのキノコの密度（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を草むらと
取り違えないか / サボテンに当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・ミルクで毒が止まるか /
ミルクバケツの色（`0xeaf2f8`）が羽根・卵・糸・雪玉と見分けられるか /
かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
鉄・金・ダイヤのブロックが材料の山と見分けられるか /
**一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`。
「キノコシチュー」は 7 文字でいちばん長い名前。**「サトウキビ」は 5 文字**）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 3 件・`rules/` に据えました）

1. **`rules/worldgen.md`**: **地表のすぐ上に立つ「生えもの」は連鎖 1 本に並んでいて、
   表どおりの確率になるのはいちばん先に引いたものだけ。** いままでキノコと草むらの
   2 つの話として書いてあったものを、**3 つ目（サトウキビ）が入っても効く形**に
   一般化し、**使っている塩 4 本を並べました**（`0x7c39` / `0x4d17` / `0x2f8b` / `0x6a55`）。
2. **`rules/worldgen.md`**: **帯状のバイオームを数える形を、実際に書いた値で埋めました**
   （±400 を 1 マスおき → 浜 11045 列 → `voxel()` は先頭 2000 列だけ。
   **`npm test` 21.5 → 22.4 秒**）。**列の集め方を広げるより `voxel()` の列数の上限を
   先に見ること**、を足しました。
3. **`rules/meshing-render.md`**（`paths` に `tools/raster.ts` を足しました）:
   **`encodePng()` の引数は「幅・高さ・画素」の順**（逆に渡すと `Buffer.alloc(NaN)` で
   落ちます）と、**`world.getVoxel()` は生成済みのチャンクしか読まない**
   （原点から離れた所を探すなら先に `primeAround()` するか `WorldGen` を直に叩くこと）。
   **どちらも使い捨ての撮影を書く周だけが踏みます** —— この周で両方踏みました。
   **そのうえで `tools/shot.ts` に `beach` の場面を足した**ので、次からは
   `npm run shot -- beach` で同じ画が撮れます（**18b で積んだサトウキビを見るのに要ります**）。

## 人がやること

- **設計の判断を 1 つお願いします（11 周持ち越し。ただし急ぎではなくなりました）:
  `main.ts` を 2 つに割ってよいか。** いま **1448 行で、止まる目安 1450 まで残り 2 行**。
  **`test/ui.test.ts` の 41 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **18a は予告どおり 0 行で通りましたが、次の 18b と 19 はどちらも配線が要ります。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 21 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
