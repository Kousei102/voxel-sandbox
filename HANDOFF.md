# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2488 件すべて成功**（**この周は B（仕様）なので
  `src/**` と `test/**` に 1 行も触っておらず、前の周から増えていません**）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1383 行 / 上限 1500**（**この周は 0 行**）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**この周は 0 個**）
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（3d・火打石と打ち金と弓の耐久値）、
  `AUTODEV-QUEUE.md` の未着手は **8 件** → **次の周は C（実装）**
- **`RULES-INBOX.md` に未取り込みが 7 件**（`mobs.md` / `projectiles.md` /
  `items-survival.md` x 2 / `inventory-screen.md` / `drops.md` / `stateful-blocks.md`）。
  **取り込むのは人が居る周**

## この周でやったこと（無人・B の周 1 種類だけ）

**`AUTODEV-QUEUE.md` の先頭 3d を `AUTODEV-SPEC.md`（118 行）にしました。** コミットは 1 つで、
**`src/**` と `test/**` には 1 行も触っていません**（B の完了条件）。

**取る前にコードで数え直しました**（B の決まり）。前の `HANDOFF.md` の見込みと違った点が 3 つ:

- **`durability.ts` に `wearForUse()` を足すだけでは済みません。** `maxUses()` が `toolOf()` しか
  見ないので、**火種も弓も `wearable()` が false のまま**です。`isFireStarter()` / `isBow()` を
  引く 1 本（`usedUp()`）を挟んで `maxUses()` を通し、**`wearForBreaking()` には逆に
  「掘る道具でなければ 0」を足す**（弓で石を掘って弓が減っては困る）形にしました
- **`items.ts` の「耐久値がまだ無い」は 2 か所ではなく 4 か所**（83〜86 / 124〜130 の説明 2 つと、
  210 / 221 行の行末コメント 2 つ）。仕様書には**コメント 4 か所だけ**触ると書いてあります
- **セーブの形は 1 バイトも変わりません。** `serializeWear()` と `wornValue()` は `maxUses()` に
  聞くだけなので、**`maxUses()` が 0 でなくなった瞬間に `wear` / `craftWear` / `dropWear` /
  `chestWear` / `furnaceWear` の 5 つ全部に自動で載ります**（新しいキーは 1 つも要りません）

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶんはありません**（仕様書だけで、動くものは 1 行も変わっていません）。

**前の周までの持ち越しが 8 件、まだ人の目を通っていません:**

- **器へ入れて出す**（`87c6a80`）—— 傷んだ道具をチェストに**つまんで入れる／シフトクリックで
  入れる**。**枠の下の細い帯**が付いたままか。閉じて開き直しても付いたままか
- **器を壊す**（`87c6a80`）—— 傷んだ道具を入れたままチェスト（とかまど）を壊し、
  地面に落ちたものを拾い直しても帯が付いたままか
- **落とし物が傷を運ぶ**（`6e8ec84`）—— `Q` で捨てて拾い直す・持ったまま死んで拾い直す
- **掴んで動かしても傷が残る**（`61657d5`）—— 収納へ動かしても帯が付いて回るか。
  **クリエイティブの一覧（`E`）から出した道具には帯が出ていないこと**
- **道具の耐久値**（`8a780e9`）—— 木のツルハシで石を掘ると帯が出る（減るほど短く・赤く）。
  使い切ると **「木のツルハシ が壊れました」** が 1 行出るか
- **大きいチェスト**（`a795564`）—— 2 個並べて開くと **54 枠が 9 列 x 6 段**、単体だと 27 枠
- **砂・砂利が落ちる**（`6209e91`）—— 砂の柱の下を掘ると上が詰まり、穴や浮いた砂が残らないこと
- **メッシュ化の詰め方**（`0c68545`）—— **面が欠けていないか**と **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-SPEC.md` が `状態: 未着手`** なので → **次は C（実装）の周**です。

- **仕様書は全文を渡すこと**（要約すると「触らないファイル」と「禁じ手」が真っ先に落ちます）。
  **`src/**` と `test/**` は `Read` / `Edit` で開くこと**を毎回書き添えること
- **サブエージェントを背景（async）で起動しないこと。** 同じ番の中で結果が返らないなら
  **自分で実装すること** —— 周を閉じずに終わるほうがずっと高くつきます
- **見た目に出ます**（傷んだ火種と弓に帯が出る）。終わったら「ブラウザで見てほしいところ」に
  2 行残すこと

人がやること:

- 上の**ブラウザ確認 8 件**（全部が持ち越しです）
- `RULES-INBOX.md` の **7 件**の取り込み（**`drops.md` / `items-survival.md` /
  `stateful-blocks.md` のぶんは `paths` に `src/durability.ts` を足す指示付き**）
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（1 回だけ走らせて緑）。
`audio.ts` / `sfx.ts` には 1 行も触っていないので、**直ったわけではありません。**
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
