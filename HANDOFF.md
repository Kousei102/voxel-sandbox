# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-06・AUTODEV 44 のあと）

- `npm test` **3143 件すべて成功**（この周は **±0 件**。B の周なのでテストを書きません）・
  `npm run typecheck` 緑
- **`npm run build` と `npm run bench` は走らせていません** —— **`src/**` に差分が 0 行**の
  周だからです（B の周の完了条件そのもの）
- **音の一群は 1 回も跳ねませんでした**（羊毛 0.552 / 雪 0.716 / 砂 2.235・
  低い声 0.162 / 高い声 0.329 と、前の周と同じ落ち着いた値）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1448 行**（`wc -l`。テストの数え方では 1449。上限 1500・
  **止まる目安 1450 まで残り 2 行。この周は ±0**）
- **アイテム 110 種・`MAX_ITEM_ID` 145・111..255 の空きは 107・1..63 の空きは 9**
- **立方体 39 / 非立方体 76**・**モブ 9 種類**・**レシピ 55 本**・**食べ物 9 種**

## この周でやったこと（AUTODEV 44 の B・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 済` で `AUTODEV-QUEUE.md` の未着手が 3 件だったので、
判定表の 2 行目に当たって **B（仕様）の周**。**キューの先頭 19b（はしごを登れる）**を
`AUTODEV-SPEC.md`（**115 行**）に落としました。**`src/**` は 1 行も触っていません。**

**取る前にコードで数え直しました**（B の決まり）:
`grep -rn "climbable\|onLadder\|isClimbable" src/ test/` は **0 件** ——
19a は「登る側を先取りしない」という禁じ手をきちんと守っていて、**まっさらな状態から
書けます**。19 を 2 件に割ったときの見積もり（ID 0 個・`main.ts` +1 行）も変わりません。

仕様書で決めた形（**サブエージェントに判断を残さないため、全部こちらで決めてあります**）:

- **旗は `blocks.ts`** —— `climbable` を `SPIKY` / `isSpiky()` と**同じ形**の表 1 本にして、
  `LADDER_OPTS` に 1 行足せば **4 向きが一度に付きます**
- **どのマスに効くかは `player.ts`** —— サボテンの `bodyTouches()` をそのまま使うので
  **`physics.ts` は 0 行**（`moveBody()` の式を触ると `test/physics.test.ts` の軌跡が落ちます）
- **速さは `player.ts`**（`WALK_SPEED` の隣）。**本家の値をそのまま暫定で**:
  登り **2.35 m/s**（`Space`）/ 滑り降り **3.0 m/s**（何も押さないとき）
- **水がはしごに勝ちます** —— `if (this.inLiquid)` の**あと**に `else if (this.onLadder)`。
  順を逆にすると水中で泳げなくなります
- **落下ダメージの打ち消しは `vitals.ts` の `updateFall()` の 2 か所だけ**
  （`inLiquid` とまったく同じ扱い）。**`main.ts` は事実を 1 行渡すだけ（1449 行）**

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` を `状態: 未着手` にしたので判定表の 3 行目です。
**`AUTODEV-SPEC.md` を全文サブエージェントに渡すこと**（要約しない）。
**当たる `rules/` は 5 本**: `blocks-shapes.md` / `mobs.md` / `vitals.md` /
`items-survival.md` / `beds.md`、**それと `test/**` を触るので `testing.md`**。
**19a の穴は「3 本のうち 1 本しか読まなかった」ことが原因**なので、**当たったものは全部読むこと。**

## ブラウザで見てほしいところ

**この周は絵を撮っていません**（`src/**` に 0 行の B の周で、見た目は 1 ドットも動いていません）。
**19a で撮ったはしごの絵（`shots/ladders.png`）がそのまま最新**です。

**次の C の周（19b）で人に見てほしくなるのは 3 つ**（まだ実装していません）:
**2.35 m/s の登りが待てる速さか** / **手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」
と感じられるか**（本家と同じ挙動ですが、**しゃがみで止まる手はここに無い**ので
落ちっぱなしに感じるかもしれません）/ **はしごの上端から地上へ出る手触り。**

**持ち越し（まだ誰も見ていません）**: **はしごの色（`0xc9a063` / `0xa8823f`）が板・原木と
見分けられるか** / **厚さ 3/16 の板が「はしご」に見えるか**（横木は無く、板 1 枚です）/
**「はしご は壁にしか付けられません」の文が、床を狙ったときに親切か** /
**`CANE_GROW_SECONDS = 180` が待てる長さか**（1 段から 3 段まで 6 分）/ **上を刈って
伸び直すのが「畑」として成り立つか** / **自然に生えたサトウキビは伸びません**（上に 1 本
置けば列ごと覚えます。理由は `TUNING.md`）/ サトウキビの背丈と密度の釣り合い（0.12 で
1〜3 段）/ **積む手ごたえ** / **⚠ 前から遊んでいる世界だけの見え方**: `localStorage` の
差分は「壊した／置いた」だけを持つので、**前に浜のサトウキビを壊した場所は、下の 1 マス
だけが空いて上の段が浮いて見えることがあります**（新しく作った世界では起きません）/
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / サトウキビ
（`0x9ad14f`）が小麦の種と並んだとき / ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け /
シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか / 食べ終わりにボウルが手に戻る
手ごたえ / 森を歩いたときのキノコの密度（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を
草むらと取り違えないか / サボテンに当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・
ミルクで毒が止まるか / ミルクバケツの色（`0xeaf2f8`）/ かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` に足すものはありませんでした。** B の周は文書しか触らず、落とし穴を 1 つも
踏んでいません（`src/**` に 0 行・差し戻し 0 回）。**19a で踏んだ「当たった `rules` を
全部読む」は `CLAUDE.md` に既にある**ので、層 2 へ二重に書きませんでした
（同じことを 2 つの層に書かないこと）。

## 人がやること

- **設計の判断を 1 つお願いします（18 周持ち越し。そろそろ急ぎです）:
  `main.ts` を 2 つに割ってよいか。** いま **1448 行で、止まる目安 1450 まで残り 2 行**。
  **19b で 1 行使って 1449 になり、残り 1 行になります。**
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 28 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`。
**この周の仕様書は 115 行**）。**`main.ts` の行数も同じ数え方**なので、
`wc -l` の 1448 はテストでは 1449 と出ます。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
