# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- `npm test` **2965 件すべて成功**（**前の周と同じ 2965。この周も `src/**` と `test/**` に
  1 行も差分がありません**）・`npm run typecheck` 緑。**`npm run build` と `npm run bench` は
  走らせていません**（**`src/**` を 1 行も触っていない B の周**のため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1464 行**
- **アイテム 98 種・`MAX_ITEM_ID` 133・111..255 の空きは 122・1..63 の空きは 9**
  （**この周は ID を 1 個も取っていません**。仕様書に「134 を取る」と書いただけです）
- **モブ 9 種類**（受動 4 = 豚・羊・鶏・牛 / 敵対 4 = ゾンビ・クモ・ブレイズ・
  エンダーマン / ボス 1 = ドラゴン）・**クラフト 44 本 / 精錬 7 本 / 食べ物 8 種**

## ⚠ 次に `main.ts` を触る周がまず見ること: 1464 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
**7 周続けて `main.ts` を 1 行も触っていないので、持ち越しです。**
**次の C の周（雪玉）も `main.ts` はコメント 1 語だけ**なので行数は動きません。
**解けるのを待っているのはキューの 15 番（ミルクバケツ）以降**です。

## この周でやったこと（AUTODEV 24 の B・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が **9 件（3 件以上）**・`AUTODEV-SPEC.md` が **`済`** だったので、
判定表を上から当てて **B（仕様）の周**。**`src/**` と `test/**` に差分は 1 行もありません**
（`git diff --stat -- src test` が空）。

- **キューの先頭 = 13 番（雪玉）**を `AUTODEV-SPEC.md` に書きました（**113 行**・上限 120）。
  飛ばしていません
- **取る前にコードで数え直しました**（B の決まり）: `items.ts` に `SNOWBALL` は 0 行、
  `DROPS`（`items.ts:623`）に `SNOW` の行は無く、`PROJECTILE_KINDS`（`projectiles.ts:149`）は
  **5 種類**、`THROWN`（`items.ts:818`）は**卵 1 行だけ**。**まだ入っていません**
- **仕様書を書くために読んで分かった、実装の周が必ず踏むもの**を仕様書に名指しで入れました:
  - **`test/projectiles.test.ts:115` の「5 種類ある」と `test/blocks.test.ts:177` の
    `MAX_ITEM_ID === STRING`、`test/blocks.test.ts:915` の「それ以外は投げられない」の
    除外条件**の 3 か所は、雪玉を足すと**必ず**直すことになります。**判定をゆるめる
    書き換えと見分けが付かない**ので、仕様書の 6 に「直してよいのはこの 3 か所だけ」と
    書きました
  - **`use.ts` は 1 行も要りません**（`thrownProjectile()` の表を通るため。`rules/projectiles.md`）。
    **`main.ts` も `throwItem()` のコメント 1 語だけ**で、`damage` を渡さないことが
    そのまま「当たっても減らない」になります

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-SPEC.md` が `未着手`（`済` でない）ので → 次は C（実装）の周**です。

- **仕様書は全文をサブエージェントに渡すこと**（要約すると 2 と 6 が落ちます）。
  **同じ番の中で結果が返らないなら、自分で実装すること**（`AUTODEV.md` の C-1）
- **渡す `rules/`**: `rules/items-survival.md` / `rules/projectiles.md` / `rules/testing.md`
- **見た目に出る周です**（投げた雪玉が飛ぶ・一覧に青白いアイテムが並ぶ）。
  **撮って `Read` で見ること**（C-3）。撮っただけでは確かめたことになりません
- **`ROADMAP.md` の予約表から 134 を 1 個**。片側だけ見て取らないこと

## ブラウザで見てほしいところ

**この周は見た目に 1 画素も出ません**（`src/**` に差分ゼロ）。**新しく見てほしいものは
ありません。** 持ち越しだけです:

**クモを 3 匹倒すまで弓が作れない**道のり（遠すぎるなら糸の個数のほうを触ること） /
**夜の敵対がゾンビとクモで半々になった手触り**と**クモの速さ 6.0** /
**クモが朝に燃えてよいか**（本家は燃えません）/ **糸・羽根・卵がどれも淡い色**
（**雪玉が入ると白っぽいものが 4 つになります**。仕様書は青白 `0xbcd8ef` を当てました）/
**投げた卵に使い道が無い** / **牛の胴が寸詰まりに見えないか** / **ステーキと革が一覧で
見分けられるか** / **産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒**。

## この周で分かったこと

**決まりごとは 0 件です**（`rules/` に 1 行も足していません）。B の周は `src/**` を
触らないので、据えるべき落とし穴が出ませんでした。

**「取る前にその 1 件だけ数え直す」が今回も効きました。** キューの 13 番は
「`DROPS` と `PROJECTILE_KINDS` 1 行」と書いてありましたが、実際にコードを引くと
**テスト側に必ず触る 3 か所（数・`MAX_ITEM_ID`・除外条件）**が出てきます。
**仕様書に書かなければ、そのままサブエージェントの勝手な判断**（＝ 判定をゆるめたのか
数を直したのか分からない差分）**になっていました**（`AUTODEV.md` の B の前書きそのまま）。

## 人がやること

- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 6 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**この周は「cutoff が低い材質ほどこもって鳴る（羊毛 < 雪 < 砂）」で
1 回赤くなり、走らせ直したら緑でした**（羊毛 0.552 < 雪 0.716 < 砂 2.235）。
**`rules/testing.md` が名指ししている揺れそのまま**なので、決まりごとは足していません。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
