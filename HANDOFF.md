# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- **この周は B（仕様）なので `src/**` と `test/**` に 1 行も触っていません**（`git diff` で確認済み）
- `npm run typecheck` は通ります。**`npm test` 2672 件すべて成功**（前の周と同じ数。src を触っていないので増えません）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1433 行 / 上限 1500**（±0）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 135**（この周は 1 個も使っていません）
- **アイテムは 87 種**、`MAX_ITEM_ID` は **120**
- **`AUTODEV-SPEC.md` は `状態: 未着手`（120 行ちょうど）** → **次の周は C（実装）**（下の「次にやること」）
- **`RULES-INBOX.md` は未取り込み 7 件**（前の 2 周ぶん。この周は 1 件も足していません）

## この周でやったこと（無人・B の周 1 種類だけ）

判定表どおり（キューの未着手 6 件 ≥ 3・`AUTODEV-SPEC.md` が `状態: 済`）→ **B の周**。
**`AUTODEV-QUEUE.md` の先頭「12. 種・小麦・パンと『育つブロック』」を仕様書にしました。**

**取る前にコードで数え直しました**（B の決まり）: `crop` / `wheat` / `小麦` / `bread` / `パン` と、
ワールドの種を除いた `seed` は `src/**` にも `test/**` にも 1 件もありません。**未実装です。**

**1 周で閉じない大きさだったので 2 件に割りました**（`AUTODEV.md` の B）。丸ごと書くと
`crops.ts`（位置ごとの育ち具合）+ ブロック 2 個 + アイテム 3 個 + `SaveData` の新しいキー +
`dimensions.ts` / `session.ts` / `storage.ts` の書き換えになり、**120 行の仕様書に収まりません**でした。

- **前半（今回の仕様書）= 小麦の種（アイテム 122）と小麦の苗（ブロック 121）。**
  草むらを壊すと 12.5% で種、種で耕地を右クリックすると上に苗が立つ。**まだ育ちません。**
  触るのは `blocks.ts` / `items.ts` / `use.ts` / `placing.ts` と `main.ts` の配線 12 行だけで、
  **セーブは 1 バイトも増えません**（苗は `edits` に乗るただのブロック）
- **後半 = キューの 13 番**（`crops.ts` と小麦・パン）。**12 番より先に取らないこと**と書きました

**仕様書で決めた形で、次の周が迷いそうな所を 2 つ**:

1. **苗は `variantOf: WHEAT_CROP`（自分自身）** —— アイテムを作らせないための唯一の入口が
   `variantOf` なのに、**苗には大元にできる相手が居ません**（土でも耕地でもない）。耕地の
   `variantOf: DIRT` とは事情が違うので、**落ちるもの（種 1 個）を `DROPS` に必ず書く**ことを
   禁じ手に入れてあります。書き忘れると「アイテムの無い番号」が地面に落ちます
2. **植えるのは `place` ではなく新しい `plant` の注文**（`use.ts`）。種の `block` は `AIR` で、
   可否（耕地の上か・塞がっていないか）は `placing.ts` の `tryPlant()` に置きます ——
   `tryTill()` とまったく同じ形なので、丸ごとヘッドレスで確かめられます

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は `src/**` を 1 行も触っていないので、撮っていません**（C-3 は実装の周のもの）。
前の周に撮った 2 枚（`docs/browser-shots/creative-hoes.png` / `farmland.png`）はそのままです。

**前の周から持ち越しで、まだ人にしか決められないもの**: **耕地の上面の色**（`0x59422d`）。
横と下は土のままなので、**横から見ると土と見分けが付きません。** 次の周で苗（緑）が上に立つと
見分けが付くようになるはずなので、**苗が入ってからもう一度見てもらうのが早い**と思います。

## 人がやること

- **`RULES-INBOX.md` の 7 件の取り込み**（前の 2 周ぶん。この周は足していません）。
  **わざと食い違わせてある 2 組**（「3 種類」対「4 種類」/「12 通り」対「13 通り」）は
  **新しいほうを採ってください。** 41 行目あたりの「未取り込みは 0 件です。」は古い行で、
  末尾の「未取り込み 7 件」が正しい数です
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**は
  まだ直っていません（`#status` は `bottom: 118px` 固定。`style.css:674`）。`REVIEW.md` に積んであります
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手は 7 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 未着手`**
なので → **次は C（実装）の周**です。**`AUTODEV-SPEC.md` を全文そのまま渡すこと**（要約しない）。

- **使う ID は 121（苗・ブロック）と 122（種・アイテム）。** `ROADMAP.md` の予約表の
  「121..255 予備」の先頭から上に詰めて取ります。**111 以降はブロックとアイテムで 1 本の
  番号列**なので、片側だけ見て取らないこと
- **`main.ts` は +12 行以内**（いま 1433 / 上限 1500。1450 を超えたら止まって人を呼ぶ線です）
- **既存のテストを 1 件だけ書き直します**（`test/blocks.test.ts` の「草むらを壊すと自分が
  手に入る」）。**草むらのドロップに確率が入るため**で、`rollDrop()` の 2 件に**割って増やす**
  こと —— 消したりゆるめたりしないこと。共有帯のアイテムの数も「9 個 → 10 個」と**増やす**だけ
- **見た目に出るので C-3 の撮影が要ります**（苗は緑の十字。`node tools/browsershot.mjs` で
  撮って **`Read` で開いて見る**こと。撮り方は `docs/browser-shots/README.md`）
- C-1 のサブエージェントは**同じ番の中で結果が返る呼び方**で。前の周の実測では
  `run_in_background: false` でも背景で起動したので、**転記ファイルに `"type":"result"` が
  出るまで前景で待つ**形にして受け取りました（13 分 23 秒）。**返らないなら自分で実装すること**

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（緑）。
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
