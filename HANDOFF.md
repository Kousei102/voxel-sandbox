# 引き継ぎメモ（2026-08-05 時点）

このファイルは**使い捨て**です。読んで頭に入れたら消してかまいません。
恒久的な決まりごとは全部 `CLAUDE.md` に書いてあるので、**まず `CLAUDE.md` を読んでください。**
ここにはそこに書けない「いまの状況」だけを書きます。

---

## 1. いまの状態

**インベントリの改修が完了しました。** モブに続く一区切りです。

```
6147ed1 インベントリ: シフトクリック・かき集め・数字キー・プレイ中の Q
435f443 引き継ぎメモの push 状況を実際の origin/master に合わせる
8a35a59 CLAUDE.md にインベントリ画面の節を足し、引き継ぎメモを書き直す
7eaa465 インベントリ: クラフト盤面と掴んでいる山をセーブに乗せる
4a3c978 インベントリ: 掴んでいるアイテムを捨てられるようにする
4df6ea8 インベントリ: 押したまま撫でてアイテムを配れるようにする
7019e11 インベントリ: 掴んだまま閉じるとカーソルにアイテムが残るのを直す
9aab068 インベントリ: 判定を craftscreen.ts へ切り出す（挙動は変えない）
b668ae0 インベントリ: swap を中身の入れ替えに揃える（挙動は変えない）
```

- `npm test` **778 件成功** / `npm run typecheck` / `npm run build` 通過
- **インベントリの 7 コミットはすべてユーザーがブラウザで確認済み**
- **`origin/master` の値は毎回 `git fetch` して確かめること。** 前のメモに書いてあった
  「1011f62 で 9 コミット遅れ」を引き写したまま出したら、実際にはモブまで push 済みでした
- インベントリの計画書（`/home/node/.claude/plans/snappy-sauteeing-cascade.md`）は**使い切りました。**
  残す価値のあることは `CLAUDE.md` の「インベントリ画面」の節に移してあります

### 入ったもの

1. **判定を `craftscreen.ts` へ切り出した。** 掴む・置く・クラフト・返却の判断と、
   盤面（`grid`）・掴んでいる山（`held`）の所有。`inventoryui.ts` は 242 行 → 描画と配線だけに。
   `mobs.ts` ↔ `mobrender.ts`、`sfx.ts` ↔ `audio.ts` と同じ対になりました
2. **ドラッグ配分。** 左＝均等、右＝1 個ずつ。撫でている間はプレビューが出ます
3. **破棄。** Q で 1 個、暗幕を左クリックで山ごと・右クリックで 1 個。**捨てたものは戻りません**
4. **盤面と `held` をセーブに。** 開いたままタブを閉じてもアイテムが消えなくなりました
5. **シフトクリック（一発移動・一括クラフト）・ダブルクリックのかき集め・数字キー・プレイ中の Q。**
   最初は意図的に外していましたが、あとから同じセッションで入れました

**操作はこれで Minecraft と同等です。** 足すときは `press()` の `MouseButton` を増やすのではなく、
`CraftScreen` に別の入口（`quickMove` / `gather` / `swapHotbar` のような）を足してください。

### 入れなかったもの（意図的）

- **盤面の並びの復元** — `craft` は「預かり物」で、読み込み時にインベントリへ返すだけです
  （3x3 のまま復元すると、次に 2x2 で開いた人が外周 5 マスを取り出せなくなります）
- **プレイ中にまとめて捨てる操作** — Q は 1 個だけにしてあります。落ちたアイテムの仕組みが
  無いので、歩きながらの押し間違いが永久の削除になります。落ちたアイテムが入ったら見直してください
- **結果スロットの数字キー** — カーソルの居場所を覚えるのはスロット（`build()` で作った div）
  だけで、`#craftout` には `mouseenter` を付けていません

### 次にやること

**セーブまわりをユーザーが別セッションでやりたいとのことです。** 下の 2 節に、
そのとき最初に見ることになる話を書いてあります。

それ以外の見取り図（テクスチャ・空腹・かまど・環境音・川と湖）は `CLAUDE.md` の
「これから入れるもの」を見てください。**着手する順はユーザーと決めること。**

---

## 2. 持ち越した課題：「保存データを削除」が効いていない

**まだ直していません。ユーザーが別セッションでやると決めました。**

`src/main.ts` の 2 つのボタンが、名前どおりに動いていません。

| ボタン | いまやっていること | やっていないこと |
| --- | --- | --- |
| 保存データを削除 | `clearSave()`（`localStorage` を消すだけ） | **メモリ上の持ち物・盤面に触らない** |
| 作り直す | 地形・時刻・体力をリセットし、最後に `saveNow()` | 同上。**メモリの持ち物をそのまま書き戻す** |

だから **「削除」→「作り直す」で持ち物が消えません。** 「削除」だけ押しても、
そのあと何かしてオートセーブが走れば同じように書き戻ります。

**これはもともとの挙動で、インベントリの改修による退行ではありません**
（持ち物は前からこの 2 つのボタンをまたいで持ち越されていました）。
盤面をセーブに乗せた（`7eaa465`）ことで、盤面も同じ扱いになっただけです。

直すときの選択肢はユーザーに出してあり、**まだ決まっていません**:

- 「削除」を完全リセットにする（＝押したあと何をしても書き戻らない）。
  「作り直す」は今のまま（地形だけ作り直して持ち物は残る）なので使い分けられる
- 「作り直す」も空にする（＝新しいゲームの開始にする）
- 盤面と手持ちだけ空にして、インベントリは今までどおり持ち越す

**どれを選ぶかで「作り直す」の意味が変わるので、必ずユーザーに決めてもらってください。**

---

## 3. セーブを触るときの下ごしらえ

- **`SaveData.version` は 1 のまま。** `load()`（`storage.ts:58`）が `version !== 1` を弾くので、
  上げた瞬間に**既存プレイヤーのワールドが全部読めなくなります。**
  キーを足すときは `time` / `creative` / `health` / `volume` / `craft` と同じ
  「省略可・無ければ既定」に揃えてください。`test/craftscreen.test.ts` が見張っています
- **アイテム ID とブロック ID を振り直さないこと。** `localStorage` の差分に ID がそのまま
  入っているので、振り直すと**別のブロックに化けます**
- インベントリは位置ベースの平坦配列（`[item, count, ...]` × 36 スロット）。
  **`INVENTORY_SIZE` を減らすと末尾が黙って消えます**（警告もフラグも出ません）
- `craft` は盤面 9 + 手 1 の 20 要素。読み込みは
  `inventory.deserialize` → `craft.deserialize` → `craft.returnAll()` の**順番厳守**
- **地形そのものは保存しません**（シードから作り直し、差分だけ持つ）。モブも保存しません

---

## 4. push について

**ユーザーに確認を取ってから push すること。** 公開サイトが差し替わる外向きの操作です。
push すると `.github/workflows/deploy.yml` が走り、`npm ci` → `npm test` → `npm run build`
を通ってから GitHub Pages（https://kousei102.github.io/voxel-sandbox/）に出ます。
**テストが落ちればデプロイされません。**

押したら `gh run watch <id> --exit-status` で Actions を見届けること。
そのあと配信中の JS のハッシュを `dist/assets/` と突き合わせると、実際に出たかまで確かめられます。

**フレーム時間の p99 判定（`world.test.ts`）はマシンが混むと落ちます。**
実測: テストを 2 本同時に走らせると p99 が 170ms まで跳ねました（上限 39ms）。
CI ランナーが混んでいると同じ落ち方をするので、落ちたら**中身の退行と決めつけず
再実行を試すこと**（フェーズ1 では実際にこれで止まりました）。

ブランチは切らず `master` に直接積んでいます（デプロイのワークフローが
`master` への push で走る設定のため）。

---

## 5. 実際に踏んだ落とし穴

同じ所でまた転ばないように残しておきます。

### インベントリで踏んだもの

- **テストの組み立てが間違っていると「機能が壊れている」ように見える。** ドラッグのテストで
  **手が空のまま `press()` していた**ので、掴むだけで終わってドラッグが始まらず、
  「配分が 1 個も効かない」形で 7 件落ちた。**先に掴んでから撫でること**
- **`#held` は `body` の直下**なので `#inventory` を隠しても一緒には消えない。
  `hide()` が `refresh()` を呼んでいなくて、掴んだまま閉じるとカーソルに表示が残っていた
  （移設前からの不具合。ユーザーがブラウザで見つけた）
- **確認項目に「アプリがもともと持っていない挙動」を書かないこと。** 上の 2 節の
  「削除したら盤面も空になる」は、こちらが勝手に前提にしていたもので、ユーザーの手を
  一往復ぶん取らせた。**確認をお願いする前に、その挙動が本当にあるかコードで確かめること**
- **リファクタのコミットに新しい状態機械を混ぜないこと。** ドラッグを Commit 1 に入れかけたが、
  `mouseup` の配線がまだ無い状態では「掴んだものが置けない」画面になっていた。
  **「挙動は変えない」と書いたコミットは、本当に変えないこと**
- **テストの期待値のほうが間違っていることがある。** かき集めで「半端な山から先に取ると
  満杯の山が無傷で残る」と書いたら落ちたが、正しくは**半端を空にしてから満杯を崩す**
  （手が上限に届くまで取るので、満杯側も必ず削れる）。落ちたときは実装だけでなく
  **期待値のほうも疑うこと**

### モブで踏んだもの

- **`>>` を float 座標に使うと負の側で 1 列ずれる**（`-0.5 >> 4` が 0 になる）。`columnOf()` を通すこと
- **崖回避は向きだけ変えても効かない。** 勢いも殺し、さらに「目標と向きがずれているあいだは
  前へ出さない」（`aligned = max(0, cos(...))`）が要る
- **モデルを当たり判定からはみ出させない**（壁の向こうへ鼻が突き抜けて見える）。
  **振る部位の箱は `y1 === 0`**（0 でないと足首・肘で回る）
- **テスト自身が偽陽性になりうる。** 「湧きを止めるつもりでプレイヤーを 300 ブロック先に置いた」ら
  モブがデスポーンして「更新されていないから落ちない」で通っていた。
  **先に「ちゃんと動いた」ことを確かめる判定を置くこと**（歩いた距離・個体数）。
  同じ筋で、確率つきドロップの検証を **1 体ごとに `seeded(...)` を作って**やったら、
  線形合同法の 1 個目が種に引きずられて 200 回全部同じ側に転び「確率 0.6 なのに 100%」で通った。
  **乱数は 1 本を回し続けること**
- **フレームの前後で体力を比べる形にしないこと。** モブのダメージは `updateVitals()` より
  前に入るので、控えを取った時点で差分が済んでいて、**ゾンビに殺されても死亡画面が出なかった。**
  いまは `vitals.takeDamage()` で 1 回だけ拾う形（順番に依存しない）
- **跳ぶだけでは壁を越えられない。** 壁に当たった時点で `moveBody` が横の速度を 0 にしていて、
  空中の加速では戻らないので、**跳ぶ瞬間に前へ出す速さを入れ直す**必要がある（`hopTimer`）
- **プレイヤーに掛かっている補正はモブにも要る。** 水中の 0.6 倍が抜けていて、
  ゾンビが水中でも陸と同じ速さで追ってきた
- **敵対モブのテストでプレイヤーを目の前に置かないこと。** 「殴る距離だから止まる」判定が
  毎フレーム効いて、モブが 1 歩も動かないまま通る
- **暗くしただけでは湧きは止まらない**（暗所はゾンビの湧き条件）。テストの試験場を静かにするには
  `quiet()`（sky 0 + block 15）を使うこと

---

## 6. 進め方（このプロジェクト特有）

**この環境では WebGL が動かず、音も鳴らせません**（詳細は `CLAUDE.md`）。なので:

- **見た目・音を変える変更は、1 まとまりごとに止めてユーザーに見て（聞いて）もらうこと。**
  未確認の変更を積み上げないでください。落ちたときに切り分けられなくなります
- 確認をお願いするときは、**具体的な確認項目を 4〜6 個の箇条書き**で出すと話が早いです。
  番号を振っておくと「5 だけ違う」と返ってきて、すぐ切り分けられます。
  **ただし、その挙動が本当にあるかコードで確かめてから書くこと**（上の落とし穴）
- **確かめられないものは、確かめられるものから切り離す。** これがこのプロジェクトの背骨です。
  いまの切り分け: GLSL は `terrainshader.ts` / `sky.ts` だけ、WebAudio は `audio.ts` だけ、
  DOM は `ui.ts` / `inventoryui.ts` だけ、モブの three は `mobrender.ts` だけ。
  残りは全部ヘッドレスで動きます。**新しく「確かめられないもの」を足すときは、必ず同じ形にしてください**
- 画面や耳でしか気付けない類は、数値に落としてテストにしています
  （面の裏返り → 発散定理で体積、置き方 → `placeSpot()` を直接呼ぶ、音 → 間隔と数値の妥当性、
  通知 → 要素の置き場所、モブとインベントリ → ソースを読む見張り + 状態機械の判定）

### デバッグに使えるもの

- `M` キー: 照準の先にモブを 1 体（**湧きの条件に関わらず**出せるので、
  「湧かない場所なのか、描けていないのか」を切り分けられます）。種類はランダムです
- `F3`: デバッグ表示にモブ数（`mobs N`）が出ます
- 開発サーバーは `npm run dev`（5173 が埋まっていると 5174 になります）

### 計測するとき

**`npm run bench` は 1 回では当てになりません。** 同じコードで `generateChunk` が
0.93〜1.46ms に振れます。疑わしいときは `git worktree add <絶対パス> HEAD` で比較対象を
用意し、`node_modules` を symlink して**同じ条件で交互に測ってください**
（終わったら `git worktree remove`）。

```
world.update    中央 3.4ms / 最悪 10〜17ms（GC 込み）
モブ 40 体      中央 0.042ms / p99 0.14ms   ← world.update の 9ms 予算に対して誤差
npm test        12 秒前後（778 項目）
```

**モブの費用は `world.update()` の外**に置いてあります（意図的）。中に入れると
`test/world.test.ts` の p99 に乗って、モブの退行とストリーミングの退行が区別できません。

---

## 7. 触るときに気をつける場所

`CLAUDE.md` に全部書いてありますが、特に壊しやすいのはこの 7 つです。

1. **`terrainshader.ts` の GLSL と `audio.ts` の WebAudio**。どちらもこの環境では
   確かめられません。**判断を持ち込まないこと。**
   壊れると「画面が真っ黒」「音が鳴らない」という形でしか出ません
2. **ブロック ID の 2 段の枠**。立方体を 64 以降に置く／64 以降にアイテムを持たせると、
   面の統合が壊れるか持てないブロックになります。**既存の ID を振り直すのも厳禁**
   （`localStorage` の差分が別のブロックに化けます）。
   **1..63 の空きは 26 個**、アイテムは 82（腐った肉）まで使用済み
3. **`Builder.quad()` は法線の成分の和で頂点の巡回順を決めています。** 斜めの法線は
   和が 0 になって面が裏返ります。**箱は軸に平行のまま作って `Object3D` で回すこと**
4. **`canPlaceAt()` と `breakUnsupported()` は対**（`world.ts`）。
   片方だけ直すと、壁の松明が空中に残ります
5. **`BlockDef` の既定値**。`sound` は `"stone"`、`blocksSky` は `opaque` と同じ、
   `boxes` は立方体。**書き忘れが「それらしく間違った状態」になる**ので、
   ブロックを足したらテストが出す一覧（ID の空き・音の材質）を必ず見てください
6. **`mobrender.ts` の `matrixAutoUpdate`**。チャンクの真似をして `false` にすると
   モブが全部原点で固まります（コメントを残してあります）
7. **`CraftScreen.activeGrid()` の参照共有**。コピーを返すと `consumeGrid()` が
   元の盤面に効かなくなり、**作れるのに材料が減りません**（画面を見ないと気付けない）

いずれもテストが見ています。壊してみて落ちることは確認済みです。
