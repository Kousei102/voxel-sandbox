# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-08・AUTODEV 62 のあと）

- `npm test` **3332 件すべて成功**（**+10**）・`npm run typecheck` 緑・`npm run build` 緑
- **`npm run bench` 3 回: generateChunk 0.88 / 0.95 / 0.75ms（中央 0.88）** ——
  `generateChunk` の内側に分岐が 1 つ増えましたが、**この箱の振れ幅の中**です（退行なし）
- **音の一群は 1 度も跳ねませんでした**（この周は 5 回走らせています。1 回目に出た
  赤 1 件は音ではなく**この周で書いたテストの側**で、下のとおり直しました）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行（±0。この周は 1 文字も開いていません）**
- **アイテム 118 種・`MAX_ITEM_ID` 156・111..255 の空きは 99・1..63 の空きは 9**（全部 ±0）
- **立方体 41 / 非立方体 78**・**モブ 9 種類**・**レシピ 60 本**・**精錬 7 本**・
  **燃料 8 種**・**食べ物 11 種**・**構造物 2**・**次元 3**・**`DamageCause` 9 種**（±0）
- **バイオームだけが 10 → 11**（凍った海）・**p99 9.9ms**（上限 35.0）
- **`AUTODEV-SPEC.md` は `状態: 済`（113 行）**・**`AUTODEV-QUEUE.md` 121 行**（未着手 **5 件**）

## この周でやったこと（AUTODEV 62 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**凍った海（25b）を実装しました。** 触ったのは **3 本だけ**:

- `src/biomes.ts` —— `FROZEN_OCEAN = 10` を **`BIOMES` の末尾に**足し、
  **`BiomeDef.seaSurface`（省略不可）**を 11 種類すべてに書き、
  `resolve()` を `snowy ? FROZEN_OCEAN : OCEAN` の **1 行**に
- `src/worldgen.ts` —— `wy === SEA_LEVEL ? seaSurface : wy < SEA_LEVEL ? WATER : …`。
  **`ICE` は import していません**（コメントに名前が出るだけ）
- `test/worldgen.test.ts` —— **+114 行・削除 0 行**（既存の判定は 1 つもゆるめていません）

**`main.ts` は 1 文字も開いていません**（F3 のバイオーム名は `biomeName()` が
`BIOMES` を引くので、予告どおり 0 行で増えました）。**ID は 0 個**です。

**仕様書のほうが 1 つ間違っていました**（詳細は `docs/autodev-log.md`）:
**「y40 = 氷 / y39 = 水 / y38 = 水」はそのまま書くと落ちます**（200 列中 67 マス不一致）。
**海の列は `height < SEA_LEVEL` なので、海底が y39 の浅瀬がある**からです。
直したのは**判定ではなく測り方**で、`heightAt` を引いて `h + 1` から `SEA_LEVEL` まで
舐める形にし、**「泳げる深さ（3 マス以上）の列が実際にある」を別の 1 件**に置きました。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので判定表の 2 行目です
（未着手は 5 件あるので A の周ではありません。**3 件を切ったら A**）。
**`AUTODEV-QUEUE.md` の先頭 = 26（フェンス）**の仕様書を書くこと。

- **取る前に、その 1 件だけコードで数え直すこと**（`grep -n 'FENCE\|フェンス' src/`）
- **ID は 157 から**（`ROADMAP.md` の予約表。**111 以降はブロックとアイテムで 1 本の番号列**）
- **フェンスは「隣と繋がる形」と「跳び越えられない当たり判定（1.5 マス）」の 2 つ**で、
  **`rules/blocks-shapes.md` と `physics.ts` の両方に当たります。**
  **120 行に収まらなければ、その場で 2 件に割ってキューへ書き戻すこと**（`AUTODEV.md` の B）
- **`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
  **`wc -l` を 119 以下に収めること**（`rules/testing.md`。3 回踏んでいます）
- **B の周は `src/**` に 1 行も差分を出さないこと**

## ブラウザで見てほしいところ

**この周は撮りました**（`node tools/browsershot.mjs` の 5 枚を更新済み。
`docs/browser-shots/game.png` と `debug.png` の中景に**淡い氷の板**が写っています）。
近くからの 2 枚は使い捨ての試し打ちで撮って `Read` で見ました（**リポジトリに差分は
残していません**）。**面の欠け・裏返り・段差・z ファイティングはどこにもありません。**

**新しく見てほしいところ（凍った海）**:

- **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の境目が読みにくい**です。
  数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、いちばん近い
  ガラスから 34.3 しか余裕がありません）。**気になるなら人が色を決めてください**
- **氷の板の上を歩けることが見て分かるか**（歩けます。`heightAt` は 1 マスも動いていません）
- **凍った海と凍っていない海の境目**（同じ高さで水 ↔ 氷にきれいに割れます。本家と同じ形）
- **氷ごしに海の底が見えるか**（`alpha` 0.6。**氷の下の水は明るいまま**です ——
  `blocksSky` を書かなかったので、氷ごしにスカイライトが通っています）

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
（理由は `TUNING.md`）。**凍った海が入ったので、これは今日から実地で試せます。**

**持ち越し（ケーキ）**: **上面 `0xffd0e4` が「ケーキ」に見えるか** /
**側面のスポンジ `0xe8c9a0` と下面 `0xd9b98a` の 3 色がケーキに読めるか**。
**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: **中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか** /
**素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか**。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか /
かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
**一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 3 件**）

**どれも `rules/worldgen.md` に `Edit` で据えました**（`.claude/rules/` ではなくルート直下）。

1. **海面 1 段に何を置くかは `BiomeDef.seaSurface`。** `worldgen.ts` は高さで引くだけで、
   **どのブロックかは `biomes.ts` が持ちます**（`ICE` を `worldgen.ts` に import しない）。
   **省略可（`?:`）にしないこと** —— 足し忘れが黙って `undefined` になります。
2. **海の列の深さは 1 マスから。「y39 も y38 も水」と決め打ちにしないこと**（上のとおり
   200 列中 43 列が 3 マス未満で、いちばん浅いのは 1 マス）。あわせて
   **`BiomeDef.surface` は「海底」であって海面ではない**ことと、
   **海のバイオームを増やしても砂と雪の隣接は増えない**（`resolve()` が必ず浜を挟む）
   ことを書きました。
3. **広いバイオームでは「先頭 N 列」を取らないこと。** 走査は x の小さい側から進むので、
   **先頭 200 列は同じ入り江の岸沿い**になります（±400 に凍った海は 155855 列）。
   **集めてから等間隔に間引くこと** —— **`npm test` は 21 秒のまま**でした。

## 人がやること

- **設計の判断（35 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**で、
  上限 1500 まで 51 行です。**次の 26（フェンス）に `main.ts` が要るかは B の周が数えます。**
  待っているのは **24b（ケーキをかじる。最低 7 行）** と **27b（革の防具。セーブのキーで
  1 行）** の 2 件です。`test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
  （**判定の意味が変わるので無人の周では触れません**）
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 45 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
**自然生成しないブロックは browsershot には 1 マスも写りません** ——
氷・ケーキ・クモの巣・本棚は `npm run shot -- <名前>` が唯一の経路です。
**凍った海は自然生成するので写りました**（予告どおり）。
**`tools/shot.ts` に場面が無いものを近くから見たいときは、scratchpad で
`tools/raster.ts` を直に叩けます**（この周で実際にやりました。リポジトリに差分は残りません）。
