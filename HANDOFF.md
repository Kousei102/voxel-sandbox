# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-07・AUTODEV 54 のあと）

- `npm test` **3250 件すべて成功**（3214 から **+36**）・`npm run typecheck` 緑・
  **`npm run build` も通しました**（`src/**` に差分がある周なので）
- **音の一群は 1 回も跳ねませんでした**（この周は 3 回走らせて 3 回とも緑）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（**形も ±0**。
  防具のセーブは 27b へ回してあります）
- **`main.ts` 1449 行**（`wc -l`。テストの数え方では 1450。上限 1500・
  **止まる目安 1450 に届いたままです。この周も ±0 で、1 文字も開いていません**）
- **アイテム 115 種・`MAX_ITEM_ID` 153・111..255 の空きは 102・1..63 の空きは 9**
  （**この周は ID を 1 つも取っていません**。予約表の次の空きは 154）
- **立方体 40 / 非立方体 76**（±0）・**モブ 9 種類**・**レシピ 59 本**・
  **精錬 7 本**・**燃料 8 種**・**食べ物 11 種**・**バイオーム 10**・**構造物 2**・
  **次元 3**・**`DamageCause` 9 種**（どれも ±0）
- **`AUTODEV-SPEC.md` 110 行**（`状態: 済`）・**`AUTODEV-QUEUE.md` 108 行**（上限 150）

## この周でやったこと（AUTODEV 54 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
仕様書（27a・防具の枠と防御の計算）を**全文そのまま**実装しています。
**サブエージェントは立てず、親が実装して親が `git diff` を点検しました**
（`AUTODEV.md` の C-1 / C-2 の「返らないなら自分で」。`rules/testing.md` の `git stash` の件）。

**足したのは 2 つだけで、着られる物は 1 つもありません**（革の 4 部位は 27b）:

- **入れ物** —— `inventory.ts` の `armor` 4 枠（`ARMOR_SIZE` / `ARMOR_SLOTS`）と
  `armorPoints`。**36 スロットとは別の配列**なので、拾ったものは 1 山も入りません
- **防具点でダメージを減らす判断** —— `vitals.ts` の `armorReduced()`（純関数）。
  `damage()` はそれを 1 行呼ぶだけで、**`cooldown` / `iframe` / `exhaustion` /
  `hurtFlash` / `cause` は 1 文字も動いていません**

**判断は仕様書のとおり 3 つに割れています**: どの部位が何点か = `items.ts` の
`ARMORS`（**空の `Map`**）/ いま合計何点か = `inventory.ts` の `armorPoints`
（**部位が枠の並びと合っているときだけ足す**）/ どれだけ減るか = `vitals.ts`。
**`vitals.ts` は `items.ts` も `inventory.ts` も import していません**（見張りを 1 件足しました）。

**数値は本家のまま `amount * (1 - min(点, 20) / 25)`**（20 点で 8 割減）。
効くのは落下・モンスター・溶岩・炎上・サボテン、効かないのは溺れ・空腹・毒・奈落の
**9 種を `Record<DamageCause, boolean>` に 1 行ずつ**（死因が増えたら `tsc` が落とします）。

**仕様書に無かったが足したもの 2 つ**（どちらも「防具点は数値でしか持たない」の裏取り）:
**`respawn()` が `armor` を 0 に戻す**（死ぬと防具枠も落とすので、湧き直した瞬間は必ず裸）と、
**`takeAll()` が防具枠も落とす**（あわせて **`deserialize()` は 36 個だけを消す**ように直しました）。

**`AUTODEV-SPEC.md` が名指しした `test/items.test.ts` は存在しなかったので作りました。**
**`test/run.ts` に import と呼び出しの 2 行**も足しています（`rules/testing.md` に据えました）。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので判定表の 2 行目です
（未着手は 7 件あるので A ではありません）。**ただし下の「人がやること」を先に見ること** ——
**キューに残る 7 件はどれも `main.ts` に配線が要ります。**

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません。見た目には 1 ドットも出ないからです** ——
`*render.ts` / `ui.ts` / `inventoryui.ts` / GLSL / `main.ts` に差分が 0 で、
`dist/` に出るのは `inventory.ts` と `vitals.ts` の中身だけです（仕様書の（7）も
「撮る必要はありません」と書いています）。**着られる物が 1 つも無いので、
遊んでいて気付ける変化も 0 です**（防具点はどう転んでも 0 で、入るダメージは
1 桁も変わっていません）。

**人に見てほしいのは 2 つ（前の周から持ち越し）**: **金のリンゴの色 `0xf0a800` が
一覧で金インゴット・ブレイズロッド・ブレイズパウダーと見分けられるか**（数値では
56.4 離れていますが、どれも「金色」なので並ぶと紛らわしいかもしれません）と、
**金インゴット 8 個 + リンゴ 1 個に「体力 4 回復」が見合うか**（本家と同じ材料・
同じ合計量ですが、**持続する効果の器が無いので本家の「再生 II が 5 秒」を即時
4 回復に均してあります** —— 戦闘中の手ざわりが本家と別物です。`TUNING.md`）。

**27b（革の防具）を実装したら見てほしくなる 1 つ**: **防具点 7 で 8 割ぶんしか減らない
（10 → 7.2）のが「着た手ごたえ」として分かるか。** 本家は残り 2 割をエンチャントで
削りますが、**経験値もエンチャント台も無いので 8 割が上限**です（`TUNING.md`）。

**持ち越し（まだ誰も見ていません）**: **本棚が「本の詰まった棚」に見えるか**
（側面 `0x9c5064` がかなり赤紫）/ **紙 `0xd4e0ec` が雪玉と 25.5 で見分けられるか** /
**本棚 1 個に材料 9 個が見合うか** / **リンゴの色 `0xe0342c` が赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か** / **地面に落ちた
リンゴが赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか** /
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか**（しゃがみで
止まる手はありません）/ **はしごの上端から地上へ出る手触り** / **はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に
見えるか** / **`CANE_GROW_SECONDS = 180` が待てる長さか** / **自然に生えたサトウキビは
伸びません**（理由は `TUNING.md`）/ **⚠ 前から遊んでいる世界だけの見え方**: 前に浜の
サトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて見えることがあります /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度 / サボテンに
当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件**・どちらも `rules/` に据えました）

- **新しいテストファイルは `test/run.ts` に 2 行足さないと 1 件も走りません**
  （`rules/testing.md`）。あそこは手で並べた一覧で探して回る仕掛けが無く、
  **書いたのに走っていないという壊れ方は `typecheck` でも `npm test` でも止まりません**
  （**件数が増えないことだけが手がかり**です）
- **`Inventory` に 2 本目の `Slot[]` を足したら、「全部空にする」経路を数え直すこと**
  （`rules/inventory-screen.md`）。`clear()` / `takeAll()` / `deserialize()` は
  **どれも「全部」と読めますが意味が 3 つとも違い**、`deserialize()` が `clear()` を
  呼んだままだと、**防具を別のキーで読み戻す順番によっては着ていたものが黙って消えます**
  （`craft` → `craftWear` → `returnAll()` の順が要るのと同じ罠。**27b で効きます**）

あわせて `rules/vitals.md` に「防具」の 1 節を足しました（判断が 3 つに割れていること・
`ARMOR_APPLIES` を省略可にしないこと・8 割が上限である理由・`respawn()` が 0 に戻すこと）。

## 人がやること

- **設計の判断を 1 つお願いします（27 周持ち越し。もう待てません）:
  `main.ts` を 2 つに割ってよいか。** いま **1449 行で、止まる目安 1450 に届いています**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **27a は予告どおり ±0 行で通りましたが、これが最後の 0 行の 1 件でした** ——
  **キューに残る 7 件（23 クモの巣・24 ケーキ・25 氷・26 フェンス・27b 革の防具・
  11 スケルトン・12 刈られた羊）はどれも `main.ts` に配線が要ります**
  （27b も防具のセーブで `currentSave()` に 1 行）。
  **つまり、次の B の周は仕様書を書けても、その次の C の周が止まります。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 37 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
