# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-17・AUTODEV 95（C の周）のあと）

**この周は C（実装）の周**でした（判定表の 3 行目 —— `AUTODEV-SPEC.md` が
`状態: 未着手` だったため）。**34b「ツタが下へ垂れる」を実装して `master` へ
push しました**（1 周 1 コミット = 1 デプロイ）。

- **ツタ（183..186）に「2 つ目の支えの候補 = 真上の同じツタ」を足しました。ID は 0 個。**
  - `BlockDef.hangsBelow` の旗（**`VINE_OPTS` に 1 行 = 4 向きぶん**）と、
    **`SUPPORT_FACES` の表**（`SUPPORT_FACE` から立てる。ツタだけ `[壁, FACE_YP]`）
  - **`World.canPlaceAt()` は `supportFaces()` の for**（どれか 1 つを満たせば置ける）、
    **`World.breakUnsupported()` は落とすかどうかを `canPlaceAt()` に聞く**形に。
    **置く側と壊す側が同じ関数**になったので、連鎖は 1 行も書いていません
  - **`vineVariant(face, supporter = AIR)`** が `FACE_YP` だけ表を引かず、
    **真上のツタと同じ向きをそのまま写します**（垂れた列の途中で板の側が入れ替わらない）
  - **`supportHint()` / `canSupport()` / はしごの表は 1 文字も触っていません**
- **触ったのは 6 ファイル**（仕様書の表どおり）: `src/blocks.ts` / `src/world.ts` /
  `test/arena.ts` / `test/blocks.test.ts` / `test/placing.test.ts` / `tools/shot.ts`。
  **`main.ts` は 1 文字も開いていません**（1450 行のまま）。`worldgen.ts` も
  `biomes.ts` も `items.ts` も `crafting.ts` も `placing.ts` も ±0 行
- **点検（C-2）は親が `git diff` を読んで自分でやりました** —— 立てた点検役の
  サブエージェントが**背景（async）で起動してしまった**ためです（`AUTODEV.md` の
  「返らないなら自分で」）。**待たずに記録まで進め、あとから返った報告を
  取り込みました**（背景のぶんが 1 件見つけました。下の ⚠）

## 数字（2026-09-17・この周で測ったもの）

- `npm test` **3729 件すべて成功**（3710 → +19。**判定を 1 件もゆるめていません**）。
  `npm run typecheck` 緑 / `npm run build` 緑。**`npm run bench` は走らせていません**
  （生成もメッシュ化も ±0 行。`test/world.test.ts` のフレーム時間は
  **p99 10.0ms / 上限 36.5ms**、中央 3.1ms で通っています）
- **音の跳ねが 1 回**: **「声色（音程の倍率）」**が 1 回目だけ赤（0.329 > 4.260）で、
  **2 回目以降は緑**。`rules/testing.md` のとおり走らせ直しただけです
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1450 行 / 上限 1500**
- **アイテム 145 種・`MAX_ITEM_ID` 186**（±0）・**立方体 42 / 非立方体 89**（±0 ——
  **ブロックを 1 つも増やさずに機能が増えた周**）
- **1..63 の空きは 7**・**111..255 の空きは 69**（どちらも ±0。**次に取るのは 187**）
- `AUTODEV-QUEUE.md` **117 行 / 上限 150・未着手 4 件**（34b を消した）・
  `AUTODEV-SPEC.md` **120 行 / 上限 120・`状態: 済`**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` で、未着手が 4 件あるので
判定表の 2 行目です。**キューの先頭は 34c「ツタの自然生成」**（`worldgen.ts` と
`biomes.ts` だけ・**ID 0 個**）。

- **34b のぶらさがりはもう効きます** —— だから 34c は「葉の下に 1 マス置いて、
  そこから下へ伸ばす」だけで済みます（**`World.setVoxel()` は `canPlaceAt()` を
  通るので、生成でも上から順に書くこと**）。**`worldgen.ts` が `setVoxel` ではなく
  列へ直に書く経路なら `canPlaceAt()` を通らない点を、仕様書で名指しすること**
- **仕様書は 119 行に収めること**（`wc -l` より 1 多く数えられます。前の周の記録）
- **`src/**` を 1 行も触らない周です**（B の完了条件）

## ブラウザで見てほしいところ

**この周は `shots/vine.png` と `shots/terrain.png` を撮って自分の目で見ました。**
**ひさしの下に緑の帯が 3 マスぶら下がって、下 2 マスの後ろは空**（地面まで届いて
いない）ことと、**面の欠け・裏返りが 0 件**をそこで確かめています。人に見てほしいのは:

- **⚠ 垂れたツタの手ざわり（34b・新しい）**: **宙にぶら下がった 1/16 の板が
  「ツタ」に見えるか**（絵では浮いて見えます）/ **ぶら下がりに上限が無い件** ——
  本家と違って**何マスでも垂らせます**（本家も手で置くぶんは無制限ですが、
  自然生成の長さは決まっています。**長さを決めるのは 34c の仕事**）
- **⚠ 薄い板の横・真上に石があると置ける件（34b で開いた道。人の判断）**:
  **ツタやはしごの横を狙っても、真上に固いブロックがあれば置けます**
  （34a までは `blocked`）。**支えは天井なので落ちはしませんが、板は狙った面の側に
  貼り付いて見えます。** 現状は `test/placing.test.ts` に 1 件書き留めてあります ——
  **直すなら「向きを決める側にその壁が支えになれるかを渡す」**話で、
  `vineVariant()` に `canSupport()` を足すと**支えを渡さない既存の呼び方が
  全部 `AIR` になる**ので 34a の判定にも手が入ります（**それだけで 1 周**）
- **⚠ 石の天井の下のツタ**: **手では置けないのに、あれば落ちません**
  （`canSupport()` をゆるめない代わりにできた非対称。本家は下面にも置けます。
  直すなら `supportsBlock()` の 1 行を `return baseBlock(...) === baseBlock(...)` に。
  `rules/blocks-shapes.md` に両方の道を書いてあります）
- **⚠ ツタ（34a）の手ざわり 3 つ**: **登る速さがはしごと同じでよいか**
  （`LADDER_CLIMB_SPEED` を共有）/ **硬さ 0.2 が「すぐちぎれる」と感じられるか**
  （剣で 0.30 秒）/ **壁に貼った緑が「ツタ」に見えるか**（`0x306d18`・本家の値）
- **⚠ 金・ダイヤの防具の色（33b）**: **金 4 段が「金」に・ダイヤ 4 段が「ダイヤ」に
  見えるか**。**金の上着 `0xefd714` とズボン `0xf8b317` は絵の上でやや近く見えます**
- **⚠ ダイヤ一式 80% 減が強すぎないか。** **防具は壊れない**ので、
  **「上限の固さが永久に持つ」状態**です（本家は傷んで直す手間があります）
- **⚠ 金を作る理由があるか。** **金一式 11 点は鉄 15 点より弱いのに 24 枚要ります**
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ 鉄の防具の色（33a）**: **「鋼の青」が鉄に見えるか** / 鉄一式 60% 減の手ざわり
- **レンガ（32b）**: `0xbc5d39` が「焼いたレンガ」に見えるか（ブロックとは 26.0）
- **粘土（32a）**: 海に潜って見つけられるか（海底の列の 7.2%）/ 粘土玉がやや青紫寄り
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか / **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らないのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白** / **氷（25a）**: 滑りの手ざわり
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 2 件**）

- **`rules/blocks-shapes.md`**: **支えの向きが 2 つ以上あるものは `supportFaces()` の
  表から引くこと。** `stacksOnSelf` は代わりになりません（`supportFace` の向きの
  自分を見る）/ **例外は `face` を必ず見る**（見ないと横に貼り付く）/
  **`return true` で足すと `canSupport()` が通る支え（石の天井）も残る**ので、
  **「手では置けないのに落ちない」の非対称ができる**（狭める道も書いてあります）/
  **壊す側は落とすかどうかを `canPlaceAt()` に聞く**（置く側と同じ関数になる）/
  **`PlaceContext` に足す材料は `?:` で省略可に**（`BlockDef` とは逆。必須にすると
  `placedVariant()` を呼ぶ既存の呼び出しが一度に落ちます）
- **`rules/meshing-render.md`**: **「宙にぶら下がっている」を撮るには、柱から
  ひさしを 1 マス出して、真横に支えが無い所へ垂らすこと**（柱の面に垂らすと
  壁掛けと 1 画素も区別が付きません）。**`World.setVoxel()` は `canPlaceAt()` を
  通るので、場面は上から順に置くこと**（下から置くと 1 マスも書けません）

**`RULES-INBOX.md` は ±0 行**（層 3 = スキルへ据えるものは 1 件も出ませんでした）。

## 人がやること

- **⚠ 設計の判断（66 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**）。
  **34c（次のタスク）は `main.ts` 0 行の見込み**なので、判断が下りなくても進みます
- **⚠ 石の天井の下のツタの非対称**（上の「ブラウザで見てほしいところ」）。
  **本家は下面にも置けます**が、置く向きの表を変えると `vineVariant()` が
  「どの向きの板を貼るか」を決められません（天井には向きがありません）——
  **本家は下面用の見た目を持っています。** 足すなら**番号 1 個**の話です
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
  （足すなら `vitals.ts` → `inventory.ts` の配線で、**それだけで 1 周**）
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周も「声色」が
  1 回跳ねました**（2 周続けて同じ判定です）。**直すなら測り方**（窓を長く取る・
  複数回鳴らして中央値）で、**判定はゆるめないこと**。**キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 95 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと**。
**⚠ 枠を数えるときは `#creative .slot` を使うこと**（素の `.slot` はホットバーと
インベントリの 36 枠まで拾います）。**⚠ `/opt/pw-browsers` には
`chromium_headless_shell-1194` も居ます** —— `startsWith("chromium-")` で弾くこと
（`tools/browsershot.mjs` の `findChrome()`）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質・**ツタ**がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**34c で自然に生えるようになったら、
場面 `vine` に「森の木から垂れているところ」を足すこと**（いまの場面は**石の柱と、
そこから出したひさしに手で掛けただけ**です）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**⚠ 防具を着た姿は撮れません**（一人称視点なので自分の体が写りません）。
