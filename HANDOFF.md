# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-03）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は無人の C（実装）の周**でした。**1 タスク = 1 コミット**で、実装（C-1）・
  点検（C-2）・撮る（C-3）・記録（C-4）を全部この周でやっています
- `npm run typecheck` / **`npm test` 2852 件すべて成功**（前の周の 2818 から **+34**）/
  `npm run build` も通っています
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **`src/main.ts` は 1450 行 / 上限 1500**（**この周も 1 行も触っていません**。
  停止条件 3 の線にちょうど乗ったままです）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 128**（**この周は 2 個使用**）
- **アイテムは 92 種**、`MAX_ITEM_ID` は **127**。**モブは 7 種類**。
  `SaveData` は `version` 1 のままで、**1 バイトも増えていません**（モブは保存しません）
- **`AUTODEV-SPEC.md` は `状態: 済`**（120 行 / 上限 120）、`AUTODEV-QUEUE.md` の未着手は
  **6 件** → **次の周は B（仕様）**
- **`RULES-INBOX.md` は未取り込み 6 件**（この周が 3 件足しました。下の「人がやること」）
- `REVIEW.md` の未着手は 0 件

## この周でやったこと — AUTODEV 16「鶏」

**草地に鶏が湧いて、倒すと生鶏肉が 1 個出て、かまどで焼くと焼き鳥になります。**
**受動モブが 3 種類目**になり、豚以外にも肉の出どころができました。
詳しくは `docs/autodev-log.md` の「16. 鶏」。

**触ったのは `src/` の 3 ファイルだけ**です:

- `mobs.ts` —— `MobKind` に 1 語 / `CHICKEN` の `MobDef` 1 つ / `MOBS`・`MOB_KINDS`・
  `PASSIVE_KINDS` に 1 語ずつ
- `items.ts` —— `RAW_CHICKEN = 126` / `COOKED_CHICKEN = 127` / `MAX_ITEM_ID` の差し替え /
  `item({...})` 2 行 / `FOODS` 2 行（**生 2 / 1.2・焼き 6 / 7.2・毒なし**）
- `smelting.ts` —— `SMELTING` に 1 行

**`main.ts` / `mobmesh.ts` / `mobrender.ts` / `use.ts` / `vitals.ts` / `crafting.ts` /
`blocks.ts` / `drops.ts` / `craftscreen.ts` / `inventoryui.ts` / `storage.ts` は
1 行も触っていません**（`git diff --numstat` で確かめました）。

**いちばん確かめたかった「モブを 1 種類足すのに描画が何行要るか」の答えは 0 行**でした
（前の B の周の見込みどおり）。形の点検（体積・巡回順・振る部位の `y1 === 0`・当たり判定に
収まるか）は `MOB_KINDS` を回す `test/mobs.test.ts` に**11 件が 1 行も書かずに増えています。**

**踏んだもの 2 つ**（次の周のために）:

1. **`test/mobs.test.ts` には「種類を足すと壊れる決め打ち」が 2 か所ありました。**
   `census()` の `counts`（名前を足し忘れると `counts[name]++` が **`NaN`**）と、
   湧きの「両方の種類が湧く」の **`=== 2`**。後者は**表から数える形**に替えたうえで、
   `=== 2` が拾えていた「余計な種類が混じらない」を**別の 1 件として足しました**
   （替えただけだと、明るい草地に敵対が湧いても緑になる形にゆるみます）
2. **`MAX_ITEM_ID` を差し替えると `npm run typecheck` が `test/blocks.test.ts` を止めます。**
   これは良い止まり方です —— 共有帯の数え直しを忘れたまま緑になりません

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は絵を撮って自分の目で見ています**（`docs/browser-shots/README.md` の 13 枚目）。
定番 5 枚を本物のブラウザで撮り直し（WebGL 2.0・console のエラー 0 件）、
**`npm run shot -- mobs` で全 7 種類を 1 列にした `docs/browser-shots/mobs.png`** を足しました。
**鶏を 10 倍に切り出して見て、面の抜けも埋まりもないこと**と、**羊の隣でも見分けが付くこと**
（とさかの赤・くちばしと脚の橙）を確かめてあります。

**絵を見て 1 つ直しました**: 体の幅が翼と同じ ±3px で、**白い体に白い翼が埋まって
振れても見えませんでした**（体積も当たり判定も全部緑のまま）。体を ±2.5px に狭めています。

それでも人の目で決めてほしいのはこの 3 つです:

1. **鶏が「鶏らしいか」**（豚・羊と並べて。撮った絵は真正面から動かない画なので、
   **歩いて翼が振れているところ**は見ていません）。声 1.8 の高さも耳で
2. **パンの茶色（`0xc49a5e`）が一覧の中で目立つか**（前の周からの持ち越し。
   板・原木・棒の茶色と近い系統です。`E` を押して末尾へ）
3. **苗の緑（`0x6f8f3f`）が草原に紛れる**件と、**`GROW_SECONDS` 180 秒が待てる長さか**
   （どちらも `TUNING.md`）/ **`#status`（オートセーブの帯）の位置**（狭い窓でも）

## 人がやること

- **`RULES-INBOX.md` の 6 件を取り込むこと**（`.claude/rules/` は無人の周が触ると
  確認待ちで止まるため）。**うち 3 件がこの周のぶん**で、**2 件は前の周の本文と
  同じ場所を指しています** —— 決まりのとおり**古いほうは直していません**ので、
  取り込む人が**新しいほう（鶏の周）を採ってください**:
  - `mobs.md`（**この周・食い違いなし**。前の節の続きに足す）
  - `vitals.md`（**食い違いあり**。食べ物が 4 つ → 6 つになり、強さの並びが変わりました）
  - `items-survival.md`（**食い違いあり**。モブのドロップの並びが古いまま）
- **上の「ブラウザで見てほしいところ」の 1〜3**
- **鶏の数値でよいか**（体力 4 / 湧きの重み 10 は本家、速さ 1.7 と声 1.8 は目分量）。
  **生鶏肉を毒なしにしたのはこちらの判断**です —— 本家の「30% で食中毒」は `FoodDef` に
  確率が無いので表せず、足すと `vitals.ts` まで及びます（`TUNING.md` の「鶏」）
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **6 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 済`**
なので → **次は B（仕様）の周**です。**`src/**` と `test/**` に 1 行も差分を出さないこと**
（B の完了条件）。

- **先頭は「7b. 羽根と、矢を本家の形へ」**。**取る前にその 1 件だけコードで数え直すこと**
  （キューは数周前の A が作ったものです）。**`MobDef.drop` は 1 つしか落とせない**ので、
  **2 つ目の落とし物（`extra` か配列）を足す話が先に要ります** —— そこが仕様書の要です。
  ブロックの `Drop.extra`（`items.ts`。実った小麦が使っています）が**先例**なので、
  **同じ形にできるかを先に見ること**（できるなら `mobs.ts` の `dropFor()` 1 本の話で済みます）
- **120 行に収まらない仕様は 1 周で閉じない信号**です。その場で 2 件に割って
  `AUTODEV-QUEUE.md` へ書き戻し、**前半だけ**を仕様書にすること
- **仕様書の 7 項目を 1 つでも欠かさないこと**（欠けたぶんは、そのまま次の C の周の
  勝手な判断になって返ってきます）。**先頭に `状態: 未着手` と `差し戻し: 0 回` の 2 行**
- **ID は `ROADMAP.md` の予約表から。次の空きは 128 です**（126 / 127 は鶏の肉が取りました）。
  **111 以降はブロックとアイテムで 1 本の番号列**なので、片側だけ見て取らないこと
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **`npm run typecheck` / `npm test` は自分で走らせること**（報告を信じない）

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に 1 件だけ赤 →
走らせ直したら緑。**この周は 5 回とも緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
