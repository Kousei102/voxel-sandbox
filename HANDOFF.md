# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-07・AUTODEV 53 のあと）

- `npm test` **3214 件すべて成功**（±0。**`src/**` も `test/**` も 1 行も触らない
  B の周**）・`npm run typecheck` 緑
- **`npm run build` は走らせていません**（`src/**` に 1 行も差分がありません）
- **音の一群が 1 回跳ねました** —— 落ちたのは**「音量がそのまま大きさに掛かる（5 倍）」**で、
  音量 0.2 のピークが `1.479e+34`・倍率 0.00。**次の 1 回で緑に戻りました**（`rules/testing.md`
  に据えました。**明るさの件だけでなくピークの件も跳ねます**）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1449 行**（`wc -l`。テストの数え方では 1450。上限 1500・
  **止まる目安 1450 に届いたままです。この周も ±0 でした**）
- **`AUTODEV-SPEC.md` 110 行**（テストの数え方では 111。上限 120）・
  **`AUTODEV-QUEUE.md` 116 行**（上限 150）
- **アイテム 115 種・`MAX_ITEM_ID` 153・111..255 の空きは 102・1..63 の空きは 9**
  （**予約表の次の空きは 154**）
- **立方体 40 / 非立方体 76**（どちらも ±0）・**モブ 9 種類**・**レシピ 59 本**・
  **精錬 7 本**・**燃料 8 種**・**食べ物 11 種**・**バイオーム 10**・**構造物 2**・
  **次元 3**・**`DamageCause` 9 種**

## この周でやったこと（AUTODEV 53 の B・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 済`」で 2 行目 = B（仕様）の周**でした
（未着手は 8 件あるので A ではありません）。**`src/**` と `test/**` は 1 行も触っていません。**

**キューの 27 を 27a / 27b に割り（キュー自身が「B の周で割ること」と書いていました）、
27a を先頭へ動かして、その仕様書を書きました。**

**先頭を入れ替えた理由**（`AUTODEV.md` の A-2。詳しくは `AUTODEV-QUEUE.md` に 1 節）:
A-2 の **1（確かめられるものが先）と 2（枠を食わないものが先）がどちらも 27a を上に置きます**
—— **27a は判断だけで画面も音も新しい形も使わず、ID を 1 個も使いません**。対して
23（クモの巣）は新しいブロックの見た目と ID 1 個が要ります。ジャンルの順（A-2 の 3）だけが
23 を上に置きますが、**それは 1 と 2 より下**です。あわせて 23〜26 は
**「`main.ts` に配線が要る＝人の判断待ち」という依存が行に書けていませんでした**。

**取る前にコードで数え直しました**（B の決まり）: **`armor` / `防具` は `src/**` に
1 か所も実装がなく**、当たるのは `vitals.ts` と `items.ts` の「防具がまだ無いので」という
コメントと、テスト 3 本のコメントだけでした。**クモの巣（`cobweb`）も 0 件**です。

**仕様書で決めた形**（7 項目そろっています）:

- **ID 0 個。`main.ts` に 0 行**（止まる目安 1450 に届いているため。これが 27a を選べた理由）
- 判断は 3 つに割りました: **どの部位が何点か = `items.ts` の `ARMORS`**（`FOODS` /
  `foodOf()` を写した空の `Map`。27b が 4 行足すだけで済む）/ **いま合計何点か =
  `inventory.ts` の `armorPoints`**（部位と枠の並びが合っているときだけ足す）/
  **どれだけ減るか = `vitals.ts` の `armorReduced()`**（純関数。`vitals.ts` は
  `items.ts` も `inventory.ts` も import しません）
- 数値は本家のまま **`amount * (1 - min(点, 20) / 25)`**。**効くのは
  落下・モンスター・溶岩・炎上・サボテン、効かないのは溺れ・空腹・毒・奈落**の 9 種を
  `Record<DamageCause, boolean>` に 1 行ずつ（死因が増えたら `tsc` が落とします）
- **セーブは 27b へ回しました。** 書き出しは `main.ts` の `currentSave()` が
  `buildSave()` へ値を並べる形なので、**キーを 1 本足すと `main.ts` に 1 行要ります**。
  **27a では防具枠がずっと空なので、足しても `[0,0,0,0,0,0,0,0]` しか書けません**
  （`inventory` のキーに 8 要素を継ぎ足す逃げ道は禁じ手にしました。`wear` を
  別キーにしたのと同じ理由）

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が **`状態: 未着手`** なので、判定表の
3 行目に当たります。**`AUTODEV-SPEC.md`（27a・防具の枠と防御の計算）を全文そのまま**
渡して実装すること（要約しないこと）。**渡す層 2 は `rules/vitals.md` /
`rules/inventory-screen.md` / `rules/items-survival.md` / `rules/drops.md` /
`rules/testing.md` の 5 本**（仕様書の（2）に並べてあります）。
**`main.ts` に 1 行でも要ると分かった時点で止まって人を呼ぶこと**（下の「人がやること」）。

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません** —— `src/**` に 1 行も差分が無い B の周で、
`dist/` は前の周（AUTODEV 52）から 1 バイトも変わりません。**次の C の周（27a）も
見た目には 1 ドットも出ません**（DOM の枠は 27b）ので、撮る必要はありません。

**人に見てほしいのは 2 つ（前の周から持ち越し）**: **金のリンゴの色 `0xf0a800` が
一覧で金インゴット・ブレイズロッド・ブレイズパウダーと見分けられるか**（数値では
56.4 離れていますが、どれも「金色」なので並ぶと紛らわしいかもしれません）と、
**金インゴット 8 個 + リンゴ 1 個に「体力 4 回復」が見合うか**（本家と同じ材料・
同じ合計量ですが、**持続する効果の器が無いので本家の「再生 II が 5 秒」を即時
4 回復に均してあります** —— 戦闘中の手ざわりが本家と別物です。`TUNING.md`）。

**持ち越し（まだ誰も見ていません）**: **本棚が「本の詰まった棚」に見えるか**
（側面 `0x9c5064` がかなり赤紫）/ **紙 `0xd4e0ec` が雪玉と 25.5 で見分けられるか** /
**本棚 1 個に材料 9 個が見合うか** / **リンゴの色 `0xe0342c` が赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か** / **地面に落ちた
リンゴが赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか** /
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか**（しゃがみで
止まる手はありません）/ **はしごの上端から地上へ出る手触り** / **はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に
見えるか** / **`CANE_GROW_SECONDS = 180` が待てる長さか** / **自然に生えたサトウキビは
伸びません**（理由は `TUNING.md`）/ **⚠ 前から遊んでいる世界だけの見え方**: 前に浜の
サトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて見えることがあります /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度 / サボテンに
当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**）

**音の一群で跳ねるのは「明るさ」の件だけではありません** —— この周は
**「音量がそのまま大きさに掛かる（5 倍）」**が落ちました。跳ねたのは明るさではなく
**ピークで、音量 0.2 のピークが 0.0525 → `1.479e+34`**（倍率が 5.00 ではなく 0.00 と出ます）。
`src/**` と `test/**` に 1 行も差分の無い周なので退行ではなく、**次の 1 回で緑に
戻りました**。`rules/testing.md` の「揺れる一群」に 3 例目として据えました
（**赤が出たらまずもう一度走らせる**。同じ件が続けて落ちたときだけ退行）。

## 人がやること

- **設計の判断を 1 つお願いします（26 周持ち越し。もう待てません）:
  `main.ts` を 2 つに割ってよいか。** いま **1449 行で、止まる目安 1450 に届いています**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **20・21・22 は予告どおり ±0 行で通りました**が、**キューに残っているのは
  配線が要る 4 件（23 クモの巣・24 ケーキ・25 氷・26 フェンス）**です。
  **この周で 0 行の 27a を先頭へ動かしたので、余裕はその 1 周ぶんだけ残っています** ——
  **27a を実装したあと、0 行で通せる行はもうありません。** 27b（革の防具 4 部位）も
  防具のセーブで `main.ts` に 1 行要ります（上の「この周でやったこと」）。
  **つまり次の C の周が終わったら、判断が来るまでループは進めません**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 36 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
