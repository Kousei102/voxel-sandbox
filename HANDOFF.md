# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-14・AUTODEV 80 のあと）

- `npm test` **3533 件すべて成功**（**±0**。**この周は `src/**` を 1 文字も触っていません**）。
  `npm run typecheck` 緑。**`npm run build` と `npm run bench` は走らせていません**
  （B の周なので `src/**` に差分が無く、走らせる理由がありません）
- **⚠ 音がいつになく荒れた周でした** —— **7 回中 3 回赤**で、**落ちた件が毎回違います**
  （2 回は「声色（音程の倍率）が波形に出る」低い声 3.477 / 4.161・1 回は「cutoff が
  低い材質ほどこもって鳴る」羊毛）。**同じ件が続けて落ちたことは一度もありません。**
  **差分が markdown 3 枚だけの周なので退行ではありません**（緑 4 回。`rules/testing.md` へ）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1450 行**（テストの数え方。**±0。1 文字も開いていません**）
- **アイテム 127 種・`MAX_ITEM_ID` 165・111..255 の空きは 90**（±0）・
  **1..63 の空きは 9**（±0。55..63）
- **立方体 41 / 非立方体 81**（±0）・**モブ 10 種類**（敵対 5）・**レシピ 66 本**（±0）・
  **`FUEL` 9 行 / `SMELTING` 9 行**（±0）・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 未着手`（30b・苗木が木に育つ）・97 行**（上限 120）・
  **`AUTODEV-QUEUE.md` 96 行（未着手 8 件）**

## この周でやったこと（AUTODEV 80 の B・クラウドの無人の周）

**判定表は 2 行目**（未着手 8 件で 1 行目は外れ・`AUTODEV-SPEC.md` が `状態: 済`）で
**B（仕様）の周**。**`src/**` は 1 文字も触っていません**（B の完了条件）。

**書いたのはキューの先頭・30b「植えた苗木が木に育つ」の仕様書**（`AUTODEV-SPEC.md`・97 行）。
**ID は 0 個**（苗木 164/165 も幹も葉ももう全部あります）。要点は 3 つ:

- **木の形を `src/treeshape.ts`（新）へ出して、生成と育ちが同じ 1 本を見る形にします。**
  `worldgen.ts` の `stampTree()` は葉と幹の二重ループを `treeCells()` の呼び出しに
  差し替えるだけ（**出す順番（葉 → 幹）も `overwrite` の真偽もそのまま**。変えると
  既存のワールドの木が動いて、セーブの差分が別の場所を指します）
- **育ちは `crops.ts` に乗せます** —— サトウキビ（`growCane()`）とまったく同じ器で、
  `notePlaced()` が苗木 2 種を覚え、`update()` の振り分けに 2 本足して `growTree()` へ。
  **`main.ts` は 0 行**（`crops.notePlaced(placed.at, placed.id, world)` が `main.ts:944` で
  もう繋がっている）。**`SaveData` も増えません**（既存の `crops` の表にそのまま乗る）
- **列をまたぐぶんは「書けたところまで」で済ませない**と明記しました ——
  **木が掛かる 4 隅の列（x±2, z±2）が全部 `hasColumn` になるまで 1 マスも書かず持ち越す**
  （半分だけの木が残ると二度と直りません）。高さは `grownTreeHeight(kind, x, z)` の
  **純関数**（`crops.ts` は `Math.random` を持てないため。範囲は生成側と同じ
  オーク 4..6・トウヒ 6..9）

**サブエージェントは 1 つも立てていません**（B の周は書くものが 1 枚だけなので、
`AUTODEV.md` の C-1 の話は掛かりません）。**撮っていません** —— `src/**` に差分が
無いので、写るものが 1 つも変わっていません（C-3 は C の周の条件）。

## 次にやること

**次は C（実装）の周** —— 未着手 8 件で 1 行目は外れ、**`AUTODEV-SPEC.md` が
`状態: 未着手`** なので 3 行目に当たります。

- **`AUTODEV-SPEC.md` を全文そのまま実装すること**（要約しない。要約すると
  「6. このタスク固有の禁じ手」が真っ先に落ちます）
- **先に引いて読むこと**: `rules/worldgen.md`（`worldgen.ts`）/
  `rules/stateful-blocks.md`（`crops.ts`・`main.ts`）/ `rules/testing.md`（`test/**`）
- **`src/treeshape.ts` を `rules/worldgen.md` の `paths` に足すこと**（足さないと次が引けません）
- **`worldgen.ts` の生成路を触るので `npm run bench` を 3 回まわして中央値を見ること**
- **撮ること**（`tools/shot.ts` に「育った木」の場面を足すのがいちばん安い）
- **共有帯の次の空きは 166 から**（この周でも ±0。`ROADMAP.md` の予約表）

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません**（`src/**` に差分が無いため）。**持ち越しは前の周のまま**です:

- **苗木（30a）**: `shots/sapling.png` と `docs/browser-shots/creative-sapling.png` は
  前の周が撮って見ています（十字の板 2 枚・面の欠けも裏返りも無し）。
  → **人に見てほしいのは「若い木に見えるか」**だけ
- **⚠ 苗木は植えてもいつまでも育ちません**（30b がまだ。**次の C の周で入ります**）。
  **遊ぶ人には「壊れている」ように見えます**
- **⚠ 一覧の 6 文字の名前（「オークの苗木」）が 2 行に折れて枠の絵に被る件**
  （`style.css` の `.slot .label`）。**人の判断待ち**なので直していません
- **木炭（29）**: **一覧で「炭」に見えるか**（`0x56473f`）
- **道具の修理（28）**: **傷んだ道具 2 本が「新品に戻る」のが気前よすぎないか**（本家の式）
- **スケルトン（11）**: **矢の当たる高さ** / 2 秒に 1 発 / **間合いを取らず殴ってくる**のが変でないか
- **防具（27b）**: **革一式（防具点 7）で 28% 減が「固くなった」と感じられるか** /
  **革 24 枚（＝牛 24 頭）で一式**が見合うか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）
- **氷（25a）**: **滑りの手ざわり**（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**・`rules/` へ据えました）

- **`rules/testing.md`**: **音の揺れは「15 回に 2 回・羊毛だけ」より荒れることがあります** ——
  この周は **`src/**` にも `test/**` にも 1 行も差分が無いのに 4 回中 2 回赤**で、
  **落ちた件が毎回違いました**（声色 / 羊毛）。**回数の見積もりを当てにせず、
  緑が 2 回出るまで走らせること。** 判定はゆるめないこと（既にあった 3 件の下へ 1 行足しました）

**`RULES-INBOX.md` には 1 行も足していません**（層 3 の話は 0 件）。

## 人がやること

- **⚠ 設計の判断（52 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**で、**`AUTODEV.md` の停止条件 2 にちょうど並んでいます**（上限 1500）。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**・
  最低 7 行）。**キューの上 4 件（30b..34）はどれも `main.ts` 0 行の見込み**なので、
  判断が下りなくても 4 周は止まりません。`test/ui.test.ts` の 40 件の見張りが
  動かせない事情は変わっていません
- **⚠ 苗木が育たない件** —— **仕様書は書けました。次の C の周で入ります**
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（足すならそれだけで 1 周）
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。この周は **7 回中 3 回赤**で、
  無人の周が毎回 3〜4 回走らせています。**直すなら測り方**（窓を長く取る・複数回
  鳴らして中央値を見る）で、**判定はゆるめないこと**（`rules/testing.md`）。
  **キューに足すのは A の周の仕事**なので、ループからは足していません
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 80 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押して `#creative` の
`scrollTop` を末尾まで送ってから撮ること**（`docs/browser-shots/README.md` の末尾）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木と同じで、**`tools/shot.ts` に場面を 1 つ
足すのがいちばん安い**（1〜2 秒で撮れて、`browsershot.mjs` を書き換えずに済みます）。
