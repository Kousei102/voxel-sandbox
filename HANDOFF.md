# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-12・AUTODEV 72 のあと）

- `npm test` **3472 件すべて成功**（**3424 → +48**。スケルトンと骨のぶん）
- `npm run typecheck` 緑 / `npm run build` 緑（`src/**` を触った周）。
  `npm run bench` は走らせていません（生成もメッシュ化も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1450 行**（テストの数え方。`wc -l` では 1449。**±0**。上限 1500）
- **アイテム 124 種・`MAX_ITEM_ID` 162・111..255 の空きは 93**（**94 → −1**）・
  **1..63 の空きは 9**（±0）
- **立方体 41 / 非立方体 79**・**モブ 10 種類**（敵対 5）・**レシピ 65 本**（±0）・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 137 行（未着手 2 件）**

## この周でやったこと（AUTODEV 72 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**キューの先頭 `11. スケルトンと骨` を実装しました**（コミット 1 つ）。

**仕様書の見立てどおり、足したのは表 2 つだけ**です:

| ファイル | 行 | 中身 |
| --- | --- | --- |
| `src/mobs.ts` | 2907 → 3020 | `MobKind` に 1 語 / `SKELETON: MobDef` 1 件 / `MOBS` と `MOB_KINDS` / `PLAYER_AIM` を `export` に |
| `src/items.ts` | +28 | `BONE = 162` と `item()` 1 行 / `MAX_ITEM_ID` を `BONE` へ |
| `test/**` | +388 | スケルトンと骨の節 / **既存 4 件の数え直し**（ゆるめていません） |

**`main.ts` / `mobrender.ts` / `mobmesh.ts` / `projectiles.ts` / `crafting.ts` は
1 文字も開いていません**（`ranged` の仕掛けはブレイズの火球で全部通っていました）。

**数え直した既存 4 件**（`rules/mobs.md` の「敵対を足すと必ず赤くなる」）:
`census()` の `counts` に **`スケルトン: 0` の 1 語**（無いと `counts[name]++` が NaN で、
**赤くならずに何も見なくなります**）/ 湧きの上限を**敵対 3 種類の合計**で / 重みの比を
**同じ重み 3 つ + エンダーマン 1/10** に / 「敵対は 4 種類」→ **5 種類**。

**仕様書の式が外れたところが 1 つ**（結論は変わりません）: 矢の落ちを
`0.5 × 30 × (距離/40)²` で 10m 0.94m と見ていましたが、**`drag` 0.02 のぶん実測は 1.13m**
で、**足元から 0.27m** に当たります（見立ては 0.46m）。16m は **2.70m** 落ちて
**プレイヤーの下を 1.30m 抜けます**。**だから式ではなく実測をテストに置きました**
（`PLAYER_AIM` を export にしたのはそのためで、テストが 1.4 を写さずに済みます）。

## 次にやること

**次は A（棚卸し）の周** —— `AUTODEV-SPEC.md` が `状態: 済` でも、**判定表は上から**なので
**`AUTODEV-QUEUE.md` の未着手が 2 件（3 件未満）**で 1 行目に当たります。

- **A-1 はコードから数え直すこと**（実装済みリストを信じない）。どのファイルを数えるかの
  表は **`docs/autodev-counting.md`**。基準は**変えないこと**（本家のバージョン順）
- **`src/**` と `test/**` に差分を出さないこと**（A の周の完了条件）
- 数え直すと変わるところ: **モブ 9 → 10 種類**（敵対 4 → 5）・**アイテム 123 → 124**・
  **`MAX_ITEM_ID` 161 → 162**・**111..255 の空き 94 → 93**

## ブラウザで見てほしいところ

**この周は `npm run shot -- mobs` と、寄って撮った 2 枚（使い捨て）で確かめました。**
**面の欠けも裏返りもありません**（体積・巡回順・当たり判定は毎回テストが見ています）。

**撮って 1 件直しました**: **胴と腕が「前から見ると 1 枚の板」でした。** 頭（±4px）より
胴を細くして輪郭は出したのに、**腕を胴の縁にぴったり付けた**ので骨色ひとつの板に見えます
（テストは全部緑のまま）。**鶏の翼と同じ手で腕を 0.6px 外へ離して**直り、
**いまは頭・胴・腕・脚と目が前からはっきり分かれて見えます**（`rules/mobs.md` に据えました）。

**人に見てほしいのは手ざわりのほうです**:

- **矢が当たる高さ** —— 10m で **足元から 0.27m**（膝より下）です。当たり判定には入って
  いますが、**「当たった感じ」がするか**は遊んでみないと分かりません。
  **間合いを縮めれば高く当たります**（`TUNING.md`）
- **2 秒に 1 発・重み 3** が「詰めるか逃げるか」を選ばせる速さか
- **間合いを取らず近づいて殴ってくる**（本家は後ずさりします）のが変に見えないか
- **骨色 `0xcdc8b0`** が一覧で「骨」に見えるか（いちばん近いのは砂で 24.6）

**持ち越し（防具 27b-2）**: 防具枠の置き場所（盤面と収納のあいだ・横 1 列）が使いやすいか /
1 文字のラベル（頭・胴・脚・足）で部位が分かるか / **革一式（防具点 7）で 28% 減が
「固くなった」と感じられるか**。**⚠ 一覧と防具枠の名前が 2 行に折れて絵に被る件**
（`style.css` の `.slot .label`。**5 文字の「革のズボン」で出ます**）は**人の判断待ち**です。

**持ち越し（革の防具 27b-1）**: 革の色 4 つが「革」に見えるか（上の 2 つは橙寄りに出ます。
**変えるなら隔たりを測ってから**。`TUNING.md`）/ **革 24 枚（＝牛 24 頭）で一式**が
見合う値段か（**革は牛しか落としません**）。

**持ち越し（フェンス 26a / 26b）**: **`shots/fence.png` に 5 通りが写っていて、
面の欠けも裏返りもありません**。**手ざわりとして「柵らしく見えるか」**だけ見てください。
**⚠ フェンスを 52fps 未満で跳び越せる件は残っています** —— 跳躍の余裕は **0.0117**
（到達は `9.2²/(2×30) + 9.2×dt/2` で、実測 **60fps 1.4883 / 30fps 1.5667 /
刻みの上限 0.05 で 1.6450**）。**直すなら跳躍の積分**（全ブロックの段差の登り方が
変わります）**か 1.5 の側**（本家の値から離れます）——**数値では決められないので人の判断です。**

**持ち越し（凍った海 25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の
境目が読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
いちばん近いガラスから 34.3 しか余裕がありません）。

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**。

**持ち越し（ケーキ）**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**
のは承知の上です（24b）。

**持ち越し（まだ誰も見ていません）**: クモの巣の歩き 1/4 / 金のリンゴ `0xf0a800` が
金インゴット・ブレイズロッドと見分けられるか / 本棚が「本の詰まった棚」に見えるか /
リンゴの色が赤キノコ・生牛肉と見分けられるか / 葉 200 枚に 1 個が待てる確率か /
はしごの登り 2.35 m/s / `CANE_GROW_SECONDS = 180` が待てる長さか /
**自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/ 一覧で砂糖が雪・羽根・卵・
糸と見分けられるか / ボウルとシチューの見分け / 森のキノコの密度 / サボテンの痛さ /
牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件・どちらも `rules/mobs.md`**）

- **同じ色の部位は「細くする」だけでは分かれません。** 胴を頭より細くしても、
  **腕を胴の縁にぴったり付ければ 1 枚の板**です（骨色ひとつ）。
  **隙間（鶏の翼の 0.6px）か別の色のどちらかが要ります。撮るまで分かりません**
- **重力を受ける弾の `ranged.range` は `PLAYER_AIM`(1.4) から決まります。**
  落ちた量が 1.4 を超えると**プレイヤーの下を抜ける「当たらない飛び道具」**になり、
  **テストは全部緑のまま**です（矢の実測値つき）。**火球の 16 を写さないこと**

## 人がやること

- **⚠ 設計の判断（45 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（テストの数え方）で、**`AUTODEV.md` の「止まって人を呼ぶ条件」の
  2 番にちょうど並んでいます。** ハードの上限は 1500 行で、**次に `main.ts` へ 1 行でも
  足す周は、先に判断を別ファイルへ出さないと止まります**（待っているのは
  **24b（ケーキをかじる。最低 7 行）**）。**この周は `main.ts` を 1 文字も開いていません。**
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）
- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 55 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を押してクリエイティブにし、`E` を押して末尾までスクロールしてから撮ること**
（手順は `docs/browser-shots/README.md` の末尾）。

**⚠ `npm run shot -- mobs` は 10 種類を 5m 間隔の一列で撮るので、1 体 1 体は小さくしか
写りません**（span が 45m まで伸びました）。**形を確かめるなら、その場面をそのまま
使わずに寄って撮ること** —— この周は使い捨ての場面（モブ 2 体・カメラ 3m）を
`tools/raster.ts` に直に渡して撮りました。**リポジトリには入れていません。**

**`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
**`wc -l` を 119 以下に収めること**（`rules/testing.md`）。
