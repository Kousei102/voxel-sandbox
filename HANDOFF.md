# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm test` は通ります。**`npm test` 2285 件すべて成功**
  （**この周は `src/**` と `test/**` を 1 行も触っていません。** 2284 → 2285 の 1 件は
  `test/docs.test.ts` が `AUTODEV-QUEUE.md` の行数を見始めたぶんで、機能は増えていません）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1372 行 / 上限 1500**（変わっていません）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（変わっていません）
- **`AUTODEV-QUEUE.md` ができました。`AUTODEV-SPEC.md` はまだありません** → **次の周は B（仕様）**
- **`RULES-INBOX.md` に未取り込みが 2 件**（`mobs.md` と `projectiles.md`）。**取り込むのは人が居る周**

## この周でやったこと（無人・A の周 1 種類だけ）

`AUTODEV.md` の「1. いまはどの周か」の判定表で **A（棚卸し）** に当たったので、
**数えて並べただけです。`src/**` も `test/**` も触っていません**（A の完了条件）。

### 数え直して分かったいちばん大きいこと —— **剣が 1 本も無い**

道具は**ツルハシ・斧・シャベルの 3 種 x 4 階層**だけで、殴るダメージは
`mobs.ts` の `TOOL_ATTACK`（斧 3 > ツルハシ 2 > シャベル 1・階層ごとに +0.5・素手 1）。
本家で最初期からあるものが丸ごと抜けていて、**`ROADMAP.md` にも載っていませんでした。**
「入れたいものの一覧」より**コードを数えるほうが速く見つかる**という、A の周の狙いどおりの例です。

### `AUTODEV-QUEUE.md`（10 件・基準つき）

基準は **本家のバージョン順（Alpha → Beta → 正式版）**で、**次の A の周でも変えません。**
並びは `AUTODEV.md` の A-2 のとおりで、**取りやすさではありません**:

1. 番号を 1 つも使わない 3 件 —— 重力ブロック（砂・砂利が落ちる）/ 大きいチェスト / 道具の耐久値
2. 番号を使う 3 件 —— 剣 4 本 / ハサミ / 小麦・種・耕地・パン
3. **見た目がユーザーにしか確かめられない 4 件** —— 鶏 / 牛 / クモ / スケルトン
   （鶏の羽根とクモの糸が入ると、**矢と弓の「代用材料」を本家の形に戻せます**）

見送った 9 件（水流・防具・経験値・レッドストーン・醸造・ネザークォーツ・天候・村・TNT）は
**理由を 1 行ずつ `docs/autodev-log.md`** に置きました。同じものを毎周検討しないためです。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周はありません**（文書しか触っていません）。

**前の周のぶんが 1 件まだ残っています** —— メッシュ化の詰め方を変えた（`0c68545`）ので、
**面が欠けていないか**（洞窟の奥・水面・ガラスごし）と **AO の陰影が前と同じか**（壁ぎわ・階段の下）。
数値では法線の一致・穴なし・三角形数まで見ていますが、陰影の出方そのものは目でしか確かめられません。

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` があり `AUTODEV-SPEC.md` が無いので、**次は B（仕様）の周**です。
先頭の 1 件（**砂と砂利が落ちる**）を、7 項目そろえて `AUTODEV-SPEC.md` に書くこと。
**取る前にその 1 件だけコードで数え直すこと**（B の決まり）。**`src/**` はまだ触りません。**

人がやること:

- 上の**ブラウザ確認 1 件**（前の周からの持ち越し）
- `RULES-INBOX.md` の 2 件の取り込み
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）はまれに 1 件落ちる**と前の周の引き継ぎにあります。
**2 回続けて落ちたときだけ退行として扱うこと。**

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
