# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05・AUTODEV 38 のあと）

- `npm test` **3090 件すべて成功**（この周は **±0** —— **B の周なので `src/**` も `test/**` も
  1 行も触っていません**）・`npm run typecheck` 緑
- **`npm run build` と `npm run bench` は走らせていません**（`src/**` に差分が 0 行のため。
  `AUTODEV.md` の検証はどちらも「`src/**` を触った周だけ」）
- **音の一群は 1 回だけ跳ねました** —— 1 回目に**羊毛 9.740**（雪 0.716 / 砂 2.235）で
  「cutoff が低い材質ほどこもって鳴る」が落ち、**2 回目は羊毛 0.552 で緑**。
  `rules/testing.md` の書いてあるとおりの揺れ方です（**判定はゆるめていません**）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1447 行**（`wc -l`。上限 1500・**止まる目安 1450 まで残り 3 行**）
- **アイテム 109 種・`MAX_ITEM_ID` 144・111..255 の空きは 111・1..63 の空きは 9**
- **立方体 39 / 非立方体 72**・**モブ 9 種類**・**レシピ 54 本**・**食べ物 9 種**

## この周でやったこと（AUTODEV 38 の B・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 済` で `AUTODEV-QUEUE.md` の未着手が 4 件だったので、
判定表の 2 行目に当たって **B（仕様）の周**。先頭の **18b（サトウキビが積める・上へ伸びる）**を
**2 件に割り**、**前半だけを `AUTODEV-SPEC.md`（119 行）にしました**。

- **割った理由は `main.ts` の行数です**（前の周の申し送りどおり、仕様の周のうちに数えました）。
  **「積める」は 0 行**で通り、**「伸びる」は 6 行要ります** —— 位置ごとの状態を持つ器を
  1 つ増やすと `import` / 実体 / `deserialize` / `collectState` / `forgetEverything` / `update`。
  **1447 + 6 = 1453 で止まる目安 1450 を越える**ので、**18c** としてキューへ戻しました
- **18b の芯は 3 つ**: `blocks.ts` に **`supportsBlock()`**（**`canSupport()` の外側**に
  「自分の上には自分を置いてよい」）/ `world.ts` の**置く側と壊す側の 2 か所**を
  それに付け替える / **サトウキビの `replaceable: true` を外す**
- **`replaceable` を外すのがこの周いちばんの判断です。** `placeSpot()` は狙ったブロックが
  `replaceable` なら**そのマス自身**を返すので、付いたままだと上面を狙っても 1 本目に重なり、
  `setVoxel` が「同じ値」で false を返して**永久に積めません**。外すと法線の側（＝真上）を
  返すので、**`placing.ts` にも `main.ts` にも 1 行も書かずに積めます**
- **18a が `replaceable` を付けた理由は実測で 1 度も起きていませんでした**（下）

**`src/**` と `test/**` に差分はありません**（B の周の完了条件）。触ったのは
`AUTODEV-SPEC.md` / `AUTODEV-QUEUE.md` / `docs/autodev-log.md` / `HANDOFF.md` の 4 つだけです。

## この周に測ったこと（使い捨ての `WorldGen` 呼び出し）

`±400` の「浜 かつ 高さ > `SEA_LEVEL`」の先頭 2000 列を、**種 3 つ**（1234 / 777 / 20260905）で:

| 種 | サトウキビ | **h+1 が葉** | h+2 が塞がり | h+3 が塞がり |
| --- | --- | --- | --- | --- |
| 1234 | 247 本 | **0 列** | 0 | **1**（葉） |
| 777 | 249 本 | **0 列** | 0 | 0 |
| 20260905 | 253 本 | **0 列** | 0 | 0 |

**サトウキビの立つマスに葉が来た列は 3 種とも 0** —— 18a が `replaceable: true` を付けた
理由（葉が欠ける）は起きていません。**外すと葉より強くなる**ので、種 1234 の 1 本
（h+3 が葉）でも**積んだ列の途中を抜かれて上が浮くことがありません**。
細かい数字は `docs/autodev-log.md` の AUTODEV 38。

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。
**仕様書を全文渡すこと**（要約すると「触らないファイル」と「禁じ手」が真っ先に落ちます）。

- **触るファイルに当たる `rules/*.md`**: `rules/blocks-shapes.md`・`rules/worldgen.md`・
  `rules/meshing-render.md`・`rules/lighting.md`（`world.ts` に当たります）・`rules/testing.md`
- **新しい ID は 0 個**（次に取るのは 145 のまま）。**`main.ts` は 1 行も書きません**
- **C-3 で撮ること**（地形の見た目が変わります）: `npm run shot -- beach terrain` と
  `node tools/browsershot.mjs`

## ブラウザで見てほしいところ

**この周は撮っていません**（`src/**` に 0 行の差分。`AUTODEV.md` の C-3 は
「見た目に出るものを触った周だけ」）。**持ち越しは前の周のまま**です:

浜のサトウキビの密度 0.12 が歩いていて気持ちいいか（撮った絵では「まばらな竹林」）/
**1 マスぶんの背丈が寸詰まりに見えないか** —— **これは次の C の周（18b）で 1〜3 段になります** /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか（雪から 22.3 しかありません）/
サトウキビ（`0x9ad14f`）が小麦の種（42.4）と並んだとき

**さらに持ち越し（まだ誰も見ていません）**: ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け /
シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか / 食べ終わりにボウルが手に戻る手ごたえ /
森を歩いたときのキノコの密度（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を草むらと
取り違えないか / サボテンに当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・ミルクで毒が止まるか /
ミルクバケツの色（`0xeaf2f8`）が羽根・卵・糸・雪玉と見分けられるか /
かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
鉄・金・ダイヤのブロックが材料の山と見分けられるか /
**一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`。
「キノコシチュー」は 7 文字でいちばん長い名前）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 0 件）

**`rules/` には 1 行も足していません。** `rules/blocks-shapes.md` の
「十字（`model: "cross"`）はどう書いても支えになれない／積み上がる生えものには
`canSupport()` の外に別の決まりが要る」は**いまも正しい**ので、**`supportsBlock()` が
実際に入る次の C の周に書き換えます**（無いコードの決まりを先に書かないこと）。

## 人がやること

- **設計の判断を 1 つお願いします（12 周持ち越し。ただし急ぎではありません）:
  `main.ts` を 2 つに割ってよいか。** いま **1447 行で、止まる目安 1450 まで残り 3 行**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **次の 18b は 0 行で通りますが、その次の 18c（伸びる）と 19（はしご）は
  どちらも配線が要ります** —— そこで止まります
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 22 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
