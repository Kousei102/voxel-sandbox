# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-14・AUTODEV 83 のあと）

- `npm test` **3596 件すべて成功**（+14）。`npm run typecheck` 緑・`npm run build` 緑。
  **音は 1 回目から緑**（羊毛 0.552 < 雪 0.716 < 砂 2.235 / 低い声 0.162 < 高い声 0.329）
- **`npm run bench` は走らせていません**（生成もメッシュ化も触っていないため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1450 行 / 上限 1500**（±0。**1 文字も開いていません**）
- **アイテム 129 種**（+2）・**`MAX_ITEM_ID` 165**（±0）・**レシピ 68 本**（+2）
- **1..63 の空きは 7**（55..56 を取って 9 → 7。**残りは 57..63**）・
  **111..255 の空きは 88**（166..167 を取って 90 → 88。**次に取るのは 168**）
- **立方体 41**（±0）・**非立方体 85**（+4）・**モブ 10 種類**（敵対 5）・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 未着手 6 件**

## この周でやったこと（AUTODEV 83 の C・クラウドの無人の周）

**判定表は 3 行目**（未着手 7 件で 1 行目は外れ・`AUTODEV-SPEC.md` が `状態: 未着手`）で
**C（実装）の周**。**31「ネザーレンガと石レンガのハーフ」を仕様書どおり全部入れました。
差し戻し 0 回。**

- **ハーフの材質が 4 → 6** に（石・丸石・板・砂岩 ＋ **ネザーレンガ・石レンガ**）
- **`src/**` は `blocks.ts`（+35 行）と `crafting.ts`（+8 行）の 2 本だけ。**
  **既にある `slabPair()` と `slabRecipe()` を呼び足しただけで、どちらも中身は
  1 文字も書き換えていません** —— 置く向き・当たり判定・支え・光・ドロップは
  既存 4 材質の仕掛けがその場で動きました
- **ID 4 個**: 大元 **55 / 56（低帯）**・上付き **166 / 167（共有帯）**。
  **`src/items.ts` は 0 行**（低帯の大元は for が作り、上付きは `variantOf` があるので
  作られず、掘ると `dropOf()` の既定で大元が落ちます）
- **`main.ts` / 生成 / `FUEL` / `SMELTING` / `placing.ts` / `mesher.ts` は ±0 行**
- **`TUNING.md` は 0 行**（色も硬さも道具も元の材質の写しなので、決めた数値がありません）

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` なので判定表の 2 行目です
（未着手 6 件なので 1 行目は外れます）。**`AUTODEV-QUEUE.md` の先頭は 32「粘土ブロック・
粘土玉・レンガ」** で、**取る前にその 1 件だけコードで数え直すこと**（`AUTODEV.md` の B）。

- **ID は `ROADMAP.md` の予約表から。次の空きは 共有帯 168・低帯 57**
- **`src/**` に 1 文字も差分を出さないこと**（B の完了条件）

## ブラウザで見てほしいところ

**この周は 2 枚撮って、どちらも `Read` で開いて見ました。**

- **`shots/slabs.png`**（`tools/shot.ts` に場面 `slabs` を足しました。**`npm run shot -- slabs`**）。
  **6 材質 x 下付き / 上付きの 2 列＋元の立方体**。**面の欠けも裏返りも 0**・
  上付きの列がはっきり高く写る・**ネザーレンガハーフは背の立方体と同じ暗い小豆色**
- **`docs/browser-shots/creative-brick-slabs.png`**（本物のブラウザ）。一覧の枠 127 → **129**・
  `.swatch` は `rgb(57,34,41)` と `rgb(125,130,136)` で**指定どおり**・console のエラー 0 件

**それでも人に見てほしいのはここです:**

- **ハーフ 2 材質（31）**: **石レンガハーフが石ハーフ・丸石ハーフと見分けられるか**
  （`0x7d8288` 対 `0x8a8f96` / `0x767b82`。**本家も似ているので色はずらしていません**）/
  ネザーレンガハーフの暗さが屋根に葺いて重すぎないか
- **苗木（30b）**: **180 秒が待てる長さか**と、**育った瞬間に木がぱっと出るのが唐突でないか**
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **この周で過去最悪を更新しました** —— **9 文字の「ネザーレンガハーフ」は 3 行に折れて
  枠を丸ごと覆います**（`docs/browser-shots/creative-brick-slabs.png`）。**人の判断待ち**
  （`.slot .label` を触ると全部の枠の手ざわりが変わるので、この周では直していません）
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）
- **道具の修理（28）**: 傷んだ道具 2 本が「新品に戻る」のが気前よすぎないか（本家の式）
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）
- **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**）

**`rules/blocks-shapes.md` の「ID の枠の細かい話」に 1 節足しました**
（`grep -l '"src/blocks.ts"' rules/*.md` で引いて読んだファイルです）:
**凍結した 1..63 の残りを取ってよいのは「アイテムとして持てるブロック」だけ**で、
**1 材質が低帯と共有帯にまたがるのが普通になった** ——
だから**空きを片方の帯だけ見て「あと何材質ぶん置けるか」を数えないこと**
（**低帯の残り 7 個で 7 材質は置けません**。ハーフは低帯 1 ＋ 共有帯 1、
**階段は低帯 1 ＋ 共有帯 7**）。**`RULES-INBOX.md` には 1 行も足していません**（層 3 は 0 件）。

## 人がやること

- **⚠ 設計の判断（55 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**で、**`AUTODEV.md` の停止条件 2 にちょうど並んでいます**（上限 1500）。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**・
  最低 7 行）。**キューの上 3 件（32..34）はどれも `main.ts` 0 行の見込み**なので、
  判断が下りなくても 3 周は止まりません
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。
  **9 文字で 3 行**まで来ました（上の「ブラウザで見てほしいところ」）
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（足すならそれだけで 1 周）
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周も 1 回目から緑**でしたが、
  前に 7 回中 3 回赤い周がありました。**直すなら測り方**（窓を長く取る・複数回鳴らして
  中央値を見る）で、**判定はゆるめないこと**（`rules/testing.md`）。
  **キューに足すのは A の周の仕事**なので、ループからは足していません
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 83 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押してから撮ること**
（`docs/browser-shots/README.md` の末尾）。**⚠ 送り先は末尾とは限りません** ——
**低帯（1..63）の番号を取ったものは一覧の真ん中に出る**ので、`scrollTop = scrollHeight`
では 1 枠も写りません（この周で 1 度撮り直しました）。**`.label` の文字列で枠を探して
`scrollIntoView({ block: "center" })` を呼ぶこと。**

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・**ハーフ 6 材質**がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
