# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-09・AUTODEV 66 のあと）

- `npm test` **3372 件すべて成功**（**+16**。26b のぶん）・`npm run typecheck` 緑・
  **`npm run build` も緑**（`src/**` を触った周なので走らせました）
- **音の一群は 2 度跳ねました**（`test/audio.test.ts`。1 回目が「cutoff が低い材質ほど
  こもって鳴る」、2 回目が**別件の「声色（音程の倍率）が波形に出る」で 0.329 ↔ 4.465**）。
  **落ちた件が違うので退行ではありません** —— **そのあと 4 回続けて緑**です（`rules/testing.md`）
- **`npm run bench` は走らせていません**（生成もメッシュ化の予算も触っていません。
  **箱が減るぶん三角形はむしろ減ります** —— 1 本だけのフェンスが 108 → 12 三角形）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（1 バイトも増えていません）
- **`main.ts` 1450 行（±0。この周は 1 度も開いていません）**・**`mesher.ts` 584 → 597 行**
- **アイテム 119 種・`MAX_ITEM_ID` 157・111..255 の空きは 98・1..63 の空きは 9**（すべて ±0）
- **立方体 41 / 非立方体 79**・**モブ 9 種類**・**レシピ 61 本**・**バイオーム 11**（すべて ±0）
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 122 行**（未着手 **4 件**）

## この周でやったこと（AUTODEV 66 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**26b「フェンスが隣と繋がって見える」を実装しました。** **ID は 0 個**で、
`main.ts` も `physics.ts` も当たり判定も 1 文字も触っていません。

- **`BlockModel` に `"fence"`**（5 つ目）。**隣のマスを見る唯一の形**です
- **判断は `blocks.ts` 側**: **`fenceConnects(id)`**（フェンスどうしと、**立方体で
  `solid` かつ `opaque`** なもの = 石・土・葉・板）。**`mesher.ts` には
  「どのブロックと繋がるか」を 1 行も書いていません**
- **形も `blocks.ts` に 1 か所**: `FENCE_POST_BOX` と `FENCE_ARMS`（4 方向 x 2 段）を出し、
  **`FENCE_BOXES` はそれを組んだもの**（並びも数値も 26a のまま）
- **`boxes` は 9 箱のまま**なので、**繋がっていない側からも狙えます**（`raycast` と選択枠）
- 三角形の数で確かめています: **1 本だけ 12**（柱）/ **2 本並べて 72** /
  **4 方向すべて = 中央 108 + 隣 4 本 x 36 = 252** / **チャンクの外の隣とも繋がる（36）**

**踏んだ落とし穴は 2 件、`rules/` に据えました**（下の「この周で分かったこと」）。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので判定表の 2 行目です
（未着手 4 件なので A ではありません）。**キューの先頭は 27b（革の防具 4 部位）**。

- **27b は `main.ts` に 1 行要ります**（セーブのキー 1 本）。**いま 1450 行で、
  止まる目安ちょうど**です —— **`AUTODEV.md` の停止条件 2 に当たるかは B の周が
  数えて決めること**（「配線が要る」と「行が増える」は別。`AUTODEV-QUEUE.md` の ⚠）
- **ID は 4 個**（革の帽子・上着・ズボン・靴）。**革だけ。鉄・金・ダイヤは取らないこと**
- **`inventory` のキーに 8 要素を継ぎ足して済ませないこと**（`rules/inventory-screen.md` の末尾）
- **未着手 4 件。27b を実装して消すと 3 件**です（**3 件を切ったら次は A の周**）

## ブラウザで見てほしいところ

**`shots/fence.png` を 3 枚撮り直して、5 通りすべてを 1 枚に収めました**（下の ⚠）。
**写ったもの**: 1 本だけ = **柱だけ** / 石の横 = **石の側 1 本だけ** / 石の上 = **柱だけ** /
直線 = **繋がった 1 本の柵** / 角 = **2 方向だけ**。**面の欠けも裏返りもありません。**

- **26b は絵の上では直っています。** 前の周が残した「柱 + 腕 4 方向を常に描く形が、
  1 本だけ置いたときに変に見えないか」は**もう起きません** ——
  **手ざわりとして「柵らしく見えるか」だけ**を公開サイトで見てください
- **⚠ フェンスを 52fps 未満で跳び越せる件は残っています**（26a から持ち越し）。
  跳躍の余裕は **0.0117 しかありません** —— 到達は `9.2²/(2×30) + 9.2×dt/2` で、
  実測 **60fps 1.4883 / 30fps 1.5667 / 刻みの上限 0.05 で 1.6450**。
  **直すなら跳躍の積分**（全ブロックの段差の登り方が変わります）**か 1.5 の側**
  （本家の値から離れます）。**数値では決められないので人の判断です**
- **当たり判定はマスいっぱいのままです**（26b でも変わりません）——
  **腕の無い側にも「見えない壁」があります。** 柱だけの太さにできない理由は
  `rules/blocks-shapes.md`（列のあいだを歩いて抜けられる）
- **色 `0x988a5e` が「木の柵」に見えるか**（板 `0xb18a56` からは 26.2 離れています）
- **棒 6 本で 2 個が、牧場 1 区画（数十本）に見合う値段か**（本家の古い値です）

**持ち越し（凍った海 25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の
境目が読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
いちばん近いガラスから 34.3 しか余裕がありません）。**気になるなら人が色を決めてください** /
**氷の板の上を歩けることが見て分かるか** / **氷ごしに海の底が見えるか**（`alpha` 0.6）。

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
（理由は `TUNING.md`）。**凍った海が入ったので実地で試せます。**

**持ち越し（ケーキ）**: 上面 `0xffd0e4` が「ケーキ」に見えるか / 側面 `0xe8c9a0` と
下面 `0xd9b98a` の 3 色がケーキに読めるか。**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: 中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか /
素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / 森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ /
牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
**一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件**）

- **`rules/testing.md`**: **「足す前と後の差」で 1 個ぶんを数えるなら、対照が足しても
  変わらないことを確かめること。** 隣ごとのフェンスの三角形を「隣だけ置いた状態」から
  引いて数えたら、**隣もフェンスの行だけ 36 ではなく 60** で赤くなりました ——
  **隣のフェンスも腕を 1 本伸ばす**ぶん（24）が差に乗るためです。
  フェンスどうしは**2 本の合計 72 を直に見る**形に直しました
- **`rules/meshing-render.md`**: **`model: "fence"` だけが隣のマスを見ます**
  （`pad` は 18³ で `padIndex()` が `[-1,16]` を取るので**入り口は要りません**）。
  **繋がる相手の表は `blocks.ts` の `fenceConnects()`**、形も `FENCE_POST_BOX` /
  `FENCE_ARMS` として `blocks.ts` に 1 か所。**`boxes` の 9 箱は減らさないこと**

## 人がやること

- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **設計の判断（39 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1450 行**で、
  上限 1500 まで 50 行。**待っているのは 24b（ケーキをかじる。最低 7 行）と
  27b（革の防具。セーブのキーで 1 行）**で、**27b がキューの先頭**です。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
  （**判定の意味が変わるので無人の周では触れません**）
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 49 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
**自然生成しないブロックは browsershot には 1 マスも写りません** ——
氷・ケーキ・クモの巣・本棚・**フェンス**は `npm run shot -- <名前>` が唯一の経路です。
**`tools/shot.ts` の場面を足したら、カメラも一緒に詰め直すこと** ——
**26b で足した「石の横のフェンス」と「角」は、26a のカメラでは画面の外**でした
（3 枚撮り直しました。1 枚 1.2 秒なので、読めない絵で妥協しないこと）。

**`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
**`wc -l` を 119 以下に収めること**（`rules/testing.md`。**いま 110 行**）。
