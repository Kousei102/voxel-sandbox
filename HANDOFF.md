# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- ブランチ **`loop/devgame`**。**この周は人が居る周**で、AUTODEV 19 のあとに
  **決まりごと（層 2）の置き場所を変えました**（下）。**`master` への push とマージは
  引き続き禁止**
- `npm test` **2906 件すべて成功**。`npm run typecheck` / `npm run build` も緑。
  **`npm run bench` は走らせていません**（`src/**` を 1 行も触っていないため）
- **前の周の 2923 件から 17 件減っています。** 消したぶん（`test/rulesinstall.test.ts`）と
  足したぶん（`test/rules.test.ts` の 8 件・`docs.test.ts` の退避先 1 件）の差で、
  **機能のテストは 1 件も減っていません**
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **ブロック / アイテム ID・アイテム 94 種・`MAX_ITEM_ID` 129・`SaveData` version 1 は
  すべて前の周のまま**（`src/**` に差分なし）
- **`main.ts` は 1463 行のまま**（⚠ 下）。`REVIEW.md` の未着手は 0 件

## ⚠ 次に `main.ts` を触る周がまず見ること: 1463 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」3 番（1450 行）を越えたままです**（上限 1500）。
**次に `main.ts` を触る周は、先に判断を別ファイルへ出すこと。**

## この周でやったこと — 層 2 を `.claude/rules/` から `rules/` へ出した

**決まりごと（層 2）はリポジトリ直下の `rules/` に移りました。** 周は **`Edit` で
普通に直します** —— スクリプトも受け渡し用のファイルも通りません。

**なぜ**: `.claude/**` は書き込みに「sensitive file」の確認が出るので、**答える人が
居ない無人の周がそこで止まります。** 迂回路として置いた `npm run rules:install` は、
**無人の周が使った回数 0 回**（AUTODEV 18 は `docs/` に書いて済ませ、AUTODEV 19 は
リポジトリ外の定期実行プロンプトとの食い違いを報告して `RULES-INBOX.md` へ回した）。
**縛りとしても機能していません** —— 層 2 より強い `CLAUDE.md` は元から自由に編集でき、
迂回用のスクリプト自身も周が編集できました。**編集させたいものを編集できない場所に
置いていた**ので、置き場所のほうを直しています。

- **`.claude/rules/*.md` → `rules/*.md`**（20 本。`git mv` なので履歴は繋がっています）
- **`rules/README.md` を新設** —— 引き方・直すときの決まり・20 本の一覧・移した理由
- **消したもの**: `scripts/install-rule.mjs`(183 行) / `test/rulesinstall.test.ts`(127 行) /
  `LOOP.md` の 4.5 の手順 / `.claude/settings.json` の許可 2 行 / `package.json` の
  `rules:install`
- **`RULES-INBOX.md` は層 3（スキル）専用**になりました。取り込みの記録は
  `docs/rules-inbox-archive.md` へ退避（**受け渡しを 1 周空けると本文が食い違う**という
  証拠なので、消さずに残してあります）
- **溜まっていた層 2 の 3 件を当てました**（`projectiles.md` の 5 種類 / `use.md` の
  15 通り / `testing.md` の山なりの的）。**未取り込み 0 件**

**引き換えに自動読み込みを捨てています。** 前は `paths` に当たるファイルを `Read` / `Edit`
した瞬間に読み込まれていました（`.claude/rules/` に置いてあることが条件の機能）。
**いまは周が自分で引きます**:

```bash
grep -l '"src/player.ts"' rules/*.md   # → blocks-shapes.md mobs.md vitals.md
```

**捨てたぶんは `test/rules.test.ts` で見える形にしました**（`paths` があるか・宛先が
実在するか・一覧と食い違っていないか・古い置き場が復活していないか）。**自動読み込みは
効かなくても静かでしたが、こちらは赤くなります。** 見張りが本当に効くことは、
`paths` を 1 文字壊す／一覧から 1 行消す、の 2 通りで**実際に赤くして確かめました。**

## 人がやること

- **リポジトリ外の定期実行プロンプトは直さなくてよくなりました。** 「`.claude/**` に
  1 行も書くな」がそのまま真になり、リポジトリ側と食い違いません
  （**スキルの更新だけは今までどおり `RULES-INBOX.md` 経由**）
- **`loop/endgame` にこの形が届いていません。** `master` 経由か cherry-pick かを
  決めてください（**あちらは `npm run rules:install` も届いていないので、この形を
  入れるほうが素直**です）
- `master` へのマージ
- 遊んで決めてほしいこと 5 つは前の周から持ち越しです: **投げた卵に使い道が無い**
  （音もヒヨコも無し）/ **卵と羽根がどちらもほぼ白** / **産卵の間隔 300〜600 秒** /
  **矢が 2x2 で作れなくてよいか** / **苗の緑と `GROW_SECONDS` 180 秒と `#status` の位置**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手が **4 件**（3 件以上）で、`AUTODEV-SPEC.md` が
**`状態: 済`** なので → **次は B（仕様）の周**です。

- 先頭は **8「牛（革・生牛肉・ステーキ）」**。**取る前にその 1 件だけコードで数え直すこと**
- **`src/**` に 1 行も差分を出さないこと**（B の周の完了条件）。仕様書は **120 行以内**で、
  `AUTODEV.md` の B の **7 項目を 1 つも欠かさないこと**
- **ID は `ROADMAP.md` の予約表から**（**次は 130**）。牛は **3 個**の見込みです
- **触るファイルに当たる決まりごとを `grep` で引いて読むこと**（上）。
  牛なら `rules/mobs.md` と `rules/items-survival.md` と `rules/drops.md` です
- **踏んだ落とし穴は `rules/` へ `Edit` で据えること**（`docs/autodev-log.md` に
  書いて済ませない。無ければ「決まりごと 0 件」と書く）

**音のテスト（`test/audio.test.ts`）は揺れます。赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。**この周は 4 回走らせて 4 回とも緑**でした。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL が動かない」は開発者の手元の devcontainer の話です）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
