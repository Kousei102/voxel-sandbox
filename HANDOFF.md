# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- ブランチ **`loop/devgame`**。**この周は無人（クラウドの定期実行）で、B（仕様）の周**でした。
  **`master` への push とマージは引き続き禁止**
- `npm test` **2906 件すべて成功**。`npm run typecheck` も緑。**`npm run build` と
  `npm run bench` は走らせていません**（**`src/**` を 1 行も触っていない**ため。
  `git status` で確かめられます）
- **件数は前の周から 1 件も動いていません**（2906 → 2906）。**進行（クリア導線）達成 13 / 13**
- **ブロック / アイテム ID・アイテム 94 種・`MAX_ITEM_ID` 129・`SaveData` version 1・
  `main.ts` 1463 行はすべて前の周のまま**（`src/**` に差分なし）
- **111..255 の空きは 126・1..63 の空きは 9**

## ⚠ 次に `main.ts` を触る周がまず見ること: 1463 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」3 番（1450 行）を越えたままです**（上限 1500）。
**次に `main.ts` を触る周は、先に判断を別ファイルへ出すこと。**
**次のタスク（牛）は `main.ts` を 1 行も触らない見込み**です（仕様書の 2）。

## この周でやったこと — 牛の仕様書を書いた（`src/**` は 0 行）

`AUTODEV.md` の「1. いまはどの周か」の判定表: `AUTODEV-QUEUE.md` の未着手が **4 件**（3 件以上）で
`AUTODEV-SPEC.md` が **`状態: 済`** → **B（仕様）の周**。当たった 1 種類だけをやりました。

- **`AUTODEV-SPEC.md` を丸ごと差し替え**: キュー先頭の **8「牛（革・生牛肉・ステーキ）」**。
  **取る前にコードで数え直しました** —— `src/**` と `test/**` に `cow` / `beef` /
  `leather` / `牛` / `革` は **1 つもありません**（未実装で確定）
- **形は鶏（AUTODEV 16）と同じ**で、違うのは **2 山目が最初から付く**ところだけです
  （`MobDrop.extra` は AUTODEV 17 で入っています）。触るのは **`items.ts` /
  `mobs.ts` / `smelting.ts` の 3 本だけ**の見込み
- **ID は 3 個**（`ROADMAP.md` の予約表の「130..255 予備」から上から詰めて
  **130 = 生牛肉 / 131 = ステーキ / 132 = 革**）。空きは **126 → 123** の見込み
- **仕様書は 118 行**（上限 120）。`AUTODEV.md` の B の **7 項目は全部あります**

## この周で踏んだ落とし穴（`rules/testing.md` に据えました）

**文書の行数の上限（`test/docs.test.ts`）は `wc -l` より 1 多く数えます** ——
数え方が `split("\n").length` なので、末尾の改行のぶんが乗ります。**仕様書を
`wc -l` で 120 行ちょうどに収めたら、121 と数えられて赤くなりました。**
上限を上げずに本文を削って直しています（あの 120 行は「1 周で閉じない」の信号なので）。

## 人がやること

- **リポジトリ外の定期実行プロンプトと `rules/` の置き場所がまだ食い違っています。**
  プロンプトは「決まりごとの更新は `RULES-INBOX.md` に書け」と言いますが、**リポジトリ側は
  2026-09-04 に層 2 を `rules/` へ出して「`Edit` で普通に直す」に変わっています**
  （`RULES-INBOX.md` は層 3 = スキル専用）。**この周は新しいほう（`rules/testing.md` を
  直接 `Edit`）に従いました** —— `.claude/**` の外なので確認は出ず、止まりません。
  プロンプト側の 1 行を直すと食い違いが消えます
- **`loop/endgame` に `rules/` の形が届いていません。** `master` 経由か cherry-pick かを
  決めてください
- `master` へのマージ
- 遊んで決めてほしいこと 5 つは前の周から持ち越しです: **投げた卵に使い道が無い**
  （音もヒヨコも無し）/ **卵と羽根がどちらもほぼ白** / **産卵の間隔 300〜600 秒** /
  **矢が 2x2 で作れなくてよいか** / **苗の緑と `GROW_SECONDS` 180 秒と `#status` の位置**

## ブラウザで見てほしいところ

**この周はありません**（`src/**` を 1 行も触っていないので、見た目は前の周のままです）。
**次の C の周は撮ること** —— 牛は見た目に出るので、`AUTODEV.md` の C-3 が掛かります。

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 未着手`** なので → **次は C（実装）の周**です。

- **`AUTODEV-SPEC.md` の全文を根拠にすること**（要約しない）。触るファイルに当たる
  決まりごとは自分で引くこと: `grep -l '"src/mobs.ts"' rules/*.md` →
  **`rules/mobs.md`**、`src/items.ts` なら **`rules/items-survival.md`**
- **`test/mobs.test.ts` の `census()` の `counts` に `牛: 0` を足すこと** ——
  忘れると `counts[name]++` が `NaN` になり、数えているつもりで緑になります
- **`test/blocks.test.ts` の「共有帯のアイテムは …16 個（129 まで）」は必ず赤くなります。**
  **ゆるめるのではなく 19 個・132 まで に数え直すこと**
- **形は当たり判定（±0.45 x 高さ 1.4）に全部収めること。** `longBody` の例外は
  豚と羊だけで、**そこへ牛を足さないこと**（足すと形の点検が丸ごと外れます）
- **C-3（撮って自分の目で見る）が掛かります。** `npm run build` →
  `(npx --no-install http-server dist -p 8080 --silent &)` → `node tools/browsershot.mjs` と
  `npm run shot -- mobs`。**撮ったら `Read` で開くこと**

**音のテスト（`test/audio.test.ts`）は揺れます。赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。**この周は 3 回走らせて 3 回とも緑**でした。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL が動かない」は開発者の手元の devcontainer の話です）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
