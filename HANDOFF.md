# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-13・AUTODEV 78 のあと）

- `npm test` **3506 件すべて成功**（**±0**。**B の周なので `src/**` も `test/**` も 1 行も
  触っていません**）。`npm run typecheck` 緑。**`npm run build` と `npm run bench` は
  走らせていません**（どちらも `src/**` を触った周だけ）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1450 行**（テストの数え方。**±0。1 文字も開いていません**）
- **アイテム 125 種・`MAX_ITEM_ID` 163・111..255 の空きは 92**（±0）・
  **1..63 の空きは 9**（±0。55..63）
- **立方体 41 / 非立方体 79**（±0）・**モブ 10 種類**（敵対 5）・**レシピ 66 本**・
  **`FUEL` 9 行 / `SMELTING` 9 行**・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 未着手`（30a・苗木）120 行**・
  **`AUTODEV-QUEUE.md` 98 行（未着手 9 件）**

## この周でやったこと（AUTODEV 78 の B・クラウドの無人の周）

**判定表は 2 行目**（未着手 8 件で 1 行目は外れ・`AUTODEV-SPEC.md` が `状態: 済` だった）で
**B（仕様）の周**でした。取ったのは**キューの先頭・30（苗木）**です。

**キューの 30 を 30a / 30b に割りました。** キューの行そのものが「120 行に収まらなければ
『落ちて植わるところまで』と『育って木になる』に割ること」と言っていて、実際に収まりません
（24a / 24b・25a / 25b と同じ形）。**仕様書にしたのは前半（30a）だけ**で、後半は
`AUTODEV-QUEUE.md` の 2 行目に書き戻してあります。

**30a は「オークとトウヒの苗木が葉から 5% で落ち、土・草・耕地の上にだけ立って、掘れば戻る」**
（**育ちません**）。**ブロック 164 / 165 の 2 個**で、`variantOf` を持たないので同じ番号で
自動的にアイテムになります（**アイテム側の番号は取りません**）。

**取る前に数え直して分かったこと 3 つ**（仕様書の 0 に書いてあります）:

- **⚠ オークの葉は山が 2 つとも埋まっています**（1 山目 = 棒 10% を `roll`、2 山目 =
  リンゴ 0.5% を `extraRoll`）。**苗木は 3 山目**で、そのままでは載りません。**乱数を
  3 本目に増やすと `breaking.ts` と `main.ts` に及ぶ**ので、仕様書は
  **2 山目を `extraRoll` の帯で 2 候補に分ける**（リンゴ 0..0.005 → 苗木 0.005..0.055）
  形にしました。**リンゴと苗木は同時に落ちません**（本家は独立。手触りの差です）
- **植える経路はもう通っています** —— 苗木はブロックなので `placing.ts` の `tryPlace()` が
  そのまま置き、**`main.ts` は 0 行の見込み**（種専用の `tryPlant()` は触りません）。
  ただし**「土の上だけ」の仕掛けはありません** —— `supportFace: FACE_YN` は
  「真下が平らな固体」までなので、いまのままだと石でも板でも立ちます。仕様書は
  **`needsSoil` の表 1 本と `supportsBlock()` の 1 行**（サトウキビの例外と同じ置き場所）に
  しています。**`canSupport()` はゆるめないこと** —— 松明が草むらに刺さります
- **`MAX_ITEM_ID` は 163（木炭）なので伸ばすこと。** 共有帯にブロックを足しただけでも、
  忘れると**クリエイティブの一覧にだけ出てこないブロック**になります（型でも止まりません）

**30b（育って木になる）に先に分かっている壁**: `worldgen.ts` の `stampTree()` は
チャンクの `Uint8Array` に直に書くので、**`setVoxel` の側からはそのまま呼べません**。
幹と葉を `setVoxel` で組む道が要り、**列をまたぐぶんは書けなかったら持ち越す**
（`growCane()` と同じ作法）。キューの 30b の行に書いてあります。

## 次にやること

**次は C（実装）の周** —— 未着手が 9 件なので 1 行目は外れ、**`AUTODEV-SPEC.md` が
`状態: 未着手`** なので 3 行目に当たります。

- **`AUTODEV-SPEC.md` を全文サブエージェントに渡すこと**（要約しない）。当たる層 2 は
  `grep -l '"src/blocks.ts"' rules/*.md` で引くこと（`blocks-shapes.md` / `items-survival.md` /
  `worldgen.md` / `beds.md` / `stateful-blocks.md` / `vitals.md`。`test/**` を触るので
  `testing.md` も）。**背景（async）で起動しないこと**。返らないなら自分で実装すること
- **取る ID は 164（オークの苗木）・165（トウヒの苗木）**（`ROADMAP.md` の予約表）。
  **共有帯の空きは 92 → 90** になります
- **`main.ts` は開かないこと**（0 行の見込み。1 行でも要るなら止まって人を呼ぶ）
- **見た目に出ます**（一覧の色 2 つと `cross` の板）—— **撮って `Read` で見ること**

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません**（`src/**` を 1 行も触らない B の周のため）。
**持ち越しはすべて前の周のままで、下に並べたものはまだ誰も見ていません**:

- **木炭（29）**: **一覧で「炭」に見えるか**（`0x56473f`。いちばん近いのは石炭鉱石と
  ソウルサンドで 24.1・石炭からは 64.0。**炭らしい暗い色は一覧でいちばん混んでいる帯**で、
  素直な `0x403a36` は火打石から 5.8 しかありません）
- **道具の修理（28）**: **傷んだ道具 2 本を並べたときの「傷の帯」で「どれだけ直るか」が
  読めるか**（帯は残り割合の幅だけで「＋何回ぶん」は出ません）/ **半分減った道具 2 本が
  「新品に戻る」のが気前よすぎないか**（本家のままの式。**一番お得な遊び方が
  「半分減ったら 2 本合わせる」になります**）
- **スケルトン（11）**: **矢が当たる高さ**（10m で足元から 0.27m。**間合いを縮めれば
  高く当たります**）/ **2 秒に 1 発・重み 3** が「詰めるか逃げるか」を選ばせる速さか /
  **間合いを取らず近づいて殴ってくる**のが変に見えないか（本家は後ずさりします）/
  **骨色 `0xcdc8b0`** が「骨」に見えるか（いちばん近いのは砂で 24.6）
- **防具（27b）**: 防具枠の置き場所（盤面と収納のあいだ・横 1 列）/ 1 文字のラベルで部位が
  分かるか / **革一式（防具点 7）で 28% 減が「固くなった」と感じられるか** /
  革の色 4 つが「革」に見えるか / **革 24 枚（＝牛 24 頭）で一式**が見合う値段か。
  **⚠ 一覧と防具枠の名前が 2 行に折れて絵に被る件**（`style.css` の `.slot .label`。
  **5 文字の「革のズボン」で出ます**）は**人の判断待ち**です
- **フェンス（26a / 26b）**: `shots/fence.png` に 5 通りが写っていて面の欠けも裏返りも
  ありません。**「柵らしく見えるか」**だけ見てください。**⚠ フェンスを 52fps 未満で
  跳び越せる件**は残っています（余裕 0.0117。直すなら**跳躍の積分**か **1.5 の側** ——
  **数値では決められないので人の判断です**）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から陸と氷の境目が読みにくい**
  （氷 `0x8fc4f2` はいちばん近いガラスから 34.3 しか余裕がありません）
- **氷（25a）**: **滑りの手ざわり**（離してから 1 秒で 0.59m。土は 0.099m）。**本家の氷は
  もっと長く滑ります** —— 伸ばすなら **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他（まだ誰も見ていません）**: クモの巣の歩き 1/4 / 金のリンゴ `0xf0a800` の見分け /
  本棚が「本の詰まった棚」に見えるか / リンゴの色 / 葉 200 枚に 1 個 / はしごの登り 2.35 m/s /
  `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** / 砂糖の見分け /
  ボウルとシチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る・ミルクで毒が止まる /
  かまど・チェスト・作業台を開いて閉じる経路 / クモの速さ 6.0 / 投げた卵に使い道が無い /
  産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` にも `RULES-INBOX.md` にも 1 行も足していません。** 落とし穴は 3 つ踏みましたが、
**どれも仕様書の中（0 と 4）に書けば足りるもの**で、領域の決まりごとではありませんでした
（葉の 2 山が埋まっている・`MAX_ITEM_ID` を伸ばす・`stampTree()` が `Uint8Array` に直に書く）。
**`MAX_ITEM_ID` の件は `items.ts` のその行のコメントが既に言っていて**、
`rules/items-survival.md` に写すと二重になります（`CLAUDE.md` の「同じことを 2 つの層に
書かないこと」）。

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えるので、実質 119 行です**
（`test/docs.test.ts`。末尾の改行が 1 行に数えられます）。この周は 120 行で 1 度赤くなり、
**上限を上げずに本文を 1 行詰めて**緑にしました。

**`npm test` の音の 1 件（羊毛 < 雪 < 砂）はこの周も 1 度だけ跳ねました** ——
走り直して 2 回続けて緑。`rules/testing.md` のとおりで、退行ではありません。

## 人がやること

- **⚠ 設計の判断（50 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**で、**`AUTODEV.md` の「止まって人を呼ぶ条件」の 2 番にちょうど並んでいます**
  （ハードの上限は 1500 行）。**待っているのは 2 件だけ**です（**35 骨粉**・`use.ts` の
  19 通り目 / **24b ケーキをかじる**・最低 7 行）。**キューの上 5 件（30a..34）はどれも
  `main.ts` 0 行の見込み**なので、**判断が下りなくても 5 周は止まりません**。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（革一式を 1 度作れば永久に 28% 減）。
  **本家は減ります。** 足すなら `ARMORS` に回数の列と 5 つ目の減り方が要り、**それだけで 1 周**。
  **修理の側はもう待っています**（`findRepair()` は `wearable()` で書いてあるので 0 行）
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（上の節。手ざわりの判断）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 78 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` で数え直すこと** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見る必要があります）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を押してクリエイティブにし、`E` を押して末尾までスクロールしてから撮ること**
（`docs/browser-shots/README.md` の末尾）。**修理の帯を絵で見たいときも同じで、
空の盤面には何も写りません。**

**⚠ `npm run shot -- mobs` は 10 種類を 5m 間隔の一列で撮るので、1 体 1 体は小さくしか
写りません**（span 45m）。**形を確かめるなら寄って撮ること。**
