# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-26）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 2033 件すべて成功**（2005 → 2033。この周の 28 件は右クリックの振り分け）
- **進行（クリア導線）達成 12 / 13**（12 件とも本物。**仮は残り 1 件** ——
  「エンダードラゴンを倒せる」だけ）。**この周は進行を動かしていません**
  （`main.ts` の減量はクリア導線の項目ではありません）
- ブロック ID **1..63 の空きは 9 個**（この周は 1 個も使っていません）。
  向き違いは **110 まで**、**アイテム ID は 94 まで**（この周は 1 個も使っていません）
- `src/main.ts` は **1445 行 / 上限 1500（余白 55 行）**。**`main.ts` の次元の分岐は 0 件**（上限 2）

## 直前にやったこと — TASKS 2-12b（`main.ts` の余白を作る）

**右クリックの振り分け（11 通り）を `use.ts` へ出しました。挙動は 1 つも変えていません。**
1499 → 1445 行。**上限は上げていません。**

| どこ | 何が入ったか |
| --- | --- |
| **`use.ts`（新規）** | **振り分けは全部ここ。** `decideUse(aim, facts)` = `UseFacts` → `UseAction`（純粋） |
| `main.ts` | `useOrPlace()` は注文を貼る `switch` だけに。`throwEye` / `igniteAt` / `placeHeld` に割った。−54 行 |
| `test/use.test.ts`（新規） | 28 件。11 通りの表・並び順 3 つ・断る理由・クリエイティブ・置く側へ渡る値 |
| `test/ui.test.ts` | `routed` に 2 件（`decideUse(` と **呼び出しの形** `useOrPlace();`） |

### 決めたこと

- **`placing.ts` に寄せるのではなく `use.ts` を作りました。** あちらは
  「置ける／点けられるか」の**可否**を持つファイルで、こちらが持つのは
  **そもそも置く話に来るかどうか**（器・バケツ・アイ・火種・弓・食べ物が先に取る）。
  混ぜると、`tryPlace()` が作業台やかまどを知ることになります
- **並び順そのものが判断です。** 入れ替えると現物を狙うまで気付けないものが 3 つあり、
  それぞれに判定を置きました:
  1. **器（作業台・かまど・チェスト・ベッド）が先** —— あとにすると、器の上に立って
     ブロックを置こうとしたときに置くほうが勝ち、**作業台が開けなくなります**
  2. **枠にアイを嵌める（`fitEye`）は投げる（`throwEye`）より先** —— 逆にすると、
     枠を狙っても手からアイが飛んでいって**永久に嵌まりません**（枠は地下 18 マス）
  3. **バケツは食べ物より先**（押しっぱなしではなく 1 回で終わるため）
- **`aim` が無くても効くものがあります**（食べる・弓・バケツ・アイを投げる）。
  **空を向いたまま食べられないのはおかしい**ので、`aim` の有無で先に弾かないこと
- **置ける／置けないは決めません。** 置けないアイテムも `place`（`base` が `AIR`）で返し、
  可否は `placing.ts` のまま。**2 か所で弾くと、片方だけ直したときに食い違います**
- **クリエイティブぶんは呼ぶ側で込みにします**（`UseFacts.hasArrow`。`bow.ts` の
  `DrawContext` と同じ約束）。`use.ts` が持ち物を見に行き始めると、
  「矢があるか」の判断が 2 か所になります

### 確かめたこと

- **わざと壊して 7 通りとも赤くなることを確かめました**（**コミットしてから壊しました**）:
  かまどを `baseBlock` で見ない → 1 件 / 嵌めると投げるを入れ替え → 2 件 /
  矢が無いときに黙る → 2 件 / クリエイティブでも食べる → 1 件 /
  バケツに狙う先を要求 → 1 件 / **`useOrPlace();` の配線だけ外す → 1 件** /
  `main.ts` に `isBucket(` を書き戻す → 1 件
- **配線だけ外したときに反応したのは「呼び出しの形」のほうだけ**でした
  （`decideUse(` は `useOrPlace()` の定義の中に残るので緑のまま）。
  2-12 で踏んだ穴がそのまま再現したので、下の「踏んだ穴 1」は生きています
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません
- **見た目・音・GLSL は 1 行も触っていない**ので `REVIEW.md` に追記はありません
  （2-3c の減量の周と同じ扱い）。**`TUNING.md` も触っていません**（新しい数値がありません）

### 前の周（2-1 〜 2-12）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージを書かない**）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ）
- **2-7**: 投げたエンダーアイが要塞の方を向く（`stronghold.ts` 新規）
- **2-8**: 要塞（石レンガ 53 + エンドポータルの部屋 + 枠 51。地面から 18 マス下）
- **2-9**: エンドポータルの起動（`endportal.ts` 新規。枠 12 個にアイ）
- **2-10**: エンド次元（`endgen.ts` 新規。エンドストーン 50 の島 1 つ。**達成 12 / 13**）
- **2-11a**: エンドの黒曜石の柱 10 本（`endgen.ts` の `PILLARS`）
- **2-11b**: エンドクリスタル（**ブロック 54**。判断は `crystals.ts`）
- **2-12**: 弓と矢（**アイテム 93 / 94**。判断は `bow.ts`）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 /
  2-8 / 2-9 / 2-10 / 2-11a / 2-11b / 2-12 の 17 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

0. **わざと壊す前に、必ずコミットしてから始めること**（2-11b で `git checkout -- src test` を
   使い、コミット前だったので実装が丸ごと飛びました）。この周もコミット後に 7 通り試して、
   毎回 `git checkout HEAD -- src test` で戻しています。
1. **「判断を別ファイルへ出した」の見張りは、呼び出しの側も見ること**（2-12 / この周）。
   `test/ui.test.ts` の `routed` に判断の名前だけを並べると、**その語は呼ぶ関数の定義の中にも
   あります。** 定義を残したままマウスの配線だけ外すと全部緑のまま通ります。
   **この周も実際にそうなりました** —— `useOrPlace();` を消しても `decideUse(` は
   定義の中に残るので、反応したのは**呼び出しの形のほうだけ**でした。
2. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（これまで 8 度）。
   **残る仮は 1 件**（「エンダードラゴンを倒せる」）で、
   `sourceHas("src/mobs.ts", "dragon")` なので**同じ形で化けます** ——
   `MobDef` に `dragon` を書いた瞬間に 13 / 13 になります。**気を付けてください。**
3. **`solid` でないブロックには飛び道具が当たりません**（2-11b）。
   `collisionBoxes()` が `solid` でないブロックに**空の箱**を返すためで、
   **「通り抜けられるが矢は当たる」はいまの仕組みでは書けません。**
4. **未読み込みの列を「無い」と読まないこと**（4 度）。`getVoxel` は未読み込みで AIR を
   返すので、**`hasColumn` を見ないと「10 個とも砕けた」と読みます。**
5. **「本物の `World` で往復する」テストは、遠い所を先に読み込むこと**（2-11b）。
6. **場所の限界は「実測した最小値」ではなく「式」で決めること**（2-11a）。
7. **「面積で形を測る」判定は真円を通します**（2-10）。**形そのものを測ること。**
8. **座標の移し方は次元ごとに違います**（2-10）。
9. **「探して無ければ建てる」を別の次元に使い回さないこと**（2-10）。
10. **同じ向きが並ぶ列から中心を逆算しないこと**（2-9）。
11. **手で並べた試験場は、建てる側との食い違いを見つけられません**（2-9）。
12. **構造物の `extent` は「どこに掛かるか」の唯一の材料です**（2-8）。
13. **1 チャンクに収まった標本だけを見ていると、端の欠けを見逃します**（2-8）。
14. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（2-7）。
15. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字まで。
16. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）。
17. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の矢・火球で死にます。
18. **相手を先に、ブロックをあとに見ること**（2-4b）。
19. **`world.seed` をセーブや場所の計算に使わないこと**（次元では塩を混ぜたあとの値）。
    種は `worldSeed`。
20. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）。
21. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）。
22. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）。
23. **`test/audio.test.ts` が揺れます**（これまでに 3 つの判定が落ちました。どれも
    **書き出した波形がまるごと無音になる**形）。**この周は `audio.ts` も `sfx.ts` も
    1 行も触っていません。** **赤が出たらまず `npm test` をもう一度**、
    2 回続けて同じ判定が落ちたときだけ本気で見てください
    （`world.test.ts` の p99 とまったく同じ扱いです）。

## 次に取るもの

1. **2-13（エンダードラゴン）。上から順なので、ここが唯一の未着手です。**
   - **回復のもとは `crystals.ts` の `liveCrystals(world)`**（生きていると
     **確かめられた**ものだけ。`unknown` は数えません）
   - **`mobs.ts` に `dragon` という語を書いた瞬間に進行が 13 / 13 に化けます。**
     書くなら**同じ周で本物の判定に差し替えること**（上の「踏んだ穴 2」）
   - **弓はもう使えます**（矢 1 本 = 最大 9）。戦場（柱 10 本 + クリスタル 10 個）は
     種に依らず同じ形です
   - **`main.ts` の余白は 55 行**（1445 / 上限 1500）。**足りなくなったら上限を上げず、
     2-12b と同じやり方でもう 1 つ出すこと** —— 次に出しやすいのは**左クリックの振り分け**
     （`mousedown` の「モブとブロックのどちらが手前か」とスポイト。
     `use.ts` に `decideAttack()` を足す形にすれば、器も見張りもそのまま使えます）
   - **出口（エンドから戻るポータル）もこの周の話です。** `portalTarget(END, "end")` は
     定義してあるのに、エンドに面が 1 枚も無いので通っていません（`rules/dimensions.md`）
2. 2-13 が終わると**上の並びに `[ ]` が残りません。** そのときは `TASKS.md` 末尾の
   「取るタスクが無くなったら」に従うこと（**実装もコミットもしない**）
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 8 件。うち 3 件は新しいファイル
   `.claude/rules/crystals.md` / `.claude/rules/bow.md` / `.claude/rules/use.md`。
   `paths` の行も書いてあります**）/ `REVIEW.md` の一括確認（**いま 17 節**）/
   `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **矢は拾い直せません**（本家は刺さった矢を拾えます）。刺さった矢は 60 秒で消えるので、
  撃ちっぱなしです。拾えるようにするなら `projectiles.onExpire` から `drops` へ渡すところから。
- **弓を引いても歩く速さは変わりません**（本家は遅くなります）。
- **弓に耐久値はありません**（道具ぜんぶ同じ）。
- **矢は火矢になりません／エンチャントもありません。**
- **クリスタルは爆発しません**（本家は当たると爆発してプレイヤーも吹き飛ぶ）。
- **クリスタルの上に乗れます**（`solid`。矢を当てるための副作用）。
- **クリスタルは素手で 1 秒で壊せます**（柱の上まで登れば、弓が無くても壊せます）。
- **ドラゴンとクリスタルを繋ぐ光の線（ビーム）はありません。**
- **エンドからは戻れません**（本家と同じ。出口は 2-13 でドラゴンを倒すと開く想定）。
  **落ちて死ねばリスポーン地点へ戻る**ので詰みにはなりませんが、**持ち物は 5 分で消えます。**
- **エンドの島の上にはモブが 1 体も湧きません**（空の明るさが 0.7 固定なので
  `spawnLight` が 10.5 になり、敵対の上限 7 を超えます）。**掘った穴の中は暗いので湧きます**
- **エンドの島は 1 つだけです**（本家は外側にも小さい島がたくさんあります）
- **要塞は「部屋 1 つ」だけで、中に入る道がありません**（掘って降りる前提）
- **投げたエンダーアイは、近づいても落ちません／減りません**（本家は 20% で壊れる）
- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  `eyeShot()` に次元を渡して「オーバーワールド以外では null」にするのが素直です
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒。`smelting.ts` に 1 行）
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）。
  いまは右クリックしても何も起きません（`use.ts` の表に無いので `place` に落ち、
  `placedBlock()` が `AIR` を返して `tryPlace()` が黙って断ります）
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方を全部見直すことになるので、**独立したタスクとして決めてください**
- **ネザーでも寝られます**（本家はベッドが爆発する）。**寝ても時間は「進みません」**
- **火球は火を点けません**（火そのものが無いので、ブロックとして足すところから）
- **`F3` の `biome` はネザーでもエンドでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラック・ソウルサンド・要塞の部屋の中にもゾンビが湧きます**（`spawnOn` を持たないため）
