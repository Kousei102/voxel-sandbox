# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-08・AUTODEV 61 のあと）

- `npm test` **3322 件すべて成功**（**±0**）・`npm run typecheck` 緑
- **`npm run build` は走らせていません**・**`npm run bench` も走らせていません**
  （**B の周なので `src/**` に 1 行も差分がありません**。触ったのは
  `AUTODEV-SPEC.md` と `rules/worldgen.md` と この `HANDOFF.md` の 3 つだけ）
- **音の一群は 1 度も跳ねませんでした**（走らせたのは 1 回）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行（±0。この周は 1 文字も開いていません）**
- **アイテム 118 種・`MAX_ITEM_ID` 156・111..255 の空きは 99・1..63 の空きは 9**
- **立方体 41 / 非立方体 78**・**モブ 9 種類**・**レシピ 60 本**・**精錬 7 本**・
  **燃料 8 種**・**食べ物 11 種**・**バイオーム 10**・**構造物 2**・**次元 3**・
  **`DamageCause` 9 種**（この周は全部 ±0）
- **`AUTODEV-SPEC.md` は `状態: 未着手`（113 行）**・**`AUTODEV-QUEUE.md` 120 行**（未着手 6 件）

## この周でやったこと（AUTODEV 61 の B・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 済`・未着手が 6 件」で 2 行目 = B（仕様）の周**でした。
**キューの先頭 = 25b（凍った海）の仕様書を書きました。** `src/**` は 1 行も触っていません。

**取る前にコードで数え直しました** —— `grep` で `FROZEN` も「凍」も `src/` に
1 件も無く（`blocks.ts` のコメントが「凍った海は 25b」と予告しているだけ）、
`BIOMES` は 10 種類のままなので**未実装で確定**です。

**仕様の前提を実測で裏取りしました**（読み取りだけの試し打ち。リポジトリに差分は残していません）:

- **凍った海は世界の 1 割前後あります** —— `height < SEA_LEVEL` かつ `気温 <= SNOW_TEMP` の
  割合は、既存のテストと同じ ±4000 を 97 マスおきに舐めて **シード 12345 で 11.8%**・
  **777 で 9.8%**・**2468 で 11.9%**。**海 26.4% が 14.6% と 11.8% に割れるだけ**なので、
  `test/worldgen.test.ts` の**「N 種類すべて生成される」も「45% を超えない」も緑のまま**通ります
- **原点のまわりにも在ります**（±400 を 4 マスおきに 40000 点舐めて、12345 で 9689 点）。
  だから **`node tools/browsershot.mjs` に写る見込み**です（`rules/worldgen.md` の
  「まれなバイオームは原点の走査に出ない」に当たりません）
- **`main.ts` は ±0 行**（数えました）—— F3 の表示は `biomeName()` が `BIOMES` の表を
  引くので、バイオームを 1 つ足すだけで勝手に増えます

**踏みやすい罠を 3 つ、仕様書の「禁じ手」に名指しで置きました**: 氷に `blocksSky` を
書かない（**氷の下の海が真っ暗になります**）/ `FROZEN_OCEAN` の `surface` を `SNOW` に
しない（「砂と雪が接するのは海岸だけ」の見張りに当たるうえ、水の底に雪が敷かれます）/
`ICE` を `worldgen.ts` に import しない（`BiomeDef.seaSurface` に置く）。

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が **`状態: 未着手`** なので判定表の 3 行目です。
**`AUTODEV-SPEC.md`（凍った海 = 25b）を全文読んで、そのとおりに実装すること。**

- **ID は 0 個**（氷 156 はもう在ります）。**次にブロック番号が要るときは 157 から**（`ROADMAP.md`）
- **`main.ts` は ±0 行の見込み**です（上）。**開かないこと**
- **`npm run bench` は 3 回まわすこと** —— `generateChunk` の内側のループに分岐が
  1 つ増える唯一の周です（この箱は同じコードで 1.08〜2.04ms に振れます）
- **`biomes.ts` を触るので `rules/worldgen.md` を先に引いて読むこと**
  （`grep -l '"src/biomes.ts"' rules/*.md`）

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません**（`src/**` に差分が無いため）。**次の C の周で撮ること。**

**次の周に見てほしくなるところ（凍った海）**: **氷ごしに海の底が見えるか**（`alpha` 0.6。
薄いとガラスに見え、濃いと海が真っ黒に見えます）/ **雪の浜と凍った海の境目**が
「浜 → 氷」と読めるか / **氷の板の上を歩けることが見て分かるか**（歩けます。
`heightAt` は 1 マスも動きません）。

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
（理由は `TUNING.md`）。ほかに **`0x8fc4f2` が一覧でガラス・雪・ダイヤ鉱石と見分けられるか**。

**持ち越し（ケーキ）**: **上面 `0xffd0e4` が「ケーキ」に見えるか** /
**側面のスポンジ `0xe8c9a0` と下面 `0xd9b98a` の 3 色がケーキに読めるか**。
**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: **中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか** /
**素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか**。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか /
かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
**一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**）

**`rules/worldgen.md` に `Edit` で据えました**（`.claude/rules/` ではなくルート直下）。

1. **バイオームは `BIOMES` の末尾に足すこと。** `biomeDef(id)` は `BIOMES[id]` を
   引くだけなので、**添字と `id` が一致していないと別のバイオームの表が返ります**
   （`surface` も `filler` も `trees` も全部ずれるので、**地表が丸ごと別のブロックになります**）。
   どこにも書かれていなかったので据えました —— **25b がまさにここを踏む周**です。

## 人がやること

- **設計の判断（34 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**で、
  上限 1500 まで 51 行です。**次の 25b も `main.ts` は ±0 行の見込み**（生成側だけ）なので、
  **この判断を待たずに次の周は回せます**。待っているのは **24b（ケーキをかじる。最低 7 行）**
  と **27b（革の防具。セーブのキーで 1 行）** の 2 件です。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
  （**判定の意味が変わるので無人の周では触れません**）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 44 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`。
いまの `AUTODEV-SPEC.md` は 113 行）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
**ただし自然生成しないブロックは browsershot には 1 マスも写りません** ——
氷・ケーキ・クモの巣・本棚は `npm run shot -- <名前>` が唯一の経路です。
**凍った海は自然生成するので、25b は browsershot に写る見込みです**（上で数えました）。
