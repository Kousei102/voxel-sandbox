# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2332 件すべて成功**
  （**前の周から増えていません** —— この周は仕様書 1 枚だけで `src/**` も `test/**` も触っていない）
- `npm run build` は走らせていません（**`src/**` に 1 行も差分が無い周**なので不要）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1375 行 / 上限 1500**（この周は 0 行）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— ID は 0 個）
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（「道具の耐久値」）、`AUTODEV-QUEUE.md` の未着手は **9 件**
  → **次の周は C（実装）**
- **`RULES-INBOX.md` に未取り込みが 2 件**（`mobs.md` と `projectiles.md`）。**取り込むのは人が居る周**

## この周でやったこと（無人・B の周 1 種類だけ）

`AUTODEV.md` の判定表は「キューの未着手が 3 件以上」「仕様書が `状態: 済`」で **B（仕様）** に
当たったので、**キューの先頭 1 件を `AUTODEV-SPEC.md` に**しました。コミットは 1 つで、
**`src/**` と `test/**` には 1 行も差分がありません**（B の完了条件）。

### 取る前にコードで数え直した（B の決まり）

`Slot` は `{ item, count }` の 2 つだけ、`ToolDef` は `kind` / `tier` / `speed` の 3 つで
**回数を持っていません**。`items.ts` の `FLINT_AND_STEEL` と `BOW` のコメントにも
「耐久値がまだ無いので 1 個あれば何度でも」と書いてあります。**まだ入っていません。**

### 1 件を 2 件に割りました（120 行に収まらなかった）

キューの 3 番は「使うたびに減る（火打石と打ち金・弓も同じ形）」でしたが、**1 周では閉じません。**
傷を**インベントリの外**（落とし物・チェスト）でも保つには `drops.ts` の
`[item, count, x, y, z]` と `chests.ts` の 54 要素というセーブの形を両方いじる必要があり、
仕様書が 120 行を超えました（`AUTODEV.md` の B「120 行に収まらない仕様は 1 周で閉じない信号」）。
そこで **`AUTODEV-QUEUE.md` に `3b` として後半を書き戻し**、前半だけを仕様書にしています。

- **3（今回の仕様書）**: 掘るたびに 1 減り 0 で壊れる。回数は本家のまま
  木 59 / 石 131 / 鉄 250 / ダイヤ 1561。新しい `src/durability.ts` に判断を全部置き、
  セーブは **`SaveData.wear`（36 要素・省略可）** を 1 つ足すだけ（**`inventory` の形は変えない**）
- **3b（次の次以降）**: 落とし物とチェストでも傷が残る + 火打石と打ち金・弓。
  **3 だけでは「傷んだ道具を落として拾い直すと新品に戻る」抜け道が残ります** ——
  仕様書の禁じ手にも書いてあるとおり**わざと残した穴**なので、C の周で塞ごうとしないこと

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶんはありません**（仕様書 1 枚だけで、画面に出るものは 1 つも足していません）。

**前の周からの持ち越しが 3 件、まだ人の目を通っていません:**

- **大きいチェストの周のぶん**（`a795564`）—— チェストを 2 個横に並べて開くと
  **54 枠が 9 列 x 6 段**で出るか、単体だと**下 3 段が消えて 27 枠**になるか、
  **パネルが画面からはみ出していないか**、3 個を横一列に並べるとどれも 27 枠に戻るか
- **砂・砂利が落ちる周のぶん**（`6209e91`）—— 砂の柱の一番下を掘ると上の砂がその場で詰まり、
  **間に穴や浮いた砂が残らないこと**。**掘っていないのに崖や洞窟の天井が崩れていないこと**
- **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**と
  **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **9 件**（3 件以上あるので A ではない）、
**`AUTODEV-SPEC.md` は `状態: 未着手`** → **次は C（実装）の周**です。

- **`AUTODEV-SPEC.md` を全文読んで、そのとおりに実装すること**（要約しないこと。
  要約すると「触らないファイル」と「このタスク固有の禁じ手」が真っ先に落ちます）
- **サブエージェントはこの環境では同じ番の中で結果が返せません。** C の周は
  **親が自分で実装して構いません**（`AUTODEV.md` の C-1。前々周がそうしました）。
  点検（C-2）も**別を立てられないなら `git diff` を自分で読むこと**
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- 終わったら `AUTODEV-QUEUE.md` の **3 番の行だけ**を消すこと（**`3b` は残す**）

人がやること:

- 上の**ブラウザ確認 3 件**（この周のぶんは無し）
- `RULES-INBOX.md` の 2 件の取り込み
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）はまれに 1 件落ちます**（この周も 1 回目だけ
「音量がそのまま大きさに掛かる」が落ち、2 回目は 2332 件すべて緑でした）。
**2 回続けて落ちたときだけ退行として扱うこと。**

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
