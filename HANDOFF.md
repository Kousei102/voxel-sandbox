# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2672 件すべて成功**（前の周の 2624 件から **+48**）
- **`npm run build` も通ります**（この周は `src/**` を触ったので走らせました）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1433 行 / 上限 1500**（この周 **+13 行**。仕様書の「+15 行以内」に収まりました）
- **ブロック / アイテム ID: 1..63 の空き 9（±0）/ 111..255 の空き 135**（この周 **5 個使用**）
- **アイテムは 87 種**（83 → 87）。`MAX_ITEM_ID` は **120**
- **`AUTODEV-SPEC.md` は `状態: 済`** → **次の周は B（仕様）**（下の「次にやること」）
- **`RULES-INBOX.md` は未取り込み 7 件**（前の周の 4 件 + **この周の 3 件**）

## この周でやったこと（無人・C の周 1 種類だけ）

**`AUTODEV-SPEC.md`「クワと耕地（土を耕す）」を全文そのまま実装しました。** 差し戻し 0 回。

**クワ 4 本（木・石・鉄・ダイヤ = 117..120）と耕地（`FARMLAND` = 116）。** 土か草を右クリック
すると耕地になってクワが 1 減り、耕地を掘ると土が 1 個落ちます。**育つものはまだ植わりません**
（割った後半 = `AUTODEV-QUEUE.md` の 12 番）。

**`variantOf: DIRT` の 1 行で、アイテムもドロップ表も 1 行も書かずに「持てないブロック」に
なりました**（点火中のかまど `FURNACE_LIT` と同じ仕掛け）。**共有帯に置いた最初のブロックで、
しかも立方体**です（`encodeFace` が 8 ビットなので置けます。`npm test` の「立方体 36」）。

**踏んだ罠**（仕様書の禁じ手 2 がそのまま当たりました。次に `ToolKind` を増やす人へ）:
`ToolKind` に 1 語足すと `TOOL_NAMES` は `Record<ToolKind, …>` なので typecheck が抜けを
教えますが、**`mobs.ts` の `TOOL_ATTACK` は `Record<string, number>` なので教えません。**
足し忘れると `attackDamage()` が **NaN** を返し、殴ったモブの体力が NaN になって**二度と
死にません。** `test/mobs.test.ts` に「クワで殴っても NaN にならない」を足して塞ぎました。

**既存のテストを 1 つ書き直しましたが、緩めていません**（**増やす側**）: `test/blocks.test.ts` の
「共有帯のアイテムは剣 4 本とシアーズの **5 個**」→「剣 4 本・シアーズ・クワ 4 本の **9 個**」。

**点検（C-2）は親が `git diff` を全部読んで自分でやりました**（書いた本人に確認させない）。
`typecheck` / `test` / `build` も親が走らせ直しています。

**サブエージェントの起動について（`AUTODEV.md` の C-1 に関わる実測）**:
**`run_in_background: false` を渡しても背景で起動しました。** そこで番を閉じずに、
**転記ファイルに `"type":"result"` が出るまで前景で待つ**形にして同じ番の中で受け取りました
（`sleep` は使えないので `timeout 5 tail -f /dev/null` で待つ）。**13 分 23 秒。**
**この待ち方なら C-1 をサブエージェントに出しても周を落としません。**

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は撮って、自分の目で見ました**（C-3）。**新しく 2 枚**（`docs/browser-shots/`）:

- **`creative-hoes.png`** —— クリエイティブの一覧が **83 → 87 枠**。末尾は
  `… シアーズ → 木/石/鉄/ダイヤのクワ`で、色は道具と同じ階層の色、**名前は 4 つとも出ています。**
  **耕地（116）は一覧に出ません**（`variantOf` が効いている証拠を目で確かめました）
- **`farmland.png`** —— **本物のブラウザで実際に耕した 1 枚。** `#debug` に
  **`target 耕地 (2, 45, -4)`** と `edits 3` が出ていて、緑の草原の中に**暗い茶色の 1 マス**が
  見えます。**console のエラーは 0 件**

**それでも人に見てほしいのは 1 つだけです**: **耕地の上面の色**（`0x59422d`。土 `0x6b533a` より
2 割ほど暗い茶）。**横と下は土のままなので、横から見ると土と見分けが付きません。**
畑らしく何マスも並べたときに「耕した」と分かるか、暗すぎて焼けた土に見えないかは、
数字では決められませんでした（`TUNING.md` の「クワと耕地」に触ったときの影響を書いてあります）。

**撮り方で踏んだものは `docs/browser-shots/README.md` の 9 枚目に書きました**
（`page.mouse.click()` はポインタロック中に視点を飛ばすので使わないこと、など）。

## 人がやること

- **`RULES-INBOX.md` の 7 件の取り込み。** **前の周の 4 件**（`items-survival.md` の
  「使って減る」の行 / `mobs.md` の「刈って取る」/ `use.md` の 2 件）と、**この周の 3 件**
  （`items-survival.md` の「減り方は 4 種類」/ `blocks-shapes.md` の `variantOf` /
  `use.md` のクワの居場所）。**本文はそのまま書いてあります**
- **この周の 3 件は、前の周の 2 件とわざと食い違わせてあります**（「3 種類」対「4 種類」、
  「12 通り」対「13 通り」）。**古いほうは直していません**（無人の周の決まり）ので、
  **取り込む人が新しいほうを採ってください。** どこが食い違うかは各節の冒頭に書いてあります
- **`RULES-INBOX.md` の 41 行目あたりの「いま未取り込みは 0 件です。」は古い行です**（剣の 2 件を
  取り込んだ時点のもの）。**末尾の「未取り込み 7 件」が正しい数**です
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**は
  まだ直っていません（`#status` は `bottom: 118px` 固定。`style.css:674`）。**`REVIEW.md` に積んであります**
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手は 6 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 済`**
なので → **次は B（仕様）の周**です。**`src/**` と `test/**` には 1 行も触らないこと。**

- **取るのはキューの先頭 = 12 番「種・小麦・パンと『育つブロック』（`crops.ts`）」。**
  **依存の 6（耕地）はこの周で済みました**（`FARMLAND` = 116 と `tilled()` / `tryTill()`）。
  **取る前に、その 1 件だけコードで数え直すこと**（B の決まり。`crops` / `wheat` / `小麦` /
  `seed` / `種` は `src/**` にも `test/**` にもまだ 1 件もありません —— この周に確認済み）
- **育ち具合はブロック ID を 8 個使わず、位置ごとの状態で持つこと**（`furnaces.ts` の器の形）。
  **種の出どころは草むら**（`TALL_GRASS` = 32。もうあります）
- **新しい番号は 121 から**（`ROADMAP.md` の予約表。116..120 はこの周が使いました）。
  **111 以降はブロックとアイテムで 1 本の番号列**なので、片側だけ見て取らないこと
- **120 行に収まらない仕様は 1 周で閉じない信号**です。その場で 2 件に割って、
  前半だけを仕様書にすること（6 番も同じように割りました）

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（緑）。
`audio.ts` / `sfx.ts` には 1 行も触っていないので、**直ったわけではありません。**
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
