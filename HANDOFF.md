# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-15・AUTODEV 84 のあと）

- `npm test` **3596 件すべて成功**（±0。**この周は `src/**` も `test/**` も 1 文字も触っていません**）。
  `npm run typecheck` 緑。**`npm run build` と `npm run bench` は走らせていません**（B の周なので不要）
- **音は 1 回目に 1 件跳ねて、2 回目から緑**（跳ねたのは**足音ではなく「声色（音程の倍率）が
  波形に出る」**で、高い声が 0.329 → **3.283**。**`rules/testing.md` の 135 行目からに
  そのまま書いてあるとおり**で、新しい落とし穴ではありません。走り直して以降は 3 回とも緑）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1450 行 / 上限 1500**（±0。**1 文字も開いていません**）
- **アイテム 129 種**（±0）・**`MAX_ITEM_ID` 165**・**レシピ 68 本**・**立方体 41 / 非立方体 85**
- **1..63 の空きは 7**（57..63）・**111..255 の空きは 88**（**次に取るのは 168**）
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（32a・119 行）・**`AUTODEV-QUEUE.md` 未着手 7 件**

## この周でやったこと（AUTODEV 84 の B・クラウドの無人の周）

**判定表は 2 行目**（未着手 6 件で 1 行目は外れ・`AUTODEV-SPEC.md` が `状態: 済`）で
**B（仕様）の周**。キューの先頭 **32「粘土ブロック・粘土玉・レンガ」** を仕様書にしました。

- **取る前にコードで数え直しました**: `clay` も `粘土` も `src/**` に 1 件も無く、
  **`BRICK`(12) は置けるのに作れない**（出どころが 0 本）のを確かめてから取っています
- **32 を 32a / 32b に割りました。** 7 項目を全部書くと **137 行**になり、120 行の上限
  （`test/docs.test.ts`。**1 周で閉じない信号**）を超えたためです。`AUTODEV.md` の B の
  とおり**前半だけを仕様書にし、後半をキューへ書き戻しました**（未着手 6 → 7 件）
  - **32a（この仕様書）**: 粘土（168）と粘土玉（169）。**海底に湧いて掘ると 4 個・2x2 で戻せる**
  - **32b（キューの 2 番目）**: レンガ（170）。**焼く 1 行と 2x2 で `BRICK`(12)**
- **決めた設計の要は 2 つ**（仕様書の 4 と 6）:
  - **`VEINS` は使えません。** あの表が効くのは `depth > 3`（石の中）で、**海底の砂の下には
    1 マスも出ません** —— 掘り当てられない粘土になります
  - だから **`BiomeDef.floorPatch`**（`{ block, chance, fill, shift, depth }`）を足して、
    **どのブロックが・どれだけ湧くかを `biomes.ts` に置きます**。`worldgen.ts` は
    塩 2 本と差し替えの 3 行だけ（**`CLAY` を import しない**。`seaSurface` に `ICE` を
    書かないのと同じ決まりで `rules/worldgen.md` の頭にあります）
- **`src/**` は 1 文字も触っていません**（B の完了条件）。差分は
  `AUTODEV-SPEC.md` と `AUTODEV-QUEUE.md` の 2 本だけです

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です
（未着手 7 件なので 1 行目は外れます）。**`AUTODEV-SPEC.md` を全文サブエージェントに渡すこと**
（要約すると 2 と 6 が真っ先に落ちます。`AUTODEV.md` の C-1）。

- **触るファイルに当たる `rules/*.md` を引いて渡すこと**:
  `grep -l '"src/worldgen.ts"' rules/*.md` → `worldgen.md`、`blocks.ts` / `items.ts` /
  `crafting.ts` の 3 本と `test/**`（`testing.md` は `paths` が glob なので grep に出ません）
- **ID は 168（粘土）と 169（粘土玉）**。**`ROADMAP.md` の予約表に 2 行足すこと**
- **生成を触るので `npm run bench` を 3 回**まわして中央値を見ること

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません**（`src/**` に差分が無いので、写るものが何も変わっていません）。
**32a の C の周では撮ること** —— クリエイティブ一覧の粘土と粘土玉の色、そして**海底の粘土**
（`npm run shot -- terrain` に写らなければ `tools/shot.ts` に場面 `clay` を 1 つ足す）。

**それでも人に見てほしいのはここです**（前の周から持ち越し。**直っていません**）:

- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **9 文字の「ネザーレンガハーフ」は 3 行に折れて枠を丸ごと覆います**
  （`docs/browser-shots/creative-brick-slabs.png`）。**人の判断待ち**
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
  （`0x7d8288` 対 `0x8a8f96` / `0x767b82`。本家も似ているので色はずらしていません）
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）/ **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）/
  **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` にも `RULES-INBOX.md` にも 1 行も足していません。** 踏んだのは
**音の揺れ（声色）**と**仕様書の 120 行**の 2 つだけで、**どちらも既に書いてあります**
（`rules/testing.md` の 135 行目から / `AUTODEV.md` の B の「120 行に収まらない仕様は、
1 周で閉じない信号です」）。**書いてあるとおりに割って進めたので、足すことがありません。**

## 人がやること

- **⚠ 設計の判断（56 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**で、**`AUTODEV.md` の停止条件 2 にちょうど並んでいます**（上限 1500）。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**・
  最低 7 行）。**キューの上 4 件（32a・32b・33・34）はどれも `main.ts` 0 行の見込み**なので、
  判断が下りなくても 4 周は止まりません
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（足すならそれだけで 1 周）
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周も 1 回目に 1 件跳ねました**
  （声色。走り直して緑）。**直すなら測り方**（窓を長く取る・複数回鳴らして中央値を見る）で、
  **判定はゆるめないこと**（`rules/testing.md`）。**キューに足すのは A の周の仕事**なので、
  ループからは足していません
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 84 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押してから撮ること**
（`docs/browser-shots/README.md` の末尾）。**⚠ 送り先は末尾とは限りません** ——
**低帯（1..63）の番号を取ったものは一覧の真ん中に出る**ので、`scrollTop = scrollHeight`
では 1 枠も写りません。**`.label` の文字列で枠を探して
`scrollIntoView({ block: "center" })` を呼ぶこと。**

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**海底の粘土（32a）も同じ扱いになる見込み**です。
