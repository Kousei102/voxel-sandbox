# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-03）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は C（実装）の周**でした。取ったのは `AUTODEV-QUEUE.md` の **7c-1
  「鶏が卵を産む（`MobDef.laying`）」**
- `npm test` **2904 件すべて成功**（前の周の 2884 + 新しい 20 件）。`npm run typecheck` /
  `npm run build` も緑。**`npm run bench` は走らせていません**（生成もメッシュ化も触っていないため）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 126**（**この周は 1 個使用 = 129 の卵**）
- **アイテムは 94 種**、`MAX_ITEM_ID` は **129**。**モブは 7 種類**。`SaveData` は `version` 1
  （**1 バイトも増えていません** —— モブを保存しないので `layTimer` も保存されません）
- **`main.ts` は 1449 行のまま 1 行も触っていません**（上限 1500）
- **`AUTODEV-SPEC.md` は `状態: 済`**（120 行）→ **次の周は B（仕様）**
- `REVIEW.md` の未着手は 0 件。`RULES-INBOX.md` は層 3（スキル）専用です
- **そのあと人が居る周で 1 つ**: この周が `docs/autodev-log.md` にしか書かなかった教訓 2 件
  （**フレーム数の決め打ちで境目を測らない** / **長く回すテストは「消えていない」を先に見る**）を
  **`.claude/rules/testing.md` へ据えました**（`npm run rules:install` の初めての実運用）。
  あわせて **`LOOP.md` 3 と `AUTODEV.md` C の完了条件に「踏んだ落とし穴を据えたか」を追加**
  （**無ければ `HANDOFF.md` に「決まりごと 0 件」と書く** —— 黙って落とさないため）。
  `npm test` は **2904 件緑のまま**

## この周でやったこと — 鶏が卵を産む（アイテム 129）

**鶏が 300〜600 秒ごとに足元へ卵を 1 個産みます**（本家の 6000〜12000 ティック）。
**倒す必要はありません** —— 羊毛に続いて**倒さずに取れる 2 つ目**で、違うのは
**プレイヤーの操作ではなく時計が起こす**ところだけです。

**`main.ts` も `drops.ts` も `storage.ts` も `projectiles.ts` も 1 行も触っていません。**
出口は倒したとき・刈ったときと**同じ `onDrop`** で足りました。足したのは:

- `src/mobs.ts` —— `LayRule`（誰が・何を・何個・どれだけの間隔で）/ `MobDef.laying`
  （既存 7 定義に `laying:` を 1 行ずつ。**鶏だけ表・ほかは `null`**）/ `Mob.layTimer` /
  **`Mobs.lay()` を新設して `update()` の `step()` の直後から毎フレーム呼ぶ**
- `src/items.ts` —— `EGG = 129` と `item({...})` の 2 行（**置けず・道具でもなく・
  食べ物でもなく、積めるのは 16 個まで**）
- `test/mobs.test.ts` / `test/blocks.test.ts` —— 下の 20 件

**踏まずに済んだ穴**（仕様書が先に書いていたもの）: `layTimer` を 0 から始めない
（湧いた全員がその場で産む）/ `lay()` を 5Hz の `think()` に置かない（遠くて動かない
個体の時計が止まる）/ 次の間隔を入れ直してから `onDrop` を鳴らす。

**踏んだのはテストの側で 2 つ**（`docs/autodev-log.md` に詳しく）:

1. **「2 秒 = 120 フレームぶん引けば 0 になる」は成り立ちません**（`1/60` を 120 回引くと
   1e-15 残る）。**「鳴った最初のフレームを探してその 1 つ手前を見る」**形に直しました
2. **10 分ぶん回すと鶏が徘徊で試験場を出て `DESPAWN_DISTANCE`(72) で消えます。**
   毎フレーム位置を貼り直して留め置き、**「回しているあいだ消えていない」を先に判定**
   しています（`rules/testing.md` の「先に『ちゃんと動いた』ことを確かめる」）

## ブラウザで見てほしいところ（見た目に出るもの）

**この周に撮った絵**: 定番 5 枚（`node tools/browsershot.mjs`。**console のエラー 0 件**・
WebGL 2.0・`inventory.png` も `game.png` も前の周と同じ）と、**クリエイティブの一覧の
末尾** `docs/browser-shots/creative-egg.png`（枠 **94 個**・末尾は羽根 → **卵**・
`title` は **`卵 x16`**）。**どちらも `Read` で開いて自分の目で見ました。**

見てほしいのは 4 つ（1 が今回、2〜4 は持ち越し）:

1. **卵（`0xf7f0e0`）と羽根（`0xe8e4dc`）が一覧で隣り合っていて、どちらもほぼ白です。**
   前の周の「羽根の白が見分けが付くか」と**同じ 1 件**になったので、2 つまとめて
   決め直すのが早いと思います（`E` を押して末尾へ）
2. **産卵の間隔 300〜600 秒は本家のままですが、ここでは長すぎるかもしれません。**
   モブは 72m で消え保存もされないので、**実際にはほとんど産まない**はずです
   （`TUNING.md` の「卵」）。囲いを作って待つ意味を出すなら短くしてください
3. **矢が 2x2 で作れなくなってよいか**（AUTODEV 17）。戻すなら `crafting.ts` の 1 行
4. **苗の緑が草原に紛れる**件と **`GROW_SECONDS` 180 秒**、**`#status` の位置**

## 人がやること

- **`loop/endgame` にはまだ層 2 の設置の仕組み（`npm run rules:install`）が届いていません。**
  行き渡らせ方（`master` 経由でマージするか、cherry-pick するか）は決めてください
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 済`** で、`AUTODEV-QUEUE.md` の未着手が **5 件**
（3 件以上ある）なので → **次は B（仕様）の周**です。

- **キューの先頭は 7c-2「卵を投げる側」**（`projectiles.ts` の表に 1 行 + `use.ts` の
  投げる判断 + `main.ts` の配線。**ID は 0 個** —— 卵はもう 129 にあります）。
  **取る前にその 1 件だけコードで数え直すこと**（`AUTODEV.md` の B）
- 残りは 8 牛 / 9 クモと糸 / 10 スケルトンと骨 / 11 刈られた羊が見て分かること
- **ID は `ROADMAP.md` の予約表から**（**次は 130。111 以降はブロックとアイテムで
  1 本の番号列**）
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に 1 件だけ赤 →
走らせ直したら緑。**この周は緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL が動かない」は開発者の手元の devcontainer の話です）。
**クリエイティブの一覧を撮る手**は `docs/browser-shots/README.md` の 12・14・15 枚目
（`#mode` をクリックしてから `E`、そのあと `#creative` の `scrollTop` を最後まで送る）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
