# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- `npm test` **2962 件すべて成功**（この周は 1 件も増減していません）・
  `npm run typecheck` 緑。**`npm run build` と `npm run bench` は走らせていません**
  （**`src/**` と `test/**` に 1 行も差分が無い周**のため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1464 行**
  （1 行も触っていません）
- **アイテム 98 種・`MAX_ITEM_ID` 133・111..255 の空きは 122・1..63 の空きは 9**
- **モブ 9 種類**（受動 4 / 敵対 4 / ボス 1）

## ⚠ 次に `main.ts` を触る周がまず見ること: 1464 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
**牛・クモ・この周の 4 周とも `main.ts` を 1 行も触っていないので、持ち越しです。**
**次に触る周は、先に判断を別ファイルへ出すこと。**

## この周でやったこと（AUTODEV 22 の B・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 済`・`AUTODEV-QUEUE.md` の未着手が **3 件**（3 件未満では
ないので A には入りません）だったので **B（仕様）の周**。当たった 1 種類だけをやりました。

- **`AUTODEV-QUEUE.md` の先頭の 10 番「弓を本家の形に戻す（羊毛 3 → 糸 3）」**を
  `AUTODEV-SPEC.md` にしました（**110 行**。上限 120 で、`test/docs.test.ts` は
  `wc -l` より 1 多く数えるので 119 が実質の上限。`rules/testing.md`）
- **取る前にコードで数え直しました**（B の決まり）: `src/crafting.ts:147` の弓は
  まだ `key: { S: STICK, W: WOOL }` で**羊毛のまま**・`src/items.ts:319` の
  `STRING = 133` は**もうある**（依存は解けている）・`crafting.ts` に `STRING` の
  import は 0 行 ＝ **糸の使い道はいまだに 0 本**
- **`src/**` と `test/**` に 1 行も差分がありません**（B の完了条件）

### 仕様書に書いておいた、実装の周が踏みそうな 3 つ

1. **`test/crafting.test.ts` の文字の表の `L: WOOL` を消さないこと** —— 消すと
   `WOOL` の import が余って **typecheck が落ちます**。`G: STRING` を**足す**ほうです
2. **`crafting.ts` の `./blocks` の `WOOL` もベッド（106 行）が使うので残すこと**
3. **`test/crafting.test.ts` にベッドの判定が 1 件もありません** —— 弓から羊毛が
   抜けると**羊毛の使い道はベッドだけ**になるので、「ベッドは今までどおり羊毛 3 + 板 3」を
   新しく足すよう仕様書に書きました（「羊毛では弓が作れない」と対で）

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 未着手`** なので → **次は C（実装）の周**です。
**`AUTODEV-SPEC.md` を全文サブエージェントに渡すこと**（要約しない）。**終わりに `master` へ push。**

- **渡す `rules/`**: `items-survival.md` / `inventory-screen.md` / `testing.md` /
  `blocks-shapes.md`（自動では読み込まれません）
- **ID は 1 個も取りません**。**見た目にも出ないので C-3 の撮影は要りません**
  （`AUTODEV.md` で撮らずに閉じてよい唯一の形。理由も仕様書に書いてあります）
- **古くなる文書が 4 つあります** —— `TUNING.md` の 241 行と 443 行・`ROADMAP.md` の
  156 行・**`rules/items-survival.md` の 178..183**（「残っている代用は弓の糸（羊毛 3）だけ」）。
  仕様書の「8. 終了条件」に全部並べてあります

## ブラウザで見てほしいところ

**この周は 1 行も動いていないので、新しく見てほしいものはありません。**
持ち越しは前の周のままです:

- **夜の敵対がゾンビとクモで半々になった手触り**（重み 100 = 本家のまま）と、
  **クモの速さ 6.0**（歩きでは逃げ切れず、走れば振り切れる狙い）
- **クモが朝に燃えてよいか**（**本家は燃えません**。`fireproof` が溶岩と日光の両方を
  止める 1 つの印なので、分けると `MobDef` が 1 列増えます）
- 遊んで決めてほしいこと: **糸・羽根・卵がどれも淡い色** / **投げた卵に使い道が無い** /
  **牛の胴が寸詰まりに見えないか** / **ステーキと革が一覧で見分けられるか** /
  **産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒と `#status` の位置**

**次の周（弓）で新しく人に見てほしくなるのは「クモを 3 匹倒すまで弓が作れない」手触り**です。

## この周で分かったこと

**決まりごとを 1 件、`rules/testing.md` に据えました**（`Edit` で普通に直しました）。

**`test/audio.test.ts` の「音（実際に鳴らして波形で見る）」は、揺れる 1 件ではなく
揺れる一群です。** `src/**` と `test/**` に 1 行も差分の無いこの周で、**2 回続けて
赤くなり、そのあと 5 回続けて緑**でした。**落ちた件は毎回違います**:

- **1 回目は「声色（音程の倍率）が波形に出る」** —— 低い声が 0.162 → **4.159** に跳ねて
  高い声 0.329 を追い抜いた（**鳴き声も揺れます。足音だけではありません**）
- **2 回目は足音のほうで、羊毛 9.796 と雪 9.416 が同時に跳ねた**（`rules/testing.md` には
  「羊毛だけが跳ねる」と書いてありましたが、**雪も跳ねます**）

**だから「2 回続けて赤い」だけでは退行と決められません** —— **同じ件が続けて落ちたときだけ**
退行として扱い、違う件なら数回走らせて**落ち着いた値**（羊毛 0.552 / 雪 0.716 / 砂 2.235・
低い声 0.162 / 高い声 0.329）に戻ることを確かめること。**判定はゆるめないこと。**

## 人がやること

- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 3 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
