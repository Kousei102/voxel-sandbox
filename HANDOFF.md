# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-09・AUTODEV 68 のあと）

- `npm test` **3407 件すべて成功**（**+35**）・`npm run typecheck` 緑・`npm run build` 緑
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **音の一群はこの周は 1 度も跳ねませんでした**（`npm test` を 4 回。揺れる件は `rules/testing.md`）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（`armor` は**省略可**で足しました）
- **`main.ts` 1449 行**（テストの数え方。`wc -l` では 1448。**−1 行**。予告どおり）
- **アイテム 123 種・`MAX_ITEM_ID` 161・111..255 の空きは 94**（**−4**）・**1..63 の空きは 9**（±0）
- **立方体 41 / 非立方体 79**・**モブ 9 種類**・**レシピ 65 本**（**+4**）・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 済`（118 行）**・**`AUTODEV-QUEUE.md` 136 行**（未着手 **4 件**）

## この周でやったこと（AUTODEV 68 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**革の防具 4 部位（27b-1）を実装しました** —— 作れて、着たまま残るところまで。

- **ID 158..161**（帽子 158 / 上着 159 / ズボン 160 / 靴 161。予約表から）
- **点数は本家のまま 1 / 3 / 2 / 1 = 合計 7**（表は `items.ts` の `ARMORS` 1 本）
- **レシピ 4 本**（革 **5 / 8 / 7 / 4** 枚・**4 本とも 3 幅なので作業台が要る**）
- **セーブは `SaveData.armor` の 8 要素・省略可**（**`inventory` の 72 要素には
  継ぎ足していません**。裸ならキーごと消えるので、着ていない人のセーブは 1 バイトも
  変わりません）。読む順は **36 枠 → 傷 → 防具**
- **`main.ts` は −1 行**（`inventory:` と `wear:` の 2 行を `inventory,` の 1 行に。
  **どのキーを載せるかは `session.ts` の `buildSave()` が決めます**）

**まだ着られません**（防具枠が画面に出るのは 27b-2。`vitals.armor` は 0 のまま）。
**傷は持たせていません**・**鉄／金／ダイヤは番号を取っていません。**

### 型で止まった罠 2 つ（どちらも `rules/items-survival.md` に前から書いてあった）

`MAX_ITEM_ID` を `FENCE` から移したので、**（1）余った `FENCE` の import** と
**（2）`test/blocks.test.ts` の `MAX_ITEM_ID === FENCE` の TS2367** が落ちました。
**どちらも判定はゆるめず**、import を消し、比べる相手を新しい番号に直しています。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので判定表の 2 行目です
（未着手 4 件なので A ではありません）。

- **キューの先頭は 27b-2**（防具枠が画面に出て、着ると固くなる）。
  `craftscreen.ts` の `"armor"` 枠・`inventoryui.ts` と `index.html` の枠 4 つ・
  `vitals.armor` へ点を貼る 1 行。**ID は 0 個**
- **`main.ts` は +1 行の見込み**で、**この周の −1 行が先に払ってあります**（1448 → 1449）
- **`ARMORS` も `armorPoints` も `armorReduced()` も揃っています** —— 27b-2 に
  残っているのは**画面と、貼る 1 行だけ**です
- **未着手 4 件。27b-2 を実装して消すと 3 件**（**3 件を切ったら次は A の周**）

## ブラウザで見てほしいところ

**この周は本物のブラウザで一覧を撮って見ました**（クリエイティブで `E`・末尾まで
スクロール）。**革の 5 つが並び、4 色は明るい順で互いにも隣とも見分けが付きます**
（帽子 `0xfca168` → 上着 `0xd88662` → ズボン `0xb16e51` → 靴 `0x644122`）。
**console のエラーは 0 件。** 撮り方は `docs/browser-shots/README.md` の末尾に足しました。

- **⚠ 写った不具合: 「革のズボン」の名前が 2 行に折れて、枠の絵に被ります。**
  **5 文字の名前で出ます**（4 文字の「革の帽子」「革の上着」「革の靴」は折れません）。
  `style.css` の `.slot .label` の**前から積んである件**で、**この周では直していません**
  —— `style.css` は仕様書の「触らないファイル」（27b-2 の持ち物）です
- **色 4 つが「革」に見えるか** —— 上の 2 つは橙寄りに出ます。木の茶色は一覧で
  いちばん混んでいる帯なので、素直な明るさの階段は真ん中が全部 20 を割ります
  （`TUNING.md`）。**変えるなら隔たりを測ってから**
- **革 24 枚（＝牛 24 頭）で一式**が見合う値段か。**革は牛しか落としません**

**持ち越し（フェンス 26a / 26b）**: **`shots/fence.png` に 5 通りが写っていて、
面の欠けも裏返りもありません**。**手ざわりとして「柵らしく見えるか」**だけ見てください。
**⚠ フェンスを 52fps 未満で跳び越せる件は残っています** —— 跳躍の余裕は **0.0117**
（到達は `9.2²/(2×30) + 9.2×dt/2` で、実測 **60fps 1.4883 / 30fps 1.5667 /
刻みの上限 0.05 で 1.6450**）。**直すなら跳躍の積分**（全ブロックの段差の登り方が
変わります）**か 1.5 の側**（本家の値から離れます）——**数値では決められないので人の判断です。**
**当たり判定はマスいっぱいのままで、腕の無い側にも「見えない壁」があります** /
**色 `0x988a5e` が「木の柵」に見えるか** / **棒 6 本で 2 個**が牧場 1 区画に見合う値段か。

**持ち越し（凍った海 25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の
境目が読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
いちばん近いガラスから 34.3 しか余裕がありません）。**気になるなら人が色を決めてください** /
**氷の板の上を歩けることが見て分かるか** / **氷ごしに海の底が見えるか**（`alpha` 0.6）。

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**。

**持ち越し（ケーキ）**: 上面 `0xffd0e4` が「ケーキ」に見えるか / 側面 `0xe8c9a0` と
下面 `0xd9b98a` の 3 色がケーキに読めるか。**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: 中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか /
素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / 森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ /
牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 3 件を `rules/` へ据えました**）

- **`rules/inventory-screen.md`**: 防具のセーブは**別キー（`SaveData.armor`）**で、
  読む順は 36 枠 → 傷 → 防具。**`deserializeArmor()` の中で `clear()` を呼ばないこと**
  （36 枠が消えます）。裸なら `undefined` でキーごと消えること
- **`rules/dimensions.md`**: **持ち物のキーが 2 つ以上ある器は、`SaveParts` へ
  器そのものを渡すこと** —— `main.ts` に `serialize()` を並べる形にしていると、
  キーが増えるたびに配線が 1 行ずつ伸びます（この周の −1 行がその実測です）
- **`rules/testing.md`**: `test/storage.test.ts` の「キーを 1 つ抜いても読める」の
  一覧に、**凍らせた v1 に元から無いキーを並べないこと** —— `without(key)` は
  1 文字も消さないので、**何も測らないまま緑**になります（`armor` で踏みかけました）

**`docs/browser-shots/README.md` にも 1 節足しました**（下の ⚠）。

## 人がやること

- **⚠ 一覧の枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）。
  **5 文字の名前で出ます** —— 「革のズボン」で実際に絵に被っているのを撮りました。
  **見た目の判断なので人が決めてください**（27b-2 が `style.css` を触る周です）
- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **設計の判断（41 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**
  （テストの数え方）で、上限 1500 まで 51 行。**27b-1 で 1 行減り、27b-2 で 1 行戻ります**
  ので、**次の周も止まりません** —— **待っているのは 24b（ケーキをかじる。最低 7 行）**です。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 51 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です（**前の周の引き継ぎが
「一覧に出るだけの色はあの 1 枚で見られる」と書いていましたが、間違いでした**）。
**一覧の色を見るなら `#mode` を押してクリエイティブにし、`E` を押して末尾まで
スクロールしてから撮ること**（手順は `docs/browser-shots/README.md` の末尾。
使い捨てのスクリプトでよく、`browsershot.mjs` は書き換えていません）。
**自然生成しないブロックは今までどおり `npm run shot -- <名前>` が唯一の経路です。**

**`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
**`wc -l` を 119 以下に収めること**（`rules/testing.md`。**いま 118 行**）。
