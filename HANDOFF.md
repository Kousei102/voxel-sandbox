# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05）

- `npm test` **3005 件すべて成功**（前の周と同じ。**`src/**` を 1 行も触っていません**）・
  `npm run typecheck` 緑。**音の一群も 1 回目から緑**でした（羊毛 0.552 < 雪 0.716 < 砂 2.235）
- **`npm run build` と `npm run bench` は走らせていません**（B の周なので `src/**` に差分ゼロ）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1463 行**（変わらず）
- **アイテム 102 種・`MAX_ITEM_ID` 137・111..255 の空きは 118・1..63 の空きは 9**
- **立方体 39 / 非立方体 69**・**クラフト 51 本 / 精錬 7 本 / 食べ物 8 種**・**モブ 9 種類**

## この周でやったこと（AUTODEV 28 の B・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が 7 件（3 件以上）・`AUTODEV-SPEC.md` が **`済`** だったので、
判定表を上から当てて **B（仕様）の周**。**`src/**` にも `test/**` にも 1 行も触っていません。**

**先頭の 15 番（ミルクバケツ）を 2 件に割り、前半だけを仕様書にしました**
（`AUTODEV.md` の B の「120 行に収まらない仕様は 2 件に割って前半だけ」）。

- **15a =「画面の開け閉め（`main.ts` 711..760 の 7 関数）を `src/panels.ts` へ出す」** ← 今回の仕様書
- **15b = ミルクバケツ**（キューに書き戻し。依存に 15a を書きました）

**割った理由は `main.ts` の 1463 行です。** 15b は注文を貼る関数が 12〜15 行要り、
16 番（サボテン）も配線が要るので、**先に空けないと 2 周で上限（1500）に近づきます。**
15a は本家の機能ではありませんが、**キューの ⚠ が前から「先に行数を空けること」と
書いていたもの**で、取りやすい 17 番へ飛ばしてはいません（`AUTODEV.md` の禁じ手）。

**取る前に 15 番だけコードで数え直しました**（B の決まり）: `milk` / `ミルク` / `MILK` は
`src/**` にも `test/**` にも `ROADMAP.md` にも **0 件**。まだ入っていません。

## ⚠ 次の C の周が知っておくこと: `main.ts` から出せる塊はもう小さい

**`test/ui.test.ts` の「main.ts は出した判断を呼び直している」は、`main.ts` に残っている
べき呼び出しを 40 件、文字列で見張っています**（`decideUse(` / `tryBucket(` / `fitEye(` /
`eyeShot(` / `wearForUse(` / `crops.plant(` …）。この周で初めて読んで分かったことで、
**だから大きい塊はどれも動かせません** —— 動かすと、判定をゆるめずには緑にできません。

**15a で空くのは 40 行ほど**（1463 → 1420 前後）で、**15b と 16 のぶんしかありません。**
その先は「見張りの一覧を新しいファイルへ付け替えてよいか」という設計の話になり、
**それは人が決めることです**（下の「人がやること」）。**無人の周が勝手に
`test/ui.test.ts` を書き換えないこと。**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手が 8 件（3 件以上）・`AUTODEV-SPEC.md` が `未着手` なので
→ 次は C（実装）の周**です。**仕様書のとおりに `src/panels.ts` を作ること。**

- **仕様書は全文をそのまま読む／渡すこと**（要約すると「触らないファイル」と
  「このタスク固有の禁じ手」が真っ先に落ちます。`AUTODEV.md` の C-1）
- 触るファイルに当たる `rules/*.md` を先に引くこと:
  `grep -l '"src/main.ts"' rules/*.md` と `grep -l '"src/inventoryui.ts"' rules/*.md`、
  それに **`test/**` を触るので `rules/testing.md`**
- **`ROADMAP.md` の予約表は触りません**（ID を 1 個も使わない周です。次の空きは 138 のまま）

## ブラウザで見てほしいところ

**この周は絵が 1 ピクセルも変わりません**（`src/**` に差分ゼロ）。撮っていません。

**次の周（15a）で人に見てほしくなるのはここです** —— ヘッドレスではポインタロックが
掛からず、**インベントリ・作業台・かまど・チェストを開いた絵は撮れません**
（`docs/browser-shots/README.md`）。配線を貼り替える周なので、**公開サイトで
4 つの画面が今までどおり開いて閉じるか**だけは人の目が要ります。

- 持ち越し: **雪玉を投げても何も起きない**のが物足りなくないか / **鉄・金・ダイヤの
  ブロックが材料の山と見分けられるか**（色を材料から写したので名前以外の手掛かりが
  ありません）/ **一覧の枠の名前が 2 行に折れてはみ出す**（「ダイヤブロック」
  「溶岩入りバケツ」。`style.css` の `.slot .label` の話）/ **クモを 3 匹倒すまで
  弓が作れない**道のり / **クモの速さ 6.0** と**朝に燃えてよいか**（本家は燃えません）/
  **投げた卵に使い道が無い** / **牛の胴が寸詰まりに見えないか** / **産卵の間隔
  300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒**

## この周で分かったこと

**決まりごとは 0 件**（`rules/` に据えるものはありません。`src/**` を触っていないので
落とし穴を踏みようがない周でした）。**分かったことは上の ⚠ の 1 つ**で、それは
`AUTODEV-QUEUE.md` の ⚠ とこのファイルに書きました（`rules/` は「触るファイルに
当たったときに引くもの」で、**周の進め方の話は置き場が違います**）。

## 人がやること

- **設計の判断を 1 つお願いします: `main.ts` を 2 つに割ってよいか。**
  15a のあとも `main.ts` は 1420 行前後で、**`test/ui.test.ts` の 40 件の見張りが
  呼び出しを `main.ts` に釘付けにしている**ため、これ以上は大きく減りません。
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に
  変える必要があり、**判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 10 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
