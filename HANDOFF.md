# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- **この周は C（実装）**。`AUTODEV-SPEC.md` の 14 番「1 回の採掘で 2 山落ちる器」を
  仕様書のとおりに入れました（**コミット 1 つ**）
- `npm run typecheck` / **`npm test` 2798 件すべて成功**（2783 → **+15 件**）/ `npm run build` も通る
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **`src/main.ts` は 1450 行 / 上限 1500**（この周は **1 行も触っていません**。`npm test` の
  数え方で、`wc -l` の 1449 とは食い違います。**停止条件の 1450 に乗っているのは `npm test` の数**）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 131**（**この周は 0 個使用**）
- **アイテムは 89 種**、`MAX_ITEM_ID` は **124**。**`SaveData` は 1 バイトも増えていません**（`version` 1）
- **`AUTODEV-SPEC.md` は `状態: 済`**、`AUTODEV-QUEUE.md` の未着手は **6 件**
  → **次の周は B（仕様）**
- **`RULES-INBOX.md` は未取り込み 14 件**（この周に 1 件足しました）

## この周でやったこと（無人・C の周 1 種類だけ）

**実った小麦を掘ると、小麦 1 個と種 1 個の 2 山が落ちるようになりました。畑が自転します。**

1. `items.ts`: **`DropStack`** 型と **`Drop.extra?: DropStack`** を新設し、
   **`rollDrops(blockId, roll): readonly DropStack[]`**（0〜2 山）を足しました。
   **`rollDrop()` は残してあります**（既存の約 25 か所の根拠。1 山目は `rollDrops()` の中から
   `rollDrop()` を呼んで作るので、`chance` / `otherwise` の判断は 1 か所のままです）
2. `DROPS` で `extra` を持つのは**実った小麦の 1 行だけ**（`extra: { item: WHEAT_SEEDS, count: 1 }`）
3. `breaking.ts`: `harvest()` の戻りを **`Burst[]`** に。**掘る経路（`tryBreak`）と支えを失う経路
   （`autoBreak`）が同じ `harvest()` を通る形は変えていません**
4. テストは `test/mining.test.ts`（山の数の一覧）/ `test/blocks.test.ts`（2 山の中身と、
   **全ブロックを回す不変条件 2 本**）/ `test/breaking.test.ts`（**2 経路 x クリエイティブ**）

**種は 1 個固定**にしました。流れてくる乱数が `roll` の 1 本しかないので、`extra` に確率や
個数の範囲を付けると**1 山目の当たり外れと必ず相関します**（砂利の火打石と種の個数が
連動する形）。**本家の「小麦 1 + 種 0〜3」より渋い**ぶんは `TUNING.md` に 1 行残しました。

## ブラウザで見てほしいところ（見た目に出るもの）

**撮って自分の目で見ました**（C-3）。`npm run shot -- crops` を撮り直して `Read` で確認し、
**描画は 1 行も触っていないので苗と実りの見た目は前の周のまま**であることを見ています。
本物のブラウザ（`node tools/browsershot.mjs`）でも 4 枚撮り、**WebGL 2.0 / 例外 0 件**・
地形と HUD が正常に出ることを確かめました。**それでも人にしか確かめられないことが 3 つ:**

1. **掘った瞬間に山が 1 → 2 に増えます**（小麦と種）。`drops.ts` の `burst()` が乱数で
   散らすので重なりはしないはずですが、**2 山が同時に飛ぶ見た目**は撮れません
2. **苗の緑（`0x6f8f3f`）が草原に紛れます**（前の周からの持ち越し。実った黄色 `0xd8c26a` は
   一目で分かります）。苗を暗く／青寄りにするか、`boxes` を低くするか（`TUNING.md`）
3. **`GROW_SECONDS` 180 秒（3 分）が待てる長さかどうか**（本家は 20 分前後ですが、
   ここは**チャンクが読み込まれている間しか育ちません**）

## 人がやること

- **`RULES-INBOX.md` の 14 件の取り込み**。**わざと食い違わせてある組**（「未取り込み N 件」や、
  **この周が足した `items-survival.md` の節と、AUTODEV 13 の B の周が書いた「1 山しか
  落ちない」の節**）は **いちばん新しいほうを採ってください**
- **種が 1 個固定でよいか**（本家は 0〜3）。範囲にするなら**乱数をもう 1 本流す**話になり、
  `BreakOrder` と `autoBreak()` の引数と `main.ts` に及びます
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**はまだ直っていません
  （`#status` は `bottom: 118px` 固定。`style.css:674`）。`REVIEW.md` に積んであります
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **6 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 済`**
なので → **次は B（仕様）の周**です。**先頭は「15. パン（小麦 3 → パン）と食べること」**で、
`AUTODEV.md` の B が要求する 7 項目を揃えた 120 行以内の 1 枚に書き直します
（**`src/**` と `test/**` には 1 行も差分を出さないこと**）。

**取る前にコードで数え直すこと**（B の決まり）。15 番は `crafting.ts` のレシピ 1 本と
`items.ts` の `FOODS` 1 行の見込みで、**新しく確かめられないものは 0** です。
**ID は `ROADMAP.md` の予約表の 125 から 1 個**（小麦 124 まで使用済み）。

- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **サブエージェントは使っていません。** `AUTODEV.md` の C-1 は「同じ番の中で返らないなら
  自分で実装すること」で、前の 5 周とも `run_in_background: false` を渡しても背景で起動しました。
  **点検（C-2）も親が `git diff` を全部読むこと**（飛ばすより確実です）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）
- **`main.ts` は 1450 行**で、`AUTODEV.md` の停止条件 3 の線にちょうど乗っています。
  **1 行でも足したくなったら、判断を別ファイルへ出すこと**

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（前の周に「声色（音程の倍率）が
波形に出る」が 1 件だけ赤 → 走らせ直したら緑。**この周は 3 回とも緑**）。
**赤が出たら、まずもう一度走らせること**（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
