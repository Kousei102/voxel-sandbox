# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-25）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。無人で回すうえで
  実際に踏んだものが 3 つあります:
  **(1)** **`.claude/rules/**` を編集すると「sensitive file」の確認で止まります。**
  → **`RULES-INBOX.md` に書いて、人が居る周が取り込む**形にしました（`LOOP.md` の「4.5」）。
  **`.claude/settings.json` に許可を書いても消えません**（2026-08-25 に 2 周とも実測）。
  **(2)** クラウドから `origin` へ push できず 403 → ユーザーが GitHub 側で権限を付け直し済み。
  **(3)** **5 時間の利用制限。** 当たった周は実装を終えた直後に止まり、次の定期実行も
  同じセッションに投げられて即終了しました（**新しい周にならず、1 回ぶんの枠が消えます**）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1783 件すべて成功**（1775 → 1783。この周の 8 件はエンダーアイのレシピ）
- **進行（クリア導線）達成 9 / 13**（9 件とも本物）。**この周で 1 つ増えました**
  —— 「エンダーアイが作れる」が本物になりました
- ブロック ID **1..63 の空きは 14 個**（この周はブロックを足していません）。
  アイテム ID は **92 まで**（91 = ブレイズパウダー / 92 = エンダーアイ）
- `src/main.ts` は **1400 行 / 上限 1500**（余白 100 行。**この周は 1 行も触っていません**）

## 直前にやったこと — TASKS 2-6（エンダーアイ）

**この周は行を足しただけです。** 新しいファイル・新しい判断のファイル・新しい
「確かめられないもの」はひとつもありません（`main.ts` も 1 行も触っていません）。
その代わり、**クリア導線の 2 本がここで初めて合流します。**

| どこ | 何が入ったか |
| --- | --- |
| `items.ts` | `BLAZE_POWDER`(91) / `ENDER_EYE`(92)。**アイは 64 個まで・パールは 16 個まで** |
| `crafting.ts` | 形なし 2 つ（ロッド 1 → パウダー 2 / パウダー + パール → アイ 1） |
| `test/crafting.test.ts` | 「エンダーアイ」の節 8 件（作れる 3 / 抜け道 3 / 枠 12 個 1 / 消費 1） |
| `test/progression.test.ts` | 9 番目の probe を本物に。`ACHIEVED_BASELINE` 8 → 9 |

- **形なしなので手持ちの 2x2 で作れます。** ネザーや夜の野外で作業台を探しに戻らずに
  済むのは Minecraft と同じで、**ここを形ありにすると導線がひとつ増えます**
- **ロッド 1 本 → パウダー 2 個が効いています。** エンドポータルの枠 12 個に要るのは
  **ロッド 6 本**で、1 個ずつなら 12 本（＝要塞に通う回数が倍）になります
- **アイのスタック上限 64 とパールの 16 を食い違わせてあるのは意図的です。**
  理由は一点で、**枠 12 個ぶんがどちらも 1 枠に収まること**。
  `test/crafting.test.ts` が 12 という数を直接持っていて見張ります
- **進行の probe は「レシピ名があるか」を見ません。** 見ると `crafting.ts` に 1 行
  足した瞬間に達成へ化けます（同じ形で 5 度踏んでいます）。いまは
  **(1)** 材料が 2 種類ともモブの落とし物か（＝要塞と夜の両方を通るか）
  **(2)** 手持ちの 2x2 で 2 段とも実際に組み上がるか
  **(3)** ロッドから直接作る抜け道が無いか、の 3 つを実際に回しています
- **わざと壊して 3 通りとも赤くなることを確かめました**: アイの材料をロッドに直結 → 4 件
  （進行の後戻りを含む）/ アイのスタックを 8 に → 1 件 / ブレイズの落とし物を腐った肉に →
  進行が 2 件（ロッドとアイ）未達に落ちて後戻りが NG
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません。
  決まりごとは **`RULES-INBOX.md` に 1 件**（`rules/items-survival.md`）、数値は
  **`TUNING.md` に 6 行**（「クリア導線の集まり具合（2-6）」の節）、
  確認項目は `REVIEW.md` の「2-6」に 6 個

### 前の周（2-1 / 2-2 / 2-3 / 2-3b / 2-3c / 2-4a / 2-4b / 2-4c / 2-5）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージは書かず、撃った側が
  `Shot.damage` に載せる**ので、2-12 の弓矢は撃つ側を足すだけで当たる側が要らない）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 の
  10 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（いまや 5 度目です）。
   この周の「エンダーアイが作れる」も、元は `recipe("エンダーアイ")` = レシピ表に
   その名前があるか、でした。**`crafting.ts` に 1 行足すだけで達成に化けます。**
   `structures.ts` / `dimensions.ts` / `projectiles.ts` / ブレイズと、同じ形で並んでいます
2. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。出来上がりの枠の下に
   `${name} x${count}` を浮かせて出すので、全角 12.5 文字を超えると盤面のスロットに
   重なります。**「ブレイズパウダー x2」= 9.5 でいまの最長**です
3. **探す半径は「正方形」であって「円」ではありません**（2-5）。`teleportSpot()` は
   軸ごとに `±radius` を取るので、対角では `radius × √2` まで届きます
4. **試験場のプレイヤーは、跳ぶモブでは `HOSTILE_SIGHT`(18) より遠くに置くこと**（2-5）
5. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）
6. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の火球で焼け死にます
7. **相手を先に、ブロックをあとに見ること**（2-4b）。逆だと壁を背にした相手に当たりません
8. **飛ぶモブには `ACCEL_GROUND` を使うこと**（空中の加速 4 では 1 フレームに 0.07 しか戻らない）
9. **`world.seed` をセーブに使わないこと**（ネザーでは塩を混ぜたあとの値）。種は `worldSeed`
10. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）
11. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）
12. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）
13. **わざと壊して、テストが赤くなるか確かめること。** この周は 3 通りとも確認済みです

## 次に取るもの

1. **2-7（投げたエンダーアイが要塞の方へ飛ぶ）**。**飛ばす仕掛けはもう全部あります** ——
   `projectiles.ts` に `eye` の行（速さ 8m/s・重力 0・壁を抜ける `pass`・寿命 12 秒）が
   あり、`N` キーのデバッグで実際に飛びます。足りないのは**方角を決める判断**だけで、
   進行テストの probe が **`strongholdDirection` という名前を探しています**
   （`anySourceHas("strongholdDirection")`）。要塞そのものは 2-8 なので、
   **この周で決めるのは「どこに建つか」の規則だけ**になるはずです（建てるのは 2-8）
2. そのあとは並びどおり 2-8 → 2-9 → … → 2-13（ドラゴン）。
   **2-12 の弓矢は、撃つ側（弓とアイテム）を足すだけで当たる側が要りません**
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 6 件**）/ `REVIEW.md` の一括確認
   （**いま 10 節**）/ `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒燃えます）。
  `smelting.ts` の燃料表に 1 行足すだけですが、2-6 の範囲外なので入れていません
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）。`projectiles.ts` に
  `eye` と同じ形で足せますが、着地でプレイヤーを動かす判断がどこに要るかは未検討です
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方（湧きの枠・逃げ方・日光・ベッドで寝られるか）を全部見直すことになるので、
  **手触りの調整のついでではなく、独立したタスクとして決めてください**
- **エンダーマンは日陰へ逃げません**し、**ブロックを持ち運びません**
- **ネザーでも寝られます**（本家はベッドが爆発する）。`canSleep()` は時刻だけで決まります
- **寝ても時間は「進みません」**（時計だけ飛びます）。精錬・空腹・モブは dt で動きます
- **火球は火を点けません**（本家は当たった所が燃える）。火そのものが無いので、
  入れるならブロックとして足すところからです
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）。手触りの数値なので `TUNING.md` に
- **`F3` の `biome` はネザーでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）。気になるなら `spawnOn` を 1 行足せば絞れます
