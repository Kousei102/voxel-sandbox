# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-18・AUTODEV 96（B の周）のあと）

**この周は B（仕様）の周**でした（判定表の 2 行目 —— `AUTODEV-SPEC.md` が
`状態: 済` で、`AUTODEV-QUEUE.md` の未着手が 4 件あったため）。
**キューの先頭 34c「ツタの自然生成」の仕様書を書いて `master` へ push しました。**

- **`src/**` も `test/**` も 1 行も触っていません**（B の完了条件）。差分は
  `AUTODEV-SPEC.md` と `HANDOFF.md` の 2 つだけです
- **取る前にコードで数え直しました** —— `grep -rn "VINE" src/` の結果、
  **`worldgen.ts` にも `biomes.ts` にも `VINE` は 1 行もありません**。
  34c は本当に未実装です（キューの行はそのまま残してあります）
- **仕様書は 105 行**（`wc -l`。テストの数え方では 106 / 上限 120）。7 項目そろっています

### ⚠ この周でいちばん大きい発見 —— キューの 34c の前提が 1 か所違います

キューは「**葉の下に 1 マス置いて、そこから下へ伸ばす**」と書いていましたが、
**葉の真下にツタは置けません。** `supportsBlock()`（`blocks.ts`）が真上を支えと
認めるのは `baseBlock(supporter) === baseBlock(id)`（＝**真上も同じツタ**）のときだけで、
葉は通りません。**1 本目は必ず「横が葉」の壁掛け**、**2 本目から下が
「真上のツタ」にぶら下がるぶん**です。読み違えると、**生成では置けているのに、
隣を 1 つ壊した瞬間に列ごと消えるツタ**になります（`World.breakUnsupported()` が
`canPlaceAt()` に聞き直すため）。仕様書の末尾に ⚠ として書いてあります。

### 仕様書で決めたこと（実装の周はこのとおりに）

- **ID 0 個。** 既存の 183..186 を置くだけ。**新しい番号を取らないこと**
- **触るのは 6 ファイル**: `src/treeshape.ts`（`vineCells()` を 1 本足す。
  **既存の `treeCells()` は 1 文字も触らない**）/ `src/biomes.ts`（`BiomeDef.vine` を
  11 行すべてに。森だけ 0 より大きい）/ `src/worldgen.ts`（`Tree` に旗 1 つと
  `stampTree()` の末尾）/ `tools/shot.ts`（場面 `vine`）/ `test/worldgen.test.ts` /
  `test/treeshape.test.ts`。**`main.ts` は 0 行の見込み**
- **`TREE_RADIUS`(2) を超えた所に書かないこと**（超えたぶんは隣の列の生成時に黙って欠ける）。
  **木の根元より下へ垂らさないこと**（`put()` の `overwrite: false` に弾かれて宙に浮く）
- **`crops.ts` は触らない** —— この周では**苗木から育った木にツタは掛かりません**（意図した違い）

## 数字（2026-09-18・この周で測ったもの）

- `npm test` **3729 件すべて成功**（前の周と同じ。**1 件も増えても減ってもいません** ——
  `src/**` も `test/**` も触らない周なので当然です）。`npm run typecheck` 緑
- **`npm run build` は走らせていません**（`src/**` の差分が 0 行。C の周の完了条件です）。
  **`npm run bench` も走らせていません**（生成もメッシュ化も ±0 行）
- **音の跳ねは 0 回**（2 回走らせて 2 回とも「声色」も「足音の cutoff」も緑でした。
  前の周は「声色」が 1 回跳ねています）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1449 行
  （`wc -l`）/ テストの数え方で 1450 / 上限 1500**
- **アイテム 145 種・`MAX_ITEM_ID` 186**（±0）・**立方体 42 / 非立方体 89**（±0）
- **1..63 の空きは 7**・**111..255 の空きは 69**（どちらも ±0。**次に取るのは 187**）
- `AUTODEV-QUEUE.md` **116 行 / 上限 150・未着手 4 件**（±0）・
  `AUTODEV-SPEC.md` **105 行 / 上限 120・`状態: 未着手`**

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。
**`AUTODEV-SPEC.md` を全文そのままサブエージェントに渡すこと**（要約しないこと）。

- **渡す `rules/*.md` は 4 本**: `rules/worldgen.md`（全部）/ `rules/blocks-shapes.md` /
  `rules/testing.md` / `rules/meshing-render.md`。**自動では読み込まれません**
- **見た目に出る周です** —— `npm run shot -- vine terrain` と
  `node tools/browsershot.mjs` を撮って、**`Read` で開いて自分の目で見ること**
- **`npm run bench` を 3 回**（`stampTree` に仕事が増えます）

## ブラウザで見てほしいところ

**この周は `src/**` を 1 行も触っていないので、新しく撮った絵はありません。**
前の周（34b）で `shots/vine.png` と `shots/terrain.png` を見て、
**ひさしの下に緑の帯が 3 マスぶら下がること**と**面の欠け・裏返り 0 件**を確かめてあります。
人に見てほしいのは（上ほど新しい）:

- **⚠ 垂れたツタの手ざわり（34b）**: **宙にぶら下がった 1/16 の板が「ツタ」に見えるか**
  （絵では浮いて見えます）/ **ぶら下がりに上限が無い件** —— 手で置くぶんは何マスでも
  垂らせます（**自然生成の長さを決めるのは次の周の 34c**）
- **⚠ 薄い板の横・真上に石があると置ける件（34b で開いた道。人の判断）**:
  ツタやはしごの横を狙っても、**真上に固いブロックがあれば置けます**（34a までは `blocked`）。
  支えは天井なので落ちませんが、板は狙った面の側に貼り付いて見えます
- **⚠ 石の天井の下のツタ**: **手では置けないのに、あれば落ちません**
  （直すなら `supportsBlock()` の 1 行。`rules/blocks-shapes.md` に両方の道）
- **⚠ ツタ（34a）の手ざわり 3 つ**: 登る速さがはしごと同じでよいか / 硬さ 0.2 が
  「すぐちぎれる」と感じられるか / 壁に貼った緑が「ツタ」に見えるか（`0x306d18`）
- **⚠ 金・ダイヤの防具の色（33b）**: **金の上着 `0xefd714` とズボン `0xf8b317` は絵の上でやや近い**
- **⚠ ダイヤ一式 80% 減が強すぎないか**（**防具は壊れません**）/ **⚠ 金を作る理由があるか**
  （金一式 11 点 < 鉄 15 点なのに 24 枚要ります）
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ 鉄の防具の色（33a）**: 「鋼の青」が鉄に見えるか / 鉄一式 60% 減の手ざわり
- **レンガ（32b）**: `0xbc5d39` が「焼いたレンガ」に見えるか / **粘土（32a）**: 海底の 7.2%
- **ハーフ 2 材質（31）** / **苗木（30b）**: 180 秒が待てる長さか / **木炭（29）** / **修理（28）**
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / **防具（27b）**: 革一式 28% 減
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白** / **氷（25a）**: 滑りの手ざわり
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 0 件**）

**`rules/` は ±0 行・`RULES-INBOX.md` も ±0 行**です。B の周は `src/**` を触らないので、
新しく踏んだ落とし穴がありませんでした。**上の ⚠（葉の真下にツタは置けない）は
すでに `rules/blocks-shapes.md` の「支えの向きが 2 つ以上あるもの」の節と
`blocks.ts` の `hangsBelow` のコメントに書かれている**ことを読んで確かめたので、
**同じことを 2 つの層に書かない決まり**（`CLAUDE.md`）に従って足していません。

## 人がやること

- **⚠ 設計の判断（67 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（テストの数え方。上限 1500）で、**`AUTODEV.md` の停止条件 2 に
  並んでいます**。**待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 /
  **24b ケーキをかじる**）。**34c（次の周）は `main.ts` 0 行の見込み**なので、
  判断が下りなくても 1 周は進みます。**その次が 12 → 35 → 24b の順で、35 と 24b で当たります**
- **⚠ 石の天井の下のツタの非対称**（上の「ブラウザで見てほしいところ」）。
  **本家は下面にも置けます**が、置く向きの表を変えると `vineVariant()` が向きを
  決められません（天井には向きがありません）—— **足すなら番号 1 個**の話です
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
  （足すなら `vitals.ts` → `inventory.ts` の配線で、**それだけで 1 周**）
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は 2 回走らせて
  2 回とも緑**でした（前の周は「声色」が 1 回跳ねています）。**直すなら測り方**
  （窓を長く取る・複数回鳴らして中央値）で、**判定はゆるめないこと**。
  **キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 96 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと**。
**⚠ 枠を数えるときは `#creative .slot` を使うこと**（素の `.slot` はホットバーと
インベントリの 36 枠まで拾います）。**⚠ `/opt/pw-browsers` には
`chromium_headless_shell-1194` も居ます** —— `startsWith("chromium-")` で弾くこと
（`tools/browsershot.mjs` の `findChrome()`）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質・**ツタ**がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**34c で自然に生えるようになったら、
場面 `vine` に「森の木から垂れているところ」を足すこと**（いまの場面は**石の柱と、
そこから出したひさしに手で掛けただけ**です）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**⚠ 防具を着た姿は撮れません**（一人称視点なので自分の体が写りません）。
