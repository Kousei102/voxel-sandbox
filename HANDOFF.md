# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-17・AUTODEV 94（B の周）のあと）

**この周は B（仕様）の周**でした（判定表の 2 行目 —— `AUTODEV-SPEC.md` が
`状態: 済` で、未着手が 4 件あったため）。**`src/**` も `test/**` も `tools/**` も
1 行も触っていません**（`git diff --stat -- src test tools` が空）。

- **キューの先頭 34b を、その場で 34b / 34c の 2 件に割りました**（`AUTODEV.md` の B の
  「120 行に収まらない仕様は 1 周で閉じない信号」）。**前半だけを仕様書にし、後半は
  `AUTODEV-QUEUE.md` に書き戻しました**
  - **34b「ツタが下へ垂れる」**（この仕様書）: `blocks.ts` の表 2 本・`world.ts` の 2 か所・
    `test/arena.ts` の写し・テスト 2 ファイル・`tools/shot.ts`
  - **34c「ツタの自然生成」**: `worldgen.ts` と `biomes.ts` だけ。**34b とは触るファイルが
    1 つも重なりません**（だからここで切れました）
- **取る前に 34b をコードで数え直しました**（B の決まり）。**34a で入っているのは
  壁掛け 4 向きまで**で、`vineVariant()` は床（`FACE_YN`）も天井（`FACE_YP`）も `AIR` を返し、
  `worldgen.ts` にも `biomes.ts` にも「ツタ」は 1 行もありません（**まだ入っていない**）
- **設計の当たりまで仕様書に書いてあります**（サブエージェントに勝手な判断をさせないため）:
  **`supportFace` は 1 ブロック 1 向きのまま**にして、**2 つ目の候補は別の表
  （`HANGS_BELOW`）から作る `supportFaces(id)`**（面番号の配列。ふつうは 1 つ、
  ツタだけ `[壁, FACE_YP]`）。`supportsBlock()` に足す例外は **1 行**で、
  **`face === FACE_YN` を必ず見ること**（見ないと横のツタにも貼り付きます）。
  `breakUnsupported()` は**落とすかどうかを `canPlaceAt()` に聞く**形にすると、
  置く側と壊す側が**同じ関数**になって連鎖も勝手に落ちます
- **`vineVariant(face, supporter = AIR)` の 2 つ目の引数**で「上と同じ向きを写す」ところまで
  書いてあります。**`PlaceContext.supporter` は省略可にすること** ——
  必須にすると `placedVariant()` を呼ぶ既存のテスト **49 か所**が全部落ちます

## 数字（2026-09-17・この周で測ったもの）

- `npm test` **3710 件すべて成功**（前の周と同じ。**この周は判定を 1 件も足していません**）。
  `npm run typecheck` 緑。**`npm run build` も `npm run bench` も走らせていません**
  （`src/**` を 1 行も触らない周なので要りません）
- **音の跳ねが 1 回出ました** —— **羊毛ではなく「声色（音程の倍率）が波形に出る」**が
  1 回目だけ赤（0.329 > 4.260）で、**2 回目は緑**。`rules/testing.md` のとおり
  もう一度走らせただけで、**判定は 1 文字もゆるめていません**
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1450 行 / 上限 1500**
- **アイテム 145 種・`MAX_ITEM_ID` 186**・**立方体 42 / 非立方体 89**
- **1..63 の空きは 7**・**111..255 の空きは 69**（**次に取るのは 187**。**34b は 0 個の見込み**）
- `AUTODEV-QUEUE.md` **117 行 / 上限 150・未着手 5 件**（34c を足したので 4 → 5）・
  `AUTODEV-SPEC.md` **119 行 / 上限 120・`状態: 未着手`**

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。
**`AUTODEV-SPEC.md`（34b・ツタが下へ垂れる）を全文そのまま渡して実装すること。**

- **仕様書を要約しないこと**（`AUTODEV.md` の禁じ手）。**触るファイルに当たる
  `rules/*.md` も渡すこと** —— 仕様書の末尾に引く `grep` と当たる 6 本が書いてあります
- **サブエージェントは同じ番の中で結果が返る呼び方でだけ使うこと。**
  返らないなら**自分で実装して、自分で `git diff` を点検する**（前の周はそうしました）
- **`main.ts` は ±0 行の見込み**です（`world.ts` と `blocks.ts` だけ）。**1450 行のまま**なら
  人の判断を待たずに進めます —— **1 行でも配線が要ると分かったらそこで止まること**
- **見た目が本体の周です**（垂れ下がり）。**`npm run shot -- vine` で撮って `Read` で
  開いて見ること。** **`World.setVoxel()` は `canPlaceAt()` を通るので、場面のツタは
  上から順に置くこと** —— 下から置くと 1 マスも書けません

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません**（`src/**` を触らない B の周なので、見た目は 1 画素も
動いていません）。人に見てほしいのは前の周から持ち越しのここです（上が新しい分）:

- **⚠ ツタ（34a）の手ざわり 3 つ**: **登る速さがはしごと同じでよいか**
  （`LADDER_CLIMB_SPEED` を共有）/ **硬さ 0.2 が「すぐちぎれる」と感じられるか**
  （剣で 0.30 秒）/ **壁に貼った緑が「ツタ」に見えるか**（`0x306d18`・本家の値）。
  **本家のように下へ垂れないのは承知**（34b で直します）
- **⚠ 金・ダイヤの防具の色（33b）**: **金 4 段が「金」に・ダイヤ 4 段が「ダイヤ」に
  見えるか**。**金の上着 `0xefd714` とズボン `0xf8b317` は絵の上でやや近く見えます**
- **⚠ ダイヤ一式 80% 減が強すぎないか。** **防具は壊れない**ので、
  **「上限の固さが永久に持つ」状態**です（本家は傷んで直す手間があります）
- **⚠ 金を作る理由があるか。** **金一式 11 点は鉄 15 点より弱いのに 24 枚要ります**
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ 鉄の防具の色（33a）**: **「鋼の青」が鉄に見えるか** / 鉄一式 60% 減の手ざわり
- **レンガ（32b）**: `0xbc5d39` が「焼いたレンガ」に見えるか（ブロックとは 26.0）
- **粘土（32a）**: 海に潜って見つけられるか（海底の列の 7.2%）/ 粘土玉がやや青紫寄り
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか / **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らないのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白** / **氷（25a）**: 滑りの手ざわり
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 0 件**）

**`rules/` は 1 本も直していません** —— この周に踏んだ落とし穴が 1 つも無かったためです
（`src/**` を触らない B の周で、読んで数えて書いただけ）。**代わりに、次の C の周が
踏みそうな 3 つを仕様書の中に書き置きました**（`rules/` へ据えるのは、実際に踏んだ周の仕事）:

- **`Slab.setVoxel()`（`test/arena.ts`）は `breakUnsupported()` を呼びません。**
  だから「支えを壊したら落ちる」は `Slab` では 1 件も確かめられず、**本物の `World`**
  が要ります（`test/placing.test.ts` の大半は `Slab` です）
- **`AUTODEV-SPEC.md` の 120 行は `wc -l` より 1 多く数えられます** ——
  **119 行に収めること**（`rules/testing.md` に記録があり、この周も 121 → 120 → 119 と
  2 回削りました。**3 回目は「上限を上げる」ではなく削るほうへ**）
- **音の揺れは羊毛だけではありません** —— この周は **`sfx` の「声色（音程の倍率）」**が
  1 回目だけ赤く（`rules/testing.md` は羊毛の例しか挙げていません）、2 回目で緑でした

## 人がやること

- **⚠ 設計の判断（65 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**）。
  **34b（次の C の周）は `main.ts` 0 行の見込み**なので、判断が下りなくても進みます
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
  （足すなら `vitals.ts` → `inventory.ts` の配線で、**それだけで 1 周**）
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は「声色」が 1 回跳ねました**。
  **直すなら測り方**（窓を長く取る・複数回鳴らして中央値）で、**判定はゆるめないこと**。
  **キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 94 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと**。
**⚠ 枠を数えるときは `#creative .slot` を使うこと**（素の `.slot` はホットバーと
インベントリの 36 枠まで拾います）。**⚠ `/opt/pw-browsers` には
`chromium_headless_shell-1194` も居ます** —— `startsWith("chromium-")` で弾くこと
（`tools/browsershot.mjs` の `findChrome()`）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質・**ツタ**がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**34c で自然に生えるようになったら、
場面 `vine` に「森の木から垂れているところ」を足すこと**（いまの場面は**石の柱に
手で掛けただけ**です）。**時間が経たないと見えないもの**（育つ・燃える・溶ける）は、
**その場で `update()` を回してから撮ること**。
**⚠ 防具を着た姿は撮れません**（一人称視点なので自分の体が写りません）。
