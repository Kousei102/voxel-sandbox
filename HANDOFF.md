# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-18・AUTODEV 97（C の周）のあと）

**この周は C（実装）の周**でした（判定表の 3 行目 —— `AUTODEV-SPEC.md` が
`状態: 未着手` だったため）。**キューの 34c「ツタの自然生成」を実装して
`master` へ push しました。ID は 1 つも使っていません。**

- **森の木の 25% にツタ（183..186）が掛かって下へ垂れます。** 実測で
  **64x64 の森に 83 マス / 木 60 本（木 1 本あたり 1.38 マス）**、
  **支えの無いツタは 0 マス**（4 つの種で数えました）
- **触ったのは 6 ファイル**: `src/treeshape.ts`（`vineCells()` と `VINE_MAX_LENGTH`。
  **`treeCells()` は 1 文字も動かしていません**）/ `src/biomes.ts`（`BiomeDef.vine` を
  11 行すべてに。**森だけ 0.25**）/ `src/worldgen.ts`（`Tree.vines` の旗 1 つと
  `stampTree()` の末尾 2 行）/ `tools/shot.ts`（場面 `vine` を差し替え）/
  `test/worldgen.test.ts` / **`test/treeshape.test.ts`（新しく作って `test/run.ts` に 2 行）**
- **`main.ts` は 0 行**（1450 行のまま）。`blocks.ts` / `items.ts` / `crafting.ts` /
  `world.ts` / `crops.ts` / `placing.ts` / `ROADMAP.md` の予約表は ±0 行

### ⚠ この周でいちばん大きい発見 —— 切れ端の心配は要りませんでした

仕様書は「生成では置けているのに、隣を 1 つ壊した瞬間に列ごと消えるツタ」を
警戒していて、**1 本目が壁掛けでなければならないのはそのとおり**でした。
ただし**隣の木や地形とぶつかったときの心配は要りません** —— `put()` は
`overwrite: false` なので、ぶつかった所は相手（葉・幹・石）が残りますが、
**`supportsBlock()` は `canSupport()` の通る固い天井を支えと認める**ので
（34b が開けた道。`rules/blocks-shapes.md`）、**上から順に書けば下のぶんは
必ず何かにぶら下がります。** だから「隣の木を避ける」ような判定は 1 行も要らず、
**4 つの種で数えて支えの無いツタは 0 マス**でした。

**もう 1 つは形の探し方**: 葉の段を**上から探すと、オークの落とした 4 隅が埋まるだけ**で
葉より下へ 1 マスしか垂れません。**下から探すと同じ長さで葉の下へ 3 マス**垂れます
（`rules/worldgen.md` へ据えました）。

## 数字（2026-09-18・この周で測ったもの）

- `npm test` **3729 → 3802 件（+73）すべて成功**。`npm run typecheck` 緑 /
  `npm run build` 緑（`src/**` を触った周なので走らせました）
- **音の跳ねは 11 回走らせて 2 回**（どちらも 1 件・音のグループ・**続けて同じ件が
  落ちたことはありません**）。`rules/testing.md` のとおり走らせ直しただけで、
  **判定は 1 文字もゆるめていません**
- **`npm run bench` 3 回**: `generateChunk` **0.98 / 1.07 / 0.99ms（中央 0.99）**・
  `world.update` 平均 3.16〜3.24ms / 最悪 10.9〜12.5ms。**この箱の振れ
  （0.75〜1.40ms）の中で、足す前と見分けが付きません**
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）・
  **`main.ts` 1450 行（テストの数え方）/ 上限 1500**
- **アイテム 145 種・`MAX_ITEM_ID` 186**（±0）・**立方体 42 / 非立方体 89**（±0）——
  **ブロックもアイテムも 1 つも増やさずに、世界の見え方が変わった周です**
- **1..63 の空きは 7**・**111..255 の空きは 69**（どちらも ±0。**次に取るのは 187**）
- `AUTODEV-QUEUE.md` **116 行 / 上限 150・未着手 3 件**（どちらもテストの数え方）・
  `AUTODEV-SPEC.md` **106 行 / 上限 120・`状態: 済`**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` で、
`AUTODEV-QUEUE.md` の未着手が 3 件あるので判定表の 2 行目です。
**先頭は 12「刈られた羊が見て分かること」**（`mobrender.ts` に 2 つ目の形と
差し替えの仕掛けが要る周で、**`unverifiable-pair` スキルを名指しで渡すこと**）。

- **取る前に、その 1 件だけコードで数え直すこと**（`Mob.woolTimer` がもう立つか）
- **`main.ts` の行数はこの周も動いていません**（1450 行）。12 も配線は
  `mobrender.ts` 側の見込みですが、**数えるのは B の周の仕事**です
- **未着手が 3 件**なので、**12 を実装した次の周（未着手 2 件）で A（棚卸し）に入ります**

## ブラウザで見てほしいところ

**この周で撮った絵は `shots/vine.png`（森のツタ）と `shots/terrain.png`、
`docs/browser-shots/` の 5 枚**です。絵で確かめたのは
**「葉の下から緑の帯が 3 マス垂れていて、後ろが空」「面の欠け・裏返り 0 件」
「ツタの緑 `0x306d18` が葉 `0x3f7a3a` より暗くて見分けられる」**の 3 つ。
**種 4242 の湧き場所は平原なので、ブラウザの 5 枚にツタは写っていません**（森は離れた所）。
人に見てほしいのは（上ほど新しい）:

- **⚠ ツタの生え方の手ざわり（34c）**: **森の木の 25%** に掛かるのは多いか少ないか /
  **1 列 0〜4 マス**の垂れ方は「ツタ」に見えるか（`TUNING.md` の 34c の節）/
  **苗木から育てた木には掛かりません**（意図した違い）/ **針葉樹林には 1 本も
  生えません**（形はもう出来ているので、`BiomeDef.vine` を変えれば生えます）
- **⚠ 垂れたツタの手ざわり（34b）**: **宙にぶら下がった 1/16 の板が「ツタ」に見えるか**
- **⚠ 薄い板の横・真上に石があると置ける件（34b で開いた道。人の判断）**
- **⚠ 石の天井の下のツタ**: **手では置けないのに、あれば落ちません**
  （**34c はこの性質に寄りかかっています** —— 狭めるなら、生成側の切れ端も一緒に見ること）
- **⚠ ツタ（34a）の手ざわり 3 つ**: 登る速さがはしごと同じでよいか / 硬さ 0.2 /
  壁に貼った緑が「ツタ」に見えるか
- **⚠ 金・ダイヤの防具の色（33b）**: **金の上着 `0xefd714` とズボン `0xf8b317` は絵の上でやや近い**
- **⚠ ダイヤ一式 80% 減が強すぎないか**（**防具は壊れません**）/ **⚠ 金を作る理由があるか**
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ 鉄の防具の色（33a）**: 「鋼の青」が鉄に見えるか / 鉄一式 60% 減の手ざわり
- **レンガ（32b）**: `0xbc5d39` が「焼いたレンガ」に見えるか / **粘土（32a）**: 海底の 7.2%
- **ハーフ 2 材質（31）** / **苗木（30b）**: 180 秒が待てる長さか / **木炭（29）** / **修理（28）**
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / **防具（27b）**: 革一式 28% 減
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白** / **氷（25a）**: 滑りの手ざわり
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 2 件**）

- **`rules/worldgen.md`**: **木に掛けるものも `treeshape.ts` が持つ**（葉の段は
  **下から探すこと**）/ **生成で支えの要るものを置くときは、支えを世界の側で
  確かめられる形にすること**（`overwrite: false` がそのまま保険になる /
  **チャンクの端をまたぐ列を書き込み時に `data` で確かめようとしない** ——
  真上が別のチャンクだと読めないので 16 マスごとに列が切れます /
  **「どれだけ生えるか」は 1 マスごとでなく「木 1 本ごと」でよい** ——
  `worldgen.ts` の生えものの連鎖（`tuft`）に 4 つ目を足さずに済みます）。
- **`rules/meshing-render.md`**: **壁に貼り付くものを自然の中で撮るときは、板の
  真向かいに立つこと**（壁の側からだと支え（葉）しか写らない / 斜めだと 1/16 が
  細い線に潰れる / **森の中はカメラが葉に埋まる**ので視線を十数点サンプルする /
  狙うのは**いちばん長く垂れている列の真ん中**）。**3 枚撮り直して分かりました。**

**`RULES-INBOX.md` は ±0 行**（層 3・スキルに足すことはありませんでした）。

## 人がやること

- **⚠ 設計の判断（68 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（テストの数え方。上限 1500）で、**`AUTODEV.md` の停止条件 2 に
  並んでいます**。**待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 /
  **24b ケーキをかじる**）。**次の 12（刈られた羊）は `mobrender.ts` 側の見込み**なので、
  判断が下りなくてもあと 1 周は進みます。**その次の 35 と 24b で当たります**
- **⚠ 石の天井の下のツタの非対称**（手では置けないのに落ちない）。**34c の生成は
  この性質を支えの保険に使っています** —— 狭めるなら、**森のツタの切れ端**も
  一緒に見てください（`test/worldgen.test.ts` の「支えを持っている」が先に落ちます）
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は 11 回走らせて
  2 回**（どちらも 1 件・続けて同じ件は落ちていません）。**直すなら測り方**
  （窓を長く取る・複数回鳴らして中央値）で、**判定はゆるめないこと**。
  **キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 97 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと**。
**⚠ 枠を数えるときは `#creative .slot` を使うこと**（素の `.slot` はホットバーと
インベントリの 36 枠まで拾います）。**⚠ `/opt/pw-browsers` には
`chromium_headless_shell-1194` も居ます** —— `startsWith("chromium-")` で弾くこと
（`tools/browsershot.mjs` の `findChrome()`）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**ツタはこの周（34c）で自然に
生えるようになったので、場面 `vine` は「手で置く」のをやめて
「生えている所を探して立つ」形に差し替えました**（`mushrooms` と同じ形）。
**時間が経たないと見えないもの**（育つ・燃える・溶ける）は、**その場で
`update()` を回してから撮ること**。
**⚠ 防具を着た姿は撮れません**（一人称視点なので自分の体が写りません）。
