# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は C（実装）の周**でした。取ったのは `AUTODEV-QUEUE.md` の先頭
  **7c-2「卵を投げる側」**で、仕様書（`AUTODEV-SPEC.md`）は `状態: 済` にしてあります
- `npm test` **2923 件すべて成功**（前の周の 2904 から **+19**）。`npm run typecheck` /
  `npm run build` も緑。**`npm run bench` は走らせていません**（生成もメッシュ化も
  触っていないため）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 126**（**この周は 0 個使用**。
  `"egg"` は `ProjectileKind` の**文字列**で番号ではありません）
- **アイテムは 94 種**、`MAX_ITEM_ID` は **129**。**飛び道具は 5 種類**（4 → 5）。
  `SaveData` は `version` 1 のままでキーも増えていません（飛び道具は保存しません）
- `REVIEW.md` の未着手は 0 件

## ⚠ 次の周がまず見ること: `main.ts` が 1463 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」3 番（1450 行）を越えました**（1449 → 1463）。
上限（テストが見張っている 1500）にはまだ 37 行ありますが、**次に `main.ts` を触る周は、
先に判断を別ファイルへ出すこと。** この周の増分（`case "throw"` 1 行 + `throwItem()`）は
配線だけで、削れる judgment は入っていません。

## この周でやったこと — 卵を投げる（AUTODEV 19）

**卵を持って右クリックすると視線の向きへ 1 個飛びます。** 落ちながら進み、ブロックか
相手に当たって消えます。**サバイバルでは 1 個減り**、**ダメージは 0**（本家と同じ）、
**ヒヨコは孵りません**（子モブの仕組みが無いので別件）。

`src/` は 4 ファイルだけ、**数値は 1 つも `main.ts` に入っていません**:

- `projectiles.ts` — `PROJECTILE_KINDS` に 1 行（速さ 20 / 重力 1 / 寿命 30 / `vanish` /
  光らない / 向かない）と、union に `"egg"` 1 語
- `items.ts` — **`THROWN` の表 1 本**（`EGG → "egg"`）と `thrownProjectile()`。
  **`import type` で運ぶこと**（値で取ると `items.ts` ↔ `projectiles.ts` が輪になります）
- `use.ts` — `UseAction` に `throw` 1 行、`decideUse()` の**アイの直後・火種の前**に 2 行
- `main.ts` — `case "throw"` 1 行と `throwItem()` 1 本（配線だけ）

**仕様から 1 か所だけ変えています**: 仕様の `throwItem(act.item, act.projectile)` は
`item` を使う所が無く、`tsconfig.json` の **`noUnusedParameters: true`** で typecheck が
通りません。**1 引数（`act.projectile` だけ）**にしました。`UseAction.throw` の `item` は
テストが使うので型からは外していません。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は撮って自分の目で見ました。** 定番 5 枚（`node tools/browsershot.mjs`）は
**console のエラー 0 件**・WebGL 2.0・前の周と同じ見た目。そのうえで**投げた卵そのもの**を
`docs/browser-shots/egg.png` に撮ってあり、**空を上がっていく小さな白い立方体**が
色（`0xf7f0e0`）も傾き（回る = `aims: false`）も表のとおりに写っています。
**地面と平行に投げると 1 枚も写りません**（0.6 秒で落ちるのに 1 枚 300〜500ms。
上を向いてから投げること。撮り方は `docs/browser-shots/README.md` の 16 枚目）。

**人に決めてほしいのは 5 つ**（1 が新しく、2〜5 は持ち越し）:

1. **投げても何も起きないので、使い道がまだ薄い**（ダメージ 0・音なし・ヒヨコなし）。
   **弓は音が鳴るのに卵は鳴らない**のも、手触りとして揃っていません（`TUNING.md`）
2. **卵（`0xf7f0e0`）と羽根（`0xe8e4dc`）が一覧で隣り合っていて、どちらもほぼ白**
   （`E` を押して末尾へ）。2 つまとめて決め直すのが早いと思います
3. **産卵の間隔 300〜600 秒は本家のままですが、ここでは長すぎるかもしれません**
4. **矢が 2x2 で作れなくなってよいか**（AUTODEV 17）。戻すなら `crafting.ts` の 1 行
5. **苗の緑が草原に紛れる**件と **`GROW_SECONDS` 180 秒**、**`#status` の位置**

## 人がやること

- **`RULES-INBOX.md` に層 2（`.claude/rules/**`）宛ての本文が 3 件たまっています。**
  `projectiles.md`（表が 5 種類）/ `use.md`（15 通り・投げるの並び）/ `testing.md`
  （山なりに飛ぶものの的の高さ）。**取り込むのは人が居る周です**
- **どこに決まりごとを書くかが 3 つの文書で食い違っています。** この周のプロンプトは
  「`.claude/**` に 1 行も書くな・`RULES-INBOX.md` へ書け」、`AUTODEV.md` の C-4 と
  `RULES-INBOX.md` の頭は「`npm run rules:install` で周が自分で据える」。
  **この周はプロンプトに従いました**（食い違いは `RULES-INBOX.md` に並べてあります）。
  **どちらを正にするか決めて、負けたほうの本文を消してください**
- **`loop/endgame` にはまだ層 2 の設置の仕組み（`npm run rules:install`）が届いていません**
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手が **4 件**（3 件以上）で、`AUTODEV-SPEC.md` が
**`状態: 済`** なので → **次は B（仕様）の周**です。

- 先頭は **8「牛（革・生牛肉・ステーキ）」**。**取る前にその 1 件だけコードで数え直すこと**
  （キューは数周前の A が作ったもので、もう入っているかもしれません）
- **`src/**` に 1 行も差分を出さないこと**（B の周の完了条件）。仕様書は **120 行以内**で、
  `AUTODEV.md` の B の **7 項目を 1 つも欠かさないこと**
- **ID は `ROADMAP.md` の予約表から**（**次は 130。111 以降はブロックとアイテムで
  1 本の番号列**）。牛は **3 個**の見込みです
- **`main.ts` の 1463 行**（上の ⚠）を仕様に織り込むこと —— 牛は `mobs.ts` の表だけで
  足りるはずですが、`main.ts` に配線が要るなら**先に何を出すか**を仕様に書くこと

**音のテスト（`test/audio.test.ts`）は揺れます。この周も 1 回だけ 1 件赤くなり、
走らせ直したら緑でした**（そのあと 2 回連続で 2923 件すべて成功）。**赤が出たら、まず
もう一度走らせること**（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL が動かない」は開発者の手元の devcontainer の話です）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
