# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-15・AUTODEV 86（B の周）のあと）

**この周は B（仕様）の周**で、**`AUTODEV-SPEC.md` を 32b（レンガ・アイテム 170）に
書き直しただけ**です。**`src/**` と `test/**` は 1 文字も触っていません**（`git status` で確認済み）。
**`npm test` 3621 件すべて成功**・`npm run typecheck` 緑。**下の数字は 1 つも動いていません。**

**この周で決めたこと（HANDOFF が「B の周で決めること」と書いていた宿題）:**

- **⚠ 名前は「ブロック = レンガブロック（12）/ アイテム = レンガ（170）」**（本家の日本語と同じ）。
  直すのは **`blocks.ts` の `def()` の文字列 1 つだけ**で **ID は 12 のまま** ——
  セーブに入るのは番号なので既存のセーブは動きません。**`BRICK` の名前を見ているテストは
  1 本もない**ことを `grep` で確かめました
- **定数名は `BRICK_ITEM`**（`BRICK` にすると `blocks.ts` の 12 と衝突して `typecheck` が落ちます）
- **色は `0xbc5d39`** —— **この周で全アイテムと突き合わせて測りました**。素直な `0x9c5a3c` は
  **`BRICK`(12) の `0xa4553f` と 9.9 しか離れません**（判定は 20）。`0xbc5d39` でいちばん
  近いのは **`BRICK`(12) 本人で 26.0**。赤茶は革・ステーキ・作業台・棒・腐った肉・革のズボンで
  混んでいます（**C の周で測り直すこと。20 を割ったらずらして `TUNING.md` へ**）

## 数字（2026-09-15・AUTODEV 85 の C のあとから動いていません）

- `npm test` **3621 件すべて成功**（+25）。`npm run typecheck` 緑・`npm run build` 緑。
  **音は 1 回目から緑**（`rules/testing.md` の揺れは出ませんでした）
- **`npm run bench` は 4 回**（生成を触ったため）。`generateChunk` **0.75 / 0.90 / 0.79 / 0.88 ms**
  （**中央 0.83**）。**前の記録 0.93〜1.40 の内側**で、遅くなっていません
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行 / 上限 1500**（±0。**1 文字も開いていません**）
- **アイテム 131 種**（+2）・**`MAX_ITEM_ID` 169**・**レシピ 69 本**・**立方体 42 / 非立方体 85**
- **1..63 の空きは 7**（57..63）・**111..255 の空きは 86**（**次に取るのは 170**）
- **`AUTODEV-SPEC.md` は `状態: 未着手`（119 行 / 上限 120）**・**`AUTODEV-QUEUE.md` 未着手 6 件**

## 前の周でやったこと（AUTODEV 85 の C・クラウドの無人の周）

**判定表は 3 行目**（未着手 6 件で 1 行目は外れ・`AUTODEV-SPEC.md` が `状態: 未着手`）で
**C（実装）の周**。**32a「粘土（168）と粘土玉（169）」**を入れました。

- **海と凍った海の海底にまだらに湧きます。** 掘ると**粘土玉 4 個**・**2x2 で粘土 1 個に戻せます**
  （雪玉 → 雪ブロックとまったく同じ対。作業台は要りません）。実測は**海底の列の 7.2%**
  （凍った海は 4.4%）・**4x4 の枡あたり 12 列**・**海底から 3 マス**
- **湧かせ方の要は `biomes.ts` の `BiomeDef.floorPatch`**（`{ block, chance, fill, shift, depth }`）。
  **`VEINS` は使えません** —— あの表が効くのは `depth > 3`（石の中）なので、
  **海底の砂の下には 1 マスも出ません**。`worldgen.ts` が持つのは**塩 2 本
  （`0x3d15` / `0x6e83`）と差し替えの 3 行だけ**で、**`CLAY` を import していません**
- **サブエージェントは立てていません**（`AUTODEV.md` の C-1 の「同じ番の中で結果が
  返らないなら自分で実装すること」）。**点検（C-2）も親が `git diff` を読んでやりました**
- **`main.ts` も `*render.ts` も `ui.ts` も `inventoryui.ts` も 1 文字も触っていません**

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です
（未着手 6 件なので 1 行目は外れ、`済` でないので 2 行目も外れます）。
**取るのは `AUTODEV-SPEC.md` の 32b（レンガ・アイテム 170）だけ。**

- **仕様書は全文を渡すこと**（要約しない。`AUTODEV.md` の C-1）。**渡す `rules/`** は
  `items-survival.md`・`blocks-shapes.md`・`inventory-screen.md`・`testing.md`
  （この周で `grep -l` で引きました）。使うスキルは **`add-block`**
- **`main.ts` は 0 行の見込み**（かまども 2x2 も既存の道を通ります）。**`npm run bench` は不要**
- **撮るのはクリエイティブ一覧 1 枚だけ**（`npm run shot` は不要 —— 新しいブロックが
  0 個なので、どの場面にも 1 マスも増えません）。**`.label` が「レンガ」と「レンガブロック」の
  枠を `scrollIntoView({ block: "center" })`** すること（末尾に送っても写りません）
- **数え直す既存の判定は 4 件**（仕様書の 5-7）: 共有帯の空き 86 → 85・共有帯のアイテム
  51 → 52 個・`MAX_ITEM_ID`・レシピ 69 → 70 本。**ゆるめるのではなく件名に理由を書くこと**

## ブラウザで見てほしいところ

**この周（B）は `src/**` を触っていないので撮っていません。** 下は**前の周（85 の C）で
2 枚撮って、両方 `Read` で開いて見たもの**です（`AUTODEV.md` の C-3）:

- **`shots/clay.png`**（`tools/shot.ts` に場面 `clay` を足しました）。**水越しに、砂の海底へ
  4x4 ほどの粘土の塊がまだらに乗っているのが読めます。** 面の欠けも裏返りもありません
- **`docs/browser-shots/creative-clay.png`**（本物のブラウザ）。**一覧の枠 129 → 131**・
  **末尾が 木炭 → 苗木 2 種 → 粘土 → 粘土玉**・`.swatch` は指定どおり
  （`rgb(157,163,181)` / `rgb(167,180,218)`）・**console のエラー 0 件**

**それでも人に見てほしいのはここです**（上が新しい分）:

- **粘土（32a）**: **海に潜って粘土を見つけられるか**（海底の列の 7.2%・4x4 の塊）と、
  **水越しに砂と見分けられるか**。**色は測ってずらしてあります** —— 仕様書の見当
  （`0xa4aab9` / `0xb0b8cc`）はバケツと 15.7 / 糸と 9.8 しか離れていなかったので、
  **`0x9da3b5`（粘土）/ `0xa7b4da`（粘土玉）**にしました（`TUNING.md`）。
  **粘土玉がやや青紫寄り**なのが気になるかどうかは人の判断です
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **9 文字の「ネザーレンガハーフ」は 3 行に折れて枠を丸ごと覆います**
  （`docs/browser-shots/creative-brick-slabs.png`）。**人の判断待ち**
  （**粘土・粘土玉は 2〜3 文字なので、この周は出ていません**）
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）/ **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）/
  **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周（B）は決まりごと 0 件**。下は前の周（85 の C）で据えた 2 件）

- **`rules/worldgen.md`**: **海底の数マスを差し替えるのは `BiomeDef.floorPatch`**。
  **`VEINS` は `depth > 3`（石の中）なので海底には 1 マスも出ません** ——
  掘り当てられない鉱脈になります。**2 本のハッシュは `ly` のループの外で引くこと**
  （中だとチャンク 1 個で 8192 回）。**「地表がバイオームどおり」を見張るテストは
  `floorPatch` を勘定に入れ、全部にゆるめないこと**（陸の行に写した・`?:` にして
  足し忘れた、が黙って通ります。**「陸に 1 マスも無い」を別の 1 件に置くこと**）
- **`rules/meshing-render.md`**: **海の底を撮るなら凍っていない海を選び、急な角度で
  見下ろすこと**（この周で 2 度撮り直しました。**氷の板**と**水面の板**がそれぞれ
  画を覆います）。**遠くのものを探すのに `primeAround` を広げないこと** ——
  `World` は用意した列をぜんぶメッシュ化するので ±128 で 2.4 秒かかります。
  **探すのは `sourceOf()` が返す生成器**（この周で足しました）で、**見つけてから
  `makeWorld(…, at)` で中心を動かすこと。**

## 人がやること

- **⚠ 設計の判断（57 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1449 行**（テストの数え方で 1450）で、**`AUTODEV.md` の停止条件 2 に並んでいます**
  （上限 1500）。**待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 /
  **24b ケーキをかじる**・最低 7 行）。**キューの上 3 件（32b・33・34）はどれも
  `main.ts` 0 行の見込み**なので、判断が下りなくても 3 周は止まりません
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（足すならそれだけで 1 周）
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は 1 回目から緑**でしたが、
  前の周は跳ねています。**直すなら測り方**（窓を長く取る・複数回鳴らして中央値を見る）で、
  **判定はゆるめないこと**（`rules/testing.md`）。**キューに足すのは A の周の仕事**です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 85 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押してから撮ること**
（`docs/browser-shots/README.md` の末尾）。**⚠ 送り先は末尾とは限りません** ——
**低帯（1..63）の番号を取ったものは一覧の真ん中に出る**ので、`scrollTop = scrollHeight`
では 1 枠も写りません。**`.label` の文字列で枠を探して
`scrollIntoView({ block: "center" })` を呼ぶこと。**

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**海底の粘土（32a）も場面 `clay` を足しました** —— ただしあれは**原点のまわりを
撮りません**（種 4242 は ±128 の海が全部凍っています。`rules/meshing-render.md`）。
