# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm test` は通ります。**`npm test` 2307 件すべて成功**
  （**前の周から増えていません** —— この周は `src/**` も `test/**` も 1 行も触っていないため）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1373 行 / 上限 1500**（この周は触っていません）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— この周は ID を 0 個も使っていない）
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（118 行 / 上限 120）→ **次の周は C（実装）**
- **`RULES-INBOX.md` に未取り込みが 2 件**（`mobs.md` と `projectiles.md`）。**取り込むのは人が居る周**

## この周でやったこと（無人・B の周 1 種類だけ）

`AUTODEV.md` の判定表は「キューの未着手が 3 件以上（9 件）」「仕様書が `済`」で **B（仕様）** に
当たったので、**`AUTODEV-SPEC.md` を 1 枚書いただけ**です。**`src/**` と `test/**` に差分はありません**
（`git status` は `AUTODEV-SPEC.md` と `HANDOFF.md` の 2 つだけ）。

### 書いた仕様 —— **大きいチェスト（隣り合った 2 個で 54 枠）**

B の決まりどおり、取る前にその 1 件だけコードで数え直しました:
`chests.ts` の `CHEST_SIZE` は 27 固定、`addToChest` も `0..CHEST_SIZE` 決め打ちで、
隣り合ったチェストを見る経路は `src/` に 1 つもありません。**まだ入っていません。**

仕様の中心は 2 つです（実装の周はここを崩さないこと）:

- **ブロック ID も新しい入れ物も作りません。** 組かどうかは**そのつど voxel から**決めます ——
  水平 4 マスに `CHEST` がちょうど 1 個で、**相手から見てもちょうど 1 個**のときだけ組。
  これで `beds.ts` と同じ「相方の相方は自分」が成り立ち、**3 個並びや 2x2 は全部 27 枠に戻ります**
- **54 枠は `Slot` の参照を並べた「見え方」**（`{ slots: [...根, ...相方] }` をそのつど作る）。
  中身は 1 マス 27 枠のまま Map に残るので、**セーブの形は 1 バイトも変わりません**
  （`version` は 1 のまま）。片方を壊しても、残った側は 27 枠として中身を保ちます

判断の置き場は `chests.ts`（`chestPartner()` / `Chests.open()`）、`craftscreen.ts` は
枠数（`chestSize`）を出すだけ、`inventoryui.ts` は **DOM だけ**、`main.ts` は **1 行だけ**。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は `src/**` を 1 行も触っていないので、新しく見るものはありません。**
**前の 2 周からの持ち越しが 2 件、まだ人の目を通っていません:**

1. **砂・砂利が落ちるようになった周のぶん**（`6209e91`）—— 砂の柱の一番下を掘ると上の砂が
   その場で詰まり、**間に穴や浮いた砂が残らないこと**（落ちる途中の姿は出ません）。
   水面の上から砂を落として**埋め立てられること**。**掘っていないのに崖や洞窟の天井が
   勝手に崩れていないこと**（生成直後の地形は触らない約束なので、崩れていたら退行です）
2. **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**
   （洞窟の奥・水面・ガラスごし）と **AO の陰影が前と同じか**（壁ぎわ・階段の下）

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **9 件**（3 件以上あるので A ではない）、
**`AUTODEV-SPEC.md` は `状態: 未着手`** → **次は C（実装）の周**です。

- **仕様書は全文を渡すこと**（要約すると「触らないファイル」と「禁じ手」が真っ先に落ちます）
- **サブエージェントを背景（async）で起動しないこと。** 同じ番の中で結果が返らないなら
  **自分で実装すること** —— 周を閉じずに終わるほうがずっと高くつきます
- 件数は **`npm test` の実測（2307 件）**から取ること。仕様書にもその数で書いてあります

人がやること:

- 上の**ブラウザ確認 2 件**（どちらも前の周からの持ち越し）
- `RULES-INBOX.md` の 2 件の取り込み
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）はまれに 1 件落ちます。**
**2 回続けて落ちたときだけ退行として扱うこと**（この周は 2 回とも通りました）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
