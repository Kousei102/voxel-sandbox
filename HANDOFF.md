# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-21）

- ブランチ **`loop/endgame`**。**`origin/master` より 14 コミット先**。**push していません**
  （`master` への push はユーザーが確認してからやります。ループはコミットまで）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1234 件すべて成功**（12 秒前後）
- **進行（クリア導線）達成 3 / 13**（**3 件とも本物の判定**）。
  達成: 溶岩がある / 黒曜石が手に入る / 火打石と打ち金が作れる
- ブロック ID **1..63 の空きは 19 個**。アイテム ID は 88 まで使用済み
- `src/main.ts` は **1330 行 / 上限 1500**

## この周でやったこと

**TASKS 1-3（砂利 → 火打石 → 火打石と打ち金）** の 1 コミット（`299168b`）。

1. ブロック 44（砂利）。`worldgen.ts` の **`VEINS`（旧 `ORES`）に相乗り**させ、
   **`shift`（塊の粗さ）を足した** —— 鉱石は 2x2x2 のまま、砂利だけ 4x4x4。
   1 のままだと石に胡椒を撒いたような散り方になります
2. アイテム 87（火打石）/ 88（火打石と打ち金）。レシピは形なしで鉄インゴット + 火打石
3. **`Drop` に `otherwise` を足した。** 「10% で火打石、外したら砂利」は今までの
   「外したら何も落ちない」では書けず、掘ると 90% で消えるブロックになります
4. **何が落ちるかは `items.ts` の `rollDrop(blockId, roll)`**（乱数は呼ぶ側が作る純関数）。
   `main.ts` から確率の比較が消え、`test/ui.test.ts` が `.chance` の再侵入を見張ります

**見張りは 3 つとも壊して噛むことを確かめました**（`otherwise` を落とす / `shift` を 1 に戻す /
`main.ts` に確率の比較を戻す）。`otherwise` を落としたときは**進行テストが 2 件に後戻りして
ラチェットも鳴りました** —— 北極星が実際に効いています。

## 測ったもの

`generateChunk` に 5 本目の `VEINS` が乗るので `npm run bench` を掛けました。
**A/B を交互に 5 回ずつ**まわして中央値 **1.31ms（砂利なし）→ 1.33ms（あり）**で、誤差の中です。

**1 回ずつの比較では「35% 遅くなった」と読めていました。** この箱は同じコードでも
**1.08〜2.04ms に振れます**（`npm run bench` を続けて回すと分かります）。
交互に測るまで信じないこと。

## 未確認のもの（ユーザーにブラウザで見てもらう）

**`REVIEW.md` の「1-3. 砂利 → 火打石 → 火打石と打ち金」7 項目だけです。**
黒曜石（1-2b-2）はユーザー確認済みなので、節ごと消してあります。

## 次にやること

**`TASKS.md` の上から順に。** 次は **1-4（`portals.ts` — 枠の検出と点火の可否）**。

- **黒曜石（1-2b-2）は確認済みなので、依存は解決しています**
- 判断だけのファイルにすること。`World` を丸ごと受け取らず `getVoxel` / `setVoxel` だけ
  （`beds.ts` と `liquids.ts` が手本）
- ID 49 (`NETHER_PORTAL`) は非立方体（`isProp`）。向き 2 種は 64 以降
- **点火の可否と枠の形だけ。次元をまたぐ移動は 1-6**
- **火打石と打ち金は今のところ何もしません**（持って右クリックしても無反応）。
  1-4 でここに繋がります

## ループの運用について

`/loop` での同席は続いています。**Routines へ移すのは、タスクの粒度と停止条件が
機能することを 5〜10 周見てから**（`LOOP.md` の 5.）。
停止条件に当たったのは **黒曜石の入手導線をどうするか（難度の判断）** の 1 回だけで、
ユーザーがバケツを選んで解決しました。砂利の湧きやすさ（石の 2.4%）は止まらずに決めて、
`REVIEW.md` で見てもらう形にしています。
