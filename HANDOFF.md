# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-07・AUTODEV 48 のあと）

- `npm test` **3170 件すべて成功**（前の周から **+15 件**）・`npm run typecheck` 緑・
  **`npm run build` も通りました**（`src/**` を触った周なので）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **音の一群がこの周に 2 回跳ねました。落ちた件は毎回違います** ——
  1 回目は「cutoff が低い材質ほど…」の羊毛、2 回目は「声色（音程の倍率）が波形に出る」で
  低い声が 0.162 → **4.048**。**どちらも次の 1 回で緑**なので退行ではありません
  （`rules/testing.md` の「**同じ件が**続けて落ちたときだけ」）。落ち着いた値は
  羊毛 0.552 / 雪 0.716 / 砂 2.235・低い声 0.162 / 高い声 0.329 で、前の周と同じです
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1449 行**（`wc -l`。テストの数え方では 1450。上限 1500・
  **止まる目安 1450 に届いたままです。この周は ±0**）
- **アイテム 111 種・`MAX_ITEM_ID` 149・111..255 の空きは 106・1..63 の空きは 9**
  （**149 = リンゴを 1 個取りました。予約表の次の空きは 150**）
- **立方体 39 / 非立方体 76**（**ブロックは 1 つも増えていません**）・**モブ 9 種類**・
  **レシピ 55 本**（±0）・**精錬 7 本**（±0）・**燃料 8 種**・**食べ物 10 種**（+1）・
  **バイオーム 10**・**構造物 2**・**次元 3**・**`DamageCause` 9 種**

## この周でやったこと（AUTODEV 48 の C・クラウドの無人の周）

**キューの先頭 20（リンゴ）を実装しました。** 判定表は「未着手 10 件（3 件以上）」
「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C の周です。

**オークの葉を壊すと 0.5% でリンゴ（149）が 1 個落ちます。** 食べると空腹 +4 /
満腹度 +2.4（本家の値）。**針葉樹の葉からは落ちません。**

**本体は番号 1 個ではなく「2 本目の乱数」でした。** `Drop.extra`（2 山目）は前から
あったのですが、**流れてくる乱数が `roll` の 1 本だけ**だったので、`extra` に確率を
付けると**1 山目の当たり外れと必ず相関**します（棒が出た葉からだけリンゴが出る形）。
この周で **`extraRoll` を 1 本通しました** —— `rollDrops(id, roll, extraRoll)`（**3 番目は
必須**）・`BreakOrder.extraRoll`・`autoBreak()` の 8 番目の引数まで。
**`main.ts` は 2 か所に `Math.random()` を 1 個ずつ足しただけで、行数は ±0 です。**

**退行の見張りは「4 通りの山の数が `1,2,0,1`」1 件**（`test/blocks.test.ts`）。
**1 本の乱数では作れない表**なので、`extraRoll` を落とすと `1,2,0,0` に潰れて落ちます。

**⚠ 点検役のサブエージェントが `git stash` を打ちました。** C-2 を投げたところ、
**`run_in_background: false` を指定したのに背景で起動し**、`git diff` を HEAD と
比べるつもりだったのか **`git stash` で作業ツリーを空にしました**（未コミットの実装が
丸ごと消えた状態が数十秒）。`git stash list` で気付いて `pop` で戻し、**点検は
こちらで `git diff` を読んでやりました**。`rules/testing.md` に据えてあります。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-QUEUE.md` の未着手が **9 件**あり、
`AUTODEV-SPEC.md` が **`状態: 済`** なので、判定表の 2 行目に当たります。

**キューの先頭は 21（紙・本・本棚）**。サトウキビ 3 → 紙 3 / 紙 3 + 革 1 → 本 /
板 6 + 本 3 → 本棚で、**材料（サトウキビ 143・革 132）はどちらも実装済み**です。
**取る番号は 150 から 3 個**（`ROADMAP.md` の予約表）。**`main.ts` は 0 行の見込み**
（レシピ 3 本と立方体 1 個だけ）。

## ブラウザで見てほしいところ

**この周は `node tools/browsershot.mjs` で 5 枚撮り直しました**（WebGL 2.0・
`gl.getError()` 0・console のエラー無し・地形は色 1164 種）。`hud.png` と
`inventory.png` を開いて見て、**前の周から見た目は変わっていない**ことを確かめました。

**リンゴは 1 枚にも写っていません** —— 一覧に出るのは**クリエイティブの持ち物画面**で、
`browsershot.mjs` はサバイバルのインベントリしか開かないためです。**色の見分けだけは
テストの数値で押さえました**（赤キノコ 0xc9403a から 29.5。判定は RGB で 20 以上）。

**この周で見てほしいもの**: **リンゴの色 `0xe0342c` が、一覧で赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か**（木 1 本の葉が
40〜60 枚なので、**1 本丸ごと刈って 4〜5 回に 1 個**。`TUNING.md` に 1 行足しました）/
**地面に落ちたリンゴの立方体が、赤キノコの落とし物と見分けられるか**

**持ち越し（まだ誰も見ていません）**: **2.35 m/s の登りが待てる速さか**（4 マスの縦穴に
1.7 秒・地表から岩盤近くまで 17 秒）/ **手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と
感じられるか**（本家と同じ挙動ですが、**しゃがみで止まる手はここにありません** ——
`Shift` は走りなので、止まる手を足すと `controls.ts` の話になります）/ **はしごの上端から
地上へ出る手触り** / **はしごの色（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか** /
**厚さ 3/16 の板が「はしご」に見えるか**（横木は無く、板 1 枚です）/
**「はしご は壁にしか付けられません」の文が、床を狙ったときに親切か** /
**`CANE_GROW_SECONDS = 180` が待てる長さか**（1 段から 3 段まで 6 分）/ **上を刈って
伸び直すのが「畑」として成り立つか** / **自然に生えたサトウキビは伸びません**（上に 1 本
置けば列ごと覚えます。理由は `TUNING.md`）/ サトウキビの背丈と密度の釣り合い（0.12 で
1〜3 段）/ **積む手ごたえ** / **⚠ 前から遊んでいる世界だけの見え方**: `localStorage` の
差分は「壊した／置いた」だけを持つので、**前に浜のサトウキビを壊した場所は、下の 1 マス
だけが空いて上の段が浮いて見えることがあります**（新しく作った世界では起きません）/
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / サトウキビ
（`0x9ad14f`）が小麦の種と並んだとき / ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け /
シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか / 食べ終わりにボウルが手に戻る
手ごたえ / 森を歩いたときのキノコの密度（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を
草むらと取り違えないか / サボテンに当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・
ミルクで毒が止まるか / ミルクバケツの色（`0xeaf2f8`）/ かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件・どちらも `rules/` へ据えました**）

- **`rules/items-survival.md`**: 「`extra` に `chance` を持たせないこと」を、
  **`extraRoll` が入った形へ書き直しました**（省略 = 必ず落ちる / 付けたぶんは
  2 本目の乱数で引く / `chance` を `DropStack` の側へ移さない / 個数の範囲はまだ無理）。
  **`rollDrops()` の 3 番目を省略可能にしないこと**も足しました
- **`rules/testing.md`**: **点検役のサブエージェントに `git` を打たせないこと**
  （上の `git stash` の件）。**`run_in_background: false` を指定しても背景で起動する
  ことがある**のも一緒に書きました

## 人がやること

- **設計の判断を 1 つお願いします（22 周持ち越し。もう待てません）:
  `main.ts` を 2 つに割ってよいか。** いま **1449 行で、止まる目安 1450 に届いています**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **20（リンゴ）は宣言どおり 0 行で通りました。21 紙・本・本棚 / 22 金のリンゴ /
  27a 防具の枠も 0 行の見込み**なので、**あと 3 周ぶんの余裕はあります。
  23 クモの巣・24 ケーキ・25 氷・26 フェンスは配線が要ります。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 32 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。
**`main.ts` の行数も同じ数え方**なので、`wc -l` の 1449 はテストでは 1450 と出ます。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
