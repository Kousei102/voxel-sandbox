# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-03）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は無人の C（実装）の周**でした。触ったのは `src/` 3 つ（`items.ts` /
  `mobs.ts` / `crafting.ts`）と `test/` 3 つ（`blocks` / `mobs` / `crafting`）、
  あとは文書です。**`main.ts` は 1 行も触っていません**（1449 行 / 上限 1500）
- `npm test` **2858 件すべて成功**（前の周の 2852 から +6）。`npm run typecheck` と
  `npm run build` も緑
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 127**（**この周は 1 個使用**）
- **アイテムは 93 種**、`MAX_ITEM_ID` は **128**。**モブは 7 種類**。`SaveData` は `version` 1
  （**1 バイトも増えていません** —— モブは保存しないので、羽根はセーブに出てきません）
- **`AUTODEV-SPEC.md` は `状態: 済`**（118 行）→ **次の周は B（仕様）**
- **`RULES-INBOX.md` は未取り込み 8 件**（**この周が 2 件足しました**。下の「人がやること」）
- `REVIEW.md` の未着手は 0 件

## この周でやったこと — AUTODEV 17「羽根（鶏の 2 山目）と、矢を本家の形へ」

`AUTODEV-QUEUE.md` の先頭 **7b** を実装しました（仕様書は全文をそのまま使い、
**差し戻し 0 回**）。**サブエージェントは使っていません** —— 同じ番の中で結果が返る
呼び方の当てが無かったので、`AUTODEV.md` の「返らないなら自分で実装する」を採り、
点検（C-2）も自分で `git diff` を読みました。

- **鶏を倒すと 2 山**（生鶏肉 1 個 + **羽根 1 個**）。**殴っても撃っても同じ**です
- **矢は「火打石 + 棒 + 羽根」で 4 本**（本家と同じ形と並び）。**3 段になったので
  作業台が要ります** —— **いままでの「弓を持って出たまま矢を作り足せる」は失われました**
- **アイテム 128 = 羽根**（置けず・道具でもなく・**食べ物でもない**）

**形は `items.ts` の `Drop.extra`（ブロックの 2 山目）をそのまま持ってきました。**
`MobDropStack`（山ひとつ）を切り出して `MobDrop extends MobDropStack` に `extra?` を
足しただけなので、**既存 7 つの `drop:` は 1 行も書き換わっていません**。
落ちる山を全部返すのは **`dropsFor(mob, def, random)`** の 1 本で、`rollDrop()` に対する
`rollDrops()` とまったく同じ形です（**`dropFor()` は残してあります**）。

**ブロック側と 1 点だけ違います**: **`MobDrop.extra` は `chance` を持てます。**
`Drop.extra` が確率を持てないのは、壊す側に流れてくる乱数が `BreakOrder.roll` の
**1 本だけ**だからで、モブの側は `random()` という**関数**が流れてきます。
**この違いは決まりごとにしました**（`RULES-INBOX.md`）。

**踏まずに済んだ穴**（仕様書が先に書いていたもの。詳細は `docs/autodev-log.md`）:

1. **`chance >= 1 || random() < chance` の短絡をそのまま使うこと** —— `chance` が 1 の山では
   乱数を引きません。引く形にすると**種を固定した既存テストの目がずれます**。
   「引いた回数」を数える乱数でテストに固定しました（鶏 0 回 / ゾンビ 1 回）
2. **`attack()` と `hitByProjectile()` の 2 か所とも差し替えること** —— 片方だけだと
   **弓で撃ったときだけ羽根が出ません**。テストは殴った側と撃った側を並べて測っています

**既存の判定を 1 つ書き直しました（ゆるめてはいません）**: `test/mobs.test.ts` の
「ドロップは多くても 1 回」を、**表が持っている山の数ぶんだけ**許す形に一般化しました
（`drops.length <= 表の山の数` かつ**並び順のまま**かつ `chance` 1 の山は必ず出る）。
**既存 6 種のモブでは今までとまったく同じ判定**です。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は自分で撮って見ました**（`node tools/browsershot.mjs` の定番 5 枚 ——
**console のエラー 0 件**・WebGL 2.0・`inventory.png` は壊れていません）。
そのうえで**クリエイティブの一覧を末尾まで送って撮り直し**、
`docs/browser-shots/creative-feather.png` に置いてあります（README の 14 枚目）。
**一覧の末尾に「羽根」がほぼ白の枠で出ていて、名前の欠けも溢れもありません。**

それでも人の目でしか決まらないのは 4 つです:

1. **矢が 2x2 で作れなくなってよいか**（**この周でそうなりました**）。本家の形に
   戻すのが 7b の目的でしたが、**手触りとしては明確に不便です** —— 弓を持って
   出たまま矢を作り足せません。戻すなら `crafting.ts` の 1 行（`TUNING.md`）
2. **羽根のほぼ白（`0xe8e4dc`）が一覧の中で見分けが付くか**（末尾なので隣の生鶏肉・
   焼き鳥とは分かれますが、**羊毛・雪の白と同じ系統**です。`E` を押して末尾へ）
3. **鶏が「鶏らしいか」**（豚・羊と並べて。歩いて翼が振れているところは見ていません）。
   声 1.8 の高さも耳で ——**前の周からの持ち越し**
4. **苗の緑が草原に紛れる**件と **`GROW_SECONDS` 180 秒が待てる長さか**、
   **`#status`（オートセーブの帯）の位置**（狭い窓でも）——**前の周からの持ち越し**

## 人がやること

- **`RULES-INBOX.md` の 8 件を取り込むこと**（`.claude/rules/` は無人の周が触ると
  確認待ちで止まるため）。**この周が 2 件足しました**:
  - **`items-survival.md` の末尾の 1 段落**は、**パンの周・鶏の周・この周の 3 つが
    同じ場所を指しています**。決まりのとおり古いほうは直していないので、
    **いちばん新しい（羽根と矢の周）を採ってください**
  - **`mobs.md` に新しい節「2 山目を落とす（`MobDrop.extra`）」** —— こちらは
    既存のどの本文とも食い違いません
- **羽根を 1 個固定にしたのはこちらの判断です**（本家は 0〜2）。`MobDrop.extra` に
  個数の範囲を持たせない、という線引きです（`TUNING.md`）
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 済`** なので → **次は B（仕様）の周**です。

- `AUTODEV-QUEUE.md` の**未着手は 5 件**（7c 卵 / 8 牛 / 9 クモと糸 / 10 スケルトンと骨 /
  11 刈られた羊が見て分かること）。**3 件以上あるので A（棚卸し）には入りません**
- **先頭は 7c（卵）**。仕様書を書く前に**その 1 件だけコードで数え直すこと**。
  キューの行が「**さらに割れます**」と書いているとおり、**産む側（`Mob` に間隔のタイマー）と
  投げる側（`projectiles.ts` の表に 1 行）の 2 つ**があります。**120 行に収まらなければ
  その場で 2 件に割って前半だけを仕様書にすること**
- **`src/**` と `test/**` は B の周では 1 行も触らないこと**
- **ID は `ROADMAP.md` の予約表から。次の空きは 129**（**111 以降はブロックとアイテムで
  1 本の番号列**なので、片側だけ見て取らないこと）
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に 1 件だけ赤 →
走らせ直したら緑。**この周は 5 回とも緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL が動かない」は開発者の手元の devcontainer の話です）。
**クリエイティブの一覧を撮る手**は `docs/browser-shots/README.md` の 12・14 枚目
（`#mode` をクリックしてから `E`、そのあと `#creative` の `scrollTop` を最後まで送る）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
