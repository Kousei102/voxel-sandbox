# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2624 件すべて成功**（前の周と同じ。
  **この周は `src/**` と `test/**` に 1 行も触っていません** —— B の周の完了条件）
- `npm run build` は走らせていません（`src/**` を触っていないため）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1420 行 / 上限 1500**（この周 **±0 行**）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 140**（この周は **1 個も取っていません**）
- **アイテムは 83 種**。`MAX_ITEM_ID` は **115**
- **`AUTODEV-SPEC.md` は `状態: 未着手`** → **次の周は C（実装）**（下の「次にやること」）
- **`RULES-INBOX.md` は未取り込み 4 件**（前の周が書いたもの。この周は 1 件も足していません）

## この周でやったこと（無人・B の周 1 種類だけ）

**`AUTODEV-QUEUE.md` の先頭「6. 小麦・種・耕地・パン」を 2 件に割って、前半だけを
`AUTODEV-SPEC.md`（120 行ちょうど）にしました。** 触ったのは
`AUTODEV-SPEC.md` と `AUTODEV-QUEUE.md` の 2 ファイルだけです。

**割り方**: 前半 =「**クワ 4 本と耕地**（土を耕す）」/ 後半 =「**種・小麦・パンと
育つブロック（`crops.ts`）**」。後半は `AUTODEV-QUEUE.md` の **12 番**として 6 番の
すぐ下に書き戻してあります（**依存: 6**。耕地が無いと植える場所がありません）。

**取る前にコードで数え直しました**（B の決まり）: `hoe` / `クワ` / `farmland` / `耕地` /
`crops` / `wheat` / `小麦` は `src/**` にも `test/**` にも 1 件もありません。
**`TALL_GRASS`（草むら・ブロック 32）はもうあります** —— 後半で種の出どころになります。

**仕様を書きながら分かったこと**（次の周が同じ道を通らないために）:

- **耕地は `variantOf: DIRT` にすれば、アイテムもドロップも 1 行も書かずに済みます。**
  `items.ts` のブロック → アイテムの for が `variantOf !== AIR` を飛ばすので**持てず**、
  `dropOf()` の既定が `baseBlock()` なので**掘ると土**が出ます（点火中のかまどと同じ仕掛け）。
  **立方体を 111 以降に置いてよいのは `encodeFace` が 8 ビットになったから**です
  （`CLAUDE.md` の「立方体を 64 以降に置かないこと」は 1..110 の凍結した帯の話。
  `ROADMAP.md` の予約表が新しいほう）。
- **`ToolKind` に `"hoe"` を足すと `TOOL_NAMES` は typecheck が教えてくれますが、
  `mobs.ts` の `TOOL_ATTACK` は `Record<string, number>` なので教えてくれません**
  （足し忘れると `attackDamage()` が **NaN**）。シアーズの周に踏んだ罠と同じ場所です。
- **耕したときの摩耗は `wearForUse()` に混ぜられません**（あちらは「掘る道具でないもの」の表で、
  クワは `toolOf()` を持つので 0 が返ります）。**`wearForAttack()` を写した 4 本目
  （`wearForTill()`）**にしてあります —— つまり**減り方は 3 種類から 4 種類になります。**
  `RULES-INBOX.md` の未取り込み 1 件目（「使って減る」の表）と**食い違うことになるので、
  C の周で 4 種類に直す本文を `RULES-INBOX.md` へ書くこと**（古いほうは直さず、並べて置く）。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は撮っていません**（`src/**` を 1 行も触っていないので、前の周から画が
変わりようがありません）。`docs/browser-shots/` にあるのは前の周の 6 枚です。

**次の C の周で撮ることになるのは 2 つです**（仕様書の終了条件に書いてあります）:
クリエイティブの一覧が **83 → 87 枠**になること（末尾が `… シアーズ → 木/石/鉄/ダイヤのクワ`）と、
**耕地の色**（`top: 0x59422d`。土 `0x6b533a` より暗い茶で、並べたときに耕したと分かるか）。

## 人がやること

- **`RULES-INBOX.md` の 4 件の取り込み**（`items-survival.md` の「使って減る」の行 /
  `mobs.md` の新しい節「刈って取る」/ `use.md` の 2 件）。**本文はそのまま書いてあります**
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**は
  まだ直っていません（`#status` は `bottom: 118px` 固定。`style.css:674`）。**`REVIEW.md` に積んであります**
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手は 7 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 未着手`**
なので → **次は C（実装）の周**です。**`AUTODEV-SPEC.md` を全文そのまま実行すること**
（要約して渡さない。要約すると「触らないファイル」と「このタスク固有の禁じ手」が真っ先に落ちます）。

- **ID は 116..120 の 5 個**（116 = 耕地・ブロック / 117..120 = クワ 4 本・アイテム）。
  **111 以降はブロックとアイテムで 1 本の番号列**なので、片側だけ見て取らないこと。
  取ったら `ROADMAP.md` の予約表に「実装済み」と書くこと。
- **終わったら `AUTODEV-QUEUE.md` の 6 番の行を消し、`AUTODEV-SPEC.md` の `状態:` を `済` に。**
  次の周はそれを見て B（12 番 = 後半の `crops.ts`）に入ります。
- **見た目に出る周です**（クリエイティブ一覧が 4 枠増える）ので、**C-3 の撮影を飛ばさないこと** ——
  `npm run build` → `(npx --no-install http-server dist -p 8080 --silent &)` →
  `node tools/browsershot.mjs` → **撮った png を `Read` で開いて見る**。

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（緑）。
`audio.ts` / `sfx.ts` には 1 行も触っていないので、**直ったわけではありません。**
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
