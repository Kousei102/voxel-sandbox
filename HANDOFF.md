# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-25）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。無人で回すうえで
  実際に踏んだものが 3 つあります:
  **(1)** **`.claude/rules/**` を編集すると「sensitive file」の確認で止まります。**
  → **`RULES-INBOX.md` に書いて、人が居る周が取り込む**形にしました（`LOOP.md` の「4.5」）。
  **`.claude/settings.json` に許可を書いても消えません**（2026-08-25 に 2 周とも実測）。
  **(2)** クラウドから `origin` へ push できず 403 → ユーザーが GitHub 側で権限を付け直し済み。
  **(3)** **5 時間の利用制限。** 当たった周は実装を終えた直後に止まり、次の定期実行も
  同じセッションに投げられて即終了しました（**新しい周にならず、1 回ぶんの枠が消えます**）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1738 件すべて成功**（1714 → 1738。この周の 24 件はリスポーンの次元）
- **進行（クリア導線）達成 7 / 13**（7 件とも本物）。**この周は増えていません**
  —— 2-4c はクリア導線の項目ではなく、**ユーザーがブラウザで見つけた不具合の直し**でした
- ブロック ID **1..63 の空きは 14 個**（この周はブロックを足していません）。
  アイテム ID も **89 まで**のまま（89 = ブレイズロッド）
- `src/main.ts` は **1401 行 / 上限 1500**（余白 99 行。この周で 36 行増えました）

## 直前にやったこと — TASKS 2-4c（死んだら寝た次元へ戻る）

**症状: ネザーで死ぬと天井の岩盤の上に湧く**（ユーザーがブラウザで発見）。
**根はリスポーン地点が次元を持っていなかったこと**で、`moveToSpawn()` が
**いま居る次元のまま**ベッドのマスを読んでいました（`surfaceY()` は上から降りてくるので、
ネザーでは最初に当たる天井の岩盤の上面が返ります）。

| どこ | 何が入ったか |
| --- | --- |
| `beds.ts` | `BedPoint.dim` / `Beds(home)` と `homeDimension` / `set(x,y,z,dim)` / **`respawnDimension()`** / **`respawnPlan(world)`** / `serializeDim()` / `deserialize(raw, dim)` |
| `storage.ts` | `SaveData.bedDim?: string`（**省略可。オーバーワールドなら書かない**） |
| `session.ts` | `SaveParts.bedDim` と `buildSave` の 1 行 |
| `main.ts` | **`returnToDimension()`（新）** と `moveToSpawn()` の並べ替え。ほかは `beds.set` に次元・セーブ 2 行・`startWorld` の `y` を省略可に |

- **地点はいまも世界に 1 つだけ**です（最後に寝たベッド）。増えたのは
  「**どの次元の** 1 点か」だけ。**次元ごとに 1 点ずつ持つ形にしないこと**
- **判断は 2 本に割れています**: `respawnDimension()`（戻る次元）→ `main.ts` が
  そこへ移る → `respawnPlan(world)`（行き先。**ベッドが壊されていたら既定の次元へ落とす**）。
  **2 段になっているのは避けられません** —— 次元を移らないとベッドのマスが読めないためです
  （未読み込みの列では `getVoxel` が AIR を返すので、先に読むと生きているベッドを
  「壊されている」と誤読します）
- **`main.ts` に次元の分岐は 1 つも増えていません**（`test/ui.test.ts` の上限 2 件は据え置き）。
  代わりに routed の見張りへ `respawnDimension(` と `respawnPlan(` を足しました
- **`beds.ts` は `dimensions.ts` を import しません**（生成器を引き連れてきます）。
  文字列で受けて、綴りは `test/beds.test.ts` が `DIMENSIONS` と突き合わせます
- セーブは **`bedDim`（省略可）**。**オーバーワールドなら書かない**ので、
  **オーバーワールドだけで遊んでいる人のセーブは今までと 1 バイトも変わりません**。
  **`bed` の `[x, y, z]` も `version`（1）も変えていません**
- **次元を移る経路が 2 つになりました**（ポータル / リスポーン）。`switchTo()` の第 2 引数に
  `liveState()` を渡し忘れると**残してきた改変・落とし物・かまど・チェストが黙って消える**ので、
  `test/dimensions.test.ts` が `main.ts` の呼び方を数えるようにしました（型では防げません）
- **わざと壊して 4 通りとも赤くなることを確かめました**: 次元を戻す 1 行を消す → 1 件 /
  `liveState()` を `emptyState()` に → 1 件 / 戻り先を常にオーバーワールドに → 4 件 /
  `bedDim` を書かない → 1 件
- 決まりごとは **`RULES-INBOX.md` に 3 件**（`rules/dimensions.md` の地点の行の差し替えと
  「ポータルを通る」への 1 行、`rules/beds.md` のセーブの行の差し替え）。
  確認項目は `REVIEW.md` の「2-4c」に 7 個。**手触りの数値は置いていないので `TUNING.md` は
  触っていません**（不具合の直しで、新しい数値がありません）
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません

### 前の周（2-1 / 2-2 / 2-3 / 2-3b / 2-3c / 2-4a / 2-4b）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージは書かず、撃った側が
  `Shot.damage` に載せる**ので、2-12 の弓矢は撃つ側を足すだけで当たる側が要らない）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c の 8 節が溜まっています**
  （一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **テストの偽陽性を 1 つ踏みました。** 2 つの次元の**同じマス**にベッドを置いて試したので、
   **行き先の次元を間違えても向こうでベッドが見つかり**、「常にオーバーワールドへ戻す」に
   壊しても 4 行の表のうち 2 行が緑のまま通りました（別のマスに置き直して直しました）。
   **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**
2. **試験用の関数が `main.ts` と同じ順でないと、壊れ方を見逃します。** 最初は
   `respawnPlan()` が返した次元をそのまま「行き先」として返していたので、
   **実際に移った次元が間違っていても通りました**（`main.ts` は
   `respawnDimension()` の側へ移ります）
3. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）。
   呼び方を数えるテストでしか止められません
4. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の火球で焼け死にます
5. **相手を先に、ブロックをあとに見ること**（2-4b）。逆だと壁を背にした相手に当たりません
6. **飛ぶモブには `ACCEL_GROUND` を使うこと**（空中の加速 4 では 1 フレームに 0.07 しか戻らない）
7. **`world.seed` をセーブに使わないこと**（ネザーでは塩を混ぜたあとの値）。種は `worldSeed`
8. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）
9. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）
10. **仮の判定は「土台があるか」に掛けないこと。** 成果物に掛けること（3 度踏みました）
11. **わざと壊して、テストが赤くなるか確かめること。** この周は 4 通りとも確認済みです

## 次に取るもの

1. **2-5（エンダーマン）**。テレポートとエンダーパール
   （アイテム ID は **90**。`ROADMAP.md` の予約表を**取る順に上から詰める**決まりで、
   89 = ブレイズロッドまで使用済み）
2. そのあとは並びどおり 2-6（エンダーアイ）→ 2-7 → 2-8 → … → 2-13（ドラゴン）。
   **2-12 の弓矢は、撃つ側（弓とアイテム）を足すだけで当たる側が要りません**
   （2-4b の `Shot` の `owner` と `damage` がそのまま効きます）
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 3 件**）/ `REVIEW.md` の一括確認 /
   `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **ネザーでも寝られます**（本家はベッドが爆発する）。`canSleep()` は時刻だけで決まります。
  **2-4c でネザーのベッドが「本当に使える」ようになった**ので、爆発させる形に寄せるなら
  リスポーン地点の扱いも一緒に考え直すことになります（いまは意図的にそのままです）
- **寝ても時間は「進みません」**（時計だけ飛びます）。精錬・空腹・モブは dt で動きます
- **火球は火を点けません**（本家は当たった所が燃える）。火そのものが無いので、
  入れるならブロックとして足すところからです
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）。手触りの数値なので `TUNING.md` に
- **`F3` の `biome` はネザーでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）。気になるなら `spawnOn` を 1 行足せば絞れます
