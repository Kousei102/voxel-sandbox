# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05）

- `npm test` **3044 件すべて成功**（前の周 3029 から **+15**）・`npm run typecheck` 緑・
  **`npm run build` 緑**（`src/**` を触った周なので走らせました）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていない周です）
- **音の一群は 1 回で緑**でした（羊毛 0.552 < 雪 0.716 < 砂 2.235・低い声 0.162 < 高い声 0.329
  —— どれも `rules/testing.md` の「落ち着いた値」ちょうど）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（**セーブは 1 バイトも増えていません**）
- **`main.ts` 1444 行**（上限 1500・**止まる目安 1450 まで残り 6 行**。この周で +1）
- **アイテム 103 種・`MAX_ITEM_ID` 138・111..255 の空きは 117・1..63 の空きは 9**
  （**この周は ID を 1 個も使っていないので、4 つとも前の周と同じ**）
- **立方体 39 / 非立方体 69**・**モブ 9 種類**（変わらず）

## この周でやったこと（AUTODEV 32 の C・クラウドの無人の周）

`AUTODEV-SPEC.md` が **`状態: 未着手`** だったので、判定表を上から当てて **C（実装）の周**。
**16 番「サボテンに触るとダメージ」を実装しました**（`src/` 5 ファイル・`test/` 4 ファイル）。

**溶岩とまったく同じ形**で、判断は 4 つに割ってあります:

- **「どのブロックが刺さるか」は `blocks.ts`** —— `BlockDef.spiky` と `isSpiky()`。
  `liquid` / `hot` / `falls` と同じ表 1 本で、**`id === CACTUS` はどこにも書いていません**
- **「触っているか」は `player.ts` の `touchingSpikes`** —— `inLava` と同じで事実だけ
- **走査は `physics.ts` の `bodyTouches()`** —— **述語を受け取る幾何**で、サボテンを知りません
- **「どれだけ痛いか」は `vitals.ts`** —— 0.5 秒ごとに 1・死因「サボテン」・
  `cooldown` は渡さない・**下限を作っていないのでサボテンでは死にます**（**9.52 秒**）

**`main.ts` は配線 1 行だけ**（`touchingSpikes: player.touchingSpikes,`）。
**ID は 0 個**なので `ROADMAP.md` は 1 行も触っていません。

**点検（C-2）は別のサブエージェントを立てて閉じました。** そちらは `bodyTouches()` を
一時的に `return true` へ書き換える実験まで走らせ、**4 件が落ちること**を確かめてから
元に戻しています（**常に真を返す実装では通らない**ことの裏。`docs/autodev-log.md`）。

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手が 5 件（3 件以上）・`AUTODEV-SPEC.md` が `状態: 済` なので
→ 次は B（仕様）の周**です。**キューの先頭は 17 番（キノコ 2 種とボウル・キノコシチュー）。**

- **17 番は `main.ts` の行数に当たります。** いま **1444 行で、止まる目安 1450 まで残り 6 行**。
  **仕様を書く前に `main.ts` が何行増えるかを見積もり、6 行に収まらないなら
  先に判断を別ファイルへ出す周として割ること**（下の「人がやること」の判断待ち）。
  16 番は 1 行で済みましたが、**キノコは食べ物とレシピとワールド生成の 3 方向**です。
- **次に取る ID は 139 から**（`ROADMAP.md` の予約表「139..255 予備 117 個」）。
  **17 番は 4 個の見積もり**です（赤キノコ・茶キノコ・ボウル・キノコシチュー）。

## ブラウザで見てほしいところ

**この周は見た目に出るものが 0 行**です（`mesher.ts` も色も形も音も触っていません）。
`npm run shot -- terrain` を 1 枚撮って `Read` で開き、**面の欠けも裏返りも無く、
木・草・遠くの砂浜が前の周と同じ**であることを確かめました（絵が変わらないことが
確かめたいことでした）。**本物のブラウザは撮っていません** —— 絵に出るものが無いためです。

- **この周で出るもの（手触りの判断をお願いします）**: **サボテンに歩いて当たったときの
  痛さ**（0.5 秒ごとに 1 = 秒 2。溶岩のちょうど 1/4）と、**上に立っても痛くないこと**が
  我慢できるか。**本家では上に立つと痛い**のですが、そうするには `CACTUS_BOX` の上面を
  削ることになり、積んだサボテンの継ぎ目に出ます（`TUNING.md` に理由を残しました）。
  **押し付けられ続けると 9.52 秒で死にます**（防具がまだ無いので）。
- 持ち越し（前の周から。まだ誰も見ていません）: **牛を搾る / ミルクを飲んで毒が止まるか** /
  **ミルクバケツの色（`0xeaf2f8`）が羽根・卵・糸・雪玉と見分けられるか** /
  かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
  鉄・金・ダイヤのブロックが材料の山と見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**
  （`style.css` の `.slot .label`）/ クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い /
  牛の胴が寸詰まりに見えないか / 産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 2 件・どちらも `rules/` に据えました）

1. **`rules/vitals.md`**: **`VitalsContext` にフィールドを足すと、`test/vitals.test.ts` の
   `ctx()` を直すだけでは足りません** —— **`test/mobs.test.ts` が `vitals.update()` へ
   オブジェクトを直書きしています**（無敵時間の効きをモブの群れで測っている件）。
   必須で足したので `npm run typecheck` が両方とも落として教えてくれましたが、
   **省略可（`?`）で足していたら黙って `undefined` が流れていました。**
2. **`rules/blocks-shapes.md`**: **「体が触れている」は当たり箱ではなく「マス」で見ること。**
   押し戻しは `contactMin - half - EPS` で止まるので、**当たり箱で見ると 1 度も真に
   なりません。** サボテンの箱は 1/16 細いぶん、押し付けられた体は**マスの中へ
   0.0615 だけ入ります**（テストが **x=2.7615 / 右端 3.0615** を出してから判定しています）。

## 人がやること

- **設計の判断を 1 つお願いします（5 周持ち越し・17 番で効きます）: `main.ts` を 2 つに
  割ってよいか。** いま **1444 行で、止まる目安 1450 まで残り 6 行**です。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 15 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
