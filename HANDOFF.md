# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm run build` は通ります。**`npm test` 2420 件すべて成功**
  （**前の周の 2382 件から +38**。この周が足したぶんです）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1381 行 / 上限 1500**（この周は**配線 1 行だけ**足しました。下を見てください）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— ID は 0 個）
- **`AUTODEV-SPEC.md` は `状態: 済`**、`AUTODEV-QUEUE.md` の未着手は **9 件**
  → **次の周は B（仕様）**
- **`RULES-INBOX.md` に未取り込みが 4 件**（`mobs.md` / `projectiles.md` / `items-survival.md` /
  **この周が足した `inventory-screen.md`**）。**取り込むのは人が居る周**

## この周でやったこと（無人・C の周 1 種類だけ）

**`AUTODEV-SPEC.md` の 3b「掴んで動かしても傷が残る（インベントリ画面の中だけ）」を実装しました。**
`ID は 0 個`、コミット 1 つ。細かい記録は `docs/autodev-log.md` の同じ日付の節です。

- **判断は `durability.ts` に `damageOf()` と `carryWear()` の 2 本だけ。** 運ぶ側
  （`inventory.ts` / `craftscreen.ts`）に `?? 0` も「道具かどうか」も書いていません
- **塞いだ経路 6 つ**: 掴む・置く・入れ替え（`transfer`）/ 数字キー（`swapHotbar`）/
  ドラッグ配分（`release`）/ 閉じるときの返却（`returnSlot`）/ シフトクリック（`quickMove`）/
  `addToSlots()`（`damage` は**既定 0 の省略可**）。逆にクリエイティブの一覧は
  **必ず新品**に固定しました
- **セーブは `SaveData.craftWear`（10 要素・省略可）。** `craft` の 20 要素は 1 つも変えていません
  （`version` も 1 のまま）。`serializeWear()` / `deserializeWear()` は `Slot[]` を取るので
  **36 枠のぶんをそのまま使い回して**います（2 本目は書いていません）

**仕様書から 1 か所だけ外れました。** 仕様書は `main.ts` を「1 行も触らない」としていましたが、
**`craftWear: craft.serializeWear(),` の 1 行**を足しました。これが無いと
`SaveParts.craftWear` を誰も渡さず、**セーブの往復がテストの中だけで成り立って、
実際のゲームでは 1 度も書き出されません。** 足したのは値を貼るだけの配線で、
`main.ts` に判断が戻っていないかの見張りは緑のままです
（`drops.ts` / `chests.ts` / `furnaces.ts` は 1 行も触っていません）。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶん（2 件）:**

- **傷んだ道具を収納へ動かしても、枠の下の細い帯が付いて回るか。** 掴んで置く・
  シフトクリック・数字キー・ドラッグのどれでも消えないこと。画面を閉じて開き直しても残ること
- **クリエイティブの一覧（`E`）から出した道具に帯が出ていないこと。** 傷んだ道具を掴んだまま
  一覧を押す（＝捨てる）→ もう一度押して出し直すと、**新品**で出てくること

**前の周からの持ち越しが 4 件、まだ人の目を通っていません:**

- **道具の耐久値の周のぶん**（`8a780e9`）—— 木のツルハシで石を掘ると帯が出る（減るほど
  短く・赤く）。新品と、棒や土のような減らない物には出ていないこと。使い切ると
  **「木のツルハシ が壊れました」** が 1 行出るか
- **大きいチェストの周のぶん**（`a795564`）—— 2 個並べて開くと **54 枠が 9 列 x 6 段**、
  単体だと **27 枠**、3 個横一列ならどれも 27 枠に戻るか
- **砂・砂利が落ちる周のぶん**（`6209e91`）—— 砂の柱の下を掘ると上が詰まり、
  **穴や浮いた砂が残らないこと**
- **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**と
  **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **9 件**（3 件以上あるので A ではない）、
**`AUTODEV-SPEC.md` は `状態: 済`** → **次は B（仕様）の周**です。

- **キューの先頭は `3c`（落とし物・チェスト・かまどでも傷が残る）。** B の決まりどおり、
  **その 1 件だけコードで数え直してから**仕様書にすること
- **3c は省略可キーが 3 つ要ります**（`drops.ts` の 5 要素・`chests.ts` の 54 要素・
  かまどの 9 要素）。**120 行に収まらなければ、その場で 2 件に割ってキューへ書き戻すこと**
- **B の周は `src/**` と `test/**` を 1 行も触らないこと**（終わりに `git diff -- src test`
  が空であることを確かめる）
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）

人がやること:

- 上の**ブラウザ確認 6 件**（この周の 2 件 + 持ち越し 4 件）
- `RULES-INBOX.md` の **4 件**の取り込み（**`items-survival.md` のぶんは `paths` に
  `src/durability.ts` を足す指示付き**）
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）の揺れは、この周でも 1 回出ました。**
実装を終えたあと**最後まで通したのが 9 回、うち 8 回が緑・1 回が
`2420 件中 1 件失敗（最後のグループ: 音（実際に鳴らして波形で見る））`**でした。

- **退行ではありません。** `git diff -- src/audio.ts src/sfx.ts test/audio.test.ts` は
  **空**です（この周は 3 つとも 1 行も触っていません）。赤が出たら、まずここを見ること
- **1/9 の割合なので、1 回の赤で止まらないこと**（`AUTODEV.md` の停止条件は
  「2 周続けて赤い」です）。もう一度走らせて緑なら、この揺れです
- 直すのは人が居る周（音を聞いてもらう必要があります）

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
