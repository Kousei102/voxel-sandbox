# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-08-31）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm test` / `npm run build` は通ります。**`npm test` 2284 件すべて成功**
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**（クリア導線そのものは done）
- `src/main.ts` は **1372 行 / 上限 1500（余白 128 行）**
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**
- `AUTODEV-QUEUE.md` も `AUTODEV-SPEC.md` も**まだありません** → **次の周は A（棚卸し）**
- **`RULES-INBOX.md` に未取り込みが 2 件**（`mobs.md` と `projectiles.md`）。**取り込むのは人が居る周**

## この周でやったこと（人が居る周。3 件）

### 1. 立方体のブロック ID を 63 → 255 へ（`0c68545`）

**枠を上げたのではなく、詰め方を直しました。** `encodeFace` が id に 6 ビットしか
割いていなかったのが 63 の出どころで、2 枚のマスクには 47 / 62 ビットしか入っていません
でした。**AO の 8 ビットを 2 枚目へ移して id を 8 ビットに**（詳細は `.claude/rules/meshing-render.md`）。

番号の帯を 3 つに引き直しました。**既存 ID は動かせない**ので下 2 帯は凍結です:

- 1..63 持てるブロック（空き 9）/ 64..110 向き違い（満杯）
- **111..255 はブロックとアイテムで 1 本の番号列**（空き 145）。**新しい番号はここから**

速さは変わっていません（`buildChunkMesh` 中央値 1.35 → 1.31ms・三角形数は同じ）。

### 2. `AUTODEV.md` を 3 種類の周に割った（`b9e7bf6`）

**1 周で 6 工程を全部やりません。** A 棚卸し / B 仕様 / C 実装の 3 種類で、
**どれをやるかはファイルの有無だけで決まります**（`AUTODEV.md` の「1. いまはどの周か」）。
**1 タスク = B + C の 2 周。**

### 3. ブレスのテストの不安定さを直した（`0c68c9b`）

番の長さちょうど回していたので、**最後の 1 フレームが次の番**でした。判断は 5Hz で
位相が `Math.random()` 次第なので、6 回に 1 回だけ落ちていました。**`src/` は触っていません。**
12 回連続で緑を確認済み。

## ブラウザで見てほしいところ（見た目に出るもの）

**1 件だけです。** メッシュ化の詰め方を変えたので、

- **面が欠けていないか**（とくに洞窟の奥・水面・ガラスごし）
- **AO の陰影が前と同じか**（壁ぎわ・階段の下）

数値では法線の一致・穴なし・三角形数まで見ていますが、**陰影の出方そのものは目でしか
確かめられません**（`CLAUDE.md` の「この環境では WebGL が動かない」）。

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。** いまは A（棚卸し）です。

人がやること:

- 上の**ブラウザ確認 1 件**
- `RULES-INBOX.md` の 2 件の取り込み
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）はまれに 1 件落ちる**と前の周の引き継ぎにあります。
この周の 12 連続では 1 度も出ていません。**2 回続けて落ちたときだけ退行として扱うこと。**

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
