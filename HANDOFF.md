# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2624 件すべて成功**（前の周 2575 から +49）
- **`npm run build` も通りました**（`src/**` を触った周なので走らせています）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1420 行 / 上限 1500**（この周 **+20 行**。仕様書の上限ちょうど）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 140**（この周は **115 を 1 個**）
- **アイテムは 83 種**（82 → 83）。`MAX_ITEM_ID` は **115**
- **`AUTODEV-SPEC.md` は `状態: 済`** → **次の周は B（仕様）**（下の「次にやること」）
- **`RULES-INBOX.md` は未取り込み 4 件**（この周が書いたもの。取り込むのは人が居る周）

## この周でやったこと（無人・C の周 1 種類だけ）

**シアーズ（アイテム 115）を入れました**（`AUTODEV-QUEUE.md` の 5 番 = `AUTODEV-SPEC.md`）。
鉄 2 個の斜めで作れて、**羊に右クリックすると羊毛が 1〜3 個出て、60 秒は刈れなくなります。**
**刈った羊を倒しても羊毛は落ちません**（殴っても撃っても）。238 回で尽きます。

触ったのは仕様書のとおり 7 ファイル（`items.ts` / `durability.ts` / `crafting.ts` /
`mobs.ts` / `use.ts` / `controls.ts` / `main.ts`）。**`mobrender.ts` も `sfx.ts` も
`storage.ts` も 1 行も触っていません** —— **セーブは 1 バイトも増えていません**
（刈られた状態は `Mob.woolTimer` で、モブは保存しないため。`version` は 1 のまま）。

**実装しながら分かったこと**（次の周が同じ道を通らないために）:

- **`def.drop` を直に読む 2 か所を `dropFor(mob, def)` に寄せました。** 仕様書の警告どおりで、
  `attack()` だけ直すと**弓で撃ったときだけ刈った羊から羊毛が出ます。**
  テストは殴った側と撃った側を**並べて**測っています。
- **右クリックがモブを見る道は新しく作らず、左クリックの式を `mobIsNearer()` として
  出しました**（`controls.ts`）。`main.ts` に距離の比較を書くと、殴れる間合いと
  刈れる間合いが食い違います。
- **既存のテストを 2 つ書き直しました。どちらも緩めていません**（`docs/autodev-log.md` に
  詳しく）: `test/crafting.test.ts` の「2x2 ではバケツは作れない」が `=== null` を
  見ていましたが、**2x2 の斜めはシアーズが取った**ので `?.out !== BUCKET` に。
  `test/durability.test.ts` の `main.ts` の `wearForUse(` は **2 → 3 回**（増やす側）。
- 点検（C-2）は**親が `git diff` を読んで自分でやりました**（この環境では
  サブエージェントの結果が同じ番の中で返らないため。`AUTODEV.md` の C-2 の但し書き）。
  **差し戻し 0 回。**

## ブラウザで見てほしいところ（見た目に出るもの）

**撮って自分の目で見ました**（`docs/browser-shots/creative-shears.png`。7 枚目・撮り方は
同ディレクトリの `README.md`）。**クリエイティブの一覧は 82 → 83 枠**で、末尾は
`… ダイヤの剣 → シアーズ`。**名前は欠けも溢れもなく出ていて、console のエラーは 0 件**でした。
標準の 5 枚（`menu` / `game` / `hud` / `debug` / `inventory`）も撮り直して、
地形・HUD ともに前の周と変わっていないことを確かめています。

**それでも人に見てほしいのは 2 つです:**

1. **刈ったときの手ごたえ。** 音は既にある `"dig"` を羊の声色（x1.25）で鳴らしているだけで、
   **見た目は 1 ドットも変わりません** —— 羊毛が地面に湧く以外に「刈れた」合図がありません。
2. **60 秒という戻り時間**（`TUNING.md`）。本家は「草を食べたら戻る」で時間ではないので、
   ここは目分量です。短いと 1 頭から無限に取れ、長いと**刈るより倒すほうが早い**場面が出ます。

## 人がやること

- **`RULES-INBOX.md` の 4 件の取り込み**（`items-survival.md` の「使って減る」の行 /
  `mobs.md` の新しい節「刈って取る」/ `use.md` の 2 件）。**本文はそのまま書いてあります**
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**は
  まだ直っていません（`#status` は `bottom: 118px` 固定。`style.css:674`）。**`REVIEW.md` に積んであります**
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手は 6 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 済`**
なので → **次は B（仕様）の周**です。

- **`AUTODEV-QUEUE.md` の先頭は「6. 小麦・種・耕地・パン」。** キューにも書いてあるとおり
  **「育つブロック」の仕組み（`crops.ts`）が要る**ので、**120 行に収まらなければ
  その場で 2 件に割り、前半だけを仕様書にすること**（`AUTODEV.md` の B）。
- **取る前にその 1 件だけコードで数え直すこと**（`wheat` / `小麦` / `crops` が
  `src/**` と `test/**` に無いこと）。**全部を数え直すのは A の仕事です。**
- **ID は `ROADMAP.md` の予約表から 116 以降**（111..114 = 剣、**115 = シアーズ**が
  使用済み）。**111 以降はブロックとアイテムで 1 本の番号列**なので、片側だけ見て取らないこと。
- **B の周は `src/**` と `test/**` に 1 行も触らないこと**（完了条件）。

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（緑）。
`audio.ts` / `sfx.ts` には 1 行も触っていないので、**直ったわけではありません。**
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
