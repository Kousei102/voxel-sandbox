# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-21）

- ブランチ **`loop/endgame`**。**`origin/master` より 26 コミット先**。**push していません**
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1392 件すべて成功**（1341 → 1392。飛び道具で 51 件増えました）
- **進行（クリア導線）達成 4 / 13**（4 件とも本物）。
  溶岩 / 黒曜石 / 火打石と打ち金 / ポータルの点火
- ブロック ID **1..63 の空きは 18 個**（64 以降は 103 まで）。アイテム ID は 88 まで
- `src/main.ts` は **1422 行 / 上限 1500**（残り 78 行。**次に増やすときは注意**）

## 直前にやったこと — TASKS 1-8（飛んでいるもの）

**無人のループで取れる範囲が 2-2 まで広がりました**（ユーザーが決定。`a506b99` で
`TASKS.md` の「⛔ ここで止まる」を 2-2 と 2-3 のあいだへ移動）。

- `projectiles.ts` = **火球・矢・エンダーアイ・ブレスが全部乗る 1 本の飛び方**。
  違うのは `PROJECTILE_KINDS` の表の値だけ（`drops.ts` ↔ `droprender.ts` と同じ対の形）
- **当たったときに何が起きるかは書いていません。** ダメージの数値は手応えの判断
  （`LOOP.md` の停止条件 4）なので、**当たったマスを `onHitBlock` で外へ渡すだけ**。
  誰に当たったか（モブ・プレイヤー）も未実装 —— **ブレイズ（2-4）と弓矢（2-12）の周に、
  ユーザーと数値を決めてから足すこと**
- 当たり判定は **0.2m ごとに刻みます**（矢は 40m/s なので、刻まないと重いフレームで壁を抜ける）
- `N` キーで 4 種類を順ぐりに 1 つ撃てます（`M` のモブ出しと同じデバッグ用）
- 決まりごとは新しい **`.claude/rules/projectiles.md`**（`CLAUDE.md` の層 2 の表にも追加）

## 踏んだ穴（同じことを繰り返さないために）

1. **仮の判定は「土台があるか」に掛けないこと。3 度目です。**
   「投げたエンダーアイが要塞の方を向く」の probe が
   `sourceHas("src/projectiles.ts", "export")` だったので、**器を作った瞬間に
   達成 5 / 13 へ化けました**（エンダーアイは 1 個も投げられないのに）。
   `structures.ts`・`dimensions.ts` に続いて同じ形です。
   **成果物に掛けること** —— 直したあとは「エンダーアイのアイテムが無い」と出ます
2. **「壁より手前で止まったか」では、めり込みを捕まえられません。**
   止める 1 行を消しても通りました（矢の中心は壁より手前のままなので）。
   **箱の前面がマスの面より手前か**を見ること
3. **`Float32` の頂点では、体積の一致を絶対値で見ないこと。** 落とし物（半径 0.125）は
   1e-9 に収まりますが、0.1 や 0.35 は収まりません。**相対で見ること**
4. **試験場（`Arena`）は y < 0 を岩盤で塞いでいます。** 落として奈落を試そうとしても
   届きません。`test/drops.test.ts` と同じく、**奈落の下に直に置く**こと
5. **音の波形テストがまた 1 度だけ落ちました。** 続けて 2 回走らせると通ったので
   退行としては扱っていません（`rules/audio.md`。前の周でも同じことが起きています）

## 未確認のもの（ユーザーにブラウザで見てもらう）

**`REVIEW.md` の「1-8. 飛んでいるもの」7 項目だけです。**

`N` キーを押すと視線の向きへ 1 つ飛びます（押すたびに 火球 → 矢 → エンダーアイ → ブレス）。
`F3` に `shots`（いま飛んでいる数）が出ます。**当たっても何も起きません**（まだ効果が無い）。

## 次にやること

**無人のループで 2-2 まで。** `TASKS.md` の「⛔ ここで止まる」がその下にあります。

1. **2-1. ネザーの地形** — ネザーラック 45 / ソウルサンド 46 / グロウストーン 47 /
   溶岩海 / 天井の岩盤。**生成器ができたら `DIMENSIONS` に 1 行足す**（`salt` は 0 以外）
2. **2-2. ネザーポータルで往復** — **ここで `portals.ts` を `main.ts` に配線します**。
   プレイヤーの位置を飛ばすので、`TASKS.md` の 4 つ（立てる場所へ出す・`resetFootprint()` を
   通す・向こう側の枠を作る・往復で同じ枠に戻る）をヘッドレスで固定してからコミットすること

**`main.ts` の残りは 78 行です。** 2-2 の配線で足りなくなりそうなら、
判断を `portals.ts` 側へ寄せること（上限に届いたら `LOOP.md` の停止条件 6）。
