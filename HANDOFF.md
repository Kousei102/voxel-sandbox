# 引き継ぎメモ（2026-08-19 時点）

**このファイルは消さずに、セッションごと（ループなら 1 周ごと）に丸ごと書き直してください。**

- **中身は毎回作り直す。** 古い状況を残したまま追記すると、どれが今の話か分からなくなります。
  前回の内容を引き写さず、**必ず現物（`git fetch` / `npm test`）で裏を取ってから**書くこと
- **ここに書くのは「いまの状況」だけ。** 恒久的な決まりごとは `CLAUDE.md`、
  次に何を入れるかは `ROADMAP.md` と `TASKS.md` に移してください
- **踏んだ失敗は「4. 進め方」に足す。** 何度も踏むものは `CLAUDE.md` へ格上げします
- **まず `CLAUDE.md` を読んでください。** ループを回すなら次に `LOOP.md`

---

## 1. いまの状態

**作業ツリーはきれいです。いるのは `loop/endgame` ブランチです。**

```
git fetch && git log --oneline origin/master..master   # master の未 push
git log --oneline master..loop/endgame                  # master に入れていないぶん
```

- `npm test` **1144 件成功** / `npm run typecheck` / `npm run build` 通過
- **進行（クリア導線）: 達成 1 / 13**（`npm test` が毎回出します）
- **ブロック ID 1..63 の空きは 21 個。** アイテム ID は 83 まで使用済み
- **`master` は push 済み**（ベッドまで公開サイトに出ています）
- **`loop/endgame` に未マージのぶんが 5 つ**（ベッド以降のループ 3 周ぶん）

```
溶岩のダメージと炎上 (TASKS 1-2)                                      ← loop/endgame（この周）
溶岩の直し: 液体を isLiquid() 1 本に寄せ、フォグを掛ける (1-1b)  02b7101
引き継ぎメモと LOOP.md: 1 周目で分かった穴を埋める              5dff621
溶岩ブロック (TASKS 1-1)                                        a2db64c
引き継ぎメモ: ベッド確認済み・ループの下ごしらえ                 3c12ff8
ループの下ごしらえ: 進行テスト・セーブの回帰・main.ts のガード   5d543ac  ← master 済み
```

### 溶岩（1-1 / 1-1b。**ユーザーがブラウザで確認済み・全項目 OK**）

ID 42。水とほとんど同じ作りで、違うのは自分で光ること（`emission` 15）と
フォグが濃いこと。`worldgen.ts` の `LAVA_LEVEL`(10) 以下の洞窟に溜まります。

### 1-2 溶岩のダメージと炎上（**ブラウザ未確認**。確認項目は `REVIEW.md` に 7 件）

- 判断は `vitals.ts` の `updateFire`。**数値は Minecraft のまま**（触れた瞬間に 4 →
  0.5 秒ごとに 4 / 出てからは 15 秒間 毎秒 1）。**防具が無いので、かすっただけで
  合計 19 入ります**（体力 20）。実測: 浸かり続けると 2.02 秒で死亡
- **手触りはユーザーのものです**（`LOOP.md` の停止条件 4）。`REVIEW.md` の 4 番で
  「溶岩から出られずに必ず死ぬか」を聞いています。答え次第で炎上 15 秒を短くします
- モブも溶岩で焼けます（**敵味方の区別なし**。日光は敵対だけ）。実測 2.0 秒
- **`isHotLiquid()` を足しました。** 焼けるかどうかも表 1 本（ネザーの溶岩海が同じ ID）

### 1-2 のついでに直った 3 つ（**どれも 1-1 で埋め込んだ穴**）

`isLiquid()` を `player.ts` / `mobs.ts` / `drops.ts` / `beds.ts` へ広げた副産物です。

| 直る前 | 原因 |
| --- | --- |
| 溶岩の中にモブが湧く | 湧きの判定が `=== WATER` だった |
| 溶岩の上にベッドが置ける | `beds.ts` の `freeForBed` が `!== WATER` だった |
| 「溶岩」というアイテムが 1 個増えていた | `items.ts` が水だけを弾いていた |

**どれも 1-1 の時点でテストが素通りしていました。** 液体を足したら、
`grep -n "WATER" src/*.ts` を 1 回やること（いまは `test/blocks.test.ts` が見張ります）。

---

## 2. 次にやること

**まず `REVIEW.md` の 1-2 をユーザーに見てもらうこと。**

次は **1-2b（黒曜石とバケツ）**。**ユーザーが「バケツを入れる」と決めました**（2026-08-19）。

決めることになった理由:

> **黒曜石は「水と溶岩が触れる」ことでしかできず、いまのワールドでは
> 水（y≤40）と溶岩（y≤10）が触れる場所がありません。**
> つまり**バケツが無いと黒曜石は 1 個も手に入らず、ネザーへ行けません。**

アイテム ID（バケツ・水バケツ・溶岩バケツ）は `ROADMAP.md` の予約表に足してあります。
**スタック上限は 1。** 大きいので `TASKS.md` で 2 つに割ってあります（バケツ / 黒曜石）。

**1-2b は 1-2 の確認を待ってから着手すること** —— 同じ溶岩まわりの見た目に続けて
触るので、落ちたときに切り分けられなくなります（`LOOP.md` の「見た目・音に触れたとき」）。

いまは `/loop` を**ユーザー同席で**回して、タスクの粒度と停止条件が機能するかを
確かめる段階です。5〜10 周まわして形が固まったら Routines へ移します。

### 周ごとに分かったこと

- **1 周目**: `LOOP.md` の完了条件に「熱い場所を触ったら bench」が抜けていた（足した）
- **2 周目（1-1b）**: 同じ判断を 3 か所に写していたのが原因。**個別に直さず 1 本に寄せた**
- **3 周目（1-2）**: **1 本に寄せた判断は、寄せた先を増やすまで穴が残る。**
  1-1b で 3 か所を直したのに、`player` / `mobs` / `drops` / `beds` の 4 か所が
  残っていた（上の表）。**寄せたら `grep` で残りを数えること**

---

## 3. push について

**ユーザーに確認を取ってから push すること。** 公開サイトが差し替わる外向きの操作です。
push すると `npm ci` → `npm test` → `npm run build` を通ってから GitHub Pages
（https://kousei102.github.io/voxel-sandbox/）に出ます。**テストが落ちればデプロイされません。**

押したら `gh run watch <id> --exit-status` で Actions を見届けること。

**ループは push しません**（`LOOP.md` の禁じ手）。`loop/endgame` にコミットを積むだけで、
`master` へのマージと push はユーザーが確認してからやります。

**フレーム時間の p99 判定（`world.test.ts`）はマシンが混むと落ちます。**
実測: テストを 2 本同時に走らせると p99 が 170ms まで跳ねました（上限 39ms）。
落ちたら**中身の退行と決めつけず、まず再実行を試すこと。**

---

## 4. 進め方（このプロジェクト特有）

**この環境では WebGL が動かず、音も鳴らせません**（理由と切り分けは `CLAUDE.md`）。なので:

- **見た目・音を変える変更は、1 まとまりごとに止めてユーザーに見て（聞いて）もらうこと。**
  ループを回している間は `REVIEW.md` に積み、まとめて見てもらいます
- 確認をお願いするときは、**具体的な確認項目を 4〜7 個の箇条書き**で。番号を振っておくと
  「5 だけ違う」と返ってきて、すぐ切り分けられます
- **直す前に原因を特定すること。** 2026-08-07、「メニューを開くと視点が回る」に対し
  ブラウザの mousemove 説を採って対策を 2 つ入れましたが、実際の原因は
  `main.ts` の `else { player.yaw += dt * 0.05 }` で、**`grep -n "yaw" src/main.ts` 1 回で
  分かる話**でした。入れた 2 つは丸ごと revert しています
- **判断を 1 本に寄せたら、寄せ残りを `grep` で数えること。** 2026-08-19、液体の判定を
  `isLiquid()` に寄せた翌周に、`player` / `mobs` / `drops` / `beds` の 4 か所が
  `=== WATER` のまま残っていました（溶岩にモブが湧く／ベッドが置ける）。
  **1 本に寄せた時点では、寄せた先しか直っていません。**
- **書いたガードが本当に効くか、必ず試すこと。** 2026-08-18、`main.ts` の
  「次元の分岐を散らさない」ガードの正規表現が `dimension === "nether"` を
  拾えていませんでした（語が `===` の**後ろ**にあるため）。仕込んだサンプルで
  検出 0 件と出て気付きました。**ガードは書いた時点では「効いていない」ほうに倒れます**
- **モブのテストでプレイヤーを遠くへ置かないこと。** 2026-08-19、溶岩で焼ける試験で
  プレイヤーを 200 ブロック先に置いたら、焼ける前に `DESPAWN_DISTANCE` で消えて
  「0.0 秒で焼け死んだ」と出ました。**まず「浸かった」証拠を取ってから見ること**
- **確認項目に「アプリがもともと持っていない挙動」を書かないこと**
- **CSS の位置は直に数値で書かないこと**（`:root` の変数と `calc()` に寄せる）
- **画面の寸法は数に落とせます。** 文字がスロットに重なるかは CSS の寸法から
  境目（138px）を出してテストにしました。**目で見るまで気付けない類は、まず数にできないか考えること**
- **テストの主張も裏を取ってから書くこと。**「咀嚼はいちばんこもった音」と書いたら
  羊毛の足音のほうがこもっていて落ちました。実装ではなく主張のほうが間違っていました
- **同じ規則を 2 か所に書かないこと**
- **ブラウザに取られるキーを確かめること**（Ctrl+Q は環境によっては窓を閉じます）

### デバッグに使えるもの

- `M` キー: 照準の先にモブを 1 体（**湧きの条件に関わらず**出せます）。種類はランダム
- `F3`: モブ数・落ちているアイテム・かまど・チェスト・空腹・**浸かっている液体と炎上**
- 開発サーバーは `npm run dev`（5173 が埋まっていると 5174）

### 計測するとき

**`npm run bench` は 1 回では当てになりません。** 同じコードで `generateChunk` が
0.93〜1.46ms に振れます。疑わしいときは `git worktree add <絶対パス> HEAD` で比較対象を
用意し、`node_modules` を symlink して**同じ条件で交互に測ってください**。

```
world.update    中央 3.4ms / 最悪 10〜17ms（GC 込み）
モブ 40 体      中央 0.048ms / p99 0.20ms
落とし物 128 個 中央 0.10ms / p99 1.3〜3.7ms（GC 込み）
npm test        13 秒前後（1144 項目）
```
