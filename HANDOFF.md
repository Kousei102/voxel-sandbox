# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-03）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は無人の C（実装）の周**でした。**前の周までの手元のコミット 2 つ
  （`84d3b21` と `f152cd8`）は push 済みで、`.claude/rules/` は取り込み後のものを読めました**
- `npm run typecheck` / **`npm test` 2818 件すべて成功**（2801 → **+17 件**）/
  `npm run build` も通る
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **`src/main.ts` は 1450 行 / 上限 1500**（**この周は 1 行も触っていません**。
  停止条件 3 の線にちょうど乗ったままです）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 130**（**この周は 125 を 1 個使用**）
- **アイテムは 90 種**、`MAX_ITEM_ID` は **125**。`SaveData` は `version` 1 のまま（キーも増えていません）
- **`AUTODEV-SPEC.md` は `状態: 済`**、`AUTODEV-QUEUE.md` の未着手は **5 件**
  → **次の周は B（仕様）**
- **`RULES-INBOX.md` は未取り込み 2 件**（この周が書きました。下の「人がやること」）
- `REVIEW.md` の未着手は 0 件（66 行）

## この周でやったこと — AUTODEV 15「パン（小麦 3 → パン）と食べること」

**`AUTODEV-SPEC.md` のとおりに入れました。差し戻し 0 回・触った `src/` は 2 ファイル 6 行です。**

- `items.ts`: `BREAD = 125` の定数 / `MAX_ITEM_ID` を差し替え / `item({...})` 1 行 /
  `FOODS` に 1 行（**空腹 +5 / 満腹度 +6 / 毒なし**。本家の値）
- `crafting.ts`: `RECIPES` に 1 行（**`["WWW"]` = 小麦 3 個の横一列 → パン 1 個**）と import 2 語

**`main.ts` / `use.ts` / `vitals.ts` / `smelting.ts` / `blocks.ts` / `craftscreen.ts` /
`inventoryui.ts` は 1 行も触っていません**（仕様書の「触らないファイル」どおり）。
**食べる経路も右クリックの振り分けもクリエイティブ一覧も、もう `foodOf()` 1 本を通っている**ので、
アイテムの名前をどこにも書かずに済みました。**これがこの周でいちばん確かめたかったことです。**

テストは **+17 件**（`test/crafting.test.ts` 5 / `test/blocks.test.ts` 4 /
`test/vitals.test.ts` 5 / `test/use.test.ts` 3）。**既存の判定は 1 つもゆるめていません** ——
書き換えたのは共有帯の 1 行だけで、**11 個 → 12 個へ数え直したうえで
`sharedItems[11] === BREAD` を足して強くしてあります**。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は本物のブラウザで撮って自分で見ました**（`docs/browser-shots/creative-bread.png`・12 枚目）。
**クリエイティブの一覧は枠 90 個で、末尾は 小麦 → パン**。**パンの色 `0xc49a5e`
（`rgb(196,154,94)`）は、隣の小麦 `rgb(216,194,106)` とは並べても分かれます**（小麦が黄・パンが茶）。
console のエラーは 0 件でした。`npm run shot -- crops` も撮り直して、**畑の見た目が
変わっていないこと**（描画は 1 行も触っていないので、変わらないのが正解）を確かめています。

それでも**人にしか決められないもの**が 1 つ増えました:

1. **パンの茶色が、板・原木・棒の茶色と近い系統です。** 隣の小麦とは分かれますが、
   **一覧の中で目立つかどうか**は目で見ないと分かりません（`E` を押して末尾まで送ってください）
2. 前の周からの持ち越し: **苗の緑（`0x6f8f3f`）が草原に紛れる**件と、
   **`GROW_SECONDS` 180 秒が待てる長さか**（どちらも `TUNING.md`）
3. 前の周からの持ち越し: **`#status`（オートセーブの帯）の位置**。人が直したぶんは
   まだブラウザで見てもらえていません（`E` と `Esc` を開いて、狭い窓でも試してください）

## 人がやること

- **`RULES-INBOX.md` の 2 件を取り込むこと**（`.claude/rules/` は無人の周が触ると
  確認待ちで止まるため）。当て先は **`vitals.md`**（食べ物を足すのに要るのは `items.ts` の
  2 行だけ・食べ物どうしの強さ）と **`items-survival.md`**（末尾の「肉は食べられます」の段落。
  **食べ物 = 肉だったころの文で、いまと食い違います**。差し替える本文をそのまま書いてあります）
- **上の「ブラウザで見てほしいところ」の 1〜3**
- **パンの数値でよいか**（空腹 5 / 満腹度 6 と小麦 3 → パン 1 は本家のまま。
  **かまど無しで作れる中では一番強い**ので、焼き豚 8 / 12.8 の立場は保っています）
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **5 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 済`**
なので → **次は B（仕様）の周**です。**キューの先頭は「7. 鶏（羽根・卵・生鶏肉・焼き鳥）」。**

- **B の周は `src/**` と `test/**` に 1 行も差分を作らないこと**（完了条件）。
  **先頭の 1 件だけをコードで数え直してから**仕様書にすること（もう入っているかもしれません）
- **7 番からは見た目（`mobmesh.ts` / `mobrender.ts`）が要ります。** ここまでの 6 周は
  判断だけで足りましたが、**鶏は新しい「確かめられないもの」を足す周**です ——
  仕様書に **`unverifiable-pair` スキルを名指しで渡すこと**（`AUTODEV.md` の B の 4）。
  **ブラウザで撮れるので C-3 で自分の目で見られます**（`docs/browser-shots/README.md`）
- **ID は `ROADMAP.md` の予約表から。次の空きは 126**（**125 = パンまで使用済み**。
  111 以降はブロックとアイテムで 1 本の番号列なので、**片側だけ見て取らないこと**）
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **サブエージェントはこの周も使っていません。** `AUTODEV.md` の C-1 は「同じ番の中で
  返らないなら自分で実装すること」で、前の 7 周とも背景で起動しました。
  **点検（C-2）も親が `git diff` を全部読むこと**（飛ばすより確実です）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）
- **`main.ts` は 1450 行**で、停止条件 3（1450 行超）の線にちょうど乗っています。
  **1 行でも足したくなったら、まず判断を別ファイルへ出すこと**

**ブラウザの撮り方**（この周に実際に動かしたもの）: `npm run build` →
`(npx --no-install http-server dist -p 8080 --silent &)` → `node tools/browsershot.mjs`（定番 5 枚）。
**クリエイティブの一覧を撮るスクリプトはリポジトリに置いていません**（そのつど書く決まり。
書き方は `docs/browser-shots/README.md` の「撮り方で分かったこと」）。
**`npm run shot -- crops` は GPU もブラウザも要りません**（1〜2 秒）。

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に 1 件だけ赤 →
走らせ直したら緑。**この周は 4 回とも緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
