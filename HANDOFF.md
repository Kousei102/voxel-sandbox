# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-21）

- ブランチ **`loop/endgame`**。**`origin/master` より 23 コミット先**。**push していません**
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1286 件すべて成功**
- **進行（クリア導線）達成 4 / 13**（4 件とも本物）。
  溶岩 / 黒曜石 / 火打石と打ち金 / ポータルの点火
- ブロック ID **1..63 の空きは 18 個**（64 以降は 103 まで）。アイテム ID は 88 まで
- `src/main.ts` は **1330 行 / 上限 1500**

## ⛔ ループはここで止まりました

**無人で回す範囲（1-4 → 1-5 → 1-7）を 3 周で終えました。** `TASKS.md` の
「⛔ ここで止まる」に当たったので、`LOOP.md` の停止条件 7 で止めています。
**ここから先（1-6 の次元、1-8 の投射物）はユーザーと同席で取る**取り決めです。

## 3 周でやったこと

### 1 周目 — TASKS 1-4（`portals.ts`）`543b55f`

- ブロック 49（X 向き）/ 103（Z 向き）。`getVoxel` / `setVoxel` の 2 つだけを受ける判断ファイル
- 枠は Minecraft と同じ **内側 2..21 x 3..21、四隅は要らない**
- 何が火種かは `items.ts` の **`isFireStarter()`**（表 1 本）
- **`main.ts` には配線していません。** 右クリックで点火する操作は 1-6 で次元と一緒に

### 2 周目 — TASKS 1-5（生成器の注入）`f0427d4`

- `World` は **`ChunkSource`**（`seed` と `generateChunk` だけ）を外から受け取る。
  **`world.ts` から `worldgen.ts` の import が消えた**（テストが見張っている）
- バイオームは載せなかった（オーバーワールドにしか無い話）。F3 用に `main.ts` が `WorldGen` を持つ
- 見張りが読むソースを **`arena.ts` の `sourceOf()`** に 1 本化（写しが 5 か所あった）

### 3 周目 — TASKS 1-7（構造物の器）`2989a41`

- `structures.ts` = `placementsFor()`（列挙）+ `stampPlacements()`（書き込み）。純粋
- **列挙は列のキャッシュに持つ**（毎チャンク列挙すると地面を測る回数が 8 倍）
- `worldgen.ts` の `STRUCTURES` は**まだ空**。最初の 1 個は 2-3 のネザー要塞

## 踏んだ穴（同じことを繰り返さないために）

1. **立方体でないブロック（`isProp`）は半透明にできません。** `buildProps()` は
   不透明側の `Builder` しか渡されないので `translucent: true` は**黙って無視されます**
   （`rules/meshing-render.md`）。ポータルは不透明の紫板にしました
2. **ソースを見る判定はコメントを落としてから。3 周とも踏みました。**
   `portals.ts` の「次元の話は 1-6 でやる」/ `world.ts` の「中で `new WorldGen()` していると」/
   `structures.ts` の「ネザー要塞・要塞・…が全部これに乗ります」が、
   それぞれ自分のガードや進行テストに当たりました。**`arena.ts` の `sourceOf()` を通せば起きません**
3. **仮の判定は「土台があるか」に掛けないこと。** `structures.ts` を作った瞬間に
   「ネザー要塞が生成される」が**要塞 0 個のまま達成へ変わりました**。
   建つものの名前で見る形に直してあります（`rules/testing.md` と進行テストの冒頭に記載）
4. **音の波形テストが 1 度だけ落ちました**（「cutoff が低い材質ほどこもって鳴る」）。
   続けて 2 回走らせると同じ数値で通ったので退行としては扱っていません。
   **落ちたらまずもう一度走らせること**（`rules/audio.md`）

## 未確認のもの（ユーザーにブラウザで見てもらう）

**`REVIEW.md` の「1-4. ネザーポータルの面」5 項目だけです。** 見た目だけの確認で、
**まだ火は点けられません**（クリエイティブの一覧から置いて見てもらう形）。

## 次にやること（**ユーザーと同席で**）

**1-6（`dimensions.ts` — 次元の切り替えとセーブ）。Phase 1 で一番大きい。**

- **閉じないと思ったら迷わず分割すること**（`LOOP.md` の 2.）
- `SaveData` に**省略可の新しいキー**（例 `dims`）。オーバーワールドは今の位置のまま。
  **`version` は 1 のまま**
- **`main.ts` に次元の分岐を散らさない**（`test/ui.test.ts` が上限 2 で見張っている）
- **1-4 で作った `portals.ts` の配線はここでやる**（右クリックで点火 → 通ると移動）
- 土台はもう揃っています: 枠の判定（1-4）・生成器の差し替え（1-5）・構造物の器（1-7）
