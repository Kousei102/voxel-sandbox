# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-14・AUTODEV 82 のあと）

- `npm test` **3582 件すべて成功**（**±0**。この周は `src/**` も `test/**` も触っていません）。
  `npm run typecheck` 緑。**音は 1 回目から緑**（羊毛 0.552 < 雪 0.716 < 砂 2.235）
- **`npm run build` と `npm run bench` は走らせていません**（B の周は `src/**` を触らないため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1450 行 / 上限 1500**（±0。1 文字も開いていません）
- **アイテム 127 種・`MAX_ITEM_ID` 165・111..255 の空きは 90**（±0）・
  **1..63 の空きは 9**（±0。55..63）・**レシピ 66 本**（±0）
- **立方体 41 / 非立方体 81**（±0）・**モブ 10 種類**（敵対 5）・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 未着手`（111 行）**・**`AUTODEV-QUEUE.md` 95 行（未着手 7 件）**

## この周でやったこと（AUTODEV 82 の B・クラウドの無人の周）

**判定表は 2 行目**（未着手 7 件で 1 行目は外れ・`AUTODEV-SPEC.md` が `状態: 済`）で
**B（仕様）の周**。`AUTODEV-QUEUE.md` の先頭 **31「ネザーレンガと石レンガのハーフ」**を
`AUTODEV-SPEC.md` にしました。**差分は `AUTODEV-SPEC.md` と `HANDOFF.md` の 2 つだけです**
（B の完了条件どおり `src/**` は 1 文字も触っていません）。

**取る前にコードで数え直しました**（`AUTODEV.md` の B の但し書き）:
`blocks.ts` のハーフは **石 28 / 丸石 29 / 板 30 / 砂岩 31 の 4 材質だけ**で、
上付きは 64..67。**ネザーレンガ（48）と石レンガ（53）のハーフはどちらもありません** ——
キューの行はそのまま生きていました。

### 仕様で決めたこと（C の周はこれを全文どおりにやること）

- **ID 4 個**: 大元 **55 ネザーレンガハーフ / 56 石レンガハーフ**（低帯。**アイテムになる**）と、
  上付き **166 / 167**（共有帯。`variantOf` があるのでアイテムを持たない）。
  **上付きが 64..110 でないのは、あの帯が満杯で凍結だから**です（`rules/blocks-shapes.md`）
- **`src/items.ts` は 0 行**。大元は低帯で `variantOf` が `AIR` なので
  ブロック → アイテムの for が作り、上付きは既定の `baseBlock()` が大元を落とします。
  **`MAX_ITEM_ID` は 165 のまま**（伸ばすと空のアイテムがクリエイティブに 2 つ並びます）
- **`blocks.ts` の `slabPair()` を 2 回・`crafting.ts` の `slabRecipe()` を 2 回呼ぶだけ**で、
  **どちらの関数も中身は 1 文字も書き換えないこと**（既存 4 材質の形がその場で動きます）
- **数え直す判定が 2 つあります**（ゆるめるのではありません）:
  `test/blocks.test.ts` の「111..255 の空きは 90」→ **88**、
  `test/crafting.test.ts` の「レシピは 66 本」→ **68 本**
- **階段は取らないこと** —— `STAIR_STATES` が 8 なので**材質 1 つで共有帯 7 個**です（別の周）

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。

- **仕様書を全文そのまま**実装すること（要約しない）。使うスキルは **`add-block`**
- **先に引いて読むこと**: `rules/blocks-shapes.md` / `rules/items-survival.md` / `rules/testing.md`
- **`npm run shot -- slabs` の場面を `tools/shot.ts` に足して、`Read` で開いて見ること**
  （ハーフは自然に湧かないので、既存の絵には 1 枚も写りません）

## ブラウザで見てほしいところ

**この周は絵を 1 枚も撮っていません**（`src/**` を触らない B の周で、見た目は 1 ドットも
変わっていないため）。前の周に撮った `shots/grown.png`（育った木）はそのまま有効です。

- **苗木（30b）**: **180 秒が待てる長さか**と、**育った瞬間に木がぱっと出るのが唐突でないか**
- **⚠ 一覧の 6 文字の名前（「オークの苗木」）が 2 行に折れて枠の絵に被る件**
  （`style.css` の `.slot .label`）。**人の判断待ち**なので直していません。
  **31 が入ると「ネザーレンガハーフ」（9 文字）でもっと長くなります**
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）
- **道具の修理（28）**: 傷んだ道具 2 本が「新品に戻る」のが気前よすぎないか（本家の式）
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか /
  革 24 枚（＝牛 24 頭）で一式が見合うか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）
- **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` に 1 行も足していません。** 落とし穴を踏んでいないためです ——
上付きハーフを共有帯から取る理由も、`variantOf` がアイテムを作らせないことも、
**`rules/blocks-shapes.md` の「ID の枠の細かい話」に既に書いてありました**
（`grep -l '"src/blocks.ts"' rules/*.md` で引いて読みました）。
**`RULES-INBOX.md` にも 1 行も足していません**（層 3 の話は 0 件）。

## 人がやること

- **⚠ 設計の判断（54 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**で、**`AUTODEV.md` の停止条件 2 にちょうど並んでいます**（上限 1500）。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**・
  最低 7 行）。**キューの上 4 件（31..34）はどれも `main.ts` 0 行の見込み**なので、
  判断が下りなくても 4 周は止まりません。`test/ui.test.ts` の 40 件の見張りが
  動かせない事情は変わっていません
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（足すならそれだけで 1 周）
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周も 1 回目から緑**でしたが、
  前に 7 回中 3 回赤い周がありました。**直すなら測り方**（窓を長く取る・複数回鳴らして
  中央値を見る）で、**判定はゆるめないこと**（`rules/testing.md`）。
  **キューに足すのは A の周の仕事**なので、ループからは足していません
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 82 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押して `#creative` の
`scrollTop` を末尾まで送ってから撮ること**（`docs/browser-shots/README.md` の末尾）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木と同じで、**`tools/shot.ts` に場面を 1 つ
足すのがいちばん安い**（1〜2 秒で撮れて、`browsershot.mjs` を書き換えずに済みます）。
**次の周のハーフ 2 材質もこれです。** **時間が経たないと見えないもの**（育つ・燃える・
溶ける）は、**その場で `update()` を回してから撮ること**。
