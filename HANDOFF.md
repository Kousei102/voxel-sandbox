# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-25）

> **⚠ この周は push できていません。** GitHub 側の権限（403）で、`git push` も API 越しの
> 書き込みも弾かれました。**理由と直し方は `TASKS.md` の「止まっているもの」**に書いてあります。
> コミットは使い捨てのコンテナの中にあるだけなので、**ユーザーへ送った patch が正です**
> （`git am` で当たります）。次の周は**まず push できるか確かめてから**始めてください。

- ブランチ **`loop/endgame`**。**`origin/master` より 34 コミット先**（`origin/loop/endgame`
  より 2 コミット先。**push は 403 で弾かれました**）。
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1648 件すべて成功**（1630 → 1648。この周の 18 件は次元ごとの空）
- **進行（クリア導線）達成 6 / 13**（6 件とも本物）。**この周は進行を動かしていません**
  —— 2-3b は見た目のタスクで、進行の 13 項目に対応するものがありません
- ブロック ID **1..63 の空きは 14 個**（64 以降は 103 まで）。アイテム ID は 88 まで
- `src/main.ts` は **1352 行 / 上限 1500**（余白 148 行。この周で +3 行）

## 直前にやったこと — TASKS 2-3b（次元ごとの空）

**ネザーの空がオーバーワールドのまま（描画距離の先に青空と星）だったのを直しました。**

- 表は **`daynight.ts` の `SKY_STYLES`** 1 か所。次元 → `fixed`（空の 3 色 + tint + 明るさ。
  **null なら今までどおり時刻から作る**）と、太陽・月・星を出すか
- ネザー = 赤黒い / 天体なし / 明るさ 1 固定、エンド = 紫 / 星だけ / 0.7。
  **エンドの行も先に置いた**ので、2-10 は表を触らずに済みます
- **`sky.ts` は 1 行も変えていません。GLSL も触っていません。** 出さない天体は
  **不透明度 0** として伝わり、もともとある `place()` と `stars.visible` が隠します
  （**`sky.ts` に次元の分岐を書き始めないこと** —— 書いた瞬間、空の見え方が
  ブラウザを開くまで確かめられないものに戻ります）
- `main.ts` は `startWorld()` で **`dayNight.setDimension(dimension)` を 1 行**呼ぶだけ。
  次元の分岐は増えていません（`test/ui.test.ts` の routed に足して見張っています）
- **`daynight.ts` は `dimensions.ts` を import しません**（あちらは生成器を引き連れてくるので、
  時刻の判断だけのファイルが重くなります）。キーは文字列で、綴りのずれは
  `test/daynight.test.ts` が `DIMENSIONS` と突き合わせます。**表に無い次元は
  オーバーワールドの空**（`dimensions.ts` の落とし先と同じ）
- 決まりごと: `.claude/rules/lighting.md` の「次元ごとの空」/ `rules/dimensions.md` に 1 行。
  数値は `TUNING.md` に 3 行（全部「目分量」）。確認項目は `REVIEW.md` の「2-3b」に 6 個
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません

### 前の周（2-1 / 2-2 / 2-3 / 2-3c）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）。決まりごとは `rules/dimensions.md` の「ポータルを通る」
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）。出した先は
  `session.ts` / `debugtext.ts` / `debugspawn.ts` と、`sfx.ts` / `daynight.ts` /
  `placing.ts` / `vitals.ts` / `inventory.ts` の中
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b の 5 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **`world.seed` をセーブに使わないこと。** ネザーでは `World` が持つのは塩を混ぜた
   あとの値なので、そのまま保存すると**次に開いたワールドの地形が丸ごと別物**になります。
   種は `main.ts` の `worldSeed`（`startWorld()` が覚える）
2. **`startWorld()` で `beds.clear()` しないこと。** 次元を移るたびにリスポーン地点が
   消えていました。消してよいのは「別のワールドを始める」2 か所だけ
3. **`PortalGate.step()` は 1 フレームに 1 回。** dt を二重に数えて待ち時間が半分になっていました
4. **出る場所は「空気だけの一番低い所」を採ること。** 見当から少しだけ下を探す形だと、
   水没した所や溶岩の海の底に出ます（出た瞬間に死にます）
5. **仮の判定は「土台があるか」に掛けないこと。3 度目です。** 成果物に掛けること
6. **わざと壊して、テストが赤くなるか確かめること。** この周もそれで 1 つ捕まえました ——
   星の判定を**最小値だけ**で見ていたので、「ネザーでも夜になると星が出る」壊し方が
   通ってしまいました（**最小と最大の両方**を見る形に直しています）
7. **`Dimensions` は次元 1 つにつき生成器を 1 個だけ作って使い回します。**
   種を変えて試すときは器ごと作り直すこと
8. **使い回しのオブジェクトを返す関数は、値を引いたらすぐ控えること**（`environmentFor()`）

## 次に取るもの

1. **2-4（ブレイズ）**。`TASKS.md` の並びで次です。**要 1-8（`projectiles.ts`）と 2-3（要塞）**
   —— どちらも済んでいます。飛行するモブ・火球・ブレイズロッドで、
   **1-8 で「当たったときに何が起きるか」を書いていない**ので、当たり判定とダメージは
   この周に足すことになります。数値は本家（体力 20 / 火球 5）を暫定で置き、`TUNING.md` へ
2. そのあとは並びどおり 2-5（エンダーマン）→ 2-13（ドラゴン）
3. 最後に人がやること: `REVIEW.md` の一括確認 / `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **ネザーでも寝られます**（本家はベッドが爆発する）。`canSleep()` は時刻だけで決まり、
  次元を知りません。クリア導線には効かないので触っていません
- **`F3` の `biome` はネザーでもオーバーワールドの値**が出ます（2-2 から既知）
