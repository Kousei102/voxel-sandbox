# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-12・AUTODEV 75 のあと）

- `npm test` **3497 件すべて成功**（**+25**。修理の項目が 3 本のテストに増えました）
- `npm run typecheck` 緑・**`npm run build` 緑**（`src/**` を触ったので走らせました）。
  **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1450 行**（テストの数え方。**±0**。上限 1500。**1 文字も開いていません**）
- **アイテム 124 種・`MAX_ITEM_ID` 162・111..255 の空きは 93**（±0）・
  **1..63 の空きは 9**（±0。55..63）
- **立方体 41 / 非立方体 79**・**モブ 10 種類**（敵対 5）・**レシピ 65 本**（±0）・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 94 行（未着手 9 件）**

## この周でやったこと（AUTODEV 75 の C・クラウドの無人の周）

**判定表は 3 行目**（未着手 10 件・`AUTODEV-SPEC.md` が `状態: 未着手`）で
**C（実装）の周**でした。**`AUTODEV-SPEC.md` をそのまま実装し、差し戻し 0 回**で閉じています。

**足したのは「道具の修理」** —— **同じ道具を 2 つ盤面に並べると 1 つになり、傷が減ります**
（本家のクラフト修理）。**2 枠しか使わないので 2x2 でも 3x3 でも作れます**（作業台が要りません）。

判断は**仕様どおり 2 つに割りました**（片方に寄せると丸め方が 2 か所に分かれます）:

- **`durability.ts`**（何回ぶん戻るか）: `REPAIR_BONUS = 0.05` と `repairedDamage(item, a, b)`
  の**純関数 1 本**。式は **残りA + 残りB + floor(最大 × 5%)** を**最大で頭打ち**
- **`crafting.ts`**（盤面が修理の形か）: `Craft` 型と `findCraft()` / `findRepair()`。
  **`findRecipe()` を先に見て、当たらなかったときだけ修理を見ます**（`findRecipe()` の
  引数・戻り値・振る舞いは 1 つも変えていません）
- **`craftscreen.ts`**: 差し替え 3 か所と傷を載せる 3 行だけ

**実測は仕様の予想どおり**でした: ボーナスは **木 2 / 石 6 / 鉄 12 / ダイヤ 78 / 弓 19 /
シアーズ 11 / 火種 3**、**新品 2 本も半分 2 本も傷 0**（＝1 本まるごと損）、
**満身創痍 2 本は 2 + ボーナスぶんだけ戻る**（木のツルハシなら残り 4 回）。

**`main.ts` / `inventoryui.ts` / `ui.ts` / `style.css` は ±0 行**（1 文字も開いていません）。
**ID は 0 個**・**`RECIPES` は 65 本のまま**です。

## 次にやること

**次は B（仕様）の周** —— 未着手が **9 件**なので 1 行目は外れ、
**`AUTODEV-SPEC.md` が `状態: 済`** なので 2 行目に当たります。

- **キューの先頭は `29. 木炭`**（原木・トウヒの原木をかまどで焼く）。**ID 1 個**の見込みで、
  **`ROADMAP.md` の予約表から番号を取ること**（**次の空きは 163 から**）
- **取る前に、その 1 件だけコードで数え直すこと**（`smelting.ts` の表と `FUEL` と
  松明のレシピ。**全部を数え直すのは A の仕事**です）
- **B の周は `src/**` に 1 行も差分を出さないこと**（`npm run build` も `npm run bench` も
  走らせる理由がありません）。**120 行に収まらなければ 2 件に割って書き戻すこと**
- **`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
  **`wc -l` を 119 以下に収めること**（`rules/testing.md`）

## ブラウザで見てほしいところ

**この周は `node tools/browsershot.mjs` で 5 枚撮り、`inventory.png` を `Read` で開きました。**
2x2 の盤面・矢印・**空の結果スロット**・防具枠（頭 / 胴 / 脚 / 足）・収納・ホットバー・
注記の 3 行が**前の周とまったく同じ並び**で、**結果スロットに帯も文字も湧いていません**
（空の盤面では `result()` が null）。`paintSlot()` に何かが流れ込んでいたらここに出ます。

**人に見てほしいのは修理の手ざわり 2 つです**（数値では決められません）:

- **傷んだ道具 2 本を並べたとき、結果スロットに出る「傷の帯」で「どれだけ直るか」が
  読めるか。** 帯は残り割合をそのまま幅にしているだけで、**「＋何回ぶん」は出ません**
- **半分減った道具 2 本が「新品に戻る」のが気前よすぎないか**（本家のままの式です）。
  **一番お得な遊び方が「半分減ったら 2 本合わせる」になります**。`TUNING.md` に 1 行

**持ち越しはすべて前の周のままです。** 下に並べたものは**まだ誰も見ていません**:

- **スケルトン（11）**: **矢が当たる高さ** —— 10m で **足元から 0.27m**（膝より下）です。
  当たり判定には入っていますが、**「当たった感じ」がするか**は遊んでみないと分かりません
  （**間合いを縮めれば高く当たります**。`TUNING.md`）/ **2 秒に 1 発・重み 3** が
  「詰めるか逃げるか」を選ばせる速さか / **間合いを取らず近づいて殴ってくる**
  （本家は後ずさりします）のが変に見えないか / **骨色 `0xcdc8b0`** が一覧で
  「骨」に見えるか（いちばん近いのは砂で 24.6）
- **防具（27b）**: 防具枠の置き場所（盤面と収納のあいだ・横 1 列）が使いやすいか /
  1 文字のラベル（頭・胴・脚・足）で部位が分かるか / **革一式（防具点 7）で 28% 減が
  「固くなった」と感じられるか** / 革の色 4 つが「革」に見えるか（上の 2 つは橙寄りに
  出ます。**変えるなら隔たりを測ってから**）/ **革 24 枚（＝牛 24 頭）で一式**が
  見合う値段か。**⚠ 一覧と防具枠の名前が 2 行に折れて絵に被る件**
  （`style.css` の `.slot .label`。**5 文字の「革のズボン」で出ます**）は**人の判断待ち**です
- **フェンス（26a / 26b）**: **`shots/fence.png` に 5 通りが写っていて、面の欠けも
  裏返りもありません**。**手ざわりとして「柵らしく見えるか」**だけ見てください。
  **⚠ フェンスを 52fps 未満で跳び越せる件は残っています** —— 跳躍の余裕は **0.0117**
  （到達は `9.2²/(2×30) + 9.2×dt/2` で、実測 **60fps 1.4883 / 30fps 1.5667 /
  刻みの上限 0.05 で 1.6450**）。**直すなら跳躍の積分**（全ブロックの段差の登り方が
  変わります）**か 1.5 の側**（本家の値から離れます）——**数値では決められないので人の判断です**
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の境目が
  読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
  いちばん近いガラスから 34.3 しか余裕がありません）
- **氷（25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
  **離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります**
  —— 伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは
  承知の上です（24b）
- **その他（まだ誰も見ていません）**: クモの巣の歩き 1/4 / 金のリンゴ `0xf0a800` が
  金インゴット・ブレイズロッドと見分けられるか / 本棚が「本の詰まった棚」に見えるか /
  リンゴの色が赤キノコ・生牛肉と見分けられるか / 葉 200 枚に 1 個が待てる確率か /
  はしごの登り 2.35 m/s / `CANE_GROW_SECONDS = 180` が待てる長さか /
  **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/ 一覧で砂糖が雪・羽根・卵・
  糸と見分けられるか / ボウルとシチューの見分け / 森のキノコの密度 / サボテンの痛さ /
  牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
  クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**）

**`rules/inventory-screen.md` に 2 つ据えました**（どちらも `Edit` で普通に直しています）:

- **「レシピの表に書けないクラフト」は `findCraft()` に足すこと。** 画面が見るのは
  `findCraft()` 1 本だけで、**`findRecipe()` は表を引くだけの下請け**になりました。
  **順番はレシピが先・特別な形は後**（逆にすると、2 枠で成立する棒とシアーズに
  将来ぶつかった日に静かに壊れます）。**`Recipe` に列を足さないこと**
- **`result()` は `Slot` の形（`item` / `count` / `damage`）で返すこと。**
  `paintSlot()` がそのまま受け取るので、**傷の帯は UI に 1 行も足さずに出ます** ——
  仕様の読みがそのとおり当たり、`inventoryui.ts` は ±0 行で済みました

## 人がやること

- **⚠ 設計の判断（48 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（テストの数え方）で、**`AUTODEV.md` の「止まって人を呼ぶ条件」の
  2 番にちょうど並んでいます。** ハードの上限は 1500 行です。
  **待っているのは 2 件だけ**です（**35 骨粉**・`use.ts` の 19 通り目 /
  **24b ケーキをかじる**・最低 7 行）。**キューの上 6 件（29..34）はどれも
  `main.ts` 0 行の見込み**なので、**判断が下りなくても 6 周は止まりません**
  （28 の修理も予想どおり ±0 行で済みました）。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（傷を持たないので、
  革一式を 1 度作れば永久に 28% 減のままです）。**本家は減ります。**
  足すなら `ARMORS` に回数の列と「殴られたら減る」5 つ目の減り方が要り、
  **それだけで 1 周**です。**修理の側はもう待っています** —— `findRepair()` は
  `wearable()` で書いてあるので、**防具が傷を持った日に 0 行で修理できるようになります**
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）
- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 58 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を押してクリエイティブにし、`E` を押して末尾までスクロールしてから撮ること**
（手順は `docs/browser-shots/README.md` の末尾）。**修理の帯を絵で見たいときも同じで、
空の盤面には何も写りません** —— 傷んだ道具を 2 本用意する手順が別に要ります。

**⚠ `npm run shot -- mobs` は 10 種類を 5m 間隔の一列で撮るので、1 体 1 体は小さくしか
写りません**（span が 45m まで伸びました）。**形を確かめるなら、その場面をそのまま
使わずに寄って撮ること**。
