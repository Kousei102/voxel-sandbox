# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-06・AUTODEV 43 のあと）

- `npm test` **3143 件すべて成功**（この周は **+20 件**）・`npm run typecheck` 緑・
  `npm run build` 緑（`src/**` を触ったため）
- **`npm run bench` は走らせていません** —— 生成にもメッシュ化にも触っていないため
  （`worldgen.ts` / `mesher.ts` / `world.ts` の予算まわりは 0 行）
- **音の一群は 1 回も跳ねませんでした**（羊毛 0.552 / 雪 0.716 / 砂 2.235・
  低い声 0.162 / 高い声 0.329 と、落ち着いた値のまま）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1448 行**（`wc -l`。テストの数え方では 1449。上限 1500・
  **止まる目安 1450 まで残り 2 行。この周は ±0**）
- **アイテム 110 種・`MAX_ITEM_ID` 145・111..255 の空きは 107・1..63 の空きは 9**
- **立方体 39 / 非立方体 76**・**モブ 9 種類**・**レシピ 55 本**・**食べ物 9 種**

## この周でやったこと（AUTODEV 43 の C・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 未着手` だったので判定表の 3 行目に当たって **C（実装）の周**。
取ったのは **19a（はしご・置ける / 壊れる / 作れる）** 1 件だけです。

- **`blocks.ts`**: `LADDER` 145 / `LADDER_XN` 146 / `LADDER_ZP` 147 / `LADDER_ZN` 148。
  **壁掛けの松明とまったく同じ形**（`supportFace` の 4 向き + `variantOf`）で、
  違うのは**厚さ 3/16 の板**（`model: "boxes"`）と**床にも天井にも付かない**ところだけ
- **向きを決めるのは `LADDER_BY_SUPPORT` と `ladderVariant()`** —— **松明の表とは別の表**です
  （`TORCH_BY_SUPPORT` は 1 文字も触っていません）
- **置けない理由の文は `supportHint(base)` が表から引きます** ——
  はしごは「**壁**にしか付けられません」、松明は今までどおり「**床か壁**にしか」
- **レシピは棒 7 本で 3 個**（本家と同じ。3x3 なので作業台が要る）
- **`main.ts` は 0 行**（仕様書の予告どおり。置く・壊す・落ちるは既存の経路に乗るだけ）

**仕様書に 1 つ穴がありました**（差し戻しは 0 回。自分で気付いて直しました）:
**`src/items.ts` は「触らない」に入っていましたが、`MAX_ITEM_ID` の 1 行だけは必要**でした
（伸ばさないと**クリエイティブの一覧にだけ出てこないブロック**になります）。
**これは `rules/items-survival.md` に名指しで書いてあります** ——
`grep -l '"src/blocks.ts"' rules/*.md` が返した 3 本のうち 1 本しか読まなかったのが原因で、
**当たったものは全部読むこと**（`CLAUDE.md`）。詳しくは `docs/autodev-log.md`。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` を `状態: 済` にしたので判定表の 2 行目です
（`AUTODEV-QUEUE.md` の未着手は 3 件なので A には当たりません）。
**キューの先頭は 19b（はしごを登れる）** で、**ID は 0 個・`main.ts` は +1 行（1449）**の
見積もりです。**`ROADMAP.md` の次の空きは 149 から**（145..148 ははしごが取りました）。

⚠ **19b は `player.ts` / `vitals.ts` / `main.ts` を触る周です。** この周では
**`climbable` の旗も登る物理も 1 行も書いていません**（先取りしない、という 19a の
禁じ手を守っています）ので、**まっさらな状態から仕様書を書けます。**

## ブラウザで見てほしいところ

**はしごを撮りました**（`shots/ladders.png`。`tools/shot.ts` に `ladders` の場面を足しました）。
石の柱の 4 面に 1 本ずつと、隣に 1 面だけ掛けた柱を並べてあります ——
**面の欠けも裏返りも 0 件**で、厚さ 3/16 のぶん掛けた柱が裸の石より太く写り、
1 面だけの柱では石の面が残ります。**本物のブラウザ（`docs/browser-shots/`）でも
WebGL 2.0 で例外 0 件**（はしごは**置くもの**なので、遊び始めの絵には出てきません）。

**この周にユーザーへ見てほしいのは 3 つ**:
**はしごの色（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか** /
**厚さ 3/16 の板が「はしご」に見えるか**（横木は無く、板 1 枚です）/
**「はしご は壁にしか付けられません」の文が、床を狙ったときに親切か。**

**持ち越し（まだ誰も見ていません）**: **`CANE_GROW_SECONDS = 180` が待てる長さか**
（1 段から 3 段まで 6 分）/ **上を刈って伸び直すのが「畑」として成り立つか** /
**自然に生えたサトウキビは伸びません**（上に 1 本置けば列ごと覚えます。理由は `TUNING.md`）/
サトウキビの背丈と密度の釣り合い（0.12 で 1〜3 段）/ **積む手ごたえ** /
**⚠ 前から遊んでいる世界だけの見え方**: `localStorage` の差分は「壊した／置いた」だけを
持つので、**前に浜のサトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて
見えることがあります**（新しく作った世界では起きません）/ 一覧で砂糖（`0xffffff`）が
雪・羽根・卵・糸・雪玉と見分けられるか / サトウキビ（`0x9ad14f`）が小麦の種と並んだとき /
ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で
材料 3 つに見合うか / 食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度
（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を草むらと取り違えないか / サボテンに
当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・ミルクで毒が止まるか / ミルクバケツの色
（`0xeaf2f8`）/ かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
鉄・金・ダイヤのブロックが材料の山と見分けられるか / **一覧の枠の名前が 2 行に折れて
はみ出す**（`style.css` の `.slot .label`）/ クモの速さ 6.0 と朝に燃えてよいか /
投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**）

- **`rules/meshing-render.md`** に、絵に撮る場面の作り方を 2 つ足しました:
  **「置くもの」は探しても 1 マスも写らない**（はしご・かまど・ベッドのように人が
  置くブロックは、`crops` の形で台を作って直に置くしかない）/
  **薄いものは隣に比べるものを置かないと厚さが読めない**（4 面に掛けた柱は
  ただの木の柱に見えました。距離も 1 度では決まらず、**撮っては `Read` で見て詰める** ——
  この周は 3 枚撮って決めました）

**`MAX_ITEM_ID` のぶんは `rules/items-survival.md` に既にあった**ので足していません
（層 2 に同じことを 2 度書かないこと）。踏んだ経緯は `docs/autodev-log.md` に残しました。

## 人がやること

- **設計の判断を 1 つお願いします（17 周持ち越し。そろそろ急ぎです）:
  `main.ts` を 2 つに割ってよいか。** いま **1448 行で、止まる目安 1450 まで残り 2 行**。
  **19b で 1 行使って 1449 になり、残り 1 行になります。**
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 27 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。
**`main.ts` の行数も同じ数え方**なので、`wc -l` の 1448 はテストでは 1449 と出ます。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
