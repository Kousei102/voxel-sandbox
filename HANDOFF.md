# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05）

- `npm test` **3014 件すべて成功**（前の周 3005 + `test/panels.test.ts` の 8 件と、
  `panels.ts` の見張り 1 件）・`npm run typecheck` 緑・`npm run build` 緑
- **音の一群は 1 回目から緑**でした（羊毛 0.552 < 雪 0.716 < 砂 2.235・低い声 0.162 < 高い声 0.329）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていない周です）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1463 → 1429 行**（上限 1500・止まる目安 1450。**目安を下回りました**）
- **アイテム 102 種・`MAX_ITEM_ID` 137・111..255 の空きは 118・1..63 の空きは 9**（変わらず）
- **立方体 39 / 非立方体 69**・**クラフト 51 本 / 精錬 7 本 / 食べ物 8 種**・**モブ 9 種類**（変わらず）

## この周でやったこと（AUTODEV 29 の C・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が 8 件（3 件以上）・`AUTODEV-SPEC.md` が **`未着手`** だったので、
判定表を上から当てて **C（実装）の周**。仕様書 15a のとおり **`src/panels.ts` を作りました。**

**画面の開け閉め（手を止める → 出す → メニューを隠す → ロックを外す）を `main.ts` から
出しました。** `openPanel` / `openInventory` / `openCreativeInventory` / `openFurnace` /
`openChest` / `closeInventory` の 6 つと、`pointerlockchange` の menu を出す条件。
**`stopHands()` は `main.ts` に残しています**（`breaking` が向こうの状態）。

- **振る舞いは 1 つも変えていません。** ID も 0 個（`ROADMAP.md` の予約表は次の空き **138** のまま）
- **器の中身を引くのは呼ぶ側のまま**（`furnaces.at()` / `chests.open()` は `main.ts`。
  `panels.ts` は `world` を持ちません）
- `test/panels.test.ts` を新しく足し、**4 経路とも「呼ばれた順」で見られる**ようになりました
  （偽の `PanelHost` に配列で控えて、`console.log` で出してから判定）

**C-2 の点検（別のエージェント）で見張りの弱点を 2 つ直しました**（コミットは 2 つに
なりました。**2 つ目は `test/**` だけで、配信されるものは 1 バイトも変わりません**）:
`panels.ts` の漏れ検査が `"new World"` を探していて **`world: World` を素通り**していたので
`"World"` へ広げ、値を出さずに判定していた 2 件に `console.log` を足しました。
点検役は **`main.ts` の守りを実際に外して `test/ui.test.ts` が落ちること**まで
確かめています（下の ⚠ の見張りが生きている証拠）。

## ⚠ 仕様書の穴を 1 つ踏みました（次の周が同じ道を通らないために）

**`test/ui.test.ts` の `routed` は `main.ts` に対して `!hud.victoryOpen` を文字列で
見張っています。** 仕様書 §4 のとおり `menuVisibleWhenUnlocked({screenOpen, dead,
victoryOpen})` へ畳んだら、そこが落ちました（仕様書 §6 の「動かす語が `test/` に
当たったら動かさない」は、書いた人が **7 関数だけ**当たっていて、690 行の条件式は
当たっていなかった）。

**`test/ui.test.ts` は 1 行も触っていません。** 引数を**「閉じている／生きている」の側**で
受ける形（`{screenClosed, alive, victoryClosed}`）に変え、**`!` を `main.ts` に残しました**。
守りを外せば `routed` と `tsc` の両方が落ちるので、**見張りは前より強くなっています。**
この落とし穴は `rules/testing.md` に据えました。

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手が 7 件（3 件以上）・`AUTODEV-SPEC.md` が `済` なので
→ 次は B（仕様）の周**です。**先頭の 15b（ミルクバケツ）を仕様書にすること。**

- **`main.ts` の行数はもう止まる目安を下回っています**（1429 / 目安 1450）。
  15b（12〜15 行）と 16（1 行）のぶんはありますが、**その次はまた足りません**
  （`AUTODEV-QUEUE.md` の ⚠ と、下の「人がやること」）
- 取る前に 15b だけコードで数え直すこと（B の決まり）。**2026-09-05 の時点で `milk` は
  `src/**` にも `test/**` にも 0 件**でしたが、もう一度当たること
- ID を 1 個使う見込みです。**番号は `ROADMAP.md` の予約表から（次の空きは 138）**

## ブラウザで見てほしいところ

**この周は絵が変わりません**（配線の貼り替えだけ）。**本物のブラウザで 5 枚撮って
`Read` で見ました**（`docs/browser-shots/`）。**console のエラー・例外は 0 件。**

- **タイトル画面（`menu.png`）は今までどおり出ています**（起動で例外が出ると真っ黒になる）
- **`E` でインベントリが開きました（`inventory.png`）** —— 貼り替えた
  `panels.openInventory(2)` の経路が本物のブラウザで通ったということです
- **`hud.png` / `game.png` / `debug.png` も変わりなし**

**分かったこと: ヘッドレスでもポインタロックは効きます**（前の `HANDOFF.md` の
「インベントリを開いた絵は撮れません」は誤りでした。`docs/browser-shots/README.md` の
19 行目のほうが正しい）。**それでも、かまど・チェスト・作業台を開いて閉じる経路は
撮っていません** —— **公開サイトでその 3 つが今までどおり開いて閉じるかだけ見てください。**

- 持ち越し: **雪玉を投げても何も起きない**のが物足りなくないか / **鉄・金・ダイヤの
  ブロックが材料の山と見分けられるか** / **一覧の枠の名前が 2 行に折れてはみ出す**
  （「ダイヤブロック」「溶岩入りバケツ」。`style.css` の `.slot .label`）/ **クモを 3 匹
  倒すまで弓が作れない**道のり / **クモの速さ 6.0** と**朝に燃えてよいか**（本家は
  燃えません）/ **投げた卵に使い道が無い** / **牛の胴が寸詰まりに見えないか** /
  **産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒**

## この周で分かったこと

**決まりごとは 2 件据えました**（どちらも `Edit` で普通に直しました）:

- `rules/testing.md` —— **`test/ui.test.ts` の `routed` は「動かしてはいけない語」の
  一覧でもある**（判断を出す周は、動かす行の語を先に `grep -rn '<語>' test/` で当たること）
- `rules/dom-ui.md` —— **「画面の開け閉め（`panels.ts`）」の節**（`document` / `World` を
  持ち込まない・器の中身を引くのは呼ぶ側・新しい画面も `panels.open(show)` を通す）。
  `paths` に `src/panels.ts` と `test/panels.test.ts` の 2 行。**本数が増えないので
  `rules/README.md` は触っていません**

## 人がやること

- **設計の判断を 1 つお願いします（前の周から持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま 1429 行で 15b・16 は入りますが、**`test/ui.test.ts` の 40 件の見張りが呼び出しを
  `main.ts` に釘付けにしている**ため、これ以上は大きく減りません。減らすなら
  「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える必要があり、
  **判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 11 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
