# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2382 件すべて成功**（前の周の 2332 から **+50**）
- **`npm run build` は通ります**（`dist/` まで出ます）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1381 行 / 上限 1500**（この周で +6 行）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— ID は 0 個）
- **`AUTODEV-SPEC.md` は `状態: 済`**、`AUTODEV-QUEUE.md` の未着手は **8 件**
  → **次の周は B（仕様）**
- **`RULES-INBOX.md` に未取り込みが 3 件**（`mobs.md` / `projectiles.md` / **`items-survival.md`**）。
  **取り込むのは人が居る周**

## この周でやったこと（無人・C の周 1 種類だけ）

`AUTODEV-QUEUE.md` の 3 番の前半 = **道具の耐久値**（掘るたびに 1 減り、0 で壊れる）。
回数は本家のまま **木 59 / 石 131 / 鉄 250 / ダイヤ 1561**。コミットは 1 つです。

- **判断は新しい `src/durability.ts` に全部あります**（回数の表・減るか・壊れたか・
  帯の割合・壊れたときの 1 行・セーブの形）。`ui.ts` は割合を `style.width` に貼るだけ、
  `main.ts` は `wearSlot()` の戻り値を見るだけ（**回数も式も書いていません**）
- **ID は 0 個**。`items.ts` の `ToolDef` にも 1 行も足していません（あちらは速さの表）
- **セーブは `SaveData.wear`（36 要素・省略可）を 1 つ足しただけ。** `inventory` の
  `[item, count]` は形を変えていません（`version` は 1 のまま）。**全部新品ならキーごと
  出ない**ので、道具を傷めていない人のセーブは耐久値が入る前と 1 バイトも変わりません
- **`Slot.damage` は省略可**にしました。必須にすると `smelting.ts` / `craftscreen.ts` /
  `inventoryui.ts`（仕様書が「触らない」と決めたファイル）に手が要ります
- 詳しくは `docs/autodev-log.md` の 2026-09-01（C の周）。**仕様書から変えた 2 点**
  （見張りの語・`test/session.test.ts` の偽物 2 つ）もそこに書いてあります

### わざと残した抜け道（次の周で片手間に塞がないこと）

**傷はインベントリの 36 枠にしか乗りません。** 落とし物・チェスト・**インベントリ画面で
掴んで動かしたとき**は `item` と `count` しか運ばないので、**傷んだ道具が新品に戻ります。**
セーブの形が 3 つ動くので **`AUTODEV-QUEUE.md` の 3b** の仕事です（`TUNING.md` にも 1 行）。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶん（道具の耐久値）:**

- 木のツルハシで石をいくつか掘ると、**ホットバーの枠の下に細い帯**が出るか
  （減るほど短く・赤く）。**インベントリ画面の枠にも同じ帯**が出るか
- **新品の道具と、棒や土のような減らない物には帯が出ていないこと**（出ていたら
  `wearBar()` が -1 を返していません）
- 使い切ると道具がその場で消えて、画面下に **「木のツルハシ が壊れました」** が 1 行出るか

**前の周からの持ち越しが 3 件、まだ人の目を通っていません:**

- **大きいチェストの周のぶん**（`a795564`）—— 2 個並べて開くと **54 枠が 9 列 x 6 段**、
  単体だと **27 枠**、3 個横一列ならどれも 27 枠に戻るか。パネルがはみ出していないか
- **砂・砂利が落ちる周のぶん**（`6209e91`）—— 砂の柱の下を掘ると上が詰まり、
  **穴や浮いた砂が残らないこと**。掘っていない崖や洞窟の天井が崩れていないこと
- **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**と
  **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **8 件**（3 件以上あるので A ではない）、
**`AUTODEV-SPEC.md` は `状態: 済`** → **次は B（仕様）の周**です。

- キューの**先頭 1 件**（いまは **3b. 耐久値がインベントリの外でも残る**）を
  `AUTODEV-SPEC.md` に書き直すこと。**取る前にコードで数え直す**（B の決まり）
- **3b は `drops.ts` / `chests.ts` / `craftscreen.ts` の 3 つとも `[item, count]` しか
  運びません。** 120 行に収まらなければ、その場で 2 件に割ってキューへ書き戻すこと
- **B の周は `src/**` を 1 行も触らないこと**
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **サブエージェントはこの環境では同じ番の中で結果が返せません。** C の周は
  親が自分で実装してよく、点検も `git diff` を自分で読むこと（`AUTODEV.md` の C-1 / C-2）

人がやること:

- 上の**ブラウザ確認 4 件**（この周のぶん 1 件 + 持ち越し 3 件）
- `RULES-INBOX.md` の **3 件**の取り込み（**`items-survival.md` のぶんは `paths` に
  `src/durability.ts` を足す指示付き**）
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）の揺れは続いています。** この周も
**1 回落ちて、そのあと 5 回続けて緑**でした（落ちたのはいつも同じ 1 件
**「音量がそのまま大きさに掛かる（5 倍）」**で、出た値は `2.29e+12` の桁の飛んだ数）。

- **この周の差分に `audio.ts` と `sfx.ts` は 1 行も入っていません**（`git diff --name-only`
  で確かめました）—— **前からある揺れで、この周が持ち込んだものではありません**
- 赤が出たら、**まず `git diff -- src/audio.ts src/sfx.ts` が空かを見てから**退行を疑うこと
- 直すのは人が居る周（音を聞いてもらう必要があります）

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
