# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-12・AUTODEV 74 のあと）

- `npm test` **3472 件すべて成功**（**±0**。`src/**` も `test/**` も触っていません）
- `npm run typecheck` 緑。**`npm run build` も `npm run bench` も走らせていません**
  （**B の周は `src/**` に 1 行も差分を出さない決まり**なので、走らせる理由がありません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1450 行**（テストの数え方。`wc -l` では 1449。**±0**。上限 1500）
- **アイテム 124 種・`MAX_ITEM_ID` 162・111..255 の空きは 93**（±0）・
  **1..63 の空きは 9**（±0。55..63）
- **立方体 41 / 非立方体 79**・**モブ 10 種類**（敵対 5）・**レシピ 65 本**・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 未着手`（118 行）**・**`AUTODEV-QUEUE.md` 95 行（未着手 10 件）**

## この周でやったこと（AUTODEV 74 の B・クラウドの無人の周）

**判定表は 2 行目**（未着手が **10 件**で 1 行目は外れ、`AUTODEV-SPEC.md` が `状態: 済`）で
**B（仕様）の周**でした。**触ったのは `AUTODEV-SPEC.md` と `HANDOFF.md` の 2 本だけ**です。

**キューの先頭 `28. 道具と防具の修理` を仕様書にしました**（7 項目すべて・118 行）。
**取る前にコードで数え直して、仕様が 3 つ動きました**:

- **⚠ 防具は傷を持ちません。** `maxUses()` が見るのは `toolOf()` と火種・弓・シアーズだけで、
  革の防具 4 部位は `wearable()` が偽です。**傷を持てるのは実測 23 種類**（道具 20・
  火種 64 回・弓 384 回・シアーズ 238 回）。**`ARMORS` に回数を足すのは別の周**にして、
  **`wearable()` で書かせました** —— 防具が傷を持った日に 0 行で効きます
- **`RECIPES` に行は増えません**（65 本のまま）。**2 枠で成立する既存レシピは棒
  （板 2 枚）とシアーズ（鉄 2 個）の 2 本だけ**で、どちらの材料も傷を持たないので
  **修理とはぶつかりません**（`findRecipe()` を先に見る順番も仕様に書いてあります）
- **判断を 2 つに割りました**: 「**何回ぶん戻るか**」は `durability.ts` の純関数
  （`repairedDamage()`・ボーナスは本家のまま 5% 切り捨てで **木 2 / ダイヤ 78**）、
  「**盤面が修理の形か**」は `crafting.ts`（`findCraft()`）。`craftscreen.ts` は
  **`findRecipe` → `findCraft` の差し替え 3 か所と傷を載せる 3 行だけ**です

**`main.ts` は ±0 行の見込み**です（作る経路は `craftscreen.ts` 1 本）。
**`inventoryui.ts` / `ui.ts` / `style.css` も ±0 行** —— `paintSlot()` は `Slot` を取るので、
`result()` が `damage` を返せば**結果スロットの傷の帯は勝手に出ます**。**ID は 0 個**です。

## 次にやること

**次は C（実装）の周** —— 未着手が 10 件なので 1 行目は外れ、**`AUTODEV-SPEC.md` が
`状態: 未着手`** なので 2 行目も外れ、3 行目に当たります。

- **`AUTODEV-SPEC.md` を全文渡して実装すること**（要約しないこと）。読ませる `rules/` は
  **`inventory-screen.md` / `items-survival.md` / `drops.md` / `testing.md`** の 4 本
- **`npm run build` は必須**（`src/**` を触ります）。`npm run bench` は不要
- **撮るのは `node tools/browsershot.mjs` を 1 回だけ** —— 見た目のファイルは ±0 行なので、
  **`inventory.png` が今までどおり出ること**だけ `Read` で見ること（差分が出たら
  `paintSlot()` に何か流れ込んだ合図）。`npm run shot` は不要（DOM は写りません）
- 終わったら `AUTODEV-QUEUE.md` の 28 の行を消し、仕様書を `状態: 済` に。
  **`ROADMAP.md` は触らないこと**（ID を 1 つも使いません）

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません** —— `src/**` を 1 行も触っていないので、
見た目に出るものが 1 つも変わっていません（`AUTODEV.md` の C-3 は C の周だけの決まりです）。

**持ち越しはすべて前の周のままです。** 下に並べたものは**まだ誰も見ていません**:

- **スケルトン（11）**: **矢が当たる高さ** —— 10m で **足元から 0.27m**（膝より下）です。
  当たり判定には入っていますが、**「当たった感じ」がするか**は遊んでみないと分かりません
  （**間合いを縮めれば高く当たります**。`TUNING.md`）/ **2 秒に 1 発・重み 3** が
  「詰めるか逃げるか」を選ばせる速さか / **間合いを取らず近づいて殴ってくる**
  （本家は後ずさりします）のが変に見えないか / **骨色 `0xcdc8b0`** が一覧で
  「骨」に見えるか（いちばん近いのは砂で 24.6）
- **防具（27b）**: 防具枠の置き場所（盤面と収納のあいだ・横 1 列）が使いやすいか /
  1 文字のラベル（頭・胴・脚・足）で部位が分かるか / **革一式（防具点 7）で 28% 減が
  「固くなった」と感じられるか** / 革の色 4 つが「革」に見えるか（上の 2 つは橙寄りに
  出ます。**変えるなら隔たりを測ってから**）/ **革 24 枚（＝牛 24 頭）で一式**が
  見合う値段か。**⚠ 一覧と防具枠の名前が 2 行に折れて絵に被る件**
  （`style.css` の `.slot .label`。**5 文字の「革のズボン」で出ます**）は**人の判断待ち**です
- **フェンス（26a / 26b）**: **`shots/fence.png` に 5 通りが写っていて、面の欠けも
  裏返りもありません**。**手ざわりとして「柵らしく見えるか」**だけ見てください。
  **⚠ フェンスを 52fps 未満で跳び越せる件は残っています** —— 跳躍の余裕は **0.0117**
  （到達は `9.2²/(2×30) + 9.2×dt/2` で、実測 **60fps 1.4883 / 30fps 1.5667 /
  刻みの上限 0.05 で 1.6450**）。**直すなら跳躍の積分**（全ブロックの段差の登り方が
  変わります）**か 1.5 の側**（本家の値から離れます）——**数値では決められないので人の判断です**
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の境目が
  読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
  いちばん近いガラスから 34.3 しか余裕がありません）
- **氷（25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
  **離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります**
  —— 伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは
  承知の上です（24b）
- **その他（まだ誰も見ていません）**: クモの巣の歩き 1/4 / 金のリンゴ `0xf0a800` が
  金インゴット・ブレイズロッドと見分けられるか / 本棚が「本の詰まった棚」に見えるか /
  リンゴの色が赤キノコ・生牛肉と見分けられるか / 葉 200 枚に 1 個が待てる確率か /
  はしごの登り 2.35 m/s / `CANE_GROW_SECONDS = 180` が待てる長さか /
  **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/ 一覧で砂糖が雪・羽根・卵・
  糸と見分けられるか / ボウルとシチューの見分け / 森のキノコの密度 / サボテンの痛さ /
  牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
  クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`src/**` を 1 行も触っていないので、踏んだ落とし穴がありません。**
数え直して分かったこと（**防具は傷を持たない**・2 枠のレシピは棒とシアーズの 2 本だけ・
`findRecipe()` の呼び先は 3 か所）は**どれも `AUTODEV-SPEC.md` に書いてあります** ——
`rules/` は「触るファイルの決まりごと」の置き場なので、仕様を決めるための数え直しは
そちらではありません。

## 人がやること

- **⚠ 設計の判断（47 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（テストの数え方）で、**`AUTODEV.md` の「止まって人を呼ぶ条件」の
  2 番にちょうど並んでいます。** ハードの上限は 1500 行です。
  **待っているのは 2 件だけ**です（**35 骨粉**・`use.ts` の 19 通り目 /
  **24b ケーキをかじる**・最低 7 行）。**キューの上 7 件はどれも `main.ts` 0 行の
  見込み**なので、**判断が下りなくても 7 周は止まりません**（28 の仕様書も ±0 行です）。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（傷を持たないので、
  革一式を 1 度作れば永久に 28% 減のままです）。**本家は減ります。**
  足すなら `ARMORS` に回数の列と「殴られたら減る」5 つ目の減り方が要り、
  **それだけで 1 周**です（28 の修理は `wearable()` で書くので、足した日に 0 行で効きます）
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）
- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 57 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を押してクリエイティブにし、`E` を押して末尾までスクロールしてから撮ること**
（手順は `docs/browser-shots/README.md` の末尾）。

**⚠ `npm run shot -- mobs` は 10 種類を 5m 間隔の一列で撮るので、1 体 1 体は小さくしか
写りません**（span が 45m まで伸びました）。**形を確かめるなら、その場面をそのまま
使わずに寄って撮ること** —— 前の周は使い捨ての場面（モブ 2 体・カメラ 3m）を
`tools/raster.ts` に直に渡して撮りました。**リポジトリには入れていません。**

**`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
**`wc -l` を 119 以下に収めること**（`rules/testing.md`）。
