# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-08・AUTODEV 59 のあと）

- `npm test` **3295 件すべて成功**（**±0**）・`npm run typecheck` 緑
- **`npm run build` は走らせていません**・**`npm run bench` も走らせていません**
  （**B（仕様）の周なので `src/**` に 1 行も差分がありません**。`git diff --stat -- src test tools` が空）
- **音の一群は跳ねませんでした**（この周に走らせた `npm test` は 3 回とも全部緑）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行（±0）**・**この周は `main.ts` を 1 文字も開いていません**
- **アイテム 117 種・`MAX_ITEM_ID` 155・111..255 の空きは 100・1..63 の空きは 9**
- **立方体 40 / 非立方体 78**・**モブ 9 種類**・**レシピ 60 本**・**精錬 7 本**・
  **燃料 8 種**・**食べ物 11 種**・**バイオーム 10**・**構造物 2**・**次元 3**・
  **`DamageCause` 9 種**（どれも ±0）
- **`AUTODEV-SPEC.md` は `状態: 未着手`（119 行）**・**`AUTODEV-QUEUE.md` 121 行**（未着手 7 件）

## この周でやったこと（AUTODEV 59 の B・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 済`」で 2 行目 = B（仕様）の周**でした
（未着手が 6 件あったので A ではありません）。**キューの先頭 = 25（氷）**の仕様書を書きました。
**サブエージェントは 1 つも立てていません**（B の周は読んで数えるだけです）。

**この周の仕事は「割る場所を決めるためにコードを追ったこと」です。** 結果、
**25 を 25a / 25b に割りました**（`AUTODEV.md` の B「120 行に収まらない仕様は
1 周で閉じない信号」）。割った根拠は 2 つ:

1. **`biomes.ts` の `resolve()` は、海面より低い列を気温に関わらず `OCEAN` に潰します**
   （`if (height < SEA_LEVEL) return OCEAN;`）。だから**雪の浜の隣の海も `OCEAN`** で、
   **凍らせるにはバイオームを 1 つ足すしかありません** —— これだけで 1 周ぶんです
2. 前半（ブロック・滑り・壊すと水）は **`worldgen.ts` も `biomes.ts` も 0 行**で通ります

**`main.ts` は ±0 行だと数えました**（キューの ⚠ が「取るときに数えること」と
言っていたもの）。**置く経路は `placing.ts`**、**壊す経路は `main.ts` の `breakBlock()`
（1026 行）が `tryBreak()` に丸投げ**していて、**「壊したあとに何が残るか」を決めるのは
`breaking.ts` の `setVoxel(x, y, z, AIR)` の 1 か所**です。ここを
`remainsAfterBreak(id)` に替えれば、氷 → 水は `main.ts` を開かずに通ります。

**滑りの 2 定数は本家から出しました**（仕様書の 4 に式ごと書いてあります）。
**いまの摩擦 `1 - dt * 12` の 12 は、本家の普通のブロック（滑りやすさ 0.6）を
毎秒に直した 12.1 とほぼ同じ**でした —— だから氷は同じ式で **2.3**、地上の加速は
**0.23 倍**（60 → 13.8。空中の 14 とほぼ同じ）になります。**値は C の周が
`TUNING.md` に据えます**（この周はまだ 1 行も書いていません）。

**`blocks.ts` の 718 行目が氷を名指しで待っていました** ——
「**`sticky` と 1 つの旗にまとめないこと** —— 氷は鈍らせるだけ、ツタは刃物だけ」。
仕様書はそのとおり **`slippery` を別の旗**にしています。

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が **`状態: 未着手`** なので判定表の 3 行目です。
**`AUTODEV-SPEC.md`（25a・氷のブロックと滑り）を全文サブエージェントに渡して実装**すること
（要約しないこと）。**渡す `rules/` は `blocks-shapes.md` / `items-survival.md` /
`drops.md` / `testing.md`**（`grep -l '"src/player.ts"' rules/*.md` で引き直すこと）。

- **ID は 156**（`ROADMAP.md` の予約表。155 = ケーキで埋まりました）
- **`main.ts` を 1 行も開かないこと**（数えた結果 ±0 行です）
- **色 `0x8fc4f2` は暫定**です。**一覧でいちばん近いのはガラス `0xa9d8e8`** なので、
  20 を割ったら `top` だけ寄せ直して `TUNING.md` に 1 行
- **`npm run shot -- ice` を撮って `Read` で開くこと**（半透明が絵に出る唯一の経路です。
  `tools/shot.ts` の `water` の注意書き「1 マスの水たまりで終わりにしない」も読むこと）

## ブラウザで見てほしいところ

**この周は `src/**` に 1 行も触っていないので、新しく見てほしいものはありません。**
公開サイトの見た目は AUTODEV 58（ケーキ）のままです。

**持ち越し（ケーキ）**: **上面 `0xffd0e4` が「ケーキ」に見えるか**（ピンクが強すぎないか）/
**側面のスポンジ `0xe8c9a0` と下面 `0xd9b98a` の 3 色が並んでケーキに読めるか** /
**一覧で薄紅が羊毛・卵・雪と見分けられるか**（いちばん近いのは羊毛で 31.5）。
**作れるのに使い道が無い**のは承知の上です（かじるのは 24b）。

**持ち越し（クモの巣）**: **中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか** /
**素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか** /
**一覧で `0xc8c8dc` が雪・ミルク・紙・糸と見分けられるか**。

**持ち越し（金のリンゴ）**: **色 `0xf0a800` が金インゴット・ブレイズロッド・
ブレイズパウダーと見分けられるか** / **金インゴット 8 個 + リンゴ 1 個に「体力 4 回復」が
見合うか** / **27b（革の防具）を実装したら: 防具点 7 で 8 割ぶんしか減らない
（10 → 7.2）のが「着た手ごたえ」として分かるか**。

**持ち越し（まだ誰も見ていません）**: **本棚が「本の詰まった棚」に見えるか**
（側面 `0x9c5064` がかなり赤紫）/ **紙 `0xd4e0ec` が雪玉と 25.5 で見分けられるか** /
**本棚 1 個に材料 9 個が見合うか** / **リンゴの色 `0xe0342c` が赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か** / **地面に落ちた
リンゴが赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか** /
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか** /
**はしごの上端から地上へ出る手触り** / **はしごの色（`0xc9a063` / `0xa8823f`）が
板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に見えるか** /
**`CANE_GROW_SECONDS = 180` が待てる長さか** / **自然に生えたサトウキビは伸びません**
（理由は `TUNING.md`）/ **⚠ 前から遊んでいる世界だけの見え方**: 前に浜の
サトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて見えることがあります /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度 / サボテンに
当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` は 1 行も直していません。** 落とし穴を踏まなかったからです ——
B の周は読んで数えるだけで、既知の罠（仕様書 120 行・`wc -l` は上限 - 1）は
`rules/testing.md` にもう書いてあり、そのとおり **119 行**に収めました。

## 人がやること

- **設計の判断（32 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**で、
  上限 1500 まで 51 行です。**25a は数えた結果 ±0 行**なので、**この判断を待たずに
  次の C の周は回せます**。待っているのは **24b（ケーキをかじる。最低 7 行）** と
  **27b（革の防具。セーブのキーで 1 行）** の 2 件です。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
  （**判定の意味が変わるので無人の周では触れません**）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 42 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
