# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-03）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は人が居る周（仕組みの手当て）**でした。**`src/**` は 1 行も触っていません**
- `npm test` **2884 件すべて成功**（前の周の 2858 + 新しい 26 件）。`npm run typecheck` も緑。
  **`npm run build` と `npm run bench` は走らせていません**（`src/**` を触っていないため）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 127**（**この周は 0 個使用**）
- **アイテムは 93 種**、`MAX_ITEM_ID` は **128**。**モブは 7 種類**。`SaveData` は `version` 1
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（120 行 / 上限 120）→ **次の周は C（実装）**
- `REVIEW.md` の未着手は 0 件。**`RULES-INBOX.md` は層 3（スキル）専用になりました**（下）

## この周でやったこと — 層 2 を無人の周が自分で直せるようにした

**これまで**: 無人の周は `.claude/rules/**` を編集できず（「sensitive file」の確認で止まる）、
`RULES-INBOX.md` に本文を書き置いて**人が居る周が手で当てて**いました。受け渡しが 1 周でも
空くと本文どうしが食い違い、取り込む側が毎回それを捌いていました（`RULES-INBOX.md` の
「取り込むときにした判断」）。**この往復を無くしました。**

**これから**（手順は `LOOP.md` の 4.5）: 周が新しい全文をスクラッチへ `Write` し、
`npm run rules:install -- <一時ファイル> .claude/rules/<名前>.md` で据えます。

**止めているものは 2 つある、と分かったのが要点です**（ここが今回の発見）:

1. **`Edit` / `Write` の「sensitive file」** —— `Edit(.claude/rules/**)` の許可行では消えません
   （2026-08-25 実測。この結論は変わっていません）
2. **Bash の書き込みに掛かる auto mode の分類器** —— こちらは**許可行 1 つで通ります**
   （2026-09-03 実測）。だから設置は Bash（スクリプト）でやります

**`.claude/settings.json` の許可行 4 つが、この仕組みの動く条件です**（同じコミットに入っています）:
`Bash(node scripts/install-rule.mjs:*)` / `Bash(npm run rules:install:*)` /
`Bash(npm test:*)` / `Bash(npm run typecheck:*)`。**消すと無人の周は最後の最後で止まります**
（`test/rulesinstall.test.ts` が 1 つ目の有無を毎周見ています）。

足したもの:

- **`scripts/install-rule.mjs`** —— 据えてよいのは `.claude/rules/` の**直下だけ**
  （親を `realpath` で突き合わせ）。`paths` の無い本文は断る。同内容なら 1 バイトも書かない。
  一時ファイル → `rename` で差し替え。**消す操作は持たない**
- **`test/rulesinstall.test.ts`（26 件）** —— 断る宛先 8 通り（`settings.json`・`skills/`・
  `..` で外へ出る形など）と、**一時ディレクトリで本当に書かせる**新規 / 更新 / 変更なし。
  **関数を import せず、必ず spawn して確かめること**（`import.meta.url` で置き場所を決めるので、
  テストに束ねると別のファイルを見ることになります）
- `package.json` に `rules:install`、`test/run.ts` に登録

**踏んだ罠（同じことをする人へ）**: 許可行は**単体のコマンドにだけ**効きます。
`node scripts/install-rule.mjs ... ; echo ...` のように `;` や `|` でつなぐと合成コマンドになり、
**全体が分類器に回って落ちます。** `LOOP.md` の 4.5 とスクリプトの先頭に書いてあります。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は `src/**` を触っていないので、何も変わりません。** 前の周からの持ち越しが 4 つ:

1. **矢が 2x2 で作れなくなってよいか**（AUTODEV 17）。戻すなら `crafting.ts` の 1 行（`TUNING.md`）
2. **羽根のほぼ白（`0xe8e4dc`）が一覧の中で見分けが付くか**（`E` を押して末尾へ）
3. **鶏が「鶏らしいか」**（豚・羊と並べて。声 1.8 の高さも耳で）
4. **苗の緑が草原に紛れる**件と **`GROW_SECONDS` 180 秒**、**`#status` の位置**

## 人がやること

- **`loop/endgame` にはまだ届いていません。** あちらの周は `LOOP.md` を自分のブランチから
  読むので、**このコミットが行き渡るまで `RULES-INBOX.md` に書き続けます**（壊れはしません）。
  行き渡らせ方（`master` 経由でマージするか、cherry-pick するか）は決めてください
- **`.claude/skills/**` は今までどおり `RULES-INBOX.md` 経由**です。更新が稀なので
  同じ口を開けていません。**開けたくなったら宛先の検査を 1 行広げるだけ**ですが、
  そのぶん周が自分で手順書を書き換えられるようになります（承知のうえで決めること）
- **クラウドで本当に止まらないかは、次の無人の周が決まりごとを直すまで分かりません。**
  止まったら `HANDOFF.md` に理由が残るはずなので、そのときは
  **シンボリックリンク案**（本文を `rules/*.md` へ出し、`.claude/rules/*.md` はそこへのリンク。
  周は `.claude/` の外を普通に `Edit` するだけ）へ切り替えてください
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 未着手`** なので → **次は C（実装）の周**です
（仕様「鶏が卵を産む（`MobDef.laying`）」。**アイテム 129 = 卵を 1 個使います**）。

- **`AUTODEV-SPEC.md` を全文そのまま使うこと**（要約しない。要約すると「触らないファイル」と
  「このタスク固有の禁じ手」が真っ先に落ちます）。仕様書の「先に潰しておいた穴」3 点
  （`layTimer` を 0 から始めない / `lay()` を `think()` に置かない / `onDrop` の説明を直す）は
  **仕様書の中に残してあります**
- **`AUTODEV-QUEUE.md` の未着手は 6 件**（7c-1 / 7c-2 / 8 牛 / 9 クモと糸 /
  10 スケルトンと骨 / 11 刈られた羊が見て分かること）。**A（棚卸し）には入りません**
- **ID は `ROADMAP.md` の予約表から**（**111 以降はブロックとアイテムで 1 本の番号列**）
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に 1 件だけ赤 →
走らせ直したら緑。**この周は緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL が動かない」は開発者の手元の devcontainer の話です）。
**クリエイティブの一覧を撮る手**は `docs/browser-shots/README.md` の 12・14 枚目
（`#mode` をクリックしてから `E`、そのあと `#creative` の `scrollTop` を最後まで送る）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
