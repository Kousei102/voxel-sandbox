# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm run build` は通ります。**`npm test` 2332 件すべて成功**
  （**前の周から +25 件** —— この周で足した「大きいチェスト」の節のぶん）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1375 行 / 上限 1500**（この周で +2 行。コメントだけ）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** ——
  この周は ID を 0 個も使っていない）
- **`AUTODEV-SPEC.md` は `状態: 済`**、`AUTODEV-QUEUE.md` の未着手は **8 件**
  → **次の周は B（仕様）**（先頭は「3. 道具の耐久値」）
- **`RULES-INBOX.md` に未取り込みが 2 件**（`mobs.md` と `projectiles.md`）。**取り込むのは人が居る周**

## この周でやったこと（無人・C の周 1 種類だけ）

`AUTODEV.md` の判定表は「キューの未着手が 3 件以上」「仕様書が `未着手`」で **C（実装）** に
当たったので、**`AUTODEV-SPEC.md` の 1 件だけ**を実装しました。コミットは 1 つです。

### 大きいチェスト（隣り合った 2 個で 54 枠）

**ブロック ID もアイテム ID も 1 個も使っていません。** 中心は 2 つです:

- **組かどうかは `chests.ts` の `chestPartner()` がそのつど voxel から決めます** ——
  水平 4 マスに `CHEST` がちょうど 1 個で、**相手から見てもちょうど 1 個**のときだけ組。
  これで `beds.ts` と同じ「相方の相方は自分」が成り立つので、**3 個並びも 2x2 も
  全部 27 枠に戻ります**（本家と違う点。`TUNING.md` に 1 行足しました）
- **54 枠は `Chests.open()` がそのつど作る「参照の並び」**で、新しい入れ物ではありません。
  中身は 1 マス 27 枠のまま Map に残るので、**セーブの形は 1 バイトも変わっていません**
  （`version` は 1 のまま／1 マスにつき 54 要素のキーが 2 つ）。片方を壊すとそのマスのぶんだけ
  落ち、残った側は 27 枠として中身を保ちます

`craftscreen.ts` は `get chestSize()` の 1 本だけ、`inventoryui.ts` は枠を 54 個作って
`craft.chestSize` を超えたぶんに `hidden` を付けるだけ（**27 / 54 を書いていません**）、
`main.ts` は `chests.open(world, ...)` の **1 行だけ**です。
`addToChest()` の枠数を `state.slots.length` にしたのが効いていて、ここを 27 のままにすると
**組の後ろ 27 枠に 1 個も入りません**（シフトクリックもかき集めもここを通ります）。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶん（チェストを 2 個、横に隣り合わせて置いてから開く）:**

1. **54 枠が 9 列 x 6 段で出ること。** 単体のチェストを開いたときは**下 3 段が消えていて**、
   27 枠（3 段）だけが出ること（余った枠は `hidden` で畳んでいます）
2. **パネルが縦に伸びるので、画面からはみ出していないか**（6 段 + 収納 3 段 + ホットバー）。
   はみ出していたら `style.css` の `.chestrow` / `#chest` の側で詰めてください
3. **3 個を横一列に並べると、どれを開いても 27 枠**に戻ること（本家と違います）

**前の周からの持ち越しが 2 件、まだ人の目を通っていません:**

- **砂・砂利が落ちる周のぶん**（`6209e91`）—— 砂の柱の一番下を掘ると上の砂がその場で詰まり、
  **間に穴や浮いた砂が残らないこと**。**掘っていないのに崖や洞窟の天井が崩れていないこと**
- **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**と
  **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **8 件**（3 件以上あるので A ではない）、
**`AUTODEV-SPEC.md` は `状態: 済`** → **次は B（仕様）の周**です。

- 取るのは**キューの先頭 1 件だけ**（「3. 道具の耐久値」）。**取る前にその 1 件だけ
  コードで数え直すこと**（もう入っていたら消して次の行へ）
- **`src/**` と `test/**` に差分を出さないこと**（B は仕様書 1 枚だけの周です）
- **サブエージェントはこの環境では同じ番の中で結果が返せません。** C の周は
  **親が自分で実装して構いません**（この周がそうしました。`AUTODEV.md` の C-1）

人がやること:

- 上の**ブラウザ確認 3 + 2 件**
- `RULES-INBOX.md` の 2 件の取り込み
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）はまれに 1 件落ちます**（この周も 1 回目だけ
「足音のこもり」が落ち、2 回目以降は緑でした）。**2 回続けて落ちたときだけ退行として扱うこと。**

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
