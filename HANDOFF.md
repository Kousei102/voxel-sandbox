# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-13・AUTODEV 77 のあと）

- `npm test` **3506 件すべて成功**（**+9**。木炭のぶん）
- `npm run typecheck` 緑・`npm run build` 緑。**`npm run bench` は走らせていません**
  （生成もメッシュ化も 1 行も触っていないため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1450 行**（テストの数え方。**±0。1 文字も開いていません**）
- **アイテム 125 種・`MAX_ITEM_ID` 163・111..255 の空きは 92**（**−1**）・
  **1..63 の空きは 9**（±0。55..63）
- **立方体 41 / 非立方体 79**（±0）・**モブ 10 種類**（敵対 5）・**レシピ 66 本**（**+1**）・
  **`FUEL` 9 行 / `SMELTING` 9 行**（+1 / +2）・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 済`（木炭）**・**`AUTODEV-QUEUE.md` 93 行（未着手 8 件）**

## この周でやったこと（AUTODEV 77 の C・クラウドの無人の周）

**判定表は 3 行目**（未着手 9 件で 1 行目は外れ・`AUTODEV-SPEC.md` が `状態: 未着手`）で
**C（実装）の周**でした。取ったのは**キューの 29・木炭**です。

**原木・トウヒの原木をかまどで焼くと木炭（アイテム 163）になり、石炭と同じ 8 個ぶん燃え、
松明も作れます。** 触ったのは `src/` 3 ファイル（`items.ts` / `smelting.ts` / `crafting.ts`）と
`test/` 4 ファイルだけで、**`main.ts` は 1 文字も開いていません**（見込みどおり ±0 行）。

**この周の形の要点 3 つ**（細部は `docs/autodev-log.md` と `ROADMAP.md` の 163 の行）:

- **原木は「焼けるもの」と「燃料」の両方に居る初めての行**です。**`FUEL` の 15 秒は
  書き換えていません** —— 消すと「石炭が無くても木だけで火を回せる」入口が閉じます
- **燃える長さは石炭とまったく同じ 80 秒**なので、`Math.max(...FUEL.values())` は 80 のまま。
  **「石炭がいちばん長持ちする」は 1 文字も触らずに緑**です
- **松明は材料違いのレシピをもう 1 本**で表しました（板が 2 行あるのと同じ形。
  `Recipe` に列は足していません）。**石炭の盤面もテストに残してあります**

**差し戻し 0 回。** `tsc` に 1 度だけ止められました —— `CHARCOAL !== COAL` が **TS2367**
（`rules/testing.md` の既知の罠）。**判定をゆるめずに**「両方が一覧に並んでいて名前も色も別」
へ置き換えています。点検（C-2）は親が `git diff` を読んで自分でやりました。

## 次にやること

**次は B（仕様）の周** —— 未着手が **8 件**なので 1 行目は外れ、
**`AUTODEV-SPEC.md` が `状態: 済`** なので 2 行目に当たります。

- **キューの先頭は `30. 苗木 2 種（オーク・トウヒ）と、植えた木が育つこと`**（ID 2 個）。
  **キューの行が自分で「120 行に収まらなければ 2 件に割ること」と言っています** ——
  「落ちて植わるところまで」と「育って木になる」（24a / 24b・25a / 25b と同じ形）
- **取る前にその 1 件だけコードで数え直すこと**（もう入っているかもしれません）。
  **全部を数え直すのは A の仕事**です
- **次に取る ID は 164 から**（`ROADMAP.md` の予約表。163 は木炭が使いました）
- **`src/**` に 1 行も差分を作らないこと**（B の周の完了条件）

## ブラウザで見てほしいところ

**この周は 1 枚撮りました**（`docs/browser-shots/creative-charcoal.png`。使い捨ての
スクリプトで、`browsershot.mjs` は書き換えていません）。**`Read` で開いて確かめたのは
3 つ**: 一覧の末尾に**「木炭」が 1 枠だけ増えていること**・色が **`rgb(86,71,63)` =
`0x56473f`**（指定どおり）であること・**console のエラー 0 件**。

**人に見てほしいのは木炭の色 1 つです**（数値では決められません）:

- **一覧で「炭」に見えるか。** いちばん近いのは**石炭鉱石とソウルサンドで 24.1**
  （判定は 20）。**石炭（`0x23262b`）からは 64.0 離れている**ので取り違えないはずですが、
  **炭らしい暗い色は一覧でいちばん混んでいる帯**（火打石 `0x3c3733` は 32.8）で、
  素直な `0x403a36` は火打石から 5.8 しかありません。**暗くするほど詰まります**

**持ち越しはすべて前の周のままです。** 下に並べたものは**まだ誰も見ていません**:

- **道具の修理（28）**: **傷んだ道具 2 本を並べたとき、結果スロットに出る「傷の帯」で
  「どれだけ直るか」が読めるか**（帯は残り割合をそのまま幅にしているだけで、
  「＋何回ぶん」は出ません）/ **半分減った道具 2 本が「新品に戻る」のが気前よすぎないか**
  （本家のままの式です。**一番お得な遊び方が「半分減ったら 2 本合わせる」になります**）
- **スケルトン（11）**: **矢が当たる高さ** —— 10m で **足元から 0.27m**（膝より下）です。
  当たり判定には入っていますが、**「当たった感じ」がするか**は遊んでみないと分かりません
  （**間合いを縮めれば高く当たります**。`TUNING.md`）/ **2 秒に 1 発・重み 3** が
  「詰めるか逃げるか」を選ばせる速さか / **間合いを取らず近づいて殴ってくる**
  （本家は後ずさりします）のが変に見えないか / **骨色 `0xcdc8b0`** が一覧で
  「骨」に見えるか（いちばん近いのは砂で 24.6）
- **防具（27b）**: 防具枠の置き場所（盤面と収納のあいだ・横 1 列）が使いやすいか /
  1 文字のラベル（頭・胴・脚・足）で部位が分かるか / **革一式（防具点 7）で 28% 減が
  「固くなった」と感じられるか** / 革の色 4 つが「革」に見えるか（上の 2 つは橙寄りに
  出ます。**変えるなら隔たりを測ってから**）/ **革 24 枚（＝牛 24 頭）で一式**が
  見合う値段か。**⚠ 一覧と防具枠の名前が 2 行に折れて絵に被る件**
  （`style.css` の `.slot .label`。**5 文字の「革のズボン」で出ます**）は**人の判断待ち**です
  —— **この周の絵にもそのまま写っています**
- **フェンス（26a / 26b）**: **`shots/fence.png` に 5 通りが写っていて、面の欠けも
  裏返りもありません**。**手ざわりとして「柵らしく見えるか」**だけ見てください。
  **⚠ フェンスを 52fps 未満で跳び越せる件は残っています** —— 跳躍の余裕は **0.0117**
  （到達は `9.2²/(2×30) + 9.2×dt/2` で、実測 **60fps 1.4883 / 30fps 1.5667 /
  刻みの上限 0.05 で 1.6450**）。**直すなら跳躍の積分**（全ブロックの段差の登り方が
  変わります）**か 1.5 の側**（本家の値から離れます）——**数値では決められないので人の判断です**
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の境目が
  読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
  いちばん近いガラスから 34.3 しか余裕がありません）
- **氷（25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
  **離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります**
  —— 伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは
  承知の上です（24b）
- **その他（まだ誰も見ていません）**: クモの巣の歩き 1/4 / 金のリンゴ `0xf0a800` が
  金インゴット・ブレイズロッドと見分けられるか / 本棚が「本の詰まった棚」に見えるか /
  リンゴの色が赤キノコ・生牛肉と見分けられるか / 葉 200 枚に 1 個が待てる確率か /
  はしごの登り 2.35 m/s / `CANE_GROW_SECONDS = 180` が待てる長さか /
  **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/ 一覧で砂糖が雪・羽根・卵・
  糸と見分けられるか / ボウルとシチューの見分け / 森のキノコの密度 / サボテンの痛さ /
  牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
  クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件**）

**どちらも `Edit` で `rules/` に据えました**（層 3 は 0 件なので `RULES-INBOX.md` は
1 行も増えていません）:

- **`rules/stateful-blocks.md`**: **`SMELTING` と `FUEL` は別の表で、同じものが両方に
  居てよい**（原木がそれ）。**片方を足すときにもう片方を消さないこと**。
  `tickFurnace()` は入力枠と燃料枠を別々に引くので、**両方に居るものを弾く分岐は要りません**。
  **同じ長さの燃料を 2 つ持つときは `Math.max(...FUEL.values())` を見ているテスト**に
  気を配ること（落ちたら**判定ではなく数値のほう**を疑う）
- **`rules/inventory-screen.md`**: **「材料が違うだけの同じレシピ」は `RECIPES` にもう 1 本**
  （`Recipe` に「どちらでもよい」列を足さない）。**形が同じでも重複判定は
  `shape + key` を突き合わせる**ので赤くなりません。**足したら両方の盤面をテストに残すこと**

**前の周が据えた `rules/testing.md` の TS2367 の罠が、この周でそのまま効きました** ——
`CHARCOAL !== COAL` と書いて `tsc` に止められ、**判定をゆるめずに意味のある判定へ
置き換える**という直し方も、あの節に書いてあるとおりでした。

## 人がやること

- **⚠ 設計の判断（49 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（テストの数え方）で、**`AUTODEV.md` の「止まって人を呼ぶ条件」の
  2 番にちょうど並んでいます。** ハードの上限は 1500 行です。
  **待っているのは 2 件だけ**です（**35 骨粉**・`use.ts` の 19 通り目 /
  **24b ケーキをかじる**・最低 7 行）。**キューの上 5 件（30..34）はどれも
  `main.ts` 0 行の見込み**なので、**判断が下りなくても 5 周は止まりません**
  （28 の修理も 29 の木炭も、予想どおり ±0 行で済みました）。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（傷を持たないので、
  革一式を 1 度作れば永久に 28% 減のままです）。**本家は減ります。**
  足すなら `ARMORS` に回数の列と「殴られたら減る」5 つ目の減り方が要り、
  **それだけで 1 周**です。**修理の側はもう待っています** —— `findRepair()` は
  `wearable()` で書いてあるので、**防具が傷を持った日に 0 行で修理できるようになります**
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）
- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 77 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` で数え直すこと** —— ここに書いた数は古くなります）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を押してクリエイティブにし、`E` を押して末尾までスクロールしてから撮ること**
（手順は `docs/browser-shots/README.md` の末尾。**この周もその手で 1 枚撮りました**）。
**修理の帯を絵で見たいときも同じで、空の盤面には何も写りません** ——
傷んだ道具を 2 本用意する手順が別に要ります。

**⚠ `npm run shot -- mobs` は 10 種類を 5m 間隔の一列で撮るので、1 体 1 体は小さくしか
写りません**（span が 45m まで伸びました）。**形を確かめるなら、その場面をそのまま
使わずに寄って撮ること**。
