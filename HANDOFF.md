# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-09・AUTODEV 67 のあと）

- `npm test` **3372 件すべて成功**（**±0**。**この周は `src/**` も `test/**` も
  1 文字も触っていません**）・`npm run typecheck` 緑
- **`npm run build` も `npm run bench` も走らせていません**（B の周なので `src/**` に差分なし）
- **音の一群はこの周は 1 度も跳ねませんでした**（`test/audio.test.ts` を 3 回。
  揺れる件は `rules/testing.md`）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1450 行**（テストの数え方。`wc -l` では 1449。**±0。この周は読んだだけ**）
- **アイテム 119 種・`MAX_ITEM_ID` 157・111..255 の空きは 98・1..63 の空きは 9**（すべて ±0）
- **立方体 41 / 非立方体 79**・**モブ 9 種類**・**レシピ 61 本**・**バイオーム 11**（すべて ±0）
- **`AUTODEV-SPEC.md` は `状態: 未着手`（118 行）**・**`AUTODEV-QUEUE.md` 134 行**（未着手 **5 件**）

## この周でやったこと（AUTODEV 67 の B・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 済`」で 2 行目 = B（仕様）の周**でした
（未着手 4 件なので A ではありません）。**キューの先頭 27b（革の防具 4 部位）の
仕様書を書きました。** `src/**` にも `test/**` にも差分はありません。

**27b は 27b-1 / 27b-2 に割りました**（120 行に収まらないため。`AUTODEV.md` の B の末尾）:

- **27b-1（この仕様書）: 作れて、着たまま残る** —— `items.ts` の `ARMORS` 4 行
  （点数は本家のまま **1 / 3 / 2 / 1 = 合計 7**）/ レシピ 4 本（革 **5 / 8 / 7 / 4**・
  4 本とも 3 幅）/ **防具のセーブ**（`SaveData.armor` の 8 要素・省略可）/ **ID 158..161**
- **27b-2: 画面に出て、着ると固くなる** —— `craftscreen.ts` の `"armor"` 枠・
  `inventoryui.ts` と `index.html` の枠 4 つ・`vitals.armor` へ点を貼る 1 行

**割れ目を「画面」にした理由**: 逆順（先に着る経路）だと、**セーブが入るまでのあいだ
着た防具がリロードで黙って消えます** —— 公開サイトに出る順として、
**先に残るようにしてから開ける**こと。

### `main.ts` の 1 行問題は解けました（**この周の一番の収穫**）

**前の周までの「27b はセーブで `main.ts` に +1 行要る（＝停止条件 2）」は覆りました。**
`currentSave()` はいま **`inventory: inventory.serialize(),` と
`wear: inventory.serializeWear(),` の 2 行**で並べていますが、**器そのものを 1 行で渡し、
どのキーを載せるかを `session.ts`（`buildSave()`）が決める形**にすると、
**`armor` を足しても `main.ts` は −1 行**（1449 → 1448・`wc -l`）です。

**読み戻す `applyRestore()` は最初から器（`targets.inventory`）を受け取っている**ので、
**書き出しの側が揃うだけ** —— 行数合わせではなく判断の移動です。
**この −1 行が 27b-2 の +1 行（`vitals.armor`）を先に払っています。**

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が **`状態: 未着手`** なので判定表の 3 行目です。
**`AUTODEV-SPEC.md` を全文渡すこと**（要約しないこと）。

- **ID は `ROADMAP.md` の予約表から 158 / 159 / 160 / 161 の 4 個**。**鉄・金・ダイヤは取らない**
- **`craftscreen.ts` / `inventoryui.ts` / `index.html` / `vitals.ts` は 1 文字も触らない**（27b-2）
- **色 4 つが難所です** —— 革 `0xa06a41` がいちばん近い相手になります。
  **互いとも既存とも RGB の隔たり 20 以上**（`Math.hypot`）。明るさで 4 段に割ること
- **未着手 5 件。27b-1 を実装して消すと 4 件**です（**3 件を切ったら次は A の周**）

## ブラウザで見てほしいところ

**この周は `src/**` を 1 文字も触っていないので、新しく撮った絵はありません。**
下は前の周までの持ち越しで、**どれもまだ人の目が要ります**。

**持ち越し（フェンス 26a / 26b）**: **`shots/fence.png` に 5 通りが写っていて、
面の欠けも裏返りもありません**（26b で撮り直し済み）。**手ざわりとして「柵らしく
見えるか」**だけ見てください。**⚠ フェンスを 52fps 未満で跳び越せる件は残っています** ——
跳躍の余裕は **0.0117 しかありません**（到達は `9.2²/(2×30) + 9.2×dt/2` で、実測
**60fps 1.4883 / 30fps 1.5667 / 刻みの上限 0.05 で 1.6450**）。**直すなら跳躍の積分**
（全ブロックの段差の登り方が変わります）**か 1.5 の側**（本家の値から離れます）——
**数値では決められないので人の判断です。** **当たり判定はマスいっぱいのままで、
腕の無い側にも「見えない壁」があります**（理由は `rules/blocks-shapes.md`）/
**色 `0x988a5e` が「木の柵」に見えるか** / **棒 6 本で 2 個が牧場 1 区画に見合う値段か**。

**持ち越し（凍った海 25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の
境目が読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
いちばん近いガラスから 34.3 しか余裕がありません）。**気になるなら人が色を決めてください** /
**氷の板の上を歩けることが見て分かるか** / **氷ごしに海の底が見えるか**（`alpha` 0.6）。

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
（理由は `TUNING.md`）。**凍った海が入ったので実地で試せます。**

**持ち越し（ケーキ）**: 上面 `0xffd0e4` が「ケーキ」に見えるか / 側面 `0xe8c9a0` と
下面 `0xd9b98a` の 3 色がケーキに読めるか。**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: 中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか /
素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / 森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ /
牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
**一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` に据えるものはありません。** B の周で `src/**` を触っていないので、
落とし穴を踏む場所がありませんでした。分かったこと（`main.ts` の −1 行）は
**そのタスクの話**なので `AUTODEV-QUEUE.md` の ⚠ の節に書いてあります ——
`rules/` は「そのファイルを触る人が毎回読むもの」なので、1 タスクの見積もりは置きません。

## 人がやること

- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **設計の判断（40 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1450 行**
  （テストの数え方）で、上限 1500 まで 50 行。**27b-1 で 1 行減り、27b-2 で 1 行戻ります**
  ので、**この 2 周は止まりません** —— **待っているのは 24b（ケーキをかじる。最低 7 行）**です。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
  （**判定の意味が変わるので無人の周では触れません**）
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 50 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
**自然生成しないブロックは browsershot には 1 マスも写りません** ——
氷・ケーキ・クモの巣・本棚・フェンスは `npm run shot -- <名前>` が唯一の経路です。
**27b-1 の 4 色は一覧に出るだけ**なので、**`node tools/browsershot.mjs` の
インベントリの 1 枚**が見る場所です（`tools/shot.ts` の場面は要りません）。

**`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
**`wc -l` を 119 以下に収めること**（`rules/testing.md`。**いま 118 行**）。
