# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-26）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。無人で回すうえで
  実際に踏んだものが 3 つあります:
  **(1)** **`.claude/rules/**` を編集すると「sensitive file」の確認で止まります。**
  → **`RULES-INBOX.md` に書いて、人が居る周が取り込む**形にしました（`LOOP.md` の「4.5」）。
  **`.claude/settings.json` に許可を書いても消えません**（2026-08-25 に 2 周とも実測）。
  **(2)** クラウドから `origin` へ push できず 403 → ユーザーが GitHub 側で権限を付け直し済み。
  **(3)** **5 時間の利用制限。** 当たった周は実装を終えた直後に止まり、次の定期実行も
  同じセッションに投げられて即終了しました（**新しい周にならず、1 回ぶんの枠が消えます**）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1929 件すべて成功**（1911 → 1929。この周の 18 件はエンドの黒曜石の柱）
- **進行（クリア導線）達成 12 / 13**（12 件とも本物。**仮は残り 1 件** ——
  「エンダードラゴンを倒せる」だけ）。**この周は進行を動かしていません**
  （柱はクリア導線の項目ではありません）
- ブロック ID **1..63 の空きは 10 個**（この周は 1 つも使っていません。柱は黒曜石 43 です）。
  向き違いは **110 まで**、アイテム ID は **92 まで**（どちらも増えていません）
- `src/main.ts` は **1449 行 / 上限 1500**（余白 51 行。**この周は 1 行も触っていません**）。
  **`main.ts` の次元の分岐は 0 件**（上限 2）

## 直前にやったこと — TASKS 2-11a（エンドの黒曜石の柱）

**エンドの島に黒曜石の柱が 10 本立ちました。** **2-11 を 2 つに割っています**（下の
「なぜ割ったか」）。**新しいブロックも新しいファイルも増えていません。**

| どこ | 何が入ったか |
| --- | --- |
| `endgen.ts` | **柱の表 `PILLARS`**（10 本・種に依らない）と `pillarTopAt()` / `EDGE_MIN_REACH` / `NO_PILLAR`。`shapeAt()` が `pillar` も返し、`blockAt()` が 4 つ目の引数を取るようになった |
| `test/endgen.test.ts` | 柱の節（+18 件）。既存の助っ人（`voxel` / `surfaceOf`）に `shape.pillar` を渡す形へ。`topSolidOf()` を足した |
| `TASKS.md` | 2-11 を **2-11a（済み）/ 2-11b（クリスタル）**に割った。2-13 の依存を 2-11b へ |
| `REVIEW.md` / `TUNING.md` / `RULES-INBOX.md` | 確認項目 6 個 / 数値 7 行 / 決まりごと 1 件 |

### なぜ 2-11 を割ったか

**柱は地形（`endgen.ts` に足すだけ）ですが、エンドクリスタルは「湧かない・歩かない・
消えない個体」という新しい形が要ります。** 1 コミットには収まりません（`LOOP.md` の「2」）。
**2-11b で先に決めることが 1 つあります** —— 下の「次に取るもの」を読んでください。

### 決めたこと

- **`structures.ts` の器には乗せませんでした。** あちらは `SiteRule`（グリッドに確率で撒く）で、
  **エンドの柱は「原点のまわりの決まった場所に必ず 10 本」**という別の性質のものです。
  島の中心が原点だと分かっているので、`endgen.ts` の中で直に建てるほうが素直でした
  （前の周の `HANDOFF.md` が「どちらにするか先に決めること」と書いていた点への答えです）
- **位置も高さも種に依りません**（`PILLARS` は `seed` を受け取りません）。
  ドラゴンの戦場（2-13）は誰のワールドでも同じ形になります。
  輪の半径 **28** / **10 本** / 高さ **12〜39**（上面 y60〜87）/ 太さ **2〜3**
- **柱を立ててよい半径は「式」で決めました**（`EDGE_MIN_REACH` = `ISLAND_RADIUS * (1 - EDGE_WOBBLE)`
  = 36）。`reachAt()` の揺れは `(n / 1.2) * 2 * EDGE_WOBBLE` で、`fbm2` の実効レンジが ±0.6 なので
  揺れは `±EDGE_WOBBLE` に収まります。**実測した最小のふち（種 12345 で 39.1）で線を引くと、
  別の種で虚空に柱が浮きます**（2-7 で踏んだ「総当りとの突き合わせでは守れない」と同じ筋で、
  測った値ではなく理屈のほうを判定に置いてあります）
- **柱の上面は絶対の高さで止めます**（`ISLAND_SURFACE + height`）。上面が平らになり、
  島のうねり（±3）は根元で吸収されるので、**根元に隙間ができません。**
  **上に 2 マス空けてある**ので、クリスタル（2-11b）はそのまま載ります
- **虚空の列には柱を置きません。** 保険は **`blockAt()` の `VOID_COLUMN` の 1 行だけ**で、
  `pillarTopAt()` は種を受け取らない＝島の形を知りません（知りようがありません）
- **走査を早く切る帯（`PILLAR_NEAR` / `PILLAR_FAR`）は実際の柱の中心から出しています。**
  `PILLAR_RING ± PILLAR_MAX_RADIUS` で決め打ちにすると、中心をマスに丸めてあるぶんだけ
  **柱のふちが 1 マス欠けます**（実際にわざと壊して 4 マス欠けることを見ました）

### 確かめたこと

- **わざと壊して 7 通りとも赤くなることを確かめました**: 帯を決め打ちに → 3 件 /
  虚空の列にも柱を置く → 1 件 / 輪を島のふちの外（40）へ → 3 件 /
  高さを列ごとの上面から測る → 4 件 / `generateChunk` が柱を渡さない → 1 件 /
  高さを昇順に並べる → 1 件 / 輪を出る所（4）に重ねる → 6 件
- `endgen.ts` はチャンク生成の中なので `npm run bench` を 3 回:
  `generateChunk` **1.54 / 1.44 / 1.43ms**（中央 1.44）。**2-10 の中央 1.42 と誤差の中**で、
  そもそも**オーバーワールドの生成器は 1 行も触っていません。**
  **エンドの生成そのものは 0.115ms**（2-10 の 0.116ms から動いていません。
  `test/endgen.test.ts` が毎回オーバーワールドと同じ土俵で測って出します）

### 前の周（2-1 〜 2-10）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージは書かず、撃った側が
  `Shot.damage` に載せる**ので、2-12 の弓矢は撃つ側を足すだけで当たる側が要らない）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ）
- **2-7**: 投げたエンダーアイが要塞の方を向く（`stronghold.ts` 新規）
- **2-8**: 要塞（石レンガ 53 + エンドポータルの部屋 + 枠 51。地面から 18 マス下）
- **2-9**: エンドポータルの起動（`endportal.ts` 新規。枠 12 個にアイ）
- **2-10**: エンド次元（`endgen.ts` 新規。エンドストーン 50 の島 1 つ。**達成 12 / 13**）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 /
  2-8 / 2-9 / 2-10 / 2-11a の 15 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（これまで 8 度）。
   **残る仮は 1 件**（「エンダードラゴンを倒せる」）で、
   `sourceHas("src/mobs.ts", "dragon")` なので**同じ形で化けます** ——
   `MobDef` に `dragon` を書いた瞬間に 13 / 13 になります。**気を付けてください。**
   **2-11b でクリスタルをモブ側に乗せるなら、ここに触れる可能性があります**
   （`mobs.ts` に `dragon` という語を書かないこと。書くなら同じ周で本物に差し替えること）
2. **場所の限界は「実測した最小値」ではなく「式」で決めること**（この周）。
   ふちの最小半径は種 12345 で 39.1 でしたが、式の上では 36 まで縮みます。
   実測で線を引くと、**別の種でだけ柱が虚空に浮きます**（しかも遠いので気付けません）。
3. **「早く切る帯」は、実際の値から出すこと**（この周）。柱の中心はマスに丸めてあるので
   `PILLAR_RING ± 半径` は帯として狭すぎ、**柱のふちが 1 マス欠けます。**
4. **「面積で形を測る」判定は真円を通します**（2-10）。**形そのものを測ること。**
5. **座標の移し方は次元ごとに違います**（2-10）。ネザーの 1:8 をエンドに写すと、
   遠くの要塞から入った人だけが虚空に出ます（近くから試すと動いて見えます）。
6. **「探して無ければ建てる」を別の次元に使い回さないこと**（2-10）。
   エンドで `arrive()` を通すと、島の真ん中に黒曜石の枠が生えます。
7. **同じ向きが並ぶ列から中心を逆算しないこと**（2-9）。
8. **手で並べた試験場は、建てる側との食い違いを見つけられません**（2-9）。
9. **構造物の `extent` は「どこに掛かるか」の唯一の材料です**（2-8）。
10. **地面から潜る構造物の深さは、地形の最低高さで頭打ちになります**（2-8）。
11. **1 チャンクに収まった標本だけを見ていると、端の欠けを見逃します**（2-8）。
12. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（2-7）。
13. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字まで。
14. **探す半径は「正方形」であって「円」ではありません**（2-5）。
15. **試験場のプレイヤーは、跳ぶモブでは `HOSTILE_SIGHT`(18) より遠くに置くこと**（2-5）。
16. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）。
17. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の火球で焼け死にます。
18. **相手を先に、ブロックをあとに見ること**（2-4b）。
19. **飛ぶモブには `ACCEL_GROUND` を使うこと**。
20. **`world.seed` をセーブや場所の計算に使わないこと**（次元では塩を混ぜたあとの値）。
    種は `worldSeed`。
21. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）。
22. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）。
23. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）。
24. **わざと壊して、テストが赤くなるか確かめること。** この周は 7 通りとも確認済みです。
25. **`test/audio.test.ts` の「声色（音程の倍率）が波形に出る」が 1 度だけ落ちました**
    （2-8 の周の途中で 1 回。`0.329 > 4.161` という出方）。**その後 3 周とも再現せず**、
    実装は 1 行も触っていません。**もう一度出たら、そのときは本気で見てください**
    （`node-web-audio-api` のレンダリングが揺れているのだと思われます）。

## 次に取るもの

1. **2-11b（エンドクリスタル）**。**柱はもう立ちます。**
   - **柱の並びは `endgen.ts` の `PILLARS` から引くこと**（写さない）。載せる高さは
     **`Pillar.top + 1`**（柱の上は 2 マス空けてあり、テストが 5 種 x 10 本で見ています）
   - **先に決めることが 1 つあります: モブ側に乗せるか、新しい器（`crystals.ts`）にするか。**
     - **モブ側**（`mobs.ts` の表に 1 行）なら、**2-12 の矢が当たる仕掛けを 1 行も
       書かずに済みます**（`hitByProjectile()` が既にあるため）。そのかわり
       **湧き・歩き・デスポーンの 3 つを止める**必要があり、**モブは保存しないので
       壊したクリスタルが読み込み直しで戻ります**（`mobs.populate()`）
     - **新しい器**なら「壊した／壊していない」をセーブに持てます（`SaveData` に
       省略可のキーを足すのは可。**`version` は 1 のまま**）。そのかわり
       **当たり判定を自分で書くことになります**
     - **ドラゴンの回復（2-13）が「クリスタルが生きているか」に直に効く**ので、
       ここは 2-13 の設計と一緒に決めてください
   - **ブロック ID の空きは 1..63 に 10 個**。予約表（`ROADMAP.md`）から取ること
2. そのあとは並びどおり 2-12 → 2-13（ドラゴン）。
   **2-12 の弓矢は、撃つ側（弓とアイテム）を足すだけで当たる側が要りません**
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 4 件**）/ `REVIEW.md` の一括確認
   （**いま 15 節**）/ `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **柱の上にはまだ何も載っていません**（クリスタルは 2-11b）。
- **柱は掘れます**（黒曜石なのでダイヤのツルハシで 9.4 秒／マス）。本家も同じです。
- **柱の足元にモブが湧くかどうかは確かめられていません。** 黒曜石が空を遮るので
  真下のスカイライトは下がりますが、計算では太い柱（半径 3）の真ん中でも 12 前後 →
  湧きの明るさ 8.4 で、上限 7 をまだ超えます（＝湧かないはず）。
  **`REVIEW.md` の「2-11a」の 6 番で見てもらいます。**
- **エンドからは戻れません**（本家と同じ。出口は 2-13 でドラゴンを倒すと開く想定）。
  **落ちて死ねばリスポーン地点へ戻る**ので詰みにはなりませんが、
  **持ち物は 5 分で消えます**（虚空に落ちたぶんはその場で消えます）
- **エンドの島の上にはモブが 1 体も湧きません**（上の項目のとおり、柱の影を入れても）。
  天井が無いのでスカイライトは全開で、空の明るさが 0.7 固定なので `spawnLight` が 10.5 に
  なり、敵対の上限 7 を超えます（受動は地面が草でないと湧きません）。
  **掘った穴の中は暗いので湧きます**（オーバーワールドの洞窟と同じ）
- **エンドの島は 1 つだけです**（本家は外側にも小さい島がたくさんあります）
- **本家の「星空」の見た目は出せません**（テクスチャが無いので暗い紫の無地）
- **要塞は「部屋 1 つ」だけです**（本家は通路・図書室・階段・シルバーフィッシュ）
- **要塞の中に入る道がありません**（掘って降りる前提）
- **投げたエンダーアイは、近づいても落ちません**（本家は近いと真下へ落ちる）
- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  `eyeShot()` に次元を渡して「オーバーワールド以外では null」にするのが素直です
- **投げたアイは地面に落ちません**（`projectiles.onExpire` で `drops` に渡すところから）
- **アイを投げても減りません**（本家は 20% で壊れる。`TUNING.md` の 2-7）
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒。`smelting.ts` に 1 行）
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方を全部見直すことになるので、**独立したタスクとして決めてください**
- **エンダーマンは日陰へ逃げません**し、**ブロックを持ち運びません**
- **ネザーでも寝られます**（本家はベッドが爆発する）
- **寝ても時間は「進みません」**（時計だけ飛びます）
- **火球は火を点けません**（火そのものが無いので、ブロックとして足すところから）
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）
- **`F3` の `biome` はネザーでもエンドでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）
- **要塞の部屋の中にも敵対モブは湧きます**（石レンガは `spawnOn` を持たないため）
