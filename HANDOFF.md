# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- **この周は C（実装）**。`npm run typecheck` / **`npm test` 2707 件すべて成功**（2672 → +35）/
  `npm run build` も通りました
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1445 行 / 上限 1500**（1433 から **+12 行**。1450 を超えると停止条件）
- **ブロック / アイテム ID: 1..63 の空き 9（±0）/ 111..255 の空き 133**（135 から **-2**）
- **アイテムは 88 種**（87 から +1）、`MAX_ITEM_ID` は **122**
- **`AUTODEV-SPEC.md` は `状態: 済`** → **次の周は B（仕様）**（下の「次にやること」）
- **`RULES-INBOX.md` は未取り込み 10 件**（前の 2 周ぶんの 7 件 + **この周の 3 件**）

## この周でやったこと（無人・C の周 1 種類だけ）

判定表どおり（キューの未着手が 3 件以上・`AUTODEV-SPEC.md` が `状態: 未着手`）→ **C の周**。
**仕様書「小麦の種と苗（耕地に植える）」をそのまま実装しました。**

**草むらを壊すと 12.5% で小麦の種（アイテム 122）が出て、種を持って耕地を右クリックすると
その上に小麦の苗（ブロック 121）が立ちます。** 苗を掘ると種が 1 個戻り、**下の耕地を掘ると
苗も一緒に壊れて種が落ちます**（`supportFace: FACE_YN`）。**まだ育ちません。**

- `blocks.ts` に `WHEAT_CROP = 121` / `items.ts` に `WHEAT_SEEDS = 122` と `isSeed()` /
  `use.ts` に `plant` の注文（**`till` の次・バケツより前**。これで 14 通り）/
  `placing.ts` に `tryPlant()`（`tryTill()` を写した形）/ `main.ts` は `plantAt()` の配線だけ
- **セーブは 1 バイトも増えていません**（`SaveData.version` は 1 のまま。苗は `edits` に乗る
  ただのブロックで、`storage.ts` / `session.ts` / `dimensions.ts` は 1 行も触っていません）
- **既存のテストを 1 件だけ書き直しました**（`test/blocks.test.ts` の「草むらを壊すと自分が
  手に入る」）。**消さずに `rollDrop()` の 2 件へ割って増やしてあります** —— 外れたら草むら、
  12.5% で種、境目（0.124 / 0.125）まで見ています。ゆるめた判定は 1 つもありません

**次の周が迷いそうな所を 1 つ**: **苗は `variantOf` を自分自身に向けてあります。**
アイテムを作らせない入口が `variantOf !== AIR` しか無いのに、苗には大元にできる相手が
居ないためです（土でも耕地でもない）。そのぶん `dropOf()` の既定が**アイテムの無い 121**を
返すので、**`DROPS` の「種 1 個」の 1 行が必須**でした（書き忘れると拾えない山が落ちます。
型でも typecheck でも止まりません）。**`RULES-INBOX.md` に本文を書いてあります。**

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は本物のブラウザで撮って、`Read` で開いて見ました**（`AUTODEV.md` の C-3）。

- `docs/browser-shots/creative-seeds.png` —— **クリエイティブの一覧が 88 枠**になり、
  末尾がクワ 4 本 → **小麦の種**。**苗（121）は一覧に出ません**（`variantOf` が効いている証拠）
- `docs/browser-shots/wheat-crop.png` —— **実際に刈って耕して植えたところ。**
  `#debug` の `target` が **`草むら → 草 → 耕地 → 小麦の苗`** と変わり、`edits 4`。
  **茶色の耕地の上に黄みの緑の十字**が立っています。console のエラーは 2 枚とも無し

**人にしか決められないのはここです**: **苗の緑（`0x6f8f3f`）が草原の緑（`0x5e9c41`）に
紛れます。** 耕地の茶色の上でははっきり見えますが、**遠目には草むらと見分けが付きません。**
育つ段階（`WHEAT_CROP_RIPE`）を足す周に **2 色まとめて決め直すのが早い**と思います
（`TUNING.md` の「小麦の種と苗」）。**前の周から持ち越しの「耕地の上面の色」は、
苗が上に立ったことで見分けが付くようになりました**（`wheat-crop.png` で確認できます）。

## 人がやること

- **`RULES-INBOX.md` の 10 件の取り込み**。**わざと食い違わせてある組**（「3 種類」対
  「4 種類」/「12 通り」対「**14 通り**」）は **新しいほうを採ってください。**
  41 行目あたりの「未取り込みは 0 件です。」は古い行で、**末尾の「未取り込み 10 件」が正しい数**です
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**は
  まだ直っていません（`#status` は `bottom: 118px` 固定。`style.css:674`）。`REVIEW.md` に積んであります
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手は 6 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 済`**
なので → **次は B（仕様）の周**です。**`src/**` と `test/**` に 1 行も触らないこと。**

- **先頭は「13. 育つ器（`crops.ts`）と小麦・パン」**（今回の後半）。**依存の 12 は済みました。**
  ここで初めて **`SaveData` の省略可キー 1 つ（`crops`）と `DimensionState` の 1 行**が要ります
  —— **`version` は 1 のまま**で、`AUTODEV.md` の停止条件 5 に当たるかどうかを
  仕様書の中で判断してください（省略可のキーを足すのは可）
- **育ち具合は位置ごとの状態**（`furnaces.ts` の器の形）で持ち、実ったことは
  **もう 1 つのブロック ID**（`WHEAT_CROP_RIPE`。`variantOf: WHEAT_CROP`）で見せる形を
  今回の仕様書の末尾に書いてあります。**次の番号は 123 から**（`ROADMAP.md` の予約表）
- **120 行に収まらないなら、その場で 2 件に割って前半だけを仕様書にすること**（B の決まり）

**サブエージェントについて（この周の実測）**: **1 体も使っていません。**
`AUTODEV.md` の C-1 は「同じ番の中で返らないなら自分で実装すること」で、前の周の記録では
`run_in_background: false` を渡しても背景で起動して 13 分 23 秒待つことになっていました。
**親が直接書いたほうが安く、周を落とす危険もありません。** 点検（C-2）も親が `git diff` を
全部読み、`npm run typecheck` / `npm test` / `npm run build` を自分で走らせています。

**ブラウザの撮り方で新しく分かったこと 2 つ**（`docs/browser-shots/README.md` の末尾）:
**`page.mouse.move()` では視点が動きません**（`movementX/Y` が出ないので、`mousemove` を
合成して投げること）。**一覧からホットバーへ入れるなら `Shift`+クリック**（掴んで置くと
入れ替えになり、次に一覧を押した 1 回が「捨てる」に化けます）。

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（緑）。
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
