# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-16・AUTODEV 89（C の周）のあと）

**この周は C（実装）の周**で、**キューの 33a「鉄の防具 4 部位」を入れました**
（判定表の 3 行目 —— `AUTODEV-SPEC.md` が `状態: 未着手` だったため）。

- **鉄の防具 4 部位（帽子 171 / 上着 172 / ズボン 173 / 靴 174）。**
  **点数は本家のまま 2 / 6 / 5 / 2 = 合計 15**（`items.ts` の `ARMORS` に 4 行）で、
  `armorReduced()` に通すと **60% 減**。**革一式 7 点 = 28% 減は動いていません**
- **`inventory.ts` も `vitals.ts` も `main.ts` も `durability.ts` も `craftscreen.ts` も ±0 行**
  でした（仕様書の見込みどおり）—— **`armorPoints` は `armorOf()` に聞くだけで材質を
  1 つも知らない**ので、表に 4 行足すだけで鉄がそのまま効きます。
  **`ARMOR_CAP`(20) / `ARMOR_DENOM`(25) にも触っていません**（15 点は上限に当たらない）
- **`crafting.ts` は `armorRecipes()` 1 本になりました**（`toolRecipes()` と同じ作法）。
  **革 4 本をベタ書きからここへ移し**、鉄は呼び出し 1 行。**出来上がるレシピは
  革も鉄も本家のまま**で、**革の名前・形・枚数・出目は 1 つも変わっていません**
- **色は「鋼の青」をこの周に測り直して、B の周と同じ値のまま通りました**（ずらさずに済みました）
- **サブエージェントは立てていません** —— この harness の `Agent` は既定で背景に出る形で、
  `AUTODEV.md` の C-1 が禁じている呼び方にしかならないためです。**仕様書が 118 行と
  小さかったので自分で実装し、C-2 の点検も自分で `git diff` を読みました**
  （`AUTODEV.md` の「返らないなら自分で実装すること」）。**差し戻し 0 回**

## 数字（2026-09-16・この周で測ったもの）

- `npm test` **3656 件すべて成功**（3636 から +20）。`npm run typecheck` 緑 /
  `npm run build` 緑（`src/**` を触ったので走らせました）
  - **⚠ 音が 1 回目に跳ねました**（足音の**羊毛 10.048 / 雪 9.491 が同時**）。
    `rules/testing.md` の既知の揺れで、**走り直して緑**（0.552 / 0.716 / 2.235 に
    戻りました）。**判定はゆるめていません**
  - **⚠ `tsc` が予告どおり 2 か所で止まりました**（TS2367）—— `MAX_ITEM_ID` を
    170 → 174 へ移すと、突き合わせている `test/blocks.test.ts` と
    `test/items.test.ts` が落ちます。**比べる相手のほうを直しました**（型で止まる安全な罠）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1450 行 / 上限 1500**（テストの数え方。`wc -l` では 1449。**1 文字も開いていません**）
- **アイテム 132 → 136 種**・**`MAX_ITEM_ID` 170 → 174**・**レシピ 70 → 74 本**
  （**革 4 本は書き方が変わっただけ**なので本数に入りません）
- **1..63 の空きは 7**（57..63・±0）・**111..255 の空きは 85 → 81**（**次に取るのは 175**）
- **立方体 42 / 非立方体 85**（どちらも ±0。**ブロックは 1 つも増えていません**）
- **`AUTODEV-SPEC.md` は `状態: 済`・118 行**・**`AUTODEV-QUEUE.md` 未着手 5 件・117 行**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` で、未着手が 5 件あるので
判定表の 2 行目です（1 行目は未着手 3 件以上なので外れます）。
**キューの先頭は 33b「金・ダイヤの防具 8 部位」**。要点だけ:

- **ID は 8 個・175 から**（`ROADMAP.md` の予約表。取ると **111..255 の空きが 81 → 73**）
- **`crafting.ts` は `armorRecipes()` の呼び出し 2 行だけ**（33a で関数に畳みました）。
  **`items.ts` に 8 行 ＋ `ARMORS` に 8 行 ＋ `MAX_ITEM_ID`**。**`main.ts` は 0 行の見込み**
- **点数は本家のまま**（金 2 / 5 / 3 / 1 = 11・ダイヤ 3 / 8 / 6 / 3 = 20）。
  **⚠ ダイヤ一式 20 点は `ARMOR_CAP`(20) にちょうど当たります** —— 33a と違って
  **上限の話が出るので、B の周で必ず 1 節を割くこと**（頭打ちが正しいのか、
  それとも `ARMOR_CAP` を触らずに 80% 減で止めるのか）
- **テストの数え方はもう覚えています** —— 33a で「材質ごとに 4 部位」＋「部位ごとに
  材質の数だけ」の 2 本にしたので、**材質が 4 つになっても `2` を `4` に直すだけ**です
- **色は測り済み**（`AUTODEV-QUEUE.md` の ⚠ に金 4 色・ダイヤ 4 色）。**取るときに
  測り直すこと**（33a は同じ値のまま通りました）
- **防具に耐久値を足さないこと**（下の「人がやること」の判断待ち）

## ブラウザで見てほしいところ

**この周は 1 枚撮って見ました**（`docs/browser-shots/creative-iron-armor.png`）。
**枠 132 → 136・末尾に鉄 4 部位・`.swatch` は指定どおり・名前の重複 0 件・
console のエラー 0 件**。**革 4 部位（橙茶）と鉄 4 部位（青灰）は絵の上でも
取り違えません**（測った隔たり 118.8）。**`npm run shot` は要りませんでした** ——
新しいブロックが 0 個で、どの場面にも 1 マスも増えないためです。

**人に見てほしいのはここです**（上が新しい分）:

- **⚠ 鉄の防具の色（33a・入りました）**: **「鋼の青」が鉄に見えるか。**
  **素直な銀灰は測って通らないと分かっています**（一覧の無彩色は既存 46 個で埋まって
  いて、いちばん遠くて 20.1〜22.1。判定は 20）。**帽子 `0xcfcfff` はとくに淡い青紫寄り**です。
  **ずらすなら `TUNING.md` の値を動かすだけ**（テストの判定はゆるめないこと）
- **⚠ 鉄一式で 60% 減が「固くなった」と感じられるか。** **防具は壊れない**ので、
  本家より気前がよい状態です（下の「人がやること」）。**鉄一式 24 枚**（鉄鉱石 24 個）が
  渋すぎ／安すぎないかも一緒に
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **5 文字から折れます。** 33a で増えたのは**「鉄のズボン」1 枠だけ**（実測 26px・
  他の 3 つは 13px）。**一覧全体ではもう 51 枠が折れていて**、最長は 9 文字の
  「ネザーレンガハーフ」で 39px = 3 行です。**33b は 6〜7 文字が 8 枠増えます**
- **レンガ（32b）**: **`0xbc5d39` が「焼いたレンガ」に見えるか**と、**一覧で
  レンガブロック（`0xa4553f`）と並んだときに 2 つあると分かるか**（隔たり 26.0）
- **粘土（32a）**: **海に潜って粘土を見つけられるか**（海底の列の 7.2%・4x4 の塊）と、
  **水越しに砂と見分けられるか**。**粘土玉がやや青紫寄り**（`0xa7b4da`）
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）/ **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）/
  **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 1 件**）

- **`rules/items-survival.md`**: **同じ種類のものが 2 つ目の材質を持ったら、
  テストの「N つとも別」を数え直すこと**（**ゆるめるのではありません**）。革だけだった
  防具に鉄を足したら `armors.length === 4` と `new Set(slots).size === 4` が
  **正しく落ちました** —— **材質をまたぐと部位は重なるのが正しい**からです。
  数え方は**「材質ごとに 4 部位」＋「部位ごとに材質の数だけ」の 2 本**にすること
  （**前より強くなります**）。**点数の表も材質ごとに出力してから判定すること**
  （合計だけだと内訳を入れ替えても緑）。**`crafting.ts` は 2 材質目で関数 1 本に畳むこと。**
  あわせて「**測った色は次の周まで持つが、それでも測り直すこと**」も 1 行足しました。

**この周で踏んだ罠**（どちらも既に `rules/` にある件を踏み直しただけです）:

- **`MAX_ITEM_ID` を移すと突き合わせているテストが `tsc` で落ちます**（TS2367・2 か所）。
  **比べる相手を新しい番号へ直すのが正解**で、**型で止まるので安全な罠**です
- **音が 1 回目に跳ねました**（羊毛と雪が同時）。**走り直して緑。判定はゆるめていません**

## 人がやること

- **⚠ 設計の判断（60 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（テストの数え方。`wc -l` では 1449）で、**`AUTODEV.md` の停止条件 2 に
  並んでいます**（上限 1500）。**待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 /
  **24b ケーキをかじる**・最低 7 行）。**キューの上 2 件（33b・34）はどちらも
  `main.ts` 0 行の見込み**なので、判断が下りなくても 2 周は止まりません
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も壊れません**（足すなら
  `vitals.ts` → `inventory.ts` の配線になるので**それだけで 1 周**）。**鉄が入って
  「一式 60% 減が永久に持つ」になったので、前の周より効いています**
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**（33b）。**次の B の周が
  1 節を割きます**が、頭打ちでよいかは手ざわりの判断です
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。
  **もう 51 枠**で、**33b で 8 枠増えます**（「ダイヤのズボン」は 7 文字）
- **⚠ 鉄の防具が「鋼の青」になる件**（上の「ブラウザで見てほしいところ」）。
  **素直な銀灰が通せないのは測った事実**で、**判定をゆるめる選択肢は取りません**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は 2 回中 1 回**
  （足音の羊毛と雪が同時に跳ねる形）。**直すなら測り方**（窓を長く取る・複数回鳴らして
  中央値を見る）で、**判定はゆるめないこと**（`rules/testing.md`）。**キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 89 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押してから撮ること**
（`docs/browser-shots/README.md` の末尾）。**⚠ 送り先は末尾とは限りません** ——
**低帯（1..63）の番号を取ったものは一覧の真ん中に出る**ので、`scrollTop = scrollHeight`
では 1 枠も写りません。**`.label` の文字列で枠を探して
`scrollIntoView({ block: "center" })` を呼ぶこと。**
**⚠ 枠を数えるときは `#creative .slot` を使うこと** —— 素の `.slot` は
**ホットバーとインベントリの 36 枠まで拾って 252 個**になり、名前の重複も嘘の数が出ます
（この周に踏みました）。**33b の 8 部位も 175 以降なので末尾に出ます。**

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**海底の粘土（32a）も場面 `clay` を足しました** —— ただしあれは**原点のまわりを
撮りません**（種 4242 は ±128 の海が全部凍っています。`rules/meshing-render.md`）。
**33a も 33b も新しいブロックが 0 個なので、`npm run shot` は要りません**（防具はアイテムです）。
**⚠ 防具を着た姿は撮れません** —— **一人称視点なので自分の体が写らない**ためで、
装備したときの手ざわりは人に見てもらうしかありません。
