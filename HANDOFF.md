# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- `npm test` **2962 件すべて成功**（2934 から +28）・`npm run typecheck` 緑・
  `npm run build` 通ります。`npm run bench` は走らせていません（生成もメッシュ化も
  1 行も触っていないため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（**1 バイトも増えていません**。
  モブは保存しません）・**`main.ts` 1464 行**（`npm test` の数え方。1 行も触っていません）
- **アイテム 98 種・`MAX_ITEM_ID` 133・111..255 の空きは 122・1..63 の空きは 9**
- **モブ 9 種類**（受動 4 / 敵対 4 / ボス 1）

## ⚠ 次に `main.ts` を触る周がまず見ること: 1464 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
**牛・クモの 3 周とも `main.ts` を 1 行も触っていないので、持ち越しです。**
**次に触る周は、先に判断を別ファイルへ出すこと。**

## この周でやったこと（AUTODEV 21 の C・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 未着手` だったので **C（実装）の周**。当たった 1 種類だけを
やりました。**キューの 9「クモと糸」** を実装して、キューから消してあります。

- **`mobs.ts` の `SPIDER`（`MobDef` 1 つ）と `items.ts` の 2 行だけ**で閉じました。
  **`main.ts` / `mobmesh.ts` / `mobrender.ts` / `crafting.ts` / `drops.ts` / `blocks.ts` は
  1 行も触っていません** —— モブを 1 種類足すのに描画側が 0 行なのは `rules/mobs.md` の
  とおりで、**新しい「確かめられないもの」は 1 つも増えていません**
- **ID は 1 個**（**133 = 糸**）。`ROADMAP.md` の予約表に「実装済み」で足し、
  **予備は 134..255**（122 個）に書き直しました
- **仕様書が読んだとおり 2 件が赤くなり、どちらもゆるめずに数え直しました**（下）
- **`TUNING.md` に「クモ（AUTODEV 21）」の節**（速さ 6.0 / 体力 16 / 重み 100 /
  声 1.5 / 糸 1 個固定 / **朝に燃えること** / 糸に使い道がまだ無いこと）

### 赤くなって数え直した 2 件（`test/mobs.test.ts`）

**`spawnWeight: 100` がゾンビと同じ**なので、**夜の「どこでも」の敵対がゾンビとクモで
半々**になります（石の上で 2000 回抽選して **ゾンビ 49.2% / クモ 46.2% /
エンダーマン 4.7%**）。

1. **「敵対の上限を超えない」→ ゾンビ + クモの合計**で見る形に。夜の草地で
   **ちょうど 13 + 11 = 24 / 24** なので、**ゾンビだけを数えていたら上限が 2 倍に
   ゆるんでも緑のまま**でした
2. **「ゾンビがいちばん出やすい」→「ゾンビとクモがほぼ同数で、エンダーマンはその 1/10」**
   （3 つの実測値を出力してから判定）

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 済`**・`AUTODEV-QUEUE.md` の未着手が **3 件**
（3 件未満ではないので A には入りません）なので → **次は B（仕様）の周**です。
**先頭は 10「弓を本家の形に戻す（羊毛 3 → 糸 3）」** —— **`items.ts` の `STRING`（133）は
もうあるので、依存は解けています**。**終わりに `master` へ push。**

- **10 番は見た目に出ません**（`crafting.ts` の弓 1 行と `test/crafting.test.ts` だけ）。
  **仕様書を書く周が引く `rules/`** は `grep -l '"AUTODEV-SPEC.md"' rules/*.md`
  → `rules/testing.md`（**行数は `wc -l` より 1 多く数えられます**）
- **弓の材料を変えると、羊毛で弓を作る道が消えます。** ベッドの羊毛はそのままです ——
  仕様書に「既存のレシピを書き換えるのはこの 1 行だけ」と書くこと

## ブラウザで見てほしいところ

**クモの手触りが、この周で新しく増えたものです。** 撮った絵では
**赤い目 4 つがはっきり見え、脚 8 本が胴（±5px）の外の ±10.6px まで出ている**ことと、
**console のエラー 0 件・WebGL 2.0** を確かめてあります（`docs/browser-shots/`）。
それでも人にしか決められないのは次の 3 つです:

- **夜の敵対がゾンビとクモで半々になった手触り**（重み 100 = 本家のまま。
  ゾンビばかりに戻したいなら下げます）。**速さ 6.0 は「歩きでは逃げ切れず、走れば
  振り切れる」**ところを狙っています
- **クモが朝に燃えてよいか**（**本家は燃えません**。ここでは `fireproof` が溶岩と日光の
  両方を止める 1 つの印なので、分けると `MobDef` が 1 列増えて 9 種類ぜんぶに手が要ります）
- **横から見ると胴と頭が 1 つの暗い塊に見えます**（本家のクモもそうなので直していません）

遊んで決めてほしいことの持ち越し: **糸・羽根・卵がどれも淡い色**
（糸は `0xb8bcc8` の冷たい灰にして 2 つから離しました）/ **投げた卵に使い道が無い** /
**牛の胴が寸詰まりに見えないか** / **ステーキと革が一覧で見分けられるか** /
**産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒と `#status` の位置**。

## この周で分かったこと

**決まりごとを 3 件、`rules/mobs.md` に据えました**（`Edit` で普通に直しました）。

1. **`swing` の箱 1 つでは「外へ伸びて下りる」形が作れません** —— 振る部位は
   `y1 === 0` なので、1 箱だと外か下のどちらかにしかなりません。**同じグループに
   2 箱**（太もも + その外端から下りるすね。どちらも `y1 === 0`）にすると Γ になり、
   **`MobMotion` を増やさずに**脚 8 本が振れます
2. **敵対を足すと「数」の判定が 2 件必ず赤くなる**（上の 2 件。**合計で見る / 実測値を
   出してから比を書き直す**）
3. **`fireproof` を 2 つに割らないこと**（`sunproof` を足さない）

**`test/audio.test.ts` の揺れ**（`rules/testing.md`）は**この周に 1 回出ました** ——
「音（実際に鳴らして波形で見る）」の 1 件が落ち、**そのあと 5 回連続で緑**です
（羊毛 0.552 / 雪 0.716 / 砂 2.235 は 5 回とも同じ値）。**赤が出たらまずもう一度
走らせること。判定はゆるめないこと**（停止条件は「2 周続けて赤い」）。

## 人がやること

- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 2 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
