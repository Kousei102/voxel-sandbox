# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05）

- `npm test` **3014 件すべて成功**（前の周と同じ数。**この周は `src/**` も `test/**` も
  1 行も触っていません**）・`npm run typecheck` 緑
- **`npm run build` は走らせていません**（`src/**` に差分が無い B の周のため。`AUTODEV.md` の検証）
- **`npm run bench` も走らせていません**（生成もメッシュ化も触っていない周です）
- **音の一群は 1 回目から緑**でした（羊毛 0.552 < 雪 0.716 < 砂 2.235・低い声 0.162 < 高い声 0.329）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1428 行**（上限 1500・止まる目安 1450。**目安を下回っています**）
- **アイテム 102 種・`MAX_ITEM_ID` 137・111..255 の空きは 118・1..63 の空きは 9**（変わらず）
- **立方体 39 / 非立方体 69**・**クラフト 51 本 / 精錬 7 本 / 食べ物 8 種**・**モブ 9 種類**（変わらず）

## この周でやったこと（AUTODEV 30 の B・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が 7 件（3 件以上）・`AUTODEV-SPEC.md` が **`状態: 済`** だったので、
判定表を上から当てて **B（仕様）の周**。**キューの先頭 15b（ミルクバケツ）を仕様書にしました。**

- **取る前にコードで数え直しました**（B の決まり）: `grep -rn -i milk src/ test/` が **0 行**。
  牛（`mobs.ts` の `COW`）・バケツ（84..86）・毒（`vitals.ts` の `POISON_TICKS`）はすでにあり、
  **繋ぐだけの周**になります
- **7 項目は揃えました**・**105 行**（上限 120）・**`src/**` に差分 0 行**（B の完了条件）
- **ID は 138 を 1 個だけ**（`ROADMAP.md` の予約表「138..255 予備 118 個」の先頭）

**仕様書を書くために読んだコードから、実装の周が踏みそうな穴を 3 つ先に潰してあります**
（どれも `AUTODEV-SPEC.md` の §6「このタスク固有の禁じ手」に入れました）:

- **`items.ts` の `FILLED_BUCKETS` に行を足すと `isBucket(MILK_BUCKET)` が真になり**、
  ミルクを地面に「流せる」ようになります（ミルクは液体ブロックではありません）
- **`FOODS` に足すと `canEat` の門に掛かり**、満腹のときに毒を消せなくなります
  （`use.ts` 158 行の「お腹は空いていません」）
- **`decideUse()` の並びは `milk` が `shear` の隣（器より前）・`drink` が `bucket` の直後**。
  逆にすると「作業台の前に立った牛だけ搾れない」「ミルクを持つと作業台が開かない」に
  なります（`rules/use.md` の「並び順そのものが判断」の 1 番目と 2 番目）

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手が 7 件（3 件以上）・`AUTODEV-SPEC.md` が `未着手` なので
→ 次は C（実装）の周**です。**`AUTODEV-SPEC.md` を全文渡して 15b を実装すること。**

- **仕様書を要約して渡さないこと**（`AUTODEV.md` の禁じ手。§2 と §6 が真っ先に落ちます）
- **触るファイルに当たる `rules/*.md` を引いて渡すこと**:
  `rules/use.md`・`rules/vitals.md`・`rules/mobs.md`・`rules/items-survival.md`・`rules/testing.md`
- **`main.ts` は 15 行以内**（いま 1428 行 → 1443 行までなら止まる目安 1450 を越えません）
- **`test/blocks.test.ts` の 3 件は「数え直す」**（24 → 25 個・`MAX_ITEM_ID` → `MILK_BUCKET`・
  空き 118 → 117）。**あれはゆるめるのではなく、もともと数で押さえる形です**

## ブラウザで見てほしいところ

**この周は絵も配信物も 1 バイトも変わりません**（`AUTODEV-SPEC.md` だけの差分）。
**撮っていません**（C-3 は「見た目に出るものを触った周だけ」）。

- 前の周に**本物のブラウザで 5 枚撮って `Read` で見ています**（`docs/browser-shots/`。
  console のエラー・例外 0 件・タイトル画面もインベントリも出ていました）
- **まだ撮れていないのは、かまど・チェスト・作業台を開いて閉じる経路**です ——
  **公開サイトでその 3 つが今までどおり開いて閉じるかだけ見てください**（15a の持ち越し）
- 持ち越し: **雪玉を投げても何も起きない**のが物足りなくないか / **鉄・金・ダイヤの
  ブロックが材料の山と見分けられるか** / **一覧の枠の名前が 2 行に折れてはみ出す**
  （「ダイヤブロック」「溶岩入りバケツ」。`style.css` の `.slot .label`）/ **クモを 3 匹
  倒すまで弓が作れない**道のり / **クモの速さ 6.0** と**朝に燃えてよいか**（本家は
  燃えません）/ **投げた卵に使い道が無い** / **牛の胴が寸詰まりに見えないか** /
  **産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒**

## この周で分かったこと

**決まりごと 0 件。** 落とし穴は踏んでいません（`src/**` を触らない周で、
分かったことは全部この 1 タスクの話なので `AUTODEV-SPEC.md` の §6 に入れました。
`rules/` は「そのファイルを触る人全員に効くもの」の置き場です）。

## 人がやること

- **設計の判断を 1 つお願いします（2 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま 1428 行で 15b（15 行）と 16 番（1 行）は入りますが、**`test/ui.test.ts` の 40 件の
  見張りが呼び出しを `main.ts` に釘付けにしている**ため、これ以上は大きく減りません。
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 12 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
