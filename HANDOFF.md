# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-14・AUTODEV 81 のあと）

- `npm test` **3582 件すべて成功**（**+49**）。`npm run typecheck` 緑・`npm run build` 緑。
  **音は 1 回目から緑**でした（前の周のような荒れ方はしていません）
- **`npm run bench`: generateChunk 0.98 / 1.01 / 1.02ms（中央値 1.01）** ——
  `rules/meshing-render.md` の 0.93〜1.40 の中で、**退行していません**
  （`worldgen.ts` の生成路を触ったので 3 回まわしました）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（**形も ±0。キーも増えていません**）
- **`main.ts` 1450 行**（テストの数え方。**±0。1 文字も開いていません**）
- **アイテム 127 種・`MAX_ITEM_ID` 165・111..255 の空きは 90**（**±0。ID を 1 つも取っていません**）・
  **1..63 の空きは 9**（±0。55..63）
- **立方体 41 / 非立方体 81**（±0）・**モブ 10 種類**（敵対 5）・**レシピ 66 本**（±0）・
  **`FUEL` 9 行 / `SMELTING` 9 行**（±0）・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 95 行（未着手 7 件）**

## この周でやったこと（AUTODEV 81 の C・クラウドの無人の周）

**判定表は 3 行目**（未着手 7 件で 1 行目は外れ・`AUTODEV-SPEC.md` が `状態: 未着手`）で
**C（実装）の周**。取ったのは**キューの 30b「植えた苗木が木に育つ」**で、
**仕様書を全文そのまま実装**しました（**ID は 0 個・`main.ts` は 1 文字も開いていません**）。

- **`src/treeshape.ts`（新・97 行）**: 木の形だけを持つ純関数 2 本。
  `treeCells(kind, height)` が**書き込む順（葉 → 幹）と `overwrite` の真偽ごと**
  マスを並べて返し、`grownTreeHeight(kind, x, z)` が**座標だけから**高さを出します
  （シードを取りません。範囲は生成側と同じオーク 4..6 / トウヒ 6..9）。
  **`TREE_RADIUS` もここへ移しました**
- **`src/worldgen.ts`（−31 行）**: `stampTree()` の二重ループを `treeCells()` の
  呼び出し 3 行に差し替え。**`CACTUS` / `LEAVES` / `SPRUCE_LEAVES` / `SPRUCE_WOOD` /
  `WOOD` の import が 5 本消えました**（形を知らなくなったため）
- **`src/crops.ts`（+69 行）**: `SAPLING_GROW_SECONDS = 180` と `growTree()`。
  `notePlaced()` が苗木 2 種を覚え、`update()` の振り分けに 1 本足しただけ。
  **木の掛かる 4 隅の列（x±2・z±2）が全部そろうまで 1 マスも書かず、秒数を持ち越します**

**サブエージェントは 1 つも立てていません**（`AUTODEV.md` の C-1 / C-2 の但し書き。
**点検は親が `git diff` を読んで自分でやりました**。`npm test` も自分で走らせています）。

### いちばん効いた確かめ（次の周はここを真似ること）

**`test/worldgen.test.ts` に足した形の見張り（半径・幹の本数・順番）だけでは足りません**
—— あれは `treeCells()` を直に呼ぶので、**`stampTree()` の差し替えを間違えても緑のまま**です。
`git show HEAD:src/worldgen.ts` を `src/worldgen_old.ts` へ置いて両方を束ね、
**シード 12345 の 12x12 列 x 4 段（2,359,296 マス・木のマス 6,489）を 1 マスずつ
突き合わせて差 0** を確かめました（`rules/worldgen.md` へ据えました）。

## 次にやること

**次は B（仕様）の周** —— 未着手 7 件で 1 行目は外れ、**`AUTODEV-SPEC.md` が
`状態: 済`** なので 2 行目に当たります。

- **`AUTODEV-QUEUE.md` の先頭は 31「ネザーレンガと石レンガのハーフ」**
  （低帯 2 個 55..56 ＋ 共有帯 2 個）。**取る前にその 1 件だけコードで数え直すこと**
- **`src/**` は 1 文字も触らないこと**（B の完了条件）。120 行に収まらなければ 2 件に割る
- **共有帯の次の空きは 166 から・低帯は 55 から**（`ROADMAP.md` の予約表）

## ブラウザで見てほしいところ

**撮ったのは `shots/grown.png` 1 枚**（`tools/shot.ts` に `grown` の場面を足しました。
**`Crops.update()` を実際に回して生やしています** —— 手で幹と葉を並べると、育つ道が
木を作れるかを 1 つも確かめないまま緑の絵になります）。**`Read` で開いて見ました**:
育ったオーク（丸い塊）と育ったトウヒ（円錐・幹が 1 段のぞく）と、育てていない苗木 1 本が
並び、**面の欠けも裏返りもなく、背景の自然の木と見分けが付きません**（狙いどおり）。

**`node tools/browsershot.mjs` は撮っていません** —— 苗木が育つのは**植えてから 180 秒後**で、
30 秒で 5 枚撮るあちらには**原理的に写りません**。生成される地形は**1 マスも変わっていない**
ことを上の突き合わせで確かめたので、`terrain` / `mobs` の絵も前の周のままです。

- **苗木（30b）**: → **人に見てほしいのは「180 秒が待てる長さか」**と、
  **育った瞬間に木がぱっと出るのが唐突でないか**（本家も同じですが、ここは 1 段階なので）
- **⚠ 一覧の 6 文字の名前（「オークの苗木」）が 2 行に折れて枠の絵に被る件**
  （`style.css` の `.slot .label`）。**人の判断待ち**なので直していません
- **木炭（29）**: **一覧で「炭」に見えるか**（`0x56473f`）
- **道具の修理（28）**: **傷んだ道具 2 本が「新品に戻る」のが気前よすぎないか**（本家の式）
- **スケルトン（11）**: **矢の当たる高さ** / 2 秒に 1 発 / **間合いを取らず殴ってくる**のが変でないか
- **防具（27b）**: **革一式（防具点 7）で 28% 減が「固くなった」と感じられるか** /
  **革 24 枚（＝牛 24 頭）で一式**が見合うか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）
- **氷（25a）**: **滑りの手ざわり**（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件**・`rules/` へ据えました）

- **`rules/worldgen.md`**: **木の形の出どころは `src/treeshape.ts` の 1 本だけ。**
  生成（`stampTree()`）も育ち（`crops.ts` の `growTree()`）も同じそれを引くこと ——
  写すと**自然の木と植えた木が別物になり、どちらも正しく見えるので気付けません**。
  **`TREE_RADIUS` も写さないこと**（育つ側が待つ列の数がずれます）。**順番（葉 → 幹）と
  `overwrite` を変えないこと**、**形を触る周は 236 万マスの突き合わせをすること**
- **`rules/stateful-blocks.md`**: **1 マスより大きくなるものは、掛かる列が全部そろうまで
  1 マスも書かないこと**（半分だけの木は二度と直りません）。**書き込みが何マスにも
  及ぶものは「根元が書けたか」だけを見て忘れること** —— 全部の `setVoxel` が真を返す
  前提にすると、**葉が 1 枚でも既にあるだけ**で（同じ値には false が返ります）永久に忘れません

**`RULES-INBOX.md` には 1 行も足していません**（層 3 の話は 0 件）。

## 人がやること

- **⚠ 設計の判断（53 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**で、**`AUTODEV.md` の停止条件 2 にちょうど並んでいます**（上限 1500）。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**・
  最低 7 行）。**キューの上 3 件（31..34）はどれも `main.ts` 0 行の見込み**なので、
  判断が下りなくても 3 周は止まりません。`test/ui.test.ts` の 40 件の見張りが
  動かせない事情は変わっていません
- **✅ 苗木が育たない件は、この周で直りました**（植えてから 180 秒で木になります）
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（足すならそれだけで 1 周）
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は 1 回目から緑**でしたが、
  前の周は 7 回中 3 回赤でした。**直すなら測り方**（窓を長く取る・複数回鳴らして
  中央値を見る）で、**判定はゆるめないこと**（`rules/testing.md`）。
  **キューに足すのは A の周の仕事**なので、ループからは足していません
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 81 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押して `#creative` の
`scrollTop` を末尾まで送ってから撮ること**（`docs/browser-shots/README.md` の末尾）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木と同じで、**`tools/shot.ts` に場面を 1 つ
足すのがいちばん安い**（1〜2 秒で撮れて、`browsershot.mjs` を書き換えずに済みます）。
**時間が経たないと見えないもの**（育つ・燃える・溶ける）は、**その場で `update()` を
回してから撮ること** —— `browsershot.mjs` の 30 秒には原理的に写りません。
