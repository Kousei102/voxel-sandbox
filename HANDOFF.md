# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-16・AUTODEV 88（B の周）のあと）

**この周は B（仕様）の周**で、**`src/**` と `test/**` に 1 行も差分がありません**
（判定表の 2 行目 —— `AUTODEV-SPEC.md` が `状態: 済` で、未着手が 5 件あったため）。

- **キューの先頭 33「鉄・金・ダイヤの防具 12 部位」を 33a / 33b に割りました。**
  **`AUTODEV-SPEC.md` は前半の 33a「鉄の防具 4 部位」だけ**です（`AUTODEV.md` の B の
  「120 行に収まらない仕様は 1 周で閉じない信号」）。後半 33b（金・ダイヤ 8 部位）は
  `AUTODEV-QUEUE.md` へ書き戻してあります
- **⚠ 割った理由は色で、この周が総当たりで測りました。** 一覧の**無彩色の帯は既存 46 個で
  埋まっていて**、**素直な銀灰は明るさのどの帯でもいちばん遠くて 20.3 / 20.1 / 22.1 /
  21.4**（判定は `>= 20`）。**12 色を一度に通すのは 1 周で閉じない大きさ**でした
- **鉄は「鋼の青」（青を 18〜48 足した 4 段）で 26.0 / 27.5 / 43.5 / 47.8** に通ります。
  **金 4 色とダイヤ 4 色も測って 26 以上**を確かめ、**12 色ぶん `AUTODEV-QUEUE.md` に
  書き置きました**（C の周は探し直さずに済みます。**ただし取るときに測り直すこと**）
- **サブエージェントは立てていません**（B の周は仕様 1 枚を書くだけです）

## 数字（2026-09-16・この周で測ったもの）

- `npm test` **3636 件すべて成功**（±0。**B の周なのでテストは 1 件も増えません**）。
  `npm run typecheck` 緑
  - **⚠ 途中 2 回赤くなりましたが、どちらも `AUTODEV-SPEC.md` が 121 行**で
    `test/docs.test.ts` の 120 行に当たったものです（**音の揺れではありません**）。
    **`split("\n").length` で数えるので、`wc -l` が 119 で上限ちょうど**です
  - **⚠ 音が 6 回中 2 回跳ねました**（どちらも「声色（音程の倍率）が波形に出る」で
    **高い声 0.329 に対して低い声が 3.9〜4.2 まで跳ねる**）。`rules/testing.md` の
    既知の揺れで、**そのつど走り直して緑**。**判定はゆるめていません** ——
    ただし**前の周（1 回）より目立ちます**（下の「人がやること」）
- **`npm run build` も `npm run bench` も走らせていません**（`src/**` が ±0 行）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行 / 上限 1500**（±0。**1 文字も開いていません**）
- **アイテム 132 種**（±0）・**`MAX_ITEM_ID` 170**・**レシピ 70 本**（±0）
- **1..63 の空きは 7**（57..63）・**111..255 の空きは 85**（**次に取るのは 171**）
- **`AUTODEV-SPEC.md` は `状態: 未着手`・118 行**・**`AUTODEV-QUEUE.md` 未着手 6 件・115 行**

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です
（未着手 6 件なので 1 行目も外れます）。**`AUTODEV-SPEC.md` を全文そのままサブエージェントへ
渡すこと**（要約しない。返らないなら自分で実装する）。要点だけ:

- **ID は 171..174 の 4 個**（`ROADMAP.md` の予約表。取ると **111..255 の空きが 85 → 81**）
- **`items.ts` に 4 行 ＋ `ARMORS` に 4 行 ＋ `crafting.ts` に `armorRecipes()` を 1 本**。
  **`main.ts` も `inventory.ts` も `vitals.ts` も 0 行**（`armorPoints` は `armorOf()` に
  聞くだけで材質を知らないので、鉄でもそのまま効きます）
- **⚠ テストの数え方が 1 か所変わります** —— いまの `test/items.test.ts` は
  **`armors.length === 4` と `new Set(slots).size === 4`** で、**材質が 2 つになると落ちます**。
  **ゆるめるのではなく「材質ごとに 4 部位」へ数え直し、件名に理由を書くこと**
- **色は測った値を仕様書が持っています**（`0xcfcfff` / `0x92a7ce` / `0x788ebe` / `0x506595`）。
  **取るときに測り直し、20 を割ったらずらして `TUNING.md` に書くこと**
- **防具に耐久値を足さないこと**（下の「人がやること」の判断待ち。足すならそれだけで 1 周）

## ブラウザで見てほしいところ

**この周（B）は 1 枚も撮っていません** —— **`src/**` に 1 行も差分が無く、見た目に出るものが
1 つも変わらないから**です（`AUTODEV.md` の C-3 は「見た目に出るものを触った周だけ」）。
前の周に撮った 2 枚は `docs/browser-shots/creative-brick.png` と `creative-brickblock.png`。

**人に見てほしいのはここです**（上が新しい分）:

- **⚠ 鉄の防具の色（33a・次の C の周で入ります）**: **「鋼の青」が鉄に見えるか。**
  **素直な銀灰は一覧の混み具合で通せないことが測って分かりました** ——
  帽子 `0xcfcfff` は特に淡い青紫寄りです。**ここは手ざわりの判断で、
  ずらすなら `TUNING.md` の値を動かすだけ**（テストの判定はゆるめないこと）
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **5 文字から折れます。** 33a で増えるのは**「鉄のズボン」1 枠だけ**（他の 3 つは
  3〜4 文字）ですが、**33b は「ダイヤのズボン」など 6〜7 文字が 8 枠**増えます。
  **人の判断待ち**（`.slot .label` を触ると全部の枠の手ざわりが変わります）
- **レンガ（32b）**: **`0xbc5d39` が「焼いたレンガ」に見えるか**と、**一覧で
  レンガブロック（`0xa4553f`）と並んだときに 2 つあると分かるか**（隔たり 26.0）。
  **粘土 1 個 = レンガブロック 1 個**という交換比が渋くないか（本家と同じ値です）
- **粘土（32a）**: **海に潜って粘土を見つけられるか**（海底の列の 7.2%・4x4 の塊）と、
  **水越しに砂と見分けられるか**。**粘土玉がやや青紫寄り**（`0xa7b4da`）
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）/ **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）/
  **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 1 件**）

- **`rules/items-survival.md`**: **無彩色（灰）の帯はもう埋まっている。新しいアイテムに
  素直な銀灰を選ばないこと。** 一覧の**彩度 30 以下は既存 46 個**で、**明るさのどの帯でも
  いちばん遠い無彩色が 20.3 / 20.1 / 22.1 / 21.4** —— 判定 `>= 20` のすぐ上しか残って
  いません。**通すには色みを足すこと**（鉄の防具は「鋼の青」で 26.0〜47.8）。
  **赤茶も同じくらい混んでいる**ので、**色は選ぶ前に総当たりで測ること**。

**この周で踏んだ罠**（決まりごとには足していません。数え方の話なので）:

- **`test/docs.test.ts` の行数は `split("\n").length`** で数えます ——
  **`wc -l` より 1 多い**ので、**上限 120 の仕様書は `wc -l` で 119 まで**です。
  2 回赤くしました（121 → 120 → 118）。**上限は上げていません**

## 人がやること

- **⚠ 設計の判断（59 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1449 行**（テストの数え方で 1450）で、**`AUTODEV.md` の停止条件 2 に並んでいます**
  （上限 1500）。**待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 /
  **24b ケーキをかじる**・最低 7 行）。**キューの上 3 件（33a・33b・34）はどれも
  `main.ts` 0 行の見込み**なので、判断が下りなくても 3 周は止まりません
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。
  **33a で 1 枠・33b で 8 枠増えます**（「ダイヤのズボン」は 7 文字）
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（足すならそれだけで 1 周。
  **33a でも 33b でも持ち込まない**と仕様書の禁じ手に書いてあります）
- **⚠ 鉄の防具が「鋼の青」になる件**（上の「ブラウザで見てほしいところ」）。
  **素直な銀灰が通せないのは測った事実**で、**判定をゆるめる選択肢は取りません**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は 6 回中 2 回跳ねました**
  （前の周は 1 回）。**跳ねるのはいつも「声色（音程の倍率）が波形に出る」の低い声**で、
  **0.162 のはずが 3.9〜4.2 まで飛びます**（20 倍以上なので、判定の近くで揺れているのでは
  なく別物が鳴っています）。**直すなら測り方**（窓を長く取る・複数回鳴らして中央値を
  見る）で、**判定はゆるめないこと**（`rules/testing.md`）。**キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 88 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押してから撮ること**
（`docs/browser-shots/README.md` の末尾）。**⚠ 送り先は末尾とは限りません** ——
**低帯（1..63）の番号を取ったものは一覧の真ん中に出る**ので、`scrollTop = scrollHeight`
では 1 枠も写りません。**`.label` の文字列で枠を探して
`scrollIntoView({ block: "center" })` を呼ぶこと。**
**33a の鉄 4 部位は 171..174 なので末尾に出ます。**

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**海底の粘土（32a）も場面 `clay` を足しました** —— ただしあれは**原点のまわりを
撮りません**（種 4242 は ±128 の海が全部凍っています。`rules/meshing-render.md`）。
**33a は新しいブロックが 0 個なので、`npm run shot` は要りません**（防具はアイテムです）。
