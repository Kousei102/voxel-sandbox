# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-25）

- ブランチ **`loop/endgame`**。**`origin/master` より 33 コミット先**で、
  **origin へ push 済み**（2026-08-25。ユーザーの承認あり。Routines から見えるようにするため）。
  **`master` への push は引き続き禁止** —— `.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1630 件すべて成功**（1494 → 1630。main.ts から出した判断のテストで 136 件増）
- **進行（クリア導線）達成 6 / 13**（6 件とも本物）。
  溶岩 / 黒曜石 / 火打石と打ち金 / ポータルの点火 / ネザーへ行って戻れる / ネザー要塞
- ブロック ID **1..63 の空きは 14 個**（64 以降は 103 まで）。アイテム ID は 88 まで
- `src/main.ts` は **1349 行 / 上限 1500**（余白 151 行）。残りのタスクに要る配線は
  見積もり 90〜100 行なので、**2-13 まで届きます。** 足りなくなったら**上限を上げず、
  同じやり方で判断を出すこと**

## 直前にやったこと — TASKS 2-3c（main.ts の減量）

**1494 → 1349 行。上限（1500）は上げていません。** 出したのは全部「判断」なので、
どれもヘッドレスで確かめられるようになりました（新しいファイルには全部テストを付けました）。

| 出したもの | 出し先 | 見張り |
| --- | --- | --- |
| セーブの組み立て・読み戻し | **`session.ts`（新規）** | `test/session.test.ts` |
| F3 のデバッグ文字列 | **`debugtext.ts`（新規）** | `test/debugtext.test.ts` |
| `M` / `N` のデバッグ湧かせ | **`debugspawn.ts`（新規）** | `test/debugspawn.test.ts` |
| 足音・着地・水しぶき（前フレームの状態ごと） | `sfx.ts` の `Footsteps` | `test/sfx.test.ts` |
| 空とフォグ・水中判定 | `daynight.ts` の `environmentFor()` | `test/daynight.test.ts` |
| バケツで汲む／流す | `placing.ts` の `tryBucket()` | `test/placing.test.ts` |
| 食べ進みと中断・死亡画面の 1 行 | `vitals.ts` の `Eating` / `deathMessage()` | `test/vitals.test.ts` |
| まとめ捨ての判定（入力の意味） | `inventory.ts` の `bulkDiscard()` | `test/inventory.test.ts` |

- **`test/ui.test.ts` に「出した判断を呼び直しているか」の見張りを足しました。**
  行数の上限だけでは「出したのに `main.ts` にも書き戻した」を止められません
  （両方あっても行は増えない）
- `test/dimensions.test.ts` の `serializeEdits` の数え先を `session.ts` に移しました
  （**`main.ts` は 0 か所**であることも同時に見ます。判定はゆるめていません）
- 決まりごとの更新: `rules/dimensions.md`（`session.ts`）/ `rules/dom-ui.md`（F3 とデバッグの鍵）/
  `rules/audio.md`（`Footsteps`）/ `rules/lighting.md`（`environmentFor`）/
  `rules/vitals.md`（`Eating`）/ `rules/items-survival.md`（`tryBucket`）/
  `rules/inventory-screen.md`（`bulkDiscard`）

### 前の周（2-1 / 2-2 / 2-3）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）。決まりごとは `rules/dimensions.md` の「ポータルを通る」
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48。
  **建てる高さは `groundAt`（床と溶岩の海面の高いほう）** —— 床をそのまま渡すと海の底に沈みます。
  費用は要塞の掛かる段 0.31ms（掛からない段 0.03ms）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 の 4 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **`world.seed` をセーブに使わないこと。** ネザーでは `World` が持つのは塩を混ぜた
   あとの値なので、そのまま保存すると**次に開いたワールドの地形が丸ごと別物**になります。
   種は `main.ts` の `worldSeed`（`startWorld()` が覚える）。**一番人に見えない事故**です
2. **`startWorld()` で `beds.clear()` しないこと。** 次元を移るたびにリスポーン地点が
   消えていました。消してよいのは「別のワールドを始める」2 か所（作り直し・データ削除）だけ
3. **`PortalGate.step()` は 1 フレームに 1 回。** 最初の `updatePortal()` は 2 回呼んでいて、
   dt を二重に数えて待ち時間が半分になっていました
4. **出る場所は「空気だけの一番低い所」を採ること。** 見当（`yHint`）から少しだけ下を
   探す形だと、**水没した所や溶岩の海の底**に出ます（出た瞬間に死にます）
5. **仮の判定は「土台があるか」に掛けないこと。3 度目です**（1-8 の周）。
   `sourceHas("src/projectiles.ts", "export")` が器を作った瞬間に達成へ化けました。
   **成果物に掛けること**
6. **わざと壊して、テストが赤くなるか確かめること。** 飛び道具の「壁の手前で止まる」は、
   位置を戻す行を消しても通りました（**箱の前面と壁の面**を比べる形に直したら捕まりました）

7. **`Dimensions` は次元 1 つにつき生成器を 1 個だけ作って使い回します。**
   同じ器に別の種を渡しても**最初の種の地形が返ってきます** ——
   進行テストで 3 つの種を試したつもりが、3 つとも「最寄り 34 マス」と出て気付きました。
   **種を変えて試すときは器ごと作り直すこと**

8. **使い回しのオブジェクトを返す関数は、値を引いたらすぐ控えること。**
   `environmentFor()` は毎フレーム呼ぶので `Color` を作り直しません。テストで 2 回呼んで
   比べたら、1 回目の結果まで書き換わっていて**テストのほうが間違って**いました
   （実装は正しい。`main.ts` は引いた直後に貼っています）

## 次に取るもの

1. **2-3b（次元ごとの空）**。ネザーの空がオーバーワールドのまま（横に青空と星が見える）。
   判断は `daynight.ts` に次元ごとの表を置き、`sky.ts` は貼るだけ。**GLSL は要りません**
   （色と、太陽・月・星を出すかどうかで足ります）
2. そのあとは `TASKS.md` の並びどおり 2-4（ブレイズ）→ 2-13（ドラゴン）。
   数値は**本家の値を暫定で置き、`TUNING.md` に 1 行足して止まらない**
3. **Routines（無人・定期実行）への移行。** push は済んだので、あとはスケジュールを作って
   1 周スモークテストするだけ（`LOOP.md` に従って 1 周進める、を 3 時間ごと）
4. 最後に人がやること: `REVIEW.md` の一括確認 / `TUNING.md` の一括調整 / master へマージ
