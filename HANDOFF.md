# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05）

- `npm test` **3044 件すべて成功**・`npm run typecheck` 緑（**2 回走らせて 2 回とも緑**）
- **`npm run build` と `npm run bench` は走らせていません** —— **`src/**` に 1 行も
  差分が無い周**（B の周）だからです。差分は `AUTODEV-SPEC.md` / `AUTODEV-QUEUE.md` /
  `rules/worldgen.md` の 3 つだけ
- **音の一群は 1 回で緑**（羊毛 0.552 < 雪 0.716 < 砂 2.235・低い声 0.162 < 高い声 0.329）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1444 行**（上限 1500・**止まる目安 1450 まで残り 6 行**。この周で ±0）
- **アイテム 103 種・`MAX_ITEM_ID` 138・111..255 の空きは 117・1..63 の空きは 9**
  （**ID を 1 個も使っていない周**なので 4 つとも前の周と同じ）
- **立方体 39 / 非立方体 69**・**モブ 9 種類**（変わらず）

## この周でやったこと（AUTODEV 33 の B・クラウドの無人の周）

`AUTODEV-SPEC.md` が **`状態: 済`**・`AUTODEV-QUEUE.md` の未着手が 5 件（3 件以上）だったので、
判定表を上から当てて **B（仕様）の周**。**`src/**` は 1 行も触っていません。**

**キューの先頭 17 番を 17a と 17b に割りました**（`AUTODEV.md` の B の「120 行に
収まらない仕様は 1 周で閉じない信号」）。元の 17 番は**ブロックと生成・アイテムと
レシピ・食べ物の 3 方向**あり、書いてみると 120 行に収まりませんでした。

- **17a（この周の仕様書・111 行）**: **赤キノコ 139 / 茶キノコ 140**。草むらと同じ
  `model: "cross"` の生えもので、**森（0.015）と針葉樹林（0.01）の地表**にまれに立つ。
  **生える確率は `biomes.ts` の `BiomeDef.mushroom`**（`worldgen.ts` に「森なら」と
  書かせない）、**赤か茶かは `worldgen.ts` の 2 本目のハッシュで半々**。
  **`main.ts` 0 行・`items.ts` 0 行**（`variantOf === AIR` のブロックには同じ番号の
  アイテムが自動で付きます。135..137 で通った道）
- **17b（キューの 2 番目）**: ボウルとキノコシチュー。**`main.ts` は配線 1 行**
  （食べ終わったら空のボウルが 1 個戻る。**戻すものを決めるのは `items.ts`**）

**取る前に数え直しました**: `grep -rn -i "mushroom|キノコ|BOWL|STEW" src/ test/` は
**1 行も出ません**（キューが数周前のものなので、B の周の決まりどおり 1 件だけ数え直し）。

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので、判定表の 3 行目に
当たります。**17a（キノコ 2 種）をそのまま実装してください。** 仕様書を**全文**
サブエージェントに渡すこと（要約しない）。渡す `rules/` は
**`blocks-shapes.md` / `worldgen.md` / `items-survival.md` / `testing.md`** の 4 本。

- **次に取る ID は 139・140**（`ROADMAP.md` の予約表「139..255 予備 117 個」の先頭 2 つ）。
  **共有帯はブロックとアイテムで 1 本の番号列**なので、アイテム側の番号を別に取らないこと
- **`main.ts` は 0 行で通る割り方**にしてあります（17b で 1 行使うと 1445 行）
- **地表に新しい形が出るので `npm run shot -- terrain ground` は必須**（C-3）

## ブラウザで見てほしいところ

**この周は絵に出るものが 0 行**（`src/**` に差分が無い周）なので、**何も撮っていません** ——
撮っても前の周と 1 ピクセルも変わらないためです。

- 持ち越し（まだ誰も見ていません）: **サボテンに当たったときの痛さ**（0.5 秒ごとに 1 =
  溶岩のちょうど 1/4。**上に立っても痛くありません**）と、押し付けられ続けると 9.52 秒で
  死ぬこと / **牛を搾る / ミルクを飲んで毒が止まるか** / **ミルクバケツの色（`0xeaf2f8`）が
  羽根・卵・糸・雪玉と見分けられるか** / かまど・チェスト・作業台を開いて閉じる経路 /
  雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と見分けられるか /
  **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
  クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに
  見えないか / 産卵の間隔 300〜600 秒
- **次の周（17a）で出るもの**: 森を歩いたときのキノコの**密度**（森 0.015 /
  針葉樹林 0.01 は暫定です）と、**赤・茶が草むらと見分けられるか**。

## この周で分かったこと（決まりごと 1 件・`rules/` に据えました）

**`rules/worldgen.md` が 1 周古くなっていました。** 「サボテンは触ってもダメージが
ありません」と書いてありましたが、**16 番（前の周）で痛くなっています。**
`spiky` と `vitals.ts` を指すように直し、**「砂漠に生やす本数を増やすと、そのぶん
痛い地形になる」**を足しました（生成の側にしか無い視点です）。

**気付き方**: 実装の周が触る `src/*.ts` から `grep -l '"src/worldgen.ts"' rules/*.md` で
引いて読んだときに出ました。**仕様を書く周でも、触る予定のファイルの `rules/` は
先に読むこと** —— 前の周が変えた事実がそのまま残っていることがあります。

## 人がやること

- **設計の判断を 1 つお願いします（6 周持ち越し・18 番で効きます）: `main.ts` を 2 つに
  割ってよいか。** いま **1444 行で、止まる目安 1450 まで残り 6 行**です。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **17a は 0 行・17b は 1 行**で通せるように割ったので、**あと 2 周は保ちます。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 16 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
