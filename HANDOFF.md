# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm test` / `npm run build` は通ります。
  **`npm test` 2307 件すべて成功**（2286 → 2307。増えた 21 件は今周の `test/gravity.test.ts`）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1373 行 / 上限 1500**（**この周は 1 行も触っていません**）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— この周は ID を 0 個しか使っていない）
- **`AUTODEV-SPEC.md` は `状態: 済`** → **次の周は B（仕様）**
- **`RULES-INBOX.md` に未取り込みが 2 件**（`mobs.md` と `projectiles.md`）。**取り込むのは人が居る周**

## この周でやったこと（無人・C の周 1 種類だけ）

`AUTODEV.md` の判定表で **C（実装）** に当たったので、`AUTODEV-SPEC.md` の 1 枚
（**砂と砂利が落ちる = 重力ブロック**）を実装 → **別の**サブエージェントで検分 → 記録、
まで同じ周でやりました。**差し戻し 0 回。**

### 入ったもの —— **支えを失った砂・砂利が、その場で下まで落ちて積み直す**

- **規則（どの ID が落ちるか）は `blocks.ts` の `fallsDown()`** —— `LIQUID` / `HOT` と同じ
  `Uint8Array` の表 1 本。`SAND` と `GRAVEL` に `falls: true`
- **どのマスに効くかは新規の `src/gravity.ts` 1 か所**（`landingY()` / `settleColumn()`）。
  `World` を丸ごと受け取らず `getVoxel` / `setVoxel` の 2 つだけを受ける（`liquids.ts` の作法）
- 呼ぶのは **`breaking.ts` の `tryBreak()` と `placing.ts` の `tryPlace()` が 1 行ずつ**。
  **`main.ts` と `world.ts` は 1 行も触っていません**（配線すら要りませんでした）
- **落下は即時**（落下エンティティも、落下中を表すブロック ID も作っていない）。
  **`world.update()` と生成器からは呼びません** → **生成された浮いた砂は掘るまで落ちません。これは仕様です**
- 松明は重力の側では運びません。砂が下がると**既存の `breakUnsupported`** で壊れます

### 検分（別のサブエージェント）が実際に確かめたこと

テストが緑なのは前提として、**テストが見ていない壊れ方**を一時ファイルで突いて確認済み
（確認後に削除）: 不連続な砂柱 / 砂の上の石 / **チャンク境界（x=15,16 と y の段またぎで 20 個）** /
**未読み込みの列を凍結したとき（`moved=0` で無変化＝砂が消えも増えもしない）** / 100 個の
巨大スタックで無限ループなし。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶん —— 砂・砂利が落ちるようになりました。**

1. **砂漠か浜で、砂の柱の一番下を掘る。** 上の砂がその場で 1 つずつ詰まり、
   **間に穴や浮いた砂が残らないこと**（落ちる途中の姿は出ません。1 フレームで着地します）
2. **水面の上から砂を落として埋め立てられること**（水は上書きされて消えます）。
   **砂の上に松明を置いてから下を掘ると、松明は運ばれずにその場で壊れます**（本家と同じ）
3. **洞窟の天井の砂利や、砂漠の崖の砂が、掘っていないのに勝手に崩れていないこと**
   —— 生成直後の地形は触らない約束なので、**崩れていたら退行です**

**前の周からの持ち越しが 1 件まだ残っています** —— メッシュ化の詰め方を変えた（`0c68545`）ので、
**面が欠けていないか**（洞窟の奥・水面・ガラスごし）と **AO の陰影が前と同じか**（壁ぎわ・階段の下）。

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **9 件**（3 件以上あるので A ではない）、
`AUTODEV-SPEC.md` は **`状態: 済`** → **次は B（仕様）の周**です。
`AUTODEV-QUEUE.md` の先頭は **2. 大きいチェスト（隣り合った 2 個で 54 枠）**。
**取る前にその 1 件だけコードで数え直すこと**（B の決まり）。`add-stateful-block` スキルと
`.claude/rules/beds.md`（**2 マスにまたがるブロックの先例**）を仕様書に名指しで渡すこと。

**件数は仕様書ではなく `npm test` から取ること** —— 今周の仕様書は「2285 件」と書いていましたが
実測は 2286 件で、1 件古いまま渡っていました（`test/docs.test.ts` が増えたぶん）。

人がやること:

- 上の**ブラウザ確認**（今周の 3 点 + 前の周からの持ち越し 1 件）
- `RULES-INBOX.md` の 2 件の取り込み
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）はまれに 1 件落ちます**（今周も 3 回のうち 2 回落ちて、
再実行で通りました）。**2 回続けて落ちたときだけ退行として扱うこと。**

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
