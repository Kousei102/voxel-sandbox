# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm run build` は通ります。**`npm test` 2525 件すべて成功**
  （前の周の 2488 件から **+37**）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1395 行 / 上限 1500**（**この周は +12 行**）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**この周は 0 個**）
- **`AUTODEV-SPEC.md` は `状態: 済`**（3d・火打石と打ち金と弓の耐久値）、
  `AUTODEV-QUEUE.md` の未着手は **7 件** → **次の周は B（仕様）**
- **`RULES-INBOX.md` に未取り込みが 8 件**（`mobs.md` / `projectiles.md` /
  `items-survival.md` x 3 / `inventory-screen.md` / `drops.md` / `stateful-blocks.md`）。
  **取り込むのは人が居る周**

## この周でやったこと（無人・C の周 1 種類だけ）

**3d を実装しました。火打石と打ち金は 64 回、弓は 384 回で壊れます**（本家のまま）。
**掘っても減らず、使ったときだけ減ります。** コミットは 1 つ、**ID は 1 個も使っていません。**

- **`maxUses()` を 1 本のまま通しました。** `toolOf()` が null のときに `usedUp(item)`
  （`isFireStarter()` → 64 / `isBow()` → 384）を返す形にしたので、**帯もセーブの 5 つ
  （`wear` / `craftWear` / `dropWear` / `chestWear` / `furnaceWear`）も自動で付いてきました。**
  **新しいキーは 1 つも足していません**（`SaveData.version` は 1 のまま）
- **`wearForBreaking()` に「掘る道具でなければ 0」を 1 行足しました。** これが無いと、
  **弓を持って石を掘るだけで弓が減ります**（`wearable()` が true になった瞬間に）
- **`main.ts` は配線だけ**（`wearHeld()` 1 本と、点火・発射からの 1 行ずつ）。回数も
  「弓かどうか」も持っていません。`breakBlock()` の 2 行も同じ `wearHeld()` に寄せました
- **`durability.ts` の先頭コメントの嘘を直しました**（「チェストとかまどの中身はまだ
  `[item, count, ...]` のまま」は前の周（`87c6a80`）で嘘になっていました）

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶんが 2 件:**

- **火を点けると火種に帯が出る** —— 黒曜石の枠に火打石と打ち金で点火。**枠の下に細い帯**が
  出て、点けるたびに短く・赤くなるか（64 回で「火打石と打ち金 が壊れました」）。
  **点かなかったとき（枠になっていない所を狙う）は減らないこと**
- **矢を放つと弓に帯が出る** —— 引き切って放つと帯が出る。**矢が無いまま離しても、
  引きが足りないまま離しても減らないこと。どちらも新品では帯が出ません**

**前の周までの持ち越しが 8 件、まだ人の目を通っていません:**

- **器へ入れて出す**（`87c6a80`）—— 傷んだ道具をチェストに**つまんで入れる／シフトクリックで
  入れる**。**枠の下の細い帯**が付いたままか。閉じて開き直しても付いたままか
- **器を壊す**（`87c6a80`）—— 傷んだ道具を入れたままチェスト（とかまど）を壊し、
  地面に落ちたものを拾い直しても帯が付いたままか
- **落とし物が傷を運ぶ**（`6e8ec84`）—— `Q` で捨てて拾い直す・持ったまま死んで拾い直す
- **掴んで動かしても傷が残る**（`61657d5`）—— 収納へ動かしても帯が付いて回るか。
  **クリエイティブの一覧（`E`）から出した道具には帯が出ていないこと**
- **道具の耐久値**（`8a780e9`）—— 木のツルハシで石を掘ると帯が出る（減るほど短く・赤く）。
  使い切ると **「木のツルハシ が壊れました」** が 1 行出るか
- **大きいチェスト**（`a795564`）—— 2 個並べて開くと **54 枠が 9 列 x 6 段**、単体だと 27 枠
- **砂・砂利が落ちる**（`6209e91`）—— 砂の柱の下を掘ると上が詰まり、穴や浮いた砂が残らないこと
- **メッシュ化の詰め方**（`0c68545`）—— **面が欠けていないか**と **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-SPEC.md` が `状態: 済`** なので → **次は B（仕様）の周**です。

- 取るのは `AUTODEV-QUEUE.md` の先頭 **4.（剣を 4 本）**。**`src/**` と `test/**` には
  1 行も触らないこと**（B の完了条件）
- **取る前にコードで数え直すこと**（B の決まり）。剣は本当に 1 本も無いか、
  攻撃力の表（`mobs.ts` の `TOOL_ATTACK`）がどういう形かを `Read` で見てから書くこと
- **ID を 4 個使います。** **`ROADMAP.md` の予約表の 111 以降から取ること** ——
  **111 以降はブロックとアイテムで 1 本の番号列**なので、片側だけ見て取らないこと
- **剣は「使って減る」ではなく「殴って減る」もの**です（本家は 1 回の攻撃で 1）。
  この周で入れた `wearForUse()` に混ぜず、**3 本目（`wearForAttack()` のような）を
  立てるかどうかを仕様書で決めること**

人がやること:

- 上の**ブラウザ確認 10 件**（この周の 2 件 + 持ち越し 8 件）
- `RULES-INBOX.md` の **8 件**の取り込み（**`drops.md` / `items-survival.md` /
  `stateful-blocks.md` のぶんは `paths` に `src/durability.ts` を足す指示付き**）
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（2 回走らせて緑）。
`audio.ts` / `sfx.ts` には 1 行も触っていないので、**直ったわけではありません。**
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
