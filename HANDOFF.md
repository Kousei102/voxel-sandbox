# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-25）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。
  無人で回すために要る 2 つはどちらも直してあります:
  **(1)** `.claude/settings.json` が層 2（`.claude/rules/**`）と層 3（`.claude/skills/**`）への
  Edit / Write を許可（**このファイルを消すと無人の周が確認待ちで止まります**）。
  **(2)** クラウドから `origin` へ push する権限（2026-08-25 にユーザーが GitHub 側で付け直し済み）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1675 件すべて成功**（1648 → 1675。この周の 27 件はブレイズ）
- **進行（クリア導線）達成 7 / 13**（7 件とも本物）。**この周で 6 → 7 に上がりました**
- ブロック ID **1..63 の空きは 14 個**（この周はブロックを足していません）。
  アイテム ID は **89 まで**（89 = ブレイズロッド）
- `src/main.ts` は **1351 行 / 上限 1500**（余白 149 行。**この周は 1 行も触っていません**）

## 直前にやったこと — TASKS 2-4a（ブレイズ本体）

**2-4 を 2 つに割りました。** 元は「飛行 + 火球 + ブレイズロッド」でしたが、
**`projectiles.ts` に「誰に当たったか」がまだ無い**（1-8 で意図的に置いていない）ので、
火球は「飛ばす」ではなく**「当てる仕組みを作る」**から始まります。1 コミットに収まりません。
**この周はモブ本体だけ**で、火球は 2-4b。

- **アイテム 89（ブレイズロッド）。50% で落ちます**（本家と同じ）
- **`MobDef` に 3 つ足しました**: `flying` / `fireproof` / `spawnOn`。
  **飛び方の違いは全部表の値**で、`step()` に「ブレイズなら」は 1 行もありません
- 高さは **`flyTarget`**。**決めるのは `think`（5Hz の `aimAltitude()`）、寄せるのは
  `step`（毎フレーム）**。床から `FLY_HOVER`(2.5) 浮き、追うときはプレイヤーの少し上
  （**必ず大きいほうを採る** —— 穴の底のプレイヤーに合わせると床にめり込みます）。
  **床が `FLY_SCAN`(8) 以内に無ければその場の高さを保つ**（溶岩の海の上がこれ）
- **どの敵対モブが湧くかは地面が決めます**（`hostileFor(ground, random)`）。
  **`spawnOn` を指定したモブがその地面で「どこでも」のモブに勝つ**ので、
  ネザーレンガ（＝ネザー要塞の床）ではブレイズだけが湧きます
- 形は黄色い芯 + 頭 + **周りの棒 4 本**（棒は `swing` なので動くと揺れます）。
  当たり判定は本家と同じ 0.6 x 1.8 で、モデルは 0.563 x 1.500 x 0.563 に収まっています
- **近接ダメージはゾンビと同じ `MOB_DAMAGE`(2) を共有**しています（本家のブレイズは 6）。
  モブごとのダメージを持つ仕組みがまだ無いので、2-4b で `MobDef` に 1 行足すところから
- 決まりごと: `.claude/rules/mobs.md` に「飛ぶモブ」「火に強いモブと、湧く地面」の 2 節。
  数値は `TUNING.md` の「モブ（ブレイズ）」に 5 行。確認項目は `REVIEW.md` の「2-4a」に 7 個
- **わざと壊して 6 件とも赤くなることを確かめました**:
  `flying` / `fireproof` を false → 「浮いて留まる」「落ちない」「溝を飛び越える」
  「溶岩で焼けない」の 4 件 / `hostileFor` を素朴な抽選に → 「ネザーレンガではブレイズだけ」
  「湧いたのはブレイズだけ」の 2 件
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません

### 前の周（2-1 / 2-2 / 2-3 / 2-3b / 2-3c）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）。出した先は
  `session.ts` / `debugtext.ts` / `debugspawn.ts` と、`sfx.ts` / `daynight.ts` /
  `placing.ts` / `vitals.ts` / `inventory.ts` の中
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a の 6 節が溜まっています**
  （一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **飛ぶモブには `ACCEL_GROUND` を使うこと。** 空中の加速（`ACCEL_AIR` = 4）は
   「跳んでいる間は向きを変えられない」ための値なので、そのまま掛けると 1 フレームに
   0.07 しか速度が戻らず、**飛ぶモブがほとんど動きません**
2. **崖回避を飛ぶモブに通さないこと。** 通すと、着地した瞬間だけ臆病になって
   溝の手前で止まります（飛ぶ意味が消えます）
3. **`world.seed` をセーブに使わないこと。** ネザーでは塩を混ぜたあとの値です。
   種は `main.ts` の `worldSeed`
4. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）
5. **`PortalGate.step()` は 1 フレームに 1 回**（2 回で待ち時間が半分になります）
6. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）
7. **仮の判定は「土台があるか」に掛けないこと。3 度目です。** 成果物に掛けること。
   この周の「ブレイズがブレイズロッドを落とす」も、**アイテム名だけを見る形をやめて**
   実際に湧かせて倒す形にしました
8. **わざと壊して、テストが赤くなるか確かめること。** この周は 6 件とも確認済みです。
   **「溝を飛び越える」は x だけ見ていると通ります**（落ちながら前へ進むので）。
   **いちばん低かった高さも見ること**
9. **`Dimensions` は次元 1 つにつき生成器を 1 個だけ作って使い回します**
   （種を変えて試すときは器ごと作り直すこと）
10. **使い回しのオブジェクトを返す関数は、値を引いたらすぐ控えること**（`environmentFor()`）

## 次に取るもの

1. **2-4b（ブレイズの火球）**。`TASKS.md` の並びで次です。要るのは 3 つ:
   - **`projectiles.ts` に「誰に当たったか」**（モブ・プレイヤー）。1-8 で意図的に
     置いていないので、`onHitMob` / `onHitPlayer` を足すところから
   - **ブレイズが撃つ AI**（`mobs.ts`。判断なので飛び道具の側に書かないこと）
   - **モブごとの近接ダメージ**（いまゾンビと共有の 2。本家のブレイズは 6）

   数値は本家（火球 5）を暫定で置いて `TUNING.md` へ。
2. そのあとは並びどおり 2-5（エンダーマン）→ 2-13（ドラゴン）
3. 最後に人がやること: `REVIEW.md` の一括確認 / `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **ネザーでも寝られます**（本家はベッドが爆発する）。`canSleep()` は時刻だけで決まり、
  次元を知りません。クリア導線には効かないので触っていません
- **`F3` の `biome` はネザーでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）。本家のゾンビピグリンの代わりと思えば筋は通りますが、
  気になるなら `spawnOn` を 1 行足せば絞れます
