# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-03）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は無人の B（仕様）の周**でした。**`src/**` と `test/**` に 1 行の差分もありません**
  （`git diff --stat -- src test` が空。B の完了条件）
- `npm run typecheck` / **`npm test` 2818 件すべて成功**（**前の周と同じ数**。
  B の周なのでテストは 1 件も増えていません）/ `npm run build` は走らせていません（`src/` 0 行）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **`src/main.ts` は 1450 行 / 上限 1500**（**この周は 1 行も触っていません**。
  停止条件 3 の線にちょうど乗ったままです）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 130**（**この周は 0 個使用**）
- **アイテムは 90 種**、`MAX_ITEM_ID` は **125**。`SaveData` は `version` 1 のまま
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（120 行 / 上限 120）、`AUTODEV-QUEUE.md` の未着手は
  **7 件** → **次の周は C（実装）**
- **`RULES-INBOX.md` は未取り込み 3 件**（この周が 1 件足しました。下の「人がやること」）
- `REVIEW.md` の未着手は 0 件（66 行）

## この周でやったこと — AUTODEV 16 の仕様「鶏」

**キューの先頭「7. 鶏（羽根・卵・生鶏肉・焼き鳥）」を、コードで数え直したうえで
3 件に割り、前半だけを `AUTODEV-SPEC.md` にしました**（`AUTODEV.md` の B の
「120 行に収まらない仕様は 1 周で閉じない信号」）。

- **7**（この仕様書）: **鶏そのものと生鶏肉・焼き鳥**。ID 2 個（**126 / 127**）
- **7b**（キューへ戻した）: **羽根と、矢を本家の形へ**。**`MobDef.drop` は 1 つしか
  落とせない**ので、2 つ目の落とし物を足す話が先に要ります
- **7c**（キューへ戻した）: **卵**（産む側と投げる側の 2 つ。さらに割れます）

**数え直しで分かったこと（コードが根拠）**: `MobKind` は 6 種類で鶏は無し /
`PASSIVE_KINDS` は `["pig", "sheep"]` / `items.ts` に `CHICKEN` / `FEATHER` / `EGG` の名は
1 つも無い / `FOODS` 4 行・`SMELTING` 5 行。

**いちばん大きいのは、前の周の見込みが外れていたことです。** 前の `HANDOFF.md` は
「鶏は新しい『確かめられないもの』を足す周なので `unverifiable-pair` を渡すこと」と
書いていましたが、**`mobmesh.ts` も `mobrender.ts` も `MobDef` を回すだけの汎用**で、
**モブを 1 種類足すのに 0 行**でした（`buildMobMesh()` は `groups` / `boxes` を回すだけ、
`MobRenderer` は `MOBS[kind]` から形を作るだけで、どちらも種類の名前を知りません）。
**だから仕様書では `unverifiable-pair` を「要らない」と明記し、`add-block` だけを渡しています。**
形の点検（体積・巡回順・振る部位・当たり判定に収まるか）は **`MOB_KINDS` を回す
`test/mobs.test.ts` に自動で乗ります。**

**C の周で踏みそうな穴を 2 つ、仕様書に書き込んであります**:

1. **`test/mobs.test.ts` の `census()` の `counts` は `{ 豚, 羊, ゾンビ }` の決め打ち**で、
   鶏を足さないと `counts[name]++` が **`NaN`** になります（数えているつもりで何も見ない）
2. **`longBody`（胴が当たり判定より長い例外）は豚と羊だけ。** 鶏は後ろもはみ出せないので、
   **`longBody` に足すのでも `size` を広げるのでもなく形を削ること**

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は `src/` を 1 行も触っていないので、撮っていません**（絵は前の周から変わりません）。
前の周からの持ち越しが 3 つあります:

1. **パンの茶色（`0xc49a5e`）が、板・原木・棒の茶色と近い系統**です。隣の小麦とは
   分かれますが、**一覧の中で目立つか**は目で見ないと分かりません（`E` を押して末尾へ）
2. **苗の緑（`0x6f8f3f`）が草原に紛れる**件と、**`GROW_SECONDS` 180 秒が待てる長さか**
   （どちらも `TUNING.md`）
3. **`#status`（オートセーブの帯）の位置**。人が直したぶんはまだ見てもらえていません
   （`E` と `Esc` を開いて、狭い窓でも試してください）

**次の C の周では鶏を撮ります**（`node tools/browsershot.mjs` と `npm run shot -- mobs`）。
そのとき「豚・羊と並べて鶏だと分かるか」を `HANDOFF.md` に書いてもらうことになります。

## 人がやること

- **`RULES-INBOX.md` の 3 件を取り込むこと**（`.claude/rules/` は無人の周が触ると
  確認待ちで止まるため）。当て先は **`vitals.md`**（食べ物を足すのに要るのは `items.ts` の
  2 行だけ・食べ物どうしの強さ）/ **`items-survival.md`**（末尾の「肉は食べられます」の段落。
  **食べ物 = 肉だったころの文で、いまと食い違います**）/ **`mobs.md`**（**この周の 1 件** ——
  モブを足すのに描画は 0 行・`longBody` の例外は豚と羊だけ）
- **上の「ブラウザで見てほしいところ」の 1〜3**
- **鶏の数値でよいか**（体力 4 / 速さ 1.7 / 湧きの重み 10 は本家。**生鶏肉を毒なしにしたのは
  こちらの判断**で、本家の「30% で食中毒」は `FoodDef` に確率が無いので表せません。
  確率を足すと `vitals.ts` まで及ぶので、C の周では取らせていません）
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **7 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 未着手`**
なので → **次は C（実装）の周**です。**`AUTODEV-SPEC.md` を全文そのまま読んで、そのとおりに
やること**（要約しないこと。要約すると「触らないファイル」と「固有の禁じ手」が真っ先に落ちます）。

- **1 周 = 1 コミット。** 実装（C-1）・点検（C-2）・撮る（C-3）・記録（C-4）を同じ周でやること
- **点検（C-2）は親が `git diff` を全部読むこと**（別のエージェントを立てられないなら、
  飛ばすより確実です。前の 8 周とも背景で起動していません）
- **見た目に出る周です。** `npm run build` →
  `(npx --no-install http-server dist -p 8080 --silent &)` → `node tools/browsershot.mjs`
  （定番 5 枚）と `npm run shot -- mobs`。**撮ったら `Read` で開いて見ること** ——
  撮っただけでは 1 つも確かめたことになりません（`docs/browser-shots/README.md`）
- **ID は 126 と 127 の 2 つだけ**（`ROADMAP.md` の予約表の「126..255 予備」の先頭）。
  **111 以降はブロックとアイテムで 1 本の番号列**なので、片側だけ見て取らないこと
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）
- **`main.ts` は 1450 行**で停止条件 3 の線にちょうど乗っています。**この周は 0 行**の予定です
  （湧きもデバッグ湧きも `MOB_KINDS` を回すので、配線は要りません）

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に 1 件だけ赤 →
走らせ直したら緑。**この周は 3 回とも緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
