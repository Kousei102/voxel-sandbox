# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-16・AUTODEV 91（C の周）のあと）

**この周は C（実装）の周**で、**キューの 33b「金・ダイヤの防具 8 部位」を実装しました**
（判定表の 3 行目 —— `AUTODEV-SPEC.md` が `状態: 未着手` だったため）。**コミット 1 つを
`master` へ push 済み**なので、**公開サイトにも載ります**。

- **アイテム 8 つ・ID 175..182**（金の帽子 175 / 上着 176 / ズボン 177 / 靴 178・
  ダイヤの帽子 179 / 上着 180 / ズボン 181 / 靴 182）。**ブロックは 1 つも増えていません。**
  **点数は本家のまま 金 2 / 5 / 3 / 1 = 11（44% 減）・ダイヤ 3 / 8 / 6 / 3 = 20（80% 減）**
- **仕様書の見込みがそのまま当たりました。** `src/` で増えたのは **`items.ts` の
  ID 8 つ ＋ `item()` 8 行 ＋ `ARMORS` 8 行 ＋ `MAX_ITEM_ID`** と、
  **`crafting.ts` の `armorRecipes()` の呼び出し 2 行**だけ（**関数の中は 1 文字も
  変えていません**）。**`main.ts` / `inventory.ts` / `vitals.ts` / `craftscreen.ts` /
  `durability.ts` は ±0 行** —— `armorPoints` は `armorOf()` に聞くだけで材質を
  知らないので、**材質が 4 つになっても 1 行も直りません**（革 → 鉄 → 金・ダイヤで 3 度）
- **⚠ `ARMOR_CAP`(20) も `ARMOR_DENOM`(25) も触っていません。** ダイヤ一式 20 点が
  上限にちょうど当たるのは**本家がそういう設計**で、「早く頭打ちになった」のではなく
  **上限に届く一式が初めて入った**だけです（手ざわりの是非は人の判断。下）
- **色は 8 つとも測り直して、B の周と同じ値のまま通りました**（一覧 144 色に対して）。
  金 **26.3 / 38.7 / 26.7 / 32.7**・ダイヤ **28.4 / 34.9 / 27.4 / 39.6**（判定は 20）・
  **16 部位互いは最小 31.2**。**ずらさずに済みました**
- **⚠ ダイヤは `SMELTING` に 1 行もありません。** 一式の道は鉄・金と違って
  **ダイヤ鉱石を掘ると `DROPS` でダイヤ 1 個**です（`smeltResultOf(DIAMOND_ORE)` は null）。
  **仕様書が先に書いていたので、落ちる前に避けられました**
- **差し戻し 0 回。サブエージェントは 1 つも立てていません** —— 実装も点検（C-2）も
  親がやりました（`AUTODEV.md` の「別を立てられないなら親が `git diff` を読む」）

## 数字（2026-09-16・この周で測ったもの）

- `npm test` **3691 件すべて成功**（3656 から **+35**）。`npm run typecheck` 緑・
  `npm run build` 緑。**`npm run bench` は走らせていません**（生成もメッシュ化も
  1 行も触っていないため）
  - **⚠ 音が 1 回だけ跳ねました**（**3 回走らせて 1 回**）。落ちたのは
    **「声色（音程の倍率）が波形に出る」で、低い声が 0.162 → 4.128** と
    高い声 0.329 を追い抜いた形です。**`rules/testing.md` に載っている既知の揺れそのもの**
    （前に測られた 4.159 / 4.161 とほぼ同じ値）で、**走り直して 2 回続けて緑・
    値も 0.162 に戻りました。判定はゆるめていません**
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1450 行 / 上限 1500**（**この周は 1 文字も開いていません**）
- **アイテム 144 種**（136 から +8）・**`MAX_ITEM_ID` 182**・**レシピ 82 本**（74 から +8）
- **1..63 の空きは 7**（57..63・±0）・**111..255 の空きは 73**（81 から −8。
  **次に取るのは 183**。停止条件の 10 個にはまだ遠い）
- **立方体 42・非立方体 85**（どちらも ±0。**ブロックは 1 つも増えていません**）
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 未着手 4 件・112 行**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` で、未着手が 4 件あるので
判定表の 2 行目です。**キューの先頭は 34「ツタ」**（**ID 4 個**・大元 + 壁 3 向きで、
**はしご 145..148 とまったく同じ形**）。

- **`src/**` を 1 文字も触らないこと**（B の周）。書くのは `AUTODEV-SPEC.md` 1 枚で、
  **7 項目が揃って 120 行以内**（`wc -l` では **119 行以下**。`test/docs.test.ts` は
  `wc -l` より 1 多く数えます）
- **書く前に `grep -l '"AUTODEV-SPEC.md"' rules/*.md` を引くこと** ——
  `rules/testing.md` が当たります（**B の周は「書くファイルそのもの」で引く**。
  これを忘れて 4 回続けて行数の上限を踏んでいます）
- **ID は `ROADMAP.md` の予約表から。次の空きは 183** で、**ツタは 4 個**取る見込み
  （大元 183 + 壁 3 向き 184..186。**向き違いは `variantOf` を書くのでアイテムを
  持ちません** —— 低帯 64..110 が満杯なので 111 以降から取ります）
- **見た目（垂れ下がり）が本体なので、C の周は撮ること**（`npm run shot` に場面を
  1 つ足すのがいちばん安い。`HANDOFF.md` 末尾の ⚠）
- **`main.ts` は 0 行の見込み**（19b で `climbable` が表 1 本になったので登るのは 1 行）

## ブラウザで見てほしいところ

**この周は 1 枚撮って見ました**（`docs/browser-shots/creative-gold-diamond-armor.png`）。
**一覧の枠が 136 → 144 個**になり、**末尾に金 4 部位（黄）とダイヤ 4 部位（水色）が
並んでいること**・**色が指定どおり**（`rgb(254,227,77)` …）・**名前の重複 0 件**・
**console のエラー 0 件**を確かめました。**写った不具合は `.label` の折れだけ**
（下の持ち越しの件）で、この周の持ち物ではないので直していません。

**人に見てほしいのはここです**（上が新しい分）:

- **⚠ 金・ダイヤの防具の色（33b・入っています）**: **金 4 段が「金」に見えるか**と
  **ダイヤ 4 段が「ダイヤ」に見えるか**。**金の上着 `0xefd714` とズボン `0xf8b317` は
  絵の上でやや近く見えます**（測った隔たりは 38.7 / 26.7 で判定は通っています）
- **⚠ ダイヤ一式 80% 減が強すぎないか。** **防具は壊れない**ので、
  **「上限の固さが永久に持つ」状態**です（本家は傷んで直す手間があります）
- **⚠ 金を作る理由があるか。** **金一式 11 点は鉄 15 点より弱いのに、要る枚数は
  同じ 24 枚**です（本家のまま。本家では金にエンチャントが乗りやすいぶんで釣り合いますが、
  **ここには経験値もエンチャント台もありません**）
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **5 文字から折れます。** **この周で 5 枠増えて、一覧全体で 56 枠**が折れています
  （最長は 9 文字の「ネザーレンガハーフ」で 39px = 3 行）
- **⚠ 鉄の防具の色（33a）**: **「鋼の青」が鉄に見えるか**（**素直な銀灰は測って
  通らないと分かっています**）。**鉄一式で 60% 減が「固くなった」と感じられるか**
- **レンガ（32b）**: **`0xbc5d39` が「焼いたレンガ」に見えるか**と、**一覧で
  レンガブロック（`0xa4553f`）と並んだときに 2 つあると分かるか**（隔たり 26.0）
- **粘土（32a）**: **海に潜って粘土を見つけられるか**（海底の列の 7.2%・4x4 の塊）と、
  **水越しに砂と見分けられるか**。**粘土玉がやや青紫寄り**（`0xa7b4da`）
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）/ **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）/
  **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 1 件**）

- **`rules/testing.md`**: **「材質が N 個に増えたので数え直した」は、件名に理由を
  書いたうえで表にすること。** 2026-09-16 に**同じ日で 2 度**（鉄で 2 材質・
  金とダイヤで 4 材質）試して、**表にしてあれば次の材質は列 1 つで済む**と分かりました ——
  `MATERIALS` / `TOTALS` に**行を 2 つ足しただけ**で 8 種 → 16 種に伸び、
  **`armors.length === 8` → `16`・`bySlot.every((n) => n === 2)` → `n === 4`** は
  **ゆるめたのではなく数え直し**です（部位の抜けと重複の両方で落ちる形はそのまま）。
  **件名に理由を書かないと、次に読む人がゆるめた跡と見分けられません。**

**この周で踏んだ罠**（どちらも**撮る道具のほうで、`docs/browser-shots/README.md` に
据えました**）:

- **`#mode` はメニューの中のボタンです**（`index.html` の `seedrow`）。**`#play` の
  あとは隠れる**ので、「`#play` → `#mode` を click」の順に書いて**30 秒待って
  `element is not visible` で落ちました。** 正しい順は
  **`#seed` → `#regen` → `#mode` → `#play` → `E`** です
- **`/opt/pw-browsers` には `chromium-1194` と `chromium_headless_shell-1194` の
  両方があります。** 素直に `startsWith("chromium")` で `sort().pop()` すると
  **後者を掴んで `executable doesn't exist` で落ちます**（`chrome-linux/chrome` が
  入っていない）。**`tools/browsershot.mjs` の `findChrome()` は
  `startsWith("chromium-")` で弾いている** —— 使い捨てのスクリプトにも写すこと

## 人がやること

- **⚠ 設計の判断（62 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**・
  最低 7 行）。**キューの先頭（34 ツタ）は `main.ts` 0 行の見込み**なので、
  判断が下りなくてもあと 1 周は止まりません
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
  （足すなら `vitals.ts` → `inventory.ts` の配線になるので**それだけで 1 周**）。
  **33b が入ったので「ダイヤ一式 80% 減が永久に持つ」になりました** —— 前の周が
  「周を追うごとに効いてくる」と書いたとおりで、**いちばん効きやすい持ち越し**です
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れました。** 頭打ちの手ざわりが
  嫌なら人の判断です（`vitals.ts` の `ARMOR_CAP` / `ARMOR_DENOM`）
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままにしてあります**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**もう 56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は 3 回中 1 回赤**
  （足音ではなく**声色**のほう。`rules/testing.md` の既知の揺れ）。
  **直すなら測り方**（窓を長く取る・複数回鳴らして中央値）で、**判定はゆるめないこと**。
  **キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 91 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を click してクリエイティブにし（メニューが開いているうち。上の罠）、
`E` を押してから撮ること**（`docs/browser-shots/README.md` の末尾）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと。**
**⚠ 枠を数えるときは `#creative .slot` を使うこと** —— 素の `.slot` は
**ホットバーとインベントリの 36 枠まで拾って嘘の数が出ます**。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**海底の粘土（32a）も場面 `clay` を足しました** —— ただしあれは**原点のまわりを
撮りません**（種 4242 は ±128 の海が全部凍っています。`rules/meshing-render.md`）。
**33a も 33b も新しいブロックが 0 個なので、`npm run shot` は要りませんでした**
（防具はアイテムです）。**34（ツタ）はブロックなので、次の C の周は要ります。**
**⚠ 防具を着た姿は撮れません** —— **一人称視点なので自分の体が写らない**ためで、
装備したときの手ざわりは人に見てもらうしかありません。
