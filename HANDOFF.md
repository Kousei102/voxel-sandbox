# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-11・AUTODEV 70 のあと）

- `npm test` **3424 件すべて成功**（**+17**。`test/craftscreen.test.ts` の防具枠 16 件と
  `test/ui.test.ts` の `routed` 1 行）
- `npm run typecheck` 緑 / **`npm run build` 緑**（`src/**` を触ったので走らせました）。
  `npm run bench` は走らせていません（生成もメッシュ化も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1450 行**（テストの数え方。`wc -l` では 1449。**+1**。上限 1500）
- **アイテム 123 種・`MAX_ITEM_ID` 161・111..255 の空きは 94**（**±0**。この周は ID 0 個）・
  **1..63 の空きは 9**（±0）
- **立方体 41 / 非立方体 79**・**モブ 9 種類**・**レシピ 65 本**・**バイオーム 11**
- **`craftscreen.ts` 985 行 / `inventoryui.ts` 334 行**
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 137 行**（未着手 **3 件**）

## この周でやったこと（AUTODEV 70 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**27b-2「防具枠が画面に出て、着ると固くなる」を実装しました**（仕様書どおり **ID 0 個**）。

| ファイル | 行 | 入れたもの |
| --- | --- | --- |
| `src/craftscreen.ts` | 940 → 985 | `SlotArea` の `"armor"` / `slotAt()` の 1 行 / **`accepts()`** / 呼ぶ 4 か所 / `quickMove` の行き先 |
| `src/inventoryui.ts` | 313 → 334 | 4 枠を拾って `wire()` と `paint()`。**判断は 1 行も書いていません** |
| `index.html` | +26 | `#armorrow` と `#armorhead` / `#armorchest` / `#armorlegs` / `#armorfeet` |
| `src/style.css` | +8 | `#inventory #armorrow` の `gap` 1 つ |
| `src/main.ts` | **+1 行だけ** | `vitals.armor = inventory.armorPoints;`（`player.canSprint = ...` の直後） |

**芯は `accepts(area, index, item)` の 1 か所**です。**置く経路は 4 本**（掴んで置く・
撫でて配る・数字キー・シフトクリック）あり、**1 本でも素通しにすると「その操作でだけ
着られる」**ので、テストも 4 本とも弾けているかを見ています。**`transfer()` の中では
見ません** —— 掴む側も通るので、掛けると着ている物が外せなくなります
（**`NO_ITEM` を必ず true にしている**のも同じ理由で、空の山と入れ替えて脱ぐ経路がこれで通ります）。

**シフトクリックの行き先の順は「チェスト → かまど → 防具枠 → ホットバー/収納」。**
器より先に防具枠を見ると、チェストを開いて防具をしまう経路が消えるので、
**退行の見張りを 2 件**書いてあります（チェストへ防具・かまどへ鉄鉱石）。

**防具枠はいつも出します**（`#storage` と `#invhotbar` と同じ扱い）。かまど・チェスト・
クリエイティブで隠すと `refresh()` に 5 つ目の `mode !==` が増え、UI に判断が 1 つ戻ります。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので判定表の 2 行目です。
**A ではありません**: 未着手は **3 件**で、A の条件は「**3 件未満**」だからです
（**前の周の `HANDOFF.md` が「3 件を切るので次は A」と書いていたのは 1 つずれています**）。

- **キューの先頭は `11. スケルトンと骨`**（ID 2 個 / 依存: 見た目）。
  **取る前に、その 1 件だけコードで数え直すこと**（B の周の決まり）
- **ID を取るなら `ROADMAP.md` の予約表から。次の空きは 162 から**
  （**111 以降はブロックとアイテムで 1 本の番号列**。片側だけ見て取らないこと）
- **⚠ `main.ts` はいま 1450 行**（テストの数え方）で、**`AUTODEV.md` の止まる目安
  （1450 行を超えた）にちょうど並びました。次に `main.ts` へ 1 行でも足す周は、
  先に判断を別ファイルへ出さないと止まります**（下の「人がやること」）
- **未着手が 3 件なので、あと 1 タスク（B + C の 2 周）回すと 2 件になり、その次が A の周**です

## ブラウザで見てほしいところ

**この周で撮ったのは `docs/browser-shots/armor-empty.png` と `armor-worn.png` の 2 枚**
（本物のブラウザ。使い捨てのスクリプトで、`browsershot.mjs` は書き換えていません）。
**確かめたこと**: 防具枠 4 つが **46x46 で y 235 に横並び**で出ていること /
**サバイバルでもクリエイティブでも出ている**こと / 着ると **4 つとも合う枠に入る**こと
（帽子 `rgb(252,161,104)` → 上着 `rgb(216,134,98)` → ズボン `rgb(177,110,81)` →
靴 `rgb(100,65,34)`）/ **console のエラー 0 件**。

**それでも人に見てほしいところ（この周のぶん）**:

- **防具枠の置き場所**（盤面と収納のあいだ・横 1 列）が使いやすいか。**本家は人形の絵の
  横に縦 1 列**ですが、ここは絵を持たないので横に並べました
- **1 文字のラベル（頭・胴・脚・足）で部位が分かるか。** かまどの「材料 / 燃料」と同じ
  `.hint` を借りています
- **革一式（防具点 7）で 28% 減が「固くなった」と感じられるか**（`armorReduced()` は
  7 / 25。数値は本家のまま）

**⚠ 一覧の枠の名前が 2 行に折れて絵に被る件は、防具枠でも同じに出ます**
（`style.css` の `.slot .label`。**5 文字の「革のズボン」で出ます**）。
**27b-2 の仕様書で「触らない」に入っていたので直していません** —— 見た目の判断なので
人が決めてください。**直すなら別の周で**（差分が 2 件に割れます）。

**持ち越し（革の防具 27b-1）**: 革の色 4 つが「革」に見えるか（上の 2 つは橙寄りに出ます。
**変えるなら隔たりを測ってから**。`TUNING.md`）/ **革 24 枚（＝牛 24 頭）で一式**が
見合う値段か（**革は牛しか落としません**）。

**持ち越し（フェンス 26a / 26b）**: **`shots/fence.png` に 5 通りが写っていて、
面の欠けも裏返りもありません**。**手ざわりとして「柵らしく見えるか」**だけ見てください。
**⚠ フェンスを 52fps 未満で跳び越せる件は残っています** —— 跳躍の余裕は **0.0117**
（到達は `9.2²/(2×30) + 9.2×dt/2` で、実測 **60fps 1.4883 / 30fps 1.5667 /
刻みの上限 0.05 で 1.6450**）。**直すなら跳躍の積分**（全ブロックの段差の登り方が
変わります）**か 1.5 の側**（本家の値から離れます）——**数値では決められないので人の判断です。**
**当たり判定はマスいっぱいのままで、腕の無い側にも「見えない壁」があります** /
**色 `0x988a5e` が「木の柵」に見えるか** / **棒 6 本で 2 個**が牧場 1 区画に見合う値段か。

**持ち越し（凍った海 25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の
境目が読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
いちばん近いガラスから 34.3 しか余裕がありません）。**気になるなら人が色を決めてください** /
**氷の板の上を歩けることが見て分かるか** / **氷ごしに海の底が見えるか**（`alpha` 0.6）。

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**。

**持ち越し（ケーキ）**: 上面 `0xffd0e4` が「ケーキ」に見えるか / 側面 `0xe8c9a0` と
下面 `0xd9b98a` の 3 色がケーキに読めるか。**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: 中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか /
素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / 森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ /
牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**）

- **`rules/inventory-screen.md` に防具枠の節を据えました**（層 2 を `Edit` で直接）。
  **`accepts()` が唯一の判断で、呼ぶのは置く側の 4 か所だけ・`transfer()` では見ない**
  （掴む側も通るので、着ている物が外せなくなる）/ **`NO_ITEM` は必ず true** /
  **行き先の順は器 → 防具枠** / **いつも出す** / **点を貼る 1 行と置き場所**。

**撮り直しを 2 回しました**（どちらも `docs/browser-shots/README.md` に据えてあります）:
`paintSlot()` が入れる `el.title` は **`"革の帽子 x1"` の形**なので、名前と `===` で
比べると 1 つも見つかりません（**探すなら `.label` の文字**）。もう 1 つは、
**クリエイティブで始めるとホットバー 9 枠が最初から埋まっている**ので、一覧から
シフトクリックで取ったものが**収納に入る**件です（`#invhotbar` だけ探すと空振りします）。

## 人がやること

- **⚠ 設計の判断（43 周持ち越し・いよいよ当たりました）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（テストの数え方）で、**`AUTODEV.md` の「止まって人を呼ぶ条件」の
  2 番（`main.ts` が 1450 行を超えた）にちょうど並びました。** ハードの上限は 1500 行で、
  **次に `main.ts` へ 1 行でも足す周は、先に判断を別ファイルへ出さないと止まります**
  （待っているのは **24b（ケーキをかじる。最低 7 行）**）。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）。
  **見た目の判断なので人が決めてください**
- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 53 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を押してクリエイティブにし、`E` を押して末尾までスクロールしてから撮ること**
（手順は `docs/browser-shots/README.md` の末尾）。
**自然生成しないブロックは今までどおり `npm run shot -- <名前>` が唯一の経路です。**

**`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
**`wc -l` を 119 以下に収めること**（`rules/testing.md`）。
