# 引き継ぎメモ（2026-08-06 時点）

**このファイルは消さずに、セッションごと丸ごと書き直して次へ渡してください。**
「使い捨て」と書いてあった時期がありますが、実際にはそう運用されていません
（毎回このファイルを更新するコミットが 1 つ積まれ、それを次のセッションが読んでいます）。
消してしまうと、次の人は「どこまで確認済みか」「何が未 push か」を git log から
推測し直すことになります。

- **中身は毎回作り直す。** 古い状況を残したまま追記すると、どれが今の話か分からなくなります。
  前回の内容を引き写さず、**必ず現物（`git fetch` / `npm test`）で裏を取ってから**書くこと
- **ここに書くのは「いまの状況」だけ。** 恒久的な決まりごとは `CLAUDE.md`、
  次に何を入れるかは `ROADMAP.md` に移してください（同じことを 2 か所に置かない）
- **踏んだ失敗は「3. 進め方」に足す。** 何度も踏むものは `CLAUDE.md` へ格上げします
- **まず `CLAUDE.md` を読んでください。** このファイルはその補足です

---

## 1. いまの状態

**作業ツリーはきれいです。`origin/master` は `f4296f8` のままで、そこから先は全部未 push です。**

```
git fetch && git log --oneline origin/master..HEAD
```

**未 push の「数」はここに書きません** —— このメモを書き直すたびに 1 増えて、
必ず古くなるためです（実際、4 と書いて 1 つずれました）。上のコマンドで数えてください。
機能のコミットは新しい順に:

```
チェスト: 板 8 個で作る 27 枠の入れ物を、かまどの器に乗せる   ea4c709
空腹: 肉ゲージと食べる操作を入れ、代用の自然回復を外す        4baaaf2
かまど: 精錬を入れて、代用していた 2 つを戻す                 24e7135
落ちたアイテム: 掘ったものが地面に落ちて拾えるようにする      a5d7297
```

（あいだに挟まっているのは、このメモを更新しただけのコミットです。）

- `npm test` **981 件成功** / `npm run typecheck` / `npm run build` 通過
- **機能の 4 コミットはどれもユーザーがブラウザで確認済み**（全項目 OK）。
  **未確認の変更は残っていません**
- **push 状況は毎回 `git fetch && git log --oneline origin/master..HEAD` で確かめること。**
  前のメモの push 状況を引き写したまま出したら、実際にはもっと先まで push 済みでした。
  **数を文章に書かないこと** —— このメモを更新した瞬間に 1 増えて、書いたそばから古くなります
- **push するかどうかは次の一手より先にユーザーへ聞くこと**（下の「2. push について」）

### 直近のコミット（チェスト）で入ったもの

決まりごとは `CLAUDE.md` の「チェスト」に全部書いてあります。ここには次に触る人が
最初に知りたいことだけを残します。

- 板 8 個で作る **27 枠**の入れ物。壊すと中身が全部地面に出ます（**クリエイティブでも**）
- **かまどが作った器に乗せた 2 つ目**です。上の「かまどと精錬」と読み比べれば、
  「位置ごとに状態を持つブロック」を足すときの型が分かります。醸造台も同じ形で入ります
- **`chests.ts` は 1 ファイル**（かまどは `smelting.ts` + `furnaces.ts` の 2 つ）。
  分ける理由は「精錬という純粋な規則が独立している」ことで、入れ物にはそれがありません。
  **`update(dt)` も持ちません**（毎フレーム進むものが無いので `main.ts` からも呼びません）
- **`CraftScreen` は増やしていません。** `SlotArea` に `"chest"` を足しただけで、
  掴む・配る・シフトクリック・数字キー・ドラッグ配分がそのまま効きます
- ブロック ID は **40**。状態違いを持たないので 1..63 を 1 個しか使いません（空きは **23 個**）

### その前（空腹）で踏んではいけない線

- **自然回復を無条件に戻さないこと**（回復には空腹 18 以上が要ります）。戻すと空腹が飾りになります
- **餓死も毒死もしません**（体力 1 で止まる）。ユーザーと決めた線です。0 にするなら必ず相談すること

---

## 1-b. 次にやること

**着手する順はユーザーと決めること。** 見取り図は `ROADMAP.md`。

### 候補 A: 死亡時ドロップ / まとめ捨て（Ctrl+Q）

**どちらも仕組みは揃っていて、あとは「入れるかどうか」だけです**（いちばん小さく終わります）。
死亡時ドロップは**ユーザーが「いまは入れない」と決めました**（難度を変えるため）。
空腹が入って死ぬ場面は増え、チェストで拠点に物を置けるようにもなったので、また話に出るかもしれません。

### 候補 B: 大きいチェスト（54 枠）

`ROADMAP.md` の「チェスト — 済み」の残り。**「位置ごとの状態が 2 マスにまたがる」初めての例**に
なるので、器のほうを育てる仕事です（隣り合っているかを見る必要があります）。

### 候補 C: 苗木 / 作物

どちらも**「育つブロック」の仕組み**が要るので、実質ひと塊です。作物が入れば食べ物の種類も増えます
（いまは肉 3 つだけ）。**モブと違って保存が要ります**（プレイヤーが植えたものなので）。

### 候補 D: 地形・バイオーム系

川・湖、砂漠を平らに、サボテンのダメージなど。見た目の確認往復が多めです。

### 候補 D: 地形・バイオーム系

川・湖、砂漠を平らに、サボテンのダメージなど。見た目の確認往復が多めです。

---

## 2. push について

**ユーザーに確認を取ってから push すること。** 公開サイトが差し替わる外向きの操作です。
push すると `.github/workflows/deploy.yml` が走り、`npm ci` → `npm test` → `npm run build`
を通ってから GitHub Pages（https://kousei102.github.io/voxel-sandbox/）に出ます。
**テストが落ちればデプロイされません。**

押したら `gh run watch <id> --exit-status` で Actions を見届けること。
そのあと配信中の JS のハッシュを `dist/assets/` と突き合わせると、実際に出たかまで確かめられます。

**フレーム時間の p99 判定（`world.test.ts`）はマシンが混むと落ちます。**
実測: テストを 2 本同時に走らせると p99 が 170ms まで跳ねました（上限 39ms）。
CI ランナーが混んでいると同じ落ち方をするので、落ちたら**中身の退行と決めつけず
再実行を試すこと。**

ブランチは切らず `master` に直接積んでいます（デプロイのワークフローが
`master` への push で走る設定のため）。

---

## 3. 進め方（このプロジェクト特有）

**この環境では WebGL が動かず、音も鳴らせません**（理由と切り分けは `CLAUDE.md`）。なので:

- **見た目・音を変える変更は、1 まとまりごとに止めてユーザーに見て（聞いて）もらうこと。**
  未確認の変更を積み上げないでください。落ちたときに切り分けられなくなります
- 確認をお願いするときは、**具体的な確認項目を 4〜7 個の箇条書き**で出すと話が早いです。
  番号を振っておくと「5 だけ違う」と返ってきて、すぐ切り分けられます
- **確認項目に「アプリがもともと持っていない挙動」を書かないこと。** 一度、こちらが勝手に
  前提にした挙動を確認項目に入れて、ユーザーの手を一往復ぶん取らせました。
  **お願いする前に、その挙動が本当にあるかコードで確かめること**
- **CSS の位置は直に数値で書かないこと。** 肉ゲージを足したとき、`#vitals` の `bottom: 74px` が
  ホットバーに 4px 食い込んでいるのが見えました（前から重なっていたのが、行が増えて表に出た形です）。
  いまはホットバーの寸法を `:root` の変数に持たせて `calc()` で出しています
- **テストの主張も裏を取ってから書くこと。**「咀嚼はいちばんこもった音」と書いたら羊毛の足音のほうが
  こもっていて落ちました。実装ではなく主張のほうが間違っていました
- **同じ規則を 2 か所に書かないこと。** チェストのとき、`Inventory.addRange` の「同じ山に先に足す →
  空き枠へ」をチェスト側へ写しかけました。いまは `inventory.ts` の **`addToSlots()`** を
  両方が使い、テストが使い回しを見張っています

### デバッグに使えるもの

- `M` キー: 照準の先にモブを 1 体（**湧きの条件に関わらず**出せます）。種類はランダム
- `F3`: デバッグ表示にモブ数（`mobs N`）・落ちているアイテム（`drops N`）・かまど（`furnaces N`）・
  **チェスト（`chests N`）**・**空腹（`food n/20`、毒なら `(毒)`）** が出ます
- 開発サーバーは `npm run dev`（5173 が埋まっていると 5174 になります）

### 計測するとき

**`npm run bench` は 1 回では当てになりません。** 同じコードで `generateChunk` が
0.93〜1.46ms に振れます。疑わしいときは `git worktree add <絶対パス> HEAD` で比較対象を
用意し、`node_modules` を symlink して**同じ条件で交互に測ってください**
（終わったら `git worktree remove`）。

```
world.update    中央 3.4ms / 最悪 10〜17ms（GC 込み）
モブ 40 体      中央 0.042ms / p99 0.14ms   ← world.update の 9ms 予算に対して誤差
落とし物 128 個 中央 0.10ms / p99 1.3〜3.7ms（GC 込み。上限いっぱいの数）
npm test        12 秒前後（981 項目）
```
