# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- `npm test` **3005 件すべて成功**（前の周の 2977 から +28）・`npm run typecheck` 緑・
  `npm run build` 緑。**`npm test` は 2 回続けて全部緑**でした（音の一群も 2 回とも緑）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1464 行**（変わらず）
- **アイテム 102 種・`MAX_ITEM_ID` 137・111..255 の空きは 118・1..63 の空きは 9**
- **立方体 39 / 非立方体 69**・**クラフト 51 本 / 精錬 7 本 / 食べ物 8 種**・**モブ 9 種類**

## ⚠ 次の周がまず見ること: `main.ts` が 1464 行で、もう避けられません

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
**10 周続けて `main.ts` に 1 行も足さずに来ましたが、先に取れるものは尽きました** ——
**キューの先頭が 15 番（ミルクバケツ）と 16 番（サボテン）で、どちらも `main.ts` に
振り分けの 1〜5 行が要ります。**

**次の B の周は、仕様を書く前にこれを見ること。** 判断を別ファイルへ出す周を
1 つ挟むか、人を呼ぶかの分かれ目です（`AUTODEV-QUEUE.md` の末尾の ⚠ にも書いてあります）。

## この周でやったこと（AUTODEV 27 の C・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が 8 件・`AUTODEV-SPEC.md` が **`未着手`** だったので、
判定表を上から当てて **C（実装）の周**。**鉄・金・ダイヤのブロック（9 個 ↔ 1 個）**を
仕様書のとおりに入れました。**1 コミット 1 タスク。**

- **取った ID: 135 鉄 / 136 金 / 137 ダイヤ**（`ROADMAP.md` の予約表どおり。予約表は
  「実装済み」に書き換え済み、次の空きは **138** です）
- **触ったのは `blocks.ts`（`def` 3 つ）/ `crafting.ts`（6 行）/ テスト 2 本**と、
  **`items.ts` の `MAX_ITEM_ID` の 1 行**（次の節）
- **サブエージェントは立てていません。** 差分が 4 ファイルなので、渡すより自分で書いて
  自分で `git diff` を読むほうが安いと判断しました（`AUTODEV.md` の C-1 / C-2 の但し書き）
- **差し戻し 0 回**

### 仕様書と食い違ったところが 1 つあります（次の周が同じ穴を踏まないために）

**仕様書は `src/items.ts` を「1 行も触らないファイル」に挙げていましたが、同じ仕様書の
完了の判定が「`MAX_ITEM_ID` が 134 → 137」を求めていました。** 両方は成り立たないので
（`MAX_ITEM_ID = SNOWBALL` と直に書いてある定数です）、**完了の判定を採って 1 行だけ
書き換えました**（`= DIAMOND_BLOCK`）。禁じられていた「`item({...})` を手で足すこと」は
1 行もしていません —— **アイテムはブロック → アイテムの for が作ります。**

**伸ばさないと静かに壊れます**: `allItemIds()` は `1..MAX_ITEM_ID` を数え上げるだけなので、
134 のままだと **`ITEMS` には入っているのにクリエイティブの一覧にだけ出てこないブロック**が
できます（置けるし掘れるしレシピも通るので、**型でも `npm run typecheck` でも止まりません**）。
**`rules/items-survival.md` に据えました。**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手が 7 件（3 件以上）・`AUTODEV-SPEC.md` が `済` なので
→ 次は B（仕様）の周**です。**先頭は 15 番（ミルクバケツ）。**

- **`src/**` を 1 行も触らない周**です（B の決まり）。書くのは `AUTODEV-SPEC.md` だけ
- **上の ⚠ が 15 番に直に掛かります。** `main.ts` の行数を先に空ける周を挟むかどうかを、
  仕様を書く前に決めること（**取りやすい 17 番へ飛ばさないこと** —— `AUTODEV.md` の禁じ手）
- 仕様書は **119 行以内**（上限 120。`test/docs.test.ts` が見張っています）

## ブラウザで見てほしいところ

**この周で絵が変わったのは 2 つだけ**です（撮った絵は `docs/browser-shots/creative-stored.png`
と `stored-blocks.png`。確かめたことはあちらの README に書きました）。

- **一覧の末尾の 3 つが、材料の山と見分けられるか。** 色を材料からそのまま写したので、
  **鉄ブロックと鉄インゴット、金ブロックと金インゴット、ダイヤブロックとダイヤが
  同じ色で並びます**（名前以外の手掛かりがありません）。狙ってそうしていますが、
  **拾い間違えないかは人の目の話**です
- **一覧の枠の名前が 2 行に折れて、枠からはみ出します**（「ダイヤブロック」7 文字）。
  **この周で作った不具合ではありません** —— 「溶岩入りバケツ」「水入りバケツ」で
  前から折れていて、`creative-stored.png` に 3 つとも写っています。直すなら
  `style.css` の `.slot .label` の話で、この周の枠を越えるので手を付けていません
- 持ち越し: **雪玉を投げても何も起きない**のが物足りなくないか / **クモを 3 匹倒すまで
  弓が作れない**道のり / **夜の敵対がゾンビとクモで半々**と**クモの速さ 6.0** /
  **クモが朝に燃えてよいか**（本家は燃えません）/ **投げた卵に使い道が無い** /
  **牛の胴が寸詰まりに見えないか** / **ステーキと革が一覧で見分けられるか** /
  **産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒**

## この周で分かったこと

**決まりごとは 1 件**（`rules/items-survival.md` の `MAX_ITEM_ID`。上の節）。もう 1 つ、
**`docs/browser-shots/README.md` に書き足したこと**があります:

**ヘッドレスの Chromium ではポインタロックが掛からず、`page.mouse.move` を送っても
カメラが 1 度も向きを変えません。** だから**ブラウザでブロックを置いた画は撮れません**
（右クリックが足元にしか飛ばず、1 個も置けない絵を 1 枚撮って気付きました）。
**置いた画が要る周は、`world.setVoxel()` で直に置いて `tools/raster.ts` で塗ること**
（`tools/shot.ts` の `crops` の場面がそのやり方。1〜2 秒）。
**`browsershot.mjs` で置こうとして時間を溶かさないこと。**

## 人がやること

- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 9 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**この周は 1 回目だけ羊毛が跳ねて（9.736）、あとの 2 回は緑**でした
（羊毛 0.552 < 雪 0.716 < 砂 2.235 の落ち着いた値）。**判定はゆるめていません。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
