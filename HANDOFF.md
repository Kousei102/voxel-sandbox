# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-03）

- ブランチ **`loop/devgame`**（**まだ push していません。コミット 2 つが手元にあります**）。
  **`master` への push とマージは引き続き禁止**
- **この周は人が居る周**（無人の周ではありません）。**クラウドの B の周（`10d8093`）の
  あとに、人が 2 件を片付けました**: `RULES-INBOX.md` の取り込みと `#status` の重なり
- `npm run typecheck` / **`npm test` 2801 件すべて成功**（2798 → **+3 件**。`test/ui.test.ts`）/
  `npm run build` も通る
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **`src/main.ts` は 1450 行 / 上限 1500**（この周は 1 行も触っていません。**`npm test` の
  数え方で、`wc -l` の 1449 とは食い違います。停止条件の 1450 に乗っているのは `npm test` の数**）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 131**（**この周は 0 個使用**）
- **アイテムは 89 種**、`MAX_ITEM_ID` は **124**。`SaveData` は `version` 1 のまま
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（パン。120 行 / 上限 120）、
  `AUTODEV-QUEUE.md` の未着手は **6 件** → **次の周は C（実装）**
- **`RULES-INBOX.md` は未取り込み 0 件**（この周に 14 件とも取り込みました）
- **`REVIEW.md` の未着手も 0 件**（66 行）

## この周でやったこと

### 1. クラウドの B の周（`10d8093`・無人）

**`AUTODEV-SPEC.md` をキューの 15 番「パン（小麦 3 → パン）と食べること」に書き直しました。**
`src/**` と `test/**` には 1 行も差分がありません（B の完了条件）。

- `crafting.ts` の `RECIPES` は **43 本**で、**小麦を材料にするレシピは 0 本**
- `items.ts` の `FOODS` は **生豚肉・焼き豚・腐った肉の 3 行だけ**
- **要点は「`main.ts` を 1 行も触らずに足せる」こと。** 右クリックの振り分け（`use.ts` の
  `decideUse()`）も食べる長さ（`vitals.ts` の `Eating`）もクリエイティブ一覧（`allItemIds()`）も
  **もう `foodOf()` 1 本を通っている**ので、`items.ts` の 3 行と `crafting.ts` の 1 行で足ります
- **取る ID は 125 の 1 個だけ**（`ROADMAP.md` の予約表の「125..255 予備」の先頭）

### 2. `RULES-INBOX.md` の 14 件を取り込んだ（`84d3b21`・人が居る周）

**未取り込み 0 件になりました。** 当てた先は `items-survival.md`（傷の表を **4 種類**へ・
`rollDrops()`）/ `mobs.md`（刈って取る）/ `use.md`（**14 通り**・並び順）/
`blocks-shapes.md`（`variantOf` の 2 用法・`placing.ts` の入口）/ `stateful-blocks.md`（育つ苗・
`paths` に `crops.ts`）/ `meshing-render.md`（撮る前に `primeAround()`・`paths` に `tools/shot.ts`）。

**食い違う 3 組はどれも新しいほうを採り、古いほうは当てずに捨てました**（書いた周の指示どおり）。
**ただし 1 か所だけ古い本文を残しています**: AUTODEV 14 の `rollDrops()` の本文に
`Drop.otherwise` と砂利の話が入っていなかったので、**まだ本当なその 3 行は残しました。**

**書かれたとおりに当てなかったのは 2 か所だけ**（どちらも数の書き間違い。コードで数え直した）:
`placing.ts` の入口「4 本」→ **5 本**、`useOrPlace();` → **`useOrPlace(mobIsNearer(`**。
経緯は `RULES-INBOX.md` の「取り込むときにした判断」に残してあります。

### 3. `#status` の重なりを直した（`f152cd8`・人が居る周）

**`ui.ts` の `setPlaying()` が `#status` に `panelopen` を付け外し**、**画面が開いている
あいだだけ `top: 14px` へ逃がします**（位置は `style.css`）。**`!playing` は `#hud` を
隠すのに既に使っている値**なので、判断は 1 つも増えていません。`body` 直下と `z-index: 8` はそのまま。

**下げずに上げたのは、撮ってもらった絵で測るとパネルの上下の余白がどちらも 60px 前後しかなく、
下の隅に `#loading`（生成中…）が居るから**です。`test/ui.test.ts` に 3 件足しました
（規則があること / `bottom: auto` を落としていないこと / `ui.ts` が付け外していること）。
`REVIEW.md` の節は `npm run archive` で `docs/tasks-done.md` へ移してあります。

## ブラウザで見てほしいところ（見た目に出るもの）

**`#status` の位置だけは撮れませんでした。** `npm run shot` は DOM を写さず、
`node tools/browsershot.mjs` は**手元に playwright が無くて動きません**（クラウドの周では動きます）。

1. **`npm run dev` で `E`（インベントリ）と `Esc`（メニュー）を開き、オートセーブの帯
   （15 秒ごと・3 秒表示）が画面の上に出て何とも重なっていないか**。狭い窓でも試してください
2. 次の C の周で出るのは **パンの色 `0xc49a5e`**（小麦 `0xd8c26a` と見分けが付くか）

前の周からの持ち越しで、まだ人にしか確かめられないものが 2 つあります:
**苗の緑（`0x6f8f3f`）が草原に紛れる**件と、**`GROW_SECONDS` 180 秒が待てる長さか**（`TUNING.md`）。

## 人がやること

- **手元の 2 コミットを push すること**（`84d3b21` と `f152cd8`）。
  **クラウドの次の周が回る前に push しないと、向こうは取り込み前の `.claude/rules/` を読みます**
- **上の「ブラウザで見てほしいところ」の 1 番**（`#status` の位置）
- **パンの数値でよいか**（空腹 5 / 満腹度 6 は本家のまま。**かまど無しで作れる中では
  一番強い**ので、焼き豚 8 / 12.8 の立場は保っています）
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **6 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 未着手`**
なので → **次は C（実装）の周**です。**`AUTODEV-SPEC.md` を全文読んで、そのとおりに
入れてください**（要約しないこと。要約すると「触らないファイル」と「禁じ手」が真っ先に落ちます）。

- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）。
  **`items-survival.md` は 4 種類の減り方の表になりました**（取り込み済み）
- **サブエージェントは使えていません。** `AUTODEV.md` の C-1 は「同じ番の中で返らないなら
  自分で実装すること」で、前の 6 周とも `run_in_background: false` を渡しても背景で起動しました。
  **点検（C-2）も親が `git diff` を全部読むこと**（飛ばすより確実です）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）
- **`main.ts` は 1450 行**で、`AUTODEV.md` の停止条件 3 の線にちょうど乗っています。
  **15 番は `main.ts` が 0 行で足ります**（仕様書の 5-5）。1 行でも足したくなったら止まる合図です
- **C の周は `AUTODEV-SPEC.md` の 8（終了条件）を上から順に潰すこと** ——
  `TUNING.md` の節・`ROADMAP.md` の 125 の行と「予備 126..255」・`AUTODEV-QUEUE.md` の
  15 番の行を消す・仕様書を `済` に・`docs/autodev-log.md` に 1 節・この `HANDOFF.md` を丸ごと

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に
「声色（音程の倍率）が波形に出る」が 1 件だけ赤 → 走らせ直したら緑。**この周は 5 回とも緑**）。
**赤が出たら、まずもう一度走らせること**（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です。
この周に 1 度踏みました）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
