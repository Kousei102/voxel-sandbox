# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-22・AUTODEV 113（C の周）のあと）

**この周は C（実装）の周**でした（判定表の 3 行目 —— `AUTODEV-SPEC.md` が
**`状態: 未着手`** だったため）。**41「ネザーレンガのフェンス」を仕様書どおり入れ、
`master` へ push しました**（1 周 = 1 コミット = 1 デプロイ）。

### 入ったもの: ネザーレンガのフェンス（ブロック + アイテム 187・本家 Beta 1.9）

**2 材質目のフェンス**です。**`src/**` の差分は 3 ファイル・実質 3 か所だけ**:

- `blocks.ts`: **ID 1 つと `def()` 1 つ**。**`boxes` と `collision` は 157 と
  同じ配列を指しています**（写して 2 本目を作っていないことを `===` で見張り）
- `items.ts`: **`MAX_ITEM_ID` の 1 行だけ**（`VINE_ZN` → `NETHER_BRICK_FENCE`。
  余った `VINE_ZN` の import は消しました）
- `crafting.ts`: **レシピ 1 本**（ネザーレンガ 6 個 → **6 本**。木の 2 本を写していない）

**157 から変えたのは 3 つだけ**: **色 `0x6e3746`** / **`tool: "pickaxe"` と
`minTier: TIER_WOOD`**（**⚠ 木のフェンスと違って素手では 1 個も落ちません**。
素手 10.0 秒 / 木の斧 10.0 秒 / 木のツルハシ 1.5 秒）/ **`sound` を書かない**（既定の石）。

**`main.ts` も `mesher.ts` も `placing.ts` も `world.ts` も `physics.ts` も
`fenceConnects()` も `isTallCollision()` も 1 行も触っていません。**

## 数字（2026-09-22・この周で走らせて確かめたもの）

- `npm test` **3882 件すべて成功**（**3867 → +15**）/ `npm run typecheck` 緑 /
  `npm run build` 緑（`src/**` を触った）/ **`npm run bench` は走らせていません**
  （生成もメッシュ化も 0 行）
- **音は 12 件で、9 つの値はビット同一のまま**（羊毛 0.552 / 雪 0.716 / 砂 2.235 /
  低い声 0.162 / 高い声 0.329 / 5.00 倍 / 地上 2.224 / 水中 0.461 / 咀嚼 0.555）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）・
  **`main.ts` 1450 行 / 上限 1500**（**±0**）
- **1..63 の空き 7**（±0）・**111..255 の空き 69 → 68**（**次に取るのは 188**）
- `CLAUDE.md` 196 / `LOOP.md` 198 / `AUTODEV.md` 197（上限 200）・`TASKS.md` 103 / 150・
  `REVIEW.md` 83 / 200
- `AUTODEV-QUEUE.md` **105 行 / 上限 150・未着手 4 件**（41 の行を消した）・
  **`AUTODEV-SPEC.md` は `状態: 済`**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** で、
キューの未着手が **4 件**（3 件以上）なので判定表の 2 行目です。

- **キューの先頭は 42「石炭ブロック」**（正式版 1.6・**ID 1 個 = 188**）。
  **鉄・金・ダイヤの立方体（135..137）とまったく同じ形**（9 個でしまう / 形なしで戻す）に
  **`FUEL` の 1 行**（本家 800 秒 = 石炭 10 個ぶん）が付くだけで、**`main.ts` 0 行の見込み**
- **⚠ 色は B の周のうちに総当たりで測っておくこと**（`rules/testing.md`。下に道具が
  そのまま置いてあります）。**石炭（65）と石炭鉱石から 20 以上離すこと** ——
  **黒は一覧で混んでいる帯**（石炭・黒曜石・石炭鉱石）なので、素直な写しは割る見込み
- **`AUTODEV-SPEC.md` は `wc -l` で 119 行以下**（テストは 120 と数えます）

## ブラウザで見てほしいところ

**この周は `npm run shot -- fence` を 3 度撮って `Read` で開きました**
（`--size 1400x1000`。既定の 640x400 でも色の違いは読めますが、腕の繋がり目は
大きいほうが確かです）。**絵で確かめたのは 4 つ**: **色が木と見分けられる** /
**材質をまたいで腕が繋がる** / **1 本だけ置いたものは柱だけ** /
**面の欠けも裏返りも無く、上端はマスの 1.0 で止まっている**（当たり判定の 1.5 は
絵に出ていない）。**下は人にしか決められないところ**です。

- **⚠ 41 ネザーレンガのフェンス**: **暗い赤紫 `0x6e3746` が「ネザーレンガ」に見えるか**
  （**本家の色そのままだと一覧でネザーレンガ 48 と見分けが付かない**ので、ずらしました。
  いちばん近いネザーラックから 25.6）/ **木のフェンスと繋がるのは違和感が無いか**
  （**本家では繋がりません**。下の「人がやること」）/ **素手で壊せないのが分かるか**
  （**木のフェンスは素手で壊せます**。ツルハシが要ります）
- **⚠ 40 エンダーマンが水で痛む**: **浅い池をうろつく個体が削られていくのが分かるか**
  （毎秒 2・20 秒で死ぬ）/ **跳んで逃げるのは半分だけ**（`hurtChance` 0.5）/
  **水路・堀がエンダーマンにだけ効きます** / **水で倒れても真珠は落ちません**
- **⚠ 39 昼のクモ**: **そばを通れるのが安心か拍子抜けか** / **松明（光量 14）のそばでは
  クモだけが襲ってこず、ゾンビは襲ってくる**のが分かりにくくないか
- **⚠ 38 クモが壁を登る**: 2 段・4 段の壁を登ってくるのが怖すぎないか / **登る速さ 3.0 m/s** /
  **囲い（家の壁・柵）がクモには効きません**
- **⚠ 37 サボテン**: **180 秒に 1 段・3 段で止まる**のは待てる長さか /
  **自然に生えたサボテンは伸びません**
- **⚠ 12 刈られた羊**: **地肌の桃色 `0xe3b09c`** / **60 秒で戻るのは待てるか**
- **⚠ 34c ツタ**: **森の木の 25%** は多いか少ないか / **1 列 0〜4 マス**
- **⚠ 33b 金・ダイヤの防具**: **金の上着 `0xefd714` とズボン `0xf8b317` は近い** /
  **ダイヤ一式 80% 減が強すぎないか**（**防具は壊れません**）/ **金を作る理由があるか**
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**（余裕 0.0117。**187 も同じ当たり判定です**）/
  **⚠ 氷と雪の浜がほぼ同じ白**
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: レンガ（32b）/ 粘土は海底の 7.2%（32a）/ ハーフ 2 材質（31）/
  苗木の 180 秒（30b）/ 木炭（29）/ 修理（28）/ スケルトンの矢（2 秒に 1 発）/
  クモの巣の歩き 1/4 / リンゴ 200 枚に 1 個 / はしご 2.35 m/s / クモの速さ 6.0

## 分かったこと（**この周で据えた決まりごとは 2 件**）

どちらも実際に踏んだもので、層 2 なので `Edit` で普通に直しています。

1. **`rules/testing.md`**: **盤面の字（`grid()` の `key`）を使い回さないこと。**
   形が同じだからと `fenceRows`（`["SSS","SSS"]`）を `{ N: NETHER_BRICK }` に渡したら、
   `key["S"]` が `undefined` になって**1 枠も置かれないまま「レシピが無い」で落ちました** ——
   実装は正しいのにテストだけが赤くなる形です。**`RECIPES.length` の件は緑のまま**
   だったのが唯一の手がかりでした
2. **`rules/meshing-render.md`**: **既にある場面へ 2 材質目を足すときは、カメラに近い
   立方体の真後ろに置かないこと。** `fence` の場面の `-5,1` は石（`-3,2`）の真後ろで、
   **絵に 1 画素も出ませんでした**（`0,3` へ動かして読めるようになりました）。
   **カメラのほうを動かさないこと** —— 前の周が詰めた 5 通りが画面から外れます

**⚠ レシピ名の長さが上限ちょうどです** —— 「ネザーレンガのフェンス x6」で
**全角 12.5 文字ぶん / 上限 12.5**（`test/ui.test.ts`）。
**次に足すレシピの名前は、これより長くできません。**

**⚠ `AUTODEV-SPEC.md` は `wc -l` で 119 行が上限です**（テストは 120 と数えます）。

**⚠ 音のテストが赤いのは「揺れ」ではなく退行です**（2026-09-19 に原因ごと消えました）。
9 つの値はビット同一で出るはずなので、**1 つでも動いたら退行**として扱ってください。

## 人がやること

- **⚠ 定期実行のプロンプトに古い行が 2 か所あります**（人がプロンプトを直すまで、
  次の周は `rules/testing.md` と `npm test` の出力のほうを信じること）:
  - **「`test/audio.test.ts` の羊毛は 15 回に 2 回ほど跳ねる」** —— 2026-09-19 に
    原因ごと消えました。**いまは 1 つでも動いたら退行**です
  - **「いま 2934 件」** —— 実際は **3882 件**です
- **⚠ 41 で本家と違えたところが 1 つあります**: **本家では木のフェンスと
  ネザーレンガのフェンスは繋がりません**が、**ここでは繋がります。**
  `fenceConnects()` は「フェンスならどれでも」の表 1 本で、材質の分岐を入れると
  **`mesher.ts` とテストの表の 2 か所に材質が漏れます**。**直すならそれだけで 1 周**です
- **⚠ 41 で見送ったものが 1 つ**: **要塞がネザーレンガのフェンスを建てること**
  （`fortress.ts` を触る話。**取るなら 1 周**）
- **⚠ 設計の判断（82 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 3 件**（**43 卵のヒヨコ**（`onHitBlock` に 1〜3 行）/ **35 骨粉**
  （`use.ts` の 19 通り目）/ **24b ケーキをかじる**（最低 7 行））。
  **次の 42（石炭ブロック）は 0 行の見込み**なので、判断が下りなくても 1 周は進みます
- **⚠ 40 で見送ったものが 2 つ**: **溺れ（酸素・息継ぎ）**と **雨で痛むこと（天候が無い）**。
  **⚠ 39 で見送ったもの: 「殴られたら明るくても追い返す」**（敵対モブは反撃の状態を
  1 つも持っていません）。**⚠ 37 で見送ったもの 2 つ**: **「砂の上にしか置けない」**と
  **「横に固いブロックがあると壊れる」**。**どれも取るならそれぞれ 1 周**です
- **⚠ 明かりの意味が種類ごとに変わりました。** 松明を 1 本立てた所では、
  **クモは襲ってこないのにゾンビは襲ってきます**（本家どおり。`TUNING.md`）
- **⚠ クモの「登れる高さに上限が無い」件** / **⚠ クモは水中では登りません**（本家は登ります）
- **⚠ 落ちたアイテムを溶岩で燃やしてよいか**（`drops.ts` の 347 行）
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **この周は ID を 1 つ取りました** —— **`ROADMAP.md` の予約表に 187 の行を足し、
  「187..255 予備 69」を「188..255 予備 68」へ直しました。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 113 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

## 色を総当たりで測る（そのまま使えます・3 分）

**入口をスクラッチパッドに書いて `esbuild` で束ねるだけ**です（判定は `top` だけ）:

```ts
import { allItemIds, itemColor, itemName } from "/home/user/voxel-sandbox/src/items";
const dist = (a: number, b: number): number =>
  Math.hypot(((a >> 16) & 255) - ((b >> 16) & 255), ((a >> 8) & 255) - ((b >> 8) & 255), (a & 255) - (b & 255));
for (const c of [0x6e3746]) {
  const near = allItemIds().map((o) => ({ o, g: dist(c, itemColor(o)) })).sort((a, b) => a.g - b.g);
  console.log(near.slice(0, 5).map((x) => `${itemName(x.o)}(${x.o}) ${x.g.toFixed(1)}`).join(" / "));
}
```

```bash
npx esbuild <その .ts> --bundle --platform=node --format=esm --outfile=node_modules/.cache/color.mjs
node node_modules/.cache/color.mjs
```

**帯ごと総当たりするなら 3 重ループで 4 刻み**（暗い赤紫の帯は 5608 通りが判定 20 を越えました）。
**いちばん遠い色を選ばないこと** —— 上位は `0x6c106c` のような**毒々しい紫**です。
**「材質らしく見える範囲でいちばん遠いもの」を選ぶこと。**
**41 では、この値（`0x6e3746`）をそのまま C の周が使えて 1 往復ぶん減りました。**

## 音のテストだけを束ねて回す（そのまま使えます）

**30〜50 回連続で回すなら全体（1 回 12 秒）ではなくこちらを**（1 回 0.30 秒）:

```ts
import { run as audio } from "/home/user/voxel-sandbox/test/audio.test";
import { summary } from "/home/user/voxel-sandbox/test/harness";
await audio(); summary();
```

```bash
npx esbuild <その .ts> --bundle --platform=node --format=esm \
  --external:node-web-audio-api --outfile=node_modules/.cache/voxel-audioonly.mjs
node node_modules/.cache/voxel-audioonly.mjs      # 「12 件すべて成功」
```

**⚠ 出力先が `node_modules/.cache/` でないと `node-web-audio-api` を見つけられず、
まるごと `skip` になります**（緑に見えて 1 件も確かめていない形になります）。

## 絵を撮るときの注意（前の周までに実測で分かっているもの）

**⚠ 前の周と絵が変わっていないことを確かめるなら `md5sum` で足ります**
（`shots/mobs.png` は 2026-09-21 の時点で `c4d0609d7eb1d910dc6ab20069f5d9c7`。
**`shots/` は `.gitignore` に入っているので、大きさは自由に変えてよい**）。
**md5 がこの引き継ぎに残っていれば `git worktree` は要りません**（**`git stash` は
使わないこと**。`rules/testing.md` の点検役の事故）。

**⚠ 既にある場面へ 2 材質目を足すなら、カメラ寄り（`z` が大きい側）の空いた所へ**
（2026-09-22 に実測。`rules/meshing-render.md`）。**`fence` の場面は `z=6` から
見下ろすので、`z` が小さいマスは手前の立方体に隠れます** —— `-5,1` は石（`-3,2`）の
真後ろで 1 画素も出ず、`0,3` へ動かしていちばん大きく読めました。
**カメラのほうを動かさないこと**（前の周が詰めた 5 通りが画面から外れます）。

**⚠ `npm run shot -- mobs` は既定の 640x400 では読めません** —— **`--size 900x900`**
（正方形）にすると水平画角が 131° → 70° に狭まり、中ほどの数体が読める大きさになります。
**⚠ その画の手前（下・中央）に毎回写る暗い穴は地形です**（モブ 0 体でも同じ絵が出ます）。

**⚠ `npm run shot` のカメラを目の高さ（`y + 1.7`）まで下げて 4 マスまで寄らないこと** ——
**画の下半分の地面が 1 枚も描かれず空色に抜けます**。**遠すぎても失敗します**
（8.5 マス引くと画の下半分が台の外の草で埋まります）。**撮り直しは 1 秒なので、
まず撮って `Read` で見ること。**

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（`sheep` と `cactus` がその形）。
**フェンスの場面（`fence`。856 行）には、いま 2 材質が並んでいます**
（直線・角・1 本だけ・石の上・石の横 + **ネザーレンガの直線 2 本と 1 本だけ**）。
**時間が経たないと見えないもの**（育つ・燃える・溶ける・刈られる）は、**その場で
`update()` を回すか、その操作を呼んでから撮ること**。**手で積んで撮らないこと。**
**⚠ 防具を着た姿は撮れません**（一人称視点）。**⚠ 動き（登る・跳ぶ・追う・水で痛んで
跳ぶ）も撮れません** —— あれは人に遊んでもらうぶんです。
