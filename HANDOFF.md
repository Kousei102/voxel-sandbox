# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05・AUTODEV 36 のあと）

- `npm test` **3079 件すべて成功**（この周は **±0**。仕様の周なので `test/**` は 0 行）・
  `npm run typecheck` 緑。**`npm run build` は走らせていません**（`src/**` が 0 行）
- **音の一群は 1 回も赤くなりませんでした**（3 回走らせて 3 回とも緑。落ち着いた値は
  前の周と一致 —— 羊毛 0.552 < 雪 0.716 < 砂 2.235 / 低い声 0.162 < 高い声 0.329）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 0 行）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1448 行**（上限 1500・**止まる目安 1450 まで残り 2 行。この周で ±0**）
- **アイテム 107 種・`MAX_ITEM_ID` 142・111..255 の空きは 113・1..63 の空きは 9**
- **立方体 39 / 非立方体 71**・**モブ 9 種類**・**レシピ 53 本**・**食べ物 9 種**（どれも ±0）

## この周でやったこと（AUTODEV 36 の B・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 済` で `AUTODEV-QUEUE.md` の未着手が 4 件だったので、
判定表の 2 行目に当たって **B（仕様）の周**。**`src/**` と `test/**` に 1 行も差分はありません。**

**キューの先頭 18（サトウキビと砂糖）を 18a と 18b に割り、18a だけを仕様書にしました**
（`AUTODEV.md` の B の「120 行に収まらない仕様は 1 周で閉じない信号」）。

- **18a = サトウキビ（ブロック 143）と砂糖（アイテム 144）。** 浜に**1 マスぶん**生えて、
  掘ると自分が落ち、砂糖はそれ 1 個で作れる。**`main.ts` は 0 行**
- **18b = 積める・上へ伸びる。** ここだけが `main.ts` の配線と育ちの表を要ります
- **`main.ts` を数えたうえで割りました**（`HANDOFF.md` の前の周の宿題）。**18a は
  置くのも掘るのも生えるのも既存の経路に乗るので、配線が 1 行も要りません** ——
  **だから人の返事を待たずに次の C の周へ進めます**（止まる必要はありません）

**仕様書に入れた実測**（B の周のうちに数えておいたもの。C の周が測り直さずに済みます）:
**浜は列の 2.81%・生えるのは高さ 41 の 444 / 729 列**・色はサトウキビ `0x9ad14f` が
既存 107 種からいちばん近くて **42.4**、砂糖 `0xffffff` が雪から **22.3**
（**`0xfdfdfd` まで暗くすると 19.0 で判定に落ちます**）。

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。

- **仕様書は全文をそのまま渡すこと**（要約しない。`AUTODEV.md` の C-1）
- **引く決まりごと**: `rules/blocks-shapes.md`・`rules/items-survival.md`・
  `rules/worldgen.md`・`rules/testing.md`（**この 4 本は仕様書の 2. にも書いてあります**）
- **次に取る ID は 143 から**（`ROADMAP.md` の予約表）
- **見た目に出る周です** —— `node tools/browsershot.mjs` と `npm run shot -- terrain` を
  撮って `Read` で開くこと（C-3）

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません**（`src/**` に 0 行の仕様の周なので、見た目は前の周から
1 ピクセルも動いていません）。前の周（17b）の分がそのまま持ち越しです。

- **17b の持ち越し**: **ボウル `0x7a4a24` とシチュー `0xf0dcb4` が、赤キノコ・茶キノコと
  並んだ一覧で見分けられるか**（色は DOM なので `npm run shot` に写りません）と、
  **シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか**（`TUNING.md`）、
  **食べ終わりにボウルが手に戻る手ごたえ**
- 持ち越し（まだ誰も見ていません）: **森を歩いたときのキノコの密度**（森 0.015 /
  針葉樹林 0.01 は暫定）/ **走っていて赤・茶を草むらと取り違えないか** /
  **サボテンに当たったときの痛さ**（0.5 秒ごとに 1）/ **牛を搾る / ミルクを飲んで毒が止まるか** /
  **ミルクバケツの色（`0xeaf2f8`）が羽根・卵・糸・雪玉と見分けられるか** /
  かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
  鉄・金・ダイヤのブロックが材料の山と見分けられるか /
  **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`。
  **「キノコシチュー」は 7 文字でいちばん長い名前です**）/ クモの速さ 6.0 と朝に燃えてよいか /
  投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか / 産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 2 件・`rules/` に据えました）

1. **`rules/blocks-shapes.md`**: **十字（`model: "cross"`）のブロックは、どう書いても
   支えになれません。** `canSupport()` が `def.solid` と「面がマスいっぱい」の両方を
   見るので、`box[u] = 0.1 > 0` で落ちます —— **`solid: true` にしても、箱の上端を 1 まで
   伸ばしても通りません。** 積み上がる生えものには別の決まりが要ります（**これが
   18 番を割った理由そのもの**です）。
2. **`rules/worldgen.md`**: **浜の地表は必ず y41 で、水面は y40（1 つ下）。**
   だから本家どおりの「真下のマスの横が水」を書くと、**生成した場所そのものが
   置けない場所になります。** あわせて、**帯状のバイオームに `patchOf()`
   （まとまった 64x64 を探す形）は使えない**ことも据えました（浜は幅数マスなので
   1 つも見つかりません）。**どちらも C の周が実装で踏む前に潰してあります。**

## 人がやること

- **設計の判断を 1 つお願いします（10 周持ち越し。ただし急ぎではなくなりました）:
  `main.ts` を 2 つに割ってよいか。** いま **1448 行で、止まる目安 1450 まで残り 2 行**。
  **`test/ui.test.ts` の 41 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **次の 18a は 0 行で通るので止まりませんが、18b と 19 はどちらも配線が要ります。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 20 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
