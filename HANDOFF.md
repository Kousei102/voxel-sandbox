# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2420 件すべて成功**
  （**前の周から増えていません** —— この周は `src/**` を 1 行も触っていないので当然です）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1382 行 / 上限 1500**（この周は触っていません）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— ID は 0 個）
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（120 行 / 上限 120）、`AUTODEV-QUEUE.md` の未着手は
  **10 件** → **次の周は C（実装）**
- **`RULES-INBOX.md` に未取り込みが 4 件**（`mobs.md` / `projectiles.md` / `items-survival.md` /
  `inventory-screen.md`）。**取り込むのは人が居る周**

## この周でやったこと（無人・B の周 1 種類だけ）

**`AUTODEV-QUEUE.md` の先頭 `3c`（落とし物・チェスト・かまどでも傷が残る）を仕様書にしました。**
`src/**` と `test/**` は **1 行も触っていません**（`git diff -- src test` が空）。

- **B の決まりどおりコードで数え直した結果、1 周には大きすぎたので 2 件に割りました。**
  傷んだ道具が地面に出る入口は 3 つ（プレイ中の `Q` / 画面の `Q` / 死亡）、器は 3 つ
  （落とし物・チェスト・かまど）で、**省略可キーを 3 つ足す形が 120 行に収まりません**
- **前半 `3c-1`（落とし物だけ）を `AUTODEV-SPEC.md` に**、後半 `3c-2`（チェストとかまど）は
  `AUTODEV-QUEUE.md` へ書き戻しました。**順番は入れ替えないこと** —— チェスト・かまどを
  壊したときの中身は `breaking.ts` の `BreakDrop` を通って**落とし物になる**ので、
  先に落とし物が傷を運べないと後半が閉じません
- 仕様の芯は 3 つです: 傷を載せる入口を **`Drops.spawn()` の 1 か所**に絞ること /
  読んだ値の丸めを **`durability.ts` の `wornValue()` 1 本**にして `deserializeWear()` と
  共有すること / セーブは **`SaveData.drops` の 5 要素を触らず、省略可キー `dropWear` を
  別に足す**こと（`craftWear` を `craft` と分けたのとまったく同じ理由）

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶんはありません**（仕様書だけで、動くものは 1 行も変わっていません）。

**前の周からの持ち越しが 6 件、まだ人の目を通っていません:**

- **掴んで動かしても傷が残る周のぶん**（`61657d5`）—— 傷んだ道具を収納へ動かしても
  枠の下の細い帯が付いて回るか（掴んで置く・シフトクリック・数字キー・ドラッグのどれでも）。
  **クリエイティブの一覧（`E`）から出した道具には帯が出ていないこと**
- **道具の耐久値の周のぶん**（`8a780e9`）—— 木のツルハシで石を掘ると帯が出る（減るほど
  短く・赤く）。使い切ると **「木のツルハシ が壊れました」** が 1 行出るか
- **大きいチェストの周のぶん**（`a795564`）—— 2 個並べて開くと **54 枠が 9 列 x 6 段**、
  単体だと **27 枠**、3 個横一列ならどれも 27 枠に戻るか
- **砂・砂利が落ちる周のぶん**（`6209e91`）—— 砂の柱の下を掘ると上が詰まり、
  **穴や浮いた砂が残らないこと**
- **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**と
  **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **10 件**（3 件以上なので A ではない）、
**`AUTODEV-SPEC.md` は `状態: 未着手`** → **次は C（実装）の周**です。

- **`AUTODEV-SPEC.md` を全文渡すこと**（要約すると「触らないファイル」と「禁じ手」が
  真っ先に落ちます）。**`chests.ts` / `furnaces.ts` / `breaking.ts` はこの周では触りません**
- **サブエージェントは背景（async）で起動しないこと。** 同じ番の中で結果が返らないなら、
  **自分で実装すること**（周を閉じずに終わるほうがずっと高くつきます）
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- 終わったら **`TUNING.md` に 1 行**（チェスト・かまどではまだ新品に戻る）と、
  **`AUTODEV-QUEUE.md` の `3c-1` の行を消して仕様書を `済` に**

人がやること:

- 上の**ブラウザ確認 5 件**（すべて持ち越し）
- `RULES-INBOX.md` の **4 件**の取り込み（**`items-survival.md` のぶんは `paths` に
  `src/durability.ts` を足す指示付き**）
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）の揺れは、この周でも 1 回出ました。**
6 回まわして、**うち 1 回だけ `2420 件中 1 件失敗（最後のグループ: 音（実際に鳴らして
波形で見る））`**、**最後の 2 回を含めて残りは緑**です。

- **退行ではありません。** `git diff -- src/audio.ts src/sfx.ts test/audio.test.ts` は
  **空**です（この周は `src/**` を 1 行も触っていません）。赤が出たら、まずここを見ること
- **1 回の赤で止まらないこと**（停止条件は「2 周続けて赤い」）。もう一度走らせて緑ならこの揺れです
- 直すのは人が居る周（音を聞いてもらう必要があります）

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
