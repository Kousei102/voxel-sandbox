# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm run build` は通ります。**`npm test` 2446 件すべて成功**
  （**前の周から +26 件** —— 落とし物の傷のぶん）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1382 行 / 上限 1500**（この周は**配線 4 行**だけ触りました）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— ID は 0 個）
- **`AUTODEV-SPEC.md` は `状態: 済`**、`AUTODEV-QUEUE.md` の未着手は **9 件**
  → **次の周は B（仕様）**
- **`RULES-INBOX.md` に未取り込みが 6 件**（`mobs.md` / `projectiles.md` /
  `items-survival.md` x 2 / `inventory-screen.md` / `drops.md`）。**取り込むのは人が居る周**

## この周でやったこと（無人・C の周 1 種類だけ）

**`AUTODEV-SPEC.md` の 3c-1（落とし物が傷を運ぶ）を実装しました**（コミット 1 つ）。
**落とした道具を拾い直しても、死んで拾い直しても、読み込み直しても、次元をまたいでも傷が残ります。**

- **傷を載せる入口は `Drops.spawn()` の `options.damage` 1 か所だけ**です。
  `burst()` / `throwOut()` / `deserialize()` はそこへ素通しするので、
  **`drop.damage` への直の代入はどこにもありません**（`carryWear()` 1 本を通します）
- **読んだ値の丸めは `durability.ts` の `wornValue(item, raw)` 1 本**にまとめ、
  `deserializeWear()` はそれに委ねる形にしました（**既存の判定は 1 つも変えていません**）
- **セーブは `drops` の 5 要素を触らず、省略可キー `dropWear` を別に足しました**
  （1 山 1 要素・並びは `drops` と同じ。全部新品ならキーごと消えます）。**`version` は 1 のまま**
- **`chests.ts` / `furnaces.ts` / `breaking.ts` / `droprender.ts` は 1 行も触っていません** ——
  **チェスト・かまどはまだ新品に戻ります**（`AUTODEV-QUEUE.md` の 3c-2 で塞ぎます。
  `TUNING.md` に 1 行足しました）
- **仕様書から 1 か所だけ外れました**: `burst()` の `damage` を `random` の**前**に置きました
  （末尾だと `main.ts` が乱数の差し替え口に `undefined` を渡すことになるため。
  理由は `docs/autodev-log.md`）

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶん 2 件**（どちらも**枠の下の細い帯**が付いて回るかを見るだけです）:

- **傷んだ道具を `Q` で捨てて、歩いて拾い直す** —— 帯が付いたままか（新品の見た目に
  戻らないこと）。インベントリ画面を開いて掴んだまま `Q` で捨てたぶんも同じです
- **傷んだ道具を持ったまま死んで、落ちた山を拾い直す** —— 帯が付いたままか。
  **拾い直したあとにブラウザを閉じて開き直しても**付いたままか（`dropWear`）

**前の周からの持ち越しが 5 件、まだ人の目を通っていません:**

- **掴んで動かしても傷が残る周のぶん**（`61657d5`）—— 傷んだ道具を収納へ動かしても
  帯が付いて回るか（掴んで置く・シフトクリック・数字キー・ドラッグのどれでも）。
  **クリエイティブの一覧（`E`）から出した道具には帯が出ていないこと**
- **道具の耐久値の周のぶん**（`8a780e9`）—— 木のツルハシで石を掘ると帯が出る（減るほど
  短く・赤く）。使い切ると **「木のツルハシ が壊れました」** が 1 行出るか
- **大きいチェストの周のぶん**（`a795564`）—— 2 個並べて開くと **54 枠が 9 列 x 6 段**、
  単体だと **27 枠**、3 個横一列ならどれも 27 枠に戻るか
- **砂・砂利が落ちる周のぶん**（`6209e91`）—— 砂の柱の下を掘ると上が詰まり、
  **穴や浮いた砂が残らないこと**
- **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**と
  **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **9 件**（3 件以上なので A ではない）、
**`AUTODEV-SPEC.md` は `状態: 済`** → **次は B（仕様）の周**です。

- **先頭は `3c-2`（チェストとかまどで傷が残る）**。**取る前にコードで数え直すこと**
  （3c-1 で `dropWear` が入ったので、残るのは `chests.ts` の 54 要素とかまどの 9 要素、
  それと `addToChest()` と壊したときの中身です）。**120 行に収まらなければまた割ること**
- **`src/**` と `test/**` には 1 行も触らない周です**（B の完了条件）
- 仕様書には**「`breaking.ts` の `BreakDrop` に `damage` を足すかどうか」を必ず書くこと** ——
  壊したときの中身は `BreakDrop` を通って落とし物になるので、ここが決まっていないと
  実装の周が勝手に決めます

人がやること:

- 上の**ブラウザ確認 7 件**（この周の 2 件 + 持ち越し 5 件）
- `RULES-INBOX.md` の **6 件**の取り込み（**`drops.md` と `items-survival.md` のぶんは
  `paths` に `src/durability.ts` を足す指示付き**）
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）の揺れは、この周でも 1 回出ました。**
**手を付ける前の 1 回目だけ**が `2420 件中 1 件失敗（声色（音程の倍率）が波形に出る）`で、
**実装したあとに 4 回まわしたぶんは 4 回とも緑**です（前の周は 6 回中 1 回）。

- **退行ではありません。** `git diff -- src/audio.ts src/sfx.ts test/audio.test.ts` は
  **空**です（この周も音は 1 行も触っていません）。赤が出たら、まずここを見ること
- **1 回の赤で止まらないこと**（停止条件は「2 周続けて赤い」）。もう一度走らせて緑ならこの揺れです
- 直すのは人が居る周（音を聞いてもらう必要があります）

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
