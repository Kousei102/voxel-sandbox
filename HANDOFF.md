# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2575 件すべて成功**（2531 → +44）。
  **`npm run build` も通っています**（`src/**` を触った周なので走らせました）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1401 行 / 上限 1500**（**この周は +6 行**）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 141**（**この周は 4 個使用**）
- **`AUTODEV-SPEC.md` は `状態: 済`**、`AUTODEV-QUEUE.md` の未着手は **6 件**
  → **次の周は B（仕様）**（下の「次にやること」）
- **`RULES-INBOX.md` は未取り込み 2 件**（この周が書きました。取り込むのは人が居る周）

## この周でやったこと（無人・C の周 1 種類だけ）

**`AUTODEV-SPEC.md`（剣 4 本）を実装しました。** 木・石・鉄・ダイヤの剣が作業台で作れて、
殴ると強く、殴ると減ります。**共有帯 111 以降の最初の使用者**です（111..114）。

| 触ったファイル | 何をしたか |
| --- | --- |
| `blocks.ts` | `ToolKind` に `"sword"` を足しただけ（**ブロックには 1 つも割り当てていません**） |
| `items.ts` | ID 4 個・`TOOL_NAMES` に `sword: "の剣"`・剣のループ・`MAX_ITEM_ID`・`isSword()` |
| `durability.ts` | **`wearForAttack()` を 3 本目として追加**（既存 2 本は 1 行も触っていません） |
| `crafting.ts` | `toolRecipes()` に剣の 1 行（引数を 1 つ増やしました） |
| `mobs.ts` | `TOOL_ATTACK` に `sword: 4` を足しただけ |
| `main.ts` | 配線 6 行（`mobs.attack()` の戻り値を受ける `if` とコメント） |

**分かったこと**（次の周が同じ道を通らないために）:

- **`tool:` を持たせた時点で耐久値が全部付いてきました。** `maxUses()` は `toolOf()` に
  聞くだけなので、**新しい表もセーブのキーも 1 行も要りません**（`SaveData.version` は 1 のまま）。
- **`MAX_ITEM_ID` が 94 → 114 へ飛んでも 95..110 の穴は素通りします。** `allItemIds()` が
  `ITEMS[id]` の無い番号を飛ばすので、アイテムは **78 → 82 種**、クリエイティブの一覧も
  **82 枠**、`111..255 の空き` は **145 → 141**。**値でもブラウザの絵でも確かめました。**
- **剣は掘っても減ります**（本家と同じ）。`wearForBreaking()` には 1 行も足していません ——
  `toolOf()` が非 null なので自動で付いてきます。右クリックでは減りません（`wearForUse()` は 0）。
- **剣の形（材料 2 + 棒 1 の縦 3）はシャベルと上下 1 マスしか違いません。** 取り違えを
  `test/crafting.test.ts` が両方向で見ています。

## ブラウザで見てほしいところ（見た目に出るもの）

**撮って自分で見ました**（`node tools/browsershot.mjs` の 5 枚 + `creative-swords.png`）。
**確かめたのは「クリエイティブの一覧の末尾に剣 4 本が並び、名前も色も出ている」ことです**
（弓 → 矢 → 木の剣 / 石の剣 / 鉄の剣 / ダイヤの剣。色は道具と同じ階層の色）。
詳しくは `docs/browser-shots/README.md` の 6 枚目の節。

**それでも人に見てほしいのは手触りのほうです:**

- **ダイヤの剣（6）でゾンビ（体力 12）が 2 発、豚（10）が 2 発**です。斧（5）だと
  ゾンビは 3 発。**剣を作る価値がちゃんと感じられるかどうか**は遊んでみないと分かりません。
  本家は 4/5/6/7 ですが、ここは階層の伸びが +0.5 なのでダイヤでも 6 です（`TUNING.md`）。
- **木の剣は 59 回で壊れます。** 掘っても減るので、剣を持ったまま掘っていると思ったより
  早く尽きるかもしれません（**本家と同じ挙動**ですが、手触りは人の判断です）。

## 人がやること

- **`RULES-INBOX.md` の 2 件の取り込み**（`items-survival.md` の「傷が付く物は 2 種類あって」の
  表が**剣で 3 種類になりました**。食い違う場所を名指しで書いてあります）
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**は
  前の周のまま直っていません（`#status` は `bottom: 118px` 固定。`style.css:674`）。
  `docs/browser-shots/inventory.png` と `menu.png` に写っています。**`REVIEW.md` に積んであります**
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-SPEC.md` が `状態: 済`** で、**`AUTODEV-QUEUE.md` の未着手は 6 件**（3 件以上ある）
なので → **次は B（仕様）の周**です。

- 先頭は **5. ハサミ**（羊を倒さずに羊毛を刈る）。**取る前にその 1 件だけコードで数え直すこと**
  （もう入っていたら消して次の行へ。**全部を数え直すのは A の仕事**）
- **ID は 115 から取ること**（111..114 は剣が使いました。`ROADMAP.md` の予約表を更新済み）
- **`src/**` と `test/**` には 1 行も触らないこと**（B の完了条件）。120 行以内
- ハサミは **`ToolKind` に `"shears"` を足す形が使えるか**を先に見ること —— 剣でうまくいった
  形ですが、**ハサミは「殴って刈る」ので `mobs.ts` 側の判断が要ります**（剣は表に 1 行
  足すだけで済みました）。120 行に収まらなければ 2 件に割ること

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（緑）。
`audio.ts` / `sfx.ts` には 1 行も触っていないので、**直ったわけではありません。**
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
