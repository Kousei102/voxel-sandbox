# 引き継ぎメモ（2026-08-07 時点）

**このファイルは消さずに、セッションごと丸ごと書き直して次へ渡してください。**
「使い捨て」と書いてあった時期がありますが、実際にはそう運用されていません
（毎回このファイルを更新するコミットが 1 つ積まれ、それを次のセッションが読んでいます）。
消してしまうと、次の人は「どこまで確認済みか」「何が未 push か」を git log から
推測し直すことになります。

- **中身は毎回作り直す。** 古い状況を残したまま追記すると、どれが今の話か分からなくなります。
  前回の内容を引き写さず、**必ず現物（`git fetch` / `npm test`）で裏を取ってから**書くこと
- **ここに書くのは「いまの状況」だけ。** 恒久的な決まりごとは `CLAUDE.md`、
  次に何を入れるかは `ROADMAP.md` に移してください（同じことを 2 か所に置かない）
- **踏んだ失敗は「3. 進め方」に足す。** 何度も踏むものは `CLAUDE.md` へ格上げします
- **まず `CLAUDE.md` を読んでください。** このファイルはその補足です

---

## 1. いまの状態

**作業ツリーはきれいです。未確認の変更も残っていません。**

```
git fetch && git log --oneline origin/master..HEAD    # 何も出なければ push 済み
```

**未 push の「数」はここに書きません** —— このメモを書き直すたびに 1 増えて、
必ず古くなるためです。上のコマンドで確かめてください。

- `npm test` **992 件成功** / `npm run typecheck` / `npm run build` 通過
- **このセッションの 3 件はどれもユーザーがブラウザで確認済み**（全項目 OK）

このセッションは**機能を足さず、見た目の直しだけ**をやりました。新しい順に:

```
豚と羊の胴を前後 2 倍に伸ばす                                    a22fad7
見回しの演出はタイトル画面だけにする                              083343d
インベントリの注記: 文字が変わってもスロットが動かないよう浮かせ、短くする  cd43433
```

### 直近 3 件で入った線

決まりごとは `CLAUDE.md` の「インベントリ画面」「モブ」にあります。

- **インベントリの注記（`#recipehint` / `#furnacehint`）は `position: absolute` で
  スロットの真下に浮かせてあります。** 流れに残すと `.slotwrap` の幅が文字の長さで変わり、
  列は中央寄せなので**盤面とかまどの枠が左右に動きます**。**この absolute を消さないこと**
  （`test/ui.test.ts` が `src/style.css` を読んで見張っています）
- 浮かせたぶん、長い文は左へ伸びて隣の枠に重なります。**上限は全角 12.5 文字ぶん**
  （CSS の寸法から出した 138px）。`test/smelting.test.ts` がかまどの文言 6 通り、
  `test/crafting.test.ts` が全レシピ名の幅を毎回測って出します。**文言を伸ばすと落ちます**
- **ゆっくり見回す演出は `everPlayed` で最初のタイトル画面だけ**にしました。
  **条件を `!playing` に戻さないこと** —— インベントリ・かまど・死亡画面でも回り出します。
  `player.syncCamera()` は条件の外に残すこと（メニューで作り直すと位置だけ動くため）
- **豚と羊の胴は当たり判定より長く、後ろ（+Z）へはみ出します。** 前（-Z）と横は必ず判定の中
  （鼻が壁に埋まると必ず目に入る）。**胴を伸ばすときは後ろ脚の pivot も後端へ動かすこと。**
  `BodySize.half` は前後左右で 1 つなので、**判定を胴に合わせて広げないこと**
  （幅も広がって 1 ブロックの隙間を通れなくなります）

---

## 1-b. 次にやること

**着手する順はユーザーと決めること。** 見取り図は `ROADMAP.md`。
前回から候補は変わっていません（このセッションは見た目の直しだけだったため）。

### 候補 A: ベッド / リスポーン地点

**死亡時ドロップが入っているので、ここがいちばん効きます。** いまリスポーン地点は
ワールドの初期位置ひとつだけで、遠くで死ぬと 5 分以内に戻れず持ち物を失います。

- ブロックを 1 個（1..63 の空きは 23 個）と、`SaveData` に地点を 1 つ足すだけ
- **寝て朝にする**まで入れるかは別の判断です（`daynight.ts` の時刻を飛ばす操作になります）

### 候補 B: 苗木 / 作物

どちらも**「育つブロック」の仕組み**が要るので、実質ひと塊です。作物が入れば食べ物の種類も
増えます（いまは肉 3 つだけ）。**モブと違って保存が要ります**（プレイヤーが植えたものなので）。

### 候補 C: 大きいチェスト（54 枠）

`ROADMAP.md` の「チェスト — 済み」の残り。**「位置ごとの状態が 2 マスにまたがる」初めての例**に
なるので、器のほうを育てる仕事です。

### 候補 D: 地形・バイオーム系

川・湖、砂漠を平らに、サボテンのダメージなど。見た目の確認往復が多めです。

### 見た目の細かい直し（今回の続き）

ユーザーが画面を見て気付いたものを 1 つずつ直す形も回っています。豚・羊の胴は
**本家より少し長め**（マイクラは豚も羊も 16px、いまは 18 / 20px）なので、
気になったら数値を詰めてください。

---

## 2. push について

**ユーザーに確認を取ってから push すること。** 公開サイトが差し替わる外向きの操作です。
push すると `.github/workflows/deploy.yml` が走り、`npm ci` → `npm test` → `npm run build`
を通ってから GitHub Pages（https://kousei102.github.io/voxel-sandbox/）に出ます。
**テストが落ちればデプロイされません。**

押したら `gh run watch <id> --exit-status` で Actions を見届けること。
そのあと配信中の JS のハッシュを `dist/assets/` と突き合わせると、実際に出たかまで確かめられます。

**フレーム時間の p99 判定（`world.test.ts`）はマシンが混むと落ちます。**
実測: テストを 2 本同時に走らせると p99 が 170ms まで跳ねました（上限 39ms）。
CI ランナーが混んでいると同じ落ち方をするので、落ちたら**中身の退行と決めつけず
再実行を試すこと。**

ブランチは切らず `master` に直接積んでいます（デプロイのワークフローが
`master` への push で走る設定のため）。

---

## 3. 進め方（このプロジェクト特有）

**この環境では WebGL が動かず、音も鳴らせません**（理由と切り分けは `CLAUDE.md`）。なので:

- **見た目・音を変える変更は、1 まとまりごとに止めてユーザーに見て（聞いて）もらうこと。**
  未確認の変更を積み上げないでください。落ちたときに切り分けられなくなります
- 確認をお願いするときは、**具体的な確認項目を 4〜7 個の箇条書き**で出すと話が早いです。
  番号を振っておくと「5 だけ違う」と返ってきて、すぐ切り分けられます
- **直す前に原因を特定すること。** 2026-08-07、「メニューやインベントリを開くと視点が回る」に対し、
  ブラウザがポインタロックの切り替わりで大きな mousemove を寄こす説を採り、対策を 2 つ入れて
  テストまで書きました。実際の原因は `main.ts` の `else { player.yaw += dt * 0.05 }`
  （ホーム画面に寄せた演出が `!playing` の全部に掛かっていた）で、**`grep -n "yaw" src/main.ts`
  1 回で分かる話**でした。入れた 2 つは丸ごと revert しています。
  **確かめられない仮説で「備え」を書かないこと** —— この環境では効いたかどうかも確かめられません
- **確認項目に「アプリがもともと持っていない挙動」を書かないこと。** 一度、こちらが勝手に
  前提にした挙動を確認項目に入れて、ユーザーの手を一往復ぶん取らせました
- **CSS の位置は直に数値で書かないこと。** 肉ゲージのとき `#vitals` の `bottom: 74px` が
  ホットバーに食い込んでいました。いまはホットバーの寸法を `:root` の変数に持たせて `calc()` で出しています。
  注記を浮かせたときの間隔も `.slotwrap` の `--hint-gap` を `gap` と `top` で共有しています
- **画面の寸法は数に落とせます。** 文字がスロットに重なる／重ならないは、CSS の寸法から
  境目（138px = 全角 12.5 文字ぶん）を出してテストにしました。**目で見るまで気付けない類は、
  まず数にできないか考えること**
- **テストの主張も裏を取ってから書くこと。**「咀嚼はいちばんこもった音」と書いたら羊毛の足音のほうが
  こもっていて落ちました。実装ではなく主張のほうが間違っていました
- **同じ規則を 2 か所に書かないこと。** チェストのとき `Inventory.addToSlots()` を写しかけました
- **ブラウザに取られるキーを確かめること。** まとめ捨ての Ctrl+Q は環境によっては
  ブラウザ自身の終了に割り当てられていて `preventDefault()` では止められません（Shift+Q も受けています）

### デバッグに使えるもの

- `M` キー: 照準の先にモブを 1 体（**湧きの条件に関わらず**出せます）。種類はランダム
- `F3`: デバッグ表示にモブ数（`mobs N`）・落ちているアイテム（`drops N`）・かまど（`furnaces N`）・
  チェスト（`chests N`）・空腹（`food n/20`、毒なら `(毒)`）が出ます
- 開発サーバーは `npm run dev`（5173 が埋まっていると 5174 になります）

### 計測するとき

**`npm run bench` は 1 回では当てになりません。** 同じコードで `generateChunk` が
0.93〜1.46ms に振れます。疑わしいときは `git worktree add <絶対パス> HEAD` で比較対象を
用意し、`node_modules` を symlink して**同じ条件で交互に測ってください**
（終わったら `git worktree remove`）。

```
world.update    中央 3.4ms / 最悪 10〜17ms（GC 込み）
モブ 40 体      中央 0.042ms / p99 0.14ms   ← world.update の 9ms 予算に対して誤差
落とし物 128 個 中央 0.10ms / p99 1.3〜3.7ms（GC 込み。上限いっぱいの数）
npm test        12 秒前後（992 項目）
```
