# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-07・AUTODEV 52 のあと）

- `npm test` **3214 件すべて成功**（前の周 3194 から +20）・`npm run typecheck` 緑・
  **`npm run build` 通る**（`src/**` を触ったため）
- **音の一群はこの周も跳ねませんでした**（3 回走らせて 3 回とも緑）。落ち着いた値は
  羊毛 0.552 / 雪 0.716 / 砂 2.235・低い声 0.162 / 高い声 0.329 で、前の周と同じです
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1449 行**（`wc -l`。テストの数え方では 1450。上限 1500・
  **止まる目安 1450 に届いたままです。この周も ±0 でした**）
- **アイテム 115 種・`MAX_ITEM_ID` 153・111..255 の空きは 102・1..63 の空きは 9**
  （**予約表の次の空きは 154**）
- **立方体 40 / 非立方体 76**（どちらも ±0）・**モブ 9 種類**・**レシピ 59 本**・
  **精錬 7 本**・**燃料 8 種**・**食べ物 11 種**・**バイオーム 10**・**構造物 2**・
  **次元 3**・**`DamageCause` 9 種**

## この周でやったこと（AUTODEV 52 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**キューの 22（金のリンゴ）を仕様書のとおり実装しました。**

**足したのは金のリンゴ（アイテム 153）とレシピ 1 本だけ**（金インゴット 8 + リンゴ 1 の
3x3。本家と同じ形）。食べると**空腹 +4 / 満腹度 +9.6 に加えて体力が 4 戻り、満腹でも
食べられます**。**ブロックは 1 つも増えていません。**

**仕様書が先に測っておいた 3 つはどれも当たりました**:

1. **色 `0xf0a800`** —— いちばん近いブレイズロッドから **56.4**（判定の下限は 20）
2. **`main.ts` は ±0 行** —— `vitals.canEat` の 2 か所を `vitals.canEatFood(...)` に
   書き換えただけ（`useOrPlace()` は `foodOf(held)` を、`updateEating()` は `food` を渡す）
3. **`use.ts` には判断が 1 行も要りませんでした** —— `decideUse()` の中身は 1 文字も
   変わっていません（**コメントだけ直しました**）

**判断の置き場**: 満腹でも食べられるかは **`vitals.ts` の `canEatFood()` 1 か所**、
戻る量は **`items.ts` の `FOODS` の 1 行**（`heal` / `alwaysEdible` は省略可で足しました）。
**`canEat` の getter は 1 文字も動かしていません**（満腹の門はそのまま。緩めると
全部の食べ物が満腹でも食べられます）。

**サブエージェントは立てず、親が実装して親が `git diff` を点検しました**
（`AUTODEV.md` の C-1 / C-2 の「返らないなら自分で」。前の周に点検役が `git stash` を
打った件があるので、`rules/testing.md` の決まりごとに従っています）。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので、判定表の
2 行目に当たります（未着手は 8 件あるので A ではありません）。
**キューの先頭は 23（クモの巣）**で、**`main.ts` に配線が要る見込みの 4 件のうちの
1 つ**です（`AUTODEV-QUEUE.md` の「⚠ `main.ts` の行数がもう効いています」）——
**仕様を書く前に、下の「人がやること」の 1 つ目を見ること。** `main.ts` に 1 行でも
足す周は止まる目安 1450 を越えます。**0 行で通せる 27a（防具の枠）を先に取るなら、
それは並びを変える話なので `AUTODEV-QUEUE.md` の A-2 の順で判断すること。**

## ブラウザで見てほしいところ

**この周は `node tools/browsershot.mjs` で 5 枚撮り直して `Read` で開いて見ました**
（WebGL 2.0・`gl.getError()` = 0・console のエラー無し）。**金のリンゴは 1 枚にも
写りません** —— 新しい世界の持ち物は空で、色が出るのはクリエイティブの一覧だけ
（あの 5 枚に含まれません）。**見たのは「`main.ts` の 2 行を書き換えた `dist/` が
今までどおり起動して描けること」**で、`hud.png` の体力・空腹・ホットバー・十字と
`inventory.png` の枠は前の周と同じでした。

**人に見てほしいのは 2 つ**: **金のリンゴの色 `0xf0a800` が一覧で金インゴット・
ブレイズロッド・ブレイズパウダーと見分けられるか**（数値では 56.4 離れていますが、
どれも「金色」なので並ぶと紛らわしいかもしれません）と、**金インゴット 8 個 +
リンゴ 1 個に「体力 4 回復」が見合うか**（本家と同じ材料・同じ合計量ですが、
**持続する効果の器が無いので本家の「再生 II が 5 秒」を即時 4 回復に均してあります**
—— 戦闘中の手ざわりが本家と別物です。`TUNING.md`）。

**持ち越し（まだ誰も見ていません）**: **本棚が「本の詰まった棚」に見えるか**
（側面 `0x9c5064` がかなり赤紫）/ **紙 `0xd4e0ec` が雪玉と 25.5 で見分けられるか** /
**本棚 1 個に材料 9 個が見合うか** / **リンゴの色 `0xe0342c` が赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か** / **地面に落ちた
リンゴが赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか** /
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか**（しゃがみで
止まる手はありません）/ **はしごの上端から地上へ出る手触り** / **はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に
見えるか** / **`CANE_GROW_SECONDS = 180` が待てる長さか** / **自然に生えたサトウキビは
伸びません**（理由は `TUNING.md`）/ **⚠ 前から遊んでいる世界だけの見え方**: 前に浜の
サトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて見えることがあります /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度 / サボテンに
当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**）

**`MAX_ITEM_ID` を新しい番号へ移すと、突き合わせているテストが `tsc` で落ちます**
（TS2367）。`test/blocks.test.ts` は共有帯の一覧と一緒に `MAX_ITEM_ID === BOOKSHELF` の
形で上限を見張っているので、上限を 153 へ移した瞬間に**「'153' と '152' は重ならない」**で
`npm run typecheck` が落ちました。**比べる相手を新しい番号にするのが正解**で、
**型で止まるので安全な罠**です（伸ばし忘れのほうは型では止まりません）。
`rules/items-survival.md` に据えました。

## 人がやること

- **設計の判断を 1 つお願いします（26 周持ち越し。もう待てません）:
  `main.ts` を 2 つに割ってよいか。** いま **1449 行で、止まる目安 1450 に届いています**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **20・21・22 は予告どおり ±0 行で通りました**が、**キューの先頭に残っているのは
  配線が要る 4 件（23 クモの巣・24 ケーキ・25 氷・26 フェンス）**です。
  **0 行で通せる見込みは 27a（防具の枠）だけ**なので、**次に取れる余裕はあと 1 周**です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 36 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
