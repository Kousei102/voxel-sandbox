# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- `npm test` **2977 件すべて成功**（前の周の 2965 から **+12**）・`npm run typecheck` 緑・
  **`npm run build` 緑**（`src/**` を触った周のため）。**`npm run bench` は走らせていません**
  （生成にもメッシュ化にも 1 行も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1464 行**
  （**触ったのは見出しコメント 1 行だけ**なので行数は動いていません）
- **アイテム 99 種・`MAX_ITEM_ID` 134・111..255 の空きは 121・1..63 の空きは 9**
  （**この周で 134 = 雪玉を 1 個だけ取りました**）
- **モブ 9 種類**（受動 4 = 豚・羊・鶏・牛 / 敵対 4 = ゾンビ・クモ・ブレイズ・
  エンダーマン / ボス 1 = ドラゴン）・**クラフト 45 本 / 精錬 7 本 / 食べ物 8 種**
- **飛び道具 6 種類**（火球・矢・エンダーアイ・ブレス・卵・**雪玉**）・
  **投げられるもの 2 つ**（卵・雪玉）

## ⚠ 次に `main.ts` を触る周がまず見ること: 1464 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
**8 周続けて `main.ts` に判断を 1 行も足していないので、持ち越しです。**
**解けるのを待っているのはキューの 15 番（ミルクバケツ）と 16 番（サボテン）**で、
**どちらも `main.ts` に振り分けの 1〜5 行が要ります。** 先に判断を別ファイルへ出すこと。
**次に取る 14 番（鉄・金・ダイヤのブロック）は 1 行も触りません。**

## この周でやったこと（AUTODEV 25 の C・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が **8 件**・`AUTODEV-SPEC.md` が **`未着手`** だったので、
判定表を上から当てて **C（実装）の周**。**キューの先頭 13 番（雪玉）**を実装しました。

- **雪ブロック（9）を掘ると雪玉 4 個**（`items.ts` の `DROPS` に 1 行）。
  **雪玉 4 個を 2x2 で雪ブロック 1 個に戻せます**（`crafting.ts` に 1 行）——
  **落とし物を差し替えたので、この戻すレシピが無いと雪が置けなくなります**
- **右クリックで投げられます**（`items.ts` の `THROWN` に 1 行 + `projectiles.ts` の
  `PROJECTILE_KINDS` に 1 行）。**飛び方は卵とまったく同じで、当たっても何も起きません**
  （`damage` を渡さない = 既定の 0。本家の雪玉もブレイズ以外には効きません）
- **`use.ts` は 1 行も触っていません**（`thrownProjectile()` の表を通るため）。
  **`main.ts` も `throwItem()` の見出しコメント 1 語だけ**（`git diff --stat` で 1 行）
- **サブエージェントは立てていません。** `AUTODEV.md` の C-1 の「同じ番の中で結果が
  返らないなら自分で実装する」に従い、点検（C-2）も親が `git diff` を読んでやりました
- **差し戻し 0 回。** typecheck / test / build は 1 回で緑（音の一群も 2 回とも緑）

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手が 8 件（3 件以上）・`AUTODEV-SPEC.md` が `済` なので
→ 次は B（仕様）の周**です。**先頭は 14 番（鉄・金・ダイヤのブロック）。**

- **B の周は `src/**` を 1 行も触らないこと。** 先頭の 1 件だけコードで数え直してから
  仕様書を書くこと（**もう入っているかもしれません**）
- **仕様書は 120 行以内**（`wc -l` で **119 以下**にすること。`test/docs.test.ts` は
  末尾の改行を 1 行として数えます。`rules/testing.md`）
- **ID は `ROADMAP.md` の予約表から。次の空きは 135 です**（134 = 雪玉まで使用済み）。
  14 番は**立方体 3 つ**なので **135・136・137** の見当ですが、**取るのは C の周**です

## ブラウザで見てほしいところ

**この周で見た目に出たのは 2 つで、どちらも撮って確かめました**
（`docs/browser-shots/creative-snowball.png` / `snowball-flight.png`）。

- **一覧の末尾の「雪玉」が拾いやすいか。** 白っぽいものが **4 つ**並びました
  （羽根・卵・糸・雪玉）。**青みがあるのは雪玉だけ**（rgb(188,216,239)）ですが、
  **拾いたいものが拾えるかは目でしか決められません**
- **投げた雪玉の手触り。** 卵と同じ速さ・同じ落ち方で、**当たっても何も起きません**
  —— **投げる楽しさが「飛んで落ちるのを見る」だけで足りているか**

持ち越し: **クモを 3 匹倒すまで弓が作れない**道のり / **夜の敵対がゾンビとクモで半々**と
**クモの速さ 6.0** / **クモが朝に燃えてよいか**（本家は燃えません）/ **投げた卵に
使い道が無い** / **牛の胴が寸詰まりに見えないか** / **ステーキと革が一覧で
見分けられるか** / **産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒**。

## この周で分かったこと

**決まりごとを 2 か所に据えました**（`rules/` を `Edit` で直しました）。

- **`rules/items-survival.md`**: **落とし物を「自分以外のもの」に差し替えたら、
  戻す道を同じ周で用意すること。** 砂利・草むらの `otherwise` は「自分が戻る」ので
  この穴が開きませんが、**別のものへ差し替えたときだけ**、置けるブロックが
  ゲームから消えます。**落ちる 4 個と戻す 4 個を食い違わせないこと**も同じ行に。
- **`rules/projectiles.md`**: 5 種類 → **6 種類**にし、**雪玉が卵の値を写したもの**
  （片方だけ触ると弾道が食い違う）と、**絵で確かめるときは空を向いてから撃つこと**
  を足しました。**地面へ向けて撃った 1 枚目は何も読めませんでした** ——
  草の上の 0.25m の四角は絵になりません（撮り直した）。

**「一覧は送らないと末尾が写らない」も撮り直しの理由でした**（雪玉は 134 = 最後）。
`docs/browser-shots/README.md` に手順ごと書き足してあります。

## 人がやること

- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 7 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**この周は 2 回走らせてどちらも緑でした**（羊毛 0.552 < 雪 0.716 <
砂 2.235・低い声 0.162 / 高い声 0.329 と、名指しされている落ち着いた値のまま）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
