# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、
**ユーザーが通しで遊んで見つけた不具合は `REVIEW.md`**。

## いまの状態（2026-08-31）

- ブランチ **`loop/endgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm test` は通ります。**`npm test` 2194 件すべて成功**
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1485 行 / 上限 1500（余白 15 行）**。この周は 1 行も触っていません
- **`RULES-INBOX.md` に未取り込みが 2 件**（`mobs.md` の「攻め方を入れ替える」節と
  `projectiles.md` の 1 行）。**取り込むのは人が居る周です**
- `TASKS.md` の「未着手」も `REVIEW.md` の `[ ]` も**空**です

## 直前にやったこと — 2-14. ドラゴンの行動パターン

**ユーザーが通しで遊んで見つけたもの**（もとは `REVIEW.md`。「追いかけてきての近接しか
しない・ブレスを撃たない」）。**この周でユーザーが `TASKS.md` へ移し、ループが取りました。**

- **`MobDef.phases`（`MobPhase[]`）を足しました。** 持っているモブだけが攻め方を
  順ぐりに入れ替えます（`orbit` / `teleport` と同じ作法で、**`update()` にも `step()` にも
  「ドラゴンなら」は 1 行もありません**）。中身は**既にある 3 つの判断を出し入れする印だけ**:
  `chase`（`chasing()`）/ `shoot`（`fire()`）/ `above`（`aimOrbit()` の高さ）
- **ドラゴンの番**: 近接 10 秒 → ブレス 12 秒（輪を回ったまま**プレイヤーの 10 マス上**から
  **1.5 秒に 1 発**）→ 旋回 8 秒（何も仕掛けない）。1 周 30 秒
- **`DRAGON.ranged` にブレス**（重み 6・間合い 4〜40m）。**ブレスの弾道を火球と同じに**
  しました（落ちない・抵抗なし・速さ 30 = 火球の 2.5 倍）
- テストは 23 件増えて 2194 件（`test/mobs.test.ts` の「攻め方」と
  `test/projectiles.test.ts` の弾道）。数値は全部 `TUNING.md` の
  「エンダードラゴン」の表に入れました

### ブラウザで見てほしいところ（見た目に出るもの）

- **エンドで 30 秒ながめると 3 通りの動きが順に出ます**: 突っ込んでくる（10 秒）→
  上空から紫の弾を撃ってくる（12 秒）→ 何もせず輪を回る（8 秒）。
  **旋回の番がクリスタルを割りに行く隙**のつもりです
- **ブレスは速く・まっすぐ飛びます**（火球の 2.5 倍）。**避けられるかどうか**と、
  **1 発 6 ダメージ（体力 20 に対して 3 発）が重すぎないか**を見てください
- ドラゴンが**プレイヤーの 10 マス上を飛ぶ**ので、上を向いたときに画面に入るかどうか

## 次にやること

**`TASKS.md` の「未着手」も `REVIEW.md` の `[ ]` も空です。**
無人の周がここへ来たら、`TASKS.md` 末尾の「取るタスクが無くなったら」に従ってください
（要点は**何も実装しない・コミットもしない・push もしない**）。

人がやること:

- **通しのテストプレイ** —— `npm run dev` で端から端まで遊び、
  おかしいところを `REVIEW.md` に足す（書き方はあのファイルの中）
- **`RULES-INBOX.md` の 2 件の取り込み**（`.claude/rules/` は無人の周には触れません）
- **`TUNING.md` の一括調整**（この周でドラゴンの行を 6 つ増やしました）
- **`master` へのマージ**

**次に `main.ts` へ何かを足す周は、まず減量から取ること**（余白 15 行）。
次に出しやすいのは**左クリックの振り分け**（`use.ts` に `decideAttack()` を足す形）。
見取り図は `docs/tasks-done.md` の 2-13b-2 の節。**上限は上げないこと。**

**音のテスト（`test/audio.test.ts`）はまれに 1 件落ちます。** この周で 2 度見ました
（「cutoff が低い材質ほどこもって鳴る」と「声色が波形に出る」）。**どちらも走らせ直すと
緑**で、実装は触っていません。**2 回続けて落ちたときだけ退行として扱うこと**
（`LOOP.md` の 5.）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
