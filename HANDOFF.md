# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05）

- `npm test` **3029 件すべて成功**（前の周と同じ。**`src/**` も `test/**` も 1 行も
  触っていない周**です）・`npm run typecheck` 緑
- **`npm run build` は走らせていません**・**`npm run bench` も走らせていません**
  （どちらも `src/**` を触った周だけ。B の周は文書 2 つだけ）
- **音の一群は 1 回で緑**でした（羊毛 0.552 < 雪 0.716 < 砂 2.235・低い声 0.162 < 高い声 0.329）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1443 行**（上限 1500・**止まる目安 1450 まで残り 7 行**。この周で 0 行）
- **アイテム 103 種・`MAX_ITEM_ID` 138・111..255 の空きは 117・1..63 の空きは 9**（変わらず）
- **立方体 39 / 非立方体 69**・**クラフト 51 本 / 精錬 7 本 / 食べ物 8 種**・**モブ 9 種類**（変わらず）

## この周でやったこと（AUTODEV 32 の B・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が 6 件（3 件以上）・`AUTODEV-SPEC.md` が **`状態: 済`** だったので、
判定表を上から当てて **B（仕様）の周**。**キューの先頭 16 番（サボテンに触るとダメージ）を
`AUTODEV-SPEC.md` にしました**（**109 行**。上限 120）。**`src/**` は 1 行も触っていません。**

**取る前にコードで数え直しました**（B の決まり）: `grep -rn -i cactus src/ test/` が出すのは
**ブロック定義（27）・地形生成・メッシュ化の絵だけ**で、**ダメージは 0 行**でした。

仕様の骨は「**溶岩とまったく同じ形**」です。判断の置き場は 4 つに割ってあります:

- **「どのブロックが刺さるか」は `blocks.ts` の表**（`BlockDef.spiky` と `isSpiky()`。
  `liquid` / `hot` / `falls` と同じ作法で、**`id === CACTUS` と書かせない**）
- **「触っているか」は `player.ts`**（`inLava` と同じで事実だけ）
- **走査は `physics.ts` の `bodyTouches()`**（述語を受け取る幾何。サボテンを知らない）
- **「どれだけ痛いか」は `vitals.ts`**（0.5 秒ごとに 1・死因「サボテン」・
  **`cooldown` は渡さない**・**下限を作らないのでサボテンでは死にます**）

**ID は 0 個**（`ROADMAP.md` の予約表を触らない周です）。**`main.ts` は 1 行**（1444 行になる見込み）。

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-SPEC.md` が `状態: 未着手` なので → 次は C（実装）の周**です。
**仕様書を要約せず全文で渡し、`rules/vitals.md`・`rules/blocks-shapes.md`・
`rules/mobs.md`・`rules/testing.md` を引いて渡すこと**（自動では読まれません）。

- **16 番のあとが本当に足りません。** `main.ts` は 16 番で 1444 行になり、
  **止まる目安 1450 まで残り 6 行**です。**17 番以降を仕様にする周は、
  先に `main.ts` の行数を見てから割ること**（下の「人がやること」の判断待ち）。
- **次に取る ID は 139 から**（`ROADMAP.md` の予約表「139..255 予備 117 個」）。
  **16 番は 1 個も使いません。**

## ブラウザで見てほしいところ

**この周の変化は文書 2 つだけで、見た目も手触りも 1 つも動いていません**（`src/**` 0 行）。
だから**撮っていません**（`AUTODEV.md` の C-3 は「見た目に出るものを触った周だけ」）。

- 持ち越し（前の周から。まだ誰も見ていません）: **牛を搾る / ミルクを飲んで毒が止まるか** /
  **ミルクバケツの色（`0xeaf2f8`）が羽根・卵・糸・雪玉と見分けられるか** /
  かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
  鉄・金・ダイヤのブロックが材料の山と見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**
  （`style.css` の `.slot .label`）/ クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い /
  牛の胴が寸詰まりに見えないか / 産卵の間隔 300〜600 秒
- **次の周（16 番）で出るもの**: サボテンに歩いて当たったときの痛さ（0.5 秒ごとに 1）と、
  **上に立っても痛くないこと**が我慢できるか。**本家では上に立つと痛い**のですが、
  そうするには `CACTUS_BOX` の上面を削ることになり、積んだサボテンの継ぎ目に出ます。

## この周で分かったこと

**決まりごと 0 件**（`rules/` に据えるものも `RULES-INBOX.md` に置くものもありません）。
B の周は文書だけなので、踏む落とし穴がありませんでした。

**数え直しで 1 つ確かめたこと**: `physics.ts` の押し戻しは
`contactMin - half - EPS` なので、サボテンに押し付けられたプレイヤーは
**マスの中へ 0.0615 だけ入ります**（サボテンの箱が 1/16 細いぶん）。だから
「**体の箱がサボテンのマスと重なる**」という判定でちょうど本家と同じになり、
**隣に立っただけでは重なりません**（仕様書 §5 の「隣に立っただけでは偽」がこれを押さえます）。

## 人がやること

- **設計の判断を 1 つお願いします（4 周持ち越し・16 番の次で効きます）: `main.ts` を 2 つに
  割ってよいか。** いま **1443 行、16 番を入れて 1444 行で、止まる目安 1450 まで残り 6 行**です。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 14 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
