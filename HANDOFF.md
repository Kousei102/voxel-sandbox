# 引き継ぎメモ（2026-08-06 時点）

このファイルは**使い捨て**です。読んで頭に入れたら消してかまいません。
恒久的な決まりごとは全部 `CLAUDE.md`、次に何を入れるかは `ROADMAP.md` にあります。
**まず `CLAUDE.md` を読んでください。** ここには「いまの状況」だけを書きます。

---

## 1. いまの状態

**かまど + 精錬までコミット済みです。push はまだです。**
その 1 つ前の落ちたアイテムも確認済み・コミット済みで、あわせて 2 コミット先行しています。

- `npm test` **912 件成功** / `npm run typecheck` / `npm run build` 通過
- **下の 10 項目はユーザーがブラウザで確認済み**（全部 OK）
- **`origin/master` の値は毎回 `git fetch` して確かめること。** 前のメモの push 状況を
  引き写したまま出したら、実際にはもっと先まで push 済みでした

### 何を入れたか

決まりごとは `CLAUDE.md` の「かまどと精錬」に書きました。要点だけ:

- `smelting.ts`（精錬の表と `tickFurnace`）＋ `furnaces.ts`（位置 → かまど 1 台、セーブ）。
  どちらも DOM も three も触らないので丸ごと `npm test` で見ています
- **画面は `CraftScreen` を増やさず、`SlotArea` に 3 枠を足しただけ**です。掴む・配る・
  シフトクリック・数字キー・ドラッグ配分がそのまま効きます
- **精錬の代用 2 つを戻しました** —— 鉄・金の鉱石は鉱石のまま落ち、砂 4 個 → ガラスの
  クラフトは消しました。テストが両方を押さえています
- 点火中は別ブロック（`FURNACE_LIT` = 39）で光ります。差し替えるのは `main.ts` だけ
- 新しいアイテム: 焼き豚（83）。**まだ食べられません**（空腹が無いため）

### 確認済みの項目（ブラウザ・全部 OK）

1. 作業台で**丸石 8 個を輪に並べるとかまどができる**（真ん中は空ける）
2. かまどを置いて右クリックすると、材料・燃料・焼き上がりの 3 枠が出る
3. 鉄鉱石を材料に、石炭を燃料に入れると**焼き始め、矢印がオレンジになる**。
   10 秒ほどで鉄インゴットが 1 個できる（残り時間が数字で出ます）
4. **焼いている途中で E を押して閉じ、少し待ってから開き直しても進んでいる**
   （閉じると止まる、では困ります）
5. 焼いている間、**かまど自体が光る**（暗いところで置くと分かりやすいです）
6. 焼き上がりの枠に**物を入れようとしても入らない**（取り出しだけできる）
7. インベントリの鉄鉱石を **Shift+クリックすると材料の枠へ、石炭なら燃料の枠へ**入る
8. 中身を入れたままかまどを壊すと、**中身が地面に落ちる**
9. 一度「保存」して再読み込みしても、かまどの中身と焼き加減が残っている
10. 砂を掘って持っていても、**クラフトではガラスにならない**（かまどで焼く）

---

## 2. push について

**ユーザーに確認を取ってから push すること。** 公開サイトが差し替わる外向きの操作です。
push すると `.github/workflows/deploy.yml` が走り、`npm ci` → `npm test` → `npm run build`
を通ってから GitHub Pages（https://kousei102.github.io/voxel-sandbox/）に出ます。
**テストが落ちればデプロイされません。**

押したら `gh run watch <id> --exit-status` で Actions を見届けること。
そのあと配信中の JS のハッシュを `dist/assets/` と突き合わせると、実際に出たかまで確かめられます。

**フレーム時間の p99 判定（`world.test.ts`）はマシンが混むと落ちます。**
実測: テストを 2 本同時に走らせると p99 が 170ms まで跳ねました（上限 39ms）。
CI ランナーが混んでいると同じ落ち方をするので、落ちたら**中身の退行と決めつけず
再実行を試すこと**（フェーズ 1 では実際にこれで止まりました）。

ブランチは切らず `master` に直接積んでいます（デプロイのワークフローが
`master` への push で走る設定のため）。

---

## 3. 進め方（このプロジェクト特有）

**この環境では WebGL が動かず、音も鳴らせません**（理由と切り分けは `CLAUDE.md`）。なので:

- **見た目・音を変える変更は、1 まとまりごとに止めてユーザーに見て（聞いて）もらうこと。**
  未確認の変更を積み上げないでください。落ちたときに切り分けられなくなります
- 確認をお願いするときは、**具体的な確認項目を 4〜6 個の箇条書き**で出すと話が早いです。
  番号を振っておくと「5 だけ違う」と返ってきて、すぐ切り分けられます
- **確認項目に「アプリがもともと持っていない挙動」を書かないこと。** 一度、こちらが勝手に
  前提にした挙動を確認項目に入れて、ユーザーの手を一往復ぶん取らせました。
  **お願いする前に、その挙動が本当にあるかコードで確かめること**
- **リファクタのコミットに新しい状態機械を混ぜないこと。**「挙動は変えない」と書いたコミットは、
  本当に変えないこと（`mouseup` の配線が無い状態のドラッグを混ぜかけて、
  「掴んだものが置けない」画面になりました）

### デバッグに使えるもの

- `M` キー: 照準の先にモブを 1 体（**湧きの条件に関わらず**出せるので、
  「湧かない場所なのか、描けていないのか」を切り分けられます）。種類はランダムです
- `F3`: デバッグ表示にモブ数（`mobs N`）・落ちているアイテムの山（`drops N`）・
  置いてあるかまど（`furnaces N`）が出ます
- 開発サーバーは `npm run dev`（5173 が埋まっていると 5174 になります）

### 計測するとき

**`npm run bench` は 1 回では当てになりません。** 同じコードで `generateChunk` が
0.93〜1.46ms に振れます。疑わしいときは `git worktree add <絶対パス> HEAD` で比較対象を
用意し、`node_modules` を symlink して**同じ条件で交互に測ってください**
（終わったら `git worktree remove`）。

```
world.update    中央 3.4ms / 最悪 10〜17ms（GC 込み）
モブ 40 体      中央 0.042ms / p99 0.14ms   ← world.update の 9ms 予算に対して誤差
落とし物 128 個 中央 0.10ms / p99 1.3〜3.7ms（GC 込み。上限いっぱいの数）
npm test        12 秒前後（912 項目）
```
