# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05・AUTODEV 35 のあと）

- `npm test` **3079 件すべて成功**（この周で **+20**）・`npm run typecheck` 緑・
  `npm run build` 緑（`src/**` を 3 ファイル触ったので走らせました）
- **音の一群は 1 回だけ赤くなり、走らせ直して緑**（`rules/testing.md` のとおり）。
  落ち着いた値は前の周と一致（羊毛 0.552 < 雪 0.716 < 砂 2.235 / 低い声 0.162 < 高い声 0.329）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 0 行）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（**この周でセーブは 1 バイトも増えていません**）
- **`main.ts` 1448 行**（上限 1500・**止まる目安 1450 まで残り 2 行**。この周で +3）
- **アイテム 107 種・`MAX_ITEM_ID` 142・111..255 の空きは 113・1..63 の空きは 9**
- **立方体 39 / 非立方体 71**（**ブロックは 1 つも増えていません**）・**モブ 9 種類**・
  **レシピ 53 本**（+2）・**食べ物 9 種**（+1）

## この周でやったこと（AUTODEV 35 の C・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 未着手` だったので判定表の 3 行目に当たって **C（実装）の周**。
**17b（ボウルとキノコシチュー）を仕様書どおりに実装しました。差し戻し 0 回。**

- **141 = ボウル / 142 = キノコシチュー**（**どちらもアイテムだけ**）。
  板 3 個の V 字 → ボウル 4 個（**3 幅なので作業台が要る**）/ ボウル + 赤 + 茶 の
  形なし → シチュー 1 個（**2x2 で作れる**）/ 食べると **6 / 7.2**（焼き鳥と同点）
- **食べ切ると空のボウルが手の中に戻ります。** 戻すものを決めるのは
  **`items.ts` の `EMPTIES` の表 1 本**（`emptyAfterEating()`）で、
  **`main.ts` は引いて貼る 3 行だけ**（`use.ts` も `vitals.ts` も **0 行**）
- **`stack: 1` は手触りではなく不変条件**（2 個以上積めると、1 個食べただけで
  山ごとボウル 1 個に潰れます）。`TUNING.md` と `rules/` の両方に書きました
- サブエージェントは立てず、**親が実装して `git diff` を読んで点検**しました
  （`AUTODEV.md` の C-1 / C-2 の但し書き。周を閉じずに終わるほうが高くつくため）

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` で、
`AUTODEV-QUEUE.md` の未着手が **4 件**（3 件以上）なので判定表の 2 行目です。

- **キューの先頭は 18（サトウキビと砂糖）**。**ただし下の「人がやること」を先に読むこと** ——
  **18 も 19（はしご）も `main.ts` の配線が要り、残りは 2 行しかありません。**
  仕様書を書く前に「何行で通せるか」を数え、**通せないなら止まって人を呼ぶこと**
  （`AUTODEV.md` の停止条件 2）
- **次に取る ID は 143 から**（`ROADMAP.md` の予約表）
- **B の周は書くファイルそのもので `rules/` を引くこと**:
  `grep -l '"AUTODEV-SPEC.md"' rules/*.md` → `rules/testing.md`（**120 行の上限は
  `wc -l` + 1 で数えられます**。3 回踏んでいます）

## ブラウザで見てほしいところ

**この周は `npm run shot -- terrain` を 1 枚だけ撮りました**（`shots/terrain.png`）。
**置けないアイテム 2 つなので、地形にもモブにも新しい形は出ません** —— 撮ったのは
「見た目に 0 の変更である」ことの裏取りで、`Read` で開いて**面の欠けも裏返りも無く**、
前の周と同じ森・草原・砂浜が出ていることを確かめました。

- **17b で見てほしいこと**: **ボウル `0x7a4a24` とシチュー `0xf0dcb4` が、赤キノコ・
  茶キノコと並んだ一覧で見分けられるか**（**色は DOM なので `npm run shot` に写りません**。
  数値では 4 つがどの 2 つも RGB 60 以上、既存 107 種からも 20 以上）と、
  **シチューが焼き鳥と同点の 6 / 7.2 で、材料 3 つ（板 3 + 赤 + 茶）に見合うか**（`TUNING.md`）。
  **食べ終わりにボウルが手に戻る手ごたえ**も、押してみないと分かりません
- 持ち越し（まだ誰も見ていません）: **森を歩いたときのキノコの密度**（森 0.015 /
  針葉樹林 0.01 は暫定）/ **走っていて赤・茶を草むらと取り違えないか** /
  **サボテンに当たったときの痛さ**（0.5 秒ごとに 1）/ **牛を搾る / ミルクを飲んで毒が止まるか** /
  **ミルクバケツの色（`0xeaf2f8`）が羽根・卵・糸・雪玉と見分けられるか** /
  かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
  鉄・金・ダイヤのブロックが材料の山と見分けられるか /
  **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`。
  **「キノコシチュー」は 7 文字でいちばん長い名前です**）/ クモの速さ 6.0 と朝に燃えてよいか /
  投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか / 産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 1 件・`rules/` に据えました）

1. **`rules/items-survival.md`** に**「食べ切ると器が戻るもの（`EMPTIES`）」**の節を
   足しました。要は **器が戻る食べ物は `stack: 1` でなければならない**（戻すのが
   `setSelected` なので、積めると山ごと潰れる）と、**`inventory.add()` で戻さない**
   （満杯のときに器が黙って消える）の 2 つです。**どちらも型では止まりません。**

**前の周の記録が効いた例**: `MAX_ITEM_ID` を 142 へ移した瞬間に
**`items.ts` の `BROWN_MUSHROOM` の import が余って `npm run typecheck` が落ちました** ——
これは `rules/items-survival.md` に**もう書いてあった**罠で、1 回で直せました。

## 人がやること

- **設計の判断を 1 つお願いします（9 周持ち越し・18 番でいよいよ効きます）:
  `main.ts` を 2 つに割ってよいか。** いま **1448 行で、止まる目安 1450 まで残り 2 行**。
  **`test/ui.test.ts` の 41 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **次の 18（サトウキビ）と 19（はしご）はどちらも配線が要ります** ——
  返事が無ければ、次の C の周は `AUTODEV.md` の停止条件 2 で止まります。
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 19 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
