# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- `npm test` **2934 件すべて成功**・`npm run typecheck` 緑。**この周は `src/**` と `test/**`
  を 1 行も触っていません**（B の周なので）。`npm run build` と `npm run bench` は
  走らせていません（`src/**` に差分が無いため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1463 行**
- **アイテム 97 種・`MAX_ITEM_ID` 132・111..255 の空きは 123・1..63 の空きは 9**

## ⚠ 次に `main.ts` を触る周がまず見ること: 1463 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
**牛の周もこの周も `main.ts` を 1 行も触っていないので、持ち越しです。**
**次に触る周は、先に判断を別ファイルへ出すこと。**（クモの周は `main.ts` 0 行で閉じます）

## この周でやったこと（AUTODEV 21 の B・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 済`・`AUTODEV-QUEUE.md` の未着手が 3 件だったので
**B（仕様）の周**。当たった 1 種類だけをやりました。**キューの先頭 9「クモと糸」を
数え直してから `AUTODEV-SPEC.md` に書き直しています。**

- **数え直しの結果**（コードが根拠）: `src/**` と `test/**` に `spider` / `クモ` / `蜘蛛` /
  糸のアイテムは **1 つも無し**（`crafting.ts` と `items.ts` の「蜘蛛がまだ居ないので
  羊毛で代用」というコメントだけ）。`MobKind` は 8 語・`HOSTILE_KINDS` は表から作られて 3 語
- **1 件を 2 件に割りました。** 「クモ + 糸 + 弓のレシピ差し替え」を 1 枚にすると **142 行**で、
  **120 行の上限（`test/docs.test.ts`）を越えました** —— `AUTODEV.md` の B のとおり、
  前半（**9. クモと糸**）だけを仕様書にし、後半（**10. 弓を羊毛 3 → 糸 3 に戻す**）を
  `AUTODEV-QUEUE.md` へ書き戻しています（**11 スケルトン / 12 刈られた羊**へ繰り下がり）
- **糸は 1 周だけ使い道がありません**（革とまったく同じ状態）。弓は 10 番で戻します
- **ID は 1 個だけ**（**133 = 糸**）。モブは ID を取りません。**予約はまだしていません**
  —— `ROADMAP.md` の予約表に「実装済み」と書くのは C の周の仕事です

### 仕様書で決めたことのうち、C の周が踏みやすい所

- **`fireproof: true` にしないこと。** 本家のクモは日光で燃えませんが、ここでは
  `fireproof` が**溶岩と日光の両方**を止める 1 つの印です（`mobs.ts` の `sunlightBurns()` の
  呼び口が `!def.fireproof` だけを見ています）。**`sunproof` のような列を足すのも禁止**
  にしました（`MobDef` に 1 列増えると 8 種類ぜんぶに手が要ります）。**クモは朝に燃えます**
- **`spawnWeight: 100` はゾンビと同じ重み**なので、**夜の敵対がゾンビとクモで半々になります。**
  `test/mobs.test.ts` の「**ゾンビがいちばん出やすい**」と「**敵対の上限を超えない**」は
  **必ず赤くなります** —— **ゆるめるのではなく数え直すこと**（上限のほうはいまゾンビしか
  数えていないので、クモを足さないと半分ぶんが素通りします）
- **`test/blocks.test.ts` の「共有帯のアイテムは 19 個・132 まで」と「111..255 の空きは 123」も
  必ず赤くなります** —— **20 個・133 まで / 空き 122** に数え直すこと
- **`census()` の `counts` と `show()` に `クモ` を足すこと**（忘れると `counts[name]++` が
  `NaN` になり、数えているつもりで緑になります）
- **当たり判定 1.4 x 0.9 は、いちばん横に広いモブ**になります（いままでの最大は 0.9）。
  **`longBody` の例外は豚と羊だけ。そこへクモを足さず、形のほうを削ること**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 未着手`** なので → **次は C（実装）の周**です
（**9. クモと糸**）。**終わりに `master` へ push。**

- **仕様書は全文をサブエージェントに渡すこと**（要約しない）。**背景（async）で起動しない**
  —— 同じ番の中で結果が返らないなら、自分で実装するほうが安いです
- **渡す `rules/`**: `mobs.md` / `items-survival.md` / `vitals.md` / `testing.md`
  （`grep -l '"src/mobs.ts"' rules/*.md` で引けます。自動では読み込まれません）
- **見た目に出る周です。** `node tools/browsershot.mjs` と `npm run shot -- mobs` を撮って
  **`Read` で開いて見ること**（脚 8 本が胴に埋まっていないか・目が見えるか）。
  鶏の翼と牛の顔で 2 度踏んだ罠がそのまま当てはまります

## ブラウザで見てほしいところ

**この周は `src/**` を 1 行も触っていないので、公開サイトの見た目は変わりません。**
前の周（牛）から持ち越しの 3 つが、そのまま残っています:

- **牛の胴が寸詰まりに見えないか**（本家の 18px ではなく 12px。当たり判定に収めたぶん）
- **ステーキ（rgb(143,82,48)）と革（rgb(160,106,65)）が一覧で見分けられるか**
- **ステーキが焼き豚と同点（空腹 +8 / 満腹度 +12.8）でよいか**

遊んで決めてほしいことの持ち越し: **投げた卵に使い道が無い** / **卵と羽根がどちらもほぼ白**
（**糸をこの 2 つから離した冷たい灰 `0xb8bcc8` にする**と仕様書に書きました）/
**産卵の間隔 300〜600 秒** / **矢が 2x2 で作れなくてよいか** /
**苗の緑と `GROW_SECONDS` 180 秒と `#status` の位置**。

## この周で分かったこと

**決まりごとを 2 件、`rules/testing.md` に据えました**（`Edit` で普通に直しました）。

1. **`test/docs.test.ts` の行数は `split("\n").length`** なので **`wc -l` より 1 多い** ——
   仕様書を `wc -l` で 120 行ちょうどに収めたら 121 と数えられて赤くなりました。
   **この落とし穴は前の B の周（牛）がもう書いていたのに、また踏みました** ——
   決まりごとの `paths` が `test/**/*.ts` だけで、**仕様書を書く周には引けなかった**ためです。
   **`paths` に `AUTODEV-SPEC.md` と `AUTODEV-QUEUE.md` を足しました**（次の B の周は
   `grep -l '"AUTODEV-SPEC.md"' rules/*.md` で引けます）
2. **削るときは折り返しを変えるのではなく言葉を落とすこと** —— 詰め直しただけの直しを
   4 回続けて 1 行も減りませんでした。**直すたびに `wc -l` を見ること**

**`test/audio.test.ts` の揺れ**（`rules/testing.md`）は、この周は**出たとは言い切れません**
—— 4 回走らせて赤が 3 回ありましたが、**3 回とも上の行数**が入っており、1 回だけ
**もう 1 件（特定していない）**が並びました。**削り終えたあとの 2 回は緑**です。
**赤が出たらまずもう一度走らせること**（停止条件は「2 周続けて赤い」）。

## 人がやること

- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）はこの周を無事に発火させました** —— 前の引き継ぎに書いた
  「05:50 の周が中断で失敗して `enabled: false`」は解消しています
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
