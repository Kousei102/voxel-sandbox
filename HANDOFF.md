# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- **この周は B（仕様）**。`AUTODEV-SPEC.md` を**キューの 14 番**で書き直しました。
  **`src/**` と `test/**` には 1 行も差分がありません**（`git diff --stat -- src test` が空）
- `npm run typecheck` / **`npm test` 2783 件すべて成功**（この周は 1 件も増減していません）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **`src/main.ts` は 1450 行 / 上限 1500**（`npm test` の数え方。**`wc -l` は 1449 で食い違います。
  停止条件の 1450 に乗っているのは `npm test` の数**なので、そちらを見ること）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 131**（この周は 1 個も使っていません）
- **アイテムは 89 種**、`MAX_ITEM_ID` は **124**
- **`AUTODEV-SPEC.md` は `状態: 未着手`**、`AUTODEV-QUEUE.md` の未着手は **7 件**
  → **次の周は C（実装）**
- **`RULES-INBOX.md` は未取り込み 13 件**（前の 5 周ぶん。この周は 1 件も足していません）

## この周でやったこと（無人・B の周 1 種類だけ）

**キューの先頭「14. 収穫で種も戻る（1 回の採掘で 2 山落ちる器）」を仕様書 1 枚にしました。**
`AUTODEV.md` の B が要求する 7 項目は揃っていて、**119 行 / 上限 120**（`npm test` の数え方）です。

**取る前にコードで数え直しました**（B の決まり）。`Drop` は
`{ item, count, chance, otherwise? }` の**1 山ぶんだけ**で、`extra` も `rollDrops` も
`src/**` にも `test/**` にも 1 件もありません。`harvest()`（`breaking.ts:176`）の戻りは
`Burst | null` の 1 山で、`rollDrop()` を呼ぶのは `breaking.ts` のこの 1 か所だけです。

**仕様の要点は 3 つです**（詳細は `AUTODEV-SPEC.md`。次の C はこれを**全文そのまま**読むこと）:

1. **`Drop` に `extra?: DropStack` を 1 行**足して、`items.ts` に
   **`rollDrops(blockId, roll): readonly DropStack[]`**（0〜2 山）を新設する。
   **`rollDrop()` は消さずにそのまま残す** —— 既存のテスト約 25 か所の根拠なので、
   名前も引数も戻り値も変えたら差し戻しです。1 山目は `rollDrops()` の中から
   `rollDrop()` を呼んで作らせます（`chance` / `otherwise` の判断を写させない）
2. **`breaking.ts` の `harvest()` を `Burst[]` に**。掘る経路（`tryBreak`）と支えを失う経路
   （`autoBreak`）が同じ `harvest()` を通る形は変えません
3. **`main.ts` は 0 行**（`for (const out of result.drops)` がもう山の数を知らないため）。
   **ID も 0 個、`SaveData` も 1 バイトも増えません**

**乱数を 1 本しか流していないので、`extra` に確率も個数の範囲も持たせない**と決めました。
付けると 1 山目の当たり外れと必ず相関します（砂利の火打石と種の個数が連動する形）。
そのぶん**種は 1 個固定**で、**本家の「小麦 1 + 種 0〜3」より渋い**ことになります。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は `src/**` を 1 行も触っていないので、撮っていません**（C-3 は C の周のもの）。
前の周に撮った `docs/browser-shots/wheat-ripe.png`（**左が実り・右が苗**）はそのままです。
**前の周から持ち越しで、まだ見てほしいことが 2 つあります:**

1. **苗の緑（`0x6f8f3f`）が草原に紛れる**（実った黄色 `0xd8c26a` は一目で分かります）。
   苗を暗く／青寄りにするか、`boxes` を低くして背の高さで伝えるか（`TUNING.md`）
2. **`GROW_SECONDS` 180 秒（3 分）が待てる長さかどうか。** 本家は 20 分前後ですが、
   ここは**チャンクが読み込まれている間しか育ちません**（離れた畑は止まります）

**14 番が入ったら 3 つ目が出ます**: **掘った瞬間に山が 1 → 2 に増えます**（小麦と種）。
`drops.ts` の `burst()` が乱数で散らすので重なりはしないはずですが、**2 山が同時に飛ぶ
見た目**は人にしか確かめられません。

## 人がやること

- **`RULES-INBOX.md` の 13 件の取り込み**。**わざと食い違わせてある組**（「10 件」対「11 件」対
  「13 件」など）は **いちばん新しいほうを採ってください**
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**はまだ直っていません
  （`#status` は `bottom: 118px` 固定。`style.css:674`）。`REVIEW.md` に積んであります
- **種が 1 個固定でよいか**（本家は 0〜3）。範囲にするなら**乱数をもう 1 本流す**話になり、
  `BreakOrder` と `autoBreak()` の引数と `main.ts` に及びます（仕様の 7-1）
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **7 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 未着手`**
なので → **次は C（実装）の周**です。**仕様書を要約せず全文を読んで、そのとおりに実装すること。**

**先に読むこと: `main.ts` は `npm test` の数え方で 1450 行**で、`AUTODEV.md` の停止条件 3
（「1450 行を超えた」）にちょうど乗っています。**14 番は `main.ts` に 1 行も要りません**
（仕様の 5-5）。もし 1 行でも足したくなったら、それは仕様から外れた合図です。

- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **サブエージェントは使っていません。** `AUTODEV.md` の C-1 は「同じ番の中で返らないなら
  自分で実装すること」で、前の 4 周とも `run_in_background: false` を渡しても背景で起動しました。
  **点検（C-2）も親が `git diff` を全部読むこと**（飛ばすより確実です）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（前の周に「声色（音程の倍率）が
波形に出る」が 1 件だけ赤 → 走らせ直したら緑。**この周は 2 回とも緑**）。
**赤が出たら、まずもう一度走らせること**（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
