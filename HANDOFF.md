# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-06・AUTODEV 40 のあと）

- `npm test` **3100 件すべて成功**（この周は **±0 件**。**`src/**` も `test/**` も 1 行も
  触っていない B の周**）・`npm run typecheck` 緑
- **`npm run build` は走らせていません**（`src/**` に差分が無いため）。**`npm run bench` も
  同じ理由で走らせていません**
- **音の一群は 1 回も跳ねませんでした**（最後まで通した 3 回とも 3100 件すべて成功。
  羊毛 0.552 / 雪 0.716 / 砂 2.235）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1447 行**（`wc -l`。上限 1500・止まる目安 1450。**この周は ±0**）
- **アイテム 109 種・`MAX_ITEM_ID` 144・111..255 の空きは 111・1..63 の空きは 9**（**±0**）
- **立方体 39 / 非立方体 72**・**モブ 9 種類**・**レシピ 54 本**・**食べ物 9 種**

## この周でやったこと（AUTODEV 40 の B・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 済` で `AUTODEV-QUEUE.md` の未着手が 4 件だったので、判定表の
2 行目に当たって **B（仕様）の周**。キューの先頭 **18c（サトウキビが時間で上へ伸びる）**の
仕様書を書きました（119 行）。

**この周の芯は「取る前にコードで数え直す」（`AUTODEV.md` の B）がそのまま効いたことです。**
前の周の `HANDOFF.md` は「18c は `main.ts` の配線 6 行が要るので、人の判断が返るまで
着手できない」と書いていました。**数え直したら覆りました**:

- **6 行は「新しい器のファイルを作る」ときの値**です（`import` / 実体 / `deserialize` /
  `collectState` / `forgetEverything` / `update`）
- **18c は新しい器を要りません。** `"x,y,z"` → 秒数 1 つで足りるので、**既にある `Crops`
  （`crops.ts`）にそのまま乗ります**。あちらは **5 本がもう繋がっています**
  （`main.ts:1229` の `crops.update(dt, world)` と `307` / `553` / `595`）
- **足りないのは「置いたものを覚える」1 本だけ** —— **`main.ts` は +1 行（1447 → 1448）**で、
  **止まる目安 1450 の内側**に収まります。**`SaveData` の形も変わりません**

仕様の中身は `AUTODEV-SPEC.md`（7 項目・119 行）。判断は**全部 `crops.ts`**に置き、
`update()` を素の `getVoxel` で 3 つ（小麦 / サトウキビ / それ以外）に分ける形です。
**覚えるのは列のいちばん下のサトウキビ**（上を覚えると、刈った瞬間に印が消えて二度と
伸びません）。`placing.ts` の `PlaceOutcome` に `at` を足すのは、`main.ts` が置いたマスを
知らないためです（1 行で済ませるための最小の変更）。

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので、判定表の 3 行目です。

## ブラウザで見てほしいところ

**この周は絵が 1 ドットも変わっていません**（`src/**` に差分が無いので撮っていません）。
**18c が入ったあとに見てほしいのは 3 つ**（実装の周にもう一度書きます）:
`CANE_GROW_SECONDS = 180` が待てる長さか / 上を刈って伸び直すのが畑として成り立つか /
**自然に生えたサトウキビは伸びません**（誰も置いていないので印がない。上に 1 本置けば
列ごと覚えます）—— これが本家との差として気になるか。

**持ち越し（まだ誰も見ていません）**: 前の周の**サトウキビの背丈と密度の釣り合い**（0.12 のまま
段数だけ 1〜3 に増えた）/ **積む手ごたえ**（上面を狙うと 1 つ上、横面を狙うと隣。いちばん下を
壊すと上が全部落ちる）/ **⚠ 前から遊んでいる世界だけの見え方**: `localStorage` の差分は
「壊した／置いた」だけを持つので、**前の周に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります**（差分の復元は `breakUnsupported` を通りません。
新しく作った世界では起きません）/ 一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と
見分けられるか / サトウキビ（`0x9ad14f`）が小麦の種と並んだとき / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度（森 0.015 /
針葉樹林 0.01）/ 走っていて赤・茶を草むらと取り違えないか / サボテンに当たったときの
痛さ（0.5 秒ごとに 1）/ 牛を搾る・ミルクで毒が止まるか / ミルクバケツの色（`0xeaf2f8`）/
かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
鉄・金・ダイヤのブロックが材料の山と見分けられるか / **一覧の枠の名前が 2 行に折れて
はみ出す**（`style.css` の `.slot .label`。「キノコシチュー」が 7 文字でいちばん長い）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 1 件を据えました）

- **`rules/stateful-blocks.md`（「育つ苗」の節）** —— **「新しい器のファイルを作る前に、
  既にある器に乗らないか見ること」**を足しました。**配線 6 本はファイルを増やすときの値**で、
  `"x,y,z"` → 数字 1 つで足りるものは `Crops` にもう乗ります。**数え直さずに「配線が要る」で
  後ろへ回すと、`main.ts` の行数を理由に入るはずのものを落とします。**
  （`AUTODEV-QUEUE.md` の ⚠ の節にも同じことを 1 段落で書いてあります）

## 人がやること

- **設計の判断を 1 つお願いします（14 周持ち越し。ただし急ぎではありません）:
  `main.ts` を 2 つに割ってよいか。** いま **1447 行で、止まる目安 1450 まで残り 3 行**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  - **18c はこの判断を待ちません**（上のとおり +1 行 = 1448 で収まります）。
    **待っているのは 19（はしご）で、あちらはまだ数え直していません**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 24 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**
（`test/docs.test.ts`。この周に 1 度踏みました）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
