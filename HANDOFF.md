# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2575 件すべて成功**（前の周と同じ数 ——
  **この周は `src/**` と `test/**` に 1 行も触っていません**。B の周の完了条件）
- **`npm run build` は走らせていません**（`src/**` に差分が無いため）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1401 行 / 上限 1500**（この周は ±0）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 141**（この周は 0 個使用）
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（120 行ちょうど。上限も 120）
  → **次の周は C（実装）**（下の「次にやること」）
- **`RULES-INBOX.md` は未取り込み 2 件**（前の周が書いたもの。取り込むのは人が居る周）

## この周でやったこと（無人・B の周 1 種類だけ）

**`AUTODEV-QUEUE.md` の先頭「5. ハサミ」を仕様書にしました**（`AUTODEV-SPEC.md`）。
**取る前にコードで数え直しました** —— `shears` / `ハサミ` は `src/**` にも `test/**` にも
1 件もありません（ROADMAP の予約表にもありません）。

**2 件に割りました。** 前半（仕様書にしたぶん）が「刈る仕組み」、後半が
**「刈られた羊が見て分かること」で、`AUTODEV-QUEUE.md` の 11 番**に書き戻してあります。
`mobrender.ts` は形を `MobKind` ごとに 1 つだけ作って使い回すので（`createRig()` の
`this.shapes`）、見た目を変えるには 2 つ目の形と差し替えの仕掛けが要り、
**120 行に収まらないうえ確かめられない側の仕事**でした。

**仕様を決めるときに読んで分かったこと**（実装の周が同じ道を通らないために）:

- **シアーズに `tool:` を持たせてはいけません。** 剣は `ToolKind` に 1 語足すだけで
  済みましたが、**`mobs.ts` の `TOOL_ATTACK` に無い種類を入れると `attackDamage()` が
  `undefined + 0.5*tier` = **NaN** を返します。** さらに `wearForBreaking()` が
  「掘る道具」として 1 を返すので、**石を掘るたびにシアーズが減ります。**
  なので**火種・弓と同じ「使って減るもの」**（`wearForUse()` / `usedUp()`）にしました。
- **`def.drop` を直に読んでいるのは `attack()` と `hitByProjectile()` の 2 か所**です。
  「刈った羊は羊毛を落とさない」を片方にだけ書くと、**弓で撃ったときだけ羊毛が出ます**
  （`rollDrop()` を `breaking.ts` の 1 か所に集めたのとまったく同じ話）。
- **右クリックはモブを 1 度も見ていません。** 左クリックだけが `mobs.pick()` の距離と
  ブロックの距離を較べています（`controls.ts` の `decideClick()`）。刈るには
  **その規則を `mobIsNearer()` として出して、右クリックからも同じ 1 本を呼ぶ**のが要ります
  —— `main.ts` に距離の比較を書くと `test/use.test.ts` の見張りと同じ罠に落ちます。
- **セーブは 1 バイトも増えません**（刈られた状態はモブの持ち物で、モブは保存しない）。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は `src/**` を触っていないので、撮っていません**（見た目は 1 ドットも変わっていません）。
撮るのは次の C の周です（クリエイティブ一覧にシアーズが 1 枠増えます）。

## 人がやること

- **`RULES-INBOX.md` の 2 件の取り込み**（前の周が書いた剣まわり。`items-survival.md` の
  「傷が付く物は 2 種類あって」の表が剣で 3 種類になった件）。**シアーズが入ると
  「使って減るもの」が 3 つ目（火種・弓・シアーズ）になります** —— 取り込むのは C の周のあとで可
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**は
  まだ直っていません（`#status` は `bottom: 118px` 固定。`style.css:674`）。**`REVIEW.md` に積んであります**
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手は 7 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 未着手`**
なので → **次は C（実装）の周**です。

- **`AUTODEV-SPEC.md` を全文サブエージェントに渡すこと**（要約しない。要約すると
  「触らないファイル」と「このタスク固有の禁じ手」が真っ先に落ちます）。
  **背景（async）で起動しないこと。同じ番の中で返らないなら自分で実装すること**
- **ID は 115 を 1 個だけ**。ブロックは 1 個も取りません
- 実装のあと **C-2（別のエージェント、無理なら自分で `git diff` を読む）→
  C-3（撮って `Read` で見る）→ C-4（記録）** まで同じ周でやること
- 済んだら `AUTODEV-QUEUE.md` の 5 番を消し、**`AUTODEV-SPEC.md` の `状態:` を `済` に**
  （次の周が B に入れなくなります）。`ROADMAP.md` の予約表に 115 を「実装済み」と書くこと

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（緑）。
`audio.ts` / `sfx.ts` には 1 行も触っていないので、**直ったわけではありません。**
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
