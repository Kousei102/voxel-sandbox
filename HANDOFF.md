# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2382 件すべて成功**（**前の周から増えていません**
  —— この周は `src/**` と `test/**` を 1 行も触っていないので、増えないのが正しい状態です）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1380 行 / 上限 1500**（この周は触っていません）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— ID は 0 個）
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（119 行 / 上限 120）、`AUTODEV-QUEUE.md` の未着手は **10 件**
  → **次の周は C（実装）**
- **`RULES-INBOX.md` に未取り込みが 3 件**（`mobs.md` / `projectiles.md` / `items-survival.md`）。
  **取り込むのは人が居る周**

## この周でやったこと（無人・B の周 1 種類だけ）

**仕様書を 1 枚書いただけです。`src/**` と `test/**` に差分はありません**（`git diff` で確認済み）。
`AUTODEV-QUEUE.md` の先頭 = 3b を、**B の決まりどおりコードで数え直してから**
`AUTODEV-SPEC.md` にしました。

- **数え直して分かった大事なこと: 傷が付く物は全部 `stack: 1`**（`items.ts` の道具 12 本）。
  だから傷は**必ず 1 個ずつ動き**、半端な山に混ざりません —— **仕様が小さくなった理由**です
- **3b は 120 行に収まらなかったので、その場で割ってキューへ書き戻しました**（B の決まり）:
  - **3b（今回の仕様書）= インベントリ画面の中だけ** —— `craftscreen.ts` の `transfer()` /
    `swapHotbar()` / `release()` / `returnSlot()` / `quickMove()` と、`inventory.ts` の
    `addToSlots()`（**ここが `slot.damage = 0` と書いています**）、セーブの `craftWear`
  - **3c = 落とし物・チェスト・かまど**（省略可キーが 3 つ要るので、また割れるかもしれません）
  - **3d = 火打石と打ち金・弓**（減るのは掘ったときではなく**使ったとき**なので別物）
- 仕様書は **`durability.ts` に `damageOf()` と `carryWear()` の 2 本だけ**足す形にしてあります。
  **`serializeWear()` / `deserializeWear()` は `Slot[]` を取るので盤面 10 枠にも使い回せます**
  （2 本目を書かせないこと、と仕様書の 4 に書いてあります）

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は見た目に出るものを 1 つも足していません**（仕様書だけ）。
**前の周からの持ち越しが 4 件、まだ人の目を通っていません:**

- **道具の耐久値の周のぶん**（`8a780e9`）—— 木のツルハシで石を掘ると**ホットバーの枠の下に
  細い帯**が出るか（減るほど短く・赤く）。**新品と、棒や土のような減らない物には出ていないこと。**
  使い切ると **「木のツルハシ が壊れました」** が 1 行出るか
- **大きいチェストの周のぶん**（`a795564`）—— 2 個並べて開くと **54 枠が 9 列 x 6 段**、
  単体だと **27 枠**、3 個横一列ならどれも 27 枠に戻るか。パネルがはみ出していないか
- **砂・砂利が落ちる周のぶん**（`6209e91`）—— 砂の柱の下を掘ると上が詰まり、
  **穴や浮いた砂が残らないこと**。掘っていない崖や洞窟の天井が崩れていないこと
- **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**と
  **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **10 件**（3 件以上あるので A ではない）、
**`AUTODEV-SPEC.md` は `状態: 未着手`** → **次は C（実装）の周**です。

- **`AUTODEV-SPEC.md` を全文読んで、そのとおりに実装すること**（要約しない）。
  触るのは `durability.ts` / `inventory.ts` / `craftscreen.ts` / `storage.ts` / `session.ts` と
  `test/durability.test.ts` の **6 つだけ**。**`drops.ts` / `chests.ts` / `main.ts` は 3c の仕事**です
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **サブエージェントはこの環境では同じ番の中で結果が返せません。** C の周は
  親が自分で実装してよく、点検も `git diff` を自分で読むこと（`AUTODEV.md` の C-1 / C-2）
- 終わったら `npm run build` まで通すこと（`src/**` を触る周なので）

人がやること:

- 上の**ブラウザ確認 4 件**（全部持ち越し）
- `RULES-INBOX.md` の **3 件**の取り込み（**`items-survival.md` のぶんは `paths` に
  `src/durability.ts` を足す指示付き**）
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）の揺れは続いています。** この周は
**「2382 件すべて成功」を最後まで見たのが 5 回、そのうち 5 回とも緑**でしたが、
前の周では同じ 1 件 **「音量がそのまま大きさに掛かる（5 倍）」**が落ちています。

- 赤が出たら、**まず `git diff -- src/audio.ts src/sfx.ts` が空かを見てから**退行を疑うこと
- 直すのは人が居る周（音を聞いてもらう必要があります）

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
