# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-11・AUTODEV 69 のあと）

- `npm test` **3407 件すべて成功**（**±0**。`src/**` も `test/**` も 1 文字も触っていません）
- `npm run typecheck` 緑。**`npm run build` と `npm run bench` は走らせていません**（B の周なので不要）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1449 行**（テストの数え方。`wc -l` では 1448。**±0**）
- **アイテム 123 種・`MAX_ITEM_ID` 161・111..255 の空きは 94**（±0）・**1..63 の空きは 9**（±0）
- **立方体 41 / 非立方体 79**・**モブ 9 種類**・**レシピ 65 本**・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 未着手`（108 行）**・**`AUTODEV-QUEUE.md` 136 行**（未着手 **4 件**）

## この周でやったこと（AUTODEV 69 の B・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 済`」で 2 行目 = B（仕様）の周**でした
（未着手 4 件なので A ではありません）。**キューの先頭 27b-2 の仕様書を書きました。**

**取る前にコードで数え直しました**（B の周の決まり）。**27b-2 はまだ 1 行も入っていません**:
`craftscreen.ts` / `inventoryui.ts` / `index.html` / `main.ts` に **`armor` の字が 0 件**。
一方で 27a と 27b-1 のぶんは全部あります（`inventory.ts` の `armor` 4 枠・`ARMOR_SLOTS`・
`ARMOR_SIZE`・`armorPoints` / `items.ts` の `ARMORS`（革 158..161）と `armorOf()` /
`vitals.ts` の `armor` フィールドと `armorReduced()`）。

仕様書で決めた芯は 3 つです（**欠けるとサブエージェントの勝手な判断になって返る**ところ）:

- **「その枠にそのアイテムを入れてよいか」は `craftscreen.ts` の `accepts(area, index, item)`
  1 か所**。呼ぶのは 4 か所（`press()` の置く側 / `hover()` の撫でた集合 / `swapHotbar()` /
  `quickMove` の行き先）で、**`transfer()` の中では見ません** —— 掴む側にも掛かると
  着ている物が外せなくなります。**`NO_ITEM` は必ず true**（空と入れ替えて脱ぐ経路）
- **シフトクリックの行き先の順は「チェスト → かまど → 防具枠 → ホットバー/収納」**。
  器より先に防具枠を見ると、チェストに防具をしまう経路が消えます
- **点を貼るのは `main.ts` の 1 行**（`vitals.armor = inventory.armorPoints;`）で、
  **置き場所は `frame()` の `player.canSprint = ...` の直後**。
  **`updateVitals()` の中には書けません** —— 殴るのは `mobs.update()` で、
  あれは `updateVitals()` より**前**に走るので 1 フレーム古い点で殴られます

**防具枠はいつも出す**ことにしました（`#storage` と `#invhotbar` と同じ扱い）。
かまど・チェスト・クリエイティブで隠すと `refresh()` に 5 つ目の `mode !==` が増え、
**UI に判断が 1 つ戻ります**。本家と違うのはここだけで、入れてよい物の規則は
`accepts()` が持つので実害はありません。

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が **`状態: 未着手`** なので判定表の 3 行目です。

- **仕様書は `AUTODEV-SPEC.md`（108 行）を全文そのまま渡すこと**（要約しないこと）
- **読む `rules/`**: `inventory-screen.md` / `dom-ui.md` / `vitals.md` / `drops.md` /
  `stateful-blocks.md` / `testing.md`（`test/**` を触るので）
- **ID は 0 個。** 終わったとき **111..255 の空きが 94 のまま**であること（減っていたら番号を取った合図）
- **`main.ts` は +1 行**（1448 → 1449 の `wc -l`。テストの数え方 1449 → **1450**）。
  **2 行目を書きたくなったら止まる合図**です
- **未着手 4 件。27b-2 を実装して消すと 3 件** —— **3 件を切るので、その次は A（棚卸し）の周**です

## ブラウザで見てほしいところ

**この周は `src/**` を 1 文字も触っていないので、撮っていません**（`AUTODEV.md` の C-3 は
「見た目に出るものを触った周だけ」）。**下はどれも前の周からの持ち越しです。**

- **⚠ 一覧の枠の名前が 2 行に折れて絵に被る件**（`style.css` の `.slot .label`）。
  **5 文字の名前で出ます** —— 「革のズボン」で実際に被っているのを 2026-09-09 に撮りました。
  **27b-2 の仕様書では「触らない」に入れてあります** —— 見た目の判断なので人が決めてください。
  直すなら 27b-2 とは別の周にしてください（差分が 2 件に割れます）
- **革の色 4 つが「革」に見えるか**（帽子 `0xfca168` → 上着 `0xd88662` → ズボン `0xb16e51` →
  靴 `0x644122`）。上の 2 つは橙寄りに出ます。**変えるなら隔たりを測ってから**（`TUNING.md`）
- **革 24 枚（＝牛 24 頭）で一式**が見合う値段か。**革は牛しか落としません**

**持ち越し（フェンス 26a / 26b）**: **`shots/fence.png` に 5 通りが写っていて、
面の欠けも裏返りもありません**。**手ざわりとして「柵らしく見えるか」**だけ見てください。
**⚠ フェンスを 52fps 未満で跳び越せる件は残っています** —— 跳躍の余裕は **0.0117**
（到達は `9.2²/(2×30) + 9.2×dt/2` で、実測 **60fps 1.4883 / 30fps 1.5667 /
刻みの上限 0.05 で 1.6450**）。**直すなら跳躍の積分**（全ブロックの段差の登り方が
変わります）**か 1.5 の側**（本家の値から離れます）——**数値では決められないので人の判断です。**
**当たり判定はマスいっぱいのままで、腕の無い側にも「見えない壁」があります** /
**色 `0x988a5e` が「木の柵」に見えるか** / **棒 6 本で 2 個**が牧場 1 区画に見合う値段か。

**持ち越し（凍った海 25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の
境目が読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
いちばん近いガラスから 34.3 しか余裕がありません）。**気になるなら人が色を決めてください** /
**氷の板の上を歩けることが見て分かるか** / **氷ごしに海の底が見えるか**（`alpha` 0.6）。

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**。

**持ち越し（ケーキ）**: 上面 `0xffd0e4` が「ケーキ」に見えるか / 側面 `0xe8c9a0` と
下面 `0xd9b98a` の 3 色がケーキに読めるか。**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: 中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか /
素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / 森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ /
牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**据えるものがありませんでした。** 踏んだのは `test/audio.test.ts` の揺れ 1 件だけで、
**`rules/testing.md` に前から書いてあったとおり**でした —— 1 回目に
**「声色（音程の倍率）が波形に出る」で低い声が 0.162 → 4.334 に跳ね**、
**2 回目は落ち着いた値（0.162 / 0.329）で緑**。**同じ件が続けて落ちたときだけ退行**で、
**判定はゆるめないこと**（`rules/testing.md` の末尾に、跳ねるのは羊毛だけでも
足音だけでもないと既に書いてあります）。

## 人がやること

- **⚠ 一覧の枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）。
  **27b-2 の仕様書では「触らない」にしました** —— 見た目の判断なので人が決めてください
- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **設計の判断（42 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**
  （テストの数え方）で、**27b-2 で 1450 行になります** —— `AUTODEV.md` の止まる目安
  （1450 行を「超えた」）にちょうど並びます。**27b-2 の次に `main.ts` へ 1 行でも
  足す周は、先に判断を出さないと止まります**（待っているのは **24b（ケーキをかじる。最低 7 行）**）。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 52 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を押してクリエイティブにし、`E` を押して末尾までスクロールしてから撮ること**
（手順は `docs/browser-shots/README.md` の末尾）。
**自然生成しないブロックは今までどおり `npm run shot -- <名前>` が唯一の経路です。**
**27b-2 は防具枠が画面に出る周なので、`E` を押した画面をそのまま撮れば確かめられます。**

**`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
**`wc -l` を 119 以下に収めること**（`rules/testing.md`。**いま 108 行**）。
