# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-21）

- ブランチ **`loop/endgame`**。**`origin/master` より 19 コミット先**。**push していません**
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1263 件すべて成功**
- **進行（クリア導線）達成 4 / 13**（**4 件とも本物の判定**）。
  溶岩 / 黒曜石 / 火打石と打ち金 / ポータルの点火
- ブロック ID **1..63 の空きは 18 個**（64 以降は 103 まで使用済み）。アイテム ID は 88 まで
- `src/main.ts` は **1330 行 / 上限 1500**

## ループを回し始めました

**`/loop`（自己ペース）で回しています。** 1 周 = `LOOP.md` の手順を 1 回。
**`TASKS.md` に「⛔ ここで止まる」の行を入れ、`LOOP.md` の停止条件 7 に足しました** ——
無人で回すのは **1-4 → 1-5 → 1-7**（どれも判断だけのファイル）までで、
**次元（1-6）と投射物（1-8）はユーザーと同席で取ります。**
そのために **1-7 を 1-6 の前に出してあります**（番号は振り直さない決まりなので、
「上から順に」のほうが優先）。

## この周でやったこと（1 周目）

**TASKS 1-4（`portals.ts` — 枠の検出と点火の可否）**（`543b55f`）。

- ブロック 49（X 向き）/ 103（Z 向き）。`portals.ts` は `getVoxel` / `setVoxel` の
  2 つだけを受ける判断ファイル
- 枠は Minecraft と同じ **内側 2..21 x 3..21、四隅は要らない**
- 向きの違いは `mapper()` 1 か所に閉じ込めた（2 通り書き写すと片方だけ壊れる）
- 何が火種かは `items.ts` の **`isFireStarter()`**（表 1 本）
- **`main.ts` には配線していません。** 右クリックで点火する操作は 1-6 で次元と一緒に

## 踏んだ穴（同じことを繰り返さないために）

1. **立方体でないブロック（`isProp`）は半透明にできません。** `buildProps()` は
   不透明側の `Builder` しか渡されないので、`translucent: true` は**黙って無視されます**。
   ポータルは不透明の紫板にしました（`rules/meshing-render.md` に追記）
2. **ソースのガードはコメントを落としてから見ること。** `portals.ts` に
   「次元の話は 1-6 でやる」と書いた説明そのものが `dimension` に引っかかって赤くなりました

## 未確認のもの（ユーザーにブラウザで見てもらう）

**`REVIEW.md` の「1-4. ネザーポータルの面」5 項目だけです。** 見た目だけの確認で、
**まだ火は点けられません**（クリエイティブの一覧から置いて見てもらう形）。

## 次にやること

**1-5（`World` の生成器を注入できるようにする）。** 見た目に関わらないので、
1-4 の確認を待たずに取れます。

- `constructor(scene, seed, edits?)` の中の `new WorldGen(seed)` をやめ、外から渡す
- **このタスクでは次元を足さない。** 差し替えられる形にするだけ
- そのあと 1-7（`structures.ts`）まで回したら、**「⛔ ここで止まる」で止まります**
