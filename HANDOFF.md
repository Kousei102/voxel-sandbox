# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-06・AUTODEV 45 のあと）

- `npm test` **3155 件すべて成功**（この周は **+12 件**）・`npm run typecheck` 緑・
  **`npm run build` 緑**（`src/**` を触った周なので走らせました）
- **`npm run bench` は走らせていません** —— 生成にもメッシュ化にも 1 行も触っていません
- **音の一群は 1 回も跳ねませんでした**（羊毛 0.552 / 雪 0.716 / 砂 2.235・
  低い声 0.162 / 高い声 0.329 と、落ち着いた値のまま）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1449 行**（`wc -l`。テストの数え方では 1450。上限 1500・
  **止まる目安 1450 まで残り 1 行。この周は予告どおり +1**）
- **アイテム 110 種・`MAX_ITEM_ID` 145・111..255 の空きは 107・1..63 の空きは 9**
  （**ID は 1 つも取っていません。±0**）
- **立方体 39 / 非立方体 76**・**モブ 9 種類**・**レシピ 55 本**・**食べ物 9 種**

## この周でやったこと（AUTODEV 45 の C・クラウドの無人の周）

**はしごを登れるようにしました（19b）。** `AUTODEV-SPEC.md` が `状態: 未着手` だったので
判定表の 3 行目 = C（実装）の周。**サブエージェントは立てず、親が仕様書のとおり自分で
実装し、`git diff` を自分で読んで点検しました**（`AUTODEV.md` の C-1 / C-2 の但し書き）。

- **`blocks.ts`**: `climbable` の旗と `CLIMBABLE` の表・`isClimbable()` を
  **`spiky` / `isSpiky()` とまったく同じ形**で。旗は **`LADDER_OPTS` に 1 行**だけで
  **4 向き（145..148）が一度に付きました**
- **`player.ts`**: `onLadder`（`touchingSpikes` の隣。**押し戻したあとで見る**）と、
  **`if (inLiquid)` の「あと」の `else if (onLadder)`**（水がはしごに勝ちます）。
  速さは `LADDER_CLIMB_SPEED = 2.35` / `LADDER_SLIDE_SPEED = 3.0`（本家の値のまま）
- **`vitals.ts`**: `VitalsContext.onLadder` と `updateFall()` の 2 か所だけ
- **`main.ts` は予告どおり 1 行**・**`src/physics.ts` は 0 行**（`bodyTouches()` は
  サボテンのものをそのまま使いました）

**踏んだのは 3 つで、差し戻しは 0 回**（詳しくは `docs/autodev-log.md`）:
**`VitalsContext` に必須で足したら `test/mobs.test.ts` の直書きも落ちた**（`rules/vitals.md` に
先に書いてあったとおり。`typecheck` が両方名指ししました）/ **滑る速さのテストが床に
着いて「2.350 m / vy=0」を測っていた**（3 秒登ってから測る形に直しました）/
**`shots/ladders.png` の「裸の石の柱」に石が横 4〜5 画素しか残っていなかった**（下）。

## 次にやること

**次は A（棚卸し）の周** —— `AUTODEV-QUEUE.md` の未着手が **2 件（11 スケルトン /
12 刈られた羊の見た目）で 3 件を切った**ので、判定表の 1 行目に当たります。
**`AUTODEV-SPEC.md` は `状態: 済` にしてあります。**

**A の周は `src/**` と `test/**` に差分 0 行**で、**実装済みをコードから数え直す**こと
（表は `docs/autodev-counting.md`）。**基準は変えないこと** ——
「本家のバージョン順（Alpha → Beta → 正式版）」のままです。
**次の空き番号は 149 から**（`ROADMAP.md` の予約表）。
**次の A で「防具を枠だけに割れないか」を見ること**（`AUTODEV-QUEUE.md` の申し送り）。

## ブラウザで見てほしいところ

**この周で撮ったのは `shots/ladders.png` 1 枚（撮り直して 2 枚目）です。** 19b は
描画に 0 行なので**絵の中身は 19a から 1 ドットも変わりません** —— 面の欠けも裏返りも
0 件で、19a の箱は壊れていないことだけ確かめました。**変えたのは場面の作り方**で、
**裸のはずの石の柱に石が横 4〜5 画素しか残っていない**のを画素を数えて見つけ、
板を +Z から **+X（奥行きの側）の面へ移して撮り直しました**（`tools/shot.ts`。
いまは石の柱がちゃんと石に見えます）。**縮小した絵では「茶色い柱が 2 本」に見えていて
気付けませんでした** —— `rules/meshing-render.md` に据えてあります。

**この周で人に見てほしいのは 3 つ**（**登る手触りは誰もまだ触っていません**）:
**2.35 m/s の登りが待てる速さか**（4 マスの縦穴に 1.7 秒・地表から岩盤近くまで 17 秒）/
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか**（本家と同じ挙動
ですが、**しゃがみで止まる手はここにありません** —— `Shift` は走りなので、
止まる手を足すと `controls.ts` の話になります）/ **はしごの上端から地上へ出る手触り。**

**持ち越し（まだ誰も見ていません）**: **はしごの色（`0xc9a063` / `0xa8823f`）が板・原木と
見分けられるか** / **厚さ 3/16 の板が「はしご」に見えるか**（横木は無く、板 1 枚です）/
**「はしご は壁にしか付けられません」の文が、床を狙ったときに親切か** /
**`CANE_GROW_SECONDS = 180` が待てる長さか**（1 段から 3 段まで 6 分）/ **上を刈って
伸び直すのが「畑」として成り立つか** / **自然に生えたサトウキビは伸びません**（上に 1 本
置けば列ごと覚えます。理由は `TUNING.md`）/ サトウキビの背丈と密度の釣り合い（0.12 で
1〜3 段）/ **積む手ごたえ** / **⚠ 前から遊んでいる世界だけの見え方**: `localStorage` の
差分は「壊した／置いた」だけを持つので、**前に浜のサトウキビを壊した場所は、下の 1 マス
だけが空いて上の段が浮いて見えることがあります**（新しく作った世界では起きません）/
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / サトウキビ
（`0x9ad14f`）が小麦の種と並んだとき / ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け /
シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか / 食べ終わりにボウルが手に戻る
手ごたえ / 森を歩いたときのキノコの密度（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を
草むらと取り違えないか / サボテンに当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・
ミルクで毒が止まるか / ミルクバケツの色（`0xeaf2f8`）/ かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**）

- **`rules/meshing-render.md`** —— 「薄いものは隣に比べるものを置く」の続きに 2 つ:
  **比べる相手は広く写る面に残ること**（掛けるのはカメラから見て奥行きのある側の面。
  広い面に掛けると、残る石が横 4〜5 画素の帯にしかならず置いた意味が消えます）/
  **絵は見た目で判ったつもりにならず、迷ったら画素を数えること**（PNG は
  `zlib.inflateSync` と Paeth の 30 行で読めます。使い捨てでよい）

## 人がやること

- **設計の判断を 1 つお願いします（19 周持ち越し。もう待てません）:
  `main.ts` を 2 つに割ってよいか。** いま **1449 行で、止まる目安 1450 まで残り 1 行**。
  **次に 1 行でも配線が要る周は、その前にここで止まります**（`AUTODEV.md` の停止条件 2）。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 29 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。
**`main.ts` の行数も同じ数え方**なので、`wc -l` の 1449 はテストでは 1450 と出ます。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
