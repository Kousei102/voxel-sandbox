# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-27）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 2128 件すべて成功**（2083 → 2128。この周の 45 件は出口ポータルまわり）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件です**（この周で件数は動いていません）
- ブロック ID **1..63 の空きは 9 個**（この周は 1 個も使っていません）。
  向き違いは **110 まで**、**アイテム ID は 94 まで**（この周は 1 個も使っていません）
- `src/main.ts` は **1462 行 / 上限 1500（余白 38 行）**。**`main.ts` の次元の分岐は 0 件**（上限 2）

## 直前にやったこと — TASKS 2-13b-1（エンドの出口ポータル）

**ドラゴンを倒すと、島の中心の脇（+X へ 3 マス）に 3x3 のエンドポータルの面が建ちます。**
踏むとオーバーワールドへ戻れて、**その面がそのまま「倒した印」**です ——
`edits` に乗るので、**エンドに入り直しても読み込み直してもドラゴンは湧き直りません**
（前の周の「気付いているが手を付けていない」の 1 つ目が塞がりました）。

| どこ | 何が入ったか |
| --- | --- |
| `exitportal.ts`（新規） | **判断は全部ここ。** `EXIT_PORTAL_CELLS`（3x3）/ `exitPortalState` / `buildExitPortal` / `syncExitPortal` |
| `endgen.ts` | **`EXIT_PORTAL_SPOT`**（居場所。`END_SPAWN` と同じ理由でこちらに置いた）。表を 1 つ足しただけで `generateChunk` の中は 1 行も変えていない |
| `mobs.ts` | `ensureBoss()` に第 3 引数 `defeated`（**既定値なし**）。`bossDefeated(dimension)` を足した |
| `main.ts` | 2 行: `const dim = dims.nameOf(dims.current)` と `mobs.ensureBoss(dim, world, syncExitPortal(world, mobs.bossDefeated(dim)))`。**+9 行**（1453 → 1462） |
| `test/exitportal.test.ts`（新規） | 面の形・建てる／読む・穴あき・未読み込み・本物のエンドで踏めて帰れて残ること |
| `test/endgen.test.ts` | 3x3 が `LANDING_RADIUS` の内側・5 つの種で平らで空いている・降りる場所と重ならない・柱に掛からない |
| `test/mobs.test.ts` | `bossDefeated` の 3 通りと、「印が立っていれば作り直しても湧かない」 |
| `test/ui.test.ts` | `routed` に 2 件（`syncExitPortal(` と `mobs.bossDefeated(`） |

### 決めたこと

- **2-13b を 2 つに割りました**（`LOOP.md` の「2」）。残りは **2-13b-2**（体力バーと
  クリア画面）で、**DOM ＝ここでは確かめられない側**なので確かめ方がまるで違います。
  `TASKS.md` に、出す条件（「倒した瞬間」であって「印が立っている」ではない）まで書いてあります
- **印はワールド（`edits`）に持たせました。** クリスタルをブロックにしたのと同じ筋で、
  **読み込み直しても残り**、**新しいセーブのキーが 1 つも要りません**（`version` は 1 のまま）
- **倒したかどうかは 2 段。** この読み込みのあいだは `Mobs.bossDefeated()`
  （`summoned` に居るのに `list` に居ない＝倒された。ボスは遠くても消えず上限でも
  間引かれないので、居なくなる理由は他にない）、**世界をまたぐのは面のほう**です。
  **`mobs.ts` は `exitportal.ts` を import していません**（`MobContext.healers` と同じ形）
- **`ensureBoss()` の第 3 引数に既定値を付けていません。** 付けると渡し忘れが型で通り、
  「倒しても湧き直すエンド」が黙って戻ります（必須にしたので、10 か所の呼び出しが
  全部コンパイルで止まりました。**これがこの周でいちばん効いた選択です**）
- **降りる場所（`END_SPAWN` = 原点）と 3 マス離してあります。** 本家は逆で、
  出口が中心・降りる所が 100 マス先ですが、この島は半径 48 なので中心の脇に寄せました

### 確かめたこと

- **わざと壊して 8 通りとも赤くなることを確かめました**（**コミットしてから壊し、
  毎回 `git checkout HEAD -- src test` で戻しています**）:
  中心 1 マスだけで「建っている」と決める → 1 件 / `hasColumn` を見ない → 2 件 /
  床を置かない → 1 件 / 倒していなくても建てる → 2 件 / `ensureBoss` が印を無視する → 2 件 /
  `bossDefeated` が返さない → 1 件 / 降りる場所に重ねる → 3 件 /
  `main.ts` の配線を外す → 1 件
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません
- **見た目に触れた**ので `REVIEW.md` に「2-13b-1」を 7 項目、数値は `TUNING.md` に 4 行、
  決まりごとは `RULES-INBOX.md` に 3 件（**うち 1 件は新しいファイル
  `.claude/rules/exitportal.md`。`paths` の行も書いてあります**）

### 前の周（2-1 〜 2-13a）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージを書かない**）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ）
- **2-7**: 投げたエンダーアイが要塞の方を向く（`stronghold.ts` 新規）
- **2-8**: 要塞（石レンガ 53 + エンドポータルの部屋 + 枠 51。地面から 18 マス下）
- **2-9**: エンドポータルの起動（`endportal.ts` 新規。枠 12 個にアイ）
- **2-10**: エンド次元（`endgen.ts` 新規。エンドストーン 50 の島 1 つ）
- **2-11a**: エンドの黒曜石の柱 10 本（`endgen.ts` の `PILLARS`）
- **2-11b**: エンドクリスタル（**ブロック 54**。判断は `crystals.ts`）
- **2-12**: 弓と矢（**アイテム 93 / 94**。判断は `bow.ts`）
- **2-12b**: 右クリックの振り分けを `use.ts` へ（1499 → 1445 行）
- **2-13a**: エンダードラゴン本体（`boss` / `orbit` / `regen` / `hover` を表に足した。**13 / 13 到達**）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 /
  2-8 / 2-9 / 2-10 / 2-11a / 2-11b / 2-12 / 2-13a / 2-13b-1 の 19 節が溜まっています**
  （一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

0. **わざと壊す前に、必ずコミットしてから始めること**（2-11b で `git checkout -- src test` を
   使い、コミット前だったので実装が丸ごと飛びました）。この周もコミット後に 8 通り試して、
   毎回 `git checkout HEAD -- src test` で戻しています。
1. **引数に既定値を付けると、渡し忘れが型で通ります**（この周）。`ensureBoss()` の
   `defeated` を必須にしたので、**10 か所の呼び出しが全部コンパイルで止まりました** ——
   既定値を付けていたら、`main.ts` の配線を書き忘れても緑のまま通っていたはずです。
   `liveState()` の渡し忘れ（下の 18）とまったく同じ話で、あちらは**型が同じせいで
   防げなかった**ぶん、テストで数える形にしてあります。
2. **未読み込みの列を「無い」と読まないこと**（6 度目がこの周の `exitPortalState`）。
   `getVoxel` は AIR を返すので、`hasColumn` を見ないと**倒した人のエンドに
   ドラゴンがもう 1 体湧きます。** 分からないうちは湧かせないほうが安全側です。
3. **「ある」を 1 マスで決めないこと**（この周）。`setVoxel` は未読み込みの列で黙って
   失敗するので、中心 1 マスだけで「建っている」と決めると、**穴あきの面が二度と
   埋まりません**（9 マスとも見て、欠けていれば次のフレームに埋め直します）。
4. **飛ぶモブの高さは、`FLY_SCAN`(8) より上に居ると測れません**（2-13a）。
   **浮く高さも種類ごと**（`MobDef.hover`）—— ブレイズの 2.5 のままだと
   `ATTACK_HEIGHT`(1.5) に届かず、**目の前まで降りてきても一度も殴れないボス**でした。
5. **「近づいてきた」「降りてきた」は、始まりの状態を先に作ってから測ること**（2-13a）。
6. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（これまで 9 度）。
   **2-13a で仮は 0 件になりました。**
7. **「判断を別ファイルへ出した」の見張りは、呼び出しの側も見ること**（2-12 / 2-12b）。
8. **`solid` でないブロックには飛び道具が当たりません**（2-11b）。
9. **「本物の `World` で往復する」テストは、遠い所を先に読み込むこと**（2-11b）。
10. **場所の限界は「実測した最小値」ではなく「式」で決めること**（2-11a）。
11. **「面積で形を測る」判定は真円を通します**（2-10）。**形そのものを測ること。**
12. **座標の移し方は次元ごとに違います**（2-10）。
13. **「探して無ければ建てる」を別の次元に使い回さないこと**（2-10）。
14. **同じ向きが並ぶ列から中心を逆算しないこと**（2-9）。
15. **手で並べた試験場は、建てる側との食い違いを見つけられません**（2-9）。
16. **構造物の `extent` は「どこに掛かるか」の唯一の材料です**（2-8）。
17. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（2-7）。
18. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字まで。
19. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）。
20. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の矢・火球で死にます。
21. **`world.seed` をセーブや場所の計算に使わないこと**（次元では塩を混ぜたあとの値）。
    種は `worldSeed`。
22. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）。
23. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）。
24. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）。
25. **`test/audio.test.ts` が揺れます**（これまでに 4 つの判定が落ちました。どれも
    **書き出した波形がまるごと無音になる**形）。**この周は `audio.ts` も `sfx.ts` も
    1 行も触っていません**（`git diff HEAD~1 -- src/audio.ts src/sfx.ts` が空）。
    **この周は 8 回まわして 3 回落ち、しかも落ちる判定が毎回違いました**
    （「声色（音程の倍率）が波形に出る」と「cutoff が低い材質ほどこもって鳴る」）——
    **落ちる判定が run ごとに変わるなら、それは揺れです。**
    **赤が出たらまず `npm test` をもう一度**、2 回続けて**同じ判定**が落ちたときだけ
    本気で見てください（`world.test.ts` の p99 とまったく同じ扱いです）。

## 次に取るもの

1. **2-13b-2（体力バーとクリア画面）。上から順なので、ここが唯一の未着手です。**
   - **進行の件数は動きません**（13 / 13 のまま）。**動かないから飛ばしてよい、では
     ありません** —— いまは倒した合図が「島の中心の脇に面が建つ」ことだけです
   - **クリア画面を出す条件は「倒した瞬間」**（`bossDefeated()` が false → true に
     変わったところ）。**印が立っているだけで出さないこと** —— 出すと、倒したあと
     エンドへ入り直すたびにクリア画面が出ます（`TASKS.md` に書いてあります）
   - **`main.ts` の余白は 38 行**（1462 / 上限 1500）。**足りなくなったら上限を上げず、
     2-12b と同じやり方でもう 1 つ出すこと**（次に出しやすいのは**左クリックの振り分け**）
2. 2-13b-2 が終わると**上の並びに `[ ]` が残りません。** そのときは `TASKS.md` 末尾の
   「取るタスクが無くなったら」に従うこと（**実装もコミットもしない**）
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 12 件。うち 4 件は新しいファイル
   `.claude/rules/crystals.md` / `.claude/rules/bow.md` / `.claude/rules/use.md` /
   `.claude/rules/exitportal.md`。`paths` の行も書いてあります**）/
   `REVIEW.md` の一括確認（**いま 19 節**）/ `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **倒したことが画面に出ません**（体力バーもクリア画面も無い）。**2-13b-2 がこれです。**
- **出口ポータルを踏むと、オーバーワールドの「原点のあたり」に出ます**（本家は
  リスポーン地点）。ベッドへ戻す形にするなら `portaltravel.ts` が `beds.ts` を
  引き込むことになるので、独立したタスクにしてください。
- **エンドの外側の島（ゲートウェイ）はありません**（島は 1 つだけなので行き先がありません）。
- **ドラゴンはブレスを撃ちません**（近接だけ）。`projectiles.ts` の `breath` は表も
  当たり判定もあるので、`DRAGON.ranged` に 1 行足せば撃てます。**足すときは
  `test/mobs.test.ts` の「撃つのはブレイズだけ」を書き換えること。**
- **ドラゴンは何も落としません**（本家は経験値と卵。どちらもまだありません）。
- **ドラゴンとクリスタルを繋ぐ光の線（ビーム）はありません。**
- **クリスタルは爆発しません／上に乗れます／素手で 1 秒で壊せます。**
- **矢は拾い直せません**（刺さった矢は 60 秒で消えます）。
- **弓を引いても歩く速さは変わりません／耐久値もありません／火矢もエンチャントもありません。**
- **エンドの島の上にはモブが 1 体も湧きません**（空の明るさが 0.7 固定なので
  `spawnLight` が 10.5 になり、敵対の上限 7 を超えます）。**掘った穴の中は暗いので湧きます**
- **要塞は「部屋 1 つ」だけで、中に入る道がありません**（掘って降りる前提）
- **投げたエンダーアイは、近づいても落ちません／減りません**（本家は 20% で壊れる）
- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  `eyeShot()` に次元を渡して「オーバーワールド以外では null」にするのが素直です
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒。`smelting.ts` に 1 行）
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）。
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方を全部見直すことになるので、**独立したタスクとして決めてください**
- **ネザーでも寝られます**（本家はベッドが爆発する）。**寝ても時間は「進みません」**
- **火球は火を点けません**（火そのものが無いので、ブロックとして足すところから）
- **`F3` の `biome` はネザーでもエンドでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラック・ソウルサンド・要塞の部屋の中にもゾンビが湧きます**（`spawnOn` を持たないため）
