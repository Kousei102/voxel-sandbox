# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-06・AUTODEV 42 のあと）

- `npm test` **3123 件すべて成功**（この周は **±0 件**）・`npm run typecheck` 緑
- **`npm run build` と `npm run bench` は走らせていません** —— **B（仕様）の周で
  `src/**` に 1 バイトも差分が無い**ため（`git diff --stat -- src test` が空）
- **音の一群は 1 回も跳ねませんでした**（羊毛 0.552 / 雪 0.716 / 砂 2.235・
  低い声 0.162 / 高い声 0.329 と、前の周とまったく同じ落ち着いた値）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1448 行**（`wc -l`。上限 1500・**止まる目安 1450 まで残り 2 行**。この周は ±0）
- **アイテム 109 種・`MAX_ITEM_ID` 144・111..255 の空きは 111・1..63 の空きは 9**（±0）
- **立方体 39 / 非立方体 72**・**モブ 9 種類**・**レシピ 54 本**・**食べ物 9 種**（±0）

## この周でやったこと（AUTODEV 42 の B・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 済` で `AUTODEV-QUEUE.md` の未着手が 3 件だったので、
判定表の 2 行目に当たって **B（仕様）の周**。キューの先頭は **19（はしご）**でした。

**先に `main.ts` をコードで数え直しました**（キューが「まだ数え直していない・残り 2 行」と
警告していたところ）。**結果は前の見積もりより安く上がります**:

- **置く・壊す・落ちるは 0 行。** はしごは**壁掛けの松明（`WALL_TORCH_*`）と
  まったく同じ形**です —— `supportFace` の 4 向き + `variantOf`、`tryPlace()` が
  `placedVariant()` と `supportFace()` を通し、壁を壊せば
  `World.breakUnsupported()` が落とします
- **登るのも 0 行。** `player.update(dt, world)` は `World` をもう受け取っているので、
  サボテン（`bodyTouches()` + `isSpiky`）とまったく同じ形で `player.ts` に閉じます
- **要るのは 1 行だけ** —— 滑り降りたぶんで落下ダメージを受けないために、
  `updateVitals()`（`main.ts:1282`）の `vitals.update` の引数へ `onLadder:` を 1 つ

**そのうえで 19 を 2 件に割りました。** 7 項目を揃えて書いたら **129 行**になり、
**120 行の上限に収まらなかった**ためです（`AUTODEV.md` の B の「1 周で閉じない信号」）。

- **19a（この仕様書）: 置ける・壊れる・作れる。** ID 145..148 の 4 個・`main.ts` **0 行**
- **19b（キューへ書き戻し）: 登れる。** ID **0 個**・`main.ts` **+1 行（1449）**

**割り方は `AUTODEV.md` の A-2 の並びどおり**です（枠を食う 19a が先、0 個の 19b が後）。
**`src/**` と `test/**` には 1 バイトも触っていません**（B の周の完了条件）。

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。
**取るのは 19a（はしご・置ける）だけ。** 仕様書を**全文**サブエージェントへ渡すこと
（要約すると「触らないファイル」と「このタスク固有の禁じ手」が真っ先に落ちます）。

⚠ **19a で `player.ts` / `vitals.ts` / `main.ts` を先取りしないこと。** あそこは 19b のもので、
**先取りすると 19b の周が空になり、`main.ts` の 1 行が数えられないまま増えます。**

## ブラウザで見てほしいところ

**この周は `src/**` を 1 行も触っていないので、撮っていません**（`AUTODEV.md` の C-3 は
「見た目に出るものを触った周だけ」）。**前の周に撮った絵と、そこで確かめたことは
`docs/browser-shots/README.md` と `shots/` にそのまま残っています。**

**19a を実装する周は必ず撮ること** —— はしごは**新しい形（厚さ 3/16 の板）が 4 向き**なので、
`model: "boxes"` の面が壁の側で欠けていないか・裏返っていないかが絵に出ます。

**持ち越し（まだ誰も見ていません）**: **`CANE_GROW_SECONDS = 180` が待てる長さか**
（1 段から 3 段まで 6 分）/ **上を刈って伸び直すのが「畑」として成り立つか** /
**自然に生えたサトウキビは伸びません**（上に 1 本置けば列ごと覚えます。理由は `TUNING.md`）/
サトウキビの背丈と密度の釣り合い（0.12 で 1〜3 段）/ **積む手ごたえ** /
**⚠ 前から遊んでいる世界だけの見え方**: `localStorage` の差分は「壊した／置いた」だけを
持つので、**前に浜のサトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて
見えることがあります**（新しく作った世界では起きません）/ 一覧で砂糖（`0xffffff`）が
雪・羽根・卵・糸・雪玉と見分けられるか / サトウキビ（`0x9ad14f`）が小麦の種と並んだとき /
ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で
材料 3 つに見合うか / 食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度
（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を草むらと取り違えないか / サボテンに
当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・ミルクで毒が止まるか / ミルクバケツの色
（`0xeaf2f8`）/ かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
鉄・金・ダイヤのブロックが材料の山と見分けられるか / **一覧の枠の名前が 2 行に折れて
はみ出す**（`style.css` の `.slot .label`）/ クモの速さ 6.0 と朝に燃えてよいか /
投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` は 1 行も直していません。** 落とし穴を踏まなかったからです ——
この周にやったのは「数える」と「書く」だけで、`src/**` にも `test/**` にも触っていません。

**分かったことは仕様書とキューの中に置きました**（そこが次の周に読まれる場所です）:
`AUTODEV-QUEUE.md` の「⚠ `main.ts` の行数がもう効いています」に **19 の数え直しの結果**を、
`AUTODEV-SPEC.md` に **松明と同じ形であること**を書いてあります。

## 人がやること

- **設計の判断を 1 つお願いします（16 周持ち越し。そろそろ急ぎです）:
  `main.ts` を 2 つに割ってよいか。** いま **1448 行で、止まる目安 1450 まで残り 2 行**。
  **19a は 0 行ですが、19b で 1 行使って 1449 になり、残り 1 行になります。**
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 26 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。
**この周の仕様書は 103 行です。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
