# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-21）

- ブランチ **`loop/endgame`**。**`origin/master` より 29 コミット先**。**push していません**
  （公開サイトが差し替わるので、push はユーザーの確認を取ってから）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1477 件すべて成功**（1392 → 1417（2-1）→ 1477。ポータルの往復と設置で 60 件増）
- **進行（クリア導線）達成 5 / 13**（5 件とも本物）。
  溶岩 / 黒曜石 / 火打石と打ち金 / ポータルの点火 / **ネザーへ行って戻れる**
- ブロック ID **1..63 の空きは 15 個**（64 以降は 103 まで）。アイテム ID は 88 まで
- `src/main.ts` は **1494 行 / 上限 1500**（残り 6 行）。
  **次に判断を足すときは、必ず別ファイルへ出すこと**（上限は上げない）

## 直前にやったこと — TASKS 2-1 / 2-2（ネザーへ行って帰る）

**無人のループで取れる範囲はここまで**です（`TASKS.md` の「⛔ ここで止まる」が
2-2 と 2-3 のあいだ）。2-3 から先はモブの手触り・戦闘・クリア画面で、
**数値の善し悪しがユーザーのもの**になります。

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）。
  `DIMENSIONS` にネザーを登録（`salt` は 0 以外）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）。`main.ts` の `travelThrough()` は値を集めて貼るだけ。
  決まりごとは `.claude/rules/dimensions.md` の「ポータルを通る」
- `main.ts` が 1500 行に届いたので、**置く／火を点ける判断を `placing.ts` へ出しました**
  （`tryPlace` / `tryIgnite`。`.claude/rules/blocks-shapes.md`）。**上限は上げていません**
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 の 3 節が溜まっています。**
  次はユーザーのブラウザ確認から（とくに 2-2 は「出た先で立てているか」が要）

## 踏んだ穴（同じことを繰り返さないために）

1. **`world.seed` をセーブに使わないこと。** ネザーでは `World` が持つのは塩を混ぜた
   あとの値なので、そのまま保存すると**次に開いたワールドの地形が丸ごと別物**になります。
   種は `main.ts` の `worldSeed`（`startWorld()` が覚える）。**一番人に見えない事故**です
2. **`startWorld()` で `beds.clear()` しないこと。** 次元を移るたびにリスポーン地点が
   消えていました。消してよいのは「別のワールドを始める」2 か所（作り直し・データ削除）だけ
3. **`PortalGate.step()` は 1 フレームに 1 回。** 最初の `updatePortal()` は 2 回呼んでいて、
   dt を二重に数えて待ち時間が半分になっていました
4. **出る場所は「空気だけの一番低い所」を採ること。** 見当（`yHint`）から少しだけ下を
   探す形だと、**水没した所や溶岩の海の底**に出ます（出た瞬間に死にます）
5. **仮の判定は「土台があるか」に掛けないこと。3 度目です**（1-8 の周）。
   `sourceHas("src/projectiles.ts", "export")` が器を作った瞬間に達成へ化けました。
   **成果物に掛けること**
6. **わざと壊して、テストが赤くなるか確かめること。** 飛び道具の「壁の手前で止まる」は、
   位置を戻す行を消しても通りました（**箱の前面と壁の面**を比べる形に直したら捕まりました）

## 次に取るもの

**`TASKS.md` の「⛔ ここで止まる」で止まっています。** 次の周は、

1. **ユーザーのブラウザ確認**（`REVIEW.md` の 1-8 / 2-1 / 2-2）。済んだ節は丸ごと消す
2. 確認で出た直しを 1 コミットずつ
3. そのうえで **2-3（ネザー要塞）から先をユーザーと同席で**。
   2-4（ブレイズ）と 2-12（弓矢）で**当たったときのダメージの数値**を決めることになります
   （`projectiles.ts` は当たったマスを外へ渡すだけで、まだ何も起こしません）
