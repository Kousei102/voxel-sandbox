# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-06・AUTODEV 41 のあと）

- `npm test` **3123 件すべて成功**（この周は **+23 件**）・`npm run typecheck` 緑・
  **`npm run build` 緑**（`src/**` を触ったので走らせました）
- **`npm run bench` は走らせていません** —— 生成もメッシュ化も 1 行も触っていないため
  （`crops.update()` は今までどおり `world.update()` の**外**で回ります）
- **音の一群は 1 回も跳ねませんでした**（走らせた 3 回とも全部成功。羊毛 0.552 /
  雪 0.716 / 砂 2.235・低い声 0.162 / 高い声 0.329 と落ち着いた値）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1448 行**（`wc -l`。上限 1500・**止まる目安 1450 まで残り 2 行**。この周は **+1**）
- **アイテム 109 種・`MAX_ITEM_ID` 144・111..255 の空きは 111・1..63 の空きは 9**（**±0**）
- **立方体 39 / 非立方体 72**・**モブ 9 種類**・**レシピ 54 本**・**食べ物 9 種**（**±0**）

## この周でやったこと（AUTODEV 41 の C・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 未着手` だったので判定表の 3 行目に当たって **C（実装）の周**。
**18c「サトウキビが時間で上へ伸びる」**を入れました（**新しい ID 0 個**）。

- **`crops.ts` は苗だけの器ではなくなりました。** 表は 1 つのまま、`update()` が列を
  確かめたあと**素の `getVoxel` で 3 つに分けます**（小麦 / サトウキビ / それ以外）。
  **小麦の道は 1 行も変えていません**（既存 9 件が緑のまま）
- **`CANE_GROW_SECONDS = 180`** で 1 マス、**`CANE_HEIGHT_MAX` の 3 段で止まります。**
  上を刈るとまた伸びます（砂糖の畑が成り立つ）
- **覚えるのは列のいちばん下**（`notePlaced()` が下へ舐めてから覚える）。**上を覚えると
  刈った瞬間に印が消えて二度と伸びません**
- **`main.ts` は +1 行**（`crops.notePlaced(placed.at, placed.id, world);`）。**何が伸びるかも
  何秒かも `main.ts` は知りません。** `placing.ts` の `PlaceOutcome` に足した `at` は
  **書き込んだマス**で、`main.ts` に座標を計算させないためのものです

**点検（C-2）は別のエージェントが `git diff` を読み、`typecheck` と `npm test` を
自分で走らせて 5 点とも問題なしと返しました**（差し戻し 0 回）。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` で、`AUTODEV-QUEUE.md` の
未着手が **3 件**（3 件未満ではない）なので、判定表の 2 行目です。
**キューの先頭は 19（はしご）。** ⚠ **あれは `main.ts` の行数をまだ数え直していません** ——
**残り 2 行しかない**ので、**仕様を書く前にコードで数えること**（18c で「6 行」が
「1 行」に覆ったのと同じ形で、逆に増えることもあります）。

## ブラウザで見てほしいところ

**この周に撮って自分で確かめたのは 3 つ**: `shots/beach.png` で浜のサトウキビが 1〜3 段で
立ち、**面の欠け・裏返り・段の継ぎ目の隙間が 0 件**であること / `shots/terrain.png` に
穴が無いこと / **本物のブラウザ（`docs/browser-shots/`）で WebGL 2.0・例外 0 件**で、
`placeHeld()` に 1 行足した**置く経路が生きている**こと。

**人にしか決められないのは 3 つです**（どれも時間が経たないと絵に出ません）:

1. **`CANE_GROW_SECONDS = 180` が待てる長さか** —— 1 段から 3 段まで 6 分、刈ったら 3 分ずつ
2. **上を刈って伸び直すのが「畑」として成り立つか**（刈る → 3 分 → また刈る）
3. **自然に生えたサトウキビは伸びません**（誰も置いていないので印が無い）。**上に 1 本
   置けば列ごと覚えます** —— 本家との差として気になるか。理由は `TUNING.md`

**持ち越し（まだ誰も見ていません）**: サトウキビの背丈と密度の釣り合い（0.12 で 1〜3 段）/
**積む手ごたえ**（上面を狙うと 1 つ上、横面を狙うと隣。いちばん下を壊すと上が全部落ちる）/
**⚠ 前から遊んでいる世界だけの見え方**: `localStorage` の差分は「壊した／置いた」だけを
持つので、**前に浜のサトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて
見えることがあります**（新しく作った世界では起きません）/ 一覧で砂糖（`0xffffff`）が
雪・羽根・卵・糸・雪玉と見分けられるか / サトウキビ（`0x9ad14f`）が小麦の種と並んだとき /
ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で
材料 3 つに見合うか / 食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度
（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を草むらと取り違えないか / サボテンに
当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・ミルクで毒が止まるか / ミルクバケツの色
（`0xeaf2f8`）/ かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
鉄・金・ダイヤのブロックが材料の山と見分けられるか / **一覧の枠の名前が 2 行に折れて
はみ出す**（`style.css` の `.slot .label`）/ クモの速さ 6.0 と朝に燃えてよいか /
投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか / 産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 1 件を据えました）

- **`rules/stateful-blocks.md`** —— 節の名前を「育つ苗」から**「育つもの」**に変え、
  **`crops.ts` が苗だけの器ではなくなったこと**を足しました: **表を 2 つに割らないこと**
  （`SaveData` が増えて `version` が動きます）/ **何を覚えるかを決めるのは `crops.ts`**
  （`placeHeld()` は全部のブロックで呼ばれるので、`main.ts` に「サトウキビなら」を
  書かないこと）/ **積み上がるものは列の下を覚えること** / **伸びきっている間は
  `changed` を立てないこと** / **自然に生えたものは伸びない**こととその理由。

## 人がやること

- **設計の判断を 1 つお願いします（15 周持ち越し。そろそろ急ぎです）:
  `main.ts` を 2 つに割ってよいか。** いま **1448 行で、止まる目安 1450 まで残り 2 行**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  - **次の 19（はしご）が 3 行以上要るなら、そこで止まります**（`AUTODEV.md` の停止条件 2）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 25 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
