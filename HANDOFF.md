# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-03）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は無人の B（仕様）の周**でした。**`src/**` と `test/**` には 1 行も差分がありません**
  （`git diff --stat` で確かめました。触ったのは `AUTODEV-SPEC.md` と `HANDOFF.md` の 2 つだけ）
- `npm test` **2852 件すべて成功**（前の周と同じ。B の周なので増えません）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **`src/main.ts` は 1450 行 / 上限 1500**（停止条件 3 の線にちょうど乗ったままです）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 128**（**この周は 0 個使用**）
- **アイテムは 92 種**、`MAX_ITEM_ID` は **127**。**モブは 7 種類**。`SaveData` は `version` 1
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（119 行 / 上限 120）→ **次の周は C（実装）**
- **`RULES-INBOX.md` は未取り込み 6 件**（この周は 1 件も足していません）
- `REVIEW.md` の未着手は 0 件

## この周でやったこと — 仕様「羽根（鶏の 2 山目）と、矢を本家の形へ」

`AUTODEV-QUEUE.md` の先頭 **7b** を `AUTODEV-SPEC.md` の 1 枚にしました
（7 項目そろい・119 行）。**取る前にその 1 件だけコードで数え直してあります**:

- `items.ts` に **`FEATHER` の名は 1 つも無い**（`mobs.ts` の `CHICKEN_FEATHER` は**色の定数**で
  別物。実装のとき混ぜないよう仕様書に書いてあります）
- 矢はいま `shape: ["F", "S"]`（火打石 + 棒で 4 本・**2x2 で作れる**）
- **`MobDrop` は `{ item, count, chance }` だけで 2 山目がありません**

**仕様の要（前の周が「ここが要」と書き残していたところ）はこう決めました:**

- **2 山目は `MobDrop.extra`**（`items.ts` の `Drop.extra` と**同じ名前・同じ意味**にする）。
  ブロック側の先例をそのまま持ってきます
- **ただし 1 点だけブロック側と違います** —— **`MobDrop.extra` は `chance` を持てます。**
  `Drop.extra` が確率を持てないのは、壊す側に流れてくる乱数が `BreakOrder.roll` の **1 本
  だけ**だからで、**モブの側は `random()` という関数が流れてくる**ので 2 山目を別に引けます。
  この違いは仕様書に書いてあり、**取り込むときの決まりごとにもなります**（下の「人がやること」）
- **`dropFor()` は残して、`dropsFor(mob, def, random)` を上に足す**（`rollDrop()` を残して
  `rollDrops()` を足したのとまったく同じ形）。**`dropFor()` は既存テスト 6 か所の根拠**です
- **`chance >= 1 || random() < chance` の短絡はそのまま使うこと**を明記しました ——
  **`chance` が 1 の山では乱数を引きません。** 引く形に変えると、**種を固定した既存テストの
  目がずれて、羽根と関係ない所が赤くなります**（実装の周がいちばん踏みそうな穴です）
- 矢は **`["F", "S", "E"]`（火打石 / 棒 / 羽根）で 4 本**。**3 段になるので作業台が要ります**
  —— いままでの「弓を持って出たまま矢を作り足せる」は失われます。**本家の形に戻すのが
  この 7b の目的**なので取りましたが、**手触りの話なので `TUNING.md` に 1 行残させます**

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は `src/**` を 1 行も触っていないので、絵は撮っていません**（B の周）。
前の周（鶏）に撮った 13 枚目の絵がそのまま最新です（`docs/browser-shots/README.md`）。

前の周からの持ち越しで、まだ人の目で決まっていないのは 3 つです:

1. **鶏が「鶏らしいか」**（豚・羊と並べて。歩いて翼が振れているところは見ていません）。
   声 1.8 の高さも耳で
2. **パンの茶色（`0xc49a5e`）が一覧の中で目立つか**（板・原木・棒と近い系統です。`E` を押して末尾へ）
3. **苗の緑（`0x6f8f3f`）が草原に紛れる**件と、**`GROW_SECONDS` 180 秒が待てる長さか**
   （どちらも `TUNING.md`）/ **`#status`（オートセーブの帯）の位置**（狭い窓でも）

## 人がやること

- **`RULES-INBOX.md` の 6 件を取り込むこと**（`.claude/rules/` は無人の周が触ると確認待ちで
  止まるため）。**この周は 1 件も足していません** —— 中身は前の周（鶏）までのぶんです。
  **`vitals.md` と `items-survival.md` の 2 件は前の周の本文と同じ場所を指しています**。
  決まりのとおり古いほうは直していないので、**新しいほう（鶏の周）を採ってください**
- **矢が 2x2 で作れなくなってよいか**（上の「仕様の要」の最後）。**駄目なら次の C の周が
  始まる前に `AUTODEV-SPEC.md` を直してください** —— 始まってからだと差し戻しになります
- **羽根を 1 個固定にしたのはこちらの判断**です（本家は 0〜2）。`MobDrop` に個数の範囲
  （`ShearRule` の `min` / `max` のような）を持たせない、という線引きです
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 未着手`** なので → **次は C（実装）の周**です。

- **仕様書は全文を渡すこと**（要約すると「触らないファイル」と「このタスク固有の禁じ手」が
  真っ先に落ちます）。**1 タスク = 1 コミットで、実装（C-1）・点検（C-2）・撮る（C-3）・
  記録（C-4）を全部同じ周でやること**
- **サブエージェントは同じ番の中で結果が返る呼び方でだけ使うこと。**
  背景（async）に投げないこと。**返らないなら自分で実装する**ほうが安いです。
  **点検（C-2）も、別を立てられないなら自分で `git diff` を読むこと**
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）
- **ID は 128 の 1 つだけ**（`ROADMAP.md` の予約表の「128..255 予備」の先頭）。
  **111 以降はブロックとアイテムで 1 本の番号列**なので、片側だけ見て取らないこと
- **見た目に出るもの（アイテムが 1 個増えて一覧が伸びる）を触るので、C-3 の絵は撮ること**

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に 1 件だけ赤 →
走らせ直したら緑。**この周は 4 回とも緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
