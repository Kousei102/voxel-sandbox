# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、
**ユーザーが通しで遊んで見つけた不具合は `REVIEW.md`**。

## いまの状態（2026-08-28）

- ブランチ **`loop/endgame`**。**この整理のぶんはまだ push していません**（`git status` 参照）。
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- `npm run typecheck` / `npm test` は通ります。**`npm test` 2168 件すべて成功**
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- ブロック ID **1..63 の空きは 9 個**、向き違いは **110 まで**、**アイテム ID は 94 まで**
- `src/main.ts` は **1485 行 / 上限 1500（余白 15 行）**。次元の分岐は 0 件（上限 2）
- **`RULES-INBOX.md` は空**（2-13b-2 の 2 件まで取り込み済み）
- `PARITY.md` は未追跡のまま残っています（この整理では触っていません）

## 直前にやったこと — 毎周読まれる文書の整理

**実装は 1 行も変えていません。** ループを回すうちに `TASKS.md` が 762 行・
`REVIEW.md` が 627 行まで育ち、**その 9 割が「終わったことの記録」**になっていたので、
毎周読まされる量を削りました。

- **`TASKS.md` 762 → 103 行。** 完了 29 件の本文を **`docs/tasks-done.md`** へ退避し、
  1 行サマリだけ残しました。**見取り図（`<!-- 着手したときの見取り図 -->`）も一緒に移して
  あります** —— 次に似たものを足す周が読む手本なので消していません
- **`REVIEW.md` 627 → 60 行。やり方ごと替えました。** 前は 1 周ごとに
  「確認してほしい項目」を書き溜めていましたが、22 節が一度も消えないまま溜まり、
  **ユーザーの手を一往復ぶん取る質問**が並ぶだけになっていました。
  いまは**ユーザーが通しで遊び、見つけた不具合を `REVIEW.md` に足す**形です。
  **ループはそこから 1 件取って直します**（`LOOP.md` の手順 1）。
  古い 22 節は **`docs/review-archive.md`** へ退避（**ループは読まないこと**）
- **`test/docs.test.ts` を足しました**（新しい 8 件）。`CLAUDE.md` / `LOOP.md` 200 行・
  `TASKS.md` 150 行・`REVIEW.md` 200 行の上限と、退避先の存在、`docs/…` への参照が
  実在するかを見ます。**わざと壊して 2 通りとも赤くなることを確かめました**
  （`TASKS.md` に 60 行足す → 1 件 / 参照の綴りを崩す → 1 件）
- 併せて直した参照: `CLAUDE.md`（上限の節を 4 行）/ `LOOP.md`（手順 1・7・8 と「4.」）/
  `ROADMAP.md` / `RULES-INBOX.md` / 3 つのスキル（`REVIEW.md` に書けと言っていた箇所を
  `HANDOFF.md` に 2〜3 行へ）

**`TUNING.md`（293 行）は触っていません。** あれはユーザーが遊びながら埋める表で、
ループが毎周読むものではないからです（`LOOP.md` の 3.5 は「1 行足す」だけ）。

## 次にやること

**`TASKS.md` の「未着手」も `REVIEW.md` の `[ ]` も空です。**
無人の周がここへ来たら、`TASKS.md` 末尾の「取るタスクが無くなったら」に従ってください
（要点は**何も実装しない・コミットもしない・push もしない**）。
**思い付いた直しに手を出さないこと。**

人がやること:

- **通しのテストプレイ** —— `npm run dev` で端から端まで遊び、
  おかしいところを `REVIEW.md` に足す（書き方はあのファイルの中）。
  道順と種は `REVIEW.md` の「遊ぶとき」
- **`TUNING.md` の一括調整** —— 手触りの数値を実際に遊んで決める
- **`master` へのマージ**

**次に `main.ts` へ何かを足す周は、まず減量から取ること**（余白 15 行）。
次に出しやすいのは**左クリックの振り分け**（`mousedown` の「モブとブロックの
どちらが手前か」とスポイト。`use.ts` に `decideAttack()` を足す形）。
見取り図は `docs/tasks-done.md` の 2-13b-2 の節。**上限は上げないこと。**

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
