# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2446 件すべて成功**（**前の周から増減なし** ——
  この周は仕様書 1 枚だけで、`src/**` と `test/**` に 1 行も触っていません）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1382 行 / 上限 1500**（**触っていません**）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— ID は 0 個）
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（120 行 / 上限 120）、`AUTODEV-QUEUE.md` の未着手は **9 件**
  → **次の周は C（実装）**
- **`RULES-INBOX.md` に未取り込みが 6 件**（`mobs.md` / `projectiles.md` /
  `items-survival.md` x 2 / `inventory-screen.md` / `drops.md`）。**取り込むのは人が居る周**

## この周でやったこと（無人・B の周 1 種類だけ）

**`AUTODEV-QUEUE.md` の先頭 3c-2（チェストとかまどで傷が残る）を `AUTODEV-SPEC.md` にしました。**
**`src/**` と `test/**` は 1 行も触っていません**（B の完了条件）。コミットは 1 つ。

**取る前にコードで数え直しました**（B の決まり）。3c-1 が入ったあとに残っている穴は 4 つです:

- `serializeChest()` の 54 要素と `serializeFurnace()` の 9 要素が傷を持たない（**セーブで消える**）
- `Chests.remove()` / `Furnaces.remove()` が `{ item, count }` しか返さない（**壊すと新品で落ちる**）
- `addToChest()`（チェストへのシフトクリック）が傷を移さない
- `craftscreen.ts` の `moveInto()`（**かまどへのシフトクリック**）が傷を移さない ——
  **キューに書いていなかった 4 つ目**で、数え直して見つかりました

**つまんで置いたぶんは既に傷ごと動きます**（`craftscreen.ts` が `Slot` の参照を扱うため）。
仕様書には **`breaking.ts` の `Burst` に `damage?` を足す**（前の周の申し送り）とも書きました ——
壊した器の中身はここを通るので、決めておかないと実装の周が勝手に決めます。

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶんは 0 件です**（仕様書だけなので、画面には何も出ません）。
**前の周までの持ち越しが 7 件、まだ人の目を通っていません:**

- **落とし物が傷を運ぶ周のぶん**（`6e8ec84`）—— 傷んだ道具を `Q` で捨てて拾い直す・
  持ったまま死んで拾い直す。どちらも**枠の下の細い帯**が付いたままか
  （拾い直したあとにブラウザを閉じて開き直しても付いたままか）
- **掴んで動かしても傷が残る周のぶん**（`61657d5`）—— 収納へ動かしても帯が付いて回るか。
  **クリエイティブの一覧（`E`）から出した道具には帯が出ていないこと**
- **道具の耐久値の周のぶん**（`8a780e9`）—— 木のツルハシで石を掘ると帯が出る（減るほど
  短く・赤く）。使い切ると **「木のツルハシ が壊れました」** が 1 行出るか
- **大きいチェストの周のぶん**（`a795564`）—— 2 個並べて開くと **54 枠が 9 列 x 6 段**、
  単体だと **27 枠**、3 個横一列ならどれも 27 枠に戻るか
- **砂・砂利が落ちる周のぶん**（`6209e91`）—— 砂の柱の下を掘ると上が詰まり、
  **穴や浮いた砂が残らないこと**
- **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**と
  **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
未着手のキューは **9 件**（3 件以上なので A ではない）、**`AUTODEV-SPEC.md` は `状態: 未着手`**
→ **次は C（実装）の周**です。

- **`AUTODEV-SPEC.md` を全文渡すこと**（要約すると「触らないファイル」と「禁じ手」が落ちます）。
  **ID は 0 個**・**`SaveData.version` は 1 のまま**・**省略可キーは `chestWear` と
  `furnaceWear` の 2 つだけ**
- **サブエージェントは背景（async）で起動しないこと。** 同じ番の中で結果が返らないなら
  自分で実装すること（`AUTODEV.md` の C-1）。点検（C-2）も、別を立てられないなら
  親が `git diff` を読んで自分でやること
- 終わったら **`AUTODEV-QUEUE.md` の 3c-2 の行を消し、仕様書を `状態: 済` に**して、
  `TUNING.md` の 317 行目と `docs/autodev-log.md` を更新すること

人がやること:

- 上の**ブラウザ確認 6 件**（この周のぶんは 0 件）
- `RULES-INBOX.md` の **6 件**の取り込み（**`drops.md` と `items-survival.md` のぶんは
  `paths` に `src/durability.ts` を足す指示付き**）
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）の揺れは、この周は 6 回中 2 回出ました**（前の周は
手を付ける前の 1 回だけ）。**落ちた判定が 2 つに増えています** —— 1 回目が
`声色（音程の倍率）が波形に出る`、3 回目が `水中では高い成分が削られる（0.461 < 0.000）`。
**どちらも「乾いた側の明るさが 0 と出た」形**で、残りの 4 回は緑です。

- **退行ではありません。** この周は `src/**` に 1 行も触っていません（`git status` が
  `AUTODEV-SPEC.md` 1 つだけ）。赤が出たら、まずここを見ること
- **1 回の赤で止まらないこと**（停止条件は「2 周続けて赤い」）。もう一度走らせて緑ならこの揺れです
- 直すのは人が居る周（音を聞いてもらう必要があります）

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
