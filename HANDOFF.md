# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-17・AUTODEV 93（C の周）のあと）

**この周は C（実装）の周**で、**`AUTODEV-SPEC.md` の 34a「ツタを壁に掛ける」を
そのとおり実装しました**（判定表の 3 行目 —— 仕様書が `状態: 未着手` だったため）。
**差し戻し 0 回**で、**サブエージェントは立てず親がそのまま実装し、`git diff` を
自分で点検しました**（`AUTODEV.md` の C-1 / C-2 の「返らないなら自分でやる」）。

- **ツタ 183..186**（大元 183 = アイテムも 183 / 184..186 は `variantOf: VINE`）。
  **はしご 145..148 とまったく同じ壁掛け 4 向き**で、違うのは
  **厚さ 1/16（はしごは 3/16）・硬さ 0.2・草の音・旗が 1 つ多い**ところだけ
- **旗は 2 つ**: **`climbable`**（登れる。速さは `player.ts` の `LADDER_CLIMB_SPEED`）と
  **`bladed`**（**剣かシアーズでだけ落ちる**）。**`tool: "axe"` は「掘る速さ」の表**なので、
  **斧は速くなるだけで 1 個も落ちません**（素手 1.00 / 斧 0.50 / 剣 0.30 秒）
- **触ったのは仕様書どおりの 5 ファイル**（`blocks.ts` / `items.ts` の `MAX_ITEM_ID` /
  `test/blocks.test.ts` / `test/items.test.ts` と `test/mining.test.ts` / `tools/shot.ts`）。
  **`main.ts` も `worldgen.ts` も `crafting.ts` も `player.ts` も `mining.ts` も ±0 行**
- **`LADDER_OPTS` は撒いていません**（`VINE_OPTS` を別に作った）。**`VINE_BY_SUPPORT` も
  `LADDER_BY_SUPPORT` とは別の表**で、`ladderVariant()` / `torchVariant()` は 1 文字も
  触っていません。**`LADDER_OPTS` の「ツタも足場もまだ無い」のコメントは直しました**
- **下へ垂れる（ツタの下にツタ）と自然生成は 34b**（キューに残っています）。
  **`supportFace` はブロック 1 つに 1 向きしか持てない**ので、「壁か、真上のツタか」の
  **2 つ目の候補**が `canPlaceAt()` / `breakUnsupported()` / `supportsBlock()` に要ります

## 数字（2026-09-17・この周で測ったもの）

- `npm test` **3710 件すべて成功**（前の周の 3691 から **+19**）。`npm run typecheck` 緑・
  **`npm run build` 緑**（`src/**` を触ったため）。**音の跳ねはこの周は 0 回**（1 回目から緑）。
  **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていないため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1（形も ±0）**
- **`main.ts` 1450 行 / 上限 1500**（**この周は 1 文字も開いていません**）
- **アイテム 144 → 145 種**・**`MAX_ITEM_ID` 182 → 186**・
  **立方体 42（±0）/ 非立方体 85 → 89**
- **1..63 の空きは 7**（±0）・**111..255 の空きは 73 → 69**（**次に取るのは 187**）
- `AUTODEV-QUEUE.md` **114 行 / 上限 150・未着手 4 件**（34a を消した）・
  **`AUTODEV-SPEC.md` は `状態: 済`**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` で、未着手が 4 件あるので
判定表の 2 行目です。**キューの先頭は 34b「ツタが下へ垂れる ＋ 自然生成」。**

- **取る前に 34b だけコードで数え直すこと**（キューは数周前の A が作ったものです）。
  **34a で入ったのは壁掛け 4 向きまで** —— `vineVariant()` は床（`FACE_YN`）も
  天井（`FACE_YP`）も `AIR` を返し、`worldgen.ts` にも `biomes.ts` にも 1 行もありません
- **34b は「置き方そのもの」を変える周です。** `supportFace` の 2 つ目の候補が
  `canPlaceAt()` / `breakUnsupported()` / `supportsBlock()` に掛かるので、
  **`test/arena.ts` の写しも同じ式にすること**（`rules/blocks-shapes.md`）
- **`stacksOnSelf` は代わりになりません** —— あれは **`supportFace` の向きの自分**を
  見るので、`VINE_XN` に付けると**真下ではなく -X 側**のツタに付きます
- **ID は 0 個の見込み**（34a の 4 つを使い回す）。**新しく取るなら 187 から**
- **仕様書は `wc -l` で 119 行以下に収めること**（テストは 1 多く数えます。
  `rules/testing.md` に 4 回踏んだ記録があります）

## ブラウザで見てほしいところ

**この周は 2 枚撮って、両方 `Read` で開いて見ました**（下の「分かったこと」）。
人に見てほしいのはここです（上が新しい分）:

- **⚠ ツタ（34a）の手ざわり 3 つ**: **登る速さがはしごと同じでよいか**
  （`LADDER_CLIMB_SPEED` を共有しています）/ **硬さ 0.2 が「すぐちぎれる」と
  感じられるか**（剣で 0.30 秒）/ **壁に貼った緑が「ツタ」に見えるか**
  （`0x306d18`・本家の値。**葉 39.4 / トウヒの葉 38.2 離れています**）。
  **本家のように下へ垂れないのは承知**（34b）
- **⚠ 金・ダイヤの防具の色（33b）**: **金 4 段が「金」に・ダイヤ 4 段が「ダイヤ」に
  見えるか**。**金の上着 `0xefd714` とズボン `0xf8b317` は絵の上でやや近く見えます**
- **⚠ ダイヤ一式 80% 減が強すぎないか。** **防具は壊れない**ので、
  **「上限の固さが永久に持つ」状態**です（本家は傷んで直す手間があります）
- **⚠ 金を作る理由があるか。** **金一式 11 点は鉄 15 点より弱いのに 24 枚要ります**
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **5 文字から折れて、一覧全体で 56 枠**（**ツタは 2 文字なので増えていません**）
- **⚠ 鉄の防具の色（33a）**: **「鋼の青」が鉄に見えるか** / 鉄一式 60% 減の手ざわり
- **レンガ（32b）**: `0xbc5d39` が「焼いたレンガ」に見えるか（ブロックとは 26.0）
- **粘土（32a）**: 海に潜って見つけられるか（海底の列の 7.2%）/ 粘土玉がやや青紫寄り
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか / **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らないのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白** / **氷（25a）**: 滑りの手ざわり
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 2 件**）

**撮った絵で確かめたこと**（C-3。**2 枚とも `Read` で開いて見ました**）:

- **`shots/vine.png`**（`npm run shot -- vine ladders`）: **4 向きとも壁に貼り付いて
  いて、裏返っているものは 1 つもありません**（見える 2 面が両方とも緑）。
  隣に**はしごを掛けた柱**を並べてあり、**緑とオークの茶は絵の上で取り違えません**
- **`docs/browser-shots/creative-vine.png`**（本物のブラウザ・新しい 1 枚）:
  **一覧 144 → 145 枠**・**末尾が「ツタ」**・`.swatch` が **`rgb(48,109,24)`**（指定どおり）・
  `title` が **`ツタ x64`**・**名前の重複 0 件**・**折れた枠 56 のまま ±0**・
  **console のエラー 0 件**

**据えた決まりごと 2 件**（どちらも `Edit` で `rules/` を直しました）:

- **`rules/testing.md`**: **旗（`climbable` / `bladed` / `sticky` …）を 2 つ目のブロックに
  付けたら、その旗を数えている件は「旗の数だけ」あります。** 仕様書が名指ししたのは
  `isBladed` の 1 件だけでしたが、**`isClimbable` の `length === 4` も落ちました** ——
  **付ける旗ごとに `grep -n '<旗の名前>' test/` を引いてから書くこと**
- **`rules/meshing-render.md`**: **壁に貼り付くブロックを 4 面とも塞いで撮らないこと。**
  はしごの場面を写して 4 面ともツタにしたら**ただの緑の柱**にしか見えず、1 枚無駄に
  しました。**柱を 2 段高くして上に素の石を残すこと**（中身が石だという手がかりは
  それだけ）。**厚さ 1/16 と 3/16 は絵では 1〜2 画素**なので、そこはテストの箱の値で見ます

## 人がやること

- **⚠ 設計の判断（64 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**）。
  **次の周（B の仕様）は `src/**` を触らない**ので、判断が下りなくてもあと 1 周は
  止まりませんが、**34b の C の周は `world.ts` を触ります**（`main.ts` は 0 行の見込み）
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
  （足すなら `vitals.ts` → `inventory.ts` の配線で、**それだけで 1 周**）
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は跳ねませんでした**。
  **直すなら測り方**（窓を長く取る・複数回鳴らして中央値）で、**判定はゆるめないこと**。
  **キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 93 周目）
- **`loop/endgame` に `rules/` の形が届いていません**（前の周に確かめました ——
  あちらのルート直下に `rules/` が無く、`master` から 50 コミット以上遅れています）。
  再開する前に `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと**
（**この周もその形で撮れました**）。**⚠ 枠を数えるときは `#creative .slot` を使うこと**
（素の `.slot` はホットバーとインベントリの 36 枠まで拾います）。
**⚠ `/opt/pw-browsers` には `chromium_headless_shell-1194` も居ます** ——
`startsWith("chromium-")` で弾くこと（`tools/browsershot.mjs` の `findChrome()`）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質・**ツタ**がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**34b で自然に生えるようになったら、
場面 `vine` に「森の木から垂れているところ」を足すこと**（いまの場面は**石の柱に
手で掛けただけ**です）。**時間が経たないと見えないもの**（育つ・燃える・溶ける）は、
**その場で `update()` を回してから撮ること**。
**⚠ 防具を着た姿は撮れません**（一人称視点なので自分の体が写りません）。
