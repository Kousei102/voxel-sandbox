# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-07・AUTODEV 55 のあと）

- `npm test` **3250 件すべて成功**（**±0**。B の周なので 1 件も増えません）・
  `npm run typecheck` は走らせていません（**`src/` にも `test/` にも差分が 0** だからです）
- **`npm run build` も `npm run bench` も走らせていません**（同じ理由）
- **音の一群は跳ねませんでした**（この周は 2 回走らせて 2 回とも緑）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行**（`wc -l`。テストの数え方では 1450。上限 1500・**止まる目安 1450**）
- **アイテム 115 種・`MAX_ITEM_ID` 153・111..255 の空きは 102・1..63 の空きは 9**
  （**この周は ID を 1 つも取っていません**。予約表の次の空きは 154）
- **立方体 40 / 非立方体 76**・**モブ 9 種類**・**レシピ 59 本**・**精錬 7 本**・
  **燃料 8 種**・**食べ物 11 種**・**バイオーム 10**・**構造物 2**・**次元 3**・
  **`DamageCause` 9 種**（どれも ±0）
- **`AUTODEV-SPEC.md` 120 行**（`状態: 未着手`。**上限 120 ちょうどです** ——
  1 行でも足すと `test/docs.test.ts` が赤くなります）・**`AUTODEV-QUEUE.md` 108 行**

## この周でやったこと（AUTODEV 55 の B・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 済`」で 2 行目 = B（仕様）の周**でした
（未着手が 7 件あるので A ではありません）。**キューの先頭 1 件 = 23（クモの巣）**を
`AUTODEV-SPEC.md` に書き直しただけで、**`src/**` にも `test/**` にも 1 行も触っていません。**

**書く前にコードで数え直しました**（B の決まり）: `COBWEB` も `sticky` も `bladed` も
1 つも無く、**まだ入っていません**。あわせて実装の道筋も先にコードで裏を取ってあり、
仕様書の数値はどれも「読んで確かめたもの」です:

- **`canHarvest()` が落ちるものを決めている**（`breaking.ts` が `tryBreak()` で 1 回呼ぶ）
  ので、**「刃物で壊したときだけ糸」は `main.ts` に 0 行**で足せます
- **`bodyTouches()` はマス単位**（`physics.ts`）なので、`cross` の細い箱でも
  はしご・サボテンとまったく同じ形で「中に居る」が取れます
- **`BlockDef.tool` に `"sword"` は書けません**（`blocks.ts` の `ToolKind` のコメントと
  `test/blocks.test.ts` の「`sword` を要求するブロックが 0 個」）。だから仕様書は
  **`bladed` という別の旗**にしてあります
- **色 `0xc8c8dc` は既存 115 種のどれとも 27.5 離れています**（実際に総当たりで
  測りました。白〜灰は雪・ミルク・紙・羊毛・糸で埋まっていて、**20 以上取れる
  無彩色はここしか残っていません**）

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が **`状態: 未着手`** なので判定表の 3 行目です。
**仕様書を全文そのまま渡すこと**（要約しないこと）。

**この周でいちばん大事なこと: 27 周続いた「`main.ts` の行数で止まる」を解いてあります。**
23 は **`main.ts` に 1 行も足しません** —— 直すのは `updateVitals()` に渡している
1 行の**名前だけ**（`onLadder: player.onLadder,` → `clinging: player.clinging,`）で、
**判断は `player.ts` の `clinging` ゲッターの側**に置きます。
`test/ui.test.ts` の 40 件の見張りは `onLadder` を 1 つも見ていないので当たりません
（`grep onLadder test/ui.test.ts` が 0 件。実測）。**`main.ts` を割る話は残ったまま**ですが、
**残り 6 件がすべて止まるという前の周の見立ては行き過ぎでした** ——
「配線が要る」と「行が増える」は別です。

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません。`src/**` に差分が 0 で、`dist/` は 1 バイトも変わりません。**
**次の C の周では必ず撮ること**（クモの巣は `model: "cross"` で見た目に出ます）。

**人に見てほしいのは 2 つ（前の周から持ち越し）**: **金のリンゴの色 `0xf0a800` が
一覧で金インゴット・ブレイズロッド・ブレイズパウダーと見分けられるか** /
**金インゴット 8 個 + リンゴ 1 個に「体力 4 回復」が見合うか**（`TUNING.md`）。

**27b（革の防具）を実装したら見てほしくなる 1 つ**: **防具点 7 で 8 割ぶんしか減らない
（10 → 7.2）のが「着た手ごたえ」として分かるか**（`TUNING.md`）。

**持ち越し（まだ誰も見ていません）**: **本棚が「本の詰まった棚」に見えるか**
（側面 `0x9c5064` がかなり赤紫）/ **紙 `0xd4e0ec` が雪玉と 25.5 で見分けられるか** /
**本棚 1 個に材料 9 個が見合うか** / **リンゴの色 `0xe0342c` が赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か** / **地面に落ちた
リンゴが赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか** /
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか** /
**はしごの上端から地上へ出る手触り** / **はしごの色（`0xc9a063` / `0xa8823f`）が
板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に見えるか** /
**`CANE_GROW_SECONDS = 180` が待てる長さか** / **自然に生えたサトウキビは伸びません**
（理由は `TUNING.md`）/ **⚠ 前から遊んでいる世界だけの見え方**: 前に浜の
サトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて見えることがあります /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度 / サボテンに
当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` に据えるものはありませんでした。** この周は `src/**` を 1 行も触っていないので
落とし穴を踏む場所が無く、**分かったことは全部この 1 件の仕様書の中にあります**
（上の「この周でやったこと」の 4 つ）。**次の C の周で踏んだものは `rules/` へ据えること。**

## 人がやること

- **設計の判断（28 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**で、
  上限 1500 まで 51 行です。**急ぎではなくなりました** —— 23 は 0 行で通る見込みで、
  `test/ui.test.ts` の 40 件の見張りが動かせない事情も変わっていません
  （動かすなら見張りの一覧を新しいファイルへ付け替える必要があり、**判定の意味が
  変わるので無人の周では触れません**）。**残る 6 件のうち何行要るかは、その都度
  B の周が数えます** —— 前の周の「6 件とも止まる」は行き過ぎでした
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 38 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
