# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-25）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。無人で回すうえで
  実際に踏んだものが 3 つあります:
  **(1)** **`.claude/rules/**` を編集すると「sensitive file」の確認で止まります。**
  → **`RULES-INBOX.md` に書いて、人が居る周が取り込む**形にしました（`LOOP.md` の「4.5」）。
  **`.claude/settings.json` に許可を書いても消えません**（2026-08-25 に 2 周とも実測）。
  **(2)** クラウドから `origin` へ push できず 403 → ユーザーが GitHub 側で権限を付け直し済み。
  **(3)** **5 時間の利用制限。** 当たった周は実装を終えた直後に止まり、次の定期実行も
  同じセッションに投げられて即終了しました（**新しい周にならず、1 回ぶんの枠が消えます**）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1714 件すべて成功**（1675 → 1714。この周の 39 件は火球の当たり判定と撃つ AI）
- **進行（クリア導線）達成 7 / 13**（7 件とも本物）。**この周は増えていません**
  —— 2-4b はクリア導線の項目ではなく、ブレイズを「殴るだけ」から「撃ってくる」に変える周でした
- ブロック ID **1..63 の空きは 14 個**（この周はブロックを足していません）。
  アイテム ID も **89 まで**のまま（89 = ブレイズロッド）
- `src/main.ts` は **1365 行 / 上限 1500**（余白 135 行。この周で 14 行増えました）

## 直前にやったこと — TASKS 2-4b（ブレイズの火球）

**火球を「飛ばす」ではなく「当てる仕組みを作る」周**でした（1-8 で意図的に置いていなかった
「誰に当たったか」を足すところから）。**`onHitMob` / `onHitPlayer` の 2 本ではなく
`onHitTarget` 1 本**にしてあります。

| どこ | 何が入ったか |
| --- | --- |
| `projectiles.ts` | `ProjectileTarget`（印・足元・大きさ）/ `Shot`（1 発ぶんの注文）/ `Projectile.owner` と `.damage` / `fire()` / `update()` の第 3 引数 / `onHitTarget` |
| `mobs.ts` | `MobDef.damage`（種類ごとの近接）/ `MobDef.ranged` / `Mob.shootTimer` / `MobContext.shoot` / `fire()`（撃つ AI）/ `hitByProjectile()` / `projectileTargets()` / `clearShot()` |
| `main.ts` | 3 か所だけ（`onHitTarget` を繋ぐ・`mobContext()` に `shoot`・`update` に的を渡す） |

- **飛び道具の側は「重なったか」という形の話しか持ちません。** 何がどれだけ減るかは
  **撃った側が弾に載せる**（`Shot.damage`）ので、**表（`PROJECTILE_KINDS`）にダメージは
  ありません。** 2-12 の弓矢は、撃つ側を足すだけで当たる側が要りません
- **`mobs.ts` は `projectiles.ts` を import しません**（型だけ）。撃つのは
  `MobContext.shoot`（注文を渡すだけ）で、`onDrop` が `drops.ts` を知らないのと同じ形です
- **近接の重みが種類ごとになりました**（ゾンビ 2 / ブレイズ 6 / 受動 0）。
  `MOB_DAMAGE` は **`ZOMBIE.damage` の別名**に変えてあるので、
  **新しいモブをそこへ繋がないこと**（繋いだ瞬間、また全員が同じ重さに戻ります）
- ブレイズは **3〜16m で 3 秒に 1 発**、**壁越しには撃ちません**（`clearShot()`）。
  **クリエイティブは狙われません**（`strike()` と同じ線を `fire()` にも引いてあります）
- **わざと壊して 4 通りとも赤くなることを確かめました**:
  撃ち手の印を外す → 3 件（自分の火球で消える）/ 見通しの判定を外す → 1 件 /
  近接を 1 つの定数に戻す → 2 件 / 0.2m 刻みを外す → 2 件
- 決まりごとは **`RULES-INBOX.md` に 2 件**（`rules/projectiles.md` の差し替えと
  `rules/mobs.md` の新しい節）。数値は `TUNING.md` の「モブ（ブレイズ）」に 5 行、
  確認項目は `REVIEW.md` の「2-4b」に 7 個
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません
  （モブ 40 体のフレーム時間は中央 0.055ms・上限 0.5ms で退行なし）

### 前の周（2-1 / 2-2 / 2-3 / 2-3b / 2-3c / 2-4a）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b の 7 節が溜まっています**
  （一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **撃つものには「撃ち手の印」が要ります。** 無いと口元から出た火球が自分に当たり、
   **ブレイズが撃つたびに自分の火球で焼け死にます**（テストでは「1 発も飛んでいない」
   という形で出ました）。印は `PLAYER_OWNER`(0) とモブの `id`（1 から）で食い違わせています
2. **相手を先に、ブロックをあとに見ること。** 逆だと、壁を背にした相手に当てたときに
   同じ刻みで壁が勝ち、当たったのに何も起きません
3. **相手も 0.2m ごとに刻んで見ること。** 壁と同じで、刻まないと 40m/s の矢が
   立っている相手をまたぎます（1/20 秒のフレームで実測）
4. **表の値を使っているかは、表を読むテストでは分かりません。** 「ブレイズの近接は 6」を
   表だけで確かめると、殴る側が定数のままでも通ります（**実際に 1 発殴らせて**確かめること）
5. **ブレイズの近接はめったに届きません。** 床から 2.5 浮くので、平地に立つプレイヤーとは
   `ATTACK_HEIGHT`(1.5) 以上離れます。**火球のほうが本体**という取り決めにしてあります
   （テストでは、プレイヤーと同じ高さに置いて 1 発殴らせています）
6. **飛ぶモブには `ACCEL_GROUND` を使うこと**（空中の加速 4 では 1 フレームに 0.07 しか戻らない）
7. **`world.seed` をセーブに使わないこと**（ネザーでは塩を混ぜたあとの値）。種は `worldSeed`
8. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）
9. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）
10. **仮の判定は「土台があるか」に掛けないこと。** 成果物に掛けること（3 度踏みました）
11. **わざと壊して、テストが赤くなるか確かめること。** この周は 4 通りとも確認済みです

## 次に取るもの

1. **2-4c（死んだらオーバーワールドへ戻す）**。**ユーザーがブラウザで見つけた不具合**で、
   2-5 の前に挿してあります。ネザーで死ぬと天井の岩盤の上に湧きます。
   **原因は `TASKS.md` に追ってあるので、推測で直さないこと**
2. **2-5（エンダーマン）**。テレポートとエンダーパール
   （アイテム ID は **90**。`ROADMAP.md` の予約表を**取る順に上から詰める**決まりで、
   89 = ブレイズロッドまで使用済み）
3. そのあとは並びどおり 2-6（エンダーアイ）→ 2-13（ドラゴン）。
   **2-12 の弓矢は、撃つ側（弓とアイテム）を足すだけで当たる側が要りません**
   （この周で `Shot` に載せた `owner` と `damage` がそのまま効きます）
4. 最後に人がやること: `RULES-INBOX.md` の取り込み / `REVIEW.md` の一括確認 /
   `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **火球は火を点けません**（本家は当たった所が燃える）。火そのものが無いので、
  入れるならブロックとして足すところからです
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）。手触りの数値なので `TUNING.md` に
- **ネザーでも寝られます**（本家はベッドが爆発する）。`canSleep()` は時刻だけで決まります
- **`F3` の `biome` はネザーでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）。気になるなら `spawnOn` を 1 行足せば絞れます
