# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-26）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1972 件すべて成功**（1929 → 1972。この周の 43 件はエンドクリスタル）
- **進行（クリア導線）達成 12 / 13**（12 件とも本物。**仮は残り 1 件** ——
  「エンダードラゴンを倒せる」だけ）。**この周は進行を動かしていません**
  （クリスタルはクリア導線の項目ではありません）
- ブロック ID **1..63 の空きは 9 個**（この周は 1 個使いました: **エンドクリスタル 54**）。
  向き違いは **110 まで**、アイテム ID は **92 まで**（アイテムは増えていません。
  クリスタルのアイテム 54 は 1..63 の決まりで自動でできますが、手に入りません）
- `src/main.ts` は **1465 行 / 上限 1500**（余白 35 行。この周は +16 行）。
  **`main.ts` の次元の分岐は 0 件**（上限 2）

## 直前にやったこと — TASKS 2-11b（エンドクリスタル）

**柱 10 本の上に 1 個ずつ載り、素手でも矢でも壊せます。**

| どこ | 何が入ったか |
| --- | --- |
| `blocks.ts` | ブロック **`END_CRYSTAL` = 54**（`model: "boxes"` の 3 段・光る・硬さ 0.2・`solid`） |
| `items.ts` | `DROPS` に 1 行（**何も落ちない**） |
| `endgen.ts` | **`CRYSTAL_SPOTS`**（`PILLARS` から `top + 1`）と `crystalTopAt` / `NO_CRYSTAL` / `CrystalSpot`。`shapeAt()` が `crystal` も返し、`blockAt()` が 5 つ目の引数を取る |
| **`crystals.ts`（新規）** | **判断は全部ここ。** `crystalStates` / `crystalState` / `liveCrystals` / `shatterCrystal` |
| `projectiles.ts` | **`remove(projectile)`** を足した（当たった先が消えたときに弾も消す） |
| `main.ts` | `projectiles.onHitBlock` を `shatterCrystal()` へ繋いだ（+15 行） |
| `test/crystals.test.ts`（新規） | 31 件。**本物の `World` でセーブを往復**して、砕けたままであることを見ている |
| `test/endgen.test.ts` | クリスタルの節（+9 件。うち 1 件は「虚空の列には載らない」）。`blockAt` の引数と `topObsidianOf()` |
| `test/arena.ts` | `Slab.hasColumn()`（`frozenColumns` と**同じ集合**を見る） |
| `test/projectiles.test.ts` | `remove()` の節（+3 件） |
| `test/ui.test.ts` | `routed` に 2 件（`shatterCrystal(` / `projectiles.remove(`） |

### 「モブ側か、新しい器か」に、3 つ目の答えを出しました

**前の周の `HANDOFF.md` は「モブ側 / 新しい器」の 2 択で残していました。** どちらにも
決定的な弱点があったので、**ブロック（54）**にしました。**両方の弱点が同時に消えます:**

| | 保存（壊したまま残るか） | 当たり判定 | 新しい「確かめられないもの」 |
| --- | --- | --- | --- |
| モブ側 | **× 生き返る**（モブは保存しない） | ○ ただで付く | 増えない（湧き・歩き・デスポーンを止める必要あり） |
| 新しい器 | ○ セーブに持てる | **× 自分で書く** | **× `crystalrender.ts` が増える** |
| **ブロック（採用）** | ○ **`edits` に乗る** | ○ **掘るのも飛び道具もただで当たる** | **増えない** |

**「柱の上に載る・壊せる」しか要らないのに、器を 1 つ増やす理由がありませんでした。**
`crystals.ts` は器ではなく**ワールドに聞く関数の集まり**で、`update(dt)` も持ちません
（`chests.ts` と同じ）。

### 決めたこと

- **`solid: true` にすること。** `collisionBoxes()` が `solid` でないブロックに空の箱を
  返すので、**`solid: false` だと矢が素通りします**（`test/crystals.test.ts` が見張り）。
  そのぶん柱の上でクリスタルに乗れます（本家は通り抜けます）
- **居場所は `endgen.ts` の `CRYSTAL_SPOTS` 1 か所**（`PILLARS.map(p => ({...p, y: p.top + 1}))`）。
  `crystals.ts` はそれを import するだけで、**輪を組み直しません**
  （`PILLAR_RING` / `Math.cos(` が出てこないことをテストが見ています）。
  **向きは endgen → crystals の 1 方向だけ** —— 逆にすると、モジュールの頭で
  `PILLARS` を読む所が循環参照になります
- **生き死には 3 つの状態**（`alive` / `gone` / `unknown`）。**`unknown` は生きているに
  数えません** —— 数えると、エンドに降りた直後（遠くの列がまだ読めていない）に
  **砕いたはずのクリスタルからドラゴンが回復し続けます**。逆に取りこぼしても、
  列が届いた次のフレームには数え直されます
- **`shatterCrystal()` は居場所の表と突き合わせません。** そのマスに実際に何があるかだけを
  見るので、クリエイティブで柱の外に置いたものも同じように砕けます。返すのは
  **砕けたブロックの ID**（何も起きなければ `AIR`）—— 呼ぶ側が「当てた弾を消すか」を
  決められるようにするため
- **何も落としません。** 拾えると、柱の上へ運び直してドラゴンの回復を復活させられます
- **爆発しません**（本家は爆発してプレイヤーも吹き飛ぶ）。火そのものが無いのと同じで、
  爆発の仕組みから作ることになります（`TUNING.md`）

### 確かめたこと

- **わざと壊して 9 通りとも赤くなることを確かめました**: 柱の上面に埋める → 10 件 /
  `solid` を外す → 1 件 / 虚空の判定より先に置く → 1 件 / `hasColumn` を見ない → 3 件 /
  何のブロックでも砕く → 2 件 / 自分自身を落とす → 1 件 / 砕いた弾を消さない → 1 件 /
  居場所を写す → 9 件 / `main.ts` が自分でブロック ID を見る → 1 件
- `endgen.ts` はチャンク生成の中なので `npm run bench` を 3 回:
  `generateChunk` **0.89 / 1.02 / 1.12ms**（中央 1.02）/ `buildChunkMesh` 0.98〜1.03ms /
  `world.update` 平均 3.00〜3.28ms・最悪 12.0〜12.6ms。**エンドの生成そのものは 0.067ms**
  （2-11a の 0.115ms から下がって見えますが、**この箱の日差**です ——
  同じ走りのオーバーワールドが 2.87 → 1.63ms に動いています。
  テストが毎回**同じ土俵で**測って比べているので、そちらを見てください）

### 前の周（2-1 〜 2-11a）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージは書かず、撃った側が
  `Shot.damage` に載せる**ので、2-12 の弓矢は撃つ側を足すだけで当たる側が要らない）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ）
- **2-7**: 投げたエンダーアイが要塞の方を向く（`stronghold.ts` 新規）
- **2-8**: 要塞（石レンガ 53 + エンドポータルの部屋 + 枠 51。地面から 18 マス下）
- **2-9**: エンドポータルの起動（`endportal.ts` 新規。枠 12 個にアイ）
- **2-10**: エンド次元（`endgen.ts` 新規。エンドストーン 50 の島 1 つ。**達成 12 / 13**）
- **2-11a**: エンドの黒曜石の柱 10 本（`endgen.ts` の `PILLARS`）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 /
  2-8 / 2-9 / 2-10 / 2-11a / 2-11b の 16 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

0. **`git checkout -- src test` で、この周の実装を一度まるごと消しました。**
   「わざと壊して赤くなるか確かめる」の後始末に使ったのですが、**コミット前だったので
   追跡済みファイルの変更が全部飛びました**（新しい 2 ファイルは未追跡なので残りました）。
   **わざと壊す前に、必ずコミットしてから始めること。** そうすれば
   `git checkout HEAD -- src test` が安全な「元に戻す」になります
   （この周は作り直してから、コミット後に 9 通り確かめ直しました）。
1. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（これまで 8 度）。
   **残る仮は 1 件**（「エンダードラゴンを倒せる」）で、
   `sourceHas("src/mobs.ts", "dragon")` なので**同じ形で化けます** ——
   `MobDef` に `dragon` を書いた瞬間に 13 / 13 になります。**気を付けてください。**
2. **`solid` でないブロックには飛び道具が当たりません**（この周）。
   `collisionBoxes()` が `solid` でないブロックに**空の箱**を返すので、
   `blockOverlapsBody()` が必ず false になります。**「通り抜けられるが矢は当たる」は
   いまの仕組みでは書けません。**
3. **未読み込みの列を「無い」と読まないこと**（この周で 4 度目 —— かまどの `syncLit()`、
   ベッドの `moveToSpawn()`、モブ・落とし物の物理、そしてクリスタル）。
   `getVoxel` は未読み込みで AIR を返すので、**`hasColumn` を見ないと
   「10 個とも砕けた」と読みます。**
4. **「本物の `World` で往復する」テストは、遠い所を先に読み込むこと**（この周）。
   `primeAround` は 1 か所ぶんしか読まないので、**残り 9 個は `unknown` のまま**でした。
   数で見ると「戻っていない」のか「まだ見えていない」のか区別できません
   （**先に「読めている」ことを確かめる**という `rules/testing.md` の話そのものです）。
5. **場所の限界は「実測した最小値」ではなく「式」で決めること**（2-11a）。
6. **「早く切る帯」は、実際の値から出すこと**（2-11a）。
7. **「面積で形を測る」判定は真円を通します**（2-10）。**形そのものを測ること。**
8. **座標の移し方は次元ごとに違います**（2-10）。
9. **「探して無ければ建てる」を別の次元に使い回さないこと**（2-10）。
10. **同じ向きが並ぶ列から中心を逆算しないこと**（2-9）。
11. **手で並べた試験場は、建てる側との食い違いを見つけられません**（2-9）。
12. **構造物の `extent` は「どこに掛かるか」の唯一の材料です**（2-8）。
13. **地面から潜る構造物の深さは、地形の最低高さで頭打ちになります**（2-8）。
14. **1 チャンクに収まった標本だけを見ていると、端の欠けを見逃します**（2-8）。
15. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（2-7）。
16. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字まで。
17. **探す半径は「正方形」であって「円」ではありません**（2-5）。
18. **試験場のプレイヤーは、跳ぶモブでは `HOSTILE_SIGHT`(18) より遠くに置くこと**（2-5）。
19. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）。
20. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の火球で焼け死にます。
21. **相手を先に、ブロックをあとに見ること**（2-4b）。
22. **飛ぶモブには `ACCEL_GROUND` を使うこと**。
23. **`world.seed` をセーブや場所の計算に使わないこと**（次元では塩を混ぜたあとの値）。
    種は `worldSeed`。
24. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）。
25. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）。
26. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）。
27. **わざと壊して、テストが赤くなるか確かめること。** この周は 9 通りとも確認済みです。
28. **`test/audio.test.ts` が揺れます（この周で 3 つ目の判定が落ちました）。**
    これまでに落ちたのは「声色（音程の倍率）が波形に出る」（2-8 の周）/
    「cutoff が低い材質ほどこもって鳴る」/ 「音量がそのまま大きさに掛かる（5 倍）
    — **0.00 倍**」の 3 つで、**どれも同じ形（書き出した波形がまるごと無音になる）**です。
    **この周は `audio.ts` も `sfx.ts` も 1 行も触っていません。**
    落ちた直後に 3 回まわして 3 回とも緑でした（1972 件すべて成功）。
    **テスト側は各レンダリングを 1 本ずつ `await` していて、同時には走らせていません**
    （＝テストの組み立ての問題ではありません）。`node-web-audio-api` の
    `OfflineAudioContext.startRendering()` が、箱が混んでいるときに無音を返している、
    という筋だと思われます。**赤が出たらまず `npm test` をもう一度**、
    2 回続けて同じ判定が落ちたときだけ本気で見てください
    （`world.test.ts` の p99 とまったく同じ扱いです）。

## 次に取るもの

1. **2-12（弓と矢）。** アイテム ID は **93 から**（`ROADMAP.md` の予約表に
   「弓 / 矢」の行があります）。
   - **当たる側は 1 行も要りません** —— `projectiles.ts` の `onHitTarget` →
     `mobs.hitByProjectile()` も、`onHitBlock` → `shatterCrystal()` も、
     もう `main.ts` に繋がっています。**足すのは「撃つ側」だけ**
     （弓のアイテム・矢のアイテム・引きの時間・`Shot.damage`）
   - **引きの時間とダメージは手触りの数値**なので、本家の値（引き 1.0 秒 /
     最大 9）を暫定で置いて `TUNING.md` に足し、止まらずに進むこと（`LOOP.md` の 3.5）
   - **`N` キーのデバッグ射出は残しておくこと**（本物の弓が動かないときの切り分け用）
2. そのあと **2-13（ドラゴン）**。
   - **回復のもとは `crystals.ts` の `liveCrystals(world)`**（生きていると
     **確かめられた**ものだけ。`unknown` は数えません）
   - **`mobs.ts` に `dragon` という語を書いた瞬間に進行が 13 / 13 に化けます。**
     書くなら**同じ周で本物の判定に差し替えること**（上の「踏んだ穴 1」）
   - 戦場（柱 10 本 + クリスタル 10 個）は**種に依らず同じ形**です
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 3 件。うち 1 件は
   新しいファイル `.claude/rules/crystals.md`**）/ `REVIEW.md` の一括確認
   （**いま 16 節**）/ `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **クリスタルは爆発しません**（本家は当たると爆発してプレイヤーも吹き飛ぶ）。
  火と同じで、爆発の仕組みそのものから作ることになります。
- **クリスタルの上に乗れます**（`solid`。本家は通り抜けます）。矢を当てるために
  `solid` にした副作用です。
- **クリスタルは素手で 1 秒で壊せます**（柱の上まで登れば、矢が無くても壊せます）。
- **ドラゴンとクリスタルを繋ぐ光の線（ビーム）はありません**（本家にはあります）。
- **柱は掘れます**（黒曜石なのでダイヤのツルハシで 9.4 秒／マス）。本家も同じです。
- **柱の足元にモブが湧くかどうかは確かめられていません**（`REVIEW.md` の「2-11a」の 6 番）。
- **エンドからは戻れません**（本家と同じ。出口は 2-13 でドラゴンを倒すと開く想定）。
  **落ちて死ねばリスポーン地点へ戻る**ので詰みにはなりませんが、
  **持ち物は 5 分で消えます**（虚空に落ちたぶんはその場で消えます）
- **エンドの島の上にはモブが 1 体も湧きません**（空の明るさが 0.7 固定なので
  `spawnLight` が 10.5 になり、敵対の上限 7 を超えます）。**掘った穴の中は暗いので湧きます**
- **エンドの島は 1 つだけです**（本家は外側にも小さい島がたくさんあります）
- **本家の「星空」の見た目は出せません**（テクスチャが無いので暗い紫の無地）
- **要塞は「部屋 1 つ」だけです**（本家は通路・図書室・階段・シルバーフィッシュ）
- **要塞の中に入る道がありません**（掘って降りる前提）
- **投げたエンダーアイは、近づいても落ちません**（本家は近いと真下へ落ちる）
- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  `eyeShot()` に次元を渡して「オーバーワールド以外では null」にするのが素直です
- **投げたアイは地面に落ちません**（`projectiles.onExpire` で `drops` に渡すところから）
- **アイを投げても減りません**（本家は 20% で壊れる。`TUNING.md` の 2-7）
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒。`smelting.ts` に 1 行）
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方を全部見直すことになるので、**独立したタスクとして決めてください**
- **エンダーマンは日陰へ逃げません**し、**ブロックを持ち運びません**
- **ネザーでも寝られます**（本家はベッドが爆発する）
- **寝ても時間は「進みません」**（時計だけ飛びます）
- **火球は火を点けません**（火そのものが無いので、ブロックとして足すところから）
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）
- **`F3` の `biome` はネザーでもエンドでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）
- **要塞の部屋の中にも敵対モブは湧きます**（石レンガは `spawnOn` を持たないため）
