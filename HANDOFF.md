# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-18・AUTODEV 99（C の周）のあと）

**この周は C（実装）の周**でした（判定表の 3 行目 —— `AUTODEV-SPEC.md` が
`状態: 未着手` だったため）。**キューの 12「刈られた羊が見て分かること」を実装しました。**

- **刈った羊が、羊毛の戻るまで「刈られた姿」で描かれます。** 判断は
  **`mobs.ts` の `mobVariant(mob)` 1 か所だけ**（`shornBoxes !== null && woolTimer > 0 ? 1 : 0`）で、
  **`kind === "sheep"` は 1 文字も書いていません**。箱の取り出しは
  **`variantBoxes(def, variant)` 1 本**（`mobmesh.ts` も `mobrender.ts` も
  `def.shornBoxes` を直に読みません）
- **ID は 0 個**・**`main.ts` は 0 行**（1450 行のまま）。`MobDef.shearing` の値（1〜3 個・60 秒）も ±0
- 触ったのは `src/mobs.ts` / `src/mobmesh.ts` / `src/mobrender.ts` / `tools/shot.ts` /
  `test/mobs.test.ts` と、文書（`AUTODEV-QUEUE.md` / `AUTODEV-SPEC.md` / `TUNING.md` /
  `docs/autodev-log.md` / `rules/mobs.md` / `rules/meshing-render.md` / この `HANDOFF.md`）

### この周で測った値（次の周がそのまま使えます）

- **体（`group 0`）の外形 0.625 x 0.563 x 1.250 → 0.438 x 0.438 x 1.063**（3 軸とも縮む）
- **⚠ モデル全体の外形は刈っても変わりません**（**0.625 x 1.125 x 1.444 のまま** ——
  脚と頭が外形を決めている）。**細さを測るなら体で比べること**
- **地肌 `0xe3b09c` は羊毛 `0xe8e4dc` から 82.6・顔と脚 `0xd6c8b4` から 36.3**（判定は 40 / 20）
- 当たり判定は**前と横 0.444 ≤ 0.45・高さ 1.125 ≤ 1.2**・後ろ 1.000
  （`longBody` の例外はそのまま。**広げていません**）

## 数字（2026-09-18・この周で測ったもの）

- `npm test` **3837 件**（+35。既存の「モブの形」5 件を `checkShape()` に括り出して
  変種 0（10 種）と変種 1（羊）の両方に掛け、`刈られた姿` の節を足したぶん）
- **⚠ 音の跳ねは相変わらずです**: この周は **7 回走らせて 5 緑 / 2 赤**で、
  **2 回とも「声色（音程の倍率）が波形に出る」**（低い声 0.162 → 4.083）。
  **`src/audio.ts` / `src/sfx.ts` / `test/audio.test.ts` に 1 行も差分が無い**ことと、
  **落ち着いた値（低い声 0.162 / 高い声 0.329）に毎回戻る**ことの 2 つで揺れと判じました
  （`rules/testing.md` のとおり、**続けて落ちたかでは見ていません**）。**判定は 1 文字もゆるめていません**
- `npm run typecheck` 緑 / `npm run build` 緑 / **`npm run bench` は走らせていません**
  （生成もメッシュ化も触っていないため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1450 行 / 上限 1500**
- **アイテム 145 種・`MAX_ITEM_ID` 186**（±0）・**立方体 42 / 非立方体 89**（±0）
- **1..63 の空きは 7**・**111..255 の空きは 69**（±0。**次に取るのは 187**）
- `mobs.ts` 3096 行 / `mobmesh.ts` 141 行 / `mobrender.ts` 204 行
- `AUTODEV-QUEUE.md` **115 行 / 上限 150・未着手 2 件**（12 の行を消した）・
  `AUTODEV-SPEC.md` **111 行 / 上限 120・`状態: 済`**

## 次にやること

**次は A（棚卸し）の周** —— `AUTODEV-QUEUE.md` の未着手が **2 件（3 件未満）**なので
判定表の 1 行目です。**基準は変えないこと**（「本家のバージョン順」。キューの先頭に 1 行あります）。

- **`src/**` と `test/**` に 1 行も差分を作らないこと**（A の周の完了条件）
- 数え方の表は `docs/autodev-counting.md`（8 ジャンル。A の周でだけ開けばよい）
- **⚠ そのときキューに「音の揺れを測り方で直す」を足すこと**（下の「人がやること」。
  前の周から持ち越しです。**直すのは測り方で、判定はゆるめないこと**）
- **残る 2 件（35 骨粉・24b ケーキをかじる）はどちらも `main.ts` を触ります** ——
  **下の ⚠「`main.ts` を 2 つに割ってよいか」がここで当たります**

## ブラウザで見てほしいところ

**この周は `npm run shot -- sheep mobs` と `node tools/browsershot.mjs` を撮って
`Read` で開きました。** 場面 `sheep` を `tools/shot.ts` に足してあります
（**`mobs` の場面には刈られた羊は出ません** —— `spawn()` したてで `woolTimer` が 0）。

- **絵で確かめたこと**: もこもこ（白くて大きい）と刈られた羊（地肌の桃色・細くて低い）が
  **並べて一目で見分けられる**こと / **面の欠けと裏返りが 0 件**（巡回順の検査も緑）/
  ブラウザ（Chromium・WebGL 2.0）で **console にエラーも例外も 0 件**
- **⚠ 刈られた羊の手ざわり（12）**: **地肌の桃色 `0xe3b09c` が「刈られた羊」に見えるか** /
  **細さ（体が 0.625 → 0.438 幅）が足りているか** / **頭が体から出た輪郭**が読めるか /
  **60 秒で戻るのは待てる長さか**（`TUNING.md` の 12 の節）
- **⚠ ツタの生え方の手ざわり（34c）**: **森の木の 25%** に掛かるのは多いか少ないか /
  **1 列 0〜4 マス**の垂れ方は「ツタ」に見えるか / **苗木から育てた木には掛かりません** /
  **針葉樹林には 1 本も生えません**（`BiomeDef.vine` を変えれば生えます）
- **⚠ 垂れたツタの手ざわり（34b）**: **宙にぶら下がった 1/16 の板が「ツタ」に見えるか**
- **⚠ 薄い板の横・真上に石があると置ける件**・**⚠ 石の天井の下のツタ**
  （**手では置けないのに、あれば落ちません**。34c はこの性質に寄りかかっています）
- **⚠ ツタ（34a）**: 登る速さがはしごと同じでよいか / 硬さ 0.2
- **⚠ 金・ダイヤの防具の色（33b）**: **金の上着 `0xefd714` とズボン `0xf8b317` は絵の上でやや近い** /
  **⚠ ダイヤ一式 80% 減が強すぎないか**（**防具は壊れません**）/ **⚠ 金を作る理由があるか**
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ 鉄の防具の色（33a）**: 「鋼の青」が鉄に見えるか / 鉄一式 60% 減の手ざわり
- **レンガ（32b）** / **粘土（32a）**: 海底の 7.2% / **ハーフ 2 材質（31）** /
  **苗木（30b）**: 180 秒が待てる長さか / **木炭（29）** / **修理（28）**
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / **防具（27b）**: 革一式 28% 減
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白** / **氷（25a）**: 滑りの手ざわり
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 2 件**）

- **`rules/mobs.md`**: 「刈られた羊はまだ見た目が変わりません」を落として、
  **「姿（`MobDef.shornBoxes` と `mobVariant()`）」の節**を足しました ——
  判断は 1 か所 / 取り出しは `variantBoxes()` / **`?:`（省略可）にしない** /
  `groups` は姿をまたいで使い回す / **形の控えの鍵は `種類:姿`**（種類だけに戻すと
  **先に湧いたほうの姿で全部が描かれます**）/ **全体でなく体で比べる** /
  地肌の色は隣の色から離す。
- **`rules/meshing-render.md`**: **`npm run shot` のカメラを目の高さまで下げて
  4 マスまで寄らないこと** —— **画の下半分の地面が 1 枚も描かれず空色に抜けます**。
  **この周でいちばん高くついた落とし穴です**（下）。

**`RULES-INBOX.md` は ±0 行**（層 3・スキルに足すことはありませんでした）。

## 人がやること

- **⚠ 音の揺れを測り方で直すか（持ち越し 2 周目）。** この周も **7 回中 2 回赤**で、
  **赤い周は公開サイトに載りません**。**直すなら測り方**（窓を長く取る・複数回鳴らして
  中央値）で、**判定はゆるめないこと**。**キューに足すのは A の周の仕事で、次の周が A です**
- **⚠ 設計の判断（70 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 2 件（35 骨粉・`use.ts` の 19 通り目 / 24b ケーキをかじる）で、
  キューに残っているのはこの 2 件だけです** —— **次に取るタスクでここに当たります**
- **⚠ 刈られた羊は倒しても羊毛が出ません**（`dropFor()`。前の周から変えていません）
- **⚠ 石の天井の下のツタの非対称**（手では置けないのに落ちない）。**34c の生成は
  この性質を支えの保険に使っています**
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 99 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

## 絵を撮るときの注意（実測で分かっているもの）

**⚠ `npm run shot` のカメラを目の高さ（`y + 1.7`）まで下げて 4 マスまで寄らないこと** ——
**画の下半分の地面が 1 枚も描かれず空色に抜けます**。谷の暗がりに見えるので地形のせいだと
思い込み、**`setVoxel` を 1365 件たたいて足場を埋め、`primeAround()` を 3 回流しました**が、
**三角形の数が 1 枚も増えませんでした**（`getVoxel` はちゃんと草を返します）。そのあと
「平らな草地を探す」仕掛けを書き足しても**一度も当たりません**（**草原は一面に丈の高い草で、
`surfaceY` が揃う 9 x 10 が無い**）。**原因はカメラが低くて近いことだけで、`y + 2.2` まで
上げて 6.5 マス引いたら一発で消えました。** **モブを撮る場面は `mobs` の構え方を写すこと。**

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと**。
**⚠ 枠を数えるときは `#creative .slot` を使うこと**（素の `.slot` はホットバーと
インベントリの 36 枠まで拾います）。**⚠ `/opt/pw-browsers` には
`chromium_headless_shell-1194` も居ます** —— `startsWith("chromium-")` で弾くこと
（`tools/browsershot.mjs` の `findChrome()`）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**この周の `sheep` がその形です。**
**時間が経たないと見えないもの**（育つ・燃える・溶ける・**刈られる**）は、
**その場で `update()` を回すか、その操作を呼んでから撮ること**
（`sheep` の場面は `mobs.shear()` を通しています。**`woolTimer` を直に書き換えないこと**）。
**⚠ 防具を着た姿は撮れません**（一人称視点なので自分の体が写りません）。
