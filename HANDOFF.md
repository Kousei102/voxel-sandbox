# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05・AUTODEV 39 のあと）

- `npm test` **3100 件すべて成功**（この周は **+10 件**）・`npm run typecheck` 緑・
  **`npm run build` 緑**（`src/**` を触ったので走らせました）
- **`npm run bench` は走らせていません** —— 生成で増えたのは**サトウキビの立つ列だけ 1 回の
  `hash2`**（浜の 12%。ほかの列は `tuft === SUGAR_CANE` で落ちて引きません）で、
  `veinAt()` のように**ブロック 1 個ごとに走る場所ではありません**
- **音の一群は 1 回も跳ねませんでした**（**最後まで通した 5 回とも 3100 件すべて成功**。
  羊毛 0.552 / 雪 0.716 / 砂 2.235 で落ち着いています）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1447 行**（`wc -l`。上限 1500・**止まる目安 1450 まで残り 3 行**。**この周は ±0**）
- **アイテム 109 種・`MAX_ITEM_ID` 144・111..255 の空きは 111・1..63 の空きは 9**（**この周は ±0**）
- **立方体 39 / 非立方体 72**・**モブ 9 種類**・**レシピ 54 本**・**食べ物 9 種**

## この周でやったこと（AUTODEV 39 の C・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 未着手` だったので判定表の 3 行目に当たって **C（実装）の周**。
**18b（サトウキビが積める）**を仕様書のとおりに実装しました（**差し戻し 0 回**。
実装も点検も自分でやっています —— サブエージェントは立てていません）。**芯は 3 つ**:

1. **`blocks.ts` の `supportsBlock(supporter, face, id)`** —— `canSupport()` の**外側**に
   「自分の上には自分を置いてよい」を 1 行。`stacksOnSelf` を true にしたのは
   **`SUGAR_CANE` だけ**。`CANE_HEIGHT_MAX = 3` も def の隣に置きました
2. **`world.ts` の 2 か所**（`canPlaceAt` と `breakUnsupported`）を同じ式に付け替え。
   **`test/arena.ts` の写しも同じ式に**（写しはあそこ 1 か所だけ）
3. **サトウキビの `replaceable: true` を外した** —— これが積める鍵です（前の周の実測どおり）

生成は `wy === h + 1` を **`wy <= h + tall`** に広げ、`tall` は**サトウキビのときだけ**
`1 + floor(hash2(wx, wz, seed ^ 0x5b27) * CANE_HEIGHT_MAX)`（**塩は新しいもの**）。

**`main.ts` / `placing.ts` / `items.ts` / `crafting.ts` / `biomes.ts` / `crops.ts` は 1 行も
触っていません。** 新しい ID は 0 個、`SaveData` も 1 バイトも増えていません。

**実測**（`npm test` が毎回出します）: **段数 1 段 33% / 2 段 37% / 3 段 30%・4 段以上 0 本** /
**密度 12.20% は前の周から動いていません**（判定は 1 文字も触っていません）/
**どの段の真下も砂かサトウキビで、破れ 0 件**。

## 撮った絵で確かめたこと（C-3）

`npm run shot -- beach terrain` と `node tools/browsershot.mjs`（5 枚・WebGL 2.0 で例外 0 件）。
**`shots/beach.png` を 4 倍に拡大して見ました**（切り出しは使い捨ての script。
`tools/raster.ts` の PNG はフィルタ 0 なので `node:zlib` だけで読めます）:

- **1〜3 段が混ざって立っています**（18a の「寸詰まり」は取れました）
- **段の継ぎ目は空いていません**（`CANE_BOX` の上端 1 が効いている）・**面の欠けも裏返りも 0**
- 砂に見えた薄い塊は**砂の段差の陰**で、この周とは無関係でした

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` で `AUTODEV-QUEUE.md` の
未着手が 4 件なので、判定表の 2 行目です。

**ただし先頭の 18c（サトウキビが時間で伸びる）は `main.ts` の配線 6 行が要ります。**
下の「人がやること」の判断が返っていなければ、**仕様書を書く前にその 1 件をコードで
数え直し**（`AUTODEV.md` の B）、**行数で閉じないと分かったらキューの並びを直して
次の 1 件へ回すこと**。19（はしご）も配線が要るので、動かせるのは 11（スケルトン）と
12（刈られた羊）です。

## ブラウザで見てほしいところ

**この周の分（サトウキビが 1〜3 段になりました）**:

- **背丈と密度の釣り合い** —— 0.12 のまま段数だけ増えたので、**浜が縦に混んで見えないか**
  （撮った絵では「まばらな竹林」から「高さの揃わない竹林」になりました）
- **積む手ごたえ** —— 上面を狙うと 1 つ上に立ち、**横面を狙うと隣のマスに立ちます**
  （本家と同じ。狙いにくくないか）。**いちばん下を壊すと上が全部落ちます**
- **⚠ 前から遊んでいる世界だけの見え方**: `localStorage` の差分は「壊した／置いた」だけを
  持つので、**前の周に浜のサトウキビを壊した場所は、下の 1 マスだけが空いて上の段が
  浮いて見えることがあります**（差分の復元は `breakUnsupported` を通りません）。
  **新しく作った世界では起きません。** 直すにはセーブの作り替えが要るので、
  **気になるかどうかを人に決めてほしいところです**

**持ち越し（まだ誰も見ていません）**: 一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と
見分けられるか（雪から 22.3）/ サトウキビ（`0x9ad14f`）が小麦の種（42.4）と並んだとき /
ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で
材料 3 つに見合うか / 食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度
（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を草むらと取り違えないか /
サボテンに当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・ミルクで毒が止まるか /
ミルクバケツの色（`0xeaf2f8`）が羽根・卵・糸・雪玉と見分けられるか /
かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
鉄・金・ダイヤのブロックが材料の山と見分けられるか /
**一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`。
「キノコシチュー」は 7 文字でいちばん長い名前）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 2 件を据えました）

- **`rules/blocks-shapes.md`** —— 「十字は支えになれない／積み上がる生えものには別の決まりが
  要る」の続きに、**`supportsBlock()` の実物**と、「置く側と壊す側が同じそれを通すこと」
  「`stacksOnSelf` を付けるなら `replaceable` を外すこと」「段を ID で表さないこと」を足しました
- **`rules/worldgen.md`** —— **「書き込みの形（`wy === h + 1`）は変えないこと」は間違いでした。**
  `h` も種類も `tall` も列のキャッシュとハッシュだけから決まるので、**上へ何マス伸ばしても
  列をまたいで欠けません**（崩れるのは「前に生成したチャンクを見て決める」形にしたときだけ）。
  段数の塩 `0x5b27` も塩の一覧に足しました

## 人がやること

- **設計の判断を 1 つお願いします（13 周持ち越し。ただし急ぎではありません）:
  `main.ts` を 2 つに割ってよいか。** いま **1447 行で、止まる目安 1450 まで残り 3 行**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **18b は予告どおり ±0 行で通りましたが、次の 18c（伸びる）と 19（はしご）は
  どちらも配線が要ります** —— そこで止まります
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 23 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
