# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- `npm test` **2965 件すべて成功**（**2962 → 2965。+3 件**）・`npm run typecheck` 緑・
  **`npm run build` 緑**（`src/**` を触ったため）。`npm run bench` は走らせていません
  （**生成もメッシュ化も 1 行も触っていない**ため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1464 行**
  （1 行も触っていません）
- **アイテム 98 種・`MAX_ITEM_ID` 133・111..255 の空きは 122・1..63 の空きは 9**
  （**この周は ID を 1 個も取っていません**）
- **モブ 9 種類**（受動 4 / 敵対 4 / ボス 1）

## ⚠ 次に `main.ts` を触る周がまず見ること: 1464 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
**牛・クモ・弓を含む 5 周とも `main.ts` を 1 行も触っていないので、持ち越しです。**
**次に触る周は、先に判断を別ファイルへ出すこと。**

## この周でやったこと（AUTODEV 22 の C・クラウドの無人の周）

`AUTODEV-SPEC.md` が `状態: 未着手` だったので **C（実装）の周**。当たった 1 種類だけを
やりました。**弓の材料を羊毛 3 → 糸 3 に差し替え、本家と同じ「棒 3 + 糸 3」に戻しました。**

- **`src/**` に足したのは 2 語だけ**: `crafting.ts` の `STRING` の import と、
  弓の `key: { S: STICK, W: STRING }`（`shape` / `count` / `out` / `name` は 1 つも動かして
  いないので、「同じ形のレシピが重複していない」は緑のまま）。あとは全部コメントです
- **糸に初めて使い道が付きました。** **弓を作るにはクモを 3 匹倒します**
  （`SPIDER.drop` が 1 個固定のため）。**羊毛の使い道はベッド 1 本だけ**になりました
- **新しい判定 3 件**（既存 3 件は盤面の `L` → `G` の差し替えで、件数は増えていません）:
  「羊毛では弓が作れない」/ **「ベッドは今までどおり羊毛 3 + 板 3」**（**この周まで
  `test/crafting.test.ts` にベッドの判定は 1 件もありませんでした**）/
  「糸を使うレシピはちょうど 1 本（弓）」（本数と名前を出力してから判定）
- **撮影（C-3）は要らない周でした** —— `#recipehint` に出る名前も「弓」のまま変わりません
  （`AUTODEV.md` で撮らずに閉じてよい唯一の形。仕様書の 10 節に理由）
- **古くなった文書を 4 か所直しました**: `TUNING.md` の弓の材料と `SPIDER.drop` /
  `ROADMAP.md` の 133 番 / **`rules/items-survival.md`**（下）

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手が 2 件（3 件未満）になったので → 次は A（棚卸し）の周**です
（判定表を上から当てるので、`AUTODEV-SPEC.md` が `済` でも B より先に A が当たります）。

- **A の周は `src/**` と `test/**` に 1 行も差分を作らないこと**（完了条件）
- **基準は変えないこと**: 「本家のバージョン順（Alpha → Beta → 正式版）」。
  `AUTODEV-QUEUE.md` の先頭に書いてあります
- **実装済みは必ずコードから数え直すこと**（`docs/autodev-counting.md` の 8 ジャンルの表）。
  **キューに残っている 11 番（スケルトンと骨）・12 番（刈られた羊）も数え直しの対象**です
- **キューの数えた結果の表は 2026-09-01 のもので、もう古い**です
  （あの表は「アイテム 78 種 / `MAX_ITEM_ID` 94 / モブ 6 種類」で、いまは 98 / 133 / 9）

## ブラウザで見てほしいところ

**この周は見た目に 1 画素も出ません。** 新しく見てほしいのは**手触りが 1 つだけ**です:

- **クモを 3 匹倒すまで弓が作れない**という道のり（本家は糸 0〜2 個なので 2 匹で済むことも
  あります）。**遠すぎるなら、クモの湧きの重み（100 = 本家）ではなく糸の個数のほうを
  触ること** —— 重みを上げると夜の顔ぶれごと変わります（`TUNING.md` に 1 行残しました）

持ち越し（前の周のまま）: **夜の敵対がゾンビとクモで半々になった手触り**と**クモの速さ 6.0** /
**クモが朝に燃えてよいか**（本家は燃えません）/ **糸・羽根・卵がどれも淡い色** /
**投げた卵に使い道が無い** / **牛の胴が寸詰まりに見えないか** / **ステーキと革が一覧で
見分けられるか** / **産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒**。

## この周で分かったこと

**決まりごとを 1 件、`rules/items-survival.md` に据えました**（`Edit` で普通に直しました）。

**「残っている代用は弓の糸（羊毛 3）だけ」という決まりごとは、その代用を外した瞬間に
嘘になります。** 仕様書の 8 節が名指しで指していなければ、**`rules/` に古い言い分が
1 件残ったまま次の周へ渡っていました**（`AUTODEV.md` の C-4「古くなった決まりごとを
残さないこと」）。**B の周は、直すコードだけでなく「そのコードを説明している `rules/` の
行」も終了条件に並べること** —— この周はそれが効きました。同じ節に、

- **代用レシピはもう 1 本も残っていません**（砂 4 個 → ガラスは精錬が入ったときに外し、
  矢は 2026-09-03、弓はこの周に本家の形へ戻りました）
- **羊毛の行き場はベッド 1 本だけ**なので、`["WWW", "PPP"]` を書き換えると羊毛が
  行き場を失うこと（足場は「ベッドは今までどおり羊毛 3 + 板 3」1 件だけです）

も足しました。

## 人がやること

- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 4 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**この周は 5 回走らせて 1 回も赤くなりませんでした**（うち 2 回は
「2965 件すべて成功」の行まで見ています。足音は羊毛 0.552 / 雪 0.716 / 砂 2.235 の
落ち着いた値でした）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
