# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05）

- `npm test` **3059 件すべて成功**（+15）・`npm run typecheck` 緑・`npm run build` 緑
- **音の一群は 6 回中 2 回だけ揺れました**（`rules/testing.md`）。**続けて 4 回緑**で、
  落ち着いた値も一致（羊毛 0.552 < 雪 0.716 < 砂 2.235 / 低い声 0.162 < 高い声 0.329）。
  **音のコードは 0 行**触っていないので退行ではありません
- `npm run bench`: **generateChunk 1.38 / 1.41 / 1.43ms**（中央値 1.41）。
  **同じ箱で直前の `master` が 1.49 / 1.39 / 1.34（中央値 1.39）**なので誤差の中です
  （キノコは列ごとにハッシュを 1〜2 本足すだけで、内側の 16 段には入っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（**1 バイトも増えていません**）
- **`main.ts` 1445 行**（上限 1500・**止まる目安 1450 まで残り 5 行**。この周で ±0）
- **アイテム 105 種・`MAX_ITEM_ID` 140・111..255 の空きは 115・1..63 の空きは 9**
- **立方体 39 / 非立方体 71**（+2）・**モブ 9 種類**（変わらず）

## この周でやったこと（AUTODEV 33 の C・クラウドの無人の周）

`AUTODEV-SPEC.md` が **`状態: 未着手`** だったので、判定表の 3 行目に当たって
**C（実装）の周**。**17a（キノコ 2 種）を仕様書どおり実装しました。**

- **`blocks.ts`**: `RED_MUSHROOM = 139` / `BROWN_MUSHROOM = 140`。
  **草むらの定義をそのまま写した `model: "cross"`**（`CROSS_BOX` / `solid: false` /
  **`replaceable: true`** / 硬さ 0 / `sound: "grass"` / `supportFace: FACE_YN`）で、
  違うのは色（赤 `0xc9403a` / 茶 `0xb5835a`）だけ
- **`biomes.ts`**: `BiomeDef.mushroom` を足して 10 バイオームぶん。
  **0 より大きいのは森 0.015 と針葉樹林 0.01 だけ**（暫定。`TUNING.md`）
- **`worldgen.ts`**: **キノコを草むらより先に引きます**（あとに回すと密度が食い違う）。
  赤か茶かは **2 本目のハッシュで半々**（塩 `0x4d17` / `0x2f8b`）
- **`items.ts` は `MAX_ITEM_ID` の 1 行だけ**（下の「分かったこと」）。
  **`main.ts` は 0 行**・`crafting.ts` / `drops.ts` / `DROPS` も 0 行

**差し戻し 0 回。** 実装も点検（C-2）も自分で書き、`git diff` を読んで閉じました。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` なので判定表の 2 行目、
`AUTODEV-QUEUE.md` の未着手も 5 件（3 件以上）あるので A には落ちません。

**キューの先頭は 17b（ボウルとキノコシチュー）です。** 取る前に 1 件だけ数え直すこと。

- **次に取る ID は 141 から**（`ROADMAP.md` の予約表「141..255 予備 115 個」）。
  17b はボウルとシチューで 2 個 = **141 / 142** の見込み
- **`main.ts` は 17b で配線 1 行**（食べ終わったら空のボウルが 1 個戻る。
  **戻すものを決めるのは `items.ts`**）。**1446 行になり、止まる目安 1450 まで残り 4 行**
- 材料の**赤 139 / 茶 140 はもう入っています**（この周のぶん）

## ブラウザで見てほしいところ

**撮って確かめたこと**: `tools/shot.ts` に **`mushrooms` の場面を 1 つ足しました**
（地形は書き換えず、**赤と茶が両方ある所を探して立つ**だけ）。`shots/mushrooms.png` を
開いて、**十字の板に面の欠けも裏返りも無いこと**と、**赤・茶・草むらの 3 つが色で
見分けられること**を確かめています。`tools/browsershot.mjs` の 5 枚も撮って、
**WebGL 2.0 で例外なし**を確認しました（**`terrain` / `ground` / ブラウザの既定の
ワールドはどれも原点が平原なので、そちらにキノコは 1 本も写っていません**）。

- **この周で人に見てほしいところ**: 森を歩いたときの**キノコの密度**
  （森 0.015 / 針葉樹林 0.01 は暫定。実測で 64x64 に森 57 本 / 針葉樹林 41 本）と、
  **走っていて赤・茶を草むらと取り違えないか**（3 つとも `cross` の板 1 枚なので、
  色だけが手掛かりです）。**キノコにはまだ使い道が 1 つもありません**（17b でシチューに）
- 持ち越し（まだ誰も見ていません）: **サボテンに当たったときの痛さ**（0.5 秒ごとに 1 =
  溶岩のちょうど 1/4。**上に立っても痛くありません**）/ **牛を搾る / ミルクを飲んで毒が
  止まるか** / **ミルクバケツの色（`0xeaf2f8`）が羽根・卵・糸・雪玉と見分けられるか** /
  かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
  鉄・金・ダイヤのブロックが材料の山と見分けられるか / **一覧の枠の名前が 2 行に折れて
  はみ出す**（`style.css` の `.slot .label`）/ クモの速さ 6.0 と朝に燃えてよいか /
  投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか / 産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 3 件・`rules/` に据えました）

1. **`rules/items-survival.md`**: **「`items.ts` は 0 行」と書いてある仕様書でも、
   `MAX_ITEM_ID` の 1 行だけは要ります。** 17a の仕様書が「1 行も要りません」と
   書いていましたが、伸ばさないと `allItemIds()` が 1..138 しか数えず、
   **クリエイティブの一覧にだけ出てこないブロック**が 2 つできます
   （置けるし掘れるので `npm run typecheck` では止まりません）。
   **気付けたのは、完了の判定に「共有帯のアイテム 25 → 27 個」を数で書いてあったから**です
   —— 一覧の名前だけを見張っていたら通っていました
2. **`rules/worldgen.md`**: **原点のまわりを間引いて舐めるだけでは、まれなバイオームの
   生成物を数えられません。** 草むらの走査を写したら**キノコが 4 本**しか当たらず
   （**草むら 853 本が全部「平原」**で、森を 1 点も踏んでいなかった）、赤茶の内訳も
   何も言えませんでした。**まとまった森を先に探してから 64x64 を密に数える**形に
   直しています（サボテンの「まとまった砂漠」と同じ）。**判定はゆるめていません**
3. **`rules/meshing-render.md`**: **`world.surfaceY()` は「一番上のブロックの 1 つ上」**
   なので、**絵に撮りたいものを探すときは 1 つ下げること**（1 本も写らない絵を 1 枚
   撮りました）。ついでに「**自然に生えるものは地形を書き換えず、生えている所を
   探して立つ**」も据えました

## 人がやること

- **設計の判断を 1 つお願いします（7 周持ち越し・18 番で効きます）: `main.ts` を 2 つに
  割ってよいか。** いま **1445 行で、止まる目安 1450 まで残り 5 行**です。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **17b は 1 行**で通せるので、**あと 1 周は保ちます**（そのあとが危ないです）。
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 17 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
