# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-25）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。無人で回すうえで
  実際に踏んだものが 3 つあります:
  **(1)** **`.claude/rules/**` を編集すると「sensitive file」の確認で止まります。**
  → **`RULES-INBOX.md` に書いて、人が居る周が取り込む**形にしました（`LOOP.md` の「4.5」）。
  **`.claude/settings.json` に許可を書いても消えません**（2026-08-25 に 2 周とも実測）。
  **(2)** クラウドから `origin` へ push できず 403 → ユーザーが GitHub 側で権限を付け直し済み。
  **(3)** **5 時間の利用制限。** 当たった周は実装を終えた直後に止まり、次の定期実行も
  同じセッションに投げられて即終了しました（**新しい周にならず、1 回ぶんの枠が消えます**）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1775 件すべて成功**（1738 → 1775。この周の 37 件はエンダーマンと湧きの重み）
- **進行（クリア導線）達成 8 / 13**（8 件とも本物）。**この周で 1 つ増えました**
  —— 「エンダーマンがエンダーパールを落とす」が本物になりました
- ブロック ID **1..63 の空きは 14 個**（この周はブロックを足していません）。
  アイテム ID は **90 まで**（90 = エンダーパール）
- `src/main.ts` は **1400 行 / 上限 1500**（余白 100 行。**この周は 1 行も触っていません**）

## 直前にやったこと — TASKS 2-5（エンダーマン）

**初めての「跳ぶ（テレポートする）モブ」です。** 飛ぶモブ（2-4a の `flying`）と
まったく同じ形にしてあり、**`update()` に「エンダーマンなら」は 1 行もありません。**

| どこ | 何が入ったか |
| --- | --- |
| `items.ts` | `ENDER_PEARL`(90)。**積めるのは 16 個まで**（本家と同じ。エンドポータルに 12 個要る） |
| `mobs.ts` | `TeleportRule` / `MobDef.teleport` / `MobDef.spawnWeight` / `ENDERMAN` / **`teleportSpot()`（新・export）** / `pickWeighted()`（新・export）/ `Mob.teleportTimer` と `Mob.teleportUrge` / `Mobs.teleport()` |
| `test/mobs.test.ts` | 「跳ぶモブ（エンダーマン）」の節 17 件 + 湧きの重みの分布 2 件 + 見張りに `teleport` / `spawnWeight` |
| `test/progression.test.ts` | 8 番目の probe を本物に。`ACHIEVED_BASELINE` 7 → 8 |

- **判断は 2 つに割れています**: 「**いつ**跳ぶか」は `Mobs.teleport()`（`world` が要る）、
  「**どこへ**跳べるか」は **`teleportSpot()` 1 本**。壁の中・液体の中・ボクセルの無い列へ
  出さない保険は**あの関数の中だけ**にあります。**呼ぶ側に写さないこと** ——
  跳ぶ理由が 2 通り（逃げる／詰める）あるので、写すと必ず片方だけ直します
- **行き先が見つからなければ跳びません**（`tries`(8) 回探して駄目ならその場に留まる）
- **殴られたときは印（`teleportUrge`）を立てるだけ。** `wound()` は殴られた・撃たれた・
  焼けたの 3 か所から呼ばれるので、印にしておけば 3 通りとも同じ経路に乗ります
  （`world` を `wound()` へ配らずに済みます）。**待っているあいだの印は溜めません**
- **湧きの抽選に重み（`spawnWeight`）を入れました**（`pickWeighted()`。**受動と敵対で共有**）。
  均等割りのままだと**夜の敵対の半分がエンダーマン**（体力 40・一撃 7）になります。
  数値は本家の湧きの重み（ゾンビ 100 / エンダーマン 10 / 豚 10 / 羊 12）
- **本家と違って中立ではありません**（見つめると怒る、が無い）。中立にするには
  `hostile` の使われ方（湧きの枠・逃げ方・日光・ベッドで寝られるか）をまとめて
  考え直すことになるので、**別のタスクにしました**（下の「気付いているが…」）
- **わざと壊して 5 通りとも赤くなることを確かめました**: 液体の保険を外す → 1 件 /
  未生成の列の保険を外す → 1 件 / 当たり判定の保険を外す → 1 件 /
  重みを均等に戻す → 2 件（敵対の比と、受動が豚だけになる）/ `cooldown` を 0 に → 1 件
- **テストの偽陽性を 1 つ踏みました**（下の「踏んだ穴」の 1）
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません。
  決まりごとは **`RULES-INBOX.md` に 2 件**（どちらも `rules/mobs.md`）、数値は
  **`TUNING.md` に 12 行**（「モブ（エンダーマン）」の節）、確認項目は `REVIEW.md` の「2-5」に 9 個

### 前の周（2-1 / 2-2 / 2-3 / 2-3b / 2-3c / 2-4a / 2-4b / 2-4c）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージは書かず、撃った側が
  `Shot.damage` に載せる**ので、2-12 の弓矢は撃つ側を足すだけで当たる側が要らない）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`。
  ユーザーがブラウザで見つけた「ネザーで死ぬと岩盤の上に湧く」の直し）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 の 9 節が
  溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **探す半径は「正方形」であって「円」ではありません**（この周）。`teleportSpot()` は
   軸ごとに `±radius` の一様乱数を取るので、対角では `radius × √2` まで届きます。
   テストを円（`hypot`）で書いたら 500 件中 42 件が「範囲外」と出て、
   **実装ではなくテストのほうが間違っていました**（軸ごとに測る形に直しました）。
   `LOOP.md` の「落ちたときは期待値のほうも疑う」がそのまま出た例です
2. **試験場のプレイヤーは、跳ぶモブでは `HOSTILE_SIGHT`(18) より遠くに置くこと**（この周）。
   近いと「間合いを詰める側」の跳びが混ざって、殴られて跳んだのかどうかが分かりません
3. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）。
   呼び方を数えるテストでしか止められません
4. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の火球で焼け死にます
5. **相手を先に、ブロックをあとに見ること**（2-4b）。逆だと壁を背にした相手に当たりません
6. **飛ぶモブには `ACCEL_GROUND` を使うこと**（空中の加速 4 では 1 フレームに 0.07 しか戻らない）
7. **`world.seed` をセーブに使わないこと**（ネザーでは塩を混ぜたあとの値）。種は `worldSeed`
8. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）
9. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）
10. **仮の判定は「土台があるか」に掛けないこと。** 成果物に掛けること（3 度踏みました）
11. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）。同じマスに置くと、
    行き先の次元を間違えても向こうで見つかってしまいます
12. **わざと壊して、テストが赤くなるか確かめること。** この周は 5 通りとも確認済みです

## 次に取るもの

1. **2-6（エンダーアイ）**。ブレイズロッド → ブレイズパウダー → エンダーパールと合わせる
   （アイテム ID は **91**（ブレイズパウダー）と **92**（エンダーアイ）。
   `ROADMAP.md` の予約表を**取る順に上から詰める**決まりで、90 = エンダーパールまで使用済み）
2. そのあとは並びどおり 2-7 → 2-8 → … → 2-13（ドラゴン）。
   **2-12 の弓矢は、撃つ側（弓とアイテム）を足すだけで当たる側が要りません**
   （2-4b の `Shot` の `owner` と `damage` がそのまま効きます）
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 5 件**）/ `REVIEW.md` の一括確認 /
   `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **エンダーマンは中立ではありません**（本家は見つめると怒る）。ゾンビと同じ
  「暗い所に湧いて襲ってくる」側です。中立にすると `hostile` の使われ方
  （湧きの枠・逃げ方・日光・ベッドで寝られるか）を全部見直すことになるので、
  **手触りの調整のついでではなく、独立したタスクとして決めてください**
- **エンダーマンは日陰へ逃げません**（本家は日光で燃えると跳んで逃げる）。
  燃えているあいだも跳びますが、行き先の明るさは見ていないので朝には焼けます
- **エンダーマンはブロックを持ち運びません**（本家の特徴のひとつ）。
  `mobs.ts` は `world.setVoxel` を呼ばない決まりなので、入れるならその線から考え直しです
- **ネザーでも寝られます**（本家はベッドが爆発する）。`canSleep()` は時刻だけで決まります
- **寝ても時間は「進みません」**（時計だけ飛びます）。精錬・空腹・モブは dt で動きます
- **火球は火を点けません**（本家は当たった所が燃える）。火そのものが無いので、
  入れるならブロックとして足すところからです
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）。手触りの数値なので `TUNING.md` に
- **`F3` の `biome` はネザーでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）。気になるなら `spawnOn` を 1 行足せば絞れます
