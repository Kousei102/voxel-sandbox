# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-27）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 2083 件すべて成功**（2033 → 2083。この周の 50 件はボスまわり）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件です** ——
  **13 件とも本物**（実際に世界を作って動かしています）
- ブロック ID **1..63 の空きは 9 個**（この周は 1 個も使っていません）。
  向き違いは **110 まで**、**アイテム ID は 94 まで**（この周は 1 個も使っていません）
- `src/main.ts` は **1453 行 / 上限 1500（余白 47 行）**。**`main.ts` の次元の分岐は 0 件**（上限 2）

## 直前にやったこと — TASKS 2-13a（エンダードラゴン本体）

**エンドにボスが 1 体湧き、島の中心のまわりを回り、近づくと降りてきて殴ってきます。
クリスタルが生きているうちは回復し続けるので、先に柱の上を落とさないと倒せません。**
**進行が 12 / 13 → 13 / 13 になりました**（`ACHIEVED_BASELINE` も 13 に上げてあります）。

| どこ | 何が入ったか |
| --- | --- |
| `mobs.ts` | `MobDef` に **`boss` / `orbit` / `regen` / `hover`** の 4 つ。`DRAGON` の表。`BOSSES`（次元 → ボス）。`Mobs.ensureBoss()` / `chasing()` / `aimOrbit()` / `regenerate()` |
| `mobs.ts` | `Mob` に **`homeX` / `homeY` / `homeZ`**（回る中心。既定は湧いた場所） |
| `mobs.ts` | `MobContext` に **`healers`**（回復のもとの数。数えるのは呼ぶ側） |
| `main.ts` | 2 行だけ: `mobs.ensureBoss(dims.nameOf(dims.current), world)` と、`mobContext()` の `healers: liveCrystals(world).length`。**+8 行**（1445 → 1453） |
| `test/mobs.test.ts` | 「ボス（エンダードラゴン）」の節（表・召喚・消えない・回る・詰める・届く・回復・戦い） |
| `test/progression.test.ts` | 13 件目を**本物**に差し替え（エンドの島を作って戦う） |
| `test/ui.test.ts` | `routed` に 2 件（`mobs.ensureBoss(` と `liveCrystals(`） |

### 決めたこと

- **2-13 を 2 つに割りました**（`LOOP.md` の「2」）。本体（ヘッドレスで確かめられる）と、
  **体力バー・クリア画面（DOM。確かめられない側）**では確かめ方がまるで違います。
  残りは **2-13b**（`TASKS.md`）で、**出口ポータルを先に**やる並びにしてあります
- **ボスの違いは全部表の値。** `update()` にも `step()` にも「ドラゴンなら」は 1 行もありません
  （飛ぶモブ・跳ぶモブとまったく同じ形）。**`boss` は 1 つの印で 3 か所に効きます** ——
  湧きの抽選に出ない / 遠くても消えない / 数の上限で間引かれない
- **回復のもとを数えるのは `main.ts`**（`liveCrystals()`）で、**1 個につきどれだけ戻るかは表**。
  `mobs.ts` は `crystals.ts` を import していません（`onDrop` が `drops.ts` を、
  `shoot` が `projectiles.ts` を知らないのと同じ筋）
- **輪の中心は表ではなく個体（`Mob.homeX/Y/Z`）が持ちます。** 表に持たせると、
  `mobs.ts` がエンドの島の座標を知ることになります
- **`hover` を種類ごとにしました**（ブレイズ 2.5 / ドラゴン 1.2）。下の「踏んだ穴 1」

### 確かめたこと

- **わざと壊して 9 通りとも赤くなることを確かめました**（**コミットしてから壊しました**）:
  抽選にボスを残す → 2 件 / 遠いボスを消す → 6 件 / 倒しても湧き直す → 2 件 /
  中心を貼り直さない → 2 件 / もとの数を数えない → 3 件 / 高い所から降りてこない → 2 件 /
  浮きをブレイズと同じにする → 1 件 / `main.ts` の召喚を外す → 2 件 /
  `main.ts` が `healers` を渡さない → 2 件
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません
- **見た目に触れた**ので `REVIEW.md` に「2-13a」を 7 項目、数値は `TUNING.md` に 13 行、
  決まりごとは `RULES-INBOX.md` に 1 件（`rules/mobs.md` の新しい節 3 つ）

### 前の周（2-1 〜 2-12b）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージを書かない**）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ）
- **2-7**: 投げたエンダーアイが要塞の方を向く（`stronghold.ts` 新規）
- **2-8**: 要塞（石レンガ 53 + エンドポータルの部屋 + 枠 51。地面から 18 マス下）
- **2-9**: エンドポータルの起動（`endportal.ts` 新規。枠 12 個にアイ）
- **2-10**: エンド次元（`endgen.ts` 新規。エンドストーン 50 の島 1 つ）
- **2-11a**: エンドの黒曜石の柱 10 本（`endgen.ts` の `PILLARS`）
- **2-11b**: エンドクリスタル（**ブロック 54**。判断は `crystals.ts`）
- **2-12**: 弓と矢（**アイテム 93 / 94**。判断は `bow.ts`）
- **2-12b**: 右クリックの振り分けを `use.ts` へ（1499 → 1445 行）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 /
  2-8 / 2-9 / 2-10 / 2-11a / 2-11b / 2-12 / 2-13a の 18 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

0. **わざと壊す前に、必ずコミットしてから始めること**（2-11b で `git checkout -- src test` を
   使い、コミット前だったので実装が丸ごと飛びました）。この周もコミット後に 9 通り試して、
   毎回 `git checkout HEAD -- src test` で戻しています。
1. **飛ぶモブの高さは、`FLY_SCAN`(8) より上に居ると測れません**（この周）。
   追いかけるときの狙いに「その場の高さ」を下限として混ぜていたので、
   **12 マス上を回るドラゴンは詰めに来ても高いまま素通り**していました。
   床が見つかったときだけ `床 + hover` で抑え、**見つからなければ相手の高さを素直に狙う**のが
   正しい形です。**浮く高さも種類ごと**（`MobDef.hover`）—— ブレイズの 2.5 のままだと
   `ATTACK_HEIGHT`(1.5) に届かず、**目の前まで降りてきても一度も殴れないボス**でした。
2. **「近づいてきた」「降りてきた」は、始まりの状態を先に作ってから測ること**（この周）。
   プレイヤーをドラゴンの真下に置いたので**フレーム 0 で距離 0**、湧いた直後は地面に居るので
   **最初から「降りている」**で、どちらも `aimOrbit` を止めても緑のままでした。
   **先に 12 秒回して輪へ上げてから**測る形に直しています。
3. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（これまで 9 度）。
   **この周で仮は 0 件になりました。** 新しい項目を足すときは、最初から
   「実際に動かす」形で書けるところまで実装が進んでから足すこと。
4. **「判断を別ファイルへ出した」の見張りは、呼び出しの側も見ること**（2-12 / 2-12b）。
5. **`solid` でないブロックには飛び道具が当たりません**（2-11b）。
6. **未読み込みの列を「無い」と読まないこと**（5 度目がこの周の `ensureBoss`）。
   `getVoxel` は AIR を返すので、`hasColumn` を見ないと**虚空にボスを置いて落とします。**
7. **「本物の `World` で往復する」テストは、遠い所を先に読み込むこと**（2-11b）。
   進行テストのドラゴン戦も `primeAround(0.5, 0.5, 3)` で柱まで読んでいます
   （狭いと**クリスタルが最初から 0 個**に見えて、「回復で倒せない」が意味を失います）。
8. **場所の限界は「実測した最小値」ではなく「式」で決めること**（2-11a）。
9. **「面積で形を測る」判定は真円を通します**（2-10）。**形そのものを測ること。**
10. **座標の移し方は次元ごとに違います**（2-10）。
11. **「探して無ければ建てる」を別の次元に使い回さないこと**（2-10）。
12. **同じ向きが並ぶ列から中心を逆算しないこと**（2-9）。
13. **手で並べた試験場は、建てる側との食い違いを見つけられません**（2-9）。
14. **構造物の `extent` は「どこに掛かるか」の唯一の材料です**（2-8）。
15. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（2-7）。
16. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字まで。
17. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）。
18. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の矢・火球で死にます。
19. **`world.seed` をセーブや場所の計算に使わないこと**（次元では塩を混ぜたあとの値）。
    種は `worldSeed`。
20. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）。
21. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）。
22. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）。
23. **`test/audio.test.ts` が揺れます**（これまでに 3 つの判定が落ちました。どれも
    **書き出した波形がまるごと無音になる**形）。**この周は `audio.ts` も `sfx.ts` も
    1 行も触っていません。** **赤が出たらまず `npm test` をもう一度**、
    2 回続けて同じ判定が落ちたときだけ本気で見てください
    （`world.test.ts` の p99 とまったく同じ扱いです）。

## 次に取るもの

1. **2-13b（体力バー・クリア画面・出口ポータル）。上から順なので、ここが唯一の未着手です。**
   - **出口ポータルを先にやること**（`TASKS.md` に理由つきで書いてあります）。
     **建った面がそのまま「倒した印」**になるので、`ensureBoss()` が
     「もう建っているなら湧かせない」を見れば、**エンドに入り直しても湧き直さなくなります**
     （いまは湧き直します。下の「気付いているが…」の 1 つ目）
   - **進行の件数は動きません**（13 / 13 のまま）。**動かないから飛ばしてよい、では
     ありません** —— 倒しても倒したことにならない穴が残っています
   - **`main.ts` の余白は 47 行**（1453 / 上限 1500）。**足りなくなったら上限を上げず、
     2-12b と同じやり方でもう 1 つ出すこと**（次に出しやすいのは**左クリックの振り分け**）
2. 2-13b が終わると**上の並びに `[ ]` が残りません。** そのときは `TASKS.md` 末尾の
   「取るタスクが無くなったら」に従うこと（**実装もコミットもしない**）
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 9 件。うち 3 件は新しいファイル
   `.claude/rules/crystals.md` / `.claude/rules/bow.md` / `.claude/rules/use.md`。
   `paths` の行も書いてあります**）/ `REVIEW.md` の一括確認（**いま 18 節**）/
   `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **倒したドラゴンは、エンドに入り直すと湧き直します**（召喚済みの印を保存していないため）。
  **2-13b の 1 つ目がこれを塞ぎます**（出口ポータルが「倒した印」になる）。
- **ドラゴンはブレスを撃ちません**（近接だけ）。`projectiles.ts` の `breath` は表も
  当たり判定もあるので、`DRAGON.ranged` に 1 行足せば撃てます。**足すときは
  `test/mobs.test.ts` の「撃つのはブレイズだけ」を書き換えること。**
- **ドラゴンは何も落としません**（本家は経験値と卵。どちらもまだありません）。
- **ドラゴンとクリスタルを繋ぐ光の線（ビーム）はありません。** 回復しているかどうかは、
  体力バー（2-13b）が入るまで見た目では分かりません。
- **クリスタルは爆発しません**（本家は当たると爆発してプレイヤーも吹き飛ぶ）。
- **クリスタルの上に乗れます**（`solid`。矢を当てるための副作用）。
- **クリスタルは素手で 1 秒で壊せます**（柱の上まで登れば、弓が無くても壊せます）。
- **矢は拾い直せません**（本家は刺さった矢を拾えます）。刺さった矢は 60 秒で消えます。
- **弓を引いても歩く速さは変わりません／耐久値もありません／火矢もエンチャントもありません。**
- **エンドからは戻れません**（出口は 2-13b）。落ちて死ねばリスポーン地点へ戻るので
  詰みにはなりませんが、**持ち物は 5 分で消えます。**
- **エンドの島の上にはモブが 1 体も湧きません**（空の明るさが 0.7 固定なので
  `spawnLight` が 10.5 になり、敵対の上限 7 を超えます）。**掘った穴の中は暗いので湧きます**
- **エンドの島は 1 つだけです**（本家は外側にも小さい島がたくさんあります）
- **要塞は「部屋 1 つ」だけで、中に入る道がありません**（掘って降りる前提）
- **投げたエンダーアイは、近づいても落ちません／減りません**（本家は 20% で壊れる）
- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  `eyeShot()` に次元を渡して「オーバーワールド以外では null」にするのが素直です
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒。`smelting.ts` に 1 行）
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）。
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方を全部見直すことになるので、**独立したタスクとして決めてください**
- **ネザーでも寝られます**（本家はベッドが爆発する）。**寝ても時間は「進みません」**
- **火球は火を点けません**（火そのものが無いので、ブロックとして足すところから）
- **`F3` の `biome` はネザーでもエンドでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラック・ソウルサンド・要塞の部屋の中にもゾンビが湧きます**（`spawnOn` を持たないため）
