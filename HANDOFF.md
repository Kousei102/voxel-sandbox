# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-26）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。無人で回すうえで
  実際に踏んだものが 3 つあります:
  **(1)** **`.claude/rules/**` を編集すると「sensitive file」の確認で止まります。**
  → **`RULES-INBOX.md` に書いて、人が居る周が取り込む**形にしました（`LOOP.md` の「4.5」）。
  **`.claude/settings.json` に許可を書いても消えません**（2026-08-25 に 2 周とも実測）。
  **(2)** クラウドから `origin` へ push できず 403 → ユーザーが GitHub 側で権限を付け直し済み。
  **(3)** **5 時間の利用制限。** 当たった周は実装を終えた直後に止まり、次の定期実行も
  同じセッションに投げられて即終了しました（**新しい周にならず、1 回ぶんの枠が消えます**）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1911 件すべて成功**（1868 → 1911。この周の 43 件はエンドの地形と移動）
- **進行（クリア導線）達成 12 / 13**（12 件とも本物。**仮は残り 1 件** ——
  「エンダードラゴンを倒せる」だけ）
- ブロック ID **1..63 の空きは 10 個**（この周で 50 を使いました）。
  向き違いは **110 まで**（増えていません）。アイテム ID は **92 まで**（増えていません）
- `src/main.ts` は **1449 行 / 上限 1500**（余白 51 行。この周で 9 行使いました）。
  **`main.ts` の次元の分岐は 0 件**（上限 2）

## 直前にやったこと — TASKS 2-10（エンド次元）

**起動したエンドポータルの面を踏むと、エンドの島に降ります。**
**エンドからは戻れません**（本家と同じで、出口はドラゴンを倒すまで開きません。2-13）。

| どこ | 何が入ったか |
| --- | --- |
| `endgen.ts`（新規） | **エンドの地形。** 原点のまわりの島 1 つだけ + `END_SPAWN`（出る場所） |
| `blocks.ts` | **エンドストーン 50**（普通の立方体。硬さ 3 / ツルハシ / `TIER_WOOD`） |
| `dimensions.ts` | `DIMENSIONS` にエンドを 1 行（`salt` はネザーとも別の値）。`END` の「まだ生成器がありません」を落とした |
| `portaltravel.ts` | **`PortalHere` / `portalAt()` / `planTravel()` / `arriveThrough()` / `landOnGround()`**。`portalTarget()` が面の種類も受けるようになった。`PortalGate.step()` は `PortalAxis` でなく `PortalHere` を受ける |
| `main.ts` | `updatePortal` / `travelThrough` の付け替え（+9 行） |
| `test/endgen.test.ts`（新規） | 23 件 |
| `test/portaltravel.test.ts` | エンドポータルの節（+18 件） |
| `test/progression.test.ts` | 「エンドへ行ける」を**本物**に差し替え、`ACHIEVED_BASELINE` を 12 へ。ネザーの配線の確認語も新しい名前へ |
| `test/dimensions.test.ts` | 「エンドはまだ表に無い」→「エンドが表にある」。**「知らない次元」の試験は架空の `"むこう"` で**やる形に直した（エンドが表に載ったので、もう「知らない次元」ではない）+ `salt` が次元ごとに違うことの判定 |
| `test/daynight.test.ts` | 次元の一覧を `Set` で均した（エンドが 2 度出ていた） |
| `ROADMAP.md` / `CLAUDE.md` | 予約表の 50 を「実装済み」に。アイテム ID の「次に取る番号」を 84 → 93 に直した（**古いまま残っていました**） |

- **エンドは「地面のほうが例外」の次元です。** オーバーワールドもネザーも
  「どこにでも地面があって、空いている所が例外」でしたが、エンドは
  **原点のまわりの島 1 つだけが地面で、あとは全部虚空。**
  **岩盤を 1 マスも置いていません** —— 塞ぐと、落ちた人が世界の底に立ってしまい、
  `VOID_Y`(-5) が効かなくなって「落ちたら死ぬ」がまるごと消えます
- **この周のいちばんの罠: エンドへは座標を掛け算で移せません。** ネザーは 1:8 で移しますが、
  エンドの島は原点のまわりにしかないので、同じ形にすると**要塞が原点から遠い人
  （＝ほぼ全員。実測で 134〜265 マス先）が虚空に出て即死します。**
  行き先は `endgen.ts` の **`END_SPAWN`（島の中心）に固定**です
- **点（`END_SPAWN`）を `endgen.ts` に置いてあるのは、「そこが地面である」ことを
  保証しているのがあのファイルだからです。** `portaltravel.ts` に写すと、島の形を変えたときに
  片方だけ直せてしまいます
- **エンドで `arrive()` を使い回してはいけません。** あちらは「枠を探して、無ければ建てる」なので、
  **島の真ん中に黒曜石の枠が生えて、そこからネザーへ抜けられます。**
  `landOnGround()` は**何も置かず**、上から降りて最初の立てる高さを返すだけです
- **ポータルが 2 種類になったので、行き先は「いまどこか」だけでは決まりません。**
  違いは**型 1 つ（`PortalHere`）に載せて運ぶ**ので、`main.ts` の次元の分岐は **0 件のまま**です。
  見分けは `portalAt()`、行き先と目指すマスは `planTravel()`、出る場所は `arriveThrough()`
- **出る所のまわり（`LANDING_RADIUS` = 5）は上面を平らにしてあります**（本家に無い仕掛け）。
  崖の途中に出ると、出た瞬間に落ちるためです。`LANDING_RAMP`(10) マス掛けてうねりに繋ぐので、
  段差が輪になって出ることもありません
- **テストの偽陽性を 1 つ踏みました**: ふちの揺れを「半径 48 の円の面積の何 %」で見ていましたが、
  **真円でも 100% なのでそのまま通ります**（実測 108% で、上限 140% の判定は無意味でした）。
  **ふちの半径そのもの**（`reachAt`）を 360 方向で測る形に置き直しました（実測 39.1〜57.4）
- **わざと壊して 6 通りとも赤くなることを確かめました**: エンドも座標を掛け算で移す → 5 件 /
  底に岩盤を敷く → 6 件 / 出る所の平らを外す → 2 件 / エンドの面を見分けない → 2 件 /
  エンドでも `arrive()` で枠を建てる → 4 件 / `portalTarget` が面の種類を無視する → 8 件
- 次元の表を触ったので `npm run bench` を**前後 3 回ずつ**測りました:
  `generateChunk` **1.49 / 1.66 / 1.48ms**（`git stash` した前）→ **1.48 / 1.42 / 1.41ms**（後）。
  中央 1.49 → 1.42 で退行なし。**この箱は今日は遅めです**（前の周は中央 1.00 でした）。
  エンドの生成そのものは **0.116ms**（オーバーワールド 2.87ms と同じ土俵で測って）
- 決まりごとは **`RULES-INBOX.md` に 3 件**（`rules/worldgen.md` の新しい節「エンド」、
  `rules/dimensions.md` の「ポータルが 2 種類になってから」、`rules/blocks-shapes.md` に 1 段落。
  **`endgen.ts` の `paths` も書いてあります**）、
  数値は **`TUNING.md` の「エンドの島と、エンドへの移動（2-10）」（13 行）**、
  確認項目は `REVIEW.md` の「2-10」に 7 個

### 前の周（2-1 〜 2-9）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージは書かず、撃った側が
  `Shot.damage` に載せる**ので、2-12 の弓矢は撃つ側を足すだけで当たる側が要らない）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ）
- **2-7**: 投げたエンダーアイが要塞の方を向く（`stronghold.ts` 新規）
- **2-8**: 要塞（石レンガ 53 + エンドポータルの部屋 + 枠 51。地面から 18 マス下）
- **2-9**: エンドポータルの起動（`endportal.ts` 新規。枠 12 個にアイ）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 /
  2-8 / 2-9 / 2-10 の 14 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（これまで 8 度）。
   **この周も予告どおり踏みました** —— 「エンドへ行ける」の仮の判定は
   `block("エンドストーン")` だけを見ていたので、**`blocks.ts` に 1 行足した瞬間に
   達成 12 / 13 に化けました**（移動の実装より前に、です）。同じ周のうちに本物へ
   差し替えてあります。**残る仮は 1 件**（「エンダードラゴンを倒せる」）で、
   `sourceHas("src/mobs.ts", "dragon")` なので**同じ形で化けます** ——
   `MobDef` に `dragon` を書いた瞬間に 13 / 13 になります。**気を付けてください。**
2. **「面積で形を測る」判定は真円を通します**（この周）。半径 48 の円の面積と比べても、
   ふちが揺れているかは分かりません（真円でちょうど 100%）。
   **形を測るときは、形そのもの（ここでは `reachAt` の半径）を測ること。**
3. **座標の移し方は次元ごとに違います**（この周）。ネザーの 1:8 をエンドに写すと、
   遠くの要塞から入った人だけが虚空に出ます（近くから試すと動いて見えます）。
4. **「探して無ければ建てる」を別の次元に使い回さないこと**（この周）。
   エンドで `arrive()` を通すと、島の真ん中に黒曜石の枠が生えます。
5. **同じ向きが並ぶ列から中心を逆算しないこと**（2-9）。輪の 1 辺に並ぶ枠は
   全部同じ向きを向いているので、向きから中心を出すと角寄りが別の場所を指します。
6. **手で並べた試験場は、建てる側との食い違いを見つけられません**（2-9）。
7. **構造物の `extent` は「どこに掛かるか」の唯一の材料です**（2-8）。
8. **地面から潜る構造物の深さは、地形の最低高さで頭打ちになります**（2-8）。
9. **1 チャンクに収まった標本だけを見ていると、端の欠けを見逃します**（2-8）。
10. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（2-7）。
11. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字まで。
12. **探す半径は「正方形」であって「円」ではありません**（2-5）。
13. **試験場のプレイヤーは、跳ぶモブでは `HOSTILE_SIGHT`(18) より遠くに置くこと**（2-5）。
14. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）。
15. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の火球で焼け死にます。
16. **相手を先に、ブロックをあとに見ること**（2-4b）。
17. **飛ぶモブには `ACCEL_GROUND` を使うこと**。
18. **`world.seed` をセーブや場所の計算に使わないこと**（次元では塩を混ぜたあとの値）。
    種は `worldSeed`。
19. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）。
20. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）。
21. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）。
22. **わざと壊して、テストが赤くなるか確かめること。** この周は 6 通りとも確認済みです。
23. **`test/audio.test.ts` の「声色（音程の倍率）が波形に出る」が 1 度だけ落ちました**
    （2-8 の周の途中で 1 回。`0.329 > 4.161` という出方）。**その後 2 周とも再現せず**、
    実装は 1 行も触っていません。**もう一度出たら、そのときは本気で見てください**
    （`node-web-audio-api` のレンダリングが揺れているのだと思われます）。

## 次に取るもの

1. **2-11（黒曜石の柱 + エンドクリスタル）**。**島はもう建ちます。**
   - 柱は **`structures.ts` の器**に乗せるのが素直です（`fortress.ts` / `stronghold.ts` が手本）。
     ただし**エンドの柱は「島の上に決まった配置で立つ」**もので、
     グリッドに確率で撒く `SiteRule` とは性質が違います。**`endgen.ts` の中で
     直に建てるほうが簡単かもしれません**（島の中心が原点だと分かっているため）。
     どちらにするかを先に決めてください
   - **柱を出る場所（`END_SPAWN` のまわり `LANDING_RADIUS` = 5）に立てないこと** ——
     出た瞬間に柱の中に埋まります。`test/endgen.test.ts` の
     「出る所のまわりが全部立てる」がそこを見ています
   - エンドクリスタルは**ブロックではなくモブ側**にするか（`mobs.ts` の表に
     動かない個体を足す）、**新しい器**にするかで形が変わります。
     **2-12 の矢が当たる側は `mobs.ts` の `hitByProjectile()` にあります**ので、
     モブ側に乗せると**当てる仕掛けを 1 行も書かずに済みます**（そのぶん
     「湧く」「歩く」「デスポーンする」を止める必要があります）
   - **ブロック ID の空きは 1..63 に 10 個**。予約表（`ROADMAP.md`）から取ること
2. そのあとは並びどおり 2-12 → 2-13（ドラゴン）。
   **2-12 の弓矢は、撃つ側（弓とアイテム）を足すだけで当たる側が要りません**
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 3 件**）/ `REVIEW.md` の一括確認
   （**いま 14 節**）/ `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **エンドからは戻れません**（本家と同じ。出口は 2-13 でドラゴンを倒すと開く想定）。
  **落ちて死ねばリスポーン地点へ戻る**ので詰みにはなりませんが、
  **持ち物は 5 分で消えます**（虚空に落ちたぶんはその場で消えます）
- **エンドの島の上にはモブが 1 体も湧きません。** 天井が無いのでスカイライトは全開で、
  空の明るさが 0.7 固定なので `spawnLight` が 10.5 になり、敵対の上限 7 を超えます
  （受動は地面が草でないと湧きません）。**掘った穴の中は暗いので湧きます**
  （オーバーワールドの洞窟と同じ）。**本家のエンダーマンを島の上に湧かせるなら、
  `spawnOn` に `END_STONE` を足すだけでは足りません**（暗さの条件に当たります）
- **エンドの島は 1 つだけです**（本家は外側にも小さい島がたくさんあります）
- **本家の「星空」の見た目は出せません**（テクスチャが無いので暗い紫の無地）
- **要塞は「部屋 1 つ」だけです**（本家は通路・図書室・階段・シルバーフィッシュ）
- **要塞の中に入る道がありません**（掘って降りる前提）
- **投げたエンダーアイは、近づいても落ちません**（本家は近いと真下へ落ちる）
- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  `eyeShot()` に次元を渡して「オーバーワールド以外では null」にするのが素直です
- **投げたアイは地面に落ちません**（`projectiles.onExpire` で `drops` に渡すところから）
- **アイを投げても減りません**（本家は 20% で壊れる。`TUNING.md` の 2-7）
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒。`smelting.ts` に 1 行）
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方を全部見直すことになるので、**独立したタスクとして決めてください**
- **エンダーマンは日陰へ逃げません**し、**ブロックを持ち運びません**
- **ネザーでも寝られます**（本家はベッドが爆発する）
- **寝ても時間は「進みません」**（時計だけ飛びます）
- **火球は火を点けません**（火そのものが無いので、ブロックとして足すところから）
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）
- **`F3` の `biome` はネザーでもエンドでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）
- **要塞の部屋の中にも敵対モブは湧きます**（石レンガは `spawnOn` を持たないため）
