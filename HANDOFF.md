# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-13・AUTODEV 79 のあと）

- `npm test` **3533 件すべて成功**（**+27**）。`npm run typecheck` 緑・**`npm run build` 緑**。
  **`npm run bench` は走らせていません**（生成もメッシュ化も触っていないため）
- **音の 1 件がこの周も 1 度だけ跳ねました**（`rules/testing.md` の既知の揺れ）。
  走り直して**2 回続けて緑**。退行ではありません
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1450 行**（テストの数え方。**±0。1 文字も開いていません**）
- **アイテム 127 種・`MAX_ITEM_ID` 165・111..255 の空きは 90**（**−2**）・
  **1..63 の空きは 9**（±0。55..63）
- **立方体 41 / 非立方体 81**（**+2**）・**モブ 10 種類**（敵対 5）・**レシピ 66 本**（±0）・
  **`FUEL` 9 行 / `SMELTING` 9 行**（±0）・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 済`（30a・苗木）**・**`AUTODEV-QUEUE.md` 96 行（未着手 8 件）**

## この周でやったこと（AUTODEV 79 の C・クラウドの無人の周）

**判定表は 3 行目**（未着手 9 件で 1 行目は外れ・`AUTODEV-SPEC.md` が `状態: 未着手`）で
**C（実装）の周**。取ったのは**キューの先頭・30a（苗木 2 種）**で、**ブロック 164 / 165**。

**オークとトウヒの苗木が葉から 5% で落ち、土・草・耕地の上にだけ立って、掘れば戻ります。**
**まだ育ちません**（木になるのは 30b。`crops.ts` も `worldgen.ts` も ±0 行）。
**`main.ts` は 1 文字も開いていません**（見込みどおり 0 行）。

**サブエージェントは 1 つも立てていません。** 仕様書が「どのファイルに何行」まで
書いてあり、**同じ番の中で結果が返る確証が無かった**ので、`AUTODEV.md` の C-1 の
「返らないなら自分で実装すること」に従いました。点検（C-2）も自分で `git diff` を
読み、`typecheck` / `test` / `build` を**自分で走らせています**。

**この周のいちばん厄介なところは葉の 2 山目です。** オークの葉は 1 山目（棒 10%）と
2 山目（リンゴ 0.5%）が**両方とも埋まっていて**、苗木は 3 山目になるはずでした。
**乱数を 3 本目に増やすと `breaking.ts` と `main.ts` に及ぶ**ので、`Drop.extra` を
**候補 2 件まで取れる形**（`ExtraDrop | readonly [ExtraDrop, ExtraDrop]`）にし、
`rollDrops()` が**`extraRoll` を上から順に帯で切って 1 件だけ**選ぶようにしました
（リンゴ 0..0.005 → 苗木 0.005..0.055）。**乱数は 2 本のまま・山は最大 2 つのまま・
`breaking.ts` も `main.ts` も ±0 行**です。代わりに**リンゴと苗木は同時に落ちません**
（本家は独立。`TUNING.md` に 1 行）。

**「土の上だけ」は `supportsBlock()` の 1 行**（`needsSoil(id) && !isSoil(supporter)`）。
`supportsBlock()` は置く側（`canPlaceAt`）と壊す側（`breakUnsupported`）の**両方が通る**
ので、**「土の上にしか置けない」と「真下の土を掘ったら落ちる」が 1 行で両方**そろい、
**`placing.ts` も `world.ts` も ±0 行**でした。**`canSupport()` は 1 文字も触っていません**
（あれは壁掛けの松明とベッドの足場）。置けない理由の文（`supportHint()`）も表から出して
**「土か草の上」**にしてあります（「床か壁」のままだと嘘になります）。

**差し戻し 0 回。** 書きながら踏んだ 3 つはその場で直しました（`docs/autodev-log.md`）:
既存のテストが `spruce.length === 1` で測っていた件（**ゆるめず「どの山もリンゴでない」に
置き換え**）/ `drop.extra.item` と直に書いていた既存の判定が `tsc` で落ちた件
（`extraDrops()` を通す形に直し、**「帯の合計が 1 を超えない」を 1 件足した**）/
`MAX_ITEM_ID === CHARCOAL` の TS2367（**比べる相手を数え直した**）。

## 次にやること

**次は B（仕様）の周** —— 未着手が 8 件なので 1 行目は外れ、**`AUTODEV-SPEC.md` が
`状態: 済`** なので 2 行目に当たります。

- 取るのは**キューの先頭・30b「植えた苗木が木に育つ」**（**ID 0 個**）。
  **⚠ `worldgen.ts` の `stampTree()` はチャンクの `Uint8Array` に直に書くので、
  `setVoxel` の側からはそのまま呼べません** —— 幹と葉を `setVoxel` で組む道が要り、
  **列をまたぐぶんは書けなかったら持ち越す**（`growCane()` と同じ作法）
- **`AUTODEV-SPEC.md` は `wc -l` で 119 行以下に収めること**（`test/docs.test.ts` は
  `split("\n").length` で数えるので +1 されます。`rules/testing.md`）
- **共有帯の次の空きは 166 から**（`ROADMAP.md` の予約表）

## ブラウザで見てほしいところ

**この周は 2 枚撮って、どちらも `Read` で開いて見ています。**

- **`shots/sapling.png`**（`tools/shot.ts` に `sapling` の場面を足しました）:
  **十字の板 2 枚が立っていて、面の欠けも裏返りもありません。** 草の上にオークと
  トウヒを並べ、土の上と耕地の上にも 1 本ずつ、比べる草むら・小麦の苗・葉を隣に
  置いてあります。**明るい黄緑（オーク）と暗い青緑（トウヒ）は並べても分かれます。**
  → **人に見てほしいのは「若い木に見えるか」**だけです（草むらと同じ形なので）
- **`docs/browser-shots/creative-sapling.png`**（本物のブラウザ）: 一覧の末尾に
  **オークの苗木 / トウヒの苗木**が並び、`.swatch` は `rgb(127,191,95)` と
  `rgb(47,127,90)`。**console のエラー 0 件**
- **⚠ 写った既知の件: 6 文字の「オークの苗木」「トウヒの苗木」が 2 行に折れて枠の絵に
  被ります**（`style.css` の `.slot .label`。「革のズボン」で前から積んである件で、
  **6 文字は今までで一番長く、被りも一番大きい**です）。**人の判断待ち**なので
  直していません
- **⚠ 苗木は植えてもいつまでも育ちません**（30b がまだ）。**遊ぶ人には「壊れている」
  ように見えます** —— 30b を早めに取るか、そこは承知で進めるかは人の判断です

**持ち越し（まだ誰も見ていないもの）**:

- **木炭（29）**: **一覧で「炭」に見えるか**（`0x56473f`。いちばん近いのは石炭鉱石と
  ソウルサンドで 24.1・石炭からは 64.0）
- **道具の修理（28）**: **傷んだ道具 2 本の「傷の帯」で「どれだけ直るか」が読めるか** /
  **半分減った道具 2 本が「新品に戻る」のが気前よすぎないか**（本家のままの式）
- **スケルトン（11）**: **矢が当たる高さ**（10m で足元から 0.27m）/ **2 秒に 1 発・重み 3** /
  **間合いを取らず近づいて殴ってくる**のが変に見えないか / **骨色 `0xcdc8b0`**
- **防具（27b）**: 防具枠の置き場所 / **革一式（防具点 7）で 28% 減が「固くなった」と
  感じられるか** / **革 24 枚（＝牛 24 頭）で一式**が見合う値段か
- **フェンス（26a / 26b）**: **「柵らしく見えるか」**。**⚠ 52fps 未満で跳び越せる件**は
  残っています（余裕 0.0117。**数値では決められないので人の判断**）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2` はガラスから 34.3）
- **氷（25a）**: **滑りの手ざわり**（離してから 1 秒で 0.59m。土は 0.099m）。
  伸ばすなら **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴ `0xf0a800` の見分け / 本棚が「本の詰まった棚」に
  見えるか / リンゴの色 / 葉 200 枚に 1 個 / はしごの登り 2.35 m/s / `CANE_GROW_SECONDS = 180` /
  **自然に生えたサトウキビは伸びません** / 砂糖の見分け / ボウルとシチューの見分け /
  森のキノコの密度 / サボテンの痛さ / 牛を搾る・ミルクで毒が止まる / クモの速さ 6.0 /
  投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件**・どちらも `rules/` へ据えました）

- **`rules/items-survival.md`**: **2 山目の候補が 3 つ目に増えたら、乱数ではなく
  `extraRoll` の帯で分けること。** 候補は**2 件まで**（タプル）で、**帯の合計が 1 を
  超えないこと**（超えると後ろの候補が永久に出ません。型でも止まりません）。
  **`drop.extra.item` と直に書かず `extraDrops(drop)` を通すこと。**
  帯なので**2 件が同時には落ちません** —— そこを本家に寄せるのは「乱数を 1 本流す」話
- **`rules/blocks-shapes.md`**: **`supportsBlock()` は「広げる」だけの場所ではなく、
  狭める例外もここ**（苗木の `needsSoil`）。**どちらも表 1 本に聞くこと**、
  **`canSupport()` を触って済ませないこと**、**置けない理由の文（`supportHint()`）も
  一緒に直すこと**

**`RULES-INBOX.md` には 1 行も足していません**（層 3 の話は 0 件）。

## 人がやること

- **⚠ 設計の判断（51 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**で、**`AUTODEV.md` の「止まって人を呼ぶ条件」の 2 番にちょうど
  並んでいます**（ハードの上限は 1500 行）。**待っているのは 2 件**（**35 骨粉**・
  `use.ts` の 19 通り目 / **24b ケーキをかじる**・最低 7 行）。**キューの上 4 件
  （30b..34）はどれも `main.ts` 0 行の見込み**なので、判断が下りなくても 4 周は
  止まりません。`test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **⚠ 苗木が育たない件**（上の節）。**30b を先に取るかどうか**は人の判断です
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）。
  **6 文字の苗木 2 種でいちばん大きく出ました**
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**。足すなら `ARMORS` に回数の
  列と 5 つ目の減り方が要り、**それだけで 1 周**（修理の側は 0 行で待っています）
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 79 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押して `#creative` の
`scrollTop` を末尾まで送ってから撮ること**（`docs/browser-shots/README.md` の末尾）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンスと同じで、**`tools/shot.ts` に場面を 1 つ足すのが
いちばん安い**（1〜2 秒で撮れて、`browsershot.mjs` を書き換えずに済みます）。
