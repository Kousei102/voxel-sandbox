# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- **この周は B（仕様）**。**`src/**` と `test/**` に 1 行も触っていません**（`git diff` で確認済み）
- `npm run typecheck` / **`npm test` 2707 件すべて成功**（前の周と同じ数。`src` を触っていないので増減なし）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1444 行 / 上限 1500**（**1450 を超えると停止条件**。残りは 6 行）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 133**（この周は 1 個も使っていません）
- **アイテムは 88 種**、`MAX_ITEM_ID` は **122**
- **`AUTODEV-SPEC.md` は `状態: 未着手`** → **次の周は C（実装）**（下の「次にやること」）
- **`RULES-INBOX.md` は未取り込み 11 件**（前の 3 周ぶんの 10 件 + **この周の 1 件**）

## この周でやったこと（無人・B の周 1 種類だけ）

判定表どおり（キューの未着手が 3 件以上・`AUTODEV-SPEC.md` が `状態: 済`）→ **B の周**。
**キューの先頭「13. 育つ器（`crops.ts`）と小麦・パン」を仕様書 1 枚にしました。**

**120 行に収まらなかったので、その場で 3 件に割りました**（B の決まり）。仕様書にしたのは
**前半だけ**で、残り 2 件は `AUTODEV-QUEUE.md` の 14 番・15 番として書き戻してあります。

- **13（仕様書）**: 育つ器（`crops.ts`）と**実った小麦**。苗が `GROW_SECONDS`（暫定 180 秒）で
  実った小麦（ブロック 123）に差し替わり、掘ると小麦（アイテム 124）が 1 個。育ち具合は
  **位置ごとの状態**（`furnaces.ts` の器の形）で持ち、**`SaveData` に省略可キー `crops` が 1 つ**増える
  （**`version` は 1 のまま**なので `AUTODEV.md` の停止条件 5 には当たりません）
- **14（書き戻し）**: **収穫で種も戻る**。いまの `Drop` は 1 ブロックにつき 1 山しか落とせません
  （`item` / `count` / `chance` / `otherwise` の 4 つだけ）。**だから 13 では種が戻らず**、
  畑は種を食いつぶします —— `chance` で小麦と種を分けると平均で種が減るので、
  **「1 回の採掘で 2 山落ちる器」を先に入れる**ほうが正しいと判断しました
- **15（書き戻し）**: パン（小麦 3 → パン）と食べること。レシピ 1 本と `FOODS` に 1 行

**次の周が最初に見るべき所**: **`main.ts` の残りは 6 行しかありません。** 仕様書の 5-4 に
配線を 6 行ぶん並べてあります（`collectState({ ... })` と `forgetEverything({ ... })` は
**中括弧に `crops` を足すだけで行が増えません**）。**それでも 1450 行を超えるなら、
無理に詰めずに `AUTODEV.md` の停止条件 3 として止まってください。**

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は `src/**` を 1 行も触っていないので、撮っていません**（C-3 は実装の周の手順）。
前の周に撮った 11 枚は `docs/browser-shots/` にそのまま残っています。

**次の周（C）で撮ってほしいもの**: **実った小麦（`0xd8c26a`）と苗（`0x6f8f3f`）が並んだ絵**。
`tools/shot.ts` の `SCENES` に場面を 1 つ足せば `npm run shot -- crops` で 1〜2 秒で撮れます
（**180 秒待たずに済みます**）。**苗の緑が草原に紛れる件は前の周からの持ち越し**なので、
**実った黄色と並べて 2 色まとめて決め直すのはこの周が好機**です（`TUNING.md` の「小麦の種と苗」）。

## 人がやること

- **`RULES-INBOX.md` の 11 件の取り込み**。**わざと食い違わせてある組**（「3 種類」対「4 種類」/
  「12 通り」対「**14 通り**」/ 「未取り込み 10 件」対「**11 件**」）は **新しいほうを採ってください**
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**はまだ直っていません
  （`#status` は `bottom: 118px` 固定。`style.css:674`）。`REVIEW.md` に積んであります
- **`GROW_SECONDS` 180 秒でよいか**（本家は 8 段階・明るさ 9 以上で平均 20 分前後。ここは
  2 段階で、**チャンクが読み込まれている間しか育ちません**）。実装の周が `TUNING.md` に 1 行足します
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手は 8 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 未着手`**
なので → **次は C（実装）の周**です。**仕様書を全文そのまま**（要約せずに）実装してください。

- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **使うスキルは `add-stateful-block`**（かまど・チェストと同じ器の形。仕様書の 4 に書いてあります）
- **点検（C-2）は自分で `git diff` を全部読むこと。** 前の 2 周とも、サブエージェントは
  `run_in_background: false` を渡しても背景で起動し、**13 分待たされました**。
  `AUTODEV.md` の C-1 は「同じ番の中で返らないなら自分で実装すること」です
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）

**音のテスト（`test/audio.test.ts`）の揺れは、この周も出ていません**（緑）。
赤が出たら、まずもう一度走らせること（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
