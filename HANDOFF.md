# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-25）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。無人で回すうえで
  実際に踏んだものが 3 つあります:
  **(1)** **`.claude/rules/**` を編集すると「sensitive file」の確認で止まります。**
  → **`RULES-INBOX.md` に書いて、人が居る周が取り込む**形にしました（`LOOP.md` の「4.5」）。
  **`.claude/settings.json` に許可を書いても消えません**（2026-08-25 に 2 周とも実測）。
  **(2)** クラウドから `origin` へ push できず 403 → ユーザーが GitHub 側で権限を付け直し済み。
  **(3)** **5 時間の利用制限。** 当たった周は実装を終えた直後に止まり、次の定期実行も
  同じセッションに投げられて即終了しました（**新しい周にならず、1 回ぶんの枠が消えます**）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1808 件すべて成功**（1783 → 1808。この周の 25 件は要塞の方角）
- **進行（クリア導線）達成 10 / 13**（10 件とも本物）。**この周で 1 つ増えました**
  —— 「投げたエンダーアイが要塞の方を向く」が本物になりました
- ブロック ID **1..63 の空きは 14 個**（この周はブロックを足していません）。
  アイテム ID は **92 まで**（この周はアイテムも足していません）
- `src/main.ts` は **1413 行 / 上限 1500**（余白 87 行。この周で 13 行増えました）

## 直前にやったこと — TASKS 2-7（投げたエンダーアイが要塞の方へ飛ぶ）

**要塞はまだ 1 個も建ちません**（建てるのは 2-8）。この周で決めたのは
**「どこに建つか」の規則と、投げたアイがどちらを向くか**だけです。

| どこ | 何が入ったか |
| --- | --- |
| `stronghold.ts`（新規） | **判断は全部ここ。** 場所の規則・いちばん近い要塞・向き・投げる注文 |
| `structures.ts` | **`SiteRule` と `siteAt()` を切り出した**（`StructureDef extends SiteRule`）。`cellSize` / `cellOf` も |
| `main.ts` | 13 行。エンダーアイを持って右クリック → `eyeShot()` を `projectiles.fire()` へ |
| `test/stronghold.test.ts`（新規） | 25 件 |
| `test/ui.test.ts` | `routed` に「エンダーアイの向き」（`eyeShot(`）を追加 |
| `test/progression.test.ts` | 10 番目の probe を本物に。`ACHIEVED_BASELINE` 9 → 10 |

- **この周でいちばん大事なのは `siteAt()` の切り出しです。** 建つ場所を決める規則
  （`spacing` / `chance` / `salt` と、マスの中で散らすハッシュ）が **1 か所**になったので、
  **2-8 は `STRONGHOLD_SITE` を広げて `StructureDef` にするだけ**で、アイの指す先と
  食い違いようがありません。**写して 2 か所に持つと、指した方角に何も無い**という形で
  静かに壊れます（しかも地面の下なので、掘るまで気付けません）。
  テストは**両向き**に見ています —— 器が列挙した場所へ立てばアイがそこを指すか、
  アイが指した場所を器も列挙するか
- **`stronghold.ts` は `projectiles.ts` を型でしか import しません**（`mobs.ts` と同じ作法）。
  持っているのは「どこへ向けて投げるか」という注文（`Shot`）までで、飛び方は
  `PROJECTILE_KINDS` の `eye` の行（1-8 で入れた。速さ 8・重力 0・壁を抜ける `pass`・寿命 12 秒）
- **種は `worldSeed`。** `world.seed` はネザーだと塩を混ぜたあとの値なので、渡すと
  **ネザーで投げたアイだけ別の場所を指します**（HANDOFF の穴 9 と同じ罠）
- **アイは投げても減りません**（本家は 20% で壊れる）。手触りの数値なので `TUNING.md` に
  置いて進めました —— 落ちたアイを拾い直す仕掛けが無いので、減らすと**枠 12 個ぶんを
  集めきる前に探索でアイを使い切る詰み**が作れます
- **わざと壊して 5 通りとも赤くなることを確かめました**: 探す広さを 1 に → 1 件 /
  投げる向きを固定に → 3 件（進行の後戻りを含む）/ 場所を別の種で持つ（＝写した状態）→
  4 件 / 上向きを 0 に → 1 件 / `main.ts` の配線を外す → 2 件
- `structures.ts` はチャンク生成の中なので、`npm run bench` を**交互に 3 回ずつ**測りました:
  `generateChunk` 1.46 / 1.45 / 1.46ms → **1.43 / 1.42 / 1.40ms**（退行なし）
- 決まりごとは **`RULES-INBOX.md` に 2 件**（`rules/worldgen.md` の構造物の節へ 3 行と、
  「要塞とエンダーアイ（`stronghold.ts`）」の新しい節）、数値は **`TUNING.md` に
  「要塞の遠さとエンダーアイ（2-7）」の節（6 行）**、確認項目は `REVIEW.md` の「2-7」に 7 個

### 前の周（2-1 / 2-2 / 2-3 / 2-3b / 2-3c / 2-4a / 2-4b / 2-4c / 2-5 / 2-6）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージは書かず、撃った側が
  `Shot.damage` に載せる**ので、2-12 の弓矢は撃つ側を足すだけで当たる側が要らない）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ。
  **クリア導線の 2 本がここで合流する**）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 の
  11 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（いまや 6 度目です）。
   この周の「投げたエンダーアイが要塞の方を向く」も、元は
   `anySourceHas("strongholdDirection")` = その名前の関数がどこかにあるか、でした。
   **関数を 1 つ書いた瞬間に達成へ化けます。** `structures.ts` / `dimensions.ts` /
   `projectiles.ts` / ブレイズ / エンダーアイのレシピと、同じ形で並んでいます。
   `anySourceHas()` はこの周で使い手が居なくなったので消しました（残りの仮 3 件は
   ブロックの有無と `sourceHas()`）
2. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（この周）。
   `STRONGHOLD_SEARCH` を 2 → 1 に落としても、手で選んだ 6 箇所でも面舐め 1600 箇所でも
   **総当りと同じ答えが返って緑のまま通りました**（取りこぼすのはマスの角のごく狭い所だけ）。
   **落ちない壊し方を見つけたら、判定のほうを理屈に置き直すこと** ——
   いまは「探す広さがマスの対角の最悪（`√2 × マス`）を超えている」を直に見ています
3. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字を超えると
   盤面のスロットに重なります。**「ブレイズパウダー x2」= 9.5 でいまの最長**です
4. **探す半径は「正方形」であって「円」ではありません**（2-5）
5. **試験場のプレイヤーは、跳ぶモブでは `HOSTILE_SIGHT`(18) より遠くに置くこと**（2-5）
6. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）
7. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の火球で焼け死にます
8. **相手を先に、ブロックをあとに見ること**（2-4b）。逆だと壁を背にした相手に当たりません
9. **飛ぶモブには `ACCEL_GROUND` を使うこと**（空中の加速 4 では 1 フレームに 0.07 しか戻らない）
10. **`world.seed` をセーブや場所の計算に使わないこと**（ネザーでは塩を混ぜたあとの値）。
    種は `worldSeed`。**この周のエンダーアイも同じ罠を通ります**
11. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）
12. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）
13. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）
14. **わざと壊して、テストが赤くなるか確かめること。** この周は 5 通りとも確認済みです

## 次に取るもの

1. **2-8（要塞。石レンガ 53 + エンドポータル部屋 + 枠 51）**。**場所はもう決まっています** ——
   `stronghold.ts` の **`STRONGHOLD_SITE` をそのまま広げて `StructureDef` にすること**
   （`{ ...STRONGHOLD_SITE, name, extent, lift, build }`）。**写さないでください** ——
   写すとアイの指す先に何も建ちません。`test/stronghold.test.ts` の
   「器が建てる場所と、アイが指す場所が一致する」が両向きに見張っています。
   - **`lift` を負にして地面の下に埋めること**（Minecraft の要塞は地下）。器は
     `ground(x, z) + (lift ?? 1)` を基準点にするので、`lift: -20` のように渡せば済みます
   - **`extent` を小さく申告しないこと**（`build()` がそこより外へ書いても端が黙って欠けます）
   - ID は `ROADMAP.md` の予約表から（石レンガ 53 / エンドポータル枠 51）
2. そのあとは並びどおり 2-9 → 2-10 → … → 2-13（ドラゴン）。
   **2-12 の弓矢は、撃つ側（弓とアイテム）を足すだけで当たる側が要りません**
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 8 件**）/ `REVIEW.md` の一括確認
   （**いま 11 節**）/ `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  次元を見ていないので当然の結果ですが、本家のネザーでの挙動とは違います。
  **`main.ts` に次元の分岐を足さずに直すなら**、`eyeShot()` に次元を渡して
  「オーバーワールド以外では null（＝『要塞の見当が付きません』）」にする形が素直です
- **投げたアイは地面に落ちません**（本家は 12 マスほど飛んで落ち、拾い直せます）。
  拾い直す仕掛けを入れるなら、`projectiles.onExpire` で `drops` に渡すところからです
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒燃えます）。
  `smelting.ts` の燃料表に 1 行足すだけです
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）。`projectiles.ts` に
  `eye` と同じ形で足せますが、着地でプレイヤーを動かす判断がどこに要るかは未検討です
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方（湧きの枠・逃げ方・日光・ベッドで寝られるか）を全部見直すことになるので、
  **手触りの調整のついでではなく、独立したタスクとして決めてください**
- **エンダーマンは日陰へ逃げません**し、**ブロックを持ち運びません**
- **ネザーでも寝られます**（本家はベッドが爆発する）。`canSleep()` は時刻だけで決まります
- **寝ても時間は「進みません」**（時計だけ飛びます）。精錬・空腹・モブは dt で動きます
- **火球は火を点けません**（本家は当たった所が燃える）。火そのものが無いので、
  入れるならブロックとして足すところからです
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）。手触りの数値なので `TUNING.md` に
- **`F3` の `biome` はネザーでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）。気になるなら `spawnOn` を 1 行足せば絞れます
