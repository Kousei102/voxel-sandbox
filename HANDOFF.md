# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` は通ります。**`npm test` 2488 件すべて成功**（**前の周から +42** ——
  チェストとかまどが傷を運ぶぶん）。`npm run build` も通ります
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1383 行 / 上限 1500**（**この周で足したのは 0 行** —— 配線 3 行を
  書き換えただけ。前の `HANDOFF.md` の「1382 行」は数え違いで、前の周から動いていません）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（**変わっていません** —— ID は 0 個）
- **`AUTODEV-SPEC.md` は `状態: 済`**、`AUTODEV-QUEUE.md` の未着手は **8 件**
  → **次の周は B（仕様）**
- **`RULES-INBOX.md` に未取り込みが 7 件**（`mobs.md` / `projectiles.md` /
  `items-survival.md` x 2 / `inventory-screen.md` / `drops.md` / **`stateful-blocks.md`**）。
  **取り込むのは人が居る周**

## この周でやったこと（無人・C の周 1 種類だけ）

**`AUTODEV-SPEC.md` の 3c-2（チェストとかまどが傷を運ぶ）を実装しました。** コミットは 1 つ。

**これで傷が消える経路は 1 つも残っていません。** 器へ入れる（つまんで置く・シフトクリック）・
出す・器を壊す・読み込み直す・次元をまたぐ、のどれを通っても傷が付いて回ります。

- セーブは **`chests` の 54 要素と `furnaces` の 9 要素を 1 つも増やさず**、省略可キーを
  2 つ足しました（**`chestWear` は 1 台 27 要素・`furnaceWear` は 3 要素**。キーは
  `"x,y,z"` で本体と同じ）。**全部新品ならキーごと出ない**ので、道具を器に入れていない人の
  セーブは 1 バイトも変わりません。**`SaveData.version` は 1 のまま**
- **`durability.ts` は 1 行も触っていません**（仕様書の「触らないもの」）。器は
  `damageOf` / `carryWear` / `serializeWear` / `deserializeWear` の 4 本を**呼ぶだけ**で、
  丸め（`wornValue()`）も写していません
- **`breaking.ts` の `Burst` に `damage?` を足して素通しに**しました。器が返した値を
  そのまま `drops.burst()` へ渡すだけで、`breaking.ts` も `main.ts` も「道具かどうか」を
  知りません
- **仕様書の完了条件のうち 1 つだけ、いまの表では道具で届きません**:
  「シフトクリックでかまどの材料枠へ入れると傷が残る」—— **焼けるものにも燃料にも
  道具が 1 本も無い**ので、`quickMove` はかまどを素通りします。`moveInto()` に
  `carryWear()` は仕様どおり入れたうえで、**「いま道具は焼けも燃えもしない」ことを値で出し、
  呼び出しの形を見張る**という 2 本立てにしました（`test/smelting.test.ts`）

## ブラウザで見てほしいところ（見た目に出るもの）

**この周のぶんは 2 件です**（帯そのものは前の周までのもので、新しい見た目はありません）:

- **器へ入れて出す** —— 傷んだ道具（木のツルハシで石を何回か掘ったもの）をチェストに
  **つまんで入れる／シフトクリックで入れる**。**枠の下の細い帯が付いたままか。**
  出したあと、ブラウザを閉じて開き直しても付いたままか（かまどの材料の枠でも同じこと）
- **器を壊す** —— 傷んだ道具を入れたままチェスト（とかまど）を壊す。
  **地面に落ちたものを拾い直しても帯が付いたままか**

**前の周までの持ち越しが 6 件、まだ人の目を通っていません:**

- **落とし物が傷を運ぶ周のぶん**（`6e8ec84`）—— 傷んだ道具を `Q` で捨てて拾い直す・
  持ったまま死んで拾い直す。どちらも**枠の下の細い帯**が付いたままか
- **掴んで動かしても傷が残る周のぶん**（`61657d5`）—— 収納へ動かしても帯が付いて回るか。
  **クリエイティブの一覧（`E`）から出した道具には帯が出ていないこと**
- **道具の耐久値の周のぶん**（`8a780e9`）—— 木のツルハシで石を掘ると帯が出る（減るほど
  短く・赤く）。使い切ると **「木のツルハシ が壊れました」** が 1 行出るか
- **大きいチェストの周のぶん**（`a795564`）—— 2 個並べて開くと **54 枠が 9 列 x 6 段**、
  単体だと **27 枠**、3 個横一列ならどれも 27 枠に戻るか
- **砂・砂利が落ちる周のぶん**（`6209e91`）—— 砂の柱の下を掘ると上が詰まり、
  **穴や浮いた砂が残らないこと**
- **メッシュ化の詰め方を変えた周のぶん**（`0c68545`）—— **面が欠けていないか**と
  **AO の陰影が前と同じか**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
未着手のキューは **8 件**（3 件以上なので A ではない）、**`AUTODEV-SPEC.md` は `状態: 済`**
→ **次は B（仕様）の周**です。

- **取るのは `AUTODEV-QUEUE.md` の先頭 `3d`**（火打石と打ち金・弓の耐久値）。
  **取る前にコードで数え直すこと**（B の決まり）—— `durability.ts` に
  `wearForUse()` を足す形になるはずですが、`items.ts` の「耐久値がまだ無い」という
  コメントが本当に 2 か所だけかは数えてから書くこと
- **7 項目を 1 つも欠かさないこと**（欠けたぶんは、そのままサブエージェントの勝手な判断に
  なって返ってきます）。**`src/**` と `test/**` には 1 行も触らないこと**（B の完了条件）

人がやること:

- 上の**ブラウザ確認 8 件**（この周のぶんが 2 件・持ち越しが 6 件）
- `RULES-INBOX.md` の **7 件**の取り込み（**`drops.md` / `items-survival.md` /
  `stateful-blocks.md` のぶんは `paths` に `src/durability.ts` を足す指示付き**）
- **`src/durability.ts` の先頭のコメントを直すこと。** 「チェストとかまどの中身は
  まだ `[item, count, ...]` のまま」と書いてありますが、**この周で嘘になりました。**
  仕様書が `durability.ts` を「1 行も触らないもの」に挙げていたので直していません
  （`TUNING.md` の 317 行目のほうは書き直してあります）
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）の揺れは、この周は一度も出ていません**（8 回以上
走らせて全部緑。前の周は 6 回中 2 回）。この周は `audio.ts` / `sfx.ts` に 1 行も
触っていないので、**揺れが直ったわけではありません。** 赤が出たら、まずもう一度
走らせること（停止条件は「2 周続けて赤い」で、1 回の赤では止まりません）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
