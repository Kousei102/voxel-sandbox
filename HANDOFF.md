# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-15・AUTODEV 87（C の周）のあと）

**この周は C（実装）の周**で、**32b「レンガ（アイテム 170）」**を入れました。

- **粘土玉（169）をかまどで焼くとレンガ 1 個**（`smelting.ts` の `SMELTING` に 1 行。
  **木炭とまったく同じ形**）。**レンガ 4 個の 2x2 でレンガブロック**（`crafting.ts` に
  1 行。**作業台は要りません** —— 雪玉・粘土玉と同じ形）
- **これで、置けるのに作れなかった `BRICK`(12) がサバイバルで初めて手に入ります**
  （粘土 1 個 → 粘土玉 4 → レンガ 4 → レンガブロック 1 個。`test/crafting.test.ts` が
  **1 本の道として出力してから**見張っています）
- **⚠ 表示名を 1 つ直しました: `BRICK`(12) が「レンガ」→「レンガブロック」。**
  **ID は 12 のまま**なので、**既存のセーブは 1 バイトも動きません**（セーブに入るのは番号）。
  直したのは `blocks.ts` の `def()` の文字列 1 つだけです
- **`main.ts` も `*render.ts` も `ui.ts` も `inventoryui.ts` も 1 文字も触っていません。**
  **`FUEL` にも `BRICK` の `DROPS` にも 1 行も足していません**（どちらも燃料ではなく、
  レンガブロックは今までどおり壊すと自分が 1 個落ちます）
- **サブエージェントは立てていません**（`AUTODEV.md` の C-1 の「同じ番の中で結果が
  返らないなら自分で実装すること」）。**点検（C-2）も親が `git diff` を読んでやりました**

## 数字（2026-09-15・この周で測ったもの）

- `npm test` **3636 件すべて成功**（+15）。`npm run typecheck` 緑・`npm run build` 緑
  - **⚠ 音が 1 回跳ねました**（「声色（音程の倍率）が波形に出る」で**低い声 0.162 →
    3.898**）。`rules/testing.md` の既知の揺れで、**走り直したら 2 回続けて緑**。
    **判定はゆるめていません**
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行 / 上限 1500**（±0。**1 文字も開いていません**）
- **アイテム 132 種**（+1）・**`MAX_ITEM_ID` 170**・**レシピ 70 本**（+1）・
  **焼けるものの表 10 行**（+1）・**燃料の表 9 行**（±0）・**立方体 42 / 非立方体 85**（±0）
- **1..63 の空きは 7**（57..63）・**111..255 の空きは 85**（**次に取るのは 171**）
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 未着手 5 件**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が `状態: 済` なので判定表の 2 行目です
（未着手 5 件なので 1 行目は外れます）。**取るのは `AUTODEV-QUEUE.md` の先頭の 1 件だけ。**

**先頭はいま 33「鉄・金・ダイヤの防具 12 部位」**（ID 12 個）。B の周の宿題:

- **ID は `ROADMAP.md` の予約表から 171..182 の 12 個**（**片側だけ見て取らないこと**。
  取ると **111..255 の空きが 85 → 73**。止まる目安の 10 個までは余裕があります）
- **一覧の色の隔たりを 12 色ぶん測ること。** 革 4 部位が `0x8c5a33`〜`0xb16e51` の
  赤茶で、**この帯はレンガ（170）でさらに混みました** —— 鉄・金の 8 部位は
  灰と黄の帯へ、ダイヤは水色へ逃がすことになります（`TUNING.md` に節を 1 つ）
- **`items.ts` の `ARMORS` に 12 行とレシピ 12 本だけ**の見込みで、**`main.ts` は 0 行**
  （防具枠の配線は 27b で済んでいます）。**点数もレシピも本家の値**を暫定で入れること
- **⚠ 名前が長くなります**（「ダイヤの上着」など）—— 下の「折れて枠に被る件」に直撃します

## ブラウザで見てほしいところ

**この周（C）は本物のブラウザで 2 枚撮り、両方 `Read` で開いて見ました**（`AUTODEV.md` の C-3。
**`npm run shot` は撮っていません** —— **新しいブロックが 0 個**なので、どの場面にも
1 マスも増えません）。詳しくは `docs/browser-shots/README.md`:

- **`docs/browser-shots/creative-brick.png`**（一覧の末尾）。**枠 131 → 132**・
  **末尾が 粘土 → 粘土玉 → レンガ**・`.swatch` は指定どおり `rgb(188,93,57)`・
  **132 枠の `.label` を全部読んで名前の重複 0 件**・console のエラー 0 件
- **`docs/browser-shots/creative-brickblock.png`**（一覧の 11 番目）。
  `.swatch` は `rgb(164,85,63)`（`BRICK`(12) のまま）・`title` は `レンガブロック x64`

**それでも人に見てほしいのはここです**（上が新しい分）:

- **レンガ（32b）**: **`0xbc5d39` が「焼いたレンガ」に見えるか**と、**一覧で
  レンガブロック（`0xa4553f`）と並んだときに 2 つあると分かるか**（測った隔たりは
  **26.0**。判定は 20 なのでぎりぎりではありません）。**粘土 1 個 = レンガブロック 1 個**
  という交換比が渋くないかも手ざわりの判断です（本家と同じ値です）
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **この周の改名で 1 枠ぶん増やしました** —— 7 文字の「レンガブロック」が 2 行に
  折れます（前は 3 文字で 1 行に収まっていた枠）。**5 文字から出る件**で、最長は
  9 文字の「ネザーレンガハーフ」のまま。**人の判断待ち**（`.slot .label` を触ると
  全部の枠の手ざわりが変わります）
- **粘土（32a）**: **海に潜って粘土を見つけられるか**（海底の列の 7.2%・4x4 の塊）と、
  **水越しに砂と見分けられるか**。**粘土玉がやや青紫寄り**（`0xa7b4da`）なのが
  気になるかどうかは人の判断です（`shots/clay.png` / `TUNING.md`）
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）/ **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）/
  **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 1 件**）

- **`rules/items-survival.md`**: **ブロックとアイテムで表示名がぶつかったら、直すのは
  `blocks.ts` の `def()` の文字列だけ。ID は動かさないこと**（セーブに入るのは番号なので、
  名前を変えても 1 バイトも動きません）。**定数名のほうは `BRICK_ITEM` のように
  別名にすること** —— `items.ts` に `BRICK` を作ると `blocks.ts` の 12 と衝突して、
  **`crafting.ts` が両方 import した瞬間に `typecheck` が落ちます**（型で止まる安全な罠）。
  **見張りは「名前の対」を 1 件**（`test/blocks.test.ts` の `brickNames()`）で、
  **色の隔たり（`>= 20`）では気付けません**（名前が同じでも色が違えば緑）。

**この周で踏んだ罠**（どちらも `rules/` が予告していたとおりで、決まりごとは足していません）:

- **`tsc` が TS2367 で 2 件**（`MAX_ITEM_ID === CLAY_BALL` の 2 か所）。
  **比べる相手を新しい番号に直して**通しました（ゆるめていません）
- **「焼けるものの表は N 行」の N を間違えました**（`SMELTING` は木炭の周ですでに
  9 行で、レンガを足して 10 行）。**落ちた出力に `10 行` と出ていたのでその場で
  直しています** —— 「値を出してから判定する」形の効きどころです

## 人がやること

- **⚠ 設計の判断（58 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1449 行**（テストの数え方で 1450）で、**`AUTODEV.md` の停止条件 2 に並んでいます**
  （上限 1500）。**待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 /
  **24b ケーキをかじる**・最低 7 行）。**キューの上 2 件（33・34）はどちらも
  `main.ts` 0 行の見込み**なので、判断が下りなくても 2 周は止まりません
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。
  **この周の改名で 1 枠ぶん増えました**（「レンガブロック」= 7 文字）。
  **次の周（33・防具 12 部位）は名前が長いので、さらに 12 枠ぶん増えます**
- **⚠ 防具に耐久値を足すか。** いまの防具は**壊れません**（足すならそれだけで 1 周）
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は 1 回跳ねました**
  （声色。走り直して 2 回続けて緑）。**直すなら測り方**（窓を長く取る・複数回鳴らして
  中央値を見る）で、**判定はゆるめないこと**（`rules/testing.md`）。
  **キューに足すのは A の周の仕事**です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 87 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を `click()` してクリエイティブにし、`E` を押してから撮ること**
（`docs/browser-shots/README.md` の末尾）。**⚠ 送り先は末尾とは限りません** ——
**低帯（1..63）の番号を取ったものは一覧の真ん中に出る**ので、`scrollTop = scrollHeight`
では 1 枠も写りません。**`.label` の文字列で枠を探して
`scrollIntoView({ block: "center" })` を呼ぶこと。**
**この周は 2 枚撮りました**（末尾のレンガ 170 と、真ん中のレンガブロック 12）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**海底の粘土（32a）も場面 `clay` を足しました** —— ただしあれは**原点のまわりを
撮りません**（種 4242 は ±128 の海が全部凍っています。`rules/meshing-render.md`）。
**レンガ（32b）は新しいブロックが 0 個なので、場面を 1 つも足していません。**
