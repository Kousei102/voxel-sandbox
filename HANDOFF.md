# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-11・AUTODEV 71 のあと）

- `npm test` **3424 件すべて成功**（**±0**。この周は `src/**` も `test/**` も触っていません）
- `npm run typecheck` は走らせていません（**B の周なので `src/**` に差分 0**）。
  `npm run build` も `npm run bench` も不要です
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1450 行**（テストの数え方。`wc -l` では 1449。**±0**。上限 1500）
- **アイテム 123 種・`MAX_ITEM_ID` 161・111..255 の空きは 94**（±0）・**1..63 の空きは 9**（±0）
- **立方体 41 / 非立方体 79**・**モブ 9 種類**・**レシピ 65 本**・**バイオーム 11**
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（**119 行**。上限は `split("\n")` で 120）・
  **`AUTODEV-QUEUE.md` 137 行**（未着手 **3 件**）

## この周でやったこと（AUTODEV 71 の B・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 済`」で 2 行目 = B（仕様）の周**でした。
**キューの先頭 `11. スケルトンと骨` を `AUTODEV-SPEC.md` に書きました**（`src/**` の差分 0）。

**取る前にコードで数え直しました**（B の周の決まり）: `MobKind` は 9 種類で `"skeleton"` は
無い / `src/**` に `骨` も `BONE` も 0 件 / 一方で **`projectiles.ts` の `"arrow"` と
`MobDef.ranged` の仕掛け（`fire()` / `clearShot()` / `shootTimer`）はブレイズで全部
通っている**。**残っているのは表 2 つ（`MobDef` 1 件・`item()` 1 行）だけ**で、
**`main.ts` も `mobrender.ts` も `mobmesh.ts` も ±0 行**です。

仕様を書きながら**先に測って決めた 2 つ**（次の C の周が調べ直さなくて済むように）:

- **⚠ 矢は重力を受けます**（`gravityScale: 1`・`GRAVITY` 30・速さ 40）。狙いは
  `PLAYER_AIM`(1.4) へ**まっすぐ**なので、落ちる量は `0.5 × 30 × (距離/40)²` ——
  **10m で 0.94m（足元から 0.46m に当たる）・16m で 2.4m（プレイヤーの下を抜ける）**。
  だから **`ranged.range` は 10** で、**伸ばすなら先読みか山なりが要る**（= `fire()` に
  新しい判断が生える）ので仕様では禁じ手にしてあります。**ブレイズの 16 を写さないこと**
  —— あちらの火球は `gravityScale: 0` です
- **骨の色は `0xcdc8b0`**（いちばん近い砂から **24.6**）。素直な `0xd8cfae` は砂と
  **20.9** しか離れず、判定（20）のすぐ上でした。**淡い暖色は一覧でいちばん混んでいる帯**
  （矢 15.2 / 鉄インゴット / 砂 / 羽根 / 羊毛）で、明るくすると羽根と羊毛に、
  暗くすると砂に寄ります

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が **`状態: 未着手`** なので判定表の 3 行目です。

- **仕様書は全文をサブエージェントに渡すこと**（要約しない）。当たる `rules/` は
  **`mobs.md` / `projectiles.md` / `items-survival.md` / `drops.md` / `bow.md` /
  `testing.md`**（`grep -l '"src/mobs.ts"' rules/*.md` で引けます）
- **ID は 162（骨）の 1 個だけ。** 終わったら `ROADMAP.md` の予約表に「実装済み」と書き、
  **`npm test` の「111..255 の空き」が 94 → 93** になることを確かめること
- **敵対モブを足すので `test/mobs.test.ts` が 4 件必ず赤くなります**（仕様書の 5.）。
  **ゆるめずに数え直すこと** —— とくに `census()` の `counts` に **`スケルトン` の 1 語**
  （無いと `counts[name]++` が **NaN** になり、何も見ていない状態で緑になります）
- **この周のあと未着手は 2 件**（12 と 24b）になるので、**その次の B の前に A（棚卸し）の周**です

## ブラウザで見てほしいところ

**この周は絵を撮っていません**（`src/**` に差分 0 の B の周です）。
**次の C の周で撮るのは `npm run shot -- mobs`** で、見るのは
**スケルトンの頭と胴が分かれて見えるか** —— 頭も体も同じ骨色なので、
**牛の頭で踏んだ「前から見ると 1 枚の板」がそのまま出ます**（`rules/mobs.md`）。

**持ち越し（防具 27b-2）**: 防具枠の置き場所（盤面と収納のあいだ・横 1 列）が使いやすいか /
1 文字のラベル（頭・胴・脚・足）で部位が分かるか / **革一式（防具点 7）で 28% 減が
「固くなった」と感じられるか**。**⚠ 一覧と防具枠の名前が 2 行に折れて絵に被る件**
（`style.css` の `.slot .label`。**5 文字の「革のズボン」で出ます**）は**人の判断待ち**です。

**持ち越し（革の防具 27b-1）**: 革の色 4 つが「革」に見えるか（上の 2 つは橙寄りに出ます。
**変えるなら隔たりを測ってから**。`TUNING.md`）/ **革 24 枚（＝牛 24 頭）で一式**が
見合う値段か（**革は牛しか落としません**）。

**持ち越し（フェンス 26a / 26b）**: **`shots/fence.png` に 5 通りが写っていて、
面の欠けも裏返りもありません**。**手ざわりとして「柵らしく見えるか」**だけ見てください。
**⚠ フェンスを 52fps 未満で跳び越せる件は残っています** —— 跳躍の余裕は **0.0117**
（到達は `9.2²/(2×30) + 9.2×dt/2` で、実測 **60fps 1.4883 / 30fps 1.5667 /
刻みの上限 0.05 で 1.6450**）。**直すなら跳躍の積分**（全ブロックの段差の登り方が
変わります）**か 1.5 の側**（本家の値から離れます）——**数値では決められないので人の判断です。**
**当たり判定はマスいっぱいのままで、腕の無い側にも「見えない壁」があります** /
**色 `0x988a5e` が「木の柵」に見えるか** / **棒 6 本で 2 個**が牧場 1 区画に見合う値段か。

**持ち越し（凍った海 25b）**: **⚠ 氷と雪の浜がほぼ同じ白で、斜め上から見ると陸と氷の
境目が読みにくい**です。数値では直せません（氷 `0x8fc4f2` は一覧の隔たりで決まっていて、
いちばん近いガラスから 34.3 しか余裕がありません）。**気になるなら人が色を決めてください** /
**氷の板の上を歩けることが見て分かるか** / **氷ごしに海の底が見えるか**（`alpha` 0.6）。

**持ち越し（氷 25a）**: **滑りの手ざわり** —— 走り出しが遅く（0.2 秒で 2.76 m/s。土は 5.2）、
**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。**本家の氷はもっと長く滑ります** ——
伸ばすなら `ICE_FRICTION` ではなく **`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**。

**持ち越し（ケーキ）**: 上面 `0xffd0e4` が「ケーキ」に見えるか / 側面 `0xe8c9a0` と
下面 `0xd9b98a` の 3 色がケーキに読めるか。**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: 中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか /
素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / 森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ /
牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて閉じる経路 /
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` へ据えるものはありませんでした。** 仕様を書きながら踏んだ 2 つ（矢の重力と
骨の色）は**どちらもこのタスク固有の数値**で、`rules/mobs.md` には
「狙うのはいまプレイヤーが居る所」も「淡い色どうしは隔たりを測る」ももう書いてあります。
**次の C の周で実装して初めて分かることがあれば、そのとき `Edit` で据えること**（C-4）。

## 人がやること

- **⚠ 設計の判断（44 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（テストの数え方）で、**`AUTODEV.md` の「止まって人を呼ぶ条件」の
  2 番にちょうど並んでいます。** ハードの上限は 1500 行で、**次に `main.ts` へ 1 行でも
  足す周は、先に判断を別ファイルへ出さないと止まります**（待っているのは
  **24b（ケーキをかじる。最低 7 行）**）。**次の C の周（スケルトン）は ±0 行の見込み**なので、
  ここでは止まりません。`test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
- **⚠ 一覧と防具枠の名前が 2 行に折れてはみ出す件**（`style.css` の `.slot .label`）。
  **見た目の判断なので人が決めてください**
- **⚠ フェンスを 52fps 未満で跳び越せる件**（上の節）。**手ざわりの判断です**
- **凍った海の白 2 色**（上の ⚠）。数値では直せないので、色を決めるなら人の判断です
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 54 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#mode` を押してクリエイティブにし、`E` を押して末尾までスクロールしてから撮ること**
（手順は `docs/browser-shots/README.md` の末尾）。
**自然生成しないブロックは今までどおり `npm run shot -- <名前>` が唯一の経路です。**

**`AUTODEV-SPEC.md` の 120 行は `split("\n").length` で数えます** ——
**`wc -l` を 119 以下に収めること**（`rules/testing.md`）。
