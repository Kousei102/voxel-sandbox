# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-07・AUTODEV 50 のあと）

- `npm test` **3194 件すべて成功**（前の周から **+24**。紙・本・本棚の一群）・
  `npm run typecheck` 緑・**`npm run build` 通った**（`dist/` は 666.74 kB）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていないため）
- **音の一群はこの周も 1 度も跳ねませんでした**（3 回まわして 3 回とも緑）。落ち着いた値は
  羊毛 0.552 / 雪 0.716 / 砂 2.235・低い声 0.162 / 高い声 0.329 で、前の周と同じです
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1449 行**（`wc -l`。テストの数え方では 1450。上限 1500・
  **止まる目安 1450 に届いたままです。この周は ±0** —— 予告どおり 1 行も足していません）
- **アイテム 114 種・`MAX_ITEM_ID` 152・111..255 の空きは 103・1..63 の空きは 9**
  （**予約表の次の空きは 153**）
- **立方体 40**（+1）**/ 非立方体 76**（±0）・**モブ 9 種類**・**レシピ 58 本**（+3）・
  **精錬 7 本**・**燃料 8 種**・**食べ物 10 種**（±0）・**バイオーム 10**・**構造物 2**・
  **次元 3**・**`DamageCause` 9 種**

## この周でやったこと（AUTODEV 50 の C・クラウドの無人の周）

**判定表は「未着手 10 件（3 件以上）」「`AUTODEV-SPEC.md` が `状態: 未着手`」で
3 行目 = C（実装）の周**でした。**キューの 21（紙・本・本棚）を実装しました。**

**足したもの**: 紙 150・本 151・本棚 152 と**レシピ 3 本**
（サトウキビ 3 → 紙 3 / 紙 3 + 革 1 → 本 1 / 板 6 + 本 3 → 本棚 1）。
**本棚を壊すと本 3 個だけ**で、**板 6 個は戻りません**（本家どおり）。
**`main.ts` は 0 行**・**`SaveData` は 1 バイトも増えていません**。

**触ったのは 6 ファイル**: `src/items.ts` / `src/blocks.ts` / `src/crafting.ts` /
`tools/shot.ts` / `test/blocks.test.ts` / `test/crafting.test.ts`。
**サブエージェントは立てていません**（`AUTODEV.md` の「返らないなら自分で実装すること」）。
**点検は親が `git diff` を読んで自分でやりました**（C-2 の「別を立てられないなら親が読む」）。

**踏んだ落とし穴 2 つを `rules/` へ据えました**（下の「この周で分かったこと」）。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので、判定表の
2 行目に当たります（未着手は 9 件なので A には落ちません）。
**キューの先頭は 22（金のリンゴ）** —— 金インゴット 8 + リンゴで、**ID 1 個・
`main.ts` は 0 行**の見込みです（`FOODS` に 1 行・レシピ 1 本）。**依存の 20 は済み**で、
**取る番号は `ROADMAP.md` の予約表から 153**。

## ブラウザで見てほしいところ

**この周に撮った絵で確かめたこと**（`shots/bookshelf.png`。仕様書の 3 点）:
**面の欠けは 0**（本棚の壁 3x2 も、離して置いた単体も 6 面ぶんが出ている）/
**上面（木口 `0xd0a878`）と側面（本の背 `0x9c5064`）が入れ替わっていない**/
**隣に並べた板の壁と見分けが付く**（板は茶、本棚の側面は赤紫でまったく別物）。
**`node tools/browsershot.mjs` も 5 枚撮り直しました**（WebGL 2.0・console のエラー無し）——
**本棚は 1 個も写りません**（自然生成しないので）。あれで見たのは
「公開する `dist/` が今までどおり動くこと」だけです。

**それでも人に見てほしいところ**: **本棚が「本の詰まった棚」に見えるか。**
絵では**側面の `0x9c5064` がかなり赤紫に寄って見えます** —— 面も向きも正しいのですが、
**レンガや羊毛のように見えないか**は手ざわりの話なので人にしか決められません
（数値では一覧のどの色とも 20 以上離れています。`TUNING.md`）。あわせて
**紙 `0xd4e0ec` が一覧で雪玉・羽根・卵・砂糖と見分けられるか**（雪玉と 25.5 で、
**判定の下限 20 に近い**）と、**本棚 1 個に材料 9 個（革 3 + サトウキビ 9 + 板 6）が
見合うか**（**使い道がまだ無い立方体です** —— 本家のエンチャント台は見送り済み）。

**持ち越し（まだ誰も見ていません）**: **リンゴの色 `0xe0342c` が一覧で赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か**（木 1 本の葉が
40〜60 枚なので、1 本丸ごと刈って 4〜5 回に 1 個）/ **地面に落ちたリンゴの立方体が
赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか**（4 マスの縦穴に
1.7 秒）/ **手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか**
（**しゃがみで止まる手はここにありません** —— `Shift` は走りなので、止まる手を足すと
`controls.ts` の話になります）/ **はしごの上端から地上へ出る手触り** / **はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に
見えるか** / **「はしご は壁にしか付けられません」の文が、床を狙ったときに親切か** /
**`CANE_GROW_SECONDS = 180` が待てる長さか**（1 段から 3 段まで 6 分）/ **上を刈って
伸び直すのが「畑」として成り立つか** / **自然に生えたサトウキビは伸びません**（上に 1 本
置けば列ごと覚えます。理由は `TUNING.md`）/ サトウキビの背丈と密度の釣り合い（0.12 で
1〜3 段）/ **積む手ごたえ** / **⚠ 前から遊んでいる世界だけの見え方**: `localStorage` の
差分は「壊した／置いた」だけを持つので、**前に浜のサトウキビを壊した場所は、下の 1 マス
だけが空いて上の段が浮いて見えることがあります**（新しく作った世界では起きません）/
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / サトウキビ
（`0x9ad14f`）が小麦の種と並んだとき / ボウル `0x7a4a24` とシチュー `0xf0dcb4` の見分け /
シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか / 食べ終わりにボウルが手に戻る
手ごたえ / 森を歩いたときのキノコの密度（森 0.015 / 針葉樹林 0.01）/ 走っていて赤・茶を
草むらと取り違えないか / サボテンに当たったときの痛さ（0.5 秒ごとに 1）/ 牛を搾る・
ミルクで毒が止まるか / ミルクバケツの色（`0xeaf2f8`）/ かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件**）

1. **`rules/blocks-shapes.md`**: **`def()` の色は `top` → `side` → `bottom` の順に落ちる**
   （`bottom ?? side ?? top`）ので、**`side` だけ書くと下面まで側面の色になります。**
   上下が同じで側面だけ違うブロックは `bottom` を必ず書くこと ——
   **型でも `npm test` でも止まらず、絵でも下から覗かないと分かりません**
   （一覧に出るのは `top` だけなので、色の隔たりの判定にも引っかかりません）
2. **`rules/testing.md`**: **同じ値に `=== A` と `!== B` を続けて書くと `tsc` が落ちます**
   （TS2367）。ID は数値リテラル型なので `drop.item === BOOK && drop.item !== BOOKSHELF` の
   2 つ目が「'151' と '152' は重ならない」になります。**型で止まるので安全な罠**です

## 人がやること

- **設計の判断を 1 つお願いします（24 周持ち越し。もう待てません）:
  `main.ts` を 2 つに割ってよいか。** いま **1449 行で、止まる目安 1450 に届いています**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **22 金のリンゴ / 27a 防具の枠は 0 行**の見込みです。
  **23 クモの巣・24 ケーキ・25 氷・26 フェンスは配線が要ります**（あと 2 周ぶんの余裕）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 34 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。
**いまの仕様書は `wc -l` で 117 行 / テストの数え方で 118 行です。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
