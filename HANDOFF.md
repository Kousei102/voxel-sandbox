# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-25）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- **1 周はクラウドの Routines が回します**（`LOOP.md` を 1 周ぶん）。無人で回すうえで
  実際に踏んだものが 3 つあります:
  **(1)** **`.claude/rules/**` を編集すると「sensitive file」の確認で止まります。**
  → **`RULES-INBOX.md` に書いて、人が居る周が取り込む**形にしました（`LOOP.md` の「4.5」）。
  **`.claude/settings.json` に許可を書いても消えません**（2026-08-25 に 2 周とも実測）。
  **(2)** クラウドから `origin` へ push できず 403 → ユーザーが GitHub 側で権限を付け直し済み。
  **(3)** **5 時間の利用制限。** 当たった周は実装を終えた直後に止まり、次の定期実行も
  同じセッションに投げられて即終了しました（**新しい周にならず、1 回ぶんの枠が消えます**）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1842 件すべて成功**（1808 → 1842。この周の 34 件は要塞の部屋と枠）
- **進行（クリア導線）達成 10 / 13**（10 件とも本物）。**この周は増えていません** ——
  2-8 は「要塞を建てる」周で、次の項目（エンドポータルの起動）は 2-9 のものです
- ブロック ID **1..63 の空きは 12 個**（この周で 51 と 53 を使いました）。
  向き違いは **110 まで**（104..110 が枠の 7 個）。アイテム ID は **92 まで**（増えていません）
- `src/main.ts` は **1413 行 / 上限 1500**（余白 87 行。**この周は 1 行も触っていません**）

## 直前にやったこと — TASKS 2-8（要塞。エンドポータルの部屋）

**要塞が地面の下に建つようになりました。** ただし**まだ起動できません**
（枠にアイを嵌める操作は 2-9）。

| どこ | 何が入ったか |
| --- | --- |
| `blocks.ts` | **石レンガ 53** と **エンドポータル枠 51**（向き 4 x アイの有無 2 = 8 通り。向き違いは 104..110）。`endPortalFrame()` / `isEndPortalFrame()` / `frameHasEye()` / `frameFacing()` |
| `stronghold.ts` | **`STRONGHOLD`（`StructureDef`）を足した。** `STRONGHOLD_SITE` を広げただけ（写していない）。殻・輪・松明の 3 つの関数 |
| `structures.ts` | `stampPlacements()` に**横の飛ばし**を足した（`extent` で見る。4 行） |
| `worldgen.ts` | `STRUCTURES` に `STRONGHOLD` を 1 行 |
| `test/stronghold.test.ts` | 「要塞の部屋（エンドポータル）」と「要塞の部屋（生成）」の 2 節 |
| `test/blocks.test.ts` | 「エンドポータルの枠」の節 |
| `test/structures.test.ts` | 「横に外れた構造物は `build()` ごと飛ばす」 |

- **形はエンドポータルの部屋 1 つだけです**（通路も階段も他の部屋もありません）。
  内側 11 マス四方 x 高さ 4、石レンガの殻、真ん中に枠 12 個の輪
  （5x5 の四隅を落とした形。**中の 3x3 は空**）、壁に松明 4 本。
  **地面から 18 マス下**（`lift: -18`）に床を置くので、アイの指した所を掘り下げて入ります
- **`stronghold.ts` が場所と形の両方を持ちます。** `{ ...STRONGHOLD_SITE, name, extent, lift, build }`
  なので、**投げたアイの指す先と実際に建つ場所が食い違いようがありません**
  （`test/stronghold.test.ts` が両向きに見張っています）
- **枠は壊せません**（`hardness` が無限。岩盤と同じ扱い）。掘れると、
  **起動する前に枠を壊してクリアできなくなる詰み**が作れます
- **枠の向き 4 種は、いまは見た目に出ません**（4 向きとも同じ形・同じ色。`BlockDef` は
  面ごとに 3 色しか持てない）。それでも状態に持たせてあるのは、**輪の向きが正しいか**を
  外から確かめられるようにするためと、**あとから足すと ID の振り直しになる**ためです
- **`stampPlacements()` の横の飛ばしがこの周の隠れた肝です。** 列挙は 1 チャンクにつき
  グリッド 3x3 マスぶん（最大 9 個）出てくるので、飛ばさないと
  **捨てられるだけの `put` を 9000 回**呼びます（要塞は 1 個 1014 マス）。
  **書き込む中身が 1 マスも変わらないこと**をテストで固定しました
- **わざと壊して 6 通りとも赤くなることを確かめました**: 枠の向きを外向きに → 1 件 /
  横の飛ばしを外す → 1 件 / `extent` を小さく申告 → 2 件 / 深さを 30 に → 1 件 /
  最初からアイを嵌める → 1 件 / `STRUCTURES` から外す → 1 件
- `structures.ts` はチャンク生成の中なので、`npm run bench` を**交互に 3 回ずつ**測りました:
  `generateChunk` 0.89 / 0.89 / 0.90ms（要塞なし）→ **0.91 / 0.88 / 0.93ms**（誤差の中）
- 決まりごとは **`RULES-INBOX.md` に 2 件**（`rules/worldgen.md` の構造物の節へ 1 行と、
  「要塞（`stronghold.ts`）」の新しい節）、数値は **`TUNING.md` の「要塞の部屋（2-8）」（9 行）**、
  確認項目は `REVIEW.md` の「2-8」に 7 個

### 前の周（2-1 〜 2-7）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ（幅 5・全長 41・頭上 4 マス）。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージは書かず、撃った側が
  `Shot.damage` に載せる**ので、2-12 の弓矢は撃つ側を足すだけで当たる側が要らない）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ。
  **クリア導線の 2 本がここで合流する**）
- **2-7**: 投げたエンダーアイが要塞の方を向く（`stronghold.ts` 新規。
  `structures.ts` から `SiteRule` と `siteAt()` を切り出した）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 /
  2-8 の 12 節が溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

1. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（これまで 6 度）。
   この周は新しく踏んでいませんが、**残っている仮 3 件のうち 2 件は
   「そのブロックがあるか」だけを見ています**（「エンドポータルが 12 個の枠で起動する」と
   「エンドへ行ける」）。**2-9 / 2-10 で本物に差し替えるとき、`block("エンドポータル")` の
   存在で通してはいけません** —— 2-8 で枠だけを足して、ポータル本体（ID 52）を
   わざと足さなかったのはそのためです（足すと、起動できないのに達成へ化けます）
2. **構造物の `extent` は「どこに掛かるか」の唯一の材料です**（この周）。
   `stampPlacements()` が縦にも**横にも**これで飛ばすようになったので、
   **`build()` が `extent` の外へ書くと端が黙って欠けます。**
   構造物を足すときは「申告した extent の外へ書かない」を必ずテストに置くこと
3. **地面から潜る構造物の深さは、地形の最低高さで頭打ちになります**（この周）。
   `STRONGHOLD_DEPTH` を 30 にしたら、海の底の要塞の床が y=0（岩盤）に刺さりました。
   実測の最低高さは 25 前後です
4. **1 チャンクに収まった標本だけを見ていると、端の欠けを見逃します**（この周）。
   最初は種 1 つだけで見ていて、たまたま要塞が 1 チャンクに収まり、
   `extent` を小さく申告しても緑のまま通りました（4 種で見る形に直しました）
5. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（2-7）
6. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字まで
7. **探す半径は「正方形」であって「円」ではありません**（2-5）
8. **試験場のプレイヤーは、跳ぶモブでは `HOSTILE_SIGHT`(18) より遠くに置くこと**（2-5）
9. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）
10. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の火球で焼け死にます
11. **相手を先に、ブロックをあとに見ること**（2-4b）。逆だと壁を背にした相手に当たりません
12. **飛ぶモブには `ACCEL_GROUND` を使うこと**（空中の加速 4 では 1 フレームに 0.07 しか戻らない）
13. **`world.seed` をセーブや場所の計算に使わないこと**（ネザーでは塩を混ぜたあとの値）。
    種は `worldSeed`
14. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）
15. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）
16. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）
17. **わざと壊して、テストが赤くなるか確かめること。** この周は 6 通りとも確認済みです
18. **`test/audio.test.ts` の「声色（音程の倍率）が波形に出る」が 1 度だけ落ちました**
    （この周の途中で 1 回。`0.329 > 4.161` という出方）。**そのあと 5 回まわして再現せず**、
    実装は 1 行も触っていません。**もう一度出たら、そのときは本気で見てください**
    （`node-web-audio-api` のレンダリングが揺れているのだと思われます）

## 次に取るもの

1. **2-9（エンドポータルの起動。枠 12 個にエンダーアイ）**。**枠はもう建っています。**
   - **アイを嵌める入口は `blocks.ts` の `endPortalFrame(facing, eye)` 1 つだけ**にすること。
     状態の番号（`向きの添字 * 2 + アイ`）を写さないでください。
     いま嵌まっているかは `frameHasEye(id)`、向きは `frameFacing(id)`
   - **ポータル本体（ID 52 `END_PORTAL`）はまだ足していません。** `ROADMAP.md` の
     予約表から取ってください。**足すと進行テストの仮の判定（「エンドポータルが
     12 個の枠で起動する」）が勝手に達成へ化けます** —— その周のうちに
     **probe を本物に差し替えること**（実際に 12 個嵌めて、3x3 が埋まるところまで）
   - 判断の置き場は `portals.ts`（ネザーポータルの点火）が手本になりますが、
     **輪の形は `stronghold.ts` が持っています**（`STRONGHOLD_RING` = 2 の 5x5 から
     四隅を落とした 12 個）。**写さないこと**
   - **枠は壊せない**ので、「壊されて半分になった輪」は考えなくて済みます
2. そのあとは並びどおり 2-10 → 2-11 → 2-12 → 2-13（ドラゴン）。
   **2-12 の弓矢は、撃つ側（弓とアイテム）を足すだけで当たる側が要りません**
3. 最後に人がやること: `RULES-INBOX.md` の取り込み（**いま 10 件**）/ `REVIEW.md` の一括確認
   （**いま 12 節**）/ `TUNING.md` の一括調整 / master へマージ

## 気付いているが、まだ手を付けていないもの

- **要塞は「部屋 1 つ」だけです**（本家は通路・図書室・階段・シルバーフィッシュ）。
  肉付けするなら `stronghold.ts` の `build()` に足す形ですが、
  **クリア導線が 13 / 13 になってからにしてください**
- **要塞の中に入る道がありません**（掘って降りる前提）。本家のように階段や入口を
  付けるなら、地表まで届く縦穴を `build()` に足すのが素直ですが、
  **`extent.up` が地表まで届く高さに膨らむ**ので、生成の費用を測り直してください
- **投げたエンダーアイは、近づいても落ちません**（本家は近いと真下へ落ちる）。
  だから「その辺り」までしか案内してくれず、**部屋を 13 マス四方と大きめ**にしてあります
- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  `eyeShot()` に次元を渡して「オーバーワールド以外では null」にするのが素直です
- **投げたアイは地面に落ちません**（`projectiles.onExpire` で `drops` に渡すところから）
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒。`smelting.ts` に 1 行）
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方を全部見直すことになるので、**独立したタスクとして決めてください**
- **エンダーマンは日陰へ逃げません**し、**ブロックを持ち運びません**
- **ネザーでも寝られます**（本家はベッドが爆発する）
- **寝ても時間は「進みません」**（時計だけ飛びます）
- **火球は火を点けません**（火そのものが無いので、ブロックとして足すところから）
- **ブレイズは本家の 3 連射をしません**（3 秒に 1 発）
- **`F3` の `biome` はネザーでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラックやソウルサンドの上にはゾンビが湧きます**（`spawnOn` を持たないので
  「どこでも」側）
- **要塞の部屋の中にも敵対モブは湧きます**（石レンガは `spawnOn` を持たないため）。
  松明 4 本では隅まで照らしきれないので、**掘り当てた部屋にゾンビが居ることがあります。**
  本家の要塞も同じなので、いまは直していません
