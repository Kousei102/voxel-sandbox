# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- `npm test` **2965 件すべて成功**（**前の周と同じ 2965。この周は `test/**` に 1 行も
  差分がありません**）・`npm run typecheck` 緑。**`npm run build` と `npm run bench` は
  走らせていません**（**`src/**` を 1 行も触っていない A の周**のため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1464 行**
- **アイテム 98 種・`MAX_ITEM_ID` 133・111..255 の空きは 122・1..63 の空きは 9**
  （**この周は ID を 1 個も取っていません**）
- **モブ 9 種類**（受動 4 = 豚・羊・鶏・牛 / 敵対 4 = ゾンビ・クモ・ブレイズ・
  エンダーマン / ボス 1 = ドラゴン）・**クラフト 44 本 / 精錬 7 本 / 食べ物 8 種**

## ⚠ 次に `main.ts` を触る周がまず見ること: 1464 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
**6 周続けて `main.ts` を 1 行も触っていないので、持ち越しです。**
**次に触る周は、先に判断を別ファイルへ出すこと。**
**キューの上 2 件（13 雪玉・14 圧縮ブロック）は `main.ts` に 1 行も要らない**ので、
行数が解けるのを待たずに取れます（15 番から先が待ちです）。

## この周でやったこと（AUTODEV 23 の A・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が **2 件（3 件未満）**だったので **A（棚卸し）の周**。
判定表を上から当てて最初に当たった 1 種類だけをやりました（`AUTODEV-SPEC.md` は `済` ですが、
**上から当てるので B より先に A**）。**`src/**` と `test/**` に差分は 1 行もありません。**

- **8 ジャンルをコードから数え直しました**（`docs/autodev-counting.md` の表）。
  **前の表（2026-09-01）はもう使えませんでした** —— あれは「アイテム 78 種 /
  `MAX_ITEM_ID` 94 / モブ 6 種類 / クラフト 34 本」で、いまは **98 / 133 / 9 / 44**
- **7 件足して、未着手 9 件になりました**（基準は変えず「本家のバージョン順」）。
  **残っていた 11・12 は消さずに末尾へ**（どちらもコードでまだ無いことを確かめました）
- **数えて初めて出た穴**: **鉄・金・ダイヤに「しまう形」（圧縮ブロック）がまるごと無い。**
  **ダイヤは掘っても道具にするしか行き場がありません**（キューの 14 番）
- **見送りを 2 件、名指しで数え直しました**（前の A が「次の A で見直す」と書いたもの）:
  **TNT** は重力ブロックが済んでも道が開いていないので**また見送り**、
  **防具**は革（132）で材料の依存だけ解けたが**枠 12〜16 個と DOM の枠が残る**ので見送り

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**キューの未着手が 9 件・`AUTODEV-SPEC.md` が `済` なので → 次は B（仕様）の周**です。

- **B の周も `src/**` に 1 行も差分を作らないこと**（完了条件）
- **取るのはキューの先頭の 1 件 = 13 番（雪玉）**。飛ばさないこと
- **取る前に、その 1 件だけコードで数え直すこと**（`AUTODEV.md` の B。全部を数え直すのは A の仕事）。
  この周に数えた根拠: `items.ts` の `DROPS` に `SNOW` の行は無く（掘ると雪ブロックが
  そのまま出る）、`PROJECTILE_KINDS` は 5 種類（火球・矢・アイ・ブレス・卵）
- **仕様書に必ず書くこと**: **落とし物を雪玉に差し替えると雪が置けなくなる**ので、
  **戻すレシピ（雪玉 4 → 雪ブロック）も同じ周で書く**こと。ID は `ROADMAP.md` の
  予約表から **134** を 1 個。**`main.ts` には 1 行も書かせないこと**

## ブラウザで見てほしいところ

**この周は見た目に 1 画素も出ません**（`src/**` に差分ゼロ）。**新しく見てほしいものは
ありません。** 持ち越しだけです:

**クモを 3 匹倒すまで弓が作れない**道のり（遠すぎるなら糸の個数のほうを触ること） /
**夜の敵対がゾンビとクモで半々になった手触り**と**クモの速さ 6.0** /
**クモが朝に燃えてよいか**（本家は燃えません）/ **糸・羽根・卵がどれも淡い色** /
**投げた卵に使い道が無い** / **牛の胴が寸詰まりに見えないか** / **ステーキと革が一覧で
見分けられるか** / **産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒**。

## この周で分かったこと

**決まりごとは 0 件です**（`rules/` に 1 行も足していません）。A の周は `src/**` を
触らないので、据えるべき落とし穴が出ませんでした。代わりに**次の A の周への申し送りを
2 つ `docs/autodev-log.md` に残しました**（TNT をまた数えること / 防具を「枠だけ」に
割れないか見ること）。

**「A の周でコードから数え直す」という決まりが、そのまま効いた回でした。**
3 周前の表を継ぎ足していたら、**モブ 6 種類・アイテム 78 種という嘘の土台**の上で
7 件を並べていました。**`AUTODEV.md` の A-1（実装済みリストを持たない）は、
面倒に見えて実際に効いています。**

## 人がやること

- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 5 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**この周は 1 回目が「声色（音程の倍率）が波形に出る」で赤くなり
（低い声 4.129 > 高い声 0.329）、2 回目以降は 3 回とも緑でした**（低い声 0.162）。
**`rules/testing.md` が「鳴き声も揺れる」とすでに名指ししていた形そのまま**だったので、
決まりごとは足していません。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
