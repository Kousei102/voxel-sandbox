# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-01）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- `npm run typecheck` / `npm test` は通ります。**`npm test` 2286 件すべて成功**
  （**この周も `src/**` と `test/**` を 1 行も触っていません。** 2285 → 2286 の 1 件は
  `test/docs.test.ts` が `AUTODEV-SPEC.md` の行数を見始めたぶんで、機能は増えていません）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- `src/main.ts` は **1372 行 / 上限 1500**（変わっていません）
- **ブロック ID: 1..63 の空き 9 / 111..255 の空き 145**（変わっていません）
- **`AUTODEV-SPEC.md` ができました（`状態: 未着手`・113 行 / 上限 120）** → **次の周は C（実装）**
- **`RULES-INBOX.md` に未取り込みが 2 件**（`mobs.md` と `projectiles.md`）。**取り込むのは人が居る周**

## この周でやったこと（無人・B の周 1 種類だけ）

`AUTODEV.md` の「1. いまはどの周か」の判定表で **B（仕様）** に当たったので、
**`AUTODEV-QUEUE.md` の先頭 1 件を仕様書にしただけです。`src/**` も `test/**` も
触っていません**（B の完了条件）。

### 取った 1 件 —— **砂と砂利が落ちる（重力ブロック）**

**取る前にコードで数え直しました**（B の決まり）。`blocks.ts` の `GRAVEL`（44）の説明に
**「落ちません。落ちるブロックの仕組みがまだ無い」**とはっきり書いてあり、`src/` に
ブロックを重力で動かす経路はありません（重力は `player.ts` / `mobs.ts` / `drops.ts` /
`projectiles.ts` が持つ**速度**だけで、ボクセルは動きません）。**まだ入っていません。**

`AUTODEV-SPEC.md` は 7 項目そろっています。決めた形の要点だけ:

- **手本は `liquids.ts` と `blocks.ts` の `quenched()` の対。** 規則（どのブロックが落ちるか）は
  `blocks.ts` の `fallsDown()` に表 1 本、どのマスに効くかは**新規の `gravity.ts`**、
  呼ぶのは `breaking.ts` と `placing.ts` が 1 行ずつ。**`main.ts` と `world.ts` は触りません**
- **ID は 0 個**（新しいブロックもアイテムも足しません）。**確かめられないもの（three / DOM /
  音 / GLSL）も 1 つも増えません** —— 落下は即時にして、落下エンティティを禁じ手にしたためです
- **`world.update()` や生成器からは呼ばせません。** 毎フレーム走らせると
  `test/world.test.ts` の p99 に乗り、砂漠の砂や洞窟の天井の砂利が生成直後に崩れます

## ブラウザで見てほしいところ（見た目に出るもの）

**この周はありません**（文書しか触っていません）。

**前の周からの持ち越しが 1 件まだ残っています** —— メッシュ化の詰め方を変えた（`0c68545`）ので、
**面が欠けていないか**（洞窟の奥・水面・ガラスごし）と **AO の陰影が前と同じか**（壁ぎわ・階段の下）。
数値では法線の一致・穴なし・三角形数まで見ていますが、陰影の出方そのものは目でしか確かめられません。

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` があって `状態: 未着手` なので、**次は C（実装）の周**です。
仕様書を**全文**サブエージェントに渡し（要約しないこと）、実装 → **別の**サブエージェントで
チェック → 記録（`docs/autodev-log.md` / キューの行を消す / 仕様書を `済` に / `HANDOFF.md`）
まで**同じ周でやること**。砂が落ちるのは見た目に出るので、
**「ブラウザで見てほしいところ」を必ず書くこと。**

人がやること:

- 上の**ブラウザ確認 1 件**（前の周からの持ち越し）
- `RULES-INBOX.md` の 2 件の取り込み
- `master` へのマージ

**音のテスト（`test/audio.test.ts`）はまれに 1 件落ちる**と前の周の引き継ぎにあります。
**2 回続けて落ちたときだけ退行として扱うこと。**

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
