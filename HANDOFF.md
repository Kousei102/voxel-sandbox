# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-28）

- ブランチ **`loop/endgame`**。**origin へ push 済み。**
  **`master` への push とマージは引き続き禁止**（`.github/workflows/deploy.yml` は
  `main` / `master` の push でだけ動くので、公開サイトはユーザーがマージするまで変わりません）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 2161 件すべて成功**（2128 → 2161。この周の 33 件は体力バーとクリア画面）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件です**（この周で件数は動いていません）
- ブロック ID **1..63 の空きは 9 個**（この周は 1 個も使っていません）。
  向き違いは **110 まで**、**アイテム ID は 94 まで**（この周は 1 個も使っていません）
- `src/main.ts` は **1485 行 / 上限 1500（余白 15 行）**。**`main.ts` の次元の分岐は 0 件**（上限 2）

## **`TASKS.md` に未着手のタスクが 1 つも残っていません**

**2-13b-2 でキューを使い切りました。** 次にここへ来た無人の周は、
**`TASKS.md` 末尾の「取るタスクが無くなったら」に従ってください**（要点は
**何も実装しない・コミットもしない・push もしない**）。**思い付いた直しに手を出さないこと。**

残っているのは**人がやること 2 つ**（`TASKS.md` の「ループは取らないもの」）です:

- **`REVIEW.md` の一括確認**（**いま 20 節**。ブラウザで見てもらう）
- **`TUNING.md` の一括調整**（手触りの数値を実際に遊んで決める）
- ついでに **`RULES-INBOX.md` の取り込み**（**いま 14 件**。うち 4 件は新しいファイル
  `.claude/rules/crystals.md` / `bow.md` / `use.md` / `exitportal.md` で、
  **今回の 2 件のうち 1 件は既にある `.claude/rules/dom-ui.md` への新しい節です**
  ——`paths` に `src/boss.ts` と `test/boss.test.ts` の 2 行を足してください）と、
  **`master` へのマージ**

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。

## 直前にやったこと — TASKS 2-13b-2（体力バーとクリア画面）

**倒したことが画面で伝わるようになりました。** 中身は 2 つだけです ——
エンドでドラゴンが居るあいだ画面の上端に出る**紫の体力バー**と、
倒した瞬間に出る**クリア画面**（死亡画面と同じ作りで、色と文言が違うもの）。
**ドラゴンの動きも数値も 1 つも変えていません**（`mobs.ts` は口を 2 本足しただけ）。

| どこ | 何が入ったか |
| --- | --- |
| `boss.ts`（新規） | **判断は全部ここ。** `bossBarState()`（出す／消す・割合・ラベル）/ `victoryMessage()` / `VictoryWatch`（倒した瞬間だけ） |
| `mobs.ts` | `activeBoss(次元)`（いま生きているボスの名前と体力）/ `bossName(次元)`（**倒したあとにも取れる**） |
| `ui.ts` | `setBoss()` / `showVictory()` / `hideVictory()` / `victoryOpen`。**DOM だけ** |
| `index.html` | `#bossbar`（**`#hud` の中**）と `#victory`（**`#hud` の外**） |
| `style.css` | 帯（上端中央・幅 `min(46vw, 360px)`）とクリア画面（死亡画面と同じ形で色だけ紫） |
| `main.ts` | 4 か所: `bossBarState(mobs.activeBoss(dim))` を貼る / `victory.update(defeated)` で `openPanel()` / 「続ける」ボタン / メニューを出さない条件に `!hud.victoryOpen`。**+23 行**（1462 → 1485） |
| `test/boss.test.ts`（新規） | 帯の中身と消える条件・本物の `Mobs` との繋がり・出す瞬間・文言・置き場所 |
| `test/ui.test.ts` | `routed` に 6 件（うち 3 件は**呼び出しの形**。下の「踏んだ穴 1」） |

### 決めたこと

- **判断は新しいファイル（`boss.ts`）に置きました。** `mobs.ts` に置くこともできましたが、
  あちらは表と AI で 2200 行あり、**画面の 1 行を確かめるのにモブの世界が全部付いてきます。**
  `boss.ts` は `mobs.ts` を import せず、受け取るのは**必要な値**（`BossFacts` =
  名前・体力・最大体力）だけ。`Mobs.activeBoss()` が返すものと**構造で合わせて**あり、
  食い違えば `test/boss.test.ts` の型検査で止まります（`vitals.ts` の `FoodValue` と
  `items.ts` の `FoodDef` と同じ作法）。
- **置き場所そのものが判断でした。** 体力バーは **`#hud` の中**（プレイ中しか意味がなく、
  メニューや死亡画面の裏に残ってはいけない）。**クリア画面は `#hud` の外** ——
  `#hud` はロックが外れると `hidden` になるので、**あの中に置くと倒しても永久に出ません**
  （クリア画面はロックを外して出すものなので）。テストが `index.html` の並びで見ています。
- **クリア画面は「倒した瞬間」だけ**（`VictoryWatch`）。**「印が立っているか」で出しません** ——
  出口ポータルは `edits` に乗って残るので、入り直すたびにクリア画面が出ます。
- **見張りに戻し方（`reset()`）を足していません。** 次元を移る・ワールドを作り直すと
  `mobs.clear()` が走って `bossDefeated()` が false へ戻るので、見張りも同じ経路で戻ります
  （別に持つと、呼び忘れた場所だけが二度と出ない形を作れます）。
- **開けるのは `openPanel()` を通します。** 掘りかけ・食べかけ・引きかけを止めるのが
  インベントリとまったく同じ 1 か所にまとまります。

### 確かめたこと

- **わざと壊して 9 通りとも赤くなることを確かめました**（**コミットしてから壊し、
  毎回 `git checkout HEAD -- src test` で戻しています**）:
  体力 0 でも帯を出す → 1 件 / 割合を頭打ちにしない → 1 件 / 残りを切り捨てる → 1 件 /
  立ち上がりを見ずに毎フレーム出す → 1 件 / `activeBoss` が次元を見ない → 2 件 /
  クリア画面を `#hud` の中へ → 1 件 / 帯の配線を外す → 1 件 /
  **倒した印で出す → 最初は 0 件** / **メニュー抑止を外す → 最初は 0 件**
  （下の「踏んだ穴 1」。見張りを直してから、2 つとも 1 件ずつ赤くなることを確かめ直しました）
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていません
- **見た目に触れた**ので `REVIEW.md` に「2-13b-2」を 7 項目、数値は `TUNING.md` に 6 行、
  決まりごとは `RULES-INBOX.md` に 2 件（**`.claude/rules/dom-ui.md` の新しい節は
  `paths` に `src/boss.ts` と `test/boss.test.ts` の 2 行が要ります**）

### 前の周（2-1 〜 2-13b-1）

- **2-1**: `nethergen.ts`（床と天井の 2 枚のノイズ・31 以下が溶岩の海・天井は岩盤）と
  ブロック 3 つ（ネザーラック 45 / ソウルサンド 46 / グロウストーン 47）
- **2-2**: `portaltravel.ts`（次元の対応・1:8 の座標・0.9 秒立つ掛け金・出る場所・
  向こうに枠が無ければ建てる）
- **2-3**: `fortress.ts` = 十字の通路 1 つ。ネザーレンガ 48
- **2-3b**: 次元ごとの空（`daynight.ts` の `SKY_STYLES`。**`sky.ts` も GLSL も触っていない**）
- **2-3c**: `main.ts` の減量（1494 → 1349 行。上限は上げていない）
- **2-4a**: ブレイズ本体（`flying` / `fireproof` / `spawnOn` の 3 つを表に足した）
- **2-4b**: ブレイズの火球（`onHitTarget` 1 本。**表にダメージを書かない**）
- **2-4c**: 死んだら寝た次元へ戻る（`BedPoint.dim` と `SaveData.bedDim`）
- **2-5**: エンダーマン（`teleport` / `spawnWeight` を表に足した。湧きに重みが入った）
- **2-6**: エンダーアイ（`items.ts` と `crafting.ts` に行を足しただけ）
- **2-7**: 投げたエンダーアイが要塞の方を向く（`stronghold.ts` 新規）
- **2-8**: 要塞（石レンガ 53 + エンドポータルの部屋 + 枠 51。地面から 18 マス下）
- **2-9**: エンドポータルの起動（`endportal.ts` 新規。枠 12 個にアイ）
- **2-10**: エンド次元（`endgen.ts` 新規。エンドストーン 50 の島 1 つ）
- **2-11a**: エンドの黒曜石の柱 10 本（`endgen.ts` の `PILLARS`）
- **2-11b**: エンドクリスタル（**ブロック 54**。判断は `crystals.ts`）
- **2-12**: 弓と矢（**アイテム 93 / 94**。判断は `bow.ts`）
- **2-12b**: 右クリックの振り分けを `use.ts` へ（1499 → 1445 行）
- **2-13a**: エンダードラゴン本体（`boss` / `orbit` / `regen` / `hover` を表に足した。**13 / 13 到達**）
- **2-13b-1**: エンドの出口ポータル（`exitportal.ts` 新規。**倒した印**が `edits` に乗る）
- **`REVIEW.md` に 1-8 / 2-1 / 2-2 / 2-3 / 2-3b / 2-4a / 2-4b / 2-4c / 2-5 / 2-6 / 2-7 /
  2-8 / 2-9 / 2-10 / 2-11a / 2-11b / 2-12 / 2-13a / 2-13b-1 / 2-13b-2 の 20 節が
  溜まっています**（一括確認はユーザーの仕事）

## 踏んだ穴（同じことを繰り返さないために）

0. **わざと壊す前に、必ずコミットしてから始めること**（2-11b で `git checkout -- src test` を
   使い、コミット前だったので実装が丸ごと飛びました）。この周もコミット後に 9 通り試して、
   毎回 `git checkout HEAD -- src test` で戻しています。
1. **`main.ts` の見張りは「呼んでいるか」だけでなく「何を渡しているか」も見ること**（この周）。
   `victory.update(` があるかだけを見ていたので、**引数を `syncExitPortal()` の返り値に
   すり替えても全部緑**でした（＝倒したあとエンドへ入り直すたびにクリア画面が出る）。
   メニューを出さない条件から `hud.victoryOpen` を外したときも同じく緑。
   **`main.ts` はヘッドレスで import できないので、中で何が起きるかは形でしか押さえられません** ——
   `routed` に `victory.update(defeated)` / `const defeated = mobs.bossDefeated(` /
   `!hud.victoryOpen` と**式の形のまま**並べて、2 つとも赤くなることを確かめ直しました。
2. **DOM の置き場所は、隠れるパネルとの相性で決まります**（この周）。プレイ中だけ意味がある
   ものは `#hud` の中、**ロックが外れてから出すものは `#hud` の外**。逆にすると
   「倒しても永久に出ない画面」と「メニューの裏に残る帯」ができます（通知の `#status` を
   `#menu` に書いていた頃と同じ罠の裏返しです）。
3. **引数に既定値を付けると、渡し忘れが型で通ります**（2-13b-1）。`ensureBoss()` の
   `defeated` を必須にしたので、**10 か所の呼び出しが全部コンパイルで止まりました。**
4. **未読み込みの列を「無い」と読まないこと**（6 度目が 2-13b-1 の `exitPortalState`）。
5. **「ある」を 1 マスで決めないこと**（2-13b-1。`setVoxel` は黙って失敗します）。
6. **飛ぶモブの高さは、`FLY_SCAN`(8) より上に居ると測れません**（2-13a）。
   **浮く高さも種類ごと**（`MobDef.hover`）。
7. **「近づいてきた」「降りてきた」は、始まりの状態を先に作ってから測ること**（2-13a）。
8. **仮の判定は「土台があるか」に掛けないこと。成果物に掛けること**（これまで 9 度）。
   **2-13a で仮は 0 件になりました。**
9. **`solid` でないブロックには飛び道具が当たりません**（2-11b）。
10. **「本物の `World` で往復する」テストは、遠い所を先に読み込むこと**（2-11b）。
11. **場所の限界は「実測した最小値」ではなく「式」で決めること**（2-11a）。
12. **「面積で形を測る」判定は真円を通します**（2-10）。**形そのものを測ること。**
13. **座標の移し方は次元ごとに違います**（2-10）。
14. **「探して無ければ建てる」を別の次元に使い回さないこと**（2-10）。
15. **同じ向きが並ぶ列から中心を逆算しないこと**（2-9）。
16. **手で並べた試験場は、建てる側との食い違いを見つけられません**（2-9）。
17. **構造物の `extent` は「どこに掛かるか」の唯一の材料です**（2-8）。
18. **「総当りと突き合わせる」テストが、探す広さを守れないことがあります**（2-7）。
19. **レシピ名の長さに上限があります**（`test/crafting.test.ts`）。全角 12.5 文字まで。
20. **`liveState()` の渡し忘れは型で防げません**（`emptyState()` も `DimensionState`）。
21. **撃つものには「撃ち手の印」が要ります**（2-4b）。無いと自分の矢・火球で死にます。
22. **`world.seed` をセーブや場所の計算に使わないこと**（次元では塩を混ぜたあとの値）。
    種は `worldSeed`。
23. **`startWorld()` で `beds.clear()` しないこと**（次元を移るたびに湧き直し地点が消えます）。
24. **出る場所は「空気だけの一番低い所」を採ること**（溶岩の海の底や水中に出ると即死）。
25. **次元をまたぐ試験場は、次元ごとに違うマスを使うこと**（2-4c）。
26. **`test/audio.test.ts` が揺れます**（これまでに 4 つの判定が落ちました。どれも
    **書き出した波形がまるごと無音になる**形）。**この周は `audio.ts` も `sfx.ts` も
    1 行も触っていません**（`git diff HEAD~1 -- src/audio.ts src/sfx.ts` が空）。
    **赤が出たらまず `npm test` をもう一度**、2 回続けて**同じ判定**が落ちたときだけ
    本気で見てください（`world.test.ts` の p99 とまったく同じ扱いです）。
    **この周は 15 回以上まわして 1 回も落ちませんでした。**

## 次に取るもの

**ありません**（上の「`TASKS.md` に未着手のタスクが 1 つも残っていません」）。
無人の周がここへ来たら、`TASKS.md` 末尾の「取るタスクが無くなったら」に従って、
**何も実装せず・コミットせず**終えてください。

人が居る周は、上に書いた 4 つ（`REVIEW.md` の一括確認 / `TUNING.md` の一括調整 /
`RULES-INBOX.md` の取り込み / `master` へのマージ）から取ってください。

## 気付いているが、まだ手を付けていないもの

- **`main.ts` の余白が 15 行しかありません**（1485 / 上限 1500）。次に何かを足す周は、
  **まず減量から取ること**（`TASKS.md` にも書きました。次に出しやすいのは**左クリックの
  振り分け** —— `mousedown` の「モブとブロックのどちらが手前か」とスポイトを
  `use.ts` の `decideAttack()` へ）。**上限は上げないこと。**
- **クリア画面は「続ける」を押すと閉じるだけです**（記録も統計も出ません）。
- **体力バーはボス以外には出ません**（ふつうのモブの体力は見えないまま）。
- **出口ポータルを踏むと、オーバーワールドの「原点のあたり」に出ます**（本家は
  リスポーン地点）。ベッドへ戻す形にするなら `portaltravel.ts` が `beds.ts` を
  引き込むことになるので、独立したタスクにしてください。
- **エンドの外側の島（ゲートウェイ）はありません**（島は 1 つだけなので行き先がありません）。
- **ドラゴンはブレスを撃ちません**（近接だけ）。`projectiles.ts` の `breath` は表も
  当たり判定もあるので、`DRAGON.ranged` に 1 行足せば撃てます。**足すときは
  `test/mobs.test.ts` の「撃つのはブレイズだけ」を書き換えること。**
- **ドラゴンは何も落としません**（本家は経験値と卵。どちらもまだありません）。
- **ドラゴンとクリスタルを繋ぐ光の線（ビーム）はありません。**
- **クリスタルは爆発しません／上に乗れます／素手で 1 秒で壊せます。**
- **矢は拾い直せません**（刺さった矢は 60 秒で消えます）。
- **弓を引いても歩く速さは変わりません／耐久値もありません／火矢もエンチャントもありません。**
- **エンドの島の上にはモブが 1 体も湧きません**（空の明るさが 0.7 固定なので
  `spawnLight` が 10.5 になり、敵対の上限 7 を超えます）。**掘った穴の中は暗いので湧きます**
- **要塞は「部屋 1 つ」だけで、中に入る道がありません**（掘って降りる前提）
- **投げたエンダーアイは、近づいても落ちません／減りません**（本家は 20% で壊れる）
- **投げたエンダーアイは、ネザーやエンドでも「オーバーワールドの要塞」を指します。**
  `eyeShot()` に次元を渡して「オーバーワールド以外では null」にするのが素直です
- **ブレイズパウダーはかまどの燃料になりません**（本家は 120 秒。`smelting.ts` に 1 行）
- **エンダーパールは投げられません**（本家は投げた所へ瞬間移動）。
- **エンダーマンは中立ではありません**（本家は見つめると怒る）。中立にすると `hostile` の
  使われ方を全部見直すことになるので、**独立したタスクとして決めてください**
- **ネザーでも寝られます**（本家はベッドが爆発する）。**寝ても時間は「進みません」**
- **火球は火を点けません**（火そのものが無いので、ブロックとして足すところから）
- **`F3` の `biome` はネザーでもエンドでもオーバーワールドの値**が出ます（2-2 から既知）
- **ネザーラック・ソウルサンド・要塞の部屋の中にもゾンビが湧きます**（`spawnOn` を持たないため）
