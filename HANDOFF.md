# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- `npm test` **2977 件すべて成功**・`npm run typecheck` 緑。**件数は前の周と同じ 2977**
  （**この周は `src/**` も `test/**` も 1 行も触っていません** —— B の周だからです）
- **`npm run build` も `npm run bench` も走らせていません**（`src/**` に差分 0）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1464 行**
- **アイテム 99 種・`MAX_ITEM_ID` 134・111..255 の空きは 121・1..63 の空きは 9**
  （**この周は 1 個も取っていません。番号を取るのは C の周です**）
- **立方体 36 / 非立方体 69**・**クラフト 45 本 / 精錬 7 本 / 食べ物 8 種**・**モブ 9 種類**

## ⚠ 次に `main.ts` を触る周がまず見ること: 1464 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
**9 周続けて `main.ts` に判断を 1 行も足していないので、持ち越しです。**
**解けるのを待っているのはキューの 15 番（ミルクバケツ）と 16 番（サボテン）**で、
**どちらも `main.ts` に振り分けの 1〜5 行が要ります。** 先に判断を別ファイルへ出すこと。
**次に実装する 14 番（鉄・金・ダイヤのブロック）は 1 行も触りません。**

## この周でやったこと（AUTODEV 26 の B・クラウドの無人の周）

`AUTODEV-QUEUE.md` の未着手が **8 件**（3 件以上）・`AUTODEV-SPEC.md` が **`済`** だったので、
判定表を上から当てて **B（仕様）の周**。**キューの先頭 14 番（鉄・金・ダイヤのブロック）**の
仕様書を書きました。**`src/**` と `test/**` は 1 行も触っていません**（`git status` で確認済み）。

- **取る前にコードで数え直しました**（B の周の決まり）。`blocks.ts` に鉱石 3 つ（15 / 16 / 17）は
  ありますが、**「しまう形」の立方体は 3 つとも 1 つも無く**、`crafting.ts` にも 0 本でした
- **`items.ts` に 1 行も要らないことが数え直しで分かりました** —— `items.ts` の 386 行目の
  `for (const block of BLOCKS)` が **`variantOf === AIR` のブロック全部に同じ番号の
  アイテムを作ります**。**手で `item({...})` を足すと同じ番号が二重に登録されます**
  （仕様書の 2 節と 6 節に禁じ手として書きました）
- **触るのは `blocks.ts`（`def` 3 つ）と `crafting.ts`（6 行）と テスト 2 本だけ**です
- **仕様書は 119 行**（上限 120。`test/docs.test.ts` が見張っています）。
  **最初に書いた版は 120 行で赤くなりました** —— 2 か所を畳んで通しました

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
**`AUTODEV-QUEUE.md` の未着手が 8 件・`AUTODEV-SPEC.md` が `未着手` なので
→ 次は C（実装）の周**です。**`AUTODEV-SPEC.md` を全文読んでそのとおりにやること。**

- **取る ID は 135（鉄）/ 136（金）/ 137（ダイヤ）**。`ROADMAP.md` の予約表の
  「135..255 予備」から先頭 3 つで、**134（雪玉）まで使用済み**という裏は取ってあります
- **仕様書を要約してサブエージェントに渡さないこと**（`AUTODEV.md` の禁じ手）。
  **同じ番の中で結果が返らないなら、自分で実装すること**
- 渡す `rules/*.md` は **`rules/items-survival.md`（`blocks.ts` / `crafting.ts` /
  `items.ts` の 3 つに当たります）と `rules/blocks-shapes.md`**、テストを書くので
  **`rules/testing.md`** も。使うスキルは **`add-block`** です
- **見た目に出ます**（一覧に 3 枠・置ける立方体が 3 つ）ので **C-3（撮って `Read` で見る）は必須**

## ブラウザで見てほしいところ

**この周は絵が 1 枚も変わっていません**（`src/**` に差分 0）。前の周の持ち越しだけです。

- **雪玉（134）の手触り。** 一覧の末尾で**白っぽい 4 つ**（羽根・卵・糸・雪玉）から
  拾えるか、**投げても何も起きないのが物足りなくないか**
- 持ち越し: **クモを 3 匹倒すまで弓が作れない**道のり / **夜の敵対がゾンビとクモで半々**と
  **クモの速さ 6.0** / **クモが朝に燃えてよいか**（本家は燃えません）/ **投げた卵に
  使い道が無い** / **牛の胴が寸詰まりに見えないか** / **ステーキと革が一覧で
  見分けられるか** / **産卵の間隔 300〜600 秒** / **苗の緑と `GROW_SECONDS` 180 秒**

## この周で分かったこと

**決まりごとは 0 件です**（`rules/` を 1 行も直していません）。B の周で踏んだのは
**「仕様書が 120 行だと赤くなる」の 1 つだけ**で、これは既に `rules/testing.md` と
前の周の `HANDOFF.md` に書いてあったことです（**`wc -l` で 119 以下**）。

**次の C の周が踏みそうな穴を、仕様書の側に先回りで書いてあります**:
`items.ts` の二重登録 / `sound` を書かずに既定の `"stone"` で通すこと（**金属の音は
無く、足すと `audio.ts` の話になります**）/ `DROPS` に 1 行も足さないこと
（`variantOf` が `AIR` なら掘ると自分が落ちます）/ **しまう 9 個と戻す 9 個を
食い違わせないこと**（雪玉の周で `rules/items-survival.md` に据えた罠と同じ形）。

## 人がやること

- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 8 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**この周は 3 回走らせて音は 3 回とも緑でした**（羊毛 0.552 < 雪 0.716 <
砂 2.235・低い声 0.162 / 高い声 0.329 と、名指しされている落ち着いた値のまま）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
