# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-20）

- ブランチ **`loop/endgame`**。**`origin/master` より 11 コミット先**。**push していません**
  （`master` への push はユーザーが確認してからやります。ループはコミットまで）
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1220 件すべて成功**（12 秒前後）
- **進行（クリア導線）達成 2 / 13**（うち本物の判定 2 件）。
  達成: 溶岩がある / 黒曜石が手に入る
- ブロック ID **1..63 の空きは 20 個**。アイテム ID は 86 まで使用済み
- `src/main.ts` は **1328 行 / 上限 1500**

## この周でやったこと

1. **クリエイティブの全アイテム一覧（`E`）をコミット**（`9eb3d34`）。
   別セッションで実装され、未コミットのまま残っていたもの。ユーザー確認済み
2. **ALSA の無い箱で音のテストだけ飛ぶようにした**（`b8a3276`）。下の「踏んだ穴」を参照
3. **黒曜石（TASKS 1-2b-2）**。`blocks.ts` の `quenched()`（規則）＋ 新しい
   `liquids.ts` の `quenchAround()`（どのマスに効くか）＋ `main.ts` から 1 行

## 踏んだ穴（同じことを繰り返さないために）

**`npm test` が丸ごと落ちました。** `node-web-audio-api` のネイティブ `.node` は
読み込む瞬間に `libasound.so.2`（ALSA）を dlopen し、**無い箱では例外ではなく
プロセスごと落ちます。** 静的 import だったので、音と関係のない 1100 件まで道連れでした。

- 直し方は 2 つとも入れてあります: この箱に `libasound2t64` を入れたのと、
  `test/audio.test.ts` を**動的 import + try/catch** にして `harness.ts` の
  新しい `skip()` で音のグループだけ飛ばすようにしたこと
- **`skip()` したものは「1220 件すべて成功」の後にもう一度名前が出ます。**
  黙って飛ばすと、その領域の見張りが外れたまま何周も進みます
- **Routines へ移すときに効きます。** クラウドは毎周まっさらな箱なので、
  この形にしていないと毎回 1 周目で落ちます

## 未確認のもの（ユーザーにブラウザで見てもらう）

**`REVIEW.md` の「1-2b-2. 黒曜石」6 項目だけです。** それ以外は全部確認済みで、
節ごと消してあります（バケツ 7 項目・音 3 項目・クリエイティブ一覧 7 項目）。

## 次にやること

**`TASKS.md` の上から順に。** 次は **1-3（砂利 → 火打石 → 火打石と打ち金）**で、
これは見た目に関わらないので黒曜石の確認を待たずに取れます。
そのあとが **1-4（`portals.ts`）**で、こちらは黒曜石の上に乗ります。

- 1-3 の注意: 砂利のドロップは確率つき。**乱数のテストは 1 本を回し続けること**
  （`seeded()` を 1 件ずつ作ると線形合同法の 1 個目に引きずられて全部同じ側に転びます）
- ID は `ROADMAP.md` の予約表から。砂利は 44、アイテムは 87 から

## ループの運用について

`/loop` での同席は続いています。**Routines へ移すのは、タスクの粒度と停止条件が
機能することを 5〜10 周見てから**（`LOOP.md` の 5. と、上の「踏んだ穴」）。
いまのところ停止条件に当たったのは **黒曜石の入手導線をどうするか（難度の判断）** の
1 回だけで、ユーザーがバケツを選んで解決しました。
