# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-17・AUTODEV 92（B の周）のあと）

**この周は B（仕様）の周**で、**キューの先頭 34「ツタ」を仕様書 1 枚にしました**
（判定表の 2 行目 —— `AUTODEV-SPEC.md` が `状態: 済` で、未着手が 4 件あったため）。
**`src/**` も `test/**` も 1 文字も触っていません**（`git diff --stat` は
`AUTODEV-SPEC.md` と `AUTODEV-QUEUE.md` の 2 つだけ）。

- **34 を 34a / 34b に割りました。** 割った理由は**`supportFace` がブロック 1 つに
  1 向きしか持てない**ことです（`world.ts` の `canPlaceAt()` も `breakUnsupported()` も
  その 1 向きしか見ません）。本家のツタは**壁が無くても真上のツタにぶら下がる**ので、
  **「壁か、真上のツタか」の 2 つ目の候補**が要り、それは `canPlaceAt()` /
  `breakUnsupported()` / `supportsBlock()` に掛かる**置き方そのものの変更**です ——
  **自然生成（森の葉から垂らす）と合わせて 34b の 1 周**にしました
- **`stacksOnSelf` は代わりになりません**（サトウキビの旗）。あれは
  **`supportFace` の向きの自分**を見るので、`VINE_XN` に付けると**真下ではなく
  -X 側**のツタに付きます。**これは仕様書の禁じ手（2）に書いてあります**
- **34a は「はしご 145..148 とまったく同じ壁掛け 4 向き」**まで。**ID は 183..186**
  （大元 183 = アイテムも 183 / 184..186 は `variantOf: VINE`）で、
  **`climbable`（登れる）と `bladed`（刃物でだけ落ちる）の旗 2 つ**が中身です。
  **`main.ts` は ±0 行の見込み**（向きを決めるのは `blocks.ts` の `placedVariant()`）
- **色は先に測っておきました**（B の周のうちに測ると C の差し戻しが 1 回減ります）。
  **`top: 0x306d18`（本家のツタ）で、一覧 144 種に対していちばん近いのは
  トウヒの葉 38.2 / 葉 39.4**（判定は 20）。**C の周は 145 種で測り直すこと**
- **C の周が踏みそうな罠を 2 つ、仕様書に名指しで書きました**:
  **(a) `test/blocks.test.ts` の「isBladed が真なのもクモの巣だけ」（`bladed.length === 1`）は
  ツタ 4 向きで落ちます** —— **`=== 5` へ数え直し、件名に理由を書くこと**
  （`rules/testing.md` の決まり。**ゆるめるのではありません**）。
  **`isSticky` の 1 個はそのまま**であることが「旗を 1 つにまとめなかった」証拠です。
  **(b) `blocks.ts` の `LADDER_OPTS` のコメント「ツタも足場もまだ無い」**は
  この次の周で嘘になるので直すこと（**`LADDER_OPTS` を撒かず `VINE_OPTS` を別に作る**）

## 数字（2026-09-17・この周で測ったもの）

- `npm test` **3691 件すべて成功**（前の周と ±0。**`src/**` を触っていないので当然です**）。
  `npm run typecheck` 緑。**`npm run build` も `npm run bench` も走らせていません**
  （B の周で `src/**` に 1 文字も差分が無いため）。**音の跳ねはこの周は 0 回**
  （1 回目から緑。`rules/testing.md` の既知の揺れ）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1450 行 / 上限 1500**（**この周は 1 文字も開いていません**）
- **アイテム 144 種**・**`MAX_ITEM_ID` 182**・**立方体 42 / 非立方体 85**（どれも ±0）
- **1..63 の空きは 7**（57..63）・**111..255 の空きは 73**（**次に取るのは 183**）
- **`AUTODEV-SPEC.md` 119 行 / 上限 120**（**`wc -l` では 118**。テストは 1 多く数えます）・
  **`AUTODEV-QUEUE.md` 115 行 / 上限 150・未着手 5 件**（34 を 2 件に割ったため 4 → 5）

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。
**`AUTODEV-SPEC.md` を全文読んで、書いてあるとおりに 34a を実装すること**（要約して
サブエージェントに渡さないこと。要約すると「触らないファイル」と「禁じ手」が落ちます）。

- **触るのは 5 ファイル**（`blocks.ts` / `items.ts` の `MAX_ITEM_ID` / `test/blocks.test.ts` /
  `test/items.test.ts` と `test/mining.test.ts` / `tools/shot.ts`）。
  **`main.ts` も `worldgen.ts` も `crafting.ts` も 0 行**
- **先に `grep -l '"src/blocks.ts"' rules/*.md` を引くこと** ——
  **`blocks-shapes.md` / `items-survival.md` / `beds.md`** が当たります。
  **`test/**` を触るので `rules/testing.md` も**、**`tools/shot.ts` は `meshing-render.md`**
- **ID は 183..186**（`ROADMAP.md` の予約表の「183..255 予備 73 個」から）。
  **`MAX_ITEM_ID` を 186 へ伸ばし忘れると一覧に 1 枠も出ません**
- **ブロックなので撮ること**（C-3）。**`tools/shot.ts` に場面 `vine` を 1 つ**足して
  **`npm run shot -- vine ladders`**。**撮ったら `Read` で開いて見ること**

## ブラウザで見てほしいところ

**この周は 1 枚も撮っていません**（`src/**` に差分が無い B の周のため）。**次の C の周は
ツタを撮ること。** 人に見てほしいのはここです（上が新しい分）:

- **⚠ 金・ダイヤの防具の色（33b）**: **金 4 段が「金」に・ダイヤ 4 段が「ダイヤ」に
  見えるか**。**金の上着 `0xefd714` とズボン `0xf8b317` は絵の上でやや近く見えます**
  （測った隔たりは 38.7 / 26.7 で判定は通っています）
- **⚠ ダイヤ一式 80% 減が強すぎないか。** **防具は壊れない**ので、
  **「上限の固さが永久に持つ」状態**です（本家は傷んで直す手間があります）
- **⚠ 金を作る理由があるか。** **金一式 11 点は鉄 15 点より弱いのに 24 枚要ります**
  （本家のまま。ここには経験値もエンチャント台もありません）
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。
  **5 文字から折れて、一覧全体で 56 枠**（最長は 9 文字の「ネザーレンガハーフ」で 3 行）
- **⚠ 鉄の防具の色（33a）**: **「鋼の青」が鉄に見えるか**（**素直な銀灰は測って
  通らないと分かっています**）。**鉄一式で 60% 減が「固くなった」と感じられるか**
- **レンガ（32b）**: **`0xbc5d39` が「焼いたレンガ」に見えるか**と、**一覧で
  レンガブロック（`0xa4553f`）と並んだときに 2 つあると分かるか**（隔たり 26.0）
- **粘土（32a）**: **海に潜って粘土を見つけられるか**（海底の列の 7.2%・4x4 の塊）と、
  **水越しに砂と見分けられるか**。**粘土玉がやや青紫寄り**（`0xa7b4da`）
- **ハーフ 2 材質（31）**: 石レンガハーフが石ハーフ・丸石ハーフと見分けられるか
- **苗木（30b）**: **180 秒が待てる長さか**と、育った瞬間に木がぱっと出るのが唐突でないか
- **木炭（29）**: 一覧で「炭」に見えるか（`0x56473f`）/ **道具の修理（28）**: 気前よすぎないか
- **スケルトン（11）**: 矢の当たる高さ / 2 秒に 1 発 / 間合いを取らず殴ってくるのが変でないか
- **防具（27b）**: 革一式（防具点 7）で 28% 減が「固くなった」と感じられるか
- **フェンス（26a / 26b）**: **⚠ 52fps 未満で跳び越せる件**（余裕 0.0117。人の判断）
- **凍った海（25b）**: **⚠ 氷と雪の浜がほぼ同じ白**（氷 `0x8fc4f2`）/
  **氷（25a）**: 滑りの手ざわり（離してから 1 秒で 0.59m）。伸ばすなら `ICE_ACCEL_SCALE`（0.23）
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**のは承知（24b）
- **その他**: クモの巣の歩き 1/4 / 金のリンゴの見分け / 本棚 / リンゴ 200 枚に 1 個 /
  はしご 2.35 m/s / `CANE_GROW_SECONDS = 180` / **自然に生えたサトウキビは伸びません** /
  砂糖・ボウル・シチューの見分け / 森のキノコの密度 / サボテンの痛さ / 牛を搾る /
  クモの速さ 6.0 / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## 分かったこと（**この周で据えた決まりごとは 0 件**）

**`rules/` は 1 文字も直していません** —— この周で踏んだ落とし穴が 0 件だったためです。
代わりに**次の周のための下ごしらえを 2 つ**しました（どちらも仕様書の中）:

- **B の周のうちに色を測っておくと、C の差し戻しが 1 回減ります。** 33a は
  「素直な銀灰が 20 を割る」のを C の周で踏んで測り直しています。**この周は
  `npm test` を通さずに `esbuild` で `src/items.ts` だけを束ねて測りました**
  （`allItemIds()` と `itemColor()` を呼ぶ 10 行。**`src/**` には 1 文字も書きません**）
- **`AUTODEV-SPEC.md` は `wc -l` で 118 行に収めました**（テストは 1 多く数えるので
  119 / 120）。**4 回続けて踏んだ上限**なので、**書き終えたら必ず `wc -l` で数えること**

## 人がやること

- **⚠ 設計の判断（63 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 2 件**（**35 骨粉**・`use.ts` の 19 通り目 / **24b ケーキをかじる**・
  最低 7 行）。**次の周（34a ツタ）は `main.ts` 0 行の見込み**なので、
  判断が下りなくてもあと 1 周は止まりません
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
  （足すなら `vitals.ts` → `inventory.ts` の配線で、**それだけで 1 周**）。
  **33b が入って「ダイヤ一式 80% 減が永久に持つ」**になった分、いちばん効く持ち越しです
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**（手ざわりは人の判断）
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **⚠ 一覧と防具枠の名前が折れて枠に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**・**凍った海の白 2 色**（手ざわりの判断）
- **⚠ 音の揺れを測り方で直すか**（`test/audio.test.ts`）。**この周は跳ねませんでした**。
  **直すなら測り方**（窓を長く取る・複数回鳴らして中央値）で、**判定はゆるめないこと**。
  **キューに足すのは A の周の仕事**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 92 周目。**周の数は
  `git log --oneline --grep='AUTODEV'` では足りません** —— C の周の件名には
  `AUTODEV` が入らないので、本文まで見ること）
- **`loop/endgame` に `rules/` の形が届いていません**（**この周に確かめました** ——
  あちらのルート直下に `rules/` が無く、`master` から 50 コミット遅れています）。
  再開する前に `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと。**
**⚠ 枠を数えるときは `#creative .slot` を使うこと**（素の `.slot` はホットバーと
インベントリの 36 枠まで拾います）。**⚠ `/opt/pw-browsers` には
`chromium_headless_shell-1194` も居ます** —— `startsWith("chromium-")` で弾くこと
（`tools/browsershot.mjs` の `findChrome()` がそうしています）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**34a のツタもこれに当たります** ——
**自然生成は 34b なので、場面 `vine` を足さないと 1 枚も写りません**
（**`ladders` の場面を写すのがいちばん安い**）。**時間が経たないと見えないもの**
（育つ・燃える・溶ける）は、**その場で `update()` を回してから撮ること**。
**⚠ 防具を着た姿は撮れません**（一人称視点なので自分の体が写りません）。
