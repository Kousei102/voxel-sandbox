# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-05・AUTODEV 34 のあと）

- `npm test` **3059 件すべて成功**（**この周で ±0**。B の周なので判定は 1 つも増えません）・
  `npm run typecheck` 緑。**`src/**` に 1 行も差分が無いので `npm run build` は走らせていません**
- **音の一群は 3 回とも緑**（`rules/testing.md`）。落ち着いた値も前の周と一致
  （羊毛 0.552 < 雪 0.716 < 砂 2.235 / 低い声 0.162 < 高い声 0.329）
- **`npm run bench` は走らせていません**（生成もメッシュ化も 0 行）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**
- **`main.ts` 1445 行**（上限 1500・**止まる目安 1450 まで残り 5 行**。この周で ±0）
- **アイテム 105 種・`MAX_ITEM_ID` 140・111..255 の空きは 115・1..63 の空きは 9**
- **立方体 39 / 非立方体 71**・**モブ 9 種類**・**レシピ 51 本**・**食べ物 8 種**（どれも変わらず）

## この周でやったこと（AUTODEV 34 の B・クラウドの無人の周）

`AUTODEV-SPEC.md` が **`状態: 済`** で、`AUTODEV-QUEUE.md` の未着手が 5 件（3 件以上）
だったので判定表の 2 行目に当たって **B（仕様）の周**。**`src/**` は 1 行も触っていません。**

- **キューの先頭 17b（ボウルとキノコシチュー）を `AUTODEV-SPEC.md` に落としました**
  （**`wc -l` で 119 行**。7 項目すべてあり）。取る前にコードで数え直して
  **`bowl` / `stew` / ボウル / シチューは `src/**` にも `test/**` にも 0 件**を確認済み
- **決めた中身**: **141 = ボウル / 142 = キノコシチュー**（どちらもアイテムだけ。
  ブロックは 1 つも増えません）/ レシピは本家と同じ 2 本（板 3 個の V 字 → ボウル 4 個 /
  ボウル + 赤 + 茶 の形なし → シチュー 1 個）/ **`FOODS` は本家の 6・7.2**
- **`emptyAfterEating()` を `items.ts` に置く形**に決めました。**`main.ts` は 3 行だけ**
  （引いて、`NO_ITEM` でなければ `inventory.setSelected(戻るもの, 1)`）で、
  **`test/ui.test.ts` に「`main.ts` に `BOWL` も `MUSHROOM_STEW` も無い」を 1 件足させます**
- **色は先に実測して決めました**（`allItemIds()` を舐める使い捨てスクリプト）。
  **ボウル `0x7a4a24` / シチュー `0xf0dcb4`** —— 赤 139・茶 140 と合わせた 4 つが
  どの 2 つも RGB 60 以上（83〜237）、既存 105 種からも 20 以上（21.8 / 23.9）
- `AUTODEV-QUEUE.md` の**数え間違い（未着手「6 件」→ 5 件）**も直しました

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が `状態: 未着手` なので判定表の 3 行目です。
**仕様書を全文渡すこと**（要約すると「触らないファイル」と禁じ手が真っ先に落ちます）。

- **渡す `rules/`**: `items-survival.md` / `vitals.md` / `use.md` / `testing.md`。
  スキルは **`add-block`**
- **`main.ts` は 3 行まで = 1448 行**（止まる目安 1450 まで残り 2 行になります）。
  **18・19 はどちらも配線が要るので、次の周のあとは下の「人がやること」が効きます**
- **次に取る ID は 141 / 142**、そのあとは **143 から**（`ROADMAP.md` の予約表）

## ブラウザで見てほしいところ

**この周は絵を撮っていません** —— `src/**` に 1 行も差分が無く、**見た目に出るものが
1 つもない**からです（`AUTODEV.md` の C-3 は「見た目に出るものを触った周だけ」）。

- **17b で人に見てほしくなること**（実装が済んだら）: **ボウル `0x7a4a24` と
  シチュー `0xf0dcb4` が一覧で見分けられるか**（**色は DOM なので `npm run shot` に
  写りません**。数値では赤・茶と合わせて 60 以上離してあります）と、**シチューが
  焼き鳥と同点の 6 / 7.2 で、材料 3 つに見合うか**（`TUNING.md`）
- 持ち越し（まだ誰も見ていません）: **森を歩いたときのキノコの密度**（森 0.015 /
  針葉樹林 0.01 は暫定。実測で 64x64 に森 57 本 / 針葉樹林 41 本）/ **走っていて
  赤・茶を草むらと取り違えないか** / **サボテンに当たったときの痛さ**（0.5 秒ごとに 1）/
  **牛を搾る / ミルクを飲んで毒が止まるか** / **ミルクバケツの色（`0xeaf2f8`）が
  羽根・卵・糸・雪玉と見分けられるか** / かまど・チェスト・作業台を開いて閉じる経路 /
  雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と見分けられるか /
  **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
  クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに
  見えないか / 産卵の間隔 300〜600 秒

## この周で分かったこと（決まりごと 1 件・`rules/` に据えました）

1. **`rules/testing.md`**: **文書の行数の上限（`wc -l` + 1 で数える）を 3 回目に踏みました。**
   決まりごと自体は前からあり、`paths` にも `AUTODEV-SPEC.md` が入っているのに、
   **`grep -l '"src/…"' rules/*.md` を `src` のファイルでしか引かなかった**ので目に入りません
   でした（120 行ちょうどで書いて `npm test` が赤 → 119 行に削って緑）。
   **「B の周は書くファイルそのもので引く」を 1 行足しました。**

**もう 1 つ、仕様書の 4. に書いてあるので `rules/` には出さないこと**:
**一覧のアイテム色は 105 種でもう密で、「既存のどれからも 40 以上」は取れません。**
暖色を総当たりしても最大 30 台で、取れるのは目に痛い原色だけでした。**だから色の判定は
「並べて見る数個の中で 60 以上・全体からは 20 以上」の 2 段**にしてあります。

## 人がやること

- **設計の判断を 1 つお願いします（8 周持ち越し・18 番で効きます）: `main.ts` を 2 つに
  割ってよいか。** いま **1445 行で、17b を入れると 1448 行**（止まる目安 1450 まで残り 2 行）。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **17b までは通りますが、18（サトウキビ）と 19（はしご）はどちらも配線が要ります。**
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 18 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
