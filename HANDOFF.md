# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-08・AUTODEV 60 のあと）

- `npm test` **3322 件すべて成功**（**+27**）・`npm run typecheck` 緑・`npm run build` 緑
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていないため。
  触ったのは `blocks.ts` の表 2 本と `player.ts` の移動だけ）
- **音の一群は 1 度だけ跳ねました**（`rules/testing.md` のとおりもう一度走らせて緑。
  最後に走らせた 2 回は 3322 件とも緑）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行（±0。この周は 1 文字も開いていません）**
- **アイテム 118 種・`MAX_ITEM_ID` 156・111..255 の空きは 99・1..63 の空きは 9**
- **立方体 41 / 非立方体 78**・**モブ 9 種類**・**レシピ 60 本**・**精錬 7 本**・
  **燃料 8 種**・**食べ物 11 種**・**バイオーム 10**・**構造物 2**・**次元 3**・
  **`DamageCause` 9 種**（立方体が +1 のほかは ±0）
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 120 行**（未着手 6 件）

## この周でやったこと（AUTODEV 60 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**氷（25a・ブロックと滑りだけ）を実装しました** —— **ID は 156 を 1 個だけ**、
**差し戻し 0 回**、**コミット 1 つ**。

**サブエージェントは 1 つも立てていません。** `AUTODEV.md` の C-1 の但し書き
（同じ番の中で返らないなら自分で実装する）と `rules/testing.md` の
「点検役に `git` を打たせない」を合わせて、**実装も点検（C-2）も自分でやりました**
（点検は `git diff` を全部読む形。テストは自分で 3 回走らせています）。

**予告どおり `main.ts` は ±0 行**・**`worldgen.ts` と `biomes.ts` は 0 行**・
**`SaveData` は 1 バイトも増えていません**。触ったのは `blocks.ts`（`def` 1 つと旗 2 つ）/
`items.ts`（`MAX_ITEM_ID` と `DROPS` の 2 行）/ `breaking.ts`（`setVoxel` 1 行）/
`physics.ts`（`bodyStandsOn()` を新設）/ `player.ts`（`onSlippery` と定数 3 つ）/
`tools/shot.ts`（場面 1 つ）です。

**踏んだ落とし穴は 2 つで、どちらもその周のうちに `rules/` へ据えました**（下）。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので判定表の 2 行目です
（未着手が 6 件あるので A ではありません）。**キューの先頭 = 25b（凍った海）**の
仕様書を書くこと。

- **ID は 0 個**（氷 156 はもう在ります）。**次に番号が要るときは 157 から**（`ROADMAP.md`）
- **`biomes.ts` に「凍った海」を 1 つ足すのが唯一の道**です —— `resolve()` が
  **海面より低い列を気温に関わらず `OCEAN` に潰す**ので、雪の浜の隣の海も `OCEAN` です
  （2026-09-08 の B の周でコードを追った結果。`rules/worldgen.md`）
- **氷の側はもう全部そろっています**（旗・壊すと水・滑り・色・`tools/shot.ts` の場面）。
  25b で足すのは**バイオーム 1 つと、海面 1 段を氷にする `worldgen.ts`** だけのはず
- **`blocksSky` を氷に書かないこと**（既定の false のまま）——
  書くと**氷の下の海が真っ暗**になります。25b がいちばん踏みやすい罠です

## ブラウザで見てほしいところ

**この周に絵で確かめたのは `npm run shot -- ice` の 1 枚です**（`tools/shot.ts` に
`ice` の場面を足しました。**7x7 の板の下に市松（石と板）を敷いてある**のは
「1 マスの水たまりで終わりにしない」と同じ理由）。**見えたのは 4 つとも良好**:
**市松が透けている**（半透明が効いている）/ **隣のガラスと色でも濃さでも見分けが付く** /
**石の上の 1 個に面の欠けも裏返りも無い** / **7x7 の内側に面が出ていない**。
**直すところはありませんでした。**

**新しく見てほしいところ（氷）**: **滑りの手ざわり** —— 走り出しが遅く
（0.2 秒で 2.76 m/s。土は 5.2）、**離してから 1 秒で 0.59 m 進みます**（土は 0.099 m）。
**本家の氷はもっと長く滑ります** —— 伸ばすなら `ICE_FRICTION` ではなく
**`ICE_ACCEL_SCALE`（0.23）を下げるほうが効きます**（理由は `TUNING.md`）。
ほかに: **`0x8fc4f2` が一覧でガラス・雪・ダイヤ鉱石と見分けられるか** /
**`alpha` 0.6 が「氷」に見えるか**（薄いとガラス・濃いと 25b で海の底が見えない）/
**壊すと水になる手ごたえ**（`npm run build` 後の公開サイトでクリエイティブから置いて壊す）。

**持ち越し（ケーキ）**: **上面 `0xffd0e4` が「ケーキ」に見えるか** /
**側面のスポンジ `0xe8c9a0` と下面 `0xd9b98a` の 3 色がケーキに読めるか** /
**一覧で薄紅が羊毛・卵・雪と見分けられるか**。**作れるのに使い道が無い**のは承知の上です（24b）。

**持ち越し（クモの巣）**: **中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか** /
**素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか**。

**持ち越し（まだ誰も見ていません）**: 金のリンゴ `0xf0a800` が金インゴット・
ブレイズロッドと見分けられるか / 金インゴット 8 個に「体力 4 回復」が見合うか /
本棚が「本の詰まった棚」に見えるか（側面 `0x9c5064` がかなり赤紫）/ 紙 `0xd4e0ec` が
雪玉と 25.5 で見分けられるか / リンゴの色 `0xe0342c` が赤キノコ・生牛肉と見分けられるか /
葉 200 枚に 1 個（本家の 0.5%）が待てる確率か / 2.35 m/s の登りが待てる速さか /
手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか / はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか / `CANE_GROW_SECONDS = 180` が
待てる長さか / **自然に生えたサトウキビは伸びません**（理由は `TUNING.md`）/
**⚠ 前から遊んでいる世界だけの見え方**: 前に浜のサトウキビを壊した場所は、下の 1 マスだけが
空いて上の段が浮いて見えることがあります / 一覧で砂糖が雪・羽根・卵・糸と見分けられるか /
ボウルとシチューの見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
森を歩いたときのキノコの密度 / サボテンに当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか /
かまど・チェスト・作業台を開いて閉じる経路 / 雪玉を投げても何も起きない /
**一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 2 件**）

**どちらも `rules/` に `Edit` で据えました**（`.claude/rules/` ではなくルート直下）。

1. **`rules/blocks-shapes.md`**: **「上に立っている」は `bodyTouches()` では永久に偽**。
   `solid` なブロックの上に立つと体は**そのマスと 1 度も重なりません**（`moveBody()` が
   `contactMax[1]` で止めるので、足元は上面ちょうど）—— だからサボテン・はしご・
   クモの巣の走査を写すと、氷の上でどう立っても偽になります。足元版の
   **`bodyStandsOn()`**（深さは接地判定と同じ `EPS * 4` 下）を足しました。
   **足元を 2 段まで見ないこと**（隣のマスの氷に乗ったことになります）。
2. **`rules/items-survival.md`**: **`tool:` を書いても `minTier` を書かなければ素手も「適正」**
   （`canHarvest()` の早い return）。**「素手は 5 倍」は `minTier` を書いたブロックだけ**の話で、
   氷は素手 0.75 秒 / 木のツルハシ 0.375 秒（道具は**速さだけ**を変える）。
   **この周で唯一赤くなったのはこれで、間違っていたのは実装ではなくテストの期待値**でした。
   ついでに**「何が落ちるか（`items.ts`）」と「壊したあとに何が残るか（`blocks.ts`）」は
   別の表**であることも同じファイルに据えました。

## 人がやること

- **設計の判断（33 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**で、
  上限 1500 まで 51 行です。**次の 25b も `main.ts` は ±0 行の見込み**（生成側だけ）なので、
  **この判断を待たずに次の周は回せます**。待っているのは **24b（ケーキをかじる。最低 7 行）**
  と **27b（革の防具。セーブのキーで 1 行）** の 2 件です。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
  （**判定の意味が変わるので無人の周では触れません**）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 43 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
**ただし自然生成しないブロックは browsershot には 1 マスも写りません** ——
氷・ケーキ・クモの巣・本棚は `npm run shot -- <名前>` が唯一の経路です。
