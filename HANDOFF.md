# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-04）

- ブランチ **`loop/devgame`**。**この周は無人（クラウドの定期実行）で、C（実装）の周**でした。
  **`master` への push とマージは引き続き禁止**
- `npm test` **2934 件すべて成功**（この周の前は 2907 件。**+27**）。`npm run typecheck` と
  `npm run build` も緑。**`npm run bench` は走らせていません**（生成もメッシュ化も触っていないため）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**・**`main.ts` 1463 行**（前の周のまま）
- **アイテム 97 種・`MAX_ITEM_ID` 132・111..255 の空きは 123・1..63 の空きは 9**

## ⚠ 次に `main.ts` を触る周がまず見ること: 1463 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」3 番（1450 行）を越えたままです**（上限 1500）。
**この周は `main.ts` を 1 行も触っていないので、持ち越しです。**
**次に触る周は、先に判断を別ファイルへ出すこと。**

## この周でやったこと — 牛（AUTODEV 20）

`AUTODEV-SPEC.md` が `状態: 未着手` だったので **C（実装）の周**。当たった 1 種類だけをやりました。

- **草地に牛が湧いて、倒すと生牛肉 1 個と革 1 個が落ち、かまどで焼くとステーキになります。**
  **形は鶏（AUTODEV 16）と同じ**で、違うのは 2 山目（`MobDrop.extra`）が最初から付くところだけ
- 触ったのは **`items.ts` / `mobs.ts` / `smelting.ts` の 3 本だけ**（仕様書のとおり）。
  **`main.ts`・`mobmesh.ts`・`mobrender.ts`・`drops.ts`・`crafting.ts` は 1 行も触っていません**
- **ID は 3 個**（**130 = 生牛肉 / 131 = ステーキ / 132 = 革**）。`ROADMAP.md` の予約表に
  「実装済み」で足し、予備を **133..255** に直しました
- **差し戻し 0 回。** サブエージェントは立てず、親が実装して `git diff` を自分で点検しています
  （背景で起動すると番を閉じてしまうため。`AUTODEV.md` の C-1 / C-2）

## 撮って直したもの（C-3）

**撮った絵**: 定番 5 枚（`node tools/browsershot.mjs`。**console のエラー 0 件**・WebGL 2.0・
見た目は前の周と同じ）/ `docs/browser-shots/mobs.png` と切り出した **`cow.png`** /
本物のブラウザで **`creative-beef.png`**（一覧の枠 94 → **97 個**）。

**絵を見て 2 回直しました**（どちらも直す前は `npm test` が全部緑でした）:

1. **頭と体が 1 枚の板に見えました** —— 同じ茶色で、頭は体より 1.1px しか前に出ておらず
   面の向きも同じなので陰でも分かれません。**顔に白い筋**（目と同じ、前面から 0.1px の薄い箱）で分けました
2. **その筋を頭の幅いっぱいに引いたら、同じ高さの角と 1 本の白い帯**になり、
   **角が角に見えなくなりました。** 筋を ±1.5px に細めています

**2 件とも `rules/mobs.md` に据えました**（「輪郭を外へ出せない部位は面に別の色を置く」
「隣り合う淡い色どうしは間に地の色を残す」）。

## ブラウザで見てほしいところ

- **牛の胴が寸詰まりに見えないか。** 本家の 18px ではなく **12px** です ——
  当たり判定（0.9 x 1.4）に前後とも収めるためで、**`longBody` の例外（豚と羊だけ）に
  牛を足さない**という線引きのぶん、本家より短くなっています
- **ステーキ（rgb(143,82,48)）と革（rgb(160,106,65)）が一覧で見分けられるか。**
  隣り合う茶色どうしで、焼き豚（rgb(196,118,63)）とも系統が近いです
- **ステーキが焼き豚と同点（空腹 +8 / 満腹度 +12.8）でよいか。** 本家がそうなので
  そのまま置きましたが、「いちばん強い食べ物」が 2 つになりました

## この周で分かったこと（`rules/testing.md` に据えました）

**`test/audio.test.ts` の「cutoff が低い材質ほどこもって鳴る（羊毛 < 雪 < 砂）」は揺れます。**
**15 回走らせて 2 回**赤くなり、**羊毛の明るさだけが 0.552 → 9.739 に跳びます**
（雪 0.716 / 砂 2.235 は動かない）。**赤が出たらまずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。**判定はゆるめていません** —— 直すなら測り方のほうです。

## 人がやること

- **`master` へのマージ**
- **`loop/endgame` に `rules/` の形が届いていません**（前の周からの持ち越し）。
  `master` 経由か cherry-pick かを決めてください
- 遊んで決めてほしいことの持ち越し: **投げた卵に使い道が無い**（音もヒヨコも無し）/
  **卵と羽根がどちらもほぼ白** / **産卵の間隔 300〜600 秒** / **矢が 2x2 で作れなくてよいか** /
  **苗の緑と `GROW_SECONDS` 180 秒と `#status` の位置**。**この周のぶんは上の 3 つ**

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 済`**、`AUTODEV-QUEUE.md` の未着手が **3 件**（9 / 10 / 11）なので
→ **次は B（仕様）の周**です（キューの先頭は **9「クモと糸」**）。

- **B の周は `src/**` を 1 行も触らないこと。** 取る前に、その 1 件だけコードで数え直すこと
  （`spider` / `string` / `クモ` / `糸` を `src/**` と `test/**` で引く）
- **ID は `ROADMAP.md` の予約表の「133..255 予備」から上から詰めて取ること**
  （**111 以降はブロックとアイテムで 1 本の番号列**。片側だけ見て取らないこと）
- **クモを足す周（C）は、`test/mobs.test.ts` の `census()` の `counts` と `show()` に
  名前を足すこと**（忘れると `counts[name]++` が `NaN` になり、数えているつもりで緑になります）。
  **`test/blocks.test.ts` の「共有帯のアイテムは 19 個・132 まで」と「111..255 の空きは 123」は
  必ず赤くなります** —— **ゆるめるのではなく数え直すこと**
- **敵対モブなので `PASSIVE_KINDS` ではなく `hostile: true`**（`HOSTILE_KINDS` は表から
  作られるので 1 語も足しません）。**形は当たり判定に全部収めること**（`longBody` の例外は
  豚と羊だけ。そこへ足さない）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
