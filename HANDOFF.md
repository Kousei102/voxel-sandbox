# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-02）

- ブランチ **`loop/devgame`**（push 済み）。**`master` への push とマージは引き続き禁止**
- **この周は C（実装）**。`AUTODEV-SPEC.md` の「育つ器（`crops.ts`）と実った小麦」を
  **全文そのまま**実装しました（7 項目とも守っています）
- `npm run typecheck` / **`npm test` 2783 件すべて成功**（2707 → +76）/ `npm run build` も通ります
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **`src/main.ts` は 1450 行 / 上限 1500**（`npm test` の数え方。**下の「次にやること」を先に読むこと**）
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 131**（この周で 123・124 の 2 個を使用）
- **アイテムは 89 種**、`MAX_ITEM_ID` は **124**
- **`AUTODEV-SPEC.md` は `状態: 済`**、`AUTODEV-QUEUE.md` の 13 番は消しました → **次の周は B（仕様）**
- **`RULES-INBOX.md` は未取り込み 13 件**（前の 4 周ぶんの 11 件 + **この周の 2 件**）

## この周でやったこと（無人・C の周 1 種類だけ）

**植えた苗が `GROW_SECONDS`（180 秒）で実り、掘ると小麦が採れるようになりました。**

- **`src/crops.ts`（新規）**: 「位置ごとに状態を持つブロック」の **3 つ目**（かまど・チェストと
  同じ器の形）。**育ち具合はワールドに書かず、`"x,y,z"` → 育った秒数の表で持ちます** ——
  本家の 8 段階は ID を 8 個食うので、**2 段階に絞って番号を 1 個で済ませました**
- **ブロック 123 = 実った小麦**（`variantOf: WHEAT_CROP`）/ **アイテム 124 = 小麦**
- **セーブに増えたのは省略可キー `crops` の 1 つだけ**（`version` は **1 のまま**）。
  次元ごとに持つので、ネザーへ行って戻っても育ち具合は残ります
- `main.ts` は **+5 行の配線だけ**（秒数も条件も 1 つも書いていません。`test/crops.test.ts` が
  `main.ts` に `GROW_SECONDS` も `180` も無いことを見張っています）

**仕様書から 1 点だけ変えました**（`docs/autodev-log.md` と `RULES-INBOX.md` に理由あり）。
仕様の 5-3 は「`baseBlock(...) !== WHEAT_CROP` なら忘れる」でしたが、**実った小麦は
`variantOf: WHEAT_CROP` なので `baseBlock()` が苗を返し、「もう実っている」を見分けられません。**
そのまま残すと `World.setVoxel()` が「同じ値」で false を返し続け（`world.ts:181`）、
**二度と消えない印が毎フレーム書き込みを試み続けます。** 素の `getVoxel(...) !== WHEAT_CROP`
に直してあります（テストの「もう実っているマスの印は忘れる」が押さえています）。

## ブラウザで見てほしいところ（見た目に出るもの）

**撮って自分の目で見ました**（C-3）。`tools/shot.ts` に場面 `crops` を足したので、
**180 秒待たずに苗と実りを並べられます**（`npm run shot -- crops`。1〜2 秒）。
撮った絵は **`docs/browser-shots/wheat-ripe.png`**（**左が実り・右が苗**）。
本物のブラウザ（`node tools/browsershot.mjs`）でも 5 枚撮り直し、**WebGL 2.0 /
`gl.getError()` = 0 / 例外 0 件**でした。

**見てほしいのは 2 つです:**

1. **実った黄色（`0xd8c26a`）と苗の緑（`0x6f8f3f`）の対比。** 絵では実ったほうが一目で
   分かりますが、**苗の緑は相変わらず草原に紛れます**（前の周からの持ち越し）。
   苗を暗く／青寄りにするか、`boxes` を低くして背の高さで伝えるか、決めてください（`TUNING.md`）
2. **180 秒（3 分）で実るのが待てる長さかどうか。** 本家は 20 分前後ですが、ここは
   **チャンクが読み込まれている間しか育ちません**（離れた畑は止まります）

## 人がやること

- **`RULES-INBOX.md` の 13 件の取り込み**。**わざと食い違わせてある組**（「10 件」対「11 件」対
  「**13 件**」など）は **いちばん新しいほうを採ってください**。この周の 2 件は
  `stateful-blocks.md` の新しい節（`paths` に `src/crops.ts` を足すこと）と
  `meshing-render.md` への 1 項目です
- **`#status`（通知）がインベントリの説明文とメニューのモード行に重なる件**はまだ直っていません
  （`#status` は `bottom: 118px` 固定。`style.css:674`）。`REVIEW.md` に積んであります
- **`GROW_SECONDS` 180 秒でよいか**と**苗の色**（上の「ブラウザで見てほしいところ」）
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-QUEUE.md` の未着手は **7 件**（3 件以上）で、**`AUTODEV-SPEC.md` が `状態: 済`**
なので → **次は B（仕様）の周**です。キューの先頭は
**「14. 収穫で種も戻る（1 回の採掘で 2 山落ちる器）」**で、`src/**` は 1 行も触りません。

**先に読むこと: `main.ts` は 1450 行で、`AUTODEV.md` の停止条件 3 の線にちょうど乗りました。**
次に C の周が来たときは、**1 行でも足す前に判断を別ファイルへ出すこと**（上限 1500 までは
余裕がありますが、停止条件は 1450 です）。**14 番は `main.ts` に 1 行も要らない見込み**
（`Drop` と `breaking.ts` の話）なので、仕様書にそう書いておくと次の C が楽になります。

- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **サブエージェントは使っていません。** `AUTODEV.md` の C-1 は「同じ番の中で返らないなら
  自分で実装すること」で、前の 3 周とも `run_in_background: false` を渡しても背景で起動しました。
  **点検（C-2）も親が `git diff` を全部読むこと**（飛ばすより確実です）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）

**音のテスト（`test/audio.test.ts`）の揺れはこの周に 1 度出ました**（「声色（音程の倍率）が
波形に出る」が 1 件だけ赤 → **走らせ直したら緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
