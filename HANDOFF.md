# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（`loop/devgame`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-03）

- ブランチ **`loop/devgame`**（**この周のコミット 1 つを push 済み**）。
  **`master` への push とマージは引き続き禁止**
- **この周は無人の B（仕様）の周**でした。**`src/**` と `test/**` は 1 行も触っていません**
  （`git diff` は `AUTODEV-SPEC.md` と `AUTODEV-QUEUE.md` と このファイルだけ）
- `npm test` **2858 件すべて成功**（前の周と同じ。B の周なので増えません）。
  `npm run typecheck` も緑。**`npm run build` と `npm run bench` は走らせていません**
  （`src/**` を触っていないため）
- **進行（クリア導線）達成 13 / 13。仮の判定は 0 件**
- **ブロック / アイテム ID: 1..63 の空き 9 / 111..255 の空き 127**（**この周は 0 個使用**）
- **アイテムは 93 種**、`MAX_ITEM_ID` は **128**。**モブは 7 種類**。`SaveData` は `version` 1
- **`AUTODEV-SPEC.md` は `状態: 未着手`**（119 行 / 上限 120）→ **次の周は C（実装）**
- **`RULES-INBOX.md` は未取り込み 0 件**（**8 件とも 2026-09-03 に人が居る周で取り込み済み**。
  当てた先は `vitals.md` / `items-survival.md` / `mobs.md`）
- `REVIEW.md` の未着手は 0 件

## この周でやったこと — 仕様「鶏が卵を産む（`MobDef.laying`）」

`AUTODEV-QUEUE.md` の先頭 **7c（卵）**を取り、**その 1 件だけコードで数え直しました**
（`AUTODEV.md` の B）。数えた結果は仕様書の先頭に書いてあります —— **`src/**` に `EGG` の名も
`egg` の綴りも 1 つも無く**、`mobs.ts` の「卵」は `DRAGON.drop` のコメント 1 行だけでした。

**7c はキューの但し書きどおり 2 件に割りました**（**1 周で閉じない大きさだったため**。
`AUTODEV.md` の B の「120 行に収まらない仕様」）:

- **7c-1 産む側（この仕様書）** —— `MobDef.laying` の表 1 本と `Mob.layTimer` だけ。
  **描画も音も 1 つも増えません**（`unverifiable-pair` は不要）。アイテム **129 = 卵**を 1 個使います
- **7c-2 投げる側（キューに残っています）** —— `projectiles.ts` の表に 1 行 +
  `use.ts` の投げる判断 + `main.ts` の配線。**ID は 0 個**。**本家の「1/8 でヒヨコ」は
  別件**（子モブの仕組みがまだ無いので、取るなら別の行として数え直すこと）

**仕様書で先に潰しておいた穴**（実装の周はここを読んでから始めること）:

1. **`spawn()` の `layTimer` を 0 から始めないこと** —— 湧いた瞬間に全員が 1 個産みます
   （`shootTimer` / `teleportTimer` を `cooldown` から始めているのとまったく同じ理屈）
2. **`lay()` を `think()`（5Hz）の中に置かないこと** —— 遠くて判断を通らない個体で
   時計が進みません（`woolTimer` を毎フレーム減らしている理由と同じ）
3. **`onDrop` の説明文を直すこと** —— いままで「倒したとき」と「刈ったとき」だけだった
   受け口に、3 つ目の入口（産んだとき）が増えます。**`main.ts` の配線はもう通っている**ので
   （`drops.burst()` まで既にできています）**`main.ts` は 0 行**です

## ブラウザで見てほしいところ（見た目に出るもの）

**この周は `src/**` を触っていないので、撮っていません**（`AUTODEV.md` の C-3 は
実装の周だけ）。**次の C の周は撮ること** —— クリエイティブの一覧に卵が 1 枠増えます。

人の目でしか決まらないものは、前の周からの持ち越しが 4 つあります:

1. **矢が 2x2 で作れなくなってよいか**（AUTODEV 17 でそうなりました）。戻すなら
   `crafting.ts` の 1 行（`TUNING.md`）
2. **羽根のほぼ白（`0xe8e4dc`）が一覧の中で見分けが付くか**（`E` を押して末尾へ。
   `docs/browser-shots/creative-feather.png`）。**卵（`0xf7f0e0` の予定）も隣に並びます**
3. **鶏が「鶏らしいか」**（豚・羊と並べて。声 1.8 の高さも耳で）
4. **苗の緑が草原に紛れる**件と **`GROW_SECONDS` 180 秒**、**`#status` の位置**

## 人がやること

- ~~`RULES-INBOX.md` の 8 件を取り込むこと~~ **済み**（2026-09-03・人が居る周）。
  `items-survival.md` の末尾を指す 3 件は**いちばん新しい（羽根と矢の周）だけを当て**、
  そのとき嘘になっていた**「弓と矢は本家の材料を 2 つとも代用しています」の箇条書きも
  消しています**（経緯は `RULES-INBOX.md` の「取り込むときにした判断」）
- **産む間隔 300〜600 秒は本家の値を暫定で置きます**（実装の周が `TUNING.md` に書きます）。
  **モブは 72m でデスポーンし保存もされない**ので、**実際にはほとんど産まないかもしれません** ——
  短くするかどうかは手触りの話なので、遊んでから決めてください
- **卵のスタックは 16**（本家の値。バケツの 1 と同じで `MAX_STACK` ではありません）
- `master` へのマージ

## 次にやること

**無人の周は `AUTODEV.md` の「1. いまはどの周か」に従うこと。**
`AUTODEV-SPEC.md` が **`状態: 未着手`** なので → **次は C（実装）の周**です。

- **`AUTODEV-SPEC.md` を全文そのまま使うこと**（要約しない。要約すると「触らないファイル」と
  「このタスク固有の禁じ手」が真っ先に落ちます）。**サブエージェントは同じ番の中で結果が
  返る呼び方だけ**。返らないなら自分で実装し、点検（C-2）も自分で `git diff` を読むこと
- **`AUTODEV-QUEUE.md` の未着手は 6 件**（7c-1 / 7c-2 / 8 牛 / 9 クモと糸 /
  10 スケルトンと骨 / 11 刈られた羊が見て分かること）。**A（棚卸し）には入りません**
- **ID は `ROADMAP.md` の予約表から。この周が取るのは 129（卵）の 1 つだけ**
  （**111 以降はブロックとアイテムで 1 本の番号列**なので、片側だけ見て取らないこと）
- **`src/**` と `test/**` は `Read` / `Edit` で開くこと**（`cat` / `sed` だと層 2 が読まれません）
- **`npm run typecheck` / `npm test` / `npm run build` は自分で走らせること**（報告を信じない）

**音のテスト（`test/audio.test.ts`）は揺れることがあります**（AUTODEV 13 の周に 1 件だけ赤 →
走らせ直したら緑。**この周は 2 回とも緑**）。**赤が出たら、まずもう一度走らせること**
（停止条件は「2 周続けて赤い」）。

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**（`[x]` にした節を `docs/tasks-done.md` へ移す道具です）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL が動かない」は開発者の手元の devcontainer の話です）。
**クリエイティブの一覧を撮る手**は `docs/browser-shots/README.md` の 12・14 枚目
（`#mode` をクリックしてから `E`、そのあと `#creative` の `scrollTop` を最後まで送る）。

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
