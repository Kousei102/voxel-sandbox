# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-21・AUTODEV 109（C の周）のあと）

**この周は C（実装）の周**でした（判定表の 3 行目 —— `AUTODEV-SPEC.md` が
`状態: 未着手` だったため）。取ったのは **39「クモは明るいところでは襲ってこない」**で、
**ID は 0 個**（`ROADMAP.md` の予約表は 1 行も触っていません）。**差し戻し 0 回。**

### 入ったもの（`src/mobs.ts` だけ。`main.ts` は 0 行）

- **`MobDef.calmLight`（列 1 つ）** —— **この明るさ以上では追わず・殴らない。0 なら
  明るさを見ない**。**クモ 12・ほかの 9 種類は 0**（`kind === "spider"` は 1 行も無し）
- **`Mob.calm`（印 1 つ）** —— 貼り直すのは 5Hz の `thinkHostile()` だけ。**保存しません**
  （`SaveData` は 1 バイトも増えず、version は 1 のまま）
- **`calmInLight(calmLight, sky, block, brightness)`（純粋関数・export）** ——
  `calmLight > 0 && spawnLight(...) >= calmLight`。**明るさの合成は湧きと同じ式**
- **止めたのは 2 か所だけ** —— `chasing()` の先頭と、`update()` の
  `if (!def.hostile || mob.calm) continue;`。**`strike()` と `fire()` は 0 行**
- **湧きは 1 文字も触っていません**（`canSpawnHostile()` / `HOSTILE_LIGHT_MAX` /
  `hostileFor()` / `trySpawn()` が ±0 行）。**クモは今までどおり暗い所に湧きます**

### ⚠ 仕様書から 1 つだけ変えたところ（**実測で落ちたため**）

**`spawn()` の `calm` の初期値**は、仕様書では `false` でしたが **`calmInLight()` に
通す形**（明るさは最大を渡す）にしました。`false` のままだと
**「昼のクモは殴らない」が 1 回で落ちます** —— 判断は 5Hz で `thinkTimer` が個体ごとに
ずれているので、**明るい所に置かれたクモが最初の判断より前に 1 発殴る**（`attackTimer`
の初期値が 0）ためです。**テストはゆるめず、実装を直しました。**
`calmLight` が 0 のモブは最大の明るさを渡しても false なので、
**ゾンビが 0.2 秒ぼんやりする形にはなりません。**

## 数字（2026-09-21・この周で走らせて確かめたもの）

- `npm test` **3860 件すべて成功**（**3853 から +7**。新しい節
  「明るい所では襲ってこない（クモ）」が 7 件）
- **音は 12 件で、9 つの値はビット同一のまま**（羊毛 0.552 / 雪 0.716 / 砂 2.235 ほか）
- `npm run typecheck` 緑 / `npm run build` 緑（`src/**` を触ったため）/
  **`npm run bench` は走らせていません**（生成もメッシュ化も 0 行）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）・
  **`main.ts` 1450 行 / 上限 1500**（**±0。1 行も触っていません**）
- **1..63 の空き 7**（±0）・**111..255 の空き 69**（**±0。次に取るのは 187**）
- `CLAUDE.md` 197 / `LOOP.md` 199 / `AUTODEV.md` 198（上限 200）・`TASKS.md` 104 / 150・
  `REVIEW.md` 84 / 200
- `AUTODEV-QUEUE.md` **108 行 / 上限 150・未着手 6 件**（**⚠ 前の引き継ぎの「8 件」は
  数え違いで、取る前も 7 件でした**）・`AUTODEV-SPEC.md` **`状態: 済`**

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** で、キューの未着手が
**6 件（3 件以上）**あるので判定表の 2 行目です。

- 取るのはキューの先頭 **40「エンダーマンが水に触れると痛い」**（**ID 0 個**の見込み）。
  **取る前に、その 1 件だけコードで数え直すこと**（`AUTODEV.md` の B）——
  **キューが「`isHotLiquid` とそっくりな形」と書いているのは数えた結果ではありません**
- **当たる `rules/` は `rules/mobs.md`**（`grep -l '"src/mobs.ts"' rules/*.md`）。
  仕様書そのものにも当たります（`grep -l '"AUTODEV-SPEC.md"' rules/*.md` → `rules/testing.md`）
- **`AUTODEV-SPEC.md` は `wc -l` で 119 行以下に収めること**（上限 120 は
  `split("\n")` で数えるので +1 されます。`rules/testing.md` に 4 回ぶんの記録あり）

## ブラウザで見てほしいところ

**撮ったのは `npm run shot -- mobs --size 900x900` の 1 枚だけ**で、`git worktree` に
立てた前の周の HEAD と **md5 が一致**しました（`c4d0609d…`・**ビット同一**）——
`MobDef` に列を足しても 10 種類の形が 1 画素も動いていない、という裏取りです。
**襲うのをやめる姿は絵になりません**（動きなので）。下は人にしか決められないぶんです。

- **⚠ 39 を入れたので見てほしいこと**（**この周のぶん**）:
  **昼のクモのそばを通れるのが安心か拍子抜けか** / **松明（光量 14）のそばでは
  クモだけが襲ってこず、ゾンビは襲ってくる**のが分かりにくくないか /
  **暗がりへ一歩入った瞬間に襲ってくる切り替わり**（判断は 5Hz なので最大 0.2 秒遅れます）
- **⚠ クモが 2 段・4 段の壁を登ってくるのが怖すぎないか**（38）/ **登る速さ 3.0 m/s**
  （はしご 2.35・歩き 6.0 の間）/ **⚠ 囲い（家の壁・柵）がクモには効きません**
  （**ただし昼のあいだは襲ってこなくなりました**。高さの上限は無し）
- **⚠ サボテンの伸び方（37）**: **置いたサボテンが 180 秒に 1 段・3 段で止まる**のは
  待てる長さか / **自然に生えたサボテンは伸びません**（サトウキビと同じ線。`TUNING.md`）
- **⚠ 刈られた羊の手ざわり（12）**: **地肌の桃色 `0xe3b09c` が「刈られた羊」に見えるか** /
  **細さ（体が 0.625 → 0.438 幅）** / **60 秒で戻るのは待てる長さか**
- **⚠ ツタの生え方（34c）**: **森の木の 25%** は多いか少ないか / **1 列 0〜4 マス** /
  **針葉樹林には 1 本も生えません** / **登る速さははしごと同じ・硬さ 0.2**
- **⚠ 金・ダイヤの防具の色（33b）**: **金の上着 `0xefd714` とズボン `0xf8b317` は
  絵の上でやや近い** / **⚠ ダイヤ一式 80% 減が強すぎないか**（**防具は壊れません**）/
  **⚠ 金を作る理由があるか** / **⚠ 鉄の防具の「鋼の青」が鉄に見えるか**（33a・60% 減）
- **⚠ 一覧の名前が折れて枠の絵に被る件**（`style.css` の `.slot .label`）。**56 枠**
- **⚠ フェンスを 52fps 未満で跳び越せる件**（余裕 0.0117）/ **⚠ 氷と雪の浜がほぼ同じ白**
- **ケーキ**: 上面 `0xffd0e4` が「ケーキ」に見えるか。**作れるのに使い道が無い**（24b）
- **その他**: レンガ（32b）/ 粘土は海底の 7.2%（32a）/ ハーフ 2 材質（31）/
  苗木の 180 秒（30b）/ 木炭（29）/ 修理（28）/ スケルトンの矢（2 秒に 1 発）/
  クモの巣の歩き 1/4 / リンゴ 200 枚に 1 個 / はしご 2.35 m/s / クモの速さ 6.0

## 分かったこと（**この周で据えた決まりごとは 1 件**）

**`rules/mobs.md` に「明るさで手を止めるモブ（`MobDef.calmLight`）」を 1 節**
（`Edit` で普通に直しました。`RULES-INBOX.md` は層 3 だけ）:

- **湧く線（7）と襲う線（12）を 1 つの定数で分け合わないこと** ——
  揃えると**光量 8〜11 の薄暗がり（湧かないが襲う）が消えます**
- **`calmLight > 0` の判断は `calmInLight()` の 1 本だけが持つこと**
- **`mob.calm` の代入は日光の `if` の外**（中だと水に潜ったクモだけ印が固まります）
- **⚠ `spawn()` の初期値を `false` にしないこと**（上の「実測で落ちた」件）
- **⚠ テストの試験場に `quiet()` を使わないこと** —— あれは `block` が 15 固定なので
  **クモが常に calm**。明るさを動かす試験場は**石の床・`sky = 14`・`block = 8`** で
  自作すること（**昼は 14 で襲わない / 夜は 8 で襲う / 8 > 7 なので湧かない /
  sky が 15 でないので日光で焼けない**の 4 つが 1 つの試験場で立ちます）

**⚠ 音のテストが赤いのは「揺れ」ではなく退行です**（2026-09-19 に原因ごと消えました）。
9 つの値はビット同一で出るはずなので、**1 つでも動いたら退行**として扱ってください。

## 人がやること

- **⚠ 定期実行のプロンプトに古い行が 2 か所あります**（人がプロンプトを直すまで、
  次の周は `rules/testing.md` と `npm test` の出力のほうを信じること）:
  - **「`test/audio.test.ts` の羊毛は 15 回に 2 回ほど跳ねる」** —— 2026-09-19 に
    原因ごと消えました。**いまは 1 つでも動いたら退行**です
  - **「いま 2934 件」** —— 実際は **3860 件**です
- **⚠ 設計の判断（79 周持ち越し）: `main.ts` を 2 つに割ってよいか。**
  いま **1450 行**（上限 1500）で、**`AUTODEV.md` の停止条件 2 に並んでいます**。
  **待っているのは 3 件**（**43 卵のヒヨコ**（`onHitBlock` に 1〜3 行）/ **35 骨粉**
  （`use.ts` の 19 通り目）/ **24b ケーキをかじる**（最低 7 行））。
  **キューの 40..42 の 3 件は 0〜1 行の見込み**なので、**判断が下りなくても 3 周は進みます**
- **⚠ 39 で見送ったもの: 「殴られたら明るくても追い返す」（本家の反撃）。**
  敵対モブはいま反撃の状態を 1 つも持っていない（`fleeTimer` は受動だけ）ので、
  **取るならそれだけで 1 周**です
- **⚠ 明かりの意味が種類ごとに変わりました。** 松明を 1 本立てた所では、
  **クモは襲ってこないのにゾンビは襲ってきます**（本家どおり。`TUNING.md`）
- **⚠ クモの「登れる高さに上限が無い」件**（38 で選んだ形）。本家にも上限が
  ありませんが、**壁ぎわに湧いた 1 体が天井まで登ります**。強すぎるなら、速さではなく
  **登れる高さ**の話になり、`MobDef` に列がもう 1 つ要ります（`TUNING.md`）
- **⚠ クモは水中では登りません**（本家は登ります）。跳躍と同じ `mob.liquid === AIR` に
  揃えたためで、**外すと水路で囲う守り方がクモにだけ効かなくなります**
- **⚠ サボテンの本家の性質を 2 つ見送りました**（`docs/autodev-log.md`）:
  **「砂の上にしか置けない」**と**「横に固いブロックがあると壊れる」**。
  **取るならそれぞれ 1 周**にしてください
- **⚠ 落ちたアイテムを溶岩で燃やしてよいか**（`drops.ts` の 347 行の
  「液体にはどれでも浮かせる」は人が選んだ判断です）
- **⚠ 防具に耐久値を足すか。** いまの防具は**革も鉄も金もダイヤも壊れません**
- **⚠ ダイヤ一式 20 点が `ARMOR_CAP`(20) にちょうど当たる件**は、
  **「本家どおりなので `ARMOR_CAP` を触らない」で入れてあります**
- **⚠ 金が鉄より弱いのに同じ 24 枚な件**（11 点 < 15 点）。**本家のままです**
- **この周も ID を 1 つも取っていないので `ROADMAP.md` の予約表は 1 行も触っていません。**
  **⚠ `ROADMAP.md` の 21 行目「サボテンに触れたときのダメージ」はもう済んでいます**
  （AUTODEV 32）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は続けて無事に発火しています**（この周で 109 周目）
- **`loop/endgame` に `rules/` の形が届いていません** —— あちらのルート直下に `rules/` が
  無く、`master` から 50 コミット以上遅れています。再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

## 音のテストだけを束ねて回す（そのまま使えます）

**30〜50 回連続で回すなら全体（1 回 12 秒）ではなくこちらを**（1 回 0.30 秒）。
入口を**スクラッチパッドに**書いて `esbuild` で束ねるだけです:

```ts
import { run as audio } from "/home/user/voxel-sandbox/test/audio.test";
import { summary } from "/home/user/voxel-sandbox/test/harness";
await audio(); summary();
```

```bash
npx esbuild <その .ts> --bundle --platform=node --format=esm \
  --external:node-web-audio-api --outfile=node_modules/.cache/voxel-audioonly.mjs
node node_modules/.cache/voxel-audioonly.mjs      # 「12 件すべて成功」
```

**⚠ 出力先が `node_modules/.cache/` でないと `node-web-audio-api` を見つけられず、
まるごと `skip` になります**（緑に見えて 1 件も確かめていない形になります）。

## 絵を撮るときの注意（実測で分かっているもの）

**⚠ `npm run shot -- mobs` は既定の 640x400 では読めません**（2026-09-20 実測）——
10 体を横 45 マスに並べた場面なので、**1 体が数画素に潰れ、手前の木に隠れた個体は
写ってすらいません**。**`--size 900x900`（正方形）にすると水平画角が 131° → 70° に
狭まり**、中ほどの数体が読める大きさになります。**場面の側（`tools/shot.ts`）は
1 文字も直さずに済みます。**

**⚠ その画の手前（下・中央）に毎回写る暗い穴は地形です。** モブを 0 体にして撮っても
**画素までまったく同じ絵**が出ます。**モブの不具合と読み違えないこと。**

**⚠ 前の周と絵が変わっていないことを確かめるなら `git worktree`**（`git stash` は
使わないこと。`rules/testing.md` の点検役の事故と同じ話）。**2026-09-21 にそのまま
通りました**（`mobs.png` が両方 `c4d0609d…`）:

```bash
git worktree add <捨て場>/base HEAD
ln -s /home/user/voxel-sandbox/node_modules <捨て場>/base/node_modules
cd <捨て場>/base && npm run shot -- mobs --size 900x900
md5sum <捨て場>/base/shots/mobs.png shots/mobs.png   # 同じならビット同一
git worktree remove --force <捨て場>/base            # symlink を先に rm すること
```

**⚠ `npm run shot` のカメラを目の高さ（`y + 1.7`）まで下げて 4 マスまで寄らないこと** ——
**画の下半分の地面が 1 枚も描かれず空色に抜けます**。谷の暗がりに見えるので地形のせいだと
思い込み、**`setVoxel` を 1365 件たたいて足場を埋めました**が、**三角形は 1 枚も
増えませんでした。原因はカメラが低くて近いことだけ**で、`y + 2.2` まで上げて 6.5 マス
引いたら一発で消えました。

**⚠ 遠すぎても失敗します**（2026-09-20・`cactus`）。8.5 マス引いて `y + 3.4` から撮ったら
**画の下半分が台の外の草で埋まり**、見たかった継ぎ目が数画素に潰れました。
**6.8 マス・`y + 2.9` に寄せ直して一発**。**撮り直しは 1 秒なので、まず撮って `Read` で見ること。**

**⚠ `node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも
写りません** —— あれは**サバイバルの空のインベントリ**です。**一覧の色を見るなら
`#seed` → `#regen` → `#mode` → `#play` → `E` の順**（`#mode` はメニューの中の
ボタンなので、`#play` のあとは隠れて `element is not visible` で落ちます）。
**⚠ 送り先は末尾とは限りません** —— **低帯（1..63）の番号を取ったものは一覧の
真ん中に出る**ので、`scrollTop = scrollHeight` では 1 枠も写りません。
**`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })` を呼ぶこと**。
**⚠ 枠を数えるときは `#creative .slot` を使うこと**（素の `.slot` はホットバーと
インベントリの 36 枠まで拾います）。**⚠ `/opt/pw-browsers` には
`chromium_headless_shell-1194` も居ます** —— `startsWith("chromium-")` で弾くこと
（`tools/browsershot.mjs` の `findChrome()`）。

**⚠ 自然に生えないブロックは `npm run shot` の既存の場面に 1 枚も写りません。**
本棚・クモの巣・ケーキ・氷・フェンス・苗木・ハーフ 6 材質がこれで、
**`tools/shot.ts` に場面を 1 つ足すのがいちばん安い**（1〜2 秒で撮れて、
`browsershot.mjs` を書き換えずに済みます）。**`sheep` と `cactus` がその形です。**
**時間が経たないと見えないもの**（育つ・燃える・溶ける・**刈られる**）は、
**その場で `update()` を回すか、その操作を呼んでから撮ること**
（`sheep` は `mobs.shear()`、**`cactus` と `grown` は `crops.update()` を通しています**）。
**手で積んで／手で並べて撮らないこと** —— **育つ道が本当に書けるかを 1 つも確かめないまま
緑の絵になります**。**⚠ 防具を着た姿は撮れません**（一人称視点なので体が写りません）。
**⚠ 動き（登る・跳ぶ・追う・壁を登る・明るさで襲うのをやめる）も撮れません** ——
あれは人に遊んでもらうぶんです。
