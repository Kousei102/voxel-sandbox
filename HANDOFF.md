# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-07・AUTODEV 51 のあと）

- `npm test` **3194 件すべて成功**（前の周と同数。**`src/**` も `test/**` も 1 行も
  触っていない B の周**）・`npm run typecheck` 緑
- **`npm run build` も `npm run bench` も走らせていません**（`src/**` に差分が無いため）
- **音の一群はこの周も跳ねませんでした**（1 回で緑）。落ち着いた値は
  羊毛 0.552 / 雪 0.716 / 砂 2.235・低い声 0.162 / 高い声 0.329 で、前の周と同じです
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も変えていません）
- **`main.ts` 1449 行**（`wc -l`。テストの数え方では 1450。上限 1500・
  **止まる目安 1450 に届いたままです。この周は ±0**）
- **アイテム 114 種・`MAX_ITEM_ID` 152・111..255 の空きは 103・1..63 の空きは 9**
  （**予約表の次の空きは 153**）
- **立方体 40 / 非立方体 76**・**モブ 9 種類**・**レシピ 58 本**・**精錬 7 本**・
  **燃料 8 種**・**食べ物 10 種**・**バイオーム 10**・**構造物 2**・**次元 3**・
  **`DamageCause` 9 種**

## この周でやったこと（AUTODEV 51 の B・クラウドの無人の周）

**判定表は「未着手 9 件（3 件以上）」「`AUTODEV-SPEC.md` が `状態: 済`」で
2 行目 = B（仕様）の周**でした。**キューの先頭 22（金のリンゴ）を仕様書にしました。**
**差分は `AUTODEV-SPEC.md` の 1 ファイルだけ**（B の完了条件どおり `src/**` は 0 行）。

**先にコードで数え直しました**: `grep -rn "GOLDEN_APPLE\|金のリンゴ" src/ test/` は
**実装 0 件**（`items.ts` のコメントに「別件です」と 2 か所あるだけ）で、材料の
**金インゴット 67 とリンゴ 149 はどちらも実装済み**でした。

**仕様書を書くために先に測った 3 つ**（次の周が測り直さなくて済むように）:

1. **色 `0xf0a800`** —— 既存のどのアイテムの色からも **56.4** 離れています
   （いちばん近いのはブレイズロッド。判定の下限は 20）。**金インゴット `0xf2d15c`
   では 0 になるので使えません**（ブレイズパウダーが `0xe8a33d` で、
   金寄りの色は思ったより埋まっています）
2. **`main.ts` は ±0 行で通せます** —— `vitals.canEat` と書いてある 2 か所
   （`useOrPlace()` の `decideUse(` と `updateEating()` の `eating.advance(`）を
   **`vitals.canEatFood(...)` に書き換えるだけ**。行も判断も足しません
3. **「満腹でも食べられる」を `use.ts` に書く必要はありません** ——
   `UseFacts.canEat` は `main.ts` が渡す事実なので、**判断は `vitals.ts` の
   `canEatFood()` 1 か所**に収まります（`decideUse()` の中身は 1 文字も変わらない）

## 次にやること

**次は C（実装）の周** —— `AUTODEV-SPEC.md` が **`状態: 未着手`** なので、判定表の
3 行目に当たります。**仕様書（`AUTODEV-SPEC.md`）を全文そのまま読んで実装すること。**
**取る番号は 153 の 1 個だけ**で、`ROADMAP.md` の予約表から取ります。
**触る `rules/` は `items-survival.md` / `vitals.md` / `use.md` /
`inventory-screen.md` / `testing.md`**（仕様書の 6 に書いてあります）。

## ブラウザで見てほしいところ

**この周は絵を 1 枚も撮っていません**（`src/**` に差分が無いので撮るものがありません）。
次の C の周も**撮る必要はありません** —— 見た目に出るのは一覧の金のリンゴの色 1 つで、
そこは色の隔たりの判定（20 以上）が数値で見ます。

**それでも人に見てほしいところ（次の周のぶん・先出し）**: **金のリンゴの色
`0xf0a800` が一覧で金インゴット・ブレイズロッド・ブレイズパウダーと見分けられるか**
（数値では 56.4 離れていますが、どれも「金色」なので並ぶと紛らわしいかもしれません）と、
**金インゴット 8 個 + リンゴ 1 個に「体力 4 回復」が見合うか**（本家と同じ材料・
同じ回復量ですが、**このプロジェクトには持続する効果が無いので、本家の
「再生 II が 5 秒」を即時 4 回復に均してあります**。`TUNING.md` に残す予定）。

**持ち越し（まだ誰も見ていません）**: **本棚が「本の詰まった棚」に見えるか**
（側面 `0x9c5064` がかなり赤紫）/ **紙 `0xd4e0ec` が雪玉と 25.5 で見分けられるか** /
**本棚 1 個に材料 9 個が見合うか** / **リンゴの色 `0xe0342c` が赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か** / **地面に落ちた
リンゴが赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか** /
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか**（しゃがみで
止まる手はありません）/ **はしごの上端から地上へ出る手触り** / **はしごの色
（`0xc9a063` / `0xa8823f`）が板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に
見えるか** / **`CANE_GROW_SECONDS = 180` が待てる長さか** / **自然に生えたサトウキビは
伸びません**（理由は `TUNING.md`）/ **⚠ 前から遊んでいる世界だけの見え方**: 前に浜の
サトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて見えることがあります /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度 / サボテンに
当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 0 件**）

**`rules/` に据えるものはありませんでした。** B の周は文書 1 枚しか触らないので、
踏む落とし穴がありません。**前の周の 2 件（`def()` の色が `top` → `side` → `bottom` の
順に落ちること・同じ値に `=== A` と `!== B` を続けて書くと TS2367 で落ちること）は
`rules/blocks-shapes.md` と `rules/testing.md` に入っています。**

## 人がやること

- **設計の判断を 1 つお願いします（25 周持ち越し。もう待てません）:
  `main.ts` を 2 つに割ってよいか。** いま **1449 行で、止まる目安 1450 に届いています**。
  **`test/ui.test.ts` の 40 件の見張りが呼び出しを `main.ts` に釘付けにしている**ため、
  減らすなら「見張りの一覧が `main.ts` と新しい配線ファイルの両方を読む」形に変える
  必要があり、**判定の意味が変わるので無人の周では触れません。**
  **22 金のリンゴ / 27a 防具の枠は 0 行**の見込みです（22 は上のとおり実測済み）。
  **23 クモの巣・24 ケーキ・25 氷・26 フェンスは配線が要ります**（あと 2 周ぶんの余裕）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 35 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。
**いまの仕様書は `wc -l` で 119 行 / テストの数え方で 120 行**（**上限ちょうど**です ——
C の周で仕様書に 1 行でも足すと赤くなります。足す必要はありません）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
