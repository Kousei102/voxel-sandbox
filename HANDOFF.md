# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `.claude/rules/`、
ループの手順は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは `REVIEW.md`。

## いまの状態（2026-08-21）

- ブランチ **`loop/endgame`**。**`origin/master` より 24 コミット先**。**push していません**
- 作業ツリーはきれい。`npm run typecheck` / `npm test` / `npm run build` すべて通ります
- **`npm test` 1341 件すべて成功**
- **進行（クリア導線）達成 4 / 13**（4 件とも本物）。
  溶岩 / 黒曜石 / 火打石と打ち金 / ポータルの点火
- ブロック ID **1..63 の空きは 18 個**（64 以降は 103 まで）。アイテム ID は 88 まで
- `src/main.ts` は **1389 行 / 上限 1500**

## 直前にやったこと — TASKS 1-6（次元の器）`7543350`

**ユーザーと同席で取りました**（`TASKS.md` の「⛔ ここで止まる」より下のタスク）。
その前の 3 周（1-4 / 1-5 / 1-7）は無人のループで、`543b55f` / `f0427d4` / `2989a41`。

- `dimensions.ts` = **いま居ない次元の持ち物（`edits` / 落とし物 / かまど / チェスト）を
  預かる器**。判断だけのファイルで、three も DOM も乱数も出てこない
- **オーバーワールドは `SaveData` の上の階層のまま**で、`dims` に入るのはそれ以外だけ。
  **オーバーワールドに居る限り保存の形は今までと同じ**（`dim` も `dims` も出ない）
- 預けるのと取り出すのは **`switchTo()` の 1 回の呼び出し**（`forSave(live)` も `stash` を兼ねる）
- **表（`DIMENSIONS`）に無い次元へは移らない。** 登録されているのは**オーバーワールドだけ**
- 決まりごとは新しい **`.claude/rules/dimensions.md`**（`CLAUDE.md` の層 2 の表にも追加）

### 1-6 を 2 つに割りました

元の 1-6 は「切り替えとセーブ」＋「`portals.ts` の配線（点火 → 通ると移動）」でしたが、
**移動には行き先（ネザーの地形）が要ります。** 仮のネザーを作ると 2-1 の先食いになって
捨てることになるので、**配線は 2-2 へ移しました**（`TASKS.md` を更新済み）。

## 踏んだ穴（同じことを繰り返さないために）

1. **仮の判定は「土台があるか」に掛けないこと。2 度目です。** 1-7 で
   `structures.ts` を作った瞬間に「ネザー要塞が生成される」が達成へ化けたのと同じ形で、
   進行テストの「ネザーへ行って戻れる」も **`dimensions.ts` に `export` があるか**を
   見ていました。作る前に probe のほうを直すこと
2. **立方体でないブロック（`isProp`）は半透明にできません。** `buildProps()` は
   不透明側の `Builder` しか渡されないので `translucent: true` は**黙って無視されます**
   （`rules/meshing-render.md`）。ポータルは不透明の紫板にしました
3. **ソースを見る判定はコメントを落としてから**（`arena.ts` の `sourceOf()`）。
   ループの 3 周とも踏みました
4. **見張りの正規表現は `\b` を落とさないこと。** `serializeEdits\(` は
   `deserializeEdits(` にも当たって「2 か所で組み立てている」に見えます
5. **音の波形テストが 1 度だけ落ちました。** 続けて 2 回走らせると同じ数値で通ったので
   退行としては扱っていません（`rules/audio.md`）

## 未確認のもの（ユーザーにブラウザで見てもらう）

**`REVIEW.md` の「1-6. 次元の器」5 項目だけです。** 1-4（ポータルの見た目）は
**確認済みで消しました**。

1-6 は**画面に出る変化がありません** —— 触ったのは保存と読み込みの経路なので、
見てもらうのは「今あるワールドがそのまま開くか」「保存の形（`localStorage`）が
変わっていないか」です。壊れていても画面には何も出ません。

## 次にやること

**`REVIEW.md` の確認が済んでから 2-1（ネザーの地形）へ。** 順番はこうなります:

1. **2-1. ネザーの地形** — ネザーラック 45 / ソウルサンド 46 / グロウストーン 47 /
   溶岩海 / 天井の岩盤。**生成器ができたら `DIMENSIONS` に 1 行足す**（`salt` は 0 以外）
2. **2-2. ネザーポータルで往復** — **ここで `portals.ts` を `main.ts` に配線します**
   （火打石と打ち金の右クリックで点火 → 立っていると移動）。座標の対応は 1:8
3. 1-8（`projectiles.ts`）は **見** なので同席で。2-4 のブレイズより前であればいつでも

土台はもう揃っています: 枠の判定（1-4）・生成器の差し替え（1-5）・構造物の器（1-7）・
次元の器（1-6）。**`TASKS.md` の「⛔ ここで止まる」は残したままです**（無人のループを
再開するなら、その上には未着手のタスクがありません —— 次の 2-1 は同席で取ってください）。
