# 引き継ぎ

**このファイルはセッションの終わりに丸ごと書き直します。** 引き写さず、`git` と
`npm test` で裏を取ってから書くこと。恒久的な決まりごとは `CLAUDE.md` と `rules/`。

**ループは 2 本あります。** クリア導線は `LOOP.md`（`loop/endgame`）、
**機能追加は `AUTODEV.md`（作業は `master`）**。いまクラウドで回っているのは後者です。

## いまの状態（2026-09-08・AUTODEV 58 のあと）

- `npm test` **3295 件すべて成功**（**+22**）・`npm run typecheck` 緑・**`npm run build` 緑**
- **`npm run bench` は走らせていません**（生成もメッシュ化も 1 行も触っていません）
- **音の一群は跳ねませんでした**（この周に走らせた `npm test` は全部緑）
- **進行（クリア導線）達成 13 / 13**・**`SaveData` version 1**（形も ±0）
- **`main.ts` 1449 行（±0）**（`wc -l`。テストの数え方では 1450。上限 1500・止まる目安 1450）
  —— **この周は `main.ts` を 1 文字も開いていません**
- **アイテム 117 種・`MAX_ITEM_ID` 155・111..255 の空きは 100・1..63 の空きは 9**
- **立方体 40 / 非立方体 78**・**モブ 9 種類**・**レシピ 60 本**（+1）・**精錬 7 本**・
  **燃料 8 種**・**食べ物 11 種**（±0）・**バイオーム 10**・**構造物 2**・**次元 3**・
  **`DamageCause` 9 種**
- **`AUTODEV-SPEC.md` は `状態: 済`**・**`AUTODEV-QUEUE.md` 119 行**（未着手 6 件）

## この周でやったこと（AUTODEV 58 の C・クラウドの無人の周）

**判定表は「`AUTODEV-SPEC.md` が `状態: 未着手`」で 3 行目 = C（実装）の周**でした。
**24a（ケーキ 155・置けるところまで）**を仕様書のとおり実装し、**差し戻し 0 回**。
**サブエージェントは 1 つも立てていません**（点検は `git diff` を自分で読みました。
`rules/testing.md` の「点検役に `git` を打たせない」を踏まないため）。

**いちばんの仕事は「クラフトの残りかす」の道を 1 本通したことです。** `consumeGrid()` は
1 枠 1 個減らして空にするだけだったので、そのままだと**ケーキ 1 個でバケツ 3 個
（鉄 9 個）が消えていました**。足したのは **`items.ts` の `LEFTOVERS` /
`leftoverOf()` の表 1 本**で、`crafting.ts` は聞くだけ。**`Recipe` にキーは足していません**
—— 残りかすは**アイテムの性質**なので、ミルクを使うレシピが増えても 1 行も直りません。

**`clearSlot()` を通すのが要でした。** 素通しで `slot.item` を書き換えると
**傷（`damage`）が空のバケツに乗り移ります**（`test/crafting.test.ts` に
「傷 7 のミルクバケツを置いても、戻った空バケツの `damage` は 0」があります）。

**型で 2 回止まりました。どちらも既知の安全な罠**（`rules/testing.md` の TS2367）——
`MAX_ITEM_ID === COBWEB` を `=== CAKE` へ、`supportFace !== NO_SUPPORT` を
**別のブロックとの比較**（クモの巣が `NO_SUPPORT`）へ。**判定はゆるめていません。**

**色は寄せ直しました**（`TUNING.md` に 1 節）。仕様書の暫定値 `0xf2ded2`（クリーム色）は
**羊毛・羽根から 15.4** しか離れず判定（20）に落ちます —— **白っぽい一覧は雪・雪玉・卵・
砂糖・紙・ミルク・羊毛・羽根で満席**です。**生クリーム + 赤い実の平均**として
`0xffd0e4`（薄紅）へ寄せ、**羊毛から 31.5**。

## 次にやること

**次は B（仕様）の周** —— `AUTODEV-SPEC.md` が **`状態: 済`** なので判定表の 2 行目です
（未着手が 6 件あるので A ではありません）。**`AUTODEV-QUEUE.md` の先頭 = 25（氷）**の
仕様書を書くこと。

- **ID は 156 から**（`ROADMAP.md` の予約表。155 = ケーキで埋まりました）
- **氷は「半透明の立方体」と「滑る（`player.ts` の摩擦）」と `worldgen.ts` の 3 つ**です。
  **B の周で `main.ts` に何行要るかを数えること**（1449 行なので、1 行でも増えるなら
  停止条件 2 に当たります）。**摩擦は `player.ts` に判断を置けば ±0 行で通るはず**ですが、
  **数えるまで決めつけないこと**（23 で「配線が要る」と「行が増える」は別だと分かりました）

## ブラウザで見てほしいところ

**この周に足したケーキは自然生成しません**（本家にも湧きません）。**遊んで見るには
クリエイティブの一覧から出すか、ミルクバケツ 3 + 砂糖 2 + 卵 1 + 小麦 3 で作ります。**

**絵で確かめたこと**（`npm run shot -- cake`。`tools/shot.ts` に場面を足しました）:
**箱 1 個が高さ 8/16 で出ていて面が欠けても裏返ってもいない** / **上面（薄紅）と側面
（スポンジ）が入れ替わっていない** / **石の台に置いた 1 個で縁 1/16 の引っ込みが見える** /
**隣の板ハーフと、高さが同じでも横幅で見分けが付く**。

**それでも人に見てほしいのは 3 つ**: **上面 `0xffd0e4` が「ケーキ」に見えるか**
（ピンクが強すぎないか。素直なクリーム色は白っぽい一覧が満席で取れませんでした）/
**側面のスポンジ `0xe8c9a0` と下面 `0xd9b98a` の 3 色が並んでケーキに読めるか** /
**一覧で薄紅が羊毛・卵・雪と見分けられるか**（いちばん近いのは羊毛で 31.5）。
**作れるのに使い道が無い**のは承知の上です（かじるのは 24b）。

**持ち越し（クモの巣）**: **中の歩きが 1/4（1.3 m/s）で「絡まった」と感じられるか** /
**素手 6.0 秒・刃物 1.8 秒の比が「剣を持ち替える理由」になるか** /
**一覧で `0xc8c8dc` が雪・ミルク・紙・糸と見分けられるか**。

**持ち越し（金のリンゴ）**: **色 `0xf0a800` が金インゴット・ブレイズロッド・
ブレイズパウダーと見分けられるか** / **金インゴット 8 個 + リンゴ 1 個に「体力 4 回復」が
見合うか** / **27b（革の防具）を実装したら: 防具点 7 で 8 割ぶんしか減らない
（10 → 7.2）のが「着た手ごたえ」として分かるか**。

**持ち越し（まだ誰も見ていません）**: **本棚が「本の詰まった棚」に見えるか**
（側面 `0x9c5064` がかなり赤紫）/ **紙 `0xd4e0ec` が雪玉と 25.5 で見分けられるか** /
**本棚 1 個に材料 9 個が見合うか** / **リンゴの色 `0xe0342c` が赤キノコ・生牛肉と
見分けられるか** / **葉 200 枚に 1 個（本家の 0.5%）が待てる確率か** / **地面に落ちた
リンゴが赤キノコの落とし物と見分けられるか** / **2.35 m/s の登りが待てる速さか** /
**手を離すと 3.0 m/s で滑り落ちるのが「掴まっている」と感じられるか** /
**はしごの上端から地上へ出る手触り** / **はしごの色（`0xc9a063` / `0xa8823f`）が
板・原木と見分けられるか** / **厚さ 3/16 の板が「はしご」に見えるか** /
**`CANE_GROW_SECONDS = 180` が待てる長さか** / **自然に生えたサトウキビは伸びません**
（理由は `TUNING.md`）/ **⚠ 前から遊んでいる世界だけの見え方**: 前に浜の
サトウキビを壊した場所は、下の 1 マスだけが空いて上の段が浮いて見えることがあります /
一覧で砂糖（`0xffffff`）が雪・羽根・卵・糸・雪玉と見分けられるか / ボウル `0x7a4a24` と
シチュー `0xf0dcb4` の見分け / シチューが焼き鳥と同点の 6 / 7.2 で材料 3 つに見合うか /
食べ終わりにボウルが手に戻る手ごたえ / 森を歩いたときのキノコの密度 / サボテンに
当たったときの痛さ / 牛を搾る・ミルクで毒が止まるか / かまど・チェスト・作業台を開いて
閉じる経路 / 雪玉を投げても何も起きない / 鉄・金・ダイヤのブロックが材料の山と
見分けられるか / **一覧の枠の名前が 2 行に折れてはみ出す**（`style.css` の `.slot .label`）/
クモの速さ 6.0 と朝に燃えてよいか / 投げた卵に使い道が無い / 牛の胴が寸詰まりに見えないか /
産卵の間隔 300〜600 秒

## この周で分かったこと（**決まりごと 1 件**）

**`rules/items-survival.md`** に **「食べ終わりに戻る器（`EMPTIES`）と、クラフトの
残りかす（`LEFTOVERS`）は別の表」** を 1 節足しました（**前の周が「`leftoverOf()` が
入ったら書くこと」と予告していたもの**）。中身は 4 点:

1. **いつ・どこへ・誰が聞くか**を表で並べた（食べ切ったとき手の中へ / クラフトで
   使い切ったとき盤面のその枠へ）
2. **`Recipe` にキーを足さないこと**（残りかすはアイテムの性質）
3. **`consumeGrid()` は必ず `clearSlot()` を通してから残りかすを置くこと**（傷の乗り移り）
4. **`LEFTOVERS` に載せてよいのは `stack: 1` のものだけ**（`EMPTIES` と同じ不変条件）

## 人がやること

- **設計の判断（31 周持ち越し）: `main.ts` を 2 つに割ってよいか。** いま **1449 行**で、
  上限 1500 まで 51 行です。**24a は予告どおり ±0 行**（1 文字も開いていません）でしたが、
  **24b（ケーキをかじる）には最低 7 行**要ります。**25（氷）で何行要るかは次の B の周が
  数えます** —— そこで 1 行でも増えるなら、`AUTODEV.md` の停止条件 2 に当たります。
  `test/ui.test.ts` の 40 件の見張りが動かせない事情は変わっていません
  （**判定の意味が変わるので無人の周では触れません**）
- **クラウドの定期実行（`trig_017Tw5EuQGad1df5mEFV2hXC`「voxel-sandbox 自動実装ループ」・
  毎時 `41 * * * *`）は 41 周続けて無事に発火しています**
- **`loop/endgame` に `rules/` の形が届いていません。** 再開する前に
  `git merge origin/master` で追いつかせること（`LOOP.md` の 7.）
- **巻き戻しは人の仕事です**（ループは `git revert` をしません）

**`npm test` が「直した不具合は REVIEW.md に残っていない」で赤くなったら、
`npm run archive` を走らせること**。

**`test/audio.test.ts` の音の一群は揺れます**（`rules/testing.md`）。**落ちる件が毎回
違うので、「2 回続けて赤い」だけでは退行と決められません** —— 同じ件が続けて落ちたときだけ
退行として扱うこと。**判定はゆるめないこと。**

**`AUTODEV-SPEC.md` の 120 行の上限は `split("\n").length` で数えます** ——
**`wc -l` が 120 だと 121 と数えられて赤くなります。119 行までにすること**（`test/docs.test.ts`）。

**クラウドのサンドボックスでは本物のブラウザが動きます**（`AUTODEV.md` の C-3。
`CLAUDE.md` の「WebGL は手元では動かない」は開発者の devcontainer の話です）。
