# 引き継ぎメモ（2026-08-06 時点）

このファイルは**使い捨て**です。読んで頭に入れたら消してかまいません。
恒久的な決まりごとは全部 `CLAUDE.md`、次に何を入れるかは `ROADMAP.md` にあります。
**まず `CLAUDE.md` を読んでください。** ここには「いまの状況」だけを書きます。

---

## 1. いまの状態

**作業ツリーはきれいです。`master` に 5 コミット積んであり、まだ push していません**
（`git fetch` で確認済み。**この 5 つにはこのメモ自身のコミットも入っています**）。

```
(HEAD)  引き継ぎメモ: 空腹まで入った状況と次の候補を書き直す  ← このファイル自身
4baaaf2 空腹: 肉ゲージと食べる操作を入れ、代用の自然回復を外す
c363657 引き継ぎメモ: 次のセッション向けに状況と次の候補を書き直す
24e7135 かまど: 精錬を入れて、代用していた 2 つを戻す
a5d7297 落ちたアイテム: 掘ったものが地面に落ちて拾えるようにする
f4296f8 README: モブと音の説明を足し、ファイル一覧を現状に合わせる  ← origin/master はここ
```

- `npm test` **943 件成功** / `npm run typecheck` / `npm run build` 通過
- **機能の 3 コミット（空腹・かまど・落ちたアイテム）はどれもユーザーがブラウザで確認済み**
  （全項目 OK）。**未確認の変更は残っていません**
- **数と `origin/master` は毎回 `git fetch && git log --oneline origin/master..HEAD` で確かめること。**
  前のメモの push 状況を引き写したまま出したら、実際にはもっと先まで push 済みでした。
  **このメモを書き換えてコミットすると、その瞬間に数が 1 増えます**（今回それで 4 と書いて間違えました）
- **push するかどうかは次の一手より先にユーザーへ聞くこと**（下の「2. push について」）

### 直近のコミット（空腹）で入ったもの

決まりごとは `CLAUDE.md` の「体力と空腹」に全部書いてあります。ここには次に触る人が
最初に知りたいことだけを残します。

- ハートの下に**肉ゲージ 10 個**。走る 40m / 歩く 400m / 掘る 800 個 / 殴る 40 回で 1 個減り、
  **満腹度（`saturation`）から先に**減ります。食べるのは**右クリック長押し 1.6 秒**
- **`CLAUDE.md` が名指ししていた「自然回復を外す」を片付けました。** 回復には空腹 18 以上が要り、
  1 回復するたび 1.5 個ぶん減ります。**無条件の回復に戻すと空腹が飾りになります**（テストが押さえています）
- **餓死も毒死もしません**（体力 1 で止まる）。ユーザーと決めた線です。0 にするなら必ず相談すること
- 判断は `vitals.ts` と `items.ts` の `FOODS` だけ。`player.ts` は `canSprint` を受け取るだけで
  空腹を見に行きません。**新しい GLSL・WebAudio・DOM の判断は 1 行も増えていません**

---

## 1-b. 次にやること

**着手する順はユーザーと決めること。** 見取り図は `ROADMAP.md`。

### 候補 A: チェスト

**かまどで作った器（`furnaces.ts` + `SaveData.furnaces`）にそのまま乗るので、いちばん安く入ります。**

- ブロックを 1 個（1..63 の空きは 24 個）、`furnaces.ts` と同じ形の `chests.ts`、
  `SlotArea` に枠を足すだけ。**`CraftScreen` は増やさないこと**（かまどがその形です）
- 決めるのは中身の枠数（27 か）だけ。壊したときに中身を落とすのはかまどと同じ

### 候補 B: 死亡時ドロップ / まとめ捨て（Ctrl+Q）

**どちらも仕組みは揃っていて、あとは「入れるかどうか」だけです。**
死亡時ドロップは**今回ユーザーが「いまは入れない」と決めました**（難度を変えるため）。
空腹が入って死ぬ場面は増えたので、また話に出るかもしれません。

### 候補 C: 空腹の続き

`ROADMAP.md` の「空腹 — 済み」の残り。食べ物の種類を増やす（作物・パン）には
「育つブロック」の仕組みが要るので、苗木と同じ塊になります。

### 候補 D: 地形・バイオーム系

川・湖、砂漠を平らに、サボテンのダメージなど。見た目の確認往復が多めです。

---

## 2. push について

**ユーザーに確認を取ってから push すること。** 公開サイトが差し替わる外向きの操作です。
push すると `.github/workflows/deploy.yml` が走り、`npm ci` → `npm test` → `npm run build`
を通ってから GitHub Pages（https://kousei102.github.io/voxel-sandbox/）に出ます。
**テストが落ちればデプロイされません。**

押したら `gh run watch <id> --exit-status` で Actions を見届けること。
そのあと配信中の JS のハッシュを `dist/assets/` と突き合わせると、実際に出たかまで確かめられます。

**フレーム時間の p99 判定（`world.test.ts`）はマシンが混むと落ちます。**
実測: テストを 2 本同時に走らせると p99 が 170ms まで跳ねました（上限 39ms）。
CI ランナーが混んでいると同じ落ち方をするので、落ちたら**中身の退行と決めつけず
再実行を試すこと。**

ブランチは切らず `master` に直接積んでいます（デプロイのワークフローが
`master` への push で走る設定のため）。

---

## 3. 進め方（このプロジェクト特有）

**この環境では WebGL が動かず、音も鳴らせません**（理由と切り分けは `CLAUDE.md`）。なので:

- **見た目・音を変える変更は、1 まとまりごとに止めてユーザーに見て（聞いて）もらうこと。**
  未確認の変更を積み上げないでください。落ちたときに切り分けられなくなります
- 確認をお願いするときは、**具体的な確認項目を 4〜7 個の箇条書き**で出すと話が早いです。
  番号を振っておくと「5 だけ違う」と返ってきて、すぐ切り分けられます
- **確認項目に「アプリがもともと持っていない挙動」を書かないこと。** 一度、こちらが勝手に
  前提にした挙動を確認項目に入れて、ユーザーの手を一往復ぶん取らせました。
  **お願いする前に、その挙動が本当にあるかコードで確かめること**
- **CSS の位置は直に数値で書かないこと。** 肉ゲージを足したとき、`#vitals` の `bottom: 74px` が
  ホットバーに 4px 食い込んでいるのが見えました（前から重なっていたのが、行が増えて表に出た形です）。
  いまはホットバーの寸法を `:root` の変数に持たせて `calc()` で出しています
- **テストの主張も裏を取ってから書くこと。**「咀嚼はいちばんこもった音」と書いたら羊毛の足音のほうが
  こもっていて落ちました。実装ではなく主張のほうが間違っていました

### デバッグに使えるもの

- `M` キー: 照準の先にモブを 1 体（**湧きの条件に関わらず**出せます）。種類はランダム
- `F3`: デバッグ表示にモブ数（`mobs N`）・落ちているアイテム（`drops N`）・かまど（`furnaces N`）・
  **空腹（`food n/20`、毒なら `(毒)`）** が出ます
- 開発サーバーは `npm run dev`（5173 が埋まっていると 5174 になります）

### 計測するとき

**`npm run bench` は 1 回では当てになりません。** 同じコードで `generateChunk` が
0.93〜1.46ms に振れます。疑わしいときは `git worktree add <絶対パス> HEAD` で比較対象を
用意し、`node_modules` を symlink して**同じ条件で交互に測ってください**
（終わったら `git worktree remove`）。

```
world.update    中央 3.4ms / 最悪 10〜17ms（GC 込み）
モブ 40 体      中央 0.042ms / p99 0.14ms   ← world.update の 9ms 予算に対して誤差
落とし物 128 個 中央 0.10ms / p99 1.3〜3.7ms（GC 込み。上限いっぱいの数）
npm test        12 秒前後（943 項目）
```
