# 決まりごとの受け渡し（層 2・層 3 の更新待ち）

**無人の周はここに書きます。取り込むのは人が居る周です。**

`.claude/rules/**` と `.claude/skills/**` を編集しようとすると「sensitive file」の
確認が出て、**答える人が居ない周はそこで止まります**（実装もテストも全部終えたあとで、です）。
`.claude/settings.json` に許可を書いても消えませんでした（2026-08-25 実測。
リポジトリの中の設定が、自分と同じ `.claude/` の中身を編集する許可を自分に出すことは
できない、という形の防御だと思われます）。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 書き方

1 件 1 節。見出しは `## <どのファイル> — <一言>`。

- **どのファイルのどの節へ足すのか**を先に書く（節が無ければ「新しい節」と書く）
- **置きたい本文をそのまま置くこと。** 「モブの飛び方について書く」のような要約は不可 ——
  取り込む側が文章を書き直すことになり、決まりごとの言い回しが周ごとにばらけます
- `paths` を足したいときは、その行もそのまま書く

## 取り込む側（人が居る周）

1. 上から順に、書かれたとおりに `.claude/rules/` へ当てる
2. **当てた節は丸ごと消す**（残すと、どれが未取り込みか分からなくなります）
3. 取り込みだけで 1 コミットにしてよい

**新しいファイルを増やした周は、`paths` の行も書くこと。** 2-4c 〜 2-9 の 12 件には
`src/stronghold.ts` と `src/endportal.ts` の `paths` が付いていなかったので、
取り込む側が足しました（無いと、そのファイルを触っても書いた決まりごとが読まれません）。

---

（2-4c 〜 2-9 の 12 件 / 2-10 / 2-11a の 4 件 / 2-11b〜2-12b の 8 件 /
2-13a の 1 件 / 2-13b-1 の 3 件は 2026-08-27 に、2-13b-2 の 2 件（ボスの体力バー・クリア画面）は
2026-08-28 に取り込み済み）

## `.claude/rules/mobs.md` — 「ボス（`MobDef.boss`）と、回る飛び方（`MobDef.orbit`）」の節の末尾へ

### 攻め方を入れ替える（`MobDef.phases`）

**ドラゴンが最初の 1 体です**（近接 10 秒 → ブレス 12 秒 → 旋回 8 秒の 3 つを順ぐりに）。
飛ぶ・跳ぶ・回ると同じで、**違いは全部表の値**にしてあり、`update()` にも `step()` にも
「ドラゴンなら」は 1 行もありません。

- **新しい動き方は 1 つも足していません。** `MobPhase` が持つのは既にある 3 つの判断を
  出し入れする印だけです: `chase`（`Mobs.chasing()` が見る）/ `shoot`（`Mobs.fire()`）/
  `above`（`aimOrbit()` の高さ）。**新しい攻め方を足したくなったら、まず
  「既にある判断の組み合わせで書けないか」を見ること。**
- **`advancePhase()` は判断（5Hz）より前に回すこと。** あとに置くと、番が替わった
  フレームだけ 1 つ前の攻め方で向きと高さを決めます（0.2 秒引きずります）。
- **「何も仕掛けない番」を必ず 1 つ入れること。** 近接とブレスだけを回すと、ボスが
  常にどちらかを仕掛けている状態になり、**柱のクリスタルを割りに行く隙が消えます。**
- **撃つ番に入った瞬間の初弾は待たせること**（`advancePhase()` が `shootTimer` を
  入れ直す）。待たせないと、撃たない番のあいだに溜まった間隔で 1 発目がいきなり飛びます。
  **撃たない番では間隔を進めないこと**（`fire()` は番を見て**間隔を減らす前に**返る）。
- **`MobPhase.above` だけはプレイヤー基準の高さです**（ほかは中心の地面 `homeY` から）。
  撃ち下ろす番だからで、中心の地面から測ると**柱の上に登られたときに頭を取れず**、
  ブレスが柱の腹に当たって終わります。
- **回るモブの `ranged.range` は `HOSTILE_SIGHT` より短くなくてかまいません**
  （ブレイズの決まりはあちらが自分から寄っていくためのもの）。ドラゴンは輪
  （半径 20）の上から中心へ撃つので、**`orbit.radius` より長いこと**が要ります。
- **テストは「番に入れてから、その番の長さぶんだけ」回すこと**（`test/mobs.test.ts` の
  `enterPhase()`）。長く回すと次の番に入るので、「近接の番では撃たない」を測っている
  つもりでブレスの番の初弾を数えます（実際に 1 発数えました）。

## `.claude/rules/items-survival.md` — 末尾に新しい節（`paths` に `src/durability.ts` を足すこと）

**`paths` の行に `src/durability.ts` を足してください**（無いと、耐久値を触るときに
この節が読まれません）。

### 道具の耐久値（`durability.ts`）

**判断は全部 `durability.ts`** にあります（`vitals.ts` と `ui.ts` の関係と同じ形）。

| ファイル | 中身 | 確かめ方 |
| --- | --- | --- |
| `durability.ts` | **判断は全部ここ。** 回数の表・減るか・壊れたか・帯の割合・1 行・セーブの形 | `npm test` |
| `inventory.ts` | `Slot.damage` を持つだけ（書き出し・読み戻しは委譲） | `npm test` |
| `ui.ts` | `wearBar()` の割合を `style.width` に貼るだけ。**数値を書かない** | ブラウザ |

- **回数の表（`TOOL_USES`）を `items.ts` の `ToolDef` に持たせないこと。** あちらは
  「掘る速さ」の表で、混ぜると耐久値を触るたびにアイテムの表ごと動かすことになります。
  **速さと回数は別の表**です（片方だけ触ると「速いのにすぐ壊れる」が作れます）。
- **`durability.ts` が `inventory.ts` から取ってよいのは `import type { Slot }` だけ。**
  逆向き（`inventory.ts` → `durability.ts`）は普通の import なので、値を取ると輪ができます。
  だから `wearSlot()` は `clearSlot()` を呼ばず、自分で 3 つの値を戻します。
- **傷を捨てる／持っていく場所は 5 つで、全部 `inventory.ts` にあります**:
  `empty()` / `clearSlot()` / `addToSlots()` の空き枠 / `setSelected()` は 0 にし、
  **`swap()` だけが持っていきます。** 1 つでも忘れると、収納へ移した道具が新品に戻ります。
- **セーブは `SaveData.wear`（36 要素・省略可）。** `inventory` の `[item, count]` を
  3 要素にしないこと —— 既存のセーブが丸ごとずれます。**全部新品なら書き出さない**ので、
  道具を傷めていない人のセーブは耐久値が入る前と 1 バイトも変わりません。
  読み戻しは **`deserialize()` のあと**（何回使える道具かは中身で決まります）。
  **最大以上の値は `最大 - 1` に丸めること** —— そのまま入れると「壊れているのに
  手に残っている道具」になります。
- **傷はいまインベントリの 36 枠にしか乗りません。** 落とし物（`[item, count, x, y, z]`）・
  チェスト（54 要素）・`craftscreen.ts` の掴んでいる山は `item` と `count` しか運ばないので、
  **落として拾い直す／画面で動かすと新品に戻ります**（`TUNING.md` に 1 行。塞ぐのは
  `AUTODEV-QUEUE.md` の 3b）。**気付いた周に片手間で塞がないこと** —— セーブの形が 3 つ動きます。
- **耐久値は速さにも収穫にも効きません**（`mining.ts` の `breakTime()` / `canHarvest()` は
  傷を見ません）。減っても最後の 1 回まで同じ速さで掘れます（本家と同じ）。
- **減らないものを増やすときは `wearForBreaking()` の 4 つを見ること**:
  クリエイティブ / 道具でない物（素手を含む）/ 壊せないブロック / **硬さ 0**（松明・草）。
  **支えを失って壊れたぶん（`autoBreak()`）も減りません**（道具を見ない経路なので）。

## `.claude/rules/projectiles.md` — 「飛んでいるもの」の箇条書きの末尾へ

- **ブレスは火球とまったく同じ飛び方です**（`gravityScale` 0 / `drag` 0）。速さだけが
  2.5 倍（30m/s）で、**プレイヤーの 10 マス上から撃ち下ろす**ためのものです。
  山なりに戻すと、狙った所より手前へ落ちて当たりません。**「ブレスは雲のように漂う」
  という本家の像に寄せ直すなら、撃つ側（`DRAGON.phases` の高さと間隔）ごと考え直すこと。**

## `.claude/rules/inventory-screen.md` — 画面は傷を「運ぶだけ」（新しい節を末尾へ）

**そのまま置いてください:**

### 道具の傷（`damage`）を運ぶ

**画面のどの操作でも、傷は `item` / `count` と一緒に動きます。**
運ぶ場所は 6 つで、**どれも `durability.ts` の `damageOf()` と `carryWear()` を通します**:
`transfer()`（掴む・置く・入れ替え）/ `swapHotbar()`（数字キー）/ `release()`（ドラッグ配分）/
`returnSlot()`（閉じるときの返却）/ `quickMove()`（シフトクリック）/
`inventory.ts` の `addToSlots()`（末尾の `damage` は**既定 0 の省略可**。必須にすると
`chests.ts` に手が要ります）。

- **`slot.damage ?? 0` を画面の側に書かないこと。** 「道具でなければ 0」の判断が
  その場ごとに散り、棒の山に傷が付く経路が 1 つずつ増えます。読むのは `damageOf()`、
  載せるのは `carryWear()`（**`to.item` を入れたあとで呼ぶこと**）。
- **入れ替えるときは、書き始める前に両方の傷を読んでおくこと。** 途中で読むと
  2 つ目が書き換えたあとの値を拾います（`Inventory.swap()` と同じ形）。
- **`craftscreen.ts` が `durability.ts` から取ってよいのは `damageOf` / `carryWear` /
  `serializeWear` / `deserializeWear` の 4 本だけ。** `maxUses` / `TOOL_USES` / `wearSlot` が
  要ったら「画面が耐久値を減らす」設計になっている合図です（`test/durability.test.ts` が
  `craftscreen.ts` に回数と `wearSlot(` が出てこないことを見張っています）。
- **山に傷を持たせないこと。** 傷が付く物は全部 `stack: 1`（`items.ts` の道具 12 本）なので、
  傷はいつも 1 個ずつ動きます。`count > 1` の枠に傷を載せる経路を作ると、
  「半分だけ傷んだ山を割る」話が要ります。
- **クリエイティブの一覧から出したものは必ず新品**（`pressCreative()` が `carryWear(held, 0)`）。
  湧き口なので、傷んだ道具を掴んだ上から出しても引き継ぎません。
- セーブは **`SaveData.craftWear`（10 要素・省略可）**で、`craft` の 20 要素とは別のキーです
  （**30 要素にすると既存のセーブが丸ごとずれます**。`wear` を `inventory` と分けたのと同じ理由）。
  読む順は **`craft.deserialize()` → `craft.deserializeWear()` → `craft.returnAll()`** ——
  **`returnAll()` より後に傷を戻すと、返した先（インベントリ）に載りません。**
- **落とし物・チェスト・かまどの中身はまだ傷を持ちません**（`TUNING.md`。塞ぐのは
  `AUTODEV-QUEUE.md` の 3c）。**チェストを開いてシフトクリックすると新品に戻ります** ——
  `addToChest()` は `chests.ts` の仕事なので、画面の側で塞ごうとしないこと。

## `.claude/rules/drops.md` — 「落ちたアイテム」の節の、保存の項を差し替え

（いまある「**モブと逆に、ドロップは保存します**…」の項を、次の 2 項に置き換えてください。
1 つ目は `[item, count, x, y, z]` だけを保存すると書いてあって、いまは合っていません。）

- **モブと逆に、ドロップは保存します**（`storage.ts` の `drops`）。モブはシードから作り直せますが、
  ドロップは**プレイヤーが掘って出したものそのもの**で、`edits` と同じ性質です。消せば
  「集めた物が黙って消えた」になります。**保存するのは `[item, count, x, y, z]`**（1 山 5 要素）で、
  寿命と猶予は持ちません（読み込み直後に足元の物が消えるより、5 分の猶予が戻るほうが安全側）。
- **道具の傷は別の省略可キー `dropWear`**（1 山 1 要素・並びは `drops` と同じ）。
  **`drops` を 6 要素にしないこと** —— 増やすと既存のセーブが丸ごとずれます
  （`wear` を `inventory` と、`craftWear` を `craft` と分けたのとまったく同じ理由）。
  **全部新品なら `serializeWear()` が `undefined` を返してキーごと消えます。**
  - **傷を載せる入口は `spawn()` の `options.damage` 1 か所だけ**（`burst()` も `throwOut()` も
    セーブの読み戻しもここを通ります）。**`drop.damage` に直に代入しないこと** ——
    道具でないものに傷が付く経路がその場ごとに増えます
  - **読み戻すときの傷は平坦配列の位置（`i / 5`）から引くこと。** 壊れた山は飛ばすので、
    `list` の並びから引くと、そこから先の傷が 1 つずつずれます
  - **`drops.ts` が `durability.ts` から取ってよいのは `damageOf` / `carryWear` / `wornValue` の
    3 本だけ**です。`maxUses` / `TOOL_USES` / `wearSlot` が要ったら「地面が耐久値を減らす」
    設計になっている合図（減らすのは掘る側だけ）。`test/drops.test.ts` が見張っています
  - **傷んだ道具は 1 山になりません**（`stack: 1` なので統合の余地が 0）。
    `mergeAll()` に「傷が違えば統合しない」を書き足さないこと —— 半端に傷んだ山を割る話が始まります
  - **チェストとかまどの中身はまだ傷を運びません**（`AUTODEV-QUEUE.md` の 3c-2）。
    壊したときに出る中身は `breaking.ts` の `BreakDrop` を通って落とし物になるので、
    塞ぐのはそちらに省略可キーを足す周です

## `.claude/rules/items-survival.md` — 末尾の「道具の耐久値はまだありません」の 1 行を差し替え

**道具の耐久値は `durability.ts`**（木 59 / 石 131 / 鉄 250 / ダイヤ 1561。掘るたびに 1 減り、
0 で壊れます）。**傷はインベントリ・クラフト画面・地面の落とし物まで付いて回ります**
（セーブは `wear` / `craftWear` / `dropWear` の 3 つの省略可キー）。
**読んだ値をどこまで信じるかは `wornValue()` 1 本**で、最大以上は `最大 - 1` に丸めます
（そのまま入れると「壊れているのに手に残っている道具」になります）。
**チェストとかまどの中身はまだ新品に戻ります**（`TUNING.md`）。

（この節を当てるときは `items-survival.md` の `paths` に `src/durability.ts` を足すこと。
`drops.md` の `paths` にも `src/durability.ts` を足してください —— 落とし物が傷を運ぶように
なったので、`durability.ts` を触ったときに上の 3 本の決まりが目に入る必要があります。）
