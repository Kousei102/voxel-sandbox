# スキル（層 3）の受け渡し

**無人の周はここに書きます。取り込むのは人が居る周です。**

`.claude/**` を `Edit` / `Write` しようとすると「sensitive file」の確認が出て、
**答える人が居ない周はそこで止まります**（実装もテストも全部終えたあとで、です）。
`.claude/settings.json` に `Edit(.claude/skills/**)` と書いても消えません（2026-08-25 実測）。
**スキルは `.claude/skills/` から動かせません**（外に置くと呼び出せない）ので、
ここだけは書き置きで渡します。更新が稀（3 本）なので、それで釣り合っています。

**層 2（決まりごと）はここを通りません**（2026-09-04）。**`rules/*.md` を `Edit` で
直接直すこと** —— `.claude/` の外なので無人の周でも確認は出ません。手順は `LOOP.md` の 4.5、
書き方は `rules/README.md`。**次の周へ書き置かないこと**: 受け渡しを 1 周空けると
本文どうしが食い違い、取り込む側が毎回それを捌くことになります
（**その実例が `docs/rules-inbox-archive.md`** に 5 周ぶん残っています）。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 書き方

1 件 1 節。見出しは `## <どのファイル> — <一言>`。

- **どのファイルのどの節へ足すのか**を先に書く（節が無ければ「新しい節」と書く）
- **置きたい本文をそのまま置くこと。** 「モブの飛び方について書く」のような要約は不可 ——
  取り込む側が文章を書き直すことになり、決まりごとの言い回しが周ごとにばらけます

## 取り込む側（人が居る周）

1. 上から順に、書かれたとおりに `.claude/skills/` へ当てる
2. **当てた節は丸ごと消す**（残すと、どれが未取り込みか分からなくなります）
3. 取り込みだけで 1 コミットにしてよい

---

**いま未取り込みは 0 件です。**

（2026-09-04 に、AUTODEV 19 が書いた層 2 の 3 件 —— `projectiles.md` の 5 種類 /
`use.md` の 15 通り / `testing.md` の山なりの的 —— を `rules/` へ当て、
**層 2 をこのファイルから外しました。** それ以前の取り込みの記録は
`docs/rules-inbox-archive.md`）
