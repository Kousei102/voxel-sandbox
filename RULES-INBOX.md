# 決まりごとの受け渡し（層 2・層 3 の更新待ち）

**無人の周はここに書きます。取り込むのは人が居る周です。**

`.claude/rules/**` と `.claude/skills/**` を編集しようとすると「sensitive file」の
確認が出て、**答える人が居ない周はそこで止まります**（実装もテストも全部終えたあとで、です）。
`.claude/settings.json` に許可を書いても消えませんでした（2026-08-25 実測。
リポジトリの中の設定が、自分と同じ `.claude/` の中身を編集する許可を自分に出すことは
できない、という形の防御だと思われます）。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 書き方

1 件 1 節。見出しは `## <どのファイル> — <一言>`。

- **どのファイルのどの節へ足すのか**を先に書く（節が無ければ「新しい節」と書く）
- **置きたい本文をそのまま置くこと。** 「モブの飛び方について書く」のような要約は不可 ——
  取り込む側が文章を書き直すことになり、決まりごとの言い回しが周ごとにばらけます
- `paths` を足したいときは、その行もそのまま書く

## 取り込む側（人が居る周）

1. 上から順に、書かれたとおりに `.claude/rules/` へ当てる
2. **当てた節は丸ごと消す**（`REVIEW.md` と同じ。残すと、どれが未取り込みか分からなくなります）
3. 取り込みだけで 1 コミットにしてよい

**新しいファイルを増やした周は、`paths` の行も書くこと。** 2-4c 〜 2-9 の 12 件には
`src/stronghold.ts` と `src/endportal.ts` の `paths` が付いていなかったので、
取り込む側が足しました（無いと、そのファイルを触っても書いた決まりごとが読まれません）。

---

（2-4c 〜 2-9 の 12 件は 2026-08-26 に取り込み済み。以下は 2-10 の 3 件）

## `.claude/rules/worldgen.md` — 新しい節「エンド（`endgen.ts`）」を末尾に足す

`paths` に `src/endgen.ts` を足すこと（無いと、あのファイルを触っても読まれません）。

置きたい本文:

```markdown
## エンド（`endgen.ts`）

**`worldgen.ts` / `nethergen.ts` と同じ `ChunkSource`** です。ネザーが「床と天井の 2 枚」
だったのに対し、こちらは **「原点のまわりの島 1 つだけが地面で、あとは全部虚空」**。
バイオームも木も鉱石も構造物もありません。

- **岩盤を 1 マスも置かないこと。** 底を塞ぐと、島から落ちた人が世界の底に立ってしまい、
  「落ちたら死ぬ」という虚空の意味がまるごと消えます（`vitals.ts` の `VOID_Y` が
  効くのは、下に何も無いからです）。**上下の岩盤を先に置く**ネザーとは逆の作法です。
- 形の全部が **`blockAt(y, 上面, 下面)` の 1 本**にあります（`nethergen.ts` と同じ作法）。
  テストはここを直に呼んで断面を確かめられるので、**分岐を `generateChunk` 側へ移さないこと。**
- **守っている不変条件は 1 つだけ: 出る場所（`END_SPAWN`）が必ず地面の上にあること。**
  崩れると**ポータルを通った人が虚空に出て即死します**（しかもセーブに残らないので、
  何が起きたか分かりません）。だから
  - **`LANDING_RADIUS`(5) の内側は上面が `ISLAND_SURFACE` ちょうど**で、
    `LANDING_RAMP`(10) マス掛けてうねりに繋ぎます。**急に切り替えないこと**
    （3 マスの段差が輪になって出ます）
  - ふちの揺れ（`EDGE_WOBBLE`）を大きくしすぎないこと。**いちばん狭い所でも
    `LANDING_RADIUS * 2` は残す**のをテストが見ています
- **出る場所は `endgen.ts` が持ちます**（`END_SPAWN`）。**`portaltravel.ts` に写さないこと** ——
  「そこが地面であること」を保証しているのはこのファイルなので、点と保証は同じ所に置きます。
- **ふちの揺れは面積では測れません。** 真円でも面積は半径どおりになるので、
  「円の何 % か」の判定は真円をそのまま通します。**ふちの半径そのもの**（`reachAt`）を
  360 方向で測ること（`test/endgen.test.ts`）。
- **天井が無いのでスカイライトは全開**ですが、空の明るさが固定 0.7
  （`daynight.ts` の `SKY_STYLES.end`）なので薄暗いままです。
  **その結果、島の上には敵対モブが湧きません**（`spawnLight` = 15 * 0.7 = 10.5 >
  `HOSTILE_LIGHT_MAX` の 7）。受動モブは地面が草でないと湧かないので、**エンドは無人**です。
  本家のエンダーマンを湧かせるなら、`spawnOn` に `END_STONE` を足すだけでは足りません
  （暗さの条件に当たります）。
- 実測（シード 12345）: 地面の列 7810 / エンドストーン 86,878 マス / 上面 45〜51 /
  ふちの半径 39.1〜57.4。**生成はネザーよりさらに速い**（0.116ms 対
  オーバーワールド 2.87ms。ノイズ 2 本と足し算だけのため）。
```

## `.claude/rules/dimensions.md` — 「ポータルを通る（`portaltravel.ts`）」の節に足す

**節の末尾**（「同じ次元なら移らないこと」の次）に足すこと。

置きたい本文:

```markdown
### ポータルが 2 種類になってから

**行き先は「いまどこに居るか」だけでは決まりません。** オーバーワールドには
ネザーポータルとエンドポータルの両方が建つので、**踏んだ面の種類**も要ります。
種類ごとの違いは**型 1 つ（`PortalHere`）に載せて運びます。**

- **面を見分けるのは `portalAt(id)` の 1 か所だけ。** `main.ts` に `id === END_PORTAL` と
  書き始めると、ポータルを足すたびに「踏んだか」「どこへ行くか」「どこに出るか」の
  3 か所を直して回ることになり、必ず 1 つ忘れます。
- **ネザーポータルだけが `axis` を持ちます**（向こう側に枠を建てるときに要るため）。
  エンドの面は寝ていて向きが 1 つしか無く、枠も建てません。
- **行き先と目指すマスを決めるのは `planTravel()` 1 本**、**出る場所を決めるのは
  `arriveThrough()` 1 本**です。種類で分かれるのはこの 2 か所だけで、`main.ts` は
  受け取った値を貼るだけ（`test/ui.test.ts` が次元の分岐を数えていて、上限は 2 です）。
- **エンドへは座標を掛け算で移さないこと。** 島は原点のまわりにしかないので、
  ネザーの 1:8 と同じ形にすると**要塞が原点から遠い人だけが虚空に出て即死します**
  （要塞は数百マス先に建つので、ほぼ全員がそうなります）。行き先は
  `endgen.ts` の **`END_SPAWN`（島の中心）に固定**です。
- **エンドで `arrive()` を使い回さないこと。** あちらは「枠を探して、無ければ建てる」なので、
  **島の真ん中に黒曜石の枠が生えて、そこからネザーへ抜けられます。**
  エンドは `landOnGround()`（島の上に降ろすだけ・何も置かない）を通します。
- **エンドからは戻れません**（本家と同じで、出口はドラゴンを倒すまで開きません）。
  `portalTarget(END, "end")` は定義してありますが、**エンドに面が 1 枚も無い**ので
  いまは通りません。落ちて死ねばリスポーン地点へ戻れるので、詰みにはなりません。
  出口を建てるのは 2-13（ドラゴン）です。
```

## `.claude/rules/blocks-shapes.md` — 「ID の枠の細かい話」の節の末尾に足す

置きたい本文:

```markdown
**その次元にしか無いブロックも 1..63 を使います。** エンドストーン（50）が
オーバーワールドに 1 マスも湧かなくても、**立方体である以上 `encodeFace` を通る**ので
63 までしか置けません。次元を足すたびに 1..63 が減るということなので、
**次元ごとの地面を何種類も足す前に空きを見ること**（`npm test` が毎回出します）。
```

（以下は 2-11a の 1 件）

## `.claude/rules/worldgen.md` — 「エンド（`endgen.ts`）」の節の末尾に足す

**上の 2-10 の節（まだ取り込まれていなければ、それ）の末尾に足すこと。**
取り込み済みなら、その節の最後に続けて置いてください。

置きたい本文:

```markdown
### 黒曜石の柱（`PILLARS`）

島の上には**黒曜石の柱が 10 本**立ちます（本家と同じ本数）。**`structures.ts` の器には
乗せていません** —— あちらは `SiteRule`（グリッドに確率で撒く）で、
**エンドの柱は「原点のまわりの決まった場所に必ず 10 本」**という別の性質のものです。
島の中心が原点だと分かっているので、`endgen.ts` の中で直に建てています。

- **位置も高さも種に依りません**（`PILLARS` は `seed` を受け取りません）。
  ドラゴンの戦場（2-13）は誰のワールドでも同じ形になります。
- **柱の並びの出どころは `PILLARS` 1 か所。** 上に載せるもの（エンドクリスタル）も
  この表を読むこと。**写して 2 か所に持つと、柱の無い所にクリスタルが浮きます**
  （しかも空中なので、下から見上げるまで気付けません）。
- **柱を立ててよい半径は式で決めること**（`EDGE_MIN_REACH` = `ISLAND_RADIUS * (1 - EDGE_WOBBLE)`）。
  `reachAt()` の揺れは `(n / 1.2) * 2 * EDGE_WOBBLE` で、`fbm2` の実効レンジが ±0.6 なので
  揺れは `±EDGE_WOBBLE` に収まります。**実測した最小のふち（ある種で 39.1）で線を引かないこと**
  —— 別の種で虚空に柱が浮きます。
- **柱の上面は絶対の高さ（`ISLAND_SURFACE + height`）で止めること。** 上面が平らになり、
  島のうねり（±3）は根元で吸収されます。**列ごとの上面から測る形にすると上面が凸凹になり、
  クリスタルが傾いて載ります。**
- **虚空の列には柱を置かないこと。** 保険は `blockAt()` の `VOID_COLUMN` の 1 行だけで、
  **`pillarTopAt()` は島の形を知りません**（種を受け取らないので知りようがありません）。
  ここを通り越して柱を置くと、島の外に黒曜石が浮きます。
- **走査を早く切る帯（`PILLAR_NEAR` / `PILLAR_FAR`）は、実際の柱の中心から出すこと。**
  `PILLAR_RING ± PILLAR_MAX_RADIUS` で決め打ちにすると、中心をマスに丸めてあるぶんだけ
  **柱のふちが 1 マス欠けます**（欠けた所から中が見えます）。テストが総当りと突き合わせています。
- **高さは高い・低いを交互に並べること。** 昇順に並べると、輪がぐるりと一周する坂に見えます。
- 実測: 柱の列 210 マス / 輪は内 25.0〜外 31.5 / 柱どうしの隙間 11.8 マス /
  上面 y60〜87。**生成の費用は変わっていません**（エンドのチャンク 1 個 0.115ms）。
```
