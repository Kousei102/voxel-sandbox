# 決まりごとの受け渡し（層 2・層 3 の更新待ち）

**無人の周はここに書きます。取り込むのは人が居る周です。**

`.claude/rules/**` と `.claude/skills/**` を編集しようとすると「sensitive file」の
確認が出て、**答える人が居ない周はそこで止まります**（実装もテストも全部終えたあとで、です）。
`.claude/settings.json` に許可を書いても消えませんでした（2026-08-25 実測。
リポジトリの中の設定が、自分と同じ `.claude/` の中身を編集する許可を自分に出すことは
できない、という形の防御だと思われます）。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 書き方

1 件 1 節。見出しは `## <どのファイル> — <一言>`。

- **どのファイルのどの節へ足すのか**を先に書く（節が無ければ「新しい節」と書く）
- **置きたい本文をそのまま置くこと。** 「モブの飛び方について書く」のような要約は不可 ——
  取り込む側が文章を書き直すことになり、決まりごとの言い回しが周ごとにばらけます
- `paths` を足したいときは、その行もそのまま書く

## 取り込む側（人が居る周）

1. 上から順に、書かれたとおりに `.claude/rules/` へ当てる
2. **当てた節は丸ごと消す**（`REVIEW.md` と同じ。残すと、どれが未取り込みか分からなくなります）
3. 取り込みだけで 1 コミットにしてよい

**新しいファイルを増やした周は、`paths` の行も書くこと。** 2-4c 〜 2-9 の 12 件には
`src/stronghold.ts` と `src/endportal.ts` の `paths` が付いていなかったので、
取り込む側が足しました（無いと、そのファイルを触っても書いた決まりごとが読まれません）。

---

（2-4c 〜 2-9 の 12 件 / 2-10 / 2-11a の 4 件 / 2-11b〜2-12b の 8 件 / 2-13a の 1 件は
2026-08-27 までに取り込み済み。下の 3 件が未取り込みです）

## `.claude/rules/exitportal.md`（新しいファイル）— エンドの出口ポータル

**`paths` の行を含めて、この節をそのままファイルにしてください。**

```yaml
---
paths:
  - "src/exitportal.ts"
  - "src/endgen.ts"
  - "test/exitportal.test.ts"
---
```

## エンドの出口ポータル（`exitportal.ts`）

**ドラゴンを倒した印であり、帰り道でもあります。** 2 つを別々に持たないこと ——
片方だけ建った状態（倒したのに帰れない／帰れるのに湧き直す）を作れます。

| ファイル | 中身 | 確かめ方 |
| --- | --- | --- |
| `exitportal.ts` | **判断は全部ここ。** 面の形・立っているかの読み方・建て方・持ち越し | `npm test` |
| `endgen.ts` | **居場所**（`EXIT_PORTAL_SPOT`）。平らな地面だと保証している側 | `npm test` |
| `mobs.ts` | 印が立っているなら湧かせない（**印そのものは知らない**） | `npm test` |
| `main.ts` | 繋ぎだけ（`syncExitPortal()` の返り値を `ensureBoss()` へ渡す） | ブラウザ |

- **印はワールド（＝`edits`）に持たせます。** エンドクリスタルをモブではなくブロックに
  したのとまったく同じ筋で、**読み込み直しても残り**、**新しいセーブのキーが要りません**
  （`SaveData.version` は 1 のまま）。`Mobs` 側の記憶（`summoned` / `bossDefeated()`）は
  `clear()` で消えるので、**世界をまたぐ印にはできません。**
- **倒したかどうかは 2 段です。** この読み込みのあいだは `Mobs.bossDefeated()`
  （「召喚したのに、もう居ない」で決める。`boss` は遠くても消えず上限でも間引かれないので、
  居なくなる理由は倒された以外にありません）。**世界をまたぐのは出口ポータルのほう**で、
  `main.ts` が `syncExitPortal(world, mobs.bossDefeated(dim))` の返り値を
  `ensureBoss()` の第 3 引数へ渡します。**`mobs.ts` から `exitportal.ts` を見に行かないこと**
  （`MobContext.healers` とまったく同じ形で、モブの表がエンドの地形に縛られます）。
- **居場所（`EXIT_PORTAL_SPOT`）は `endgen.ts` が持ちます**（`END_SPAWN` と同じ理由 ——
  そこが平らな地面だと保証しているのがあのファイルなので、**点と保証は同じ所に置く**）。
  写して 2 か所に持つと、島の形を変えたときに**面が崖の途中に建って踏めない**形で
  静かに壊れます。`test/endgen.test.ts` が「3x3 が `LANDING_RADIUS` の内側」と
  「どの種でも平らで空いている」を見ています。
- **降りる場所（`END_SPAWN`）と重ねないこと。** 重ねると、オーバーワールドから降りた人が
  着いた瞬間に面の中に立ち、掛け金（`PortalGate.latch`）が外れた次の一歩で引き返されます
  （＝島に立てない）。3 マス離してあります。
- **面は地面の 1 つ上に置くこと**（立つ人の足元と同じ段）。`main.ts` が見るのは足元のマスなので、
  地面の上面に埋めると**上を歩いても踏んだことになりません。**
- **床（黒曜石）を先に置くこと。** 面は通り抜けられるので、下を掘られていると踏んだ人が
  抜けて虚空へ落ちます。上書きでかまいません（そこの上面は必ず `ISLAND_SURFACE`）。
- **9 マスとも見て「建っている」を決めること。** 中心 1 マスだけで決めると、書き込みが
  途中で落ちた**穴あきの面が二度と埋まりません**（`setVoxel` は未読み込みの列で黙って
  失敗します）。読めない列は `unknown` で、**「無い」と読まないこと** ——
  読むと、倒した人のエンドにドラゴンがもう 1 体湧きます（かまどの `syncLit()` /
  クリスタルの `crystalState()` と同じ罠）。分からないうちは湧かせないほうが安全側です。
- **倒していないあいだは 1 マスも書かないこと**（`defeated` が false なら読むだけ）。
  ボスの居ない次元でも毎フレーム呼ばれるので、ここが漏れると**別の次元に帰り道が生えます。**

## `.claude/rules/dimensions.md` — 「ポータルが 2 種類になってから」の最後の項を差し替え

**いまある「**エンドからは戻れません**〜出口を建てるのは 2-13（ドラゴン）です。」の項を、
下の 2 項に置き換えてください。**

- **エンドから戻れるのは、ドラゴンを倒したあとだけです。** 倒すと島の中心の脇に
  エンドポータルの面が建ち（`exitportal.ts`。`rules/exitportal.md`）、そこを踏むと
  `portalTarget(END, "end")` でオーバーワールドへ戻ります。**倒す前は面が 1 枚も無い**ので
  通れません（落ちて死ねばリスポーン地点へ戻れるので、詰みにはなりません）。
- **帰りは座標を移しません**（`planTravel()` がいまの座標をそのまま使います）。
  島の中心は原点なので、オーバーワールドの原点のあたりに出ます。**ネザーの 1:8 を
  使い回さないこと** —— 行きも帰りも掛け算をしないのがエンドの約束です。

## `.claude/rules/mobs.md` — 「ボス（`MobDef.boss`）」の節に足す

- **ボスを倒した印は 2 段で持ちます。** この読み込みのあいだは `Mobs.bossDefeated()`
  （`summoned` に入っているのに `list` に居ない＝倒された。**別の印を足さないこと** ——
  `summoned` と 2 つに分かれると、`clear()` で片方だけ消える形を作れます）。
  **世界をまたぐ印はワールドに建つもの**（エンドなら出口ポータル。`rules/exitportal.md`）で、
  `ensureBoss()` は**それが立っているかを第 3 引数で受け取るだけ**です。
  **`mobs.ts` からその印を見に行かないこと**（`MobContext.healers` と同じ形）。
- **`ensureBoss()` の第 3 引数に既定値を付けないこと。** 付けると、印を渡し忘れた
  呼び出しが型で通り、**倒しても湧き直すエンド**が黙って戻ります
  （2-13b-1 では、必須にしてあるおかげで 10 か所の呼び出しが全部コンパイルで止まりました）。
