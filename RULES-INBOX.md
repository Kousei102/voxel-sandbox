# 決まりごとの受け渡し（層 2・層 3 の更新待ち）

**無人の周はここに書きます。取り込むのは人が居る周です。**

`.claude/rules/**` と `.claude/skills/**` を編集しようとすると「sensitive file」の
確認が出て、**答える人が居ない周はそこで止まります**（実装もテストも全部終えたあとで、です）。
`.claude/settings.json` に許可を書いても消えませんでした（2026-08-25 実測。
リポジトリの中の設定が、自分と同じ `.claude/` の中身を編集する許可を自分に出すことは
できない、という形の防御だと思われます）。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 書き方

1 件 1 節。見出しは `## <どのファイル> — <一言>`。

- **どのファイルのどの節へ足すのか**を先に書く（節が無ければ「新しい節」と書く）
- **置きたい本文をそのまま置くこと。** 「モブの飛び方について書く」のような要約は不可 ——
  取り込む側が文章を書き直すことになり、決まりごとの言い回しが周ごとにばらけます
- `paths` を足したいときは、その行もそのまま書く

## 取り込む側（人が居る周）

1. 上から順に、書かれたとおりに `.claude/rules/` へ当てる
2. **当てた節は丸ごと消す**（残すと、どれが未取り込みか分からなくなります）
3. 取り込みだけで 1 コミットにしてよい

**新しいファイルを増やした周は、`paths` の行も書くこと。** 2-4c 〜 2-9 の 12 件には
`src/stronghold.ts` と `src/endportal.ts` の `paths` が付いていなかったので、
取り込む側が足しました（無いと、そのファイルを触っても書いた決まりごとが読まれません）。

---

（2-4c 〜 2-9 の 12 件 / 2-10 / 2-11a の 4 件 / 2-11b〜2-12b の 8 件 /
2-13a の 1 件 / 2-13b-1 の 3 件は 2026-08-27 に、2-13b-2 の 2 件（ボスの体力バー・クリア画面）は
2026-08-28 に、ドラゴンの `phases` と耐久値の 8 件と剣 4 本の 2 件は 2026-09-02 に、
**AUTODEV 5 / 6 / 12 / 13 / 14 の 14 件（シアーズ・クワ・種・苗・2 山）は 2026-09-03 に
取り込み済み**）

**いま未取り込みは 3 件です**（2 件は AUTODEV 15・パンの周、1 件は AUTODEV 16 の B の周）。

---

## `vitals.md` — 「空腹」の節の「食べ物の表は…」の行の**すぐ下**に足す（新しい箇条書き）

- **食べ物を足すのに要るのは `items.ts` の 2 行だけです**（`item({...})` 1 行と
  `FOODS` の 1 行）。**`vitals.ts` にも `use.ts` にも `main.ts` にもアイテムの名前を
  書かないこと** —— 右クリックが「食べる」に来るか（`use.ts` の `decideUse()`）も、
  食べ終わるまでの長さと中断の条件（`vitals.ts` の `Eating`）も、クリエイティブの一覧
  （`items.ts` の `allItemIds()`）も、**もう `foodOf()` 1 本を通っています。**
  **パン（125）はこれを実際に確かめた 4 つ目の食べ物で、`src/` は 6 行しか増えていません**
  （2026-09-03。`test/use.test.ts` の「パンも同じ経路で食べられる」が、次に足す人が
  `use.ts` を触り始めていないかを見張っています）。
- **食べ物どうしの強さは表の中だけで決めること。** いまは
  生豚肉 3 / 1.8 ＜ 腐った肉 4 / 0.8（毒つき）＜ **パン 5 / 6** ＜ 焼き豚 8 / 12.8 で、
  **パンが「かまど無しで作れる中では一番強い」**という一点だけが理由です
  （焼く見返りを残すため、焼き豚には届かせていません。`TUNING.md`）。

## `items-survival.md` — 末尾の「モブのドロップは…」の段落を差し替える

**古い本文と食い違います**（取り込む人へ: 新しいほうを採ってください）。
古いほうは「**肉は食べられます**」とだけ書いていて、**食べ物 = 肉だったころの文**です。
いまは肉でない食べ物（パン）があるので、下の本文に置き換えてください。

> モブのドロップは羊毛（ブロック 37）・生豚肉（アイテム 81）・腐った肉（アイテム 82）。
> かまどで焼くと焼き豚（アイテム 83）になります。**食べられるものの表は `items.ts` の
> `FOODS` 1 本**で、**肉だけではありません** —— **パン（125）は小麦 3 個の横一列**
> （`crafting.ts`。3 幅なので作業台が要ります）で作る、**畑から取れる食べ物**です。
> **小麦（124）そのものは食べられません**（本家と同じで、パンにしてから食べます。
> `test/blocks.test.ts` が `foodOf(WHEAT) === null` を見張っています）。
> 何がどれだけ戻るかは `rules/vitals.md` を見てください。

## `mobs.md` — 冒頭の 4 行の表の**すぐ下**に足す（新しい箇条書き 1 つ）

- **モブを 1 種類足すのに `mobmesh.ts` と `mobrender.ts` は 0 行です。**
  `buildMobMesh()` は `MobDef` の `groups` / `boxes` を回すだけ、`MobRenderer` は
  `MOBS[kind]` から形を作って使い回すだけで、**どちらも種類の名前を 1 つも知りません。**
  だから**新しいモブは「確かめられないもの」を増やしません** ——
  `unverifiable-pair` スキルは要らず、足すのは `mobs.ts` の表 1 つと
  （落とし物があれば）`items.ts` の行だけです。**形の点検は `MOB_KINDS` を回す
  `test/mobs.test.ts` に自動で乗ります**（体積・巡回順・振る部位の `y1 === 0`・
  当たり判定に収まるか）。**ここに手が要ったら、それは表で表せないものを足そうと
  している合図**です（`MobMotion` を増やす・`mobrender.ts` に分岐を書く、など）。
- **`test/mobs.test.ts` の `longBody`（当たり判定より胴が長い例外）は豚と羊だけです。**
  ほかのモブは**後ろ（+Z）もはみ出せません**。新しいモブが収まらないときは、
  **`longBody` に足すのでも `size` を広げるのでもなく、形を削ること** ——
  足すと、そのモブの形の点検が丸ごと外れます。

## `mobs.md` — 上の「`mobs.md` — 冒頭の 4 行の表の…」の節の**すぐ下**に続けて足す（2026-09-03・鶏の周）

**上の節と食い違いません（裏が取れました）。** 前の周（B）は**コードを読んで**
「描画は 0 行」と書きましたが、この周に**実際に鶏を足して確かめました** ——
`mobmesh.ts` も `mobrender.ts` も `main.ts` も **1 行も触らずに** 7 種類目が動き、
`test/mobs.test.ts` に鶏ぶんの点検が 11 件、1 行も書かずに増えました。
そのうえで**新しく分かったこと 2 つ**を足してください。

- **形が「全部のテストに通っているのに見えない」ことがあります。** 鶏の翼を体と同じ幅
  （±3px）に置いたら、**白い体に白い翼が埋まって、振れても 1 画素も動きませんでした。**
  体積も巡回順も振る部位の `y1 === 0` も当たり判定も**全部緑**です（箱としては正しいので）。
  **`npm run shot -- mobs` を撮って `Read` で開くまで気付けません。**
  **部位の色が隣の部位と同じなら、必ず輪郭を外へ出すこと**（鶏は体を ±2.5px に狭めて
  翼を 0.6px ぶん外へ出しました。判定の縁 ±3.2px には触れていません）。
- **`test/mobs.test.ts` には「種類を足すと壊れる決め打ち」が 2 か所あります。**
  1 つは `census()` の `counts`（`{ 豚, 羊, 鶏, ゾンビ }` の直書き。**名前を足し忘れると
  `counts[name]++` が `NaN` になり、数えているつもりで何も見ていない状態で緑になります**）。
  もう 1 つは湧きの節にあった「両方の種類が湧く」の `=== 2` で、**受動が 3 種類目に
  なった瞬間に赤くなりました。** 後者は**表から数える形**
  （`MOB_KINDS.filter((k) => !MOBS[k].hostile).every(...)`）に替えてありますが、
  **替えるときは `=== 2` が拾えていた「余計な種類が混じらない」を別の 1 件として
  必ず足すこと** —— 替えただけだと、明るい草地に敵対が湧いても緑になる形にゆるみます。
- **受動モブを足すのは `PASSIVE_KINDS` に 1 語だけ**です（`trySpawn()` には 1 行も
  要りません）。**足し忘れると、表にあるのに 1 体も湧かないモブが黙って残ります** ——
  「昼の草地にその種類が湧く」を 1 件足して見張ってください。

## `vitals.md` — 上の「`vitals.md` — 「空腹」の節の…」で足す 2 つ目の箇条書きを、下の本文に差し替える（2026-09-03・鶏の周）

**取り込む人へ: 上の節（パンの周）の 2 つ目の箇条書きと食い違います。新しいこちらを
採ってください**（食べ物が 4 つ → 6 つに増え、並びが変わりました）。上の節の
**1 つ目の箇条書き（「食べ物を足すのに要るのは `items.ts` の 2 行だけ」）はそのままです。**

> - **食べ物どうしの強さは表の中だけで決めること。** いまは
>   **生鶏肉 2 / 1.2** ＜ 生豚肉 3 / 1.8 ＜ 腐った肉 4 / 0.8（毒つき）＜ パン 5 / 6 ＜
>   **焼き鳥 6 / 7.2** ＜ 焼き豚 8 / 12.8 で、**焼き豚がいちばん強い**という一点が
>   動いていないことがすべてです（焼く見返りを残すため。`TUNING.md`）。
>   **パンは「かまど無しで作れる中では一番強い」**、**焼き鳥はパンより上・焼き豚より下**。
> - **`FoodDef` に「確率」はありません**（`hunger` / `saturation` / `poison` の 3 つだけ）。
>   だから**本家の「生鶏肉は 30% で食中毒」は表せず、毒なしにしてあります**（2026-09-03）。
>   **確率を足したくなったら、それは `items.ts` だけでは済まない合図**です ——
>   毒を入れるのは `vitals.ts` の `eat()` なので、あちらまで及びます。

## `items-survival.md` — 末尾の「モブのドロップは…」の段落を差し替える（2026-09-03・鶏の周）

**取り込む人へ: 上の「`items-survival.md` — 末尾の…」の節（パンの周）と同じ 1 段落を
指していて、食い違います。新しいこちらを採ってください**（あちらは鶏を知らないので、
モブのドロップの並びが古いままです）。**古いほうは直していません**（決まりのとおり）。

> モブのドロップは羊毛（ブロック 37）・生豚肉（アイテム 81）・腐った肉（アイテム 82）・
> **生鶏肉（アイテム 126）**。かまどで焼くと焼き豚（アイテム 83）と**焼き鳥（アイテム 127）**に
> なります。**食べられるものの表は `items.ts` の `FOODS` 1 本**で、**肉だけではありません** ——
> **パン（125）は小麦 3 個の横一列**（`crafting.ts`。3 幅なので作業台が要ります）で作る、
> **畑から取れる食べ物**です。**小麦（124）そのものは食べられません**（本家と同じで、
> パンにしてから食べます。`test/blocks.test.ts` が `foodOf(WHEAT) === null` を見張っています）。
> 何がどれだけ戻るかは `rules/vitals.md` を見てください。
>
> **矢の羽根はまだ代用のままです。** 鶏は居ますが、**`MobDef.drop` は 1 つしか落とせない**ので
> 羽根が出ません（2 つ目の落とし物を足す話が先。`AUTODEV-QUEUE.md` の 7b）。
> **弓の糸（羊毛 3）と矢の羽根（火打石 + 棒）の代用は、どちらも残っています。**

## `items-survival.md` — 末尾の「モブのドロップは…」の段落を差し替える（2026-09-03・羽根と矢の周）

**取り込む人へ: 上の 2 つの「`items-survival.md` — 末尾の…」の節（パンの周・鶏の周）と
同じ 1 段落を指していて、どちらとも食い違います。いちばん新しいこちらを採ってください**
（あちらは羽根を知らないので、矢の材料と代用の話が古いままです）。
**古いほうは 2 つとも直していません**（決まりのとおり）。

> モブのドロップは羊毛（ブロック 37）・生豚肉（アイテム 81）・腐った肉（アイテム 82）・
> **生鶏肉（アイテム 126）と羽根（アイテム 128）**。かまどで焼くと焼き豚（アイテム 83）と
> **焼き鳥（アイテム 127）**になります。**食べられるものの表は `items.ts` の `FOODS` 1 本**で、
> **肉だけではありません** —— **パン（125）は小麦 3 個の横一列**（`crafting.ts`。3 幅なので
> 作業台が要ります）で作る、**畑から取れる食べ物**です。**小麦（124）そのものは
> 食べられません**（本家と同じで、パンにしてから食べます。`test/blocks.test.ts` が
> `foodOf(WHEAT) === null` を見張っています）。**羽根（128）も食べ物ではありません**
> （矢の材料だけ。`foodOf(FEATHER) === null` を同じテストが見ています）。
> 何がどれだけ戻るかは `rules/vitals.md` を見てください。
>
> **矢は本家の形に戻しました**（2026-09-03）。**火打石 + 棒 + 羽根の縦 1 列で 4 本**で、
> **3 段なので作業台が要ります** —— いままでの「弓を持って出たまま矢を作り足せる」は
> 失われました（`TUNING.md`）。**残っている代用は弓の糸（羊毛 3）だけ**です
> （蜘蛛がまだ居ないため。羊毛はベッドとも材料を取り合うので、羊を探す道のりが
> 弓に二重に効いています）。**縦 1 列 3 段はシャベル・剣と形が並びます**が材料が違うので、
> `test/crafting.test.ts` の「同じ形のレシピが重複していない」は緑のままです。

## `mobs.md` — 「刈って取る（`MobDef.shearing`）」の節の**すぐ上**に足す（新しい節。2026-09-03・羽根と矢の周）

**取り込む人へ: これは新しい節で、既存のどの本文とも食い違いません。**

> ### 2 山目を落とす（`MobDrop.extra`）
>
> **鶏が最初の 1 体です**（生鶏肉 1 個 + 羽根 1 個）。`items.ts` の `Drop.extra`
> （ブロックの 2 山目）と**同じ名前・同じ意味**にしてあり、**1 山目の当たり外れとは
> 無関係に別に引きます** —— 別の山だからです。
>
> - **ブロック側と 1 点だけ違って、`MobDrop.extra` は `chance` を持てます。**
>   `Drop.extra` が確率を持てないのは、壊す側に流れてくる乱数が `BreakOrder.roll` の
>   **1 本だけ**で、付けると 1 山目の当たり外れと必ず相関するからです。**モブの側は
>   `random()` という関数が流れてくる**ので、2 山目を別に引けます。
>   **`rules/items-survival.md` の「`extra` に `chance` を持たせないこと」は
>   ブロックの話で、モブには掛かりません。**
> - **`extra` に個数の範囲（min / max）を持たせないこと。** 本家の羽根 0〜2 は
>   **1 個固定**にしてあります。範囲が要るなら `ShearRule` のような別の表の話です。
> - **落ちる山を全部返すのは `dropsFor(mob, def, random)` の 1 本**（0〜2 山）。
>   **`dropFor()` は残してあります** —— 既存テストの根拠で、`rollDrop()` に対する
>   `rollDrops()` とまったく同じ扱いです。**`dropsFor()` の 1 山目も `dropFor()` に
>   作らせること**（刈った羊の抑えを 2 か所に写さないため）。
> - **`chance >= 1 || random() < chance` の短絡をそのまま使うこと。** `chance` が 1 の山では
>   **乱数を引きません。** 引く形に変えると、**種を固定した既存テストの目がずれて、
>   その山と関係ない所が赤くなります**（`test/mobs.test.ts` が「引いた回数」を数えています）。
> - **呼ぶ側（`attack()` / `hitByProjectile()`）に `chance` の比較を残さないこと。**
>   2 か所に散ると、**弓で撃ったときだけ 2 山目が出ない**形が戻ります
>   （刈った羊の羊毛とまったく同じ話）。テストは殴った側と撃った側を**並べて**測ること。
> - **`MobDef.drop` の表を書き換えないこと。** 2 山目を足すのは `extra` の 1 行だけで、
>   既存 7 つの `drop:` は 1 行も動きません。
> - **`main.ts` / `drops.ts` / `breaking.ts` に「2 山を並べて置く」を書かないこと。**
>   `onDrop` は**山ごとに 1 回鳴るだけ**で、地面での散らばりは `drops.ts` の `burst()` の
>   仕事です（ブロックの 2 山とまったく同じ）。

## 取り込むときにした判断（同じことが起きたら同じようにすること）

**2026-09-02（剣の周）**: 3c の周と 3d の周が **`items-survival.md` の同じ 1 行**
（「道具の耐久値はまだありません。」）を指していました。**新しいほう（3d）の本文を採り**、
3c にしかない「どこまで付いて回るか」をその頭に置いています。3c の文にあった
「火打石と打ち金・弓は無限に使えます」は **3d で嘘になっていたので落としました。**
同じ周で書いた `durability.ts` の節の「傷はいまインベントリの 36 枠にしか乗りません」も
同じ理由で書き直しています —— **受け渡しが 1 周でも空くと、本文どうしが食い違います。**

**2026-09-03（14 件をまとめて）**: 食い違う組が 3 つありました。**どれも新しいほうを採り、
古いほうは当てずに捨てました**（書いた周の指示どおり）。

1. **「傷が付く物は N 種類」**: AUTODEV 5 が 3 種類のままシアーズの行を書き、AUTODEV 6 が
   4 種類（クワ）にしました。**表は 4 行**にして、AUTODEV 5 のシアーズ行も
   AUTODEV 6 のクワ行も両方入れてあります（食い違っていたのは**数だけ**で、行は別物です）。
   ついでに同じ節の「**この 3 つを混ぜないこと**」も「この 4 つ」に直しました
   （表を指している文なので、直さないと数だけが古く残ります）。
2. **`use.md` の「N 通り」**: 11 →（5 で 12）→（6 で 13）→（12 前半で 14）。
   **14 通り**を採り、行き先の列挙にも刈る・耕す・植えるを足しました。
   **並び順の「静かに壊れるもの」は 3 つ → 4 つ**（AUTODEV 5 の本文は「3 つ」と書いて
   いましたが、「いまの 1〜3 は 2〜4 に繰り下がります」と指示していたので後者を採りました）。
3. **`items-survival.md` の「1 山しか落ちない」**（AUTODEV 13 の B）: **当てていません。**
   AUTODEV 14 がその制限そのものを外し、`rollDrops()` の節で置き換えたためです。
   ただし AUTODEV 14 の本文には `Drop.otherwise` と砂利の話（**まだ本当**）が
   入っていなかったので、**古い本文のその 3 行は残して**新しい 4 つの箇条書きを前に置きました。

**書かれた本文をそのまま当てなかったのは 2 か所だけです**（どちらも数の書き間違い。
コードで数え直しました）:

- `blocks-shapes.md` の「右クリックで世界を書き換える入口は `placing.ts` に **4 本**」→
  **5 本**（`tryPlace` / `tryIgnite` / `tryBucket` / `tryTill` / `tryPlant`。
  本文自身が 5 つ並べていました）
- `use.md` の「`test/ui.test.ts` の `routed` が `decideUse(` と **`useOrPlace();`**」→
  **`useOrPlace(mobIsNearer(`**（AUTODEV 5 が同じ周に書き換えていたので、
  前からあった行のほうが古くなっていました）
