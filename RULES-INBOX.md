# 決まりごとの受け渡し（層 2・層 3 の更新待ち）

**無人の周はここに書きます。取り込むのは人が居る周です。**

`.claude/rules/**` と `.claude/skills/**` を編集しようとすると「sensitive file」の
確認が出て、**答える人が居ない周はそこで止まります**（実装もテストも全部終えたあとで、です）。
`.claude/settings.json` に許可を書いても消えませんでした（2026-08-25 実測。
リポジトリの中の設定が、自分と同じ `.claude/` の中身を編集する許可を自分に出すことは
できない、という形の防御だと思われます）。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 書き方

1 件 1 節。見出しは `## <どのファイル> — <一言>`。

- **どのファイルのどの節へ足すのか**を先に書く（節が無ければ「新しい節」と書く）
- **置きたい本文をそのまま置くこと。** 「モブの飛び方について書く」のような要約は不可 ——
  取り込む側が文章を書き直すことになり、決まりごとの言い回しが周ごとにばらけます
- `paths` を足したいときは、その行もそのまま書く

## 取り込む側（人が居る周）

1. 上から順に、書かれたとおりに `.claude/rules/` へ当てる
2. **当てた節は丸ごと消す**（残すと、どれが未取り込みか分からなくなります）
3. 取り込みだけで 1 コミットにしてよい

**新しいファイルを増やした周は、`paths` の行も書くこと。** 2-4c 〜 2-9 の 12 件には
`src/stronghold.ts` と `src/endportal.ts` の `paths` が付いていなかったので、
取り込む側が足しました（無いと、そのファイルを触っても書いた決まりごとが読まれません）。

---

（2-4c 〜 2-9 の 12 件 / 2-10 / 2-11a の 4 件 / 2-11b〜2-12b の 8 件 /
2-13a の 1 件 / 2-13b-1 の 3 件は 2026-08-27 に、2-13b-2 の 2 件（ボスの体力バー・クリア画面）は
2026-08-28 に、**ドラゴンの `phases` と耐久値の 8 件（`mobs.md` / `projectiles.md` /
`items-survival.md` x 3 / `inventory-screen.md` / `drops.md` / `stateful-blocks.md`）は
2026-09-02 に取り込み済み**）

**いま未取り込みは 2 件です**（下の「剣が入った周（2026-09-02・AUTODEV の C の周）」）。

**取り込むときに 1 か所だけ判断しました**（同じことが起きたら同じようにすること）:
3c の周と 3d の周が **`items-survival.md` の同じ 1 行**（「道具の耐久値はまだありません。」）を
指していました。**新しいほう（3d）の本文を採り**、3c にしかない「どこまで付いて回るか」を
その頭に置いています。3c の文にあった「火打石と打ち金・弓は無限に使えます」は
**3d で嘘になっていたので落としました。** 同じ周で書いた `durability.ts` の節の
「傷はいまインベントリの 36 枠にしか乗りません」も同じ理由で書き直しています ——
**受け渡しが 1 周でも空くと、本文どうしが食い違います。**

---

# 剣が入った周（2026-09-02・AUTODEV の C の周）

**この 2 件は、いまの `.claude/rules/items-survival.md` の本文と食い違います。**
古いほうは直さずに、どこと食い違うかを並べておきます（取り込む人が新しいほうを採ってください）。

## `items-survival.md` の「### 道具の耐久値（`durability.ts`）」の直前の表 — 減り方が 3 通りになった

**食い違う場所**: いまの本文は「**傷が付く物は 2 種類あって、減り方が違います**」で始まり、
表の行が「掘って減る」「使って減る」の 2 つです。**剣が入って 3 つになりました。**
下の本文で 2 行を差し替えてください（表の行と、そのあとの箇条書きの 1 つ目）。

**傷が付く物は 3 種類あって、減り方が違います**（判断は全部 `durability.ts`）。

| 減り方 | 何が | 何回 | どこで減るか |
| --- | --- | --- | --- |
| **掘って減る** | ツルハシ・斧・シャベル 12 本 | 木 59 / 石 131 / 鉄 250 / ダイヤ 1561 | `wearForBreaking()`（`breaking.ts` の `tryBreak()` が返す `wear`） |
| **使って減る** | 火打石と打ち金・弓 | **64 / 384**（本家のまま） | `wearForUse()`（`main.ts` の `igniteAt()` と `loose()`） |
| **殴って減る** | 剣 4 本 | 掘る道具と**同じ階層の表**（59 / 131 / 250 / 1561） | `wearForAttack()`（`main.ts` の `mousedown` の `attack`） |

- **この 3 つを混ぜないこと。** 火種と弓に `tool:` を付けると `ToolDef` は
  「掘る速さ」の表なので**弓で石が速く掘れます**し、`wearForBreaking()` から
  「掘る道具でなければ 0」を外すと**弓で石を掘って弓が減ります。**
  逆に `wearForUse()` が掘る道具に 1 を返すと、**ツルハシを右クリックしただけで減ります。**
  **`wearForAttack()` を `wearForUse()` に混ぜるのも同じ罠で、剣を右クリックしただけで減ります。**
- **剣は掘っても減ります**（`wearForBreaking()` が 1 を返す。本家と同じ）。
  `toolOf()` が非 null なので**1 行も足さずに付いてきます** —— 外さないこと。
- **ツルハシ・斧・シャベルは殴っても減りません。** 本家は減りますが、
  ここでそう変えると**既存 12 本の寿命が黙って縮みます**（掘るための回数のまま戦闘ぶんが乗る）。
  変えるならユーザーと決めてください。

## `items-survival.md` の同じ節 — 剣（新しい箇条書き。上の表のすぐ下へ）

- **剣 4 本（木 111 / 石 112 / 鉄 113 / ダイヤ 114）は「殴るための道具」です。**
  `ToolKind` に `"sword"` を足しただけで、**耐久値もセーブの 5 キーも 1 行も書かずに
  付いてきます**（`maxUses()` が `toolOf()` に聞くだけなので）。
  **`TOOL_USES` に剣用の 5 つ目の表を作らないこと。**
- **どのブロックにも `tool: "sword"` を付けないこと**（`test/blocks.test.ts` が見張っています）。
  付けた瞬間、剣がそのブロックの採掘道具になって速く掘れます。**掘る速さは
  `speed: 1`**（素手と同じ）で、`TIER_SPEEDS` を渡さないこと。
- **どれが剣かは `items.ts` の `isSword()`**（`toolOf(id)?.kind === "sword"`）。
  火種・弓と違って表を持たないのは、剣が 4 本とも `ToolDef` を持っているからです。
  **`durability.ts` に `item === WOOD_SWORD` と書かないこと**（`test/durability.test.ts` が見張り）。
- **攻撃力は `mobs.ts` の `TOOL_ATTACK` に 1 行**（`sword: 4`）。剣 4.5 / 5 / 5.5 / 6 で、
  **ダイヤの剣（6）がダイヤの斧（5）より強い**。本家は 4/5/6/7（階層ごとに +1）ですが、
  ここは `TIER_ATTACK` が 0.5 なので伸びが半分です（`TUNING.md`）。
- **減らすのは「殴れたとき」だけ。** `mobs.attack()` は**クールダウン中なら false** を返すので、
  `main.ts` はその戻り値の中でだけ `wearHeld(wearForAttack(...))` を呼びます
  （**戻り値を捨てて呼ぶと、連打しただけで剣が減ります**）。
  `test/durability.test.ts` が `main.ts` の `wearForAttack(` を**1 回**と数えています。
