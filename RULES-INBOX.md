# スキル（層 3）の受け渡し

**無人の周はここに書きます。取り込むのは人が居る周です。**

`.claude/**` を `Edit` / `Write` しようとすると「sensitive file」の確認が出て、
**答える人が居ない周はそこで止まります**（実装もテストも全部終えたあとで、です）。
`.claude/settings.json` に `Edit(.claude/skills/**)` と書いても消えません（2026-08-25 実測）。

**層 2（`.claude/rules/**`）はここを通らなくなりました**（2026-09-03）。
`npm run rules:install -- <新しい全文> .claude/rules/<名前>.md` で周が自分で据えます。
**止めているものが 2 つあると分かったから**です: `Edit` / `Write` の「sensitive file」と、
**Bash の書き込みに掛かる auto mode の分類器**。後者は許可行 1 つで通り
（`Bash(node scripts/install-rule.mjs:*)`）、**前者は許可行では通りません。**
手順は `LOOP.md` の 4.5。**ここに残っているのは `.claude/skills/**` の更新待ちだけ**です
（更新が稀なので、人が居る周が取り込みます）。下の「取り込むときにした判断」は、
**受け渡しを 1 周空けると何が起きるか**の記録として残します。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 書き方

1 件 1 節。見出しは `## <どのファイル> — <一言>`。

- **どのファイルのどの節へ足すのか**を先に書く（節が無ければ「新しい節」と書く）
- **置きたい本文をそのまま置くこと。** 「モブの飛び方について書く」のような要約は不可 ——
  取り込む側が文章を書き直すことになり、決まりごとの言い回しが周ごとにばらけます
- `paths` を足したいときは、その行もそのまま書く

## 取り込む側（人が居る周）

1. 上から順に、書かれたとおりに `.claude/skills/` へ当てる
2. **当てた節は丸ごと消す**（残すと、どれが未取り込みか分からなくなります）
3. 取り込みだけで 1 コミットにしてよい

**新しいファイルを増やした周は、`paths` の行も書くこと。** 2-4c 〜 2-9 の 12 件には
`src/stronghold.ts` と `src/endportal.ts` の `paths` が付いていなかったので、
取り込む側が足しました（無いと、そのファイルを触っても書いた決まりごとが読まれません）。

---

（2-4c 〜 2-9 の 12 件 / 2-10 / 2-11a の 4 件 / 2-11b〜2-12b の 8 件 /
2-13a の 1 件 / 2-13b-1 の 3 件は 2026-08-27 に、2-13b-2 の 2 件（ボスの体力バー・クリア画面）は
2026-08-28 に、ドラゴンの `phases` と耐久値の 8 件と剣 4 本の 2 件は 2026-09-02 に、
AUTODEV 5 / 6 / 12 / 13 / 14 の 14 件（シアーズ・クワ・種・苗・2 山）と、
**AUTODEV 15 / 16 / 17 の 8 件（パン・鶏・羽根と矢）は 2026-09-03 に取り込み済み**）

**いま未取り込みは 3 件です**（下の AUTODEV 19 の 3 節。**層 2 = `.claude/rules/**` 宛て**）。

### 層 2 がここに戻っている理由（2026-09-04・**上の 9〜16 行目と食い違います**）

**古いほうを直さずに並べます**（取り込む人が新しいほうを採ってください）。

- **上の 9〜16 行目**は「層 2 はここを通らなくなった。`npm run rules:install` で周が
  自分で据える」と書いています（2026-09-03 に書かれたもの）。
- **AUTODEV 19 を回した定期実行のプロンプト**（2026-09-04）は、**`.claude/rules/**` と
  `.claude/skills/**` に 1 行も書くなと明示**していて、**`Bash` から書くのも「判定を
  素通りする回避」として禁じ手**に並べ、**代わりに `RULES-INBOX.md` へ本文をそのまま
  書け**と指示しています。**`AUTODEV.md` の C-4（`npm run rules:install` で据える）とも
  食い違います。**
- **この周はプロンプトのほうに従いました**（無人の周が禁じ手に触れるより、
  取り込み待ちで 1 周空けるほうが安いため）。**どちらを正にするかは人が決めてください** ——
  決まったら、負けたほうの本文（この節か、上の 9〜16 行目か、`AUTODEV.md` の C-4）を
  消してください。

## `.claude/rules/projectiles.md` — 表が 5 種類になった（卵）

**「## 飛んでいるもの（火球・矢・エンダーアイ・ブレス）」の見出しと、その下の
「種類を足すのは…」の箇条書きを、次の本文へ差し替えてください。**

```
## 飛んでいるもの（火球・矢・エンダーアイ・ブレス・卵）
```

```
- **種類を足すのは `PROJECTILE_KINDS` に 1 行**で、飛び方の実装は増やさないこと。
  5 種類（火球・矢・エンダーアイ・ブレス・**卵**）の違いは全部この表の値です。
- **プレイヤーが投げるものは、表とは別に `items.ts` の `THROWN` を通ります。**
  「どのアイテムを投げると何が飛ぶか」は `items.ts` の表 1 本（`thrownProjectile()`）で、
  **どう飛ぶか**はここの表 1 行 —— この 2 つを混ぜないこと。`use.ts` にも `main.ts` にも
  **`held === EGG` と書かないこと**（`isBucket` / `isBow` とまったく同じ作法で、
  書くと投げるものが増えるたびに `decideUse()` に分岐が 1 本ずつ生えます）。
  **`items.ts` と `use.ts` が `projectiles.ts` から取ってよいのは
  `import type { ProjectileKind }` だけです** —— 値で入れると輪になりますし、
  `test/use.test.ts` が `use.ts` に `Projectiles` が出てこないことを見張っています。
- **卵は当たっても何も起きません**（`Shot.damage` を渡さない = 既定の 0。
  `mobs.ts` の `hitByProjectile()` が `shot.damage <= 0` で戻ります）。
  **表にダメージを書かないこと**は今までどおりで、**渡さないことがそのまま
  「当たっても減らない」になります。**
- **本家の「1/8 でヒヨコが孵る」は入っていません**（子モブの仕組みが 1 つも無いため）。
  入れるなら `mobs.ts` の話で、`projectiles.ts` の表は 1 行も動きません。
```

**同じファイルの `main.ts` の `N` キーの行（「**`N` キー**が 4 種類を順ぐりに 1 つ撃ちます」）
も、次に差し替えてください。**

```
- `main.ts` の **`N` キー**が 5 種類を順ぐりに 1 つ撃ちます（`M` のモブ出しと同じ、
  切り分けのためのデバッグ用）。**卵は右クリックで投げられるので本物の手段がありますが、
  `N` は表の全部を出せる唯一の道**です（火球・ブレスを出す手段はほかにありません）。
```

## `.claude/rules/use.md` — 15 通りになった（投げる）

**「## 右クリックで何が起きるか（`use.ts`）」の最初の段落と、並び順の箇条書きの
本文を、次へ差し替えてください。**

```
**マウスの右ボタンには 15 通りの行き先があります**（刈る・作業台・かまど・チェスト・ベッド・
耕す・植える・バケツ・枠にアイを嵌める・アイを投げる・**投げる**・火を点ける・弓を引く・
食べる・置く）。その振り分けは `use.ts` の **`decideUse()` 1 か所**で、three も DOM も
`World` も持ち物も出てきません（見張りは `test/use.test.ts`）。
```

**並び順の 4 つの箇条書きのあと（「**クワ（`till`）は…**」の段落の前）に、次を足してください。**

```
  **投げる（`throw`）はアイを投げる（`throwEye`）の次、火種（`ignite`）より前。**
  **器より前に出さないこと** —— 出すと、作業台の上に立って卵を持っているあいだ
  **作業台が開きません**（並び順の 2 番目とまったく同じ壊れ方）。
  **`aim` は見ないこと** —— 空へ投げられるのが正しく、食べる・弓・バケツと同じ扱いです。
  **地面を狙っていても `throw` に来ます**（`place` に落とすと、卵が地面に「置ける」
  ことになります）。**何を投げると何が飛ぶかはここで決めないこと** ——
  `items.ts` の `thrownProjectile()` の表 1 本で、`use.ts` はその結果を運ぶだけです。
```

**同じファイルの「`main.ts` に `if` の列を書き戻さないこと」の箇条書きに、次を足してください。**

```
  - `test/use.test.ts` は `main.ts` に **`EGG`** と **`thrownProjectile(`** が
    戻っていないかも見ています（`isShears(` / `isHoe(` / `isSeed(` と同じ理由）。
    **`main.ts` が受け取ってよいのは「何が飛ぶか」（`ProjectileKind`）だけ**で、
    どのアイテムが投げられるかは `items.ts` の表 1 本です。
```

## `.claude/rules/testing.md` — 山なりに飛ぶものの的の高さ

**「テストの書き方」の箇条書き（`**先に「ちゃんと動いた」ことを…**` で始まる並び）に、
次の 1 件を足してください。**

```
- **山なりに飛ぶものの的は、落ちるぶんだけ下げること。** 矢の的（足元 19.2）を卵に
  写したら、**当たり 0 回**で「相手に当たらない飛び道具」が緑で通りかけました。
  卵は 20m/s・`gravityScale: 1` なので **6m 先までに 1.35m 落ちます**（矢は 40m/s で
  8.5m 先まで 0.76m）。**速さの違う弾は弾道が別物**なので、的の座標を写さないこと ——
  **落ちる量を見当してから的を置き、その理由をコメントに残すこと。**
```

## 取り込むときにした判断（同じことが起きたら同じようにすること）

**2026-09-02（剣の周）**: 3c の周と 3d の周が **`items-survival.md` の同じ 1 行**
（「道具の耐久値はまだありません。」）を指していました。**新しいほう（3d）の本文を採り**、
3c にしかない「どこまで付いて回るか」をその頭に置いています。3c の文にあった
「火打石と打ち金・弓は無限に使えます」は **3d で嘘になっていたので落としました。**
同じ周で書いた `durability.ts` の節の「傷はいまインベントリの 36 枠にしか乗りません」も
同じ理由で書き直しています —— **受け渡しが 1 周でも空くと、本文どうしが食い違います。**

**2026-09-03（14 件をまとめて）**: 食い違う組が 3 つありました。**どれも新しいほうを採り、
古いほうは当てずに捨てました**（書いた周の指示どおり）。

1. **「傷が付く物は N 種類」**: AUTODEV 5 が 3 種類のままシアーズの行を書き、AUTODEV 6 が
   4 種類（クワ）にしました。**表は 4 行**にして、AUTODEV 5 のシアーズ行も
   AUTODEV 6 のクワ行も両方入れてあります（食い違っていたのは**数だけ**で、行は別物です）。
   ついでに同じ節の「**この 3 つを混ぜないこと**」も「この 4 つ」に直しました
   （表を指している文なので、直さないと数だけが古く残ります）。
2. **`use.md` の「N 通り」**: 11 →（5 で 12）→（6 で 13）→（12 前半で 14）。
   **14 通り**を採り、行き先の列挙にも刈る・耕す・植えるを足しました。
   **並び順の「静かに壊れるもの」は 3 つ → 4 つ**（AUTODEV 5 の本文は「3 つ」と書いて
   いましたが、「いまの 1〜3 は 2〜4 に繰り下がります」と指示していたので後者を採りました）。
3. **`items-survival.md` の「1 山しか落ちない」**（AUTODEV 13 の B）: **当てていません。**
   AUTODEV 14 がその制限そのものを外し、`rollDrops()` の節で置き換えたためです。
   ただし AUTODEV 14 の本文には `Drop.otherwise` と砂利の話（**まだ本当**）が
   入っていなかったので、**古い本文のその 3 行は残して**新しい 4 つの箇条書きを前に置きました。

**書かれた本文をそのまま当てなかったのは 2 か所だけです**（どちらも数の書き間違い。
コードで数え直しました）:

- `blocks-shapes.md` の「右クリックで世界を書き換える入口は `placing.ts` に **4 本**」→
  **5 本**（`tryPlace` / `tryIgnite` / `tryBucket` / `tryTill` / `tryPlant`。
  本文自身が 5 つ並べていました）
- `use.md` の「`test/ui.test.ts` の `routed` が `decideUse(` と **`useOrPlace();`**」→
  **`useOrPlace(mobIsNearer(`**（AUTODEV 5 が同じ周に書き換えていたので、
  前からあった行のほうが古くなっていました）

**2026-09-03（AUTODEV 15 / 16 / 17 の 8 件をまとめて）**: 食い違う組が 2 つあり、
**どちらも書いた周の指示どおり、いちばん新しいほうだけを当てました。**

1. **`items-survival.md` の「モブのドロップは…」の 1 段落**: パンの周・鶏の周・
   羽根と矢の周の 3 つが同じ 1 段落を指していました。**羽根と矢の周（いちばん新しい）
   だけを当て**、前の 2 つは捨てています。
2. **`vitals.md` の「食べ物どうしの強さ」**: パンの周が 4 つ（生豚肉・腐った肉・パン・
   焼き豚）、鶏の周が 6 つ（生鶏肉と焼き鳥を足した並び）。**6 つのほうを採りました。**
   パンの周の**1 つ目の箇条書き（「食べ物を足すのに要るのは `items.ts` の 2 行だけ」）は
   食い違っていない**ので、そのまま当ててあります。

**書かれていないのに消したのが 1 か所あります**（今回だけ、指示より広く当てました）:

- `items-survival.md` の**「弓と矢は本家の材料を 2 つとも代用しています」の箇条書きを
  丸ごと消しました。** 受け渡しは「モブのドロップは…」の段落の差し替えしか書いて
  いませんでしたが、**その差し替え文が矢の代用の話を引き取っている**うえに、
  古い箇条書きの本文（「矢は羽根の代わりに**火打石 + 棒だけで 4 本**（鶏が居ない）」
  「矢の形は松明と 1 マスしか違いません」）は**AUTODEV 17 で嘘になっていました。**
  コードで裏を取っています（`crafting.ts` の矢は `shape: ["F", "S", "E"]` の
  **縦 1 列 3 段・`count: 4`・`E` は `FEATHER`**）。残すと、同じファイルの中で
  代用の話が 2 か所に分かれて食い違います —— **剣の周に「火打石と打ち金・弓は
  無限に使えます」を落としたのとまったく同じ判断です。**
