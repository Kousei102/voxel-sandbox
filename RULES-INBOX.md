# 決まりごとの受け渡し（層 2・層 3 の更新待ち）

**無人の周はここに書きます。取り込むのは人が居る周です。**

`.claude/rules/**` と `.claude/skills/**` を編集しようとすると「sensitive file」の
確認が出て、**答える人が居ない周はそこで止まります**（実装もテストも全部終えたあとで、です）。
`.claude/settings.json` に許可を書いても消えませんでした（2026-08-25 実測。
リポジトリの中の設定が、自分と同じ `.claude/` の中身を編集する許可を自分に出すことは
できない、という形の防御だと思われます）。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 書き方

1 件 1 節。見出しは `## <どのファイル> — <一言>`。

- **どのファイルのどの節へ足すのか**を先に書く（節が無ければ「新しい節」と書く）
- **置きたい本文をそのまま置くこと。** 「モブの飛び方について書く」のような要約は不可 ——
  取り込む側が文章を書き直すことになり、決まりごとの言い回しが周ごとにばらけます
- `paths` を足したいときは、その行もそのまま書く

## 取り込む側（人が居る周）

1. 上から順に、書かれたとおりに `.claude/rules/` へ当てる
2. **当てた節は丸ごと消す**（残すと、どれが未取り込みか分からなくなります）
3. 取り込みだけで 1 コミットにしてよい

**新しいファイルを増やした周は、`paths` の行も書くこと。** 2-4c 〜 2-9 の 12 件には
`src/stronghold.ts` と `src/endportal.ts` の `paths` が付いていなかったので、
取り込む側が足しました（無いと、そのファイルを触っても書いた決まりごとが読まれません）。

---

（2-4c 〜 2-9 の 12 件 / 2-10 / 2-11a の 4 件 / 2-11b〜2-12b の 8 件 /
2-13a の 1 件 / 2-13b-1 の 3 件は 2026-08-27 に、2-13b-2 の 2 件（ボスの体力バー・クリア画面）は
2026-08-28 に、**ドラゴンの `phases` と耐久値の 8 件（`mobs.md` / `projectiles.md` /
`items-survival.md` x 3 / `inventory-screen.md` / `drops.md` / `stateful-blocks.md`）と、
剣 4 本の 2 件（`items-survival.md`）は 2026-09-02 に取り込み済み**）

**いま未取り込みは 0 件です。**

**取り込むときに 1 か所だけ判断しました**（同じことが起きたら同じようにすること）:
3c の周と 3d の周が **`items-survival.md` の同じ 1 行**（「道具の耐久値はまだありません。」）を
指していました。**新しいほう（3d）の本文を採り**、3c にしかない「どこまで付いて回るか」を
その頭に置いています。3c の文にあった「火打石と打ち金・弓は無限に使えます」は
**3d で嘘になっていたので落としました。** 同じ周で書いた `durability.ts` の節の
「傷はいまインベントリの 36 枠にしか乗りません」も同じ理由で書き直しています ——
**受け渡しが 1 周でも空くと、本文どうしが食い違います。**

---

## `items-survival.md` — 「傷が付く物は 3 種類あって」の表の「使って減る」の行（2026-09-02・AUTODEV 5）

**いまの本文**（`durability.ts` の 3d の周が書いたもの）:

> | **使って減る** | 火打石と打ち金・弓 | **64 / 384**（本家のまま） | `wearForUse()`（`main.ts` の `igniteAt()` と `loose()`） |

**これに差し替えてください**（シアーズが 3 つ目になりました）:

> | **使って減る** | 火打石と打ち金・弓・**シアーズ** | **64 / 384 / 238**（本家のまま） | `wearForUse()`（`main.ts` の `igniteAt()` と `loose()` と `shearMob()`） |

同じ節の次の 2 行も、名前を足してください:

> - **どれが火種・弓・シアーズかは `items.ts` の `isFireStarter()` / `isBow()` / `isShears()` の表 1 本**です。
>   `durability.ts` に `item === BOW` と書き始めると、種類が増えたときに
>   「持てる側」と「減る側」の 2 か所を直すことになり、必ず片方を忘れます
>   （`test/durability.test.ts` が `durability.ts` にアイテムの名前が出てこないことを見張っています）。
> - **`main.ts` に回数（64 / 384 / 238）と「弓かどうか」を書かないこと。**
>   `test/durability.test.ts` が `main.ts` に `384` が無いことと、`wearForUse(` が
>   **点火・発射・刈るの 3 か所から**呼ばれていることの両方を見ています（減っていたら配線が落ちています）。

**シアーズに `tool:` を持たせないこと。** 剣は `ToolKind` に 1 語足すのがいちばん安かった
（耐久値もセーブの 5 キーも 1 行も書かずに付いてくる）のですが、**シアーズで同じことを
すると壊れます**: `mobs.ts` の `TOOL_ATTACK` に無い種類が入ると `attackDamage()` が
`undefined + 0.5 * tier` = **NaN** を返し、`wearForBreaking()` は「掘る道具」として 1 を
返すので**石を掘るたびに減ります。** **新しい道具を足すときは、`ToolKind` に足すかどうかより
先に「減り方の 3 種類のどれか」を決めること。**

## `mobs.md` — 新しい節「刈って取る（`MobDef.shearing`）」（2026-09-02・AUTODEV 5）

**「ボス（`MobDef.boss`）と、回る飛び方（`MobDef.orbit`）」の節の後ろに置いてください。**

> ### 刈って取る（`MobDef.shearing`）
>
> **羊が最初の 1 体です**（シアーズで右クリックすると羊毛が 1〜3 個出て、60 秒で戻る）。
> 飛ぶ・跳ぶ・回ると同じで、**違いは全部表（`ShearRule`）の値**にしてあり、
> `update()` にも `step()` にも「羊なら」は 1 行もありません。
>
> - **`kind === "sheep"` と書かないこと**（`ranged` / `teleport` / `orbit` と同じ作法）。
>   刈れるモブが増えるたびに、刈る側と戻す側の 2 か所に分岐が生えます。
> - **何で刈るか（シアーズか）は `mobs.ts` は見ません** —— それは `items.ts` の
>   `isShears()` の仕事で、ここが持つのは「誰から・何が・何個・いつ戻るか」だけです。
>   **`use.ts` の `decideUse()` が 2 つを込みにします**（`UseFacts.shearable` =
>   「手前に居る」×「刈れる」。`hasArrow` とまったく同じ約束）。
> - **倒したときのドロップは `dropFor(mob, def)` の 1 本に通すこと。** `def.drop` を
>   直に読む経路は `attack()`（殴って倒す）と `hitByProjectile()`（撃って倒す）の
>   **2 つ**あり、片方だけ直すと**弓で撃ったときだけ刈った羊から羊毛が出ます**
>   （`rollDrop()` を `breaking.ts` の 1 か所に集めたのとまったく同じ話）。
>   テストは殴った側と撃った側を**並べて**測ること。
> - **`MobDef.drop` の表を書き換えないこと。** 倒したときに何が出るかと、刈ったあとで
>   出ないことは別の話です（抑えるのは `dropFor()`）。
> - **刈れなければ `shear()` は false を返します。** 呼ぶ側（`main.ts`）はその中でだけ
>   道具を減らすので、**戻り値を捨てて呼ぶと、刈れない羊を撫でるだけでシアーズが尽きます**
>   （`mobs.attack()` の戻り値と剣の関係とまったく同じ形）。
> - **`Mob.woolTimer` は `hurtTimer` の隣で毎フレーム減らすこと**（判断は 5Hz なので、
>   そこに置くと遠くて動かない個体では戻りません）。**保存しません** ——
>   モブそのものを保存しないので、セーブは 1 バイトも増えません。
> - **`regrow` を 0 にしないこと**（連打で羊毛が無限に出ます）。数値は `TUNING.md`。
> - **刈られた羊はまだ見た目が変わりません**（`AUTODEV-QUEUE.md` の 11 番）。
>   `mobrender.ts` は形を `MobKind` ごとに 1 つ作って使い回すので、2 つ目の形と
>   差し替えの仕掛けが要ります。**`test/mobs.test.ts` の見張りに `shearing` と
>   `woolTimer` を足してあります**（判断が描画側へ漏れたら赤くなります）。

## `use.md` — 「右クリックで何が起きるか（`use.ts`）」の冒頭と並び順（2026-09-02・AUTODEV 5）

**冒頭の「11 通り」は 12 通りになりました**（刈るが増えた）。並べ替えると静かに壊れるものも
**3 つ**になったので、いまの「3 つ」の並びの**先頭に**これを足してください:

> 1. **刈る（`shear`）がいちばん先。** あとにすると、作業台やチェストの前に立った羊だけ
>    刈れません（そこまで追い込まないと気付けない形で壊れます）

（いまの 1〜3 は 2〜4 に繰り下がります。）

**同じ節の「`main.ts` に `if` の列を書き戻さないこと」の箇条書きに足してください:**

> `test/use.test.ts` は `main.ts` に **`isShears(`** が戻っていないかも見ています。
> **`main.ts` が持ってよいのは「刈れるモブが手前に居る」という事実だけ**で、
> どれがシアーズかは `items.ts` の表 1 本です。

## `use.md` — 「右クリック以外（`controls.ts`）」の節（2026-09-02・AUTODEV 5）

**「モブがブロックより手前なら殴る」の箇条書きの後ろに足してください:**

> - **「手前に居るのはモブか」は `mobIsNearer()` の 1 本**で、左クリック（殴る／掘る）と
>   右クリック（刈る／置く）が**同じ関数を呼びます。** `main.ts` に距離の比較を
>   書き直すと、殴れる間合いと刈れる間合いが食い違い、**「殴れるのに刈れない羊」**が
>   ブラウザを開くまで分かりません。`test/ui.test.ts` の `routed` が
>   **`useOrPlace(mobIsNearer(`** という**呼び出しの形**で見張っています
>   （`useOrPlace();` のままだと、比較を書き直しても緑で通ります）。

## `items-survival.md` — 減り方は **4 種類**になりました（2026-09-02・AUTODEV 6）

**この節は、上の「`items-survival.md` — 「傷が付く物は 3 種類あって」の表の「使って減る」の行」
（AUTODEV 5 が書いたもの）と食い違います。** 古いほうは直していません（この周の決まり）ので、
**取り込む人は新しいほう＝こちらを採ってください。** 食い違うのは次の 2 か所です:

1. **「3 種類」という数**。あちらは「傷が付く物は 3 種類あって」の表を前提に書かれていますが、
   クワが入って **4 種類**になりました（掘って減る / 使って減る / 殴って減る / **耕して減る**）
2. **あちらの末尾「新しい道具を足すときは、`ToolKind` に足すかどうかより先に
   「減り方の 3 種類のどれか」を決めること」の「3 種類」** —— **4 種類**に読み替えてください

**「傷が付く物は 3 種類あって」の見出しと表に、4 行目としてこれを足してください**
（見出しの「3 種類」も「4 種類」へ）:

> | **耕して減る** | クワ 4 本 | **59 / 131 / 250 / 1561**（掘る道具と同じ `TOOL_USES`） | `wearForTill()`（`main.ts` の `tillAt()`） |

同じ節に、次の 3 行を足してください:

> - **どれがクワかは `items.ts` の `isHoe()` の表 1 本**です（`isSword()` と同じ形）。
> - **`wearForUse()` にクワを混ぜないこと。** あちらは「掘る道具でないもの」の表で、
>   クワは `toolOf()` を持つので 0 が返ります。**右クリックしても減らないのが正しい** ——
>   減るのは**耕せたときだけ**（`placing.ts` の `tryTill()` が `placed` を返したときだけ
>   `main.ts` が呼びます）。**逆に `wearForBreaking()` からは外さないこと** ——
>   **クワで掘れば減るのは剣と同じ**で、1 行も足さずに付いてきます。
> - **`main.ts` の `wearForTill(` は 1 か所だけ**（`test/durability.test.ts` が見張っています）。

**`ToolKind` に 1 語足すときの手順は、シアーズの節（上）の逆側の注意です。** 剣と同じく
クワは `ToolKind` に足して正解でしたが、**`mobs.ts` の `TOOL_ATTACK` に `hoe: 1` を足すのを
忘れると `attackDamage()` が `undefined + 0.5 * tier` = NaN を返し、クワで殴ったモブの体力が
NaN になって二度と死にません。** `TOOL_ATTACK` は `Record<string, number>` なので
**typecheck は通ります**（`TOOL_NAMES` は `Record<ToolKind, …>` なので教えてくれる、という
非対称がここにあります）。**この周は `test/mobs.test.ts` に「クワで殴っても NaN にならない」を
足して塞ぎました。次に `ToolKind` を増やす人も同じ 1 行を足してください。**

## `blocks-shapes.md` — `variantOf` は「向き違い」だけのものではありません（2026-09-02・AUTODEV 6）

**「置く判断」の節か、`variantOf` を説明している所へ足してください**（新しい節でも可）:

> **`variantOf` は「アイテムにしないブロック」を作る道具でもあります。** 点火中のかまど
> （`FURNACE_LIT`）と耕地（`FARMLAND` = 116）がこの形で、`variantOf` に大元を書くと
> (a) `items.ts` のブロック → アイテムの for が `variantOf !== AIR` を飛ばすので
> **アイテムが作られず**（クリエイティブの一覧にも持ち物にも出ず、持ち歩いて置けない）、
> (b) `dropOf()` の既定が `baseBlock()` なので**掘ると大元が落ちます**（耕地 → 土）。
> **「本家では手に入らないブロック」は、ドロップ表にも `items.ts` にも 1 行も書かずに
> `variantOf` だけで作れます。**
>
> **立方体を 111 以降に置いてよいのは `encodeFace` が 8 ビットになったから**です
> （`CLAUDE.md` の「立方体を 64 以降に置かないこと」は **1..110 の凍結した帯**の話。
> 耕地は **116 の立方体**で、面の統合キーは壊れていません —— `npm test` の
> 「立方体 36」がそれを数えています）。

## `use.md` — クワの居場所（器の次・バケツより前）（2026-09-02・AUTODEV 6）

**上の「`use.md` — 「右クリックで何が起きるか（`use.ts`）」の冒頭と並び順」の節と
数が食い違います**（あちらは「11 通り → 12 通り」。クワが入って **13 通り**になりました）。
**古いほうは直していません。取り込む人は、あちらの並び順の追記を当てたうえで、
冒頭の数だけ 13 通りに読み替えてください。**

**同じ節の並び順の説明に足してください:**

> **クワ（`till`）は器（作業台・かまど・チェスト・ベッド）の次、バケツより前。**
> 器より前に出すと、**作業台の上に立ってクワを持っているあいだ作業台が開きません。**
> **耕せるかどうか（土か草か・上が塞がっていないか）はここで決めないこと** ——
> `place` と同じで、可否は `placing.ts` の `tryTill()` です。`decideUse()` は
> 「クワを持って何かを狙っている」という事実だけで `till` の注文を出します。

**同じ節の「`main.ts` に `if` の列を書き戻さないこと」の箇条書きにも足してください:**

> `test/use.test.ts` は `main.ts` に **`isHoe(`** が戻っていないかも見ています
> （`isShears(` と同じ理由）。

## `blocks-shapes.md` — `variantOf` を**自分自身**に向ける形（2026-09-02・AUTODEV 12 前半）

**これは上の「`blocks-shapes.md` — `variantOf` は「向き違い」だけのものではありません」
（AUTODEV 6）の続きで、食い違ってはいません**（あちらが「大元に向ける」形、こちらが
「向ける相手が居ない」形）。**同じ節の続きとして足してください:**

> **大元にできる相手が居なければ、`variantOf` は自分自身に向けます。** 小麦の苗
> （`WHEAT_CROP` = 121）がこれで、**土でも耕地でもない**ので耕地（`variantOf: DIRT`）の
> ようには書けません。`variantOf: WHEAT_CROP` にすると `items.ts` の for は今までどおり
> 飛ばす（**アイテムが作られない**）一方で、**`baseBlock()` が自分自身を返す**ので
> `dropOf()` の既定が**アイテムの無い番号**になります。
>
> **だから「自分自身に向けたブロック」は、落ちるものを `items.ts` の `DROPS` に
> 必ず 1 行書くこと。** 書き忘れると、掘った瞬間に**拾えない山**（`itemName()` が空の
> アイテム ID）が地面に落ちます。**型でも `npm run typecheck` でも止まりません。**
>
> **`replaceable` を付けるかどうかは形とは別の判断です。** 草むらと苗は
> `model: "cross"` も `CROSS_BOX` も `solid: false` も同じですが、**草むらは
> `replaceable: true`、苗は付けません** —— 付けると、植えた苗の上にブロックを置いた
> 拍子に苗が黙って消えます（`placeSpot()` が狙ったマス自身を返すため）。

## `use.md` — 種の居場所（クワの次・バケツより前）（2026-09-02・AUTODEV 12 前半）

**上の「`use.md` — 「右クリックで何が起きるか（`use.ts`）」の冒頭と並び順」
（AUTODEV 5 が「11 通り → 12 通り」と書いた節）と食い違います。** 古いほうは
直していません（この周の決まり）ので、**取り込む人は新しいほう＝こちらを採ってください。**

- **通りの数**: あちらは「12 通り」ですが、そのあとクワ（AUTODEV 6）と種（この周）が
  入って **14 通り**になりました。`src/use.ts` の冒頭のコメントも 14 と書いてあります。

**同じ節の並び順の説明に足してください:**

> **種（`plant`）はクワ（`till`）の次、バケツより前。** 「耕す → 植える」の順に並べておくと
> 読めます。**植わるかどうか（耕地の上か・塞がっていないか）はここで決めないこと** ——
> `till` とまったく同じで、可否は `placing.ts` の `tryPlant()` です。`decideUse()` は
> 「種を持って何かを狙っている」という事実だけで `plant` の注文を出します
> （**土を狙っても `plant` に来ます**。弾くのは `placing.ts` の仕事で、2 か所で弾かないこと）。

**同じ節の「`main.ts` に `if` の列を書き戻さないこと」の箇条書きにも足してください:**

> `test/use.test.ts` は `main.ts` に **`isSeed(`** が戻っていないかも見ています
> （`isHoe(` / `isShears(` と同じ理由）。**`test/ui.test.ts` の `routed` には
> `["苗を植える", "tryPlant("]`** が入っていて、置ける／置けないの規則が
> `main.ts` へ戻っていないかを見ています。

## `blocks-shapes.md` — 「置く判断」の節に `tryPlant()` を（2026-09-02・AUTODEV 12 前半）

**`placing.ts` の入口を並べている所（`tryPlace` / `tryIgnite` が出てくる箇条書き）に
足してください:**

> **右クリックで世界を書き換える入口は `placing.ts` に 4 本あります**:
> `tryPlace()`（置く）/ `tryIgnite()`（火を点ける）/ `tryBucket()`（汲む・流す）/
> `tryTill()`（耕す）/ `tryPlant()`（植える）。**どれも `World` を丸ごと受け取らず**、
> `main.ts` は結果（`"none"` / `"blocked"` / `"placed"`）を貼るだけです。
> **`tryTill()` と `tryPlant()` は対**で、**上のマスが塞がっているかの規則
> （`!isReplaceable(above) || isLiquid(above)`）を共有しています** ——
> 片方だけ変えると「耕せるのに植えられない」ができます。

---

**未取り込み 10 件**（AUTODEV 5 の 4 件 + AUTODEV 6 の 3 件 + **この周の 3 件**）。

**上の「いま未取り込みは 0 件です。」（41 行目あたり）は古い行です。** あれは剣の 2 件を
取り込んだ時点のもので、そのあと AUTODEV 5 が 4 件、AUTODEV 6 が 3 件、この周が 3 件を
足しました。**古いほうは直していません**（この周の決まり）ので、**数はこの行を採ってください。**
