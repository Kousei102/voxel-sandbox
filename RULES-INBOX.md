# 決まりごとの受け渡し（層 2・層 3 の更新待ち）

**無人の周はここに書きます。取り込むのは人が居る周です。**

`.claude/rules/**` と `.claude/skills/**` を編集しようとすると「sensitive file」の
確認が出て、**答える人が居ない周はそこで止まります**（実装もテストも全部終えたあとで、です）。
`.claude/settings.json` に許可を書いても消えませんでした（2026-08-25 実測。
リポジトリの中の設定が、自分と同じ `.claude/` の中身を編集する許可を自分に出すことは
できない、という形の防御だと思われます）。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 書き方

1 件 1 節。見出しは `## <どのファイル> — <一言>`。

- **どのファイルのどの節へ足すのか**を先に書く（節が無ければ「新しい節」と書く）
- **置きたい本文をそのまま置くこと。** 「モブの飛び方について書く」のような要約は不可 ——
  取り込む側が文章を書き直すことになり、決まりごとの言い回しが周ごとにばらけます
- `paths` を足したいときは、その行もそのまま書く

## 取り込む側（人が居る周）

1. 上から順に、書かれたとおりに `.claude/rules/` へ当てる
2. **当てた節は丸ごと消す**（`REVIEW.md` と同じ。残すと、どれが未取り込みか分からなくなります）
3. 取り込みだけで 1 コミットにしてよい

---

## `rules/dimensions.md` — リスポーン地点の 1 行を差し替え

「次元（ディメンション）」の節の箇条書きにある、この行:

> - **モブは次元ごとに持ちません**（そもそも保存しません）。移ったら消えるのが正しい。
>   **リスポーン地点（`bed`）も次元ごとに持ちません** —— 世界に 1 つの地点です。

を、**丸ごと下に差し替えてください**（2-4c で後半が嘘になりました。地点はいまも
1 つですが、**どの次元の 1 点か**を持つようになっています）。

- **モブは次元ごとに持ちません**（そもそも保存しません）。移ったら消えるのが正しい。
  **リスポーン地点（`bed`）も 1 点のまま**ですが、**どの次元の 1 点かを持ちます**
  （`SaveData.bedDim`。`beds.ts` の `BedPoint.dim`）。**次元ごとに 1 点ずつ持つ形に
  しないこと** —— 本家と違って戻り先はいつも「最後に寝たベッド」1 つです。
  - **`bedDim` はオーバーワールドなら省きます**（`dim` とまったく同じ作法）。だから
    オーバーワールドだけで遊んでいる人のセーブは、次元が入る前と 1 バイトも変わりません
  - **死んだらまず地点の次元へ戻すこと**（`main.ts` の `moveToSpawn()` が
    `beds.respawnDimension()` → `returnToDimension()` → `beds.respawnPlan()` の順で通します）。
    いま居る次元のままベッドのマスを読むと、**ネザーで死んだ人が天井の岩盤の上に湧きます**
    （`surfaceY()` は上から降りてくるので、最初に当たるのが天井の岩盤）
  - **ベッドが壊されていたらオーバーワールドへ落とすこと**（`respawnPlan()` が `home` を返す）。
    「いま居る次元」に落とすと、ネザーでベッドを壊してから死んだときに岩盤の上へ戻ります

## `rules/dimensions.md` — 「ポータルを通る」の節に 1 行足す

`portaltravel.ts` の箇条書きの末尾に、そのまま足してください。

- **次元を移る経路は `switchTo(行き先, liveState())` の形をそのまま写すこと**
  （いまはポータル（`travelThrough()`）とリスポーン（`returnToDimension()`）の 2 つ）。
  第 2 引数は「置いていく次元の持ち物」で、`emptyState()` などを渡すと**残してきた
  改変・落とし物・かまど・チェストが黙って消えます**（型では防げません。どちらも
  `DimensionState` なので、`test/dimensions.test.ts` が `main.ts` の呼び方を数えています）。
  **同じ次元なら移らないこと**（世界を作り直す必要がありません）。

## `rules/beds.md` — セーブの 1 行を差し替え

いちばん下の、この行:

> - セーブは `SaveData.bed`（`[x, y, z]`。省略可）。ベッドそのものは `edits` に入っているので、
>   ここが持つのは「どこで寝たか」だけです。

を、**丸ごと下に差し替えてください。**

- セーブは `SaveData.bed`（`[x, y, z]`。省略可）と **`SaveData.bedDim`**（次元の名前。
  **オーバーワールドなら省略**）。ベッドそのものは `edits` に入っているので、ここが持つのは
  「**どの次元の**どこで寝たか」だけです。**`bed` の形は変えていません**（`version` は 1 のまま）。
  - **`bedDim` が無ければオーバーワールド**とみなします。ネザーが入る前のセーブは、
    寝た場所が必ずオーバーワールドだったので、これで古いワールドも正しく読めます
  - **`beds.ts` は `dimensions.ts` を import しません**（生成器を引き連れてきます）。
    次元は `daynight.ts` の `setDimension()` と同じで**文字列で受ける**だけで、
    綴りのずれは `test/beds.test.ts` が `DIMENSIONS` と突き合わせています
  - **どの次元へ戻るかは `beds.ts` の判断です**（`respawnDimension()` と `respawnPlan()`）。
    **`main.ts` に `dim === NETHER` のような分岐を書かないこと**
    （`test/ui.test.ts` が `main.ts` の次元の分岐を上限 2 件で数えています）
