# ブラウザ確認の積み残し

**ループが追記し、ユーザーが見て消します。** この環境では画面も音も確かめられないので
（理由は `CLAUDE.md`）、見た目・音に触れたタスクはここに溜まります。

## 書き方

- タスク番号ごとに 1 節。**確認項目は番号付きで 4〜7 個**
  （番号を振ると「5 だけ違う」と返ってきて、すぐ切り分けられます）
- **「こうなっているはず」で書かないこと。** 実装を読んで裏を取ってから書く
- **アプリがもともと持っていない挙動を書かないこと**
- 確認が済んだ節は**丸ごと消す**（済んだものを残すと、どれが未確認か分からなくなります）

## 見てもらうとき

```bash
npm run dev        # http://localhost:5173（5173 が埋まっていると 5174）
```

デバッグに使えるもの:

- `M` キー — 照準の先にモブを 1 体（**湧きの条件に関わらず**出せます）
- `F3` — モブ数・落ちているアイテム・かまど・チェスト・空腹

---

## 1-6. 次元の器（見た目は変わりません。**セーブの経路だけ**）

**画面に出る変化はありません。** 触ったのは保存と読み込みの経路で、
壊れていても画面には何も出ず、**ワールドが黙って消える**形でしか気付けません
（`npm test` の 1341 件は通っていますが、実物の `localStorage` は見られません）。

**いま遊んでいるワールドがあるまま**開いてください（消してからでは 1 が見られません）。

1. **今あるワールドがそのまま開く。** 掘った跡・持ち物・かまどとチェストの中身・
   リスポーン地点が、閉じたときのまま戻っている
2. **メニューの「保存」→ リロードで、その続きから始まる**（オートセーブでも同じ）
3. **保存の形が変わっていない。** F12 →「アプリケーション」→ ローカルストレージ →
   `voxel-sandbox:v1` を開くと、**`dim` も `dims` も出ていない**こと
   （オーバーワールドしか無いので、次元が入る前と同じ形のままが正しい）
4. **「この種で作り直す」で新しいワールドになり、リロードしてもその新しいほうが開く**
5. **「保存データを削除」→ リロードで、まっさらなワールドから始まる**
