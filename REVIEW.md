# 通しで遊んで見つけた不具合

**ユーザーが端から端まで遊び、おかしいところをここに足します。** ループはここから 1 つ取って直します。

この環境では画面も音も確かめられないので（理由は `CLAUDE.md`）、
**「壊れているかどうか」を知っているのは実際に遊んだ人だけ**です。

## やり方を変えた理由

前は**ループが 1 周ごとに「確認してほしい項目」を書き溜めていました**（22 節・627 行）。
一度も消えないまま溜まり、**ユーザーの手が一往復ぶん要る質問**が並ぶだけになったので、
**通しのテストプレイで見つけ次第ここに足す**やり方に替えました。
古い 22 節は `docs/review-archive.md` に退避してあります（**ループは読まないこと**）。

## 書き方（ユーザー）

1 件 1 節。**節の見出しは症状**（「〜のはず」ではなく、**見えたこと**を書く）。

**足す場所はこのファイルのいちばん下**（「## 見つかった不具合」の下）です。
下は**書き方の見本**なので、ここに書き足さないこと:

```markdown
## [ ] ネザーから戻ると足元が岩盤の中

- **どこで**: ネザーポータルをくぐって戻った直後。種 `1`
- **見えたこと**: 画面が真っ黒で、動くと窒息のダメージが入る
- **どうなってほしいか**: 地面の上に出る
```

- **`[ ]` 未着手 / `[x]` 直した / `[!]` 直せない（理由を書く）**
- **直したあとも節を消さないこと。** 何が壊れていて何を直したかが残ります
- 種・次元・座標（`F3` の `xyz`）が分かれば書くと、ループが再現テストを書けます

## 直すとき（ループ）

- **上から `[ ]` を 1 つ**。1 周 = 1 件 = コミット 1 つ（`LOOP.md` の手順どおり）
- **再現するテストを先に書くこと。** 直したつもりが直っていないことは、
  この環境では確かめられません（`.claude/rules/testing.md`）
- **原因の 1 行を `grep` で探してから直すこと。** 確かめられない仮説で「備え」を書かない
- 直したら節を `[x]` にして、**何が原因でどう直したかを節の末尾に 1〜3 行**足す

## 遊ぶとき

```bash
npm run dev        # http://localhost:5173（5173 が埋まっていると 5174）
```

デバッグに使えるもの:

- `M` キー — 照準の先にモブを 1 体（**湧きの条件に関わらず**出せます）
- `N` キー — 飛び道具を 1 つ（押すたびに 火球 → 矢 → エンダーアイ → ブレス）
- `C` キー — クリエイティブの切り替え / `E` — 一覧
- `F3` — モブ数・落ちているアイテム・かまど・チェスト・空腹・`xyz`

**クリアまでの道**: 鉄 → バケツ → 黒曜石 → 火打石と打ち金 → ネザー →
ブレイズ（要塞）→ エンダーマン → エンダーアイ → 要塞 → エンドポータル →
エンド → クリスタルを弓で割る → エンダードラゴン。
種 `1` だと要塞は **(96, 54)**、輪の中心は **(96, 55, 54)** です。

---

# 見つかった不具合

**新しいものはこの下に足してください。** 上から順にループが取ります。

## [ ] エンダードラゴンの行動パターンの調整

- **どこで**: エンド
- **見えたこと**: ドラゴンの行動パターンがこちらを追いかけてきてからの近接攻撃しかなく、ブレスを使うこともない。
- **どうなってほしいか**: 
  - ブレスの挙動を変える。
  - ドラゴンが定期的に３種類の行動パターンを入れ替える。
  ### ブレスの挙動変更
  - **弾道**: ブレイズの火の玉と同じく、落ちずにまっすぐ飛ぶ
  - **速度**: ブレイズの火の玉の2.5倍にする
  ### 行動パターン
  - **プレイヤーを追いかけて近接攻撃**: 現状の行動パターン
  - **上空を飛びながらブレスの弾を撃つ**: 輪を回る飛行のパターンで飛び、プレイヤーに向かってブレスを一定間隔で撃つ。この時、プレイヤーよりも10ブロック上の高さを飛ぶ。ブレスの発射レートは１.5秒に1発
