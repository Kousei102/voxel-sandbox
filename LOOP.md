# ループの手順書

**毎周これを読んでから始めてください。** 1 周 = タスク 1 つ = コミット 1 つ。

このファイルは**手順と禁じ手**だけです。恒久的な決まりごとは `CLAUDE.md` と
`.claude/rules/`（触ったファイルに応じて自動で読まれます）、
タスクの並びは `TASKS.md`、いまの状況は `HANDOFF.md`、
**ユーザーが通しで遊んで見つけた不具合は `REVIEW.md`**。

**毎周読まれる文書には行数の上限があります**（`TASKS.md` 150 / `REVIEW.md` 200 /
`CLAUDE.md` 200 / `LOOP.md` 200。`test/docs.test.ts` が見ています）。
**上限に当たったら上限を上げず、済んだぶんを `docs/` へ退避すること**
（`main.ts` の 1500 行と同じ扱い）。退避先は `docs/tasks-done.md` と
`docs/review-archive.md` で、**どちらもループは読みません。**

---

## 0. 先に読むもの

1. **`CLAUDE.md`** — とくに「ブロック ID の枠」「確かめられないものは、確かめられるものから
   切り離す」「崩してはいけない不変条件」。**この 3 節を守れないタスクは、やらずに人を呼ぶこと**
   - 領域別の決まりごとは `.claude/rules/*.md` にあり、**`Read` / `Edit` でそのファイルを
     開いた時点で自動的に読み込まれます。** `cat` で読むと載らないので、`src/**` と
     `test/**` は `Read` / `Edit` を使うこと
   - 手順書は `.claude/skills/`（`add-block` / `add-stateful-block` / `unverifiable-pair`）
2. **`ROADMAP.md` の「ID の予約表」** — 使ってよい番号はここにあるものだけ
3. **`HANDOFF.md`** — 前の周が何をして、何が未確認か

## 1. 1 周の手順

```
git pull --rebase              # **無人でも人でも、まずここから**（他の周の成果を取り込む）
git status                     # 汚れていたら止まる（前の周が閉じていない）
npm test                       # 始める前に緑であることを確かめる（赤なら 5. へ）
```

1. **仕事を 1 つ取る。順番は次のとおり。**
   1. **`TASKS.md` の「未着手」**から、依存が解決済みのものを上から 1 つ。飛ばさないこと
      （飛ばしたくなったら依存が書けていないので、`TASKS.md` を直してから取る）
   2. 無ければ **`REVIEW.md` の `[ ]`**（ユーザーが通しで遊んで見つけた不具合）を上から 1 つ
   3. どちらも無ければ、`TASKS.md` 末尾の「取るタスクが無くなったら」に従う
      （実装しない・コミットしない）
2. **実装する。** 判断は判断のファイルへ、three と DOM は描画側へ（`CLAUDE.md` の対の表）
3. **テストを書く。** 下の「3. 完了条件」を満たすこと
4. `npm run typecheck` → `npm test`
5. **手触りの数値を置いたなら `TUNING.md` に 1 行足す**（下の「3.5」）
6. **緑ならコミット 1 つ。** 赤なら直す。直らなければ 5. へ
7. **状態を更新して、済んだ本文を退避する**（下の「4.」）
8. **確認項目を書き溜めないこと。** 見た目・音・GLSL に触れても `REVIEW.md` には
   何も足しません（下の「4.」）
9. **決まりごと（`.claude/rules/`）を直したくなったら `RULES-INBOX.md` へ書く**（下の「4.5」）。
   **直接編集しないこと** —— 無人の周はそこで止まります
10. **`HANDOFF.md` を丸ごと書き直す。** 引き写さず、`npm test` の結果で裏を取ってから
11. **`loop/endgame` へ push する**（下の「7.」）

## 2. 1 タスクの大きさ

**`typecheck` と `test` が緑になって、コミット 1 つで閉じる分量。**

閉じないと分かったら、**その場でタスクを分割して `TASKS.md` に書き戻し、その周は終える。**
中途半端なコミットを積まないこと（次の周が、前の周の未完成な状態を読み解くはめになる）。

## 3. 完了条件

- [ ] `npm run typecheck` が通る
- [ ] `npm test` が緑（**進行テストの未達は失敗ではない**。`check()` の NG が 0 件という意味）
- [ ] **足した判断にテストがある。** 値を出力してから判定する形（`.claude/rules/testing.md`）
- [ ] **新しい「確かめられないもの」を足したなら、対のガードも足した**
      （`*render.ts` を作ったら、そこに判断が漏れていないかを見張るテスト。
      `test/mobs.test.ts` の `mobrender.ts に判断が漏れていない` が手本。
      `test/ui.test.ts` が「見張りのないレンダラ」を検出するので、忘れると赤くなる）
- [ ] **進行テストの項目を達成したなら、`test/progression.test.ts` の
      `ACHIEVED_BASELINE` を上げ、その項目の probe を `仮` から `本物` に差し替えた**
- [ ] **熱い場所を触ったなら `npm run bench` を 3 回まわして中央値を見た。**
      対象は `worldgen.ts` の `generateChunk` / `heightAt`、`mesher.ts`、
      `World.update()` の中。**1 回では当てになりません**（同じコードで
      `generateChunk` が 0.93〜1.46ms に振れます）。**測った値をコミットメッセージに残すこと**
- [ ] **手触りの数値を置いたなら `TUNING.md` に 1 行足した**（下の「3.5」）
- [ ] コミットメッセージは日本語 1 行で「何をしたか」

## 3.5 手触りの数値は、止まらずに置いて記録する

**ダメージ・体力・湧きの条件・引きの時間のような「難度と手触り」の数値で止まらないこと。**
あとからいくらでも調整できるもので、**先にクリアまで通すほうが優先**だとユーザーと決めました。

- **本家 Minecraft の値をそのまま暫定で置くこと**（ブレイズの体力 20、火球 5、
  弓の最大 9、ドラゴンの体力 200 など）。根拠のある値なので大崩れしません
- **分からないものは目分量で置いてよい。** 止まる理由にはなりません
- **置いたら `TUNING.md` に 1 行**。場所（ファイルと定数名）・いまの値・
  出典（本家の値 / 目分量）・触ると何が変わるか。**最後にユーザーと一括で調整します**
- 数値は**judgment のファイル**（`mobs.ts` の表など）に置くこと。
  `main.ts` や `*render.ts` に散らすと、調整のときに探せません

## 4. 周の終わりに、済んだ本文を退避する

**毎周読まれる文書は短く保つこと。** ループを回すうちに `TASKS.md` が 762 行・
`REVIEW.md` が 627 行まで育ち、**その 9 割が「終わったことの記録」**になりました
（2026-08-28 に整理済み）。ループはそれを毎周読まされます。

- `TASKS.md` のタスクを終えたら、**その節の本文を丸ごと `docs/tasks-done.md` の
  末尾へ移し**、`TASKS.md` には `- [x] 番号. 名前` の 1 行だけ残すこと
- 見取り図（`<!-- 着手したときの見取り図 -->`）も一緒に移すこと。
  **次に似たものを足す周が読む手本**なので、消さない
- `REVIEW.md` の不具合を直したら `[x]` にして、**原因と直し方を節の末尾に 1〜3 行**足し、
  **`npm run archive` を走らせること。** `[x]` の節が `docs/tasks-done.md` の末尾へ移り、
  `REVIEW.md` から消えます。**手で移さないこと** —— 退避先を開いて末尾を探すと、
  このスクリプトが避けている 700 行ぶんの読み込みが毎周戻ってきます
  （`[ ]` と `[!]` は動きません。移すのは `[x]` だけ）
- 上限に当たると `npm test` が赤くなります。**上限を上げないこと**

### 見た目・音に触れたとき

**この環境では画面も音も確かめられません**（理由は `CLAUDE.md`）。
それでも **`REVIEW.md` に確認項目を書き溜めないこと。**

いまは**ユーザーが通しで遊び、おかしいところを `REVIEW.md` に足す**やり方です
（前のやり方をやめた理由は `REVIEW.md` の中）。代わりに、触れた周は次の 2 つを:

- **`HANDOFF.md` に「何が見た目に出るか」を 2〜3 行**（次にユーザーが遊ぶときの見どころ）
- **GLSL に触ったなら必ず書くこと。** シェーダの壊れ方は「画面が真っ黒」なので、
  ヘッドレスのテストが全部緑でも壊れていることがあります

## 4.5 決まりごと（層 2・層 3）の更新は `RULES-INBOX.md` へ

**無人の周は `.claude/rules/**` と `.claude/skills/**` を直接編集しないこと。**
編集しようとすると「sensitive file」の確認が出て、**答える人が居ないのでそこで止まります**
（実装もテストも全部終えたあとで、です）。`.claude/settings.json` に許可を書いても
消えませんでした（2026-08-25 実測）。

代わりに**ルート直下の `RULES-INBOX.md` に追記**してください。書き方はあのファイルの中に
あります。**要約せず、置きたい本文をそのまま置くこと。** 取り込みは人が居る周がやります。

**`CLAUDE.md` は層 1（`.claude/` の外）なので、今までどおり普通に編集してかまいません。**

## 5. 止まって人を呼ぶ条件

**迷ったら止まること。** 止まるのは失敗ではありません。`HANDOFF.md` に理由を書いて終えます。

1. **`ROADMAP.md` の予約表に無いブロック ID / アイテム ID を使いたくなった**
2. **`SaveData` の形を変えたくなった**
   （省略可の**キーを足す**のは可。**`version` は絶対に 1 のまま**）
3. **2 周続けてテストが赤い**
4. **`main.ts` が上限 1500 行に届いた**（判断を別ファイルへ出しても収まらないとき）
5. **`world.test.ts` の p99 が落ちた** —— まず `npm test` を**もう一度**走らせること。
   マシンが混むと落ちます（実測: 2 本同時で p99 が 170ms、上限 39ms）。
   2 回続けて落ちたときだけ退行として扱う

**止まらなくなったもの（前は止まっていました）:**

- **難度・手触りの数値** → 上の「3.5」。本家の値を暫定で置き、`TUNING.md` に記録して進む
- **GLSL** → 書いてよい。ただし判断は `daynight.ts` 側に置き、`REVIEW.md` に確認項目を書く
- **`TASKS.md` の「⛔ ここで止まる」** → その行は外しました。上から順に最後まで取ります

## 6. 禁じ手

- **既存の ID を振り直すこと。** `localStorage` にブロック ID がそのまま入っているので、
  既存プレイヤーのワールドが別のブロックに化けます。**番号は足すだけ**
- **`SaveData.version` を上げること。** `load()` が弾くので、全員のワールドが読めなくなります
- **`master` への push。** 公開サイト（GitHub Pages）が差し替わる外向きの操作です。
  ループが push してよいのは **`loop/endgame` だけ**（下の「7.」）。
  `master` へのマージと push はユーザーが確認してからやります
- **テストの判定をゆるめて緑にすること。** とくに `world.test.ts` の p99 の上限を
  `constants.ts` の予算から計算する形に戻さないこと（予算そのものを壊す退行を見逃します）
- **`test/progression.test.ts` の未達項目を `check()` に掛けること**
  （`npm test` が最初から赤くなり、本物の退行と区別できなくなります）
- **確かめられない仮説で「備え」を書くこと。** この環境では効いたかどうかも確かめられません。
  まず `grep` で原因の 1 行を探すこと
- **無人の周が `.claude/rules/` や `.claude/skills/` を直接編集すること。**
  確認待ちでそこで止まります。書きたいことは `RULES-INBOX.md` へ（上の「4.5」）
- **判断を `sky.ts` / `terrainshader.ts` / `*render.ts` に書くこと。** GLSL は書いてよく
  なりましたが、**何色にするか・いつ変えるかは `daynight.ts` 側**です（`CLAUDE.md` の対の表）。
  ここを崩すと、その領域が丸ごと「ブラウザを開くまで確かめられないもの」に戻ります

## 7. ブランチ

作業は **`loop/endgame`**。`master` には触りません。

- **1 周の終わりに `loop/endgame` へ push すること。** 無人で回っているので、
  push しないと次の周（別のマシン・別のセッション）から成果が見えません。
  周の頭の `git pull --rebase` と対です
- **`master` への push は禁止**（公開サイトが差し替わる外向きの操作。ユーザーがやります）。
  `.github/workflows/deploy.yml` は `main` / `master` への push でしか走らないので、
  `loop/endgame` へ push してもデプロイは起きません

## 8. 使えるもの

```bash
npm run dev        # ユーザーがブラウザで見る用（この環境では WebGL が動きません）
npm run typecheck
npm test           # 20 秒前後
npm run bench      # 1 回では当てになりません。数回まわして中央値を見ること
npm run archive    # REVIEW.md の [x] を docs/tasks-done.md の末尾へ移す
```
