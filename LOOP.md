# ループの手順書

**毎周これを読んでから始めてください。** 1 周 = タスク 1 つ = コミット 1 つ。

このファイルは**手順と禁じ手**だけです。恒久的な決まりごとは `CLAUDE.md` と
`.claude/rules/`（触ったファイルに応じて自動で読まれます）、
タスクの並びは `TASKS.md`、いまの状況は `HANDOFF.md`、ブラウザ確認の
積み残しは `REVIEW.md`。

---

## 0. 先に読むもの

1. **`CLAUDE.md`** — とくに「ブロック ID の枠」「確かめられないものは、確かめられるものから
   切り離す」「崩してはいけない不変条件」。**この 3 節を守れないタスクは、やらずに人を呼ぶこと**
   - 領域別の決まりごとは `.claude/rules/*.md` にあり、**`Read` / `Edit` でそのファイルを
     開いた時点で自動的に読み込まれます。** `cat` で読むと載らないので、`src/**` と
     `test/**` は `Read` / `Edit` を使うこと
   - 手順書は `.claude/skills/`（`add-block` / `add-stateful-block` / `unverifiable-pair`）
2. **`ROADMAP.md` の「ID の予約表」** — 使ってよい番号はここにあるものだけ
3. **`HANDOFF.md`** — 前の周が何をして、何が未確認か

## 1. 1 周の手順

```
git status                     # 汚れていたら止まる（前の周が閉じていない）
npm test                       # 始める前に緑であることを確かめる（赤なら 5. へ）
```

1. **`TASKS.md` の上から、未着手で依存が解決済みのタスクを 1 つ取る。**
   飛ばさないこと。飛ばしたくなったら、それは依存が書けていないということなので
   `TASKS.md` を直してから取る
2. **実装する。** 判断は判断のファイルへ、three と DOM は描画側へ（`CLAUDE.md` の対の表）
3. **テストを書く。** 下の「3. 完了条件」を満たすこと
4. `npm run typecheck` → `npm test`
5. **緑ならコミット 1 つ。** 赤なら直す。直らなければ 5. へ
6. `TASKS.md` の状態を更新（`[ ]` → `[x]`）
7. **見た目・音に触れたなら `REVIEW.md` に追記**（下の「4.」）
8. **`HANDOFF.md` を丸ごと書き直す。** 引き写さず、`npm test` の結果で裏を取ってから

## 2. 1 タスクの大きさ

**`typecheck` と `test` が緑になって、コミット 1 つで閉じる分量。**

閉じないと分かったら、**その場でタスクを分割して `TASKS.md` に書き戻し、その周は終える。**
中途半端なコミットを積まないこと（次の周が、前の周の未完成な状態を読み解くはめになる）。

## 3. 完了条件

- [ ] `npm run typecheck` が通る
- [ ] `npm test` が緑（**進行テストの未達は失敗ではない**。`check()` の NG が 0 件という意味）
- [ ] **足した判断にテストがある。** 値を出力してから判定する形（`.claude/rules/testing.md`）
- [ ] **新しい「確かめられないもの」を足したなら、対のガードも足した**
      （`*render.ts` を作ったら、そこに判断が漏れていないかを見張るテスト。
      `test/mobs.test.ts` の `mobrender.ts に判断が漏れていない` が手本。
      `test/ui.test.ts` が「見張りのないレンダラ」を検出するので、忘れると赤くなる）
- [ ] **進行テストの項目を達成したなら、`test/progression.test.ts` の
      `ACHIEVED_BASELINE` を上げ、その項目の probe を `仮` から `本物` に差し替えた**
- [ ] **熱い場所を触ったなら `npm run bench` を 3 回まわして中央値を見た。**
      対象は `worldgen.ts` の `generateChunk` / `heightAt`、`mesher.ts`、
      `World.update()` の中。**1 回では当てになりません**（同じコードで
      `generateChunk` が 0.93〜1.46ms に振れます）。**測った値をコミットメッセージに残すこと**
- [ ] コミットメッセージは日本語 1 行で「何をしたか」

## 4. 見た目・音に触れたとき

**この環境では画面も音も確かめられません**（理由は `CLAUDE.md`）。触れたら
`REVIEW.md` に**確認項目を番号付きで 4〜7 個**書くこと。

- **「こうなっているはず」で書かないこと。** 実装を読んで裏を取ってから書く
- **アプリがもともと持っていない挙動を書かないこと**（ユーザーの手を一往復ぶん取ります）
- 未確認のまま次の見た目の変更を積み上げないこと。**同じ場所の見た目を続けて 2 回変えるなら、
  1 回目の確認を待つ**（落ちたときに切り分けられなくなります）

## 5. 止まって人を呼ぶ条件

**迷ったら止まること。** 止まるのは失敗ではありません。`HANDOFF.md` に理由を書いて終えます。

1. **`ROADMAP.md` の予約表に無いブロック ID / アイテム ID を使いたくなった**
2. **`SaveData` の形を変えたくなった**
   （省略可の**キーを足す**のは可。**`version` は絶対に 1 のまま**）
3. **2 周続けてテストが赤い**
4. **難度・手触りの判断**（ドラゴンの体力、湧きの条件、ポータルに要る黒曜石の数、
   餓死の線と同じ類）。ゲームの手応えを決めるものはユーザーのものです
5. **GLSL を書きたくなった**（`terrainshader.ts` / `sky.ts` の中でも、増やすなら呼ぶこと）
6. **`main.ts` が上限 1500 行に届いた**
7. **`TASKS.md` の「⛔ ここで止まる」の行に当たった** —— そこから下のタスクは
   **ユーザーと同席で取るもの**です（見た目・セーブの形に触るなど、落ちたときに
   無人では切り分けられないもの）。**その行を飛び越えて下から取らないこと。**
   その周は何も取らずに終えて、`HANDOFF.md` に「ここで止まった」と書きます
8. **`world.test.ts` の p99 が落ちた** —— まず `npm test` を**もう一度**走らせること。
   マシンが混むと落ちます（実測: 2 本同時で p99 が 170ms、上限 39ms）。
   2 回続けて落ちたときだけ退行として扱う

## 6. 禁じ手

- **既存の ID を振り直すこと。** `localStorage` にブロック ID がそのまま入っているので、
  既存プレイヤーのワールドが別のブロックに化けます。**番号は足すだけ**
- **`SaveData.version` を上げること。** `load()` が弾くので、全員のワールドが読めなくなります
- **`master` への push。** 公開サイト（GitHub Pages）が差し替わる外向きの操作です。
  ループは**コミットまで**。push はユーザーが確認してからやります
- **テストの判定をゆるめて緑にすること。** とくに `world.test.ts` の p99 の上限を
  `constants.ts` の予算から計算する形に戻さないこと（予算そのものを壊す退行を見逃します）
- **`test/progression.test.ts` の未達項目を `check()` に掛けること**
  （`npm test` が最初から赤くなり、本物の退行と区別できなくなります）
- **確かめられない仮説で「備え」を書くこと。** この環境では効いたかどうかも確かめられません。
  まず `grep` で原因の 1 行を探すこと

## 7. ブランチ

作業は **`loop/endgame`**。`master` には触りません。
`.github/workflows/deploy.yml` は `main` / `master` への push でしか走らないので、
ループのコミットでデプロイは起きません。

## 8. 使えるもの

```bash
npm run dev        # ユーザーがブラウザで見る用（この環境では WebGL が動きません）
npm run typecheck
npm test           # 12 秒前後
npm run bench      # 1 回では当てになりません。数回まわして中央値を見ること
```
