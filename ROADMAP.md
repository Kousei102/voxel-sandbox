# これから入れるもの

恒久的な決まりごとは `CLAUDE.md` にあります。ここに書くのは「次に何を入れるか」だけです。
**着手する順はユーザーと決めること。**

## フェーズ 2 — 完了

- **ハーフブロック — 済み**（石・丸石・板・砂岩の 4 材質。上付きは 64 以降）。
- **草 — 済み**（`TALL_GRASS` = 32。`model: "cross"`。生えやすさは `biomes.ts` の `grass`）。
- **階段 — 済み**（同じ 4 材質。大元 4 個が 33..36、向き違い 28 個が 68..95）。
- 羊毛（37）がモブのドロップとして 1 個使っています。

材質を足す手順と ID の空きは `CLAUDE.md` の「ブロック ID の枠」を見てください。

## バイオームに続けて入れると効くもの

- 砂漠を平らに、山をもっと険しく（`heightAt()` に気候を渡す。費用に注意）。
- 川・湖。いまの水は海面より下だけ。
- サバンナ・ジャングルなど、木の形が違うバイオーム（`biomes.ts` の表に足すだけ）。
- サボテンに触れたときのダメージ（`vitals.ts` に足す）。

## フェーズ 3（当初の見取り図。ここから先は 1 つが大仕事）

いまのフェーズ 2 の残りが 1 日仕事なのに対し、こちらは 1 つずつが数日規模。

| | 中身 | 難所 |
| --- | --- | --- |
| 音 | **済み**（`CLAUDE.md` の「音」）。残っているのは環境音（風・洞窟）だけ | 鳴りっぱなしの音は「うるさい」と感じやすいので、入れるなら音量を別に持つこと |
| モブ | **済み**（`CLAUDE.md` の「モブ」）。豚・羊・ゾンビ。残っているのは種類を足すことだけ（`MOBS` の表に 1 行） | — |
| インベントリ | **済み**（`CLAUDE.md` の「インベントリ画面」）。判定の切り出し・ドラッグ配分・破棄・盤面のセーブ・シフトクリック・かき集め・数字キー・プレイ中の Q。操作は Minecraft と同等 | 操作を足すときの形は `CLAUDE.md` の「インベントリ画面」に書いてあります |
| テクスチャ | Canvas2D でアトラスを実行時生成し、面に貼る | **いちばん重く、いちばん危ない。** UV を頂点属性に足すだけでは足りず、greedy で統合した長方形にタイルを繰り返す必要がある（タイル番号を属性で渡し、フラグメント側で `fract()` する）。つまりシェーダ作業が要り、この環境では検証できない |

## 空腹 — 済み

`CLAUDE.md` の「体力と空腹」。肉ゲージ 10 個・消耗・餓死（体力 1 で止まる）・右クリック長押しで
食べる・腐った肉の毒。**ピースフルの代用だった自然回復は外しました**（空腹 18 以上でだけ回復）。

残っているのは:

- **食べ物の種類** — いまは肉 3 つだけ。作物（小麦・パン）を入れるなら、先に苗木と同じで
  「育つブロック」の仕組みが要ります。
- **満腹度の保存** — いまは空腹だけ保存しています（`SaveData.hunger`）。
- **空腹で死ぬかどうか** — `STARVE_FLOOR` を 0 にすれば死にます。**難度が変わるのでユーザーと決めること。**

## 落ちたアイテム — 済み

`CLAUDE.md` の「落ちたアイテム」。掘ったもの・倒したモブのドロップ・捨てたものが地面に落ち、
近づくと拾えます。支えを失った松明も回収できます。**保存します**（モブとは逆）。

これで塞がっていたものが解けました。**死亡時ドロップとまとめ捨ても入りました**（下）。
残っているのは**吸い寄せ**（アイテムがプレイヤーへ飛んでくる演出）だけで、
いまは範囲に入ったら拾うだけです。

## 死亡時ドロップ / まとめ捨て — 済み

`CLAUDE.md` の「体力と空腹」と「インベントリ画面」。

- **死ぬと持ち物を全部その場に落とします。** 落とすのは `inventory.takeAll()`、
  場所を決めるのは `main.ts` の `dropOnDeath()`。**奈落で死んだぶんは消えます**
  （Minecraft と同じ。ユーザーと決めた線）。
- **Ctrl+Q（Shift+Q でも可）で山ごと捨てます。** プレイ中も画面を開いている間も同じ規則。

残っているのは:

- **リスポーン地点** — いまもワールドの初期位置ひとつだけ。持ち物を落とすようになったぶん、
  ベッドの有無が効いてきます（`ROADMAP.md` の「塞がっている」）。
- **落とした持ち物の猶予** — 5 分（`DESPAWN_AGE`）は他の落とし物と共通です。
  死んだぶんだけ延ばすなら、山ごとに寿命を持たせる必要があります。

## かまど + 精錬 — 済み

`CLAUDE.md` の「かまどと精錬」。丸石 8 個で作り、材料・燃料・焼き上がりの 3 枠。
**精錬の代用 2 つを戻しました**（鉄・金の鉱石は鉱石のまま落ち、砂 4 個 → ガラスは削除）。
焼き豚（アイテム 83）が増えたので、「空腹」がそのまま繋がりました（上）。

「位置ごとに状態を持つブロック」の器（`furnaces.ts` + `SaveData.furnaces`）ができたので、
**チェスト（下）がそのまま乗り、醸造台も同じ形で入ります。**

## チェスト — 済み

`CLAUDE.md` の「チェスト」。板 8 個で作り、27 枠。壊すと中身が地面に出ます。
**かまどが作った器に乗せた 2 つ目**なので、`chests.ts` 1 ファイルと `SlotArea` への
`"chest"` 追加だけで済みました（`CraftScreen` は増やしていません）。

残っているのは:

- **大きいチェスト**（2 個並べて 54 枠）— 隣り合っているかを見る必要があり、
  「位置ごとの状態」が 2 マスにまたがる初めての例になります。
- **中身を見せる見た目** — いまは開いていても閉じていても同じブロックです。
  かまどのような状態違いの ID を足せば作れますが、1..63 をもう 1 個使います。

## 塞がっている（先に別のものが要る）

| 入れたいもの | 先に要るもの |
| --- | --- |
| 苗木 | — （いまは葉から棒だけ出る） |
| 道具の耐久値 | — |
| 大きいチェスト（54 枠） | — （隣り合ったチェストを 1 つとして扱う仕組み） |

---

# クリア導線（ネザー → エンド → エンダードラゴン）

**ここから先はループで進めます。** 手順は `LOOP.md`、タスクの並びは `TASKS.md`、
ブラウザ確認の積み残しは `REVIEW.md`。

**ダイヤは既に入っています**（`DIAMOND_ORE` = 17 / `y ≤ 14` / ダイヤ道具 78-80）。
欠けているのは**黒曜石から先**です。

## ID の予約表

**この表に無い番号を使わないこと。** 使いたくなったら人を呼んでください（`LOOP.md` の停止条件）。
既存の ID を振り直すと `localStorage` のセーブが別のブロックに化けます
（`CLAUDE.md` の「ブロック ID の枠」）。

**予約であって実装ではありません。** 空の `def()` を `blocks.ts` に置かないこと ——
`test/blocks.test.ts` が毎回出す「1..63 の空き」が狂います。

| ID | ブロック | 備考 |
| --- | --- | --- |
| 42 | `LAVA` | 静的でよい（流動は後）。水と同じ半透明レイヤー |
| 43 | `OBSIDIAN` | `minTier: TIER_DIAMOND` |
| 44 | `GRAVEL` | 火打石の出どころ |
| 45 | `NETHERRACK` | |
| 46 | `SOUL_SAND` | |
| 47 | `GLOWSTONE` | `emission` を持つ（松明と同じ枠） |
| 48 | `NETHER_BRICK` | ネザー要塞の材料 |
| 49 | `NETHER_PORTAL` | 非立方体（`isProp`）。向き 2 種は 64 以降 |
| 50 | `END_STONE` | |
| 51 | `END_PORTAL_FRAME` | 非立方体。向き 4 種 x 目の有無は 64 以降 |
| 52 | `END_PORTAL` | 非立方体 |
| 53 | `STONE_BRICK` | 要塞（stronghold） |
| 54..63 | **予備 10 個** | ネザーレンガ・石レンガのハーフ／階段はここから |

**立方体は 63 まで**（`encodeFace` の 6 ビット）。向き違い・状態違いは 64 以降へ。
上の非立方体（ポータル・枠）は `isProp()` が true になるので、向き違いを 64 以降に置けます。

アイテム ID は **84 から**（83 = 焼き豚まで使用済み。255 まで余裕があります）:
火打石・火打石と打ち金・ブレイズロッド・ブレイズパウダー・エンダーパール・
エンダーアイ・弓・矢・ネザークォーツ。

## 土台（クリア導線より先に要るもの）

| 土台 | なぜ要るか | いまの状態 |
| --- | --- | --- |
| 溶岩 | 黒曜石が作れない。ネザーの溶岩海も | 無し（水は静的な `WATER` だけ） |
| `portals.ts` | 枠の検出・点火の可否 | 無し |
| `dimensions.ts` + `World` の生成器注入 | ネザー / エンド | `World` が `new WorldGen(seed)` を内部で作っている |
| `structures.ts` | ネザー要塞・要塞・エンドの柱 | 木を隣接 8 列に ±2 でスタンプしているだけ |
| `projectiles.ts` + `projectilerender.ts` | 火球・矢・投げたエンダーアイ・ブレス | 無し。`drops.ts` / `droprender.ts` と同じ対の形にすること |

`WORLD_HEIGHT` は 128（`CHUNK_LAYERS` 8 x 16）なので、**ネザー（天井付き 128）も
エンドもそのまま入ります。縦は触らないこと。**

## 進み具合

`test/progression.test.ts` が「達成 N / 12」を毎回出します。**これが進捗の唯一の指標**で、
`npm test` の緑は「壊していない」しか言いません。
