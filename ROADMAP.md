# これから入れるもの

恒久的な決まりごとは `CLAUDE.md` と `rules/` にあります。
ここに書くのは「次に何を入れるか」だけです。
**着手する順はユーザーと決めること。**

## フェーズ 2 — 完了

- **ハーフブロック — 済み**（石・丸石・板・砂岩の 4 材質。上付きは 64 以降）。
- **草 — 済み**（`TALL_GRASS` = 32。`model: "cross"`。生えやすさは `biomes.ts` の `grass`）。
- **階段 — 済み**（同じ 4 材質。大元 4 個が 33..36、向き違い 28 個が 68..95）。
- 羊毛（37）がモブのドロップとして 1 個使っています。

材質を足す手順は `add-block` スキル、ID の枠は `CLAUDE.md` を見てください。

## バイオームに続けて入れると効くもの

- 砂漠を平らに、山をもっと険しく（`heightAt()` に気候を渡す。費用に注意）。
- 川・湖。いまの水は海面より下だけ。
- サバンナ・ジャングルなど、木の形が違うバイオーム（`biomes.ts` の表に足すだけ）。
- サボテンに触れたときのダメージ（`vitals.ts` に足す）。

## フェーズ 3（当初の見取り図。ここから先は 1 つが大仕事）

いまのフェーズ 2 の残りが 1 日仕事なのに対し、こちらは 1 つずつが数日規模。

| | 中身 | 難所 |
| --- | --- | --- |
| 音 | **済み**（`rules/audio.md`）。残っているのは環境音（風・洞窟）だけ | 鳴りっぱなしの音は「うるさい」と感じやすいので、入れるなら音量を別に持つこと |
| モブ | **済み**（`rules/mobs.md`）。豚・羊・ゾンビ。残っているのは種類を足すことだけ（`MOBS` の表に 1 行） | — |
| インベントリ | **済み**（`rules/inventory-screen.md`）。判定の切り出し・ドラッグ配分・破棄・盤面のセーブ・シフトクリック・かき集め・数字キー・プレイ中の Q。操作は Minecraft と同等 | 操作を足すときの形は `CLAUDE.md` の「インベントリ画面」に書いてあります |
| テクスチャ | Canvas2D でアトラスを実行時生成し、面に貼る | **いちばん重く、いちばん危ない。** UV を頂点属性に足すだけでは足りず、greedy で統合した長方形にタイルを繰り返す必要がある（タイル番号を属性で渡し、フラグメント側で `fract()` する）。つまりシェーダ作業が要り、この環境では検証できない |

## 空腹 — 済み

`rules/vitals.md`。肉ゲージ 10 個・消耗・餓死（体力 1 で止まる）・右クリック長押しで
食べる・腐った肉の毒。**ピースフルの代用だった自然回復は外しました**（空腹 18 以上でだけ回復）。

残っているのは:

- **食べ物の種類** — いまは肉 3 つだけ。作物（小麦・パン）を入れるなら、先に苗木と同じで
  「育つブロック」の仕組みが要ります。
- **満腹度の保存** — いまは空腹だけ保存しています（`SaveData.hunger`）。
- **空腹で死ぬかどうか** — `STARVE_FLOOR` を 0 にすれば死にます。**難度が変わるのでユーザーと決めること。**

## 落ちたアイテム — 済み

`rules/drops.md`。掘ったもの・倒したモブのドロップ・捨てたものが地面に落ち、
近づくと拾えます。支えを失った松明も回収できます。**保存します**（モブとは逆）。

これで塞がっていたものが解けました。**死亡時ドロップとまとめ捨ても入りました**（下）。
残っているのは**吸い寄せ**（アイテムがプレイヤーへ飛んでくる演出）だけで、
いまは範囲に入ったら拾うだけです。

## 死亡時ドロップ / まとめ捨て — 済み

`rules/vitals.md` と `rules/inventory-screen.md`。

- **死ぬと持ち物を全部その場に落とします。** 落とすのは `inventory.takeAll()`、
  場所を決めるのは `main.ts` の `dropOnDeath()`。**奈落で死んだぶんは消えます**
  （Minecraft と同じ。ユーザーと決めた線）。
- **Ctrl+Q（Shift+Q でも可）で山ごと捨てます。** プレイ中も画面を開いている間も同じ規則。

残っているのは:

- **リスポーン地点** — いまもワールドの初期位置ひとつだけ。持ち物を落とすようになったぶん、
  ベッドの有無が効いてきます（`ROADMAP.md` の「塞がっている」）。
- **落とした持ち物の猶予** — 5 分（`DESPAWN_AGE`）は他の落とし物と共通です。
  死んだぶんだけ延ばすなら、山ごとに寿命を持たせる必要があります。

## かまど + 精錬 — 済み

`rules/stateful-blocks.md` の「かまどと精錬」。丸石 8 個で作り、材料・燃料・焼き上がりの 3 枠。
**精錬の代用 2 つを戻しました**（鉄・金の鉱石は鉱石のまま落ち、砂 4 個 → ガラスは削除）。
焼き豚（アイテム 83）が増えたので、「空腹」がそのまま繋がりました（上）。

「位置ごとに状態を持つブロック」の器（`furnaces.ts` + `SaveData.furnaces`）ができたので、
**チェスト（下）がそのまま乗り、醸造台も同じ形で入ります。**

## チェスト — 済み

`rules/stateful-blocks.md` の「チェスト」。板 8 個で作り、27 枠。壊すと中身が地面に出ます。
**かまどが作った器に乗せた 2 つ目**なので、`chests.ts` 1 ファイルと `SlotArea` への
`"chest"` 追加だけで済みました（`CraftScreen` は増やしていません）。

**大きいチェスト（隣り合った 2 個で 54 枠）も済みです**（AUTODEV の 2 番）。
**ブロック ID もアイテム ID も 1 個も使っていません** —— 組かどうかは
`chests.ts` の `chestPartner()` がそのつど voxel から決め、54 枠は
`Chests.open()` が作る参照の並びなので、**セーブの形も変わっていません**
（1 マスにつき 27 枠のまま）。3 個以上並べると全部 27 枠に戻ります（`TUNING.md`）。

残っているのは:

- **中身を見せる見た目** — いまは開いていても閉じていても同じブロックです。
  かまどのような状態違いの ID を足せば作れますが、番号をもう 1 個使います。

## 塞がっている（先に別のものが要る）

| 入れたいもの | 先に要るもの |
| --- | --- |
| 苗木 | — （いまは葉から棒だけ出る） |
| 道具の耐久値 | — |

---

# クリア導線（ネザー → エンド → エンダードラゴン）

**ここから先はループで進めます。** 手順は `LOOP.md`、タスクの並びは `TASKS.md`、
ユーザーが通しで遊んで見つけた不具合は `REVIEW.md`、済んだタスクの記録は `docs/tasks-done.md`。

**ダイヤは既に入っています**（`DIAMOND_ORE` = 17 / `y ≤ 14` / ダイヤ道具 78-80）。
欠けているのは**黒曜石から先**です。

## ID の予約表

**この表に無い番号を使わないこと。** 使いたくなったら人を呼んでください（`LOOP.md` の停止条件）。
既存の ID を振り直すと `localStorage` のセーブが別のブロックに化けます
（`CLAUDE.md` の「ブロック ID の枠」）。

**予約であって実装ではありません。** 空の `def()` を `blocks.ts` に置かないこと ——
`test/blocks.test.ts` が毎回出す空きの数が狂います。

| ID | ブロック | 備考 |
| --- | --- | --- |
| 42 | `LAVA` | 静的でよい（流動は後）。水と同じ半透明レイヤー。**実装済み** |
| 43 | `OBSIDIAN` | `minTier: TIER_DIAMOND`。**実装済み**（`blocks.ts` の `quenched()`） |
| 44 | `GRAVEL` | 火打石の出どころ。**実装済み**（`worldgen.ts` の `VEINS`） |
| 45 | `NETHERRACK` | 硬さ 0.4（石より柔らかい）。**実装済み** |
| 46 | `SOUL_SAND` | 溶岩の海のほとり。遅くなる仕掛けは無し。**実装済み** |
| 47 | `GLOWSTONE` | `emission` 15（溶岩と同じ）。天井にぶら下がる。**実装済み** |
| 48 | `NETHER_BRICK` | ネザー要塞の材料。**実装済み** |
| 49 | `NETHER_PORTAL` | 非立方体（`isProp`）。X 向きが 49 / Z 向きが **103**。**実装済み** |
| 50 | `END_STONE` | エンドの島の地面（`endgen.ts`）。硬さ 3 / ツルハシ。**実装済み** |
| 51 | `END_PORTAL_FRAME` | 非立方体。向き 4 種 x 目の有無 = 8 通り（104..110）。**壊せない。実装済み** |
| 52 | `END_PORTAL` | 非立方体。寝ているので向きは 1 つだけ。**壊せない。実装済み**（起動は `endportal.ts`） |
| 53 | `STONE_BRICK` | 要塞（stronghold）の材料。**実装済み** |
| 54 | `END_CRYSTAL` | 非立方体。エンドの柱の上に 1 個ずつ。**すぐ壊せて何も落ちない。実装済み**（居場所は `endgen.ts` の `CRYSTAL_SPOTS`、生き死には `crystals.ts`） |
| 55 | `NETHER_BRICK_SLAB`（**ネザーレンガハーフ**・31）。**大元（下付き）だけが低帯**で、上付きは **166**（64..110 が満杯で凍結しているため）。`blocks.ts` の `slabPair()` を 1 回・`crafting.ts` の `slabRecipe()` を 1 行呼ぶだけで、**どちらの関数も中身は 1 文字も変えていません**。**色・硬さ 2・ツルハシ・`TIER_WOOD` は元のネザーレンガ（48）の写し**。**`items.ts` は 0 行**（低帯で `variantOf` が `AIR` なのでブロック → アイテムの for が作ります。`MAX_ITEM_ID` は 165 のまま）。**`main.ts` も生成も `FUEL` も `SMELTING` も ±0 行・`SaveData` は 1 バイトも増えません。実装済み** | ブロック 2 つ（55 と 166）。`blocks.ts` の ID 2 つと `slabPair()` 1 回 / `crafting.ts` の `slabRecipe()` 1 行 / `tools/shot.ts` の場面 `slabs` |
| 56 | `STONE_BRICK_SLAB`（**石レンガハーフ**・31）。上付きは **167**。**55 とまったく同じ形**で、色・硬さ・道具は元の石レンガ（53）の写し。**レンガそのもののレシピは足していません**（手に入るのは要塞と遺跡からだけのまま）。**実装済み** | ブロック 2 つ（56 と 167）。上と同じ 1 組 |
| 57..63 | **予備 7 個** | ネザーレンガ・石レンガの**階段**はここから（**上付き・向き違いは 170 以降**） |
| 64..110 | 向き違い・状態違い（**満杯**） | 上付きハーフ・階段・ベッド・ポータル・エンドポータル枠 |
| 111..114 | **剣 4 本**（木 / 石 / 鉄 / ダイヤ）。**共有帯の最初の使用者**。**実装済み** | アイテム。`items.ts` の剣のループ |
| 115 | **シアーズ**（羊を刈る）。**実装済み** | アイテム。`items.ts` の `SHEARS` / `isShears()` |
| 116 | **`FARMLAND`（耕地）**。**共有帯に置いた最初のブロック**で、**立方体**（`encodeFace` が 8 ビットなので置けます）。`variantOf: DIRT` なので**アイテムにならず、掘ると土**。**実装済み** | ブロック。`blocks.ts` の `def` / 何が耕地になるかは `tilled()` |
| 117..120 | **クワ 4 本**（木 / 石 / 鉄 / ダイヤ）。**実装済み** | アイテム。`items.ts` のクワのループ |
| 121 | **`WHEAT_CROP`（小麦の苗）**。非立方体（`model: "cross"`）で、**`variantOf` が自分自身**なのでアイテムを持ちません（大元にできる相手が居ないため。耕地の `variantOf: DIRT` とは事情が違います）。**落ちるものは `DROPS` の 1 行が唯一の根拠。実装済み** | ブロック。`blocks.ts` の `def` / 植わるかは `placing.ts` の `tryPlant()` |
| 122 | **小麦の種**。**実装済み** | アイテム。`items.ts` の `isSeed()` |
| 123 | **`WHEAT_CROP_RIPE`（実った小麦）**。非立方体（`model: "cross"`）で **`variantOf: WHEAT_CROP`**（苗と違って大元にできる相手が居ます）。だから**アイテムも名前も増えず**、落ちるものは `DROPS` の 1 行が唯一の根拠。**育ち具合はブロック ID でなく位置ごとの状態**（`crops.ts`）なので、段階を増やしても番号は増えません。**実装済み** | ブロック。`blocks.ts` の `def` / 実らせるのは `crops.ts` の `update()` |
| 124 | **小麦**。**実装済み** | アイテム。`items.ts` の `DROPS`（実った小麦から**小麦 1 + 種 1 の 2 山**。2 山目は `Drop.extra`） |
| 125 | **パン**。**小麦 3 個の横一列**（3 幅なので作業台が要ります）。**置けません**（`block: AIR`）。食べると**空腹 +5 / 満腹度 +6**（`FOODS` の 1 行）。**実装済み** | アイテム。`items.ts` の `FOODS` / レシピは `crafting.ts` の 1 行 |
| 126 | **生鶏肉**。鶏（`mobs.ts` の `CHICKEN`）を倒すと必ず 1 個。**置けず・道具でもなく・食べられる**（空腹 +2 / 満腹度 +1.2 / **毒なし**）。**実装済み** | アイテム。`items.ts` の `FOODS` / 落とすのは `MobDef.drop` |
| 127 | **焼き鳥**。かまどで生鶏肉を焼くと 1 個（空腹 +6 / 満腹度 +7.2）。**パン（5 / 6）より上・焼き豚（8 / 12.8）より下。実装済み** | アイテム。`items.ts` の `FOODS` / `smelting.ts` の `SMELTING` |
| 128 | **羽根**。鶏を倒すと生鶏肉と一緒に 1 個（`mobs.ts` の **`MobDrop.extra`** = **モブの 2 山目**。1 山目の当たり外れとは無関係に別に引きます）。**矢の材料**で、**置けず・道具でもなく・食べ物でもありません**。本家の 0〜2 個ではなく**1 個固定**（`TUNING.md`）。**実装済み** | アイテム。`items.ts` の `FEATHER` / 落とすのは `MobDef.drop.extra` / 矢は `crafting.ts` |
| 129 | **卵**。**鶏が 300〜600 秒ごとに足元に 1 個産みます**（`mobs.ts` の **`MobDef.laying`** = **倒さずに取れる 2 つ目**。羊毛と違ってプレイヤーの操作ではなく時計が起こします）。**置けず・道具でもなく・食べ物でもありません**。**積めるのは 16 個まで**（本家の値。エンダーパールと同じ）。**右クリックで投げられます**（`projectiles.ts` の `PROJECTILE_KINDS` に 1 行 = 5 種類目。速さ 20・寿命 30・**ダメージ 0**・ヒヨコは孵りません）。**実装済み** | アイテム。`items.ts` の `EGG` / 産むのは `mobs.ts` の `Mobs.lay()` |
| 130 | **生牛肉**。牛（`mobs.ts` の `COW`）を倒すと必ず 1 個（1 山目）。**置けず・道具でもなく・食べられる**（空腹 +3 / 満腹度 +1.8 / **毒なし**。生豚肉と同点）。**実装済み** | アイテム。`items.ts` の `FOODS` / 落とすのは `MobDef.drop` |
| 131 | **ステーキ**。かまどで生牛肉を焼くと 1 個（空腹 +8 / 満腹度 +12.8）。**焼き豚と同点**（本家がそうなので下げていません。`TUNING.md`）。**実装済み** | アイテム。`items.ts` の `FOODS` / `smelting.ts` の `SMELTING` |
| 132 | **革**。牛を倒すと生牛肉と一緒に 1 個（`mobs.ts` の **`MobDrop.extra`** = モブの 2 山目。羽根と同じ形で、本家の 0〜2 個ではなく**1 個固定**）。**置けず・道具でもなく・食べ物でもありません**。**使い道はまだ 1 つもありません**（防具も本も別件なので、`crafting.ts` にも `SMELTING` にも 0 行）。**実装済み** | アイテム。`items.ts` の `LEATHER` / 落とすのは `MobDef.drop.extra` |
| 133 | **糸**。クモ（`mobs.ts` の `SPIDER`）を倒すと必ず 1 個（1 山目。**2 山目は持ちません** —— 本家の 0〜2 個ではなく**1 個固定**）。**置けず・道具でもなく・食べ物でもありません**。**使い道は弓（棒 3 + 糸 3）1 本だけ**で、`SMELTING` には 0 行のまま（2026-09-04 に羊毛の代用を外しました）。**実装済み** | アイテム。`items.ts` の `STRING` / 落とすのは `MobDef.drop` / 弓は `crafting.ts` |
| 134 | **雪玉**。**雪ブロック（9）を掘ると 4 個**（`items.ts` の `DROPS` の 1 行。本家と同じ個数）。**雪玉 4 個を 2x2 で雪ブロック 1 個に戻せます** —— 落とし物を差し替えたので、**この戻すレシピが無いと雪が置けません**。**右クリックで投げられます**（`THROWN` の 1 行 = **投げるもの 2 つ目** / `PROJECTILE_KINDS` の 1 行 = **6 種類目**。飛び方は卵とまったく同じで、**ダメージ 0**）。**置けず・道具でもなく・食べ物でもありません**。**積めるのは 16 個まで**（卵と同じ）。**実装済み** | アイテム。`items.ts` の `SNOWBALL` / 落ちるのは `DROPS` の `SNOW` / 戻すのは `crafting.ts` |
| 135 / 136 / 137 | **鉄ブロック / 金ブロック / ダイヤブロック**。**インゴット・ダイヤ 9 個で 1 個、崩すと 9 個**（`crafting.ts` の 6 行。しまう側は 3x3 なので作業台が要り、戻す側は形なし）。**倉庫の枠を 9 分の 1 にするためだけ**の立方体で、**自然生成しません**。硬さ 5 / 3 / 5・`minTier` は石 / 鉄 / 鉄。**アイテムは `items.ts` に 1 行も書かずに付いてきます**（`variantOf` が `AIR` なので、ブロック → アイテムの for が作る。**`MAX_ITEM_ID` だけは伸ばすこと**）。**実装済み** | ブロック。`blocks.ts` の `def` 3 つ / `crafting.ts` の 6 行 |
| 138 | **ミルクバケツ**。**空のバケツ（84）を持って牛を右クリックすると手の中で入れ替わり、右クリックで飲むと毒が消えて空のバケツへ戻ります**（本家と同じで何度でも搾れ、待ち時間も位置ごとの状態もありません）。**空腹も満腹度も体力も 1 も動きません** —— だから `FOODS` に 1 行もなく、**満腹でも飲めます**。**`FILLED_BUCKETS` にも足しません**（足すと地面に「流せる」ようになります）。**積めるのは 1 個まで**（バケツ 84・水入り 85・溶岩入り 86 と同じ）。**実装済み** | アイテム。`items.ts` の `MILK_BUCKET` / 誰から搾れるかは `mobs.ts` の `MobDef.milkable` / 毒を消すのは `vitals.ts` の `drinkMilk()` / 振り分けは `use.ts` |
| 139 / 140 | **赤キノコ / 茶キノコ**。**草むら（32）とまったく同じ形**（`model: "cross"` / `CROSS_BOX` / `solid: false` / `replaceable: true` / 硬さ 0 / `sound: "grass"`）で、違うのは**色と生える場所**だけ。**森（0.015）と針葉樹林（0.01）の地表**にまれに立つ（`biomes.ts` の `BiomeDef.mushroom`。**`worldgen.ts` に「森なら」とは書かない**）。**赤か茶かは `worldgen.ts` の 2 本目のハッシュで半々**。**アイテムは `items.ts` に 1 行も書かずに付いてきます**（`variantOf` が `AIR` なので、ブロック → アイテムの for が作る。**`MAX_ITEM_ID` だけは伸ばすこと** —— いまはこの 140 が上限）。掘ると自分が 1 個（`DROPS` に 0 行）。**セーブは 1 バイトも増えません**。**実装済み** | ブロック。`blocks.ts` の `def` 2 つ / 生える所は `biomes.ts` + `worldgen.ts` |
| 141 / 142 | **ボウル / キノコシチュー**。**どちらもアイテムだけ**（ブロックは 1 つも増えていません）。ボウルは**板 3 個の V 字で 4 個**（3 幅なので作業台が要る。形はバケツと同じで材料違い）、シチューは**ボウル + 赤キノコ + 茶キノコ の形なし**（2x2 で作れる）。食べると**空腹 +6 / 満腹度 +7.2**（本家の値。**焼き鳥と同点**で焼き豚には届かない）で、**食べ切ると空のボウルが手の中に戻ります**（`items.ts` の `EMPTIES` の 1 行 → `emptyAfterEating()`。`main.ts` はそれを引く 3 行だけ）。**シチューが 1 枠 1 個なのは不変条件** —— 器を `setSelected` で戻すので、2 個以上積めると山ごと潰れます。**置けず・道具でもありません**。**セーブは 1 バイトも増えません。実装済み** | アイテム。`items.ts` の `BOWL` / `MUSHROOM_STEW` / `FOODS` / `EMPTIES` / レシピは `crafting.ts` の 2 行 |
| 143 / 144 | **サトウキビ（ブロック）/ 砂糖（アイテム）**。サトウキビは**キノコ（139 / 140）とまったく同じ形**（`model: "cross"` / `solid: false` / 硬さ 0 / `sound: "grass"` / `supportFace: FACE_YN`）で、違うのは**色・生える場所・箱の上端・積めること**の 4 つ。**浜（`BEACH`）の地表にだけ 0.12 で立つ**（`biomes.ts` の `BiomeDef.cane`。**`worldgen.ts` に「浜なら」とは書かない**）。**水際は見ません** —— 浜の地表は必ず y41 で水面は y40 なので、本家どおりの判定を書くと生成した場所そのものが置けない場所になります。**箱は `CANE_BOX`（上端 1）** —— `CROSS_BOX` の 0.8 のままだと積んだときに継ぎ目が空きます。**自分の上に自分を積めます**（`stacksOnSelf: true`。支えの判定は `canSupport()` の**外側**の `supportsBlock()` で、置く側と壊す側が同じそれを通します）。**`replaceable` は付けません** —— 付いていると `placeSpot()` が狙ったマス自身を返し、上面を狙っても 1 本目に重なって永久に積めません（18b で外しました）。**生成では 1〜3 段**（`CANE_HEIGHT_MAX`）。**時間で 1 マスずつ上へ伸びます**（18c・実装済み。`crops.ts` の `CANE_GROW_SECONDS` 180 秒で、**段の違いは ID ではなく積み方**。覚えるのは**列のいちばん下**で、**プレイヤーが置いた列だけ**が伸びます —— 自然に生えたものは印が無いので伸びません）。掘ると自分が 1 個（`DROPS` に 0 行）。砂糖は**サトウキビ 1 個の形なし**（2x2 で作れる）で、**置けず・道具でもなく・食べ物でもありません**。**セーブは 1 バイトも増えません。実装済み** | ブロック + アイテム。`blocks.ts` の `def` 1 つ / `items.ts` の `SUGAR` / 生える所は `biomes.ts` + `worldgen.ts` / レシピは `crafting.ts` の 1 行 |
| 145..148 | **はしご（`LADDER` / `LADDER_XN` / `LADDER_ZP` / `LADDER_ZN`）**。**壁掛けの松明（`WALL_TORCH_*`）とまったく同じ形**で、違うのは**見た目（`model: "boxes"` の厚さ 3/16 の板）と、床にも天井にも付かない**ところだけ。145 が大元で**アイテム 145 もこれ**（`variantOf` を書かないので、ブロック → アイテムの for が作ります。**`MAX_ITEM_ID` だけは手作業で伸ばしました**）、146..148 は `variantOf: LADDER` なので**アイテムを持たず、掘ると 145 が 1 個**（`DROPS` に 0 行）。硬さ 0.4 / 斧 / `sound: "wood"` / `solid: false`（**通り抜けられます**）。**`replaceable` も `stacksOnSelf` も付けません**。向きを決めるのは `blocks.ts` の **`LADDER_BY_SUPPORT` と `ladderVariant()`**（**松明の表とは別の表**）で、置く経路は今までどおり `placing.ts` の `tryPlace()` → `placedVariant()`、壁を壊すと `World.breakUnsupported()` が落とします（**`main.ts` は 0 行**）。**まだ登れません**（掴まる物理は 19b）。**レシピは棒 7 本で 3 個**（本家と同じ。3x3 なので作業台が要る）。**セーブは 1 バイトも増えません。実装済み** | ブロック。`blocks.ts` の `def` 4 つ / `crafting.ts` の 1 行 / `placing.ts` の 1 行（`supportHint()`） |
| 149 | **リンゴ（アイテムのみ）**。**オークの葉を壊すと 0.5% で 1 個**落ちる（`items.ts` の `DROPS` の `extra`。針葉樹の葉には付けません —— 本家がオークとダークオークだけなので）。**棒の 10% とは別々に当たります** —— 2 山目の抽選は `roll` ではなく **`extraRoll`**（この周で足した 2 本目の乱数。`BreakOrder` と `autoBreak()` と `main.ts` の 2 か所を通ります）。食べると**空腹 +4 / 満腹度 +2.4**（本家の値。パン 5 / 6 より下）。**置けず・道具でもなく、レシピも 0 本**（金のリンゴは別件）。**ブロックは 1 つも増えていません。セーブは 1 バイトも増えません。実装済み** | アイテム。`items.ts` の `APPLE` / `FOODS` の 1 行 / `DROPS` の葉 1 行 / `ExtraDrop` の型 |
| 150 / 151 / 152 | **紙・本・本棚**。サトウキビ 3 → 紙 3（`["CCC"]`。3 幅なので作業台が要る）/ 紙 3 + 革 1 → 本 1（形なし。2x2 で作れる）/ 板 6 + 本 3 → 本棚 1（`["PPP","BBB","PPP"]`）。**本棚を壊すと本 3 個だけ**で、**板 6 個は戻りません**（本家どおり。「戻す」レシピも足していません）。本棚は**普通の立方体**（硬さ 1.5・斧・素手でも壊せる・木の音）で、**自然生成しません**。**エンチャントは持ち込んでいません** —— いまのところ「置ける立方体と本 3 個の入れ物」でしかありません。**`main.ts` は 0 行・`SaveData` は 1 バイトも増えません。実装済み** | アイテム 2 つ + ブロック 1 つ。`items.ts` の `PAPER` / `BOOK` / `MAX_ITEM_ID` / `DROPS` 1 行 / `blocks.ts` の `def` 1 つ / `crafting.ts` の 3 行 |
| 153 | **金のリンゴ（アイテムのみ）**。**金インゴット 8 個でリンゴ 1 個を囲む 3x3**（本家と同じ形・同じ個数。作業台が要る）。食べると**空腹 +4 / 満腹度 +9.6 に加えて体力が 4 戻り、満腹でも食べられる**（本家の「再生 II が 5 秒」は、持続する効果の器が無いので**即時 4 回復に均して**あります。`TUNING.md`）。**`FOODS` でこの 2 つ（`heal` / `alwaysEdible`）を持つ唯一の行**で、既存の 10 行は 1 行も書き換えていません。判断は **`vitals.ts` の `canEatFood()` 1 か所**（`use.ts` にも `main.ts` にも `alwaysEdible` を読む行はありません）。**`main.ts` は ±0 行**（`vitals.canEat` の 2 か所を `vitals.canEatFood(...)` に書き換えただけ）。**ブロックは 1 つも増えず、`SaveData` は 1 バイトも増えません。エンチャントの金のリンゴは持ち込んでいません。実装済み** | アイテム。`items.ts` の `GOLDEN_APPLE` / `FOODS` の 1 行 / `MAX_ITEM_ID` / `vitals.ts` の `canEatFood()` と `eat()` の 1 行 / レシピは `crafting.ts` の 1 行 |
| 154 | **クモの巣**。**キノコ（139 / 140）と同じ十字**（`model: "cross"` / `CROSS_BOX` / `solid: false` / 硬さ 1.2 / `sound: "wool"`）で、違うのは**色・硬さ・旗 2 つ・支えが要らない**ところ（`supportFace` は `NO_SUPPORT` なので**宙に浮きます**。`replaceable` は**付けません**）。**旗は 2 つに割ってあります**: **`sticky`（`isSticky()`）= 中に居ると鈍る**（横 0.25 倍 / 上下 1.0 m/s 止まり。数値は `player.ts` の 2 定数）と、**`bladed`（`isBladed()`）= 刃物でだけ落ちる**（**何が刃物かは `items.ts` の `isBlade()` = 剣 or シアーズ**、効くのは `mining.ts` の `canHarvest()` の 1 行）。**`tool: "sword"` とは書きません** —— 書くと剣が採掘道具になって速く掘れます（`rules/items-survival.md`）。**絡まっているあいだは落下ダメージが積まれません**（`player.clinging` = はしご or 巣。**`main.ts` は ±0 行**で、`updateVitals()` に渡す 1 行の名前を `onLadder:` → `clinging:` に書き換えただけ）。壊すと**糸が 1 個**。**アイテムは `items.ts` に 1 行も書かずに付いてきます**（`variantOf` が `AIR`。**`MAX_ITEM_ID` だけは手作業で伸ばしました** —— いまはこの 154 が上限）。**自然生成もレシピも 0 行**（本家にレシピは無く、廃坑・要塞に湧かせるのは別の周）。**`SaveData` は 1 バイトも増えません。実装済み** | ブロック。`blocks.ts` の `def` 1 つ + 旗 2 つ / `items.ts` の `MAX_ITEM_ID` と `DROPS` 1 行と `isBlade()` / `mining.ts` の 1 行 / `player.ts` の `inCobweb` と `clinging` / `vitals.ts` の `clinging` へ改名 |
| 155 | **ケーキ（24a・置けるところまで）**。**ミルクバケツ 3 + 砂糖 2 + 卵 1 + 小麦 3 の 3x3**（本家と同じ形・並び。作業台が要る）。**ミルクバケツ 3 個は空のバケツになって盤面に残ります**（＝鉄 9 個は消えない）—— 表は **`items.ts` の `LEFTOVERS` / `leftoverOf()`** の 1 本で、`crafting.ts` の `consumeGrid()` は**聞くだけ**（`Recipe` にキーは足していません。残りかすはアイテムの性質）。**`EMPTIES`（食べ切ると戻る器）とは別の表**です。形は **`model: "boxes"` の `CAKE_BOX`**（本家と同じ縁 1/16・高さ 8/16）で、**`solid: true`（歩いて乗れる。0.5 < `STEP_HEIGHT` 0.6）/ `supportFace: FACE_YN`（床が要る）/ 硬さ 0.5 / `sound: "wool"`**。**`replaceable` も `stacksOnSelf` も `variantOf` も付けません。壊すと何も落ちません**（`DROPS` に `NO_ITEM` の 1 行。素手でもツルハシでも剣でも 0 個）。**色の上面は薄紅 `0xffd0e4`** —— 素直なクリーム色（`0xf2ded2`）は羊毛から 15.4 しか離れず判定に落ちるので、生クリーム + 赤い実の平均へ寄せてあります（31.5。`TUNING.md`）。**アイテムは `items.ts` に 1 行も書かずに付いてきます**（`variantOf` が `AIR`。**`MAX_ITEM_ID` だけは手作業で伸ばしました** —— いまはこの 155 が上限）。**まだかじれません**（`FOODS` に 0 行。かじる 7 回は **24b**）。**自然生成 0 行・`main.ts` は ±0 行・`SaveData` は 1 バイトも増えません。実装済み** | ブロック。`blocks.ts` の `CAKE_BOX` と `def` 1 つ / `items.ts` の `MAX_ITEM_ID` と `DROPS` 1 行と `LEFTOVERS` / `crafting.ts` のレシピ 1 本と `consumeGrid()` / `tools/shot.ts` の場面 1 つ |
| 156 | **氷（25a・ブロックと滑りだけ）**。**普通の立方体**（`opaque: false` / `translucent: true` / `alpha: 0.6` / 硬さ 0.5 / `tool: "pickaxe"`（**`minTier` は書かない** = 素手でも落ちる…が落ちるものが無い）/ `sound: "glass"`）。**`blocksSky` は書きません** —— 書くと 25b で氷の下の海が真っ暗になります。**`solid` も `replaceable` も `variantOf` も `supportFace` も付けません**。**旗は 2 つに割ってあります**: **`slippery`（`isSlippery()`）= 上に立つと滑る**（摩擦 12 → **2.3**・地上の加速 60 → **13.8**（0.23 倍）。数値は `player.ts` の 3 定数で、**`GROUND_FRICTION` は前の 12 のまま名前を付けただけ**）と、**`breaksInto`（`remainsAfterBreak()`）= 壊したあとにマスへ残るブロック**（既定は `AIR`・**氷だけ `WATER`**）。**`sticky` と 1 つにまとめません** —— 氷は滑らせるだけ・クモの巣は鈍らせるだけ（`blocks.ts` のコメントが名指しで断っています）。**足元のマスを見るのは `physics.ts` の `bodyStandsOn()`**（`bodyTouches()` の足元版。氷は `solid` なので体と重ならず、体の箱で探すと永久に偽）。壊すと**何も落ちません**（`DROPS` に `NO_ITEM` の 1 行。素手でもツルハシでも剣でも 0 個。本家のシルクタッチはエンチャントごとありません）。**色は `0x8fc4f2`** —— いちばん近いガラス（`0xa9d8e8`）から 34.3（`TUNING.md`）。**アイテムは `items.ts` に 1 行も書かずに付いてきます**（`variantOf` が `AIR`。**`MAX_ITEM_ID` だけは手作業で伸ばしました** —— いまはこの 156 が上限）。**自然生成もレシピも 0 行**（凍った海は **25b**）。**`main.ts` は ±0 行・`SaveData` は 1 バイトも増えません。実装済み** | ブロック。`blocks.ts` の `def` 1 つ + 旗 2 つ / `items.ts` の `MAX_ITEM_ID` と `DROPS` 1 行 / `breaking.ts` の `setVoxel` 1 行 / `physics.ts` の `bodyStandsOn()` / `player.ts` の `onSlippery` と 3 定数 / `tools/shot.ts` の場面 1 つ |
| 157 | **フェンス（26a・置けて跳び越えられないところまで）**。**棒 6 本で 2 個**（`["SSS","SSS"]`。3 幅なので作業台が要る）。**`BlockDef.collision` を持つ唯一のブロック**で、**見た目と狙いの形（`FENCE_BOXES`・柱 1 + 腕 4 方向 x 2 段 = 9 個・上端 1.0）と当たり判定（`FENCE_COLLISION_BOX` = `[[0,0,0,1,1.5,1]]`）が違います** ——これは「形は 3 つの用途を兼ねる」の**唯一の例外**です（`rules/blocks-shapes.md`）。**`def()` の既定は `boxes` そのもの**なので、書かないブロックは今までどおり 3 つが同じ形。**1 マスより高い箱が下から届くように、`physics.ts` の `collides()` が 1 段下も見ます**（**その 1 層だけ `isTallCollision()` が偽のマスを飛ばす**。表は `collision` の最大 y > 1 から立てるので、手で旗は書きません）。**跳んでも越えられるかは刻み次第**: 到達は `9.2²/(2×30) + 9.2×dt/2` で、**60fps は 1.4883 m（余裕 0.0117）で越えられませんが、およそ 52fps を割ると越えます**（`main.ts` の刻みは 0.05 で頭打ち。`TUNING.md`）。**腕は 4 方向とも常に描きます** —— 隣に何も無い側を消すのは **26b**（`mesher.ts` は 1 行も触っていません）。硬さ 2・斧・木の音・`opaque: false`・**`blocksSky` も `supportFace` も `replaceable` も `stacksOnSelf` も `variantOf` も旗も書きません**（宙に浮きます）。壊すと**自分が 1 個**（`DROPS` は 0 行 —— 既定の `baseBlock()`）。**色は `0x988a5e`** —— 板（`0xb18a56`）は一覧で板から 7.1 しか離れず判定（20）に落ちるので、灰緑へ寄せて 26.2（`TUNING.md`）。**アイテムは `items.ts` に 1 行も書かずに付いてきます**（`variantOf` が `AIR`。**`MAX_ITEM_ID` だけは手作業で伸ばしました** —— いまはこの 157 が上限）。**自然生成 0 行・`main.ts` は ±0 行・`SaveData` は 1 バイトも増えません。実装済み** | ブロック。`blocks.ts` の `FENCE_BOXES` / `FENCE_COLLISION_BOX` / `BlockDef.collision` / `def()` / `isTallCollision()` / `def` 1 つ / `physics.ts` の `collides()` / `items.ts` の `MAX_ITEM_ID` / `crafting.ts` のレシピ 1 本 / `tools/shot.ts` の場面 1 つ |
| 158..161 | **革の防具 4 部位（アイテムのみ）**。**帽子 158 / 上着 159 / ズボン 160 / 靴 161** で、**点数は本家のまま 1 / 3 / 2 / 1 = 合計 7**（表は `items.ts` の `ARMORS` 1 本。`FOODS` と同じ作法で、`inventory.ts` にも `vitals.ts` にもアイテムの名前は書きません）。レシピも本家の形・枚数（革 **5 / 8 / 7 / 4**・**4 本とも 3 幅なので作業台が要る**）。**着た物はセーブに残ります** —— **`SaveData.armor` の 8 要素・省略可**で、**`inventory` の 72 要素には継ぎ足していません**（`wear` を分けたのと同じ理由。裸なら**キーごと消える**ので、着ていない人のセーブは 1 バイトも変わりません）。読む順は `deserialize()`（36 枠）→ `deserializeWear()` → `deserializeArmor()` で、**`deserializeArmor()` は防具枠 4 つだけを空にします**（`clear()` を呼ぶと 36 枠が消えます）。**どのキーを載せるかは `session.ts` の `buildSave()`** —— `main.ts` は器そのものを `inventory,` の 1 行で渡すだけなので、**この周の `main.ts` は −1 行**（1449 → 1448）。**2026-09-11 の 27b-2 で着られるようになりました**（防具枠 4 つが画面に出て、入れてよい部位の規則は `craftscreen.ts` の `accepts()` 1 か所。点は `main.ts` の `vitals.armor = inventory.armorPoints;` の 1 行）。**傷は持たせていません・鉄／金／ダイヤは番号を取っていません・`SaveData.version` は 1 のまま。実装済み** | アイテム 4 つ。`items.ts` の ID 4 つと `item()` 4 行と `ARMORS` 4 行と `MAX_ITEM_ID` / `crafting.ts` のレシピ 4 本 / `inventory.ts` の `serializeArmor()` と `deserializeArmor()` / `storage.ts` の `armor?` / `session.ts` の `SaveParts.inventory` と `applyRestore()` の 1 行 / `main.ts` は −1 行 |
| 162 | **骨（アイテムのみ）**。**スケルトン（`mobs.ts` の `SKELETON`）を倒すと必ず 1 個**落ちます（`MobDef.drop` の 1 山目。**2 山目は矢 1 本が半分の確率**で、`MobDrop.extra.chance` を持つ初めての行です）。**スケルトンそのものは番号を使いません**（`MobKind` は文字列）—— 足したのは **`mobs.ts` の表 1 件**だけで、**`main.ts` も `mobrender.ts` も `mobmesh.ts` も `projectiles.ts` も ±0 行**（`ranged` の仕掛けはブレイズの火球で全部通っています）。**`ranged.range` は 10** —— 矢は重力を受けるので、**16m だと 2.70m 落ちてプレイヤーの下を抜けます**（実測。`TUNING.md` と `rules/mobs.md`）。**置けず・道具でもなく・食べ物でもなく**（`block: AIR` / `tool: null`）、**レシピも精錬も 0 行**（骨粉は別の周）。**色は `0xcdc8b0`** —— いちばん近い砂から 24.6（素直な `0xd8cfae` は砂から 20.9 しかありません）。**`MAX_ITEM_ID` はここへ伸ばしました**（上限がアイテム側なのは 2 度続けて）。**`SaveData` は 1 バイトも増えません**（モブは保存しない）。**実装済み** | アイテム 1 つ。`items.ts` の ID 1 つと `item()` 1 行と `MAX_ITEM_ID` / `mobs.ts` の `MobKind` 1 語と `SKELETON` 1 件と `MOBS` / `MOB_KINDS` |
| 163 | **木炭（アイテムのみ）**。**原木・トウヒの原木をかまどで焼くと 1 個**（`smelting.ts` の `SMELTING` に 2 行）。**原木は「焼けるもの」と「燃料」の両方に居る初めての行**で、**`FUEL` の 15 秒は書き換えていません**（石炭が 1 つも無くても木だけで火を回せる入口をそのまま残すため）。**燃える長さは石炭とまったく同じ 80 秒 = 8 個ぶん**（本家と同じ。違いは手に入る道だけで、`Math.max(...FUEL.values())` は 80 のままなので「石炭がいちばん長持ちする」は緑のまま）。**松明も作れます** —— `crafting.ts` に**材料違いのレシピをもう 1 本**（板が原木とトウヒの原木で 2 行あるのと同じ形。`Recipe` に列は足していません）。**石炭（65）とは別の番号**なので同じ枠には積めません（本家と同じ）。**置けず・道具でもなく・食べ物でもありません**（`block: AIR` / `tool: null`）。**色は `0x56473f`** —— 炭の暗い暖色は一覧でいちばん混んでいる帯（火打石・ソウルサンド・ネザーレンガ・岩盤・石炭）で、素直な `0x403a36` は**火打石から 5.8** しかありません。この値でいちばん近いのは**石炭鉱石とソウルサンドで 24.1**、**石炭からは 64.0**（`TUNING.md`）。**`MAX_ITEM_ID` はここへ伸ばしました**（上限がアイテム側なのは 3 度続けて）。**`main.ts` は ±0 行・ブロックは 1 つも増えず・`SaveData` は 1 バイトも増えません。実装済み** | アイテム 1 つ。`items.ts` の ID 1 つと `item()` 1 行と `MAX_ITEM_ID` / `smelting.ts` の `SMELTING` 2 行と `FUEL` 1 行 / `crafting.ts` のレシピ 1 本 |
| 164..165 | **苗木 2 種（オーク 164 / トウヒ 165・30a・落ちて植わるところまで）**。**オークの葉から 5%・トウヒの葉から 5%** で落ちます（`items.ts` の `DROPS`）。**オークの葉は 1 山目（棒 10%）と 2 山目（リンゴ 0.5%）が埋まっていた**ので、**乱数を 3 本目に増やさず `extraRoll` を帯で切って**載せました（リンゴ 0..0.005 → 苗木 0.005..0.055。`Drop.extra` が候補 2 件を取れるようになり、`rollDrops()` が上から順に 1 件だけ選びます）。**だからリンゴと苗木は同時に落ちません**（本家は独立。`TUNING.md`）—— **`breaking.ts` も `main.ts` も ±0 行**。**形は小麦の苗（121）の写し**（`model: "cross"` / `CROSS_BOX` / `solid: false` / 硬さ 0 / `sound: "grass"` / `supportFace: FACE_YN`）で、違うのは**色**と**`variantOf` を書かないこと**（書くとアイテムが作られず掘っても戻らない）と **`needsSoil: true`** の 3 つ。**土・草・耕地の上にだけ立ちます** —— 表 1 本（`needsSoil()` / `isSoil()`）と `supportsBlock()` の 1 行だけで、**`canSupport()` はゆるめていません**（あれは壁掛けの松明とベッドの足場）。`supportsBlock()` は置く側（`canPlaceAt`）と壊す側（`breakUnsupported`）の両方が通るので、**真下の土を掘れば苗木も落ちます**。置けない理由の文も `supportHint()` が表から出します（**「土か草の上」**。「床か壁」のままだと嘘）。**2026-09-14 の 30b で育つようになりました**（`crops.ts` の `growTree()` が 180 秒で木にします。形は `src/treeshape.ts` の `treeCells()` 1 本で、**生成側の `stampTree()` と同じそれを引きます** —— **ID は 1 つも使っていません**）。**色は `0x7fbf5f`（オーク）と `0x2f7f5a`（トウヒ）** —— 緑は一覧でいちばん混んでいる帯で、いちばん近いのは草から 35.0 と葉から 36.1、**2 種どうしは 102.6**（`TUNING.md`）。**アイテムは `items.ts` に 1 行も書かずに付いてきます**（`variantOf` が `AIR`。**`MAX_ITEM_ID` だけは手作業で伸ばしました** —— いまは 165 が上限）。**自然生成 0 行・`main.ts` は ±0 行・`SaveData` は 1 バイトも増えません。実装済み** | ブロック 2 つ。`blocks.ts` の ID 2 つと `def()` 2 つと `BlockDef.needsSoil` / `soil` と `NEEDS_SOIL` / `SOIL` の表と `needsSoil()` / `isSoil()` / `supportsBlock()` の 1 行 / `supportHint()` の 1 行 / `items.ts` の `MAX_ITEM_ID` と `Drop.extra` の帯と `extraDrops()` と `DROPS` の葉 2 行 / `tools/shot.ts` の場面 1 つ |
| 168 / 169 | **粘土（ブロック 168）と粘土玉（アイテム 169）**（32a）。**海と凍った海の海底にまだらに湧きます** —— **`VEINS` ではなく `biomes.ts` の `BiomeDef.floorPatch`**（`{ block, chance, fill, shift, depth }`。あの表が効くのは `depth > 3`（石の中）なので、**海底の砂の下には 1 マスも出ず、掘り当てられない粘土になります**）。`worldgen.ts` が持つのは**塩 2 本（`0x3d15` / `0x6e83`）と差し替えの 3 行だけ**で、**`CLAY` を import しません**（`seaSurface` に `ICE` を書かないのと同じ決まり）。**11 バイオーム全部に `floorPatch` を書き、粘土を持つのは海と凍った海だけ**（残る 9 つは `null`。**`?:` にしないこと**）。数値は `chance` 0.06 / `fill` 0.8 / `shift` 2（4x4 の塊）/ `depth` 3（`TUNING.md`）で、**実測は海底の列の 7.2%（凍った海は 4.4%）・4x4 の枡あたり 12 列**。粘土は**普通の立方体**（硬さ 0.6 / シャベル / `sound: "sand"` / **`falls` は付けません** —— 本家の粘土は落ちません）。**掘ると粘土玉 4 個**（`items.ts` の `DROPS` の 1 行）なので**そのままでは手に入らず**、**粘土玉 4 個の 2x2 で戻します**（雪玉 → 雪ブロックとまったく同じ対。作業台は要りません）。粘土玉は**置けず・道具でもなく・食べ物でもありません**（`block: AIR` / `tool: null`）。**色は `0x9da3b5`（粘土）と `0xa7b4da`（粘土玉）** —— 灰青は一覧でとても混んでいる帯で、仕様書の見当（`0xa4aab9` / `0xb0b8cc`）は**バケツから 15.7 / 糸から 9.8** しかありませんでした。この値でいちばん近いのは**シアーズ 26.1 / 糸 26.0**、**2 つどうしは 41.9**（`TUNING.md`）。**`MAX_ITEM_ID` は 169 へ伸ばしました**（上限がアイテム側に戻りました）。**`smelting.ts` は ±0 行**（焼いてレンガは **32b**）。**`main.ts` は ±0 行・`SaveData` は 1 バイトも増えません。実装済み** | ブロック 1 つ + アイテム 1 つ。`blocks.ts` の ID 1 つと `def()` 1 つ / `biomes.ts` の `FloorPatch` と `BiomeDef.floorPatch` 11 行と `OCEAN_CLAY` / `worldgen.ts` の 2 本のハッシュと 3 行 / `items.ts` の ID 1 つと `item()` 1 行と `DROPS` 1 行と `MAX_ITEM_ID` / `crafting.ts` のレシピ 1 本 / `tools/shot.ts` の場面 `clay` と `sourceOf()` |
| 170 | **レンガ（アイテム 170）**（32b）。**粘土玉（169）をかまどで焼くと 1 個**（`smelting.ts` の `SMELTING` に 1 行。**木炭とまったく同じ形**）で、**レンガ 4 個の 2x2 で既にある `BRICK`(12) に組めます**（`crafting.ts` に 1 行。2x2 なので**作業台が要りません** —— 雪玉 → 雪ブロック・粘土玉 → 粘土と同じ形）。**これで、置けるのに作れなかった `BRICK`(12) がサバイバルで初めて手に入ります**（粘土 1 個 → 粘土玉 4 → レンガ 4 → レンガブロック 1 個。`test/crafting.test.ts` が 1 本の道として見張っています）。**定数名は `BRICK_ITEM`** —— `BRICK` にすると `blocks.ts` の 12 と衝突して `crafting.ts` が両方 import した瞬間に `typecheck` が落ちます（型で止まる安全な罠）。**表示名は「ブロック = レンガブロック / アイテム = レンガ」**（本家の日本語と同じ）で、直したのは `blocks.ts` の `def()` の文字列 1 つだけ。**`BRICK` の ID は 12 のまま**なので、セーブは 1 バイトも動きません。レンガは**置けず・道具でもなく・食べ物でもありません**（`block: AIR` / `tool: null`）。**`FUEL` にも `BRICK` の `DROPS` にも 1 行も足していません**（どちらも燃料ではなく、レンガブロックは今までどおり自分が 1 個落ちます）。**色は `0xbc5d39`** —— 赤茶は一覧でとても混んでいる帯（革・ステーキ・作業台・棒・腐った肉・革のズボン）で、仕様書が退けた素直な `0x9c5a3c` は **`BRICK`(12) の `0xa4553f` と 9.9** しかありませんでした。この値でいちばん近いのは**生牛肉 26.0**、**`BRICK`(12) とも 26.0**（`TUNING.md`）。**`MAX_ITEM_ID` は 170 へ伸ばしました**（上限はアイテム側のまま）。**`main.ts` は ±0 行・`SaveData` は 1 バイトも増えません。実装済み** | アイテム 1 つ（ブロックは 1 つも増えていません）。`items.ts` の ID 1 つと `item()` 1 行と `MAX_ITEM_ID` / `smelting.ts` の `SMELTING` 1 行 / `crafting.ts` のレシピ 1 本 / `blocks.ts` の `def()` の表示名 1 つ |
| 171..174 | **鉄の防具 4 部位（アイテムのみ）**（33a）。**帽子 171 / 上着 172 / ズボン 173 / 靴 174** で、**点数は本家のまま 2 / 6 / 5 / 2 = 合計 15**（`items.ts` の `ARMORS` に 4 行）。`armorReduced()` に通すと **15 / 25 = 60% 減**で、**`ARMOR_CAP`(20) にも `ARMOR_DENOM`(25) にも触っていません**（革一式 7 点 = 28% 減はそのまま）。**`inventory.ts` も `vitals.ts` も `main.ts` も `durability.ts` も ±0 行** —— `armorPoints` は `armorOf()` に聞くだけで**材質を 1 つも知らない**ので、鉄でもそのまま効きます。**レシピは `crafting.ts` の `armorRecipes()` 1 本**（`toolRecipes()` と同じ作法。**革 4 本をベタ書きからここへ移しました** —— 名前も形も枚数も出目も 1 つも変わっていません）。**形も枚数も本家のまま**（帽子 5 / 上着 8 / ズボン 7 / 靴 4 枚・**4 本とも 3 幅なので作業台が要る**）で、**鉄一式 24 枚**は鉄鉱石 → かまど → 鉄インゴットで届きます（`test/crafting.test.ts` が 1 本の道として見張っています）。**防具に耐久値はまだありません**（革と同じ。足すならそれだけで 1 周）。**色は「鋼の青」`0xcfcfff` / `0x92a7ce` / `0x788ebe` / `0x506595`** —— **素直な銀灰はどう選んでも通りません**（一覧の無彩色は既存 46 個で埋まっていて、明るさのどの帯でもいちばん遠い無彩色が 20.3 / 20.1 / 22.1 / 21.4。判定は 20）。この 4 段でいちばん近いのは**紙 26.0 / 粘土玉 27.5 / 粘土 43.5 / 丸石 47.8**、**互いは最小 39.5・革 4 部位とは最小 118.8**（`TUNING.md`。**この周に測り直して同じ値**）。**`MAX_ITEM_ID` は 174 へ伸ばしました**（上限はアイテム側のまま）。**ブロックは 1 つも増えず・`SaveData` は 1 バイトも増えません。実装済み** | アイテム 4 つ。`items.ts` の ID 4 つと `item()` 4 行と `ARMORS` 4 行と `MAX_ITEM_ID` / `crafting.ts` の `armorRecipes()` 1 本と呼び出し 2 行 |
| 175..182 | **金・ダイヤの防具 8 部位（アイテムのみ）**（33b）。**金 175..178 / ダイヤ 179..182**（どちらも 帽子 → 上着 → ズボン → 靴）で、**点数は本家のまま 金 2 / 5 / 3 / 1 = 合計 11・ダイヤ 3 / 8 / 6 / 3 = 合計 20**（`items.ts` の `ARMORS` に 8 行）。`armorReduced()` に通すと **金 11 / 25 = 44% 減・ダイヤ 20 / 25 = 80% 減**で、**革 7 点 = 28% 減も鉄 15 点 = 60% 減も 1 つも動いていません**。**⚠ ダイヤ一式 20 点は `ARMOR_CAP`(20) にちょうど当たります** —— 「早く頭打ちになった」のではなく**上限に届く一式が初めて入った**だけなので、**`ARMOR_CAP` も `ARMOR_DENOM` も触っていません**（本家の残り 2 割はエンチャントが削るぶん。`rules/vitals.md`）。**⚠ 金（11 点）は鉄（15 点）より弱いのに要る枚数は同じ 24 枚**ですが、これも本家のままです（`TUNING.md`）。**`crafting.ts` は `armorRecipes()` の呼び出し 2 行だけ**で、**関数の中は 1 文字も変わっていません**（33a が畳んだとおり。形も枚数も本家のまま 5 / 8 / 7 / 4 枚・**8 本とも 3 幅なので作業台が要る**）。**⚠ ダイヤは鉄・金と道が違います** —— **`SMELTING` に 1 行も無く、ダイヤ鉱石を掘ると `DROPS` でダイヤ 1 個**です（`smeltResultOf(DIAMOND_ORE)` は null。金は金鉱石 → かまど → 金インゴット）。**`inventory.ts` も `vitals.ts` も `main.ts` も `durability.ts` も ±0 行** —— `armorPoints` は `armorOf()` に聞くだけで材質を知らないので、材質が 4 つになってもそのまま効きます（革 → 鉄 → 金・ダイヤで 3 度そのとおり）。**防具に耐久値はまだありません**（人の判断待ち）。**色は金 `0xfee34d` / `0xefd714` / `0xf8b317` / `0xc2aa35`・ダイヤ `0xd1fefe` / `0xa4f8f5` / `0x92e6e3` / `0x80d4d1`** —— この周に測り直して**金 26.3（金インゴット）/ 38.7（ブレイズロッド）/ 26.7（金のリンゴ）/ 32.7（金鉱石）**・**ダイヤ 28.4（ミルクバケツ）/ 34.9（ガラス）/ 27.4（ガラス）/ 39.6（氷）**、**16 部位互いは最小 31.2**（判定は 20。`TUNING.md`）。**`MAX_ITEM_ID` は 182 へ伸ばしました**（上限はアイテム側のまま）。**ブロックは 1 つも増えず・`SaveData` は 1 バイトも増えません。実装済み** | アイテム 8 つ。`items.ts` の ID 8 つと `item()` 8 行と `ARMORS` 8 行と `MAX_ITEM_ID` / `crafting.ts` の `armorRecipes()` の呼び出し 2 行 |
| **183..255** | **予備 73 個。ブロックもアイテムもここから** | 立方体でも向き違いでもアイテムでも、**次の空き番号を 1 つ取る** |

**立方体が 63 までだったのは `encodeFace` が id に 6 ビットしか割いていなかったから**で、
いまは 8 ビットあります（`mesher.ts`）。**天井は 255**（ボクセルが `Uint8Array`）。

**111 以降はブロックとアイテムで 1 本の番号列です。** 片側だけ見て空き番号を取ると、
「ブロック側では空きなのにアイテム側では埋まっている」番号を掴み、置いて壊すと別のものが
手に入ります（`test/blocks.test.ts` が突き合わせています）。**1 つの番号は 1 つのものだけ。**

アイテム ID は **139 から**（94 = 矢、114 = ダイヤの剣、115 = シアーズ、**116 = 耕地（ブロック）、
120 = ダイヤのクワ、121 = 小麦の苗（ブロック）、122 = 小麦の種、123 = 実った小麦（ブロック）、
124 = 小麦、125 = パン、126 = 生鶏肉、127 = 焼き鳥、128 = 羽根、129 = 卵、130 = 生牛肉、
131 = ステーキ、132 = 革、133 = 糸、134 = 雪玉、135 = 鉄ブロック（ブロック）、
136 = 金ブロック（ブロック）、137 = ダイヤブロック（ブロック）、138 = ミルクバケツ、
139 = 赤キノコ（ブロック）、140 = 茶キノコ（ブロック）、141 = ボウル、
142 = キノコシチュー、143 = サトウキビ（ブロック）、144 = 砂糖、
145 = はしご（ブロック。146..148 は向き違いなのでアイテムを持ちません）、
149 = リンゴ、150 = 紙、151 = 本、152 = 本棚（ブロック）、153 = 金のリンゴ、
154 = クモの巣（ブロック）、155 = ケーキ（ブロック）、156 = 氷（ブロック）、
157 = フェンス（ブロック）、158..161 = 革の防具 4 部位（帽子・上着・ズボン・靴）、
162 = 骨、163 = 木炭、164..165 = 苗木 2 種（ブロック）、
**166..167 = 上付きハーフ 2 つ**（ネザーレンガ / 石レンガ。**ブロックだけで、
`variantOf` があるのでアイテムは持ちません**）、168 = 粘土（ブロック）、169 = 粘土玉、
170 = レンガ、**171..174 = 鉄の防具 4 部位**（帽子・上着・ズボン・靴）、
**175..178 = 金の防具 4 部位**、**179..182 = ダイヤの防具 4 部位**
**まで使用済み。**次に取るのは 183**）。
**95..110 は空けたままにすること** ——
ブロック側の向き違いが使っている番号で、ここにアイテムを置くと番号が二重になります。
**番号は取る順に上から詰めること**（振り直しはブロックと同じく禁止）:

| アイテム | 何のため |
| --- | --- |
| **84 / 85 / 86** バケツ / 水入りバケツ / 溶岩入りバケツ | **黒曜石の入手導線**（鉄インゴット 3 個）。**実装済み** |
| **87 / 88** 火打石 / 火打石と打ち金 | ポータルの点火（砂利から 10% で出る）。**実装済み** |
| **89** ブレイズロッド | ブレイズが 50% で落とす。**実装済み**（`mobs.ts` の `BLAZE`） |
| **90** エンダーパール | エンダーマンが 50% で落とす。**積めるのは 16 個まで**（本家と同じ）。**実装済み**（`mobs.ts` の `ENDERMAN`） |
| **91** ブレイズパウダー | エンダーアイの材料（ロッド 1 本 → 2 個）。**実装済み**（`crafting.ts`） |
| **92** エンダーアイ | 要塞探し（2-7）とエンドポータルの起動（2-9）。**積めるのは 64 個まで**（枠 12 個が 1 枠に収まる）。**実装済み** |
| **93 / 94** 弓 / 矢 | エンドクリスタルを離れた所から壊す手段。**実装済み**（材料は `crafting.ts`） |
| **111 / 112 / 113 / 114** 木 / 石 / 鉄 / ダイヤの剣 | **共有帯 111 以降の最初の使用者**（2026-09-02）。殴るための道具で、掘る速さは素手と同じ。**実装済み**（`items.ts` の剣のループ / 攻撃力は `mobs.ts` の `TOOL_ATTACK` / 減り方は `durability.ts` の `wearForAttack()`） |
| **115** シアーズ | **羊を倒さずに羊毛を刈る**（2026-09-02）。鉄 2 個の斜めで作れて、238 回で尽きる。**実装済み**（`items.ts` の `isShears()` / 誰が刈れるかは `mobs.ts` の `MobDef.shearing` / 減り方は `durability.ts` の `wearForUse()`） |
| **117 / 118 / 119 / 120** 木 / 石 / 鉄 / ダイヤのクワ | **土か草を耕して耕地（116）にする**（2026-09-02）。掘る速さは素手と同じで、**耕したときだけ**減ります。**実装済み**（`items.ts` の `isHoe()` / 耕せるかは `placing.ts` の `tryTill()` / 減り方は `durability.ts` の `wearForTill()`） |
| **122** 小麦の種 | **草むらを壊すと 12.5% で出て、耕地の上に苗（121）を立てる**（2026-09-02）。**道具でも食べ物でもなく、置けるアイテムでもありません**（`block: AIR`。植えるのは `place` でなく `plant` の経路）。**実装済み**（`items.ts` の `isSeed()` / 植わるかは `placing.ts` の `tryPlant()`） |
| **124** 小麦 | **実った小麦（123）を掘ると 1 個**（2026-09-02）。**種も 1 個戻ります**（`DROPS` の `extra`。1 回の採掘で 2 山。番号は取っていません。`TUNING.md`）。**道具でも置けるアイテムでもありません**。**食べ物でもありません** —— 本家と同じで、**パン（125）にしてから食べます**。**実装済み**（`items.ts` の `DROPS`） |
| **125** パン | **小麦 3 個の横一列**（2026-09-03）。**3 幅なので作業台が要ります**。空腹 +5 / 満腹度 +6 で、**かまど無しで作れる中では一番強い**（焼き豚 8 / 12.8 の立場は保っています）。**`use.ts` も `vitals.ts` も `main.ts` も 1 行も触っていません** —— 食べる経路は `foodOf()` 1 本を通るので、`items.ts` の 2 行と `crafting.ts` の 1 行で足りました。**実装済み**（`items.ts` の `FOODS` / `crafting.ts` の `RECIPES`） |
| **126 / 127** 生鶏肉 / 焼き鳥 | **鶏（3 種類目の受動モブ）を倒すと生鶏肉 1 個、かまどで焼くと焼き鳥**（2026-09-03）。**`main.ts` も `mobmesh.ts` も `mobrender.ts` も 1 行も触っていません** —— モブの形も湧きもドロップも `MobDef` の表 1 つを回るので、`mobs.ts` の 1 定義と `items.ts` の 4 行・`smelting.ts` の 1 行で足りました。**実装済み**（`mobs.ts` の `CHICKEN` / `items.ts` の `FOODS` / `smelting.ts` の `SMELTING`） |
| **128** 羽根 | **鶏の 2 山目**（生鶏肉と一緒に必ず 1 個。2026-09-03）。**矢を本家の形へ戻すためだけに取った番号**で、置けず・道具でもなく・食べ物でもありません。**`main.ts` も `drops.ts` も `breaking.ts` も 1 行も触っていません** —— モブの 2 山目は `MobDrop.extra` と `dropsFor()` の 1 本で足り、`items.ts` の 2 行と `crafting.ts` の 1 行が付くだけでした。**実装済み**（`items.ts` の `FEATHER` / `mobs.ts` の `dropsFor()` / `crafting.ts` の 矢） |
| **129** 卵 | **鶏が 300〜600 秒ごとに足元へ 1 個産む**（2026-09-03）。**倒さずに取れる 2 つ目**（羊毛に続いて）で、違うのは**プレイヤーの操作ではなく時計が起こす**ところだけ。**`main.ts` も `drops.ts` も `storage.ts` も 1 行も触っていません** —— 出口は倒したとき・刈ったときと同じ `onDrop` 1 本で足り、`mobs.ts` の `LayRule` と `Mobs.lay()`・`items.ts` の 2 行が付くだけでした。**実装済み**（`items.ts` の `EGG` / `mobs.ts` の `MobDef.laying`）。**2026-09-04 に投げられるようになりました** —— **ID は 1 個も使っていません**（`"egg"` は `ProjectileKind` の文字列で、番号ではありません）。`projectiles.ts` の表に 1 行・`items.ts` の `THROWN` の表 1 本・`use.ts` の 2 行・`main.ts` の配線 2 か所だけで、**`mobs.ts` も `sfx.ts` も `projectilerender.ts` も 1 行も触っていません** |
| **133** 糸 | **クモ（2 種類目の地表の敵対モブ）を倒すと 1 個**（2026-09-04）。**表の作りはゾンビとまったく同じ**で、違うのは**当たり判定 1.4 x 0.9 と脚 8 本**だけ。**`mobs.ts` の 1 定義と `items.ts` の 2 行以外は 1 行も触っていません**（`main.ts` / `mobmesh.ts` / `mobrender.ts` / `crafting.ts` は 0 行）。**糸の使い道はまだありません** —— **弓を「棒 3 + 糸 3」に戻すのが次の周**です。**実装済み**（`mobs.ts` の `SPIDER` / `items.ts` の `STRING`） |
| **130 / 131 / 132** 生牛肉 / ステーキ / 革 | **牛（4 種類目の受動モブ）を倒すと生牛肉 1 個 + 革 1 個、かまどで焼くとステーキ**（2026-09-04）。**鶏（126..128）とまったく同じ形**で、違うのは**2 山目が最初から付く**ところだけ。**`main.ts` も `mobmesh.ts` も `mobrender.ts` も `crafting.ts` も 1 行も触っていません** —— `mobs.ts` の 1 定義と `items.ts` の 6 行・`smelting.ts` の 1 行だけです。**革の使い道はまだありません**（防具も本も別件）。**実装済み**（`mobs.ts` の `COW` / `items.ts` の `LEATHER` / `smelting.ts` の `SMELTING`） |
| ネザークォーツ | ネザーの鉱石。**166 から取ること**（111..114 は剣、115 はシアーズ、116 は耕地、117..120 はクワ、121 は小麦の苗、122 は小麦の種、123 は実った小麦、124 は小麦、125 はパン、126 は生鶏肉、127 は焼き鳥、128 は羽根、129 は卵、130 は生牛肉、131 はステーキ、132 は革、133 は糸、134 は雪玉、135..137 は鉄・金・ダイヤのブロック、138 はミルクバケツ、139..140 は赤・茶のキノコ、141..142 はボウルとキノコシチュー、143..144 はサトウキビと砂糖、145..148 ははしごの 4 向き、149 はリンゴ、150..152 は紙・本・本棚、153 は金のリンゴ、154 はクモの巣、155 はケーキ、156 は氷、157 はフェンス、158..161 は革の防具 4 部位、162 は骨、163 は木炭、164..165 は苗木 2 種が使いました） |

**バケツは 3 つで 1 組**です（空・水・溶岩）。中身をアイテム ID で表すのは Minecraft と同じで、
**スタック上限は 1**（16 個の水を 1 枠に持てると、水路作りが別のゲームになります）。

## 土台（クリア導線より先に要るもの）

| 土台 | なぜ要るか | いまの状態 |
| --- | --- | --- |
| 溶岩 | 黒曜石が作れない。ネザーの溶岩海も | **済み**（`LAVA` 42 / `quenched()`） |
| `portals.ts` | 枠の検出・点火の可否 | **済み**（配線は 2-2） |
| `dimensions.ts` + `World` の生成器注入 | ネザー / エンド | **済み**（`ChunkSource`。次元の器は `rules/dimensions.md`） |
| `structures.ts` | ネザー要塞・要塞 | **済み**（ネザー要塞 `fortress.ts` と要塞 `stronghold.ts` が乗った。**エンドの柱は乗っていません** —— グリッドに確率で撒く `SiteRule` とは性質が違うので、`endgen.ts` の中で直に建てています） |
| `projectiles.ts` + `projectilerender.ts` | 火球・矢・投げたエンダーアイ・ブレス | **済み**（当たった効果はまだ無い） |
| `nethergen.ts` | ネザーの地形 | **済み**（`DIMENSIONS` に登録済み。行く手段は 2-2） |

`WORLD_HEIGHT` は 128（`CHUNK_LAYERS` 8 x 16）なので、**ネザー（天井付き 128）も
エンドもそのまま入ります。縦は触らないこと。**

## 進み具合

`test/progression.test.ts` が「達成 N / 13」を毎回出します。**これが進捗の唯一の指標**で、
`npm test` の緑は「壊していない」しか言いません。
