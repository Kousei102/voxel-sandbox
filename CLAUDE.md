# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このプロジェクトは何か

ブラウザで動くボクセルサンドボックス。**サーバー・API・DB・ゲームエンジンは存在しません。**
Vite + TypeScript + three.js だけの静的サイトで、外部アセット（テクスチャ・モデル・フォント）も
一切使いません。見た目はすべて頂点カラーに焼き込んで作っています。

セーブは `localStorage` のキー `voxel-sandbox:v1`。**地形そのものは保存しません。**
シードから毎回生成し直し、プレイヤーが壊した／置いたブロックの差分だけを持ちます。
「ワールド全体をシリアライズする」実装は設計違反です。

## コマンド

```bash
npm install
npm run dev        # http://localhost:5173

npm run typecheck  # src と test をまとめて
npm test           # 85 項目。WebGL 不要（後述）
npm run bench      # 生成とメッシュ化の速度計測
npm run build      # typecheck + dist/ に静的ファイル一式
```

## この環境では WebGL が動かない

**確認済み: このコンテナの Chromium では WebGL コンテキストを作れません。**
SwiftShader の初期化が `BindToCurrentSequence failed` で落ちます。ANGLE/Vulkan/Xvfb・
ヘッドレスシェル・フル Chromium と一通り試して不可でした。Playwright でスクリーンショットを
撮ろうとしても時間を捨てるだけなので、やらないでください。

したがって **描画結果の確認はユーザーにブラウザで見てもらうしかありません。**
その代わり、画面を見ないと気付けない類の不具合は `npm test` で数値的に検証しています:

- 全三角形について、頂点の巡回順から求めた法線と格納された法線が一致するか
  （ずれると裏面カリングで面が丸ごと消える）
- 描画距離内にメッシュ化されないチャンクが残らないか（穴が空いたまま直らない）
- 頂点座標・index・色・法線が値域に収まっているか

**シェーダを手書き／`onBeforeCompile` で注入する変更は、ここでは検証できません。**
GLSL のコンパイルエラーは画面が真っ黒になる形でしか現れないため、入れるなら
ユーザーに確認してもらう前提で、小さく切り出してください。

## 座標とインデックスの規約

破ると静かに壊れるので、必ず合わせてください。

| もの | 規約 |
| --- | --- |
| チャンク内の添字 | `(ly * 16 + lz) * 16 + lx`（`chunk.ts` の `localIndex`） |
| チャンクキー | `"cx,cy,cz"` の文字列。列キーは `"cx,cz"` |
| メッシュ用パッド | 18³。`padIndex(x,y,z)` は `x,y,z ∈ [-1, 16]` |
| ワールド → チャンク | `wx >> 4`、ローカルは `wx & 15`（負数でも正しく動く） |
| 面の番号 | `0=+X 1=-X 2=+Y 3=-Y 4=+Z 5=-Z` |

`CHUNK_SIZE` は 2 の冪であることを前提にビット演算しています。変えるなら `CHUNK_BITS` と
`CHUNK_MASK` も合わせてください。

## 崩してはいけない不変条件

### 1. メッシュ化するチャンクは、周囲 3x3 列のボクセルが「完成済み」であること

面の可視判定と AO は隣接ブロックを見るので、隣の列が無いと誤った面ができます。
`meshChunk()` は 9 列が `columns` に揃っていなければ、**その場で生成せずキューの末尾に戻します。**

ここで `ensureColumn()` を呼ぶと 1 フレームに数十 ms のスパイクが出ます（実測 27ms）。
だからボクセルだけは描画距離より 1 リング広く用意しています（`DATA_DISTANCE`）。
生成範囲は「描画円盤を 1 マス膨らませた形」で、単純に半径 +1 の円盤にすると角の列が漏れて
永久に待たされるチャンクができます（`rebuildColumnQueue` のコメント参照）。

### 2. フレームあたりの仕事は「個数」ではなく「時間」で区切る

チャンク 1 個の生成もメッシュ化も約 1ms です。`world.update()` は生成 3ms・メッシュ化 6ms の
予算で打ち切り、続きを次のフレームに回します。列 8 段をまとめて作ると 7ms 食うので、
ストリーミング中は段単位（`generateColumnStep`）に割っています。

現状の実測: 平均 2.8ms / 最悪 10〜17ms（GC 込み）。`npm run bench` で再測できます。

### 3. 面の巡回順

`+` 方向の面は `0,1,2 / 0,2,3`、`-` 方向は逆順。`mesher.ts` の `Builder.quad()` が唯一の発行点です。
軸の取り方 `u=(d+1)%3, v=(d+2)%3` は `u × v = +d` になるよう選んであります。

## アイテムとサバイバル

- **アイテム ID の 1..63 はブロック ID と同じ番号**（置けるアイテム）。64 以降が棒・鉱物・道具。
  ブロック ID が 6 ビット（63 まで）なのは mesher の `encodeFace` の都合なので、この境目は動かせません。
- 採掘時間は Minecraft と同じ式（`mining.ts`）: `硬さ × (適正なら 1.5 : 5) / 道具の速さ`。
  「適正」= 道具の種類が合っていて階層が `minTier` 以上。**種類だけ合っていて階層が足りない道具は、
  速くはなるが何も落とさない**（石のツルハシでダイヤ鉱石を掘っても消えるだけ）。
- 形ありレシピは**盤面もレシピも空の行・列を落としてから**比べます（`crafting.ts` の `trim`）。
  レシピ側のトリムを忘れると、斧のように右端が空いた形が永久に一致しません。左右反転も受け付けます。
- インベントリ・クラフト・採掘の計算は DOM に触らないので、全部ヘッドレスでテストできます。
  **判定を `inventoryui.ts` 側に書かないこと。**
- クリエイティブへ切り替えるときは**空いているホットバーだけ**を埋めます（上書きすると集めたものが消えます）。

かまどがまだ無いので、**鉄と金の鉱石はインゴットを直接落とし**、砂 4 個からガラスを作れるようにしてあります
（どちらも精錬の代用）。かまどを入れるときはこの 2 つを戻してください。道具の耐久値もまだありません。

## 体力

判定は `vitals.ts` に全部あります。**ダメージの条件を描画側に書かないこと**（目で見るまで壊れに気付けません）。

- 落下は「空中に居るあいだの最高到達点 − 着地点」。3 ブロックまで無傷で、そこから 1 ブロックにつき 1。
  水に落ちる・飛行に切り替えるとリセットされます。
- 息は 15 秒。切れたあと毎秒 2。頭が水中かどうかで判定するので、`main.ts` の `underwater`
  （カメラ位置で判定）を渡します。**`updateEnvironment()` のあとに `updateVitals()` を呼ぶこと。**
- クリエイティブは無敵で、落差も息も溜めません（溜めると、切り替えた瞬間に溜まった落差で死にます）。
  奈落に落ちた場合はダメージではなく初期位置へ戻します。
- **空腹はまだありません。** 食料源（作物・動物）が無い状態で入れると回復手段が無く餓死するだけなので、
  代わりにピースフルと同じ自然回復（被弾 5 秒後から 2 秒に 1）を入れてあります。モブか農業が入ったら
  この自然回復を外して空腹に置き換えてください。
- 死んでもインベントリは失いません。アイテムを落とすには、まず地面に落ちたアイテムの仕組みが要ります。
  ベッドも無いので、リスポーン地点はワールドの初期位置ひとつだけです。

## 光（スカイライト）

光源は **「真上に遮るものが無いボクセル」** で、そこから 6 方向へ BFS で広がります。
1 マスにつき 1（水中は 3）減衰し、不透明ブロックは通しません。

- 「真下へは減衰しない」という Minecraft の特例は**入れていません。**
  代わりに光源の判定を (x,z) ごとの `skyHeights` で持っているので、真上が空いている縦穴は
  底まで最大光量になります（結果として Minecraft と同じ見え方になります）。
  特例が無いぶん、ブロックの置き／壊しの差分更新が素直な BFS 2 本で書けます。
- 差分更新は `relightEdit()`。**消える光の BFS → 増える光の BFS** の順で回します。
  消す側だけ、増やす側だけ、では境界が壊れます。
- `lightColumn()` は 1 列 32,768 ボクセルを触ります。**必ずチャンクの配列を直接なめること。**
  1 マスずつ `getVoxel()` を通す実装にしたら、1 列あたり 8ms かかって描画が引っかかりました。
- 種まき（`seedColumnLight`）は「隣のマスが暗い可能性がある高さ」だけに絞っています。
  光源を全部積むと 1 列で数万件になります。逆に絞りすぎると、あとから読み込まれた列に
  光が流れ込まず、崖の洞窟の入口が暗いままになります。

明るさは `AMBIENT_LIGHT`（光量 0 のときの明るさ）と `LIGHT_FALLOFF` で決まります。
`AMBIENT_LIGHT` を 0 にすると洞窟が完全な暗闇になり、光源を持たないこのゲームでは
探索できなくなります。**0 にするなら、同時に手持ちの光源が要ります。**

## 昼夜サイクル

時刻 `t` は `[0, 1)` で、**0 = 日の出、0.25 = 南中、0.5 = 日没、0.75 = 真夜中**（`clock()` では 06:00 起点）。
1 周 20 分（`DAY_LENGTH_SECONDS`）。

- **時刻に関する計算は全部 `daynight.ts` に置くこと。** three の `Color` と `Vector3` しか使っていないので
  ヘッドレスで検証できます。`sky.ts` 側は出てきた値を貼るだけにしてください（判定を散らすとテストが効かなくなります）。
- 地形の明るさは `world.setDaylight()` → `MeshBasicMaterial.color`。これは頂点カラーに**乗算**されるので、
  焼き込んだ光量をそのままに再メッシュ化ゼロで夜にできます。**時刻ごとに頂点色を焼き直す実装にしないでください。**
- `NIGHT_BRIGHTNESS` は `AMBIENT_LIGHT` と同じ理由で 0 にできません。夜は全体が 0.28 倍になるので、
  洞窟の見え方は実質 `AMBIENT_LIGHT × NIGHT_BRIGHTNESS` です。
- フォグの色は `dayNight.horizon` に追従させます。天球の地平線と色がずれるとチャンクの出現が見えます。
- 太陽・月は板ポリ、星は `Points`。**テクスチャもシェーダも使いません**（`sky.ts` の GLSL は天球の
  グラデーションだけで、昼夜のために手を入れていません）。天体は `transparent: true` なので
  不透明の地形より後に描かれ、深度テストで山の陰に隠れます。
- 明るさが不連続に飛ぶと「急に暗転する」形でしか気付けないので、1 分刻みの変化量をテストで押さえています。

## greedy meshing の要点

3 軸それぞれのスライスで面マスクを作り、**完全に同一の面だけ**を長方形に統合します。
「同一」の判定は 1 つの整数に詰めた `ブロック ID | 向き | 四隅の AO | 四隅の光量` の一致です
（`encodeFace`）。AO と光量を含めるので、陰影が違う面は統合されません。ここを緩めると陰影が壊れます。

ビットは 31 本すべて使い切っています（id 6 / 向き 1 / AO 2x4 / 光量 4x4）。
情報を足すなら詰め方から見直してください（`Int32Array` の符号ビットは使えません）。

16x16 の平らな床が 12 三角形（未統合なら 1536）になることをテストで固定しています。

## 描画

マテリアルは **`MeshBasicMaterial` 2 枚だけ**（不透明用・半透明用）。ライトは 1 つも置いていません。
陰影は「面の向きによる明暗 × AO」を頂点カラーに焼き込んで表現しています。

- 頂点カラーは **itemSize 4（RGBA）**。three は itemSize が 4 のときだけ `USE_COLOR_ALPHA` を
  定義するので、これを 3 にすると水とガラスの透明度が消えます。
- 半透明レイヤーは `depthWrite: false` + `DoubleSide`（水面を下から見るため）。
- 空はカメラ追従の天球（`sky.ts`）。フォグと地平線の色を合わせてチャンクの出現を隠しています。

## テストの書き方

`test/harness.ts` は自前の 20 行の代物です（`check(ラベル, 真偽, 補足)`）。
アサーションではなく **値を出力してから判定する** 形にしてください。
「山が 16% 生成される」のように分布を出しておくと、生成パラメータを触ったときに
壊れ方が一目で分かります。

three の描画以外（`Scene`・`BufferGeometry`・`Vector3`）は Node でそのまま動くので、
`World` も `Player` もヘッドレスで丸ごと動かせます。テストは esbuild で束ねて node で実行します。

## 地形を触るとき

`worldgen.ts` の `heightAt()` に大陸・丘・尾根の 3 ノイズが集約されています。
`fbm2()` の実効レンジは **概ね ±0.6**（±1 ではない）ので、しきい値はその前提で決めてあります。
`smoothstep(0.05, 0.34, continent)` の上限を 0.55 などにすると山がまったく生えなくなります。

木は列をまたいで枝葉が伸びるため、チャンク生成時に **自分の列と隣接 8 列の木**を
`applyTrees()` で毎回スタンプし直しています（保留マップを持たない代わりに決定的に再計算）。
