# CLAUDE.md

このファイルは Claude Code（claude.ai/code）への案内です。
**ここに書くのは「どの作業でも要るもの」だけ**にしてください（下の「決まりごとの置き場所」）。

## このプロジェクトは何か

ブラウザで動くボクセルサンドボックス。**サーバー・API・DB・ゲームエンジンは存在しません。**
Vite + TypeScript + three.js だけの静的サイトで、外部アセット（テクスチャ・モデル・フォント）も
一切使いません。色も陰影も頂点に焼き込んで作っています。

セーブは `localStorage` のキー `voxel-sandbox:v1`。**地形そのものは保存しません。**
シードから毎回生成し直し、プレイヤーが壊した／置いたブロックの差分だけを持ちます。
「ワールド全体をシリアライズする」実装は設計違反です。

## コマンド

```bash
npm install
npm run dev        # http://localhost:5173

npm run typecheck  # src と test をまとめて
npm test           # 912 項目・12 秒前後。WebGL 不要（後述）
npm run bench      # 生成とメッシュ化の速度計測
npm run build      # typecheck + dist/ に静的ファイル一式
```

次に何を入れるかは `ROADMAP.md`。**着手する順はユーザーと決めること。**
いまの状況（未コミットのもの・push の有無・どこまでブラウザで確認済みか）は `HANDOFF.md`。

**クリア導線（ネザー → エンド → エンダードラゴン）はループで進めています。**
1 周の手順と禁じ手は `LOOP.md`、タスクの並びは `TASKS.md`、ブラウザ確認の積み残しは
`REVIEW.md`。進み具合は `npm test` の「進行（クリア導線）」が「達成 N / 13」で出します
（`test/progression.test.ts`）。**ブロック ID とアイテム ID は `ROADMAP.md` の予約表から取ること。**
**`HANDOFF.md` は消さずに、セッションの終わりに丸ごと書き直して次へ渡すこと**
（引き写さず、`git fetch` と `npm test` で裏を取ってから）。

## 決まりごとの置き場所（3 層）

**全部をここに書かないでください。** 常時読み込まれるのはこのファイルだけなので、
領域別の話を足すとどの作業でも代金を払うことになります。

| 層 | 場所 | いつ読まれるか | 置くもの |
| --- | --- | --- | --- |
| 1 | **`CLAUDE.md`（ここ）** | 毎回 | どの作業でも要るもの。**200 行以内** |
| 2 | **`.claude/rules/*.md`** | `paths` に合うファイルを触ったときだけ | 領域別の決まりごと |
| 3 | **`.claude/skills/*/SKILL.md`** | その作業をするときだけ | 手順書 |

いまある層 2（`paths` は各ファイルの先頭）:

`blocks-shapes.md`（形・支え・液体・置く判断・ID の細部）/ `items-survival.md` / `inventory-screen.md` /
`vitals.md`（体力と空腹）/ `mobs.md` / `drops.md` / `projectiles.md`（飛び道具）/
`stateful-blocks.md`（かまど・チェスト）/
`beds.md` / `lighting.md`（光・昼夜・GLSL）/ `meshing-render.md` / `audio.md` / `dom-ui.md` /
`testing.md` / `worldgen.md`（地形・バイオーム）/ `dimensions.md`（次元・ポータルで移る・セーブの形）

いまある層 3: `add-block`（ブロック / アイテムを足す）/ `add-stateful-block`（位置ごとに
状態を持つブロック・器を足す）/ `unverifiable-pair`（確かめられないものを足す）

### この形を保つための決まり

- **`src/**` と `test/**` のファイルは `Read` / `Edit` ツールで開くこと。**
  `cat` や `sed` で読むと**層 2 が読み込まれません**（実測で確認済み。`paths` は
  `Read` と `Edit` で効き、Bash 経由の読み出しでは効きません）。
- **層 2 のファイルには必ず `paths` を書くこと。** 無いと常時読み込みになり、
  ここに直接書いたのと同じことになります。
- **同じことを 2 つの層に書かないこと。** 食い違ったときにどちらが勝つか決まりません。
  層 1 は要点だけを持ち、細部は層 2 へ置いて名前で参照します。
- 新しい節を足したくなったら、まず**どの層か**を決めます。判断はこの順:
  1. **どのファイルを触るときも要るか** → 層 1（ここ）
  2. **触るファイルが決まっているか** → 層 2（そのファイルを `paths` に書く）
  3. **手順（順番に沿ってやること）か** → 層 3
- **層 2 は `paths` を広めに取ってよい。** `player.ts` を触ったときに `mobs.md` の
  「水中 0.6 倍」が目に入る、という**横断的な気付きが実際にバグを見つけています。**
  関わるファイルは削らないこと。
- **このファイルが 200 行を超えたら、分割ではなくまず層 2 へ動かすこと。**
  `/context` で実際の読み込み量を確かめられます。

## この環境では WebGL が動かない

**確認済み: このコンテナの Chromium では WebGL コンテキストを作れません。**
SwiftShader の初期化が `BindToCurrentSequence failed` で落ちます。ANGLE/Vulkan/Xvfb・
ヘッドレスシェル・フル Chromium と一通り試して不可でした。Playwright でスクリーンショットを
撮ろうとしても時間を捨てるだけなので、やらないでください。

したがって **描画結果の確認はユーザーにブラウザで見てもらうしかありません。**
その代わり、画面を見ないと気付けない類の不具合は `npm test` で数値的に検証しています:

- 全三角形について、頂点の巡回順から求めた法線と格納された法線が一致するか
  （ずれると裏面カリングで面が丸ごと消える）
- 描画距離内にメッシュ化されないチャンクが残らないか（穴が空いたまま直らない）
- 頂点座標・index・色・法線が値域に収まっているか
- TS が `getElementById` で引く id が `index.html` に実在するか
  （`as HTMLElement` で null が握りつぶされるので、触るまで壊れに気付けない）

GLSL を触るときの制約は `.claude/rules/lighting.md`。

## 確かめられないものは、確かめられるものから切り離す

**このプロジェクトの背骨です。** 画面も音もこの環境では確かめられないので、確かめられないものは
専用のファイルへ閉じ込め、**判断（何を・いつ・どんな数値で）は必ず相方の側に置きます。**
そうすれば「壊れたかどうか」は `npm test` で分かり、ユーザーに見てもらうのは
「気持ちいいかどうか」だけで済みます。

| 確かめられないもの | 閉じ込めてあるファイル | 判断を持つ相方（`npm test` で見る） |
| --- | --- | --- |
| GLSL | `sky.ts` / `terrainshader.ts` | `daynight.ts` |
| WebAudio | `audio.ts` | `sfx.ts`（**波形は `test/audio.test.ts` が実際に鳴らして見ます**） |
| DOM | `ui.ts` / `inventoryui.ts` | `craftscreen.ts` / `inventory.ts` / `crafting.ts` / `smelting.ts` |
| three（描画） | `mobrender.ts` / `droprender.ts` | `mobs.ts` / `mobmesh.ts` / `drops.ts` |

**この対はテストがソースを読んで両方向に見張っています。** 閉じ込めた側に判断が漏れていないか
（`mobrender.ts` に `Math.random(` / `spawn` / `damage` / `hostile` が無いか）と、
判断の側に確かめられないものが入り込んでいないか（`mobs.ts` に `Mesh` や `AudioContext`、
`craftscreen.ts` に `document` や `HTMLElement`、`sfx.ts` に `AudioContext` が無いか）の
両方です。とくに **`inventoryui.ts` が `crafting.ts` と `smelting.ts` を import しないこと**が一番強い判定です。

**新しく「確かめられないもの」を足すときは、必ず同じ形にしてください。** 崩した瞬間、
その領域が丸ごと「ブラウザを開くまで確かめられないもの」に戻ります。
手順は `unverifiable-pair` スキル。

## ブロック ID の枠

**2 段に分かれています。** 増やすときはどちらに置くかを決めてください。

| 枠 | 置くもの | 効いている制限 |
| --- | --- | --- |
| 1..63 | 立方体すべてと、**アイテムとして持てる**ブロック（ハーフや階段の「大元」も） | `encodeFace` の id 6 ビット / アイテム ID と同じ番号 |
| 64..255 | `variantOf` を持つ**向き違いだけ**（上付きハーフ、階段の向き） | ボクセルが `Uint8Array` なので 255 まで |

- **立方体を 64 以降に置かないこと**（面の統合キーが壊れます）。
- **64 以降にアイテムを持つブロックを置かないこと**（持てないブロックになります）。
- どちらもテストで押さえています（`test/blocks.test.ts`）。空きは `npm test` が毎回出します。

既存の ID を振り直すと**セーブの差分が別のブロックに化けます**（`localStorage` にはブロック ID が
そのまま入っています）。番号は足すだけにしてください。アイテム ID も同じ理由で振り直せません
（83 = 焼き豚まで使用済み）。

足すときの手順は `add-block` スキル。細部（`isProp()` と 64 以降の関係・立方体の状態違い・
水平 4 向きの表）は `.claude/rules/blocks-shapes.md`。

## 座標とインデックスの規約

破ると静かに壊れるので、必ず合わせてください。

| もの | 規約 |
| --- | --- |
| チャンク内の添字 | `(ly * 16 + lz) * 16 + lx`（`chunk.ts` の `localIndex`） |
| チャンクキー | `"cx,cy,cz"` の文字列。列キーは `"cx,cz"` |
| メッシュ用パッド | 18³。`padIndex(x,y,z)` は `x,y,z ∈ [-1, 16]` |
| ワールド → チャンク | `wx >> 4`、ローカルは `wx & 15`（負数でも正しく動く） |
| 面の番号 | `0=+X 1=-X 2=+Y 3=-Y 4=+Z 5=-Z` |

`CHUNK_SIZE` は 2 の冪であることを前提にビット演算しています。変えるなら `CHUNK_BITS` と
`CHUNK_MASK` も合わせてください。

## 崩してはいけない不変条件

### 1. メッシュ化するチャンクは、周囲 3x3 列のボクセルが「完成済み」であること

面の可視判定と AO は隣接ブロックを見るので、隣の列が無いと誤った面ができます。
`meshChunk()` は 9 列が `columns` に揃っていなければ、**その場で生成せずキューの末尾に戻します。**

ここで `ensureColumn()` を呼ぶと 1 フレームに数十 ms のスパイクが出ます（実測 27ms）。
だからボクセルだけは描画距離より 1 リング広く用意しています（`DATA_DISTANCE`）。
生成範囲は「描画円盤を 1 マス膨らませた形」で、単純に半径 +1 の円盤にすると角の列が漏れて
永久に待たされるチャンクができます（`rebuildColumnQueue` のコメント参照）。

### 2. フレームあたりの仕事は「個数」ではなく「時間」で区切る

チャンク 1 個の生成もメッシュ化も約 1ms です。`world.update()` は生成 3ms・メッシュ化 6ms の
予算で打ち切り、続きを次のフレームに回します。列 8 段をまとめて作ると 7ms 食うので、
ストリーミング中は段単位（`generateColumnStep`）に割っています。

現状の実測: 平均 3.4ms / 最悪 10〜17ms（GC 込み）。`npm run bench` で再測できます。
**`npm run bench` の値は 1 回では当てになりません**（同じコードで generateChunk が
0.93〜1.40ms に振れます）。比べるときは数回まわして中央値を見てください。
テスト側がどう判定しているかは `.claude/rules/meshing-render.md`。

### 3. 面の巡回順

`+` 方向の面は `0,1,2 / 0,2,3`、`-` 方向は逆順。`mesher.ts` の `Builder.quad()` が唯一の発行点です。
軸の取り方 `u=(d+1)%3, v=(d+2)%3` は `u × v = +d` になるよう選んであります。
