# あとで決める数値（手触りの預かり表）

**クリアまで通すのが先で、難度と手触りは後から一括で調整する**とユーザーと決めました
（`LOOP.md` の「3.5」）。ループは**本家 Minecraft の値をそのまま暫定で置き、ここに 1 行足します。**

## 書き方

| 列 | 中身 |
| --- | --- |
| 場所 | ファイルと定数名（**judgment のファイルに置くこと**。`main.ts` や `*render.ts` に散らさない） |
| いま | 現在の値 |
| 出典 | **本家** = Minecraft と同じ / **目分量** = このプロジェクトで決めた |
| 触ると | 変えたときに何が変わるか（1 行） |

**行を消さないこと。** 調整が済んだ行は「済」を頭に付けます（何をどう決めたかが残ります）。
ユーザーが実際に遊んで決めるのはこの表で、**ここに無い数値は「決めた覚えがない数値」**です。

---

## 戦闘・ダメージ

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `vitals.ts` `MAX_HEALTH` | 20 | 本家 | 全部の被弾の重み |
| `vitals.ts` `LAVA_DAMAGE` / `LAVA_INTERVAL` | 4 / 0.5 秒 | 本家 | 溶岩に触れたときの致死性。**防具が無いので実質 19 入る** |
| `vitals.ts` `BURN_SECONDS` | 15 秒 | 本家 | 溶岩から出たあとの炎上の長さ |
| `vitals.ts` `DROWN_DAMAGE` / `AIR_SECONDS` | 2 / 15 秒 | 本家 | 溺れの厳しさ |
| `vitals.ts` `VOID_DAMAGE` | 8 | 目分量 | 奈落の即死性 |
| `vitals.ts` `FALL_SAFE` | 3 | 本家 | 落下で痛くならない高さ |
| `vitals.ts` `MOB_HURT_COOLDOWN` | 0.5 秒 | 本家 | モブが何体居ても入る量の上限 |
| `mobs.ts` `MOB_DAMAGE` / `MOB_ATTACK_COOLDOWN` | 2 / 1 秒 | 本家（ゾンビ・イージー相当） | 敵に殴られる速さ |
| `mobs.ts` `HOSTILE_SIGHT` / `ATTACK_RANGE` | 18 / 1.4 | 目分量 | 敵に追われ始める距離 |
| `mobs.ts` `TOOL_ATTACK` / `TIER_ATTACK` | 斧 3・ツルハシ 2・シャベル 1 / 階層 +0.5 | 目分量 | 殴り返す強さ（本家より単純） |
| `mobs.ts` `PLAYER_ATTACK_COOLDOWN` | 0.5 秒 | 目分量 | 連打の効き |

## モブ

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `mobs.ts` `PIG.maxHealth` / `speed` | 10 / 1.7 | 本家 | 豚の硬さと逃げ足 |
| `mobs.ts` `SHEEP.maxHealth` / `speed` | 8 / 1.5 | 本家 | 羊の硬さ |
| `mobs.ts` `ZOMBIE.maxHealth` / `speed` | 12 / 4.6 | 目分量（本家は 20 / 遅い） | **速い**ので本家より怖い。真っ先に見る行 |
| `mobs.ts` `ZOMBIE.drop.chance` | 0.6 | 目分量 | 腐った肉の出やすさ |
| `mobs.ts` `PASSIVE_SKY_MIN` / `HOSTILE_LIGHT_MAX` | 9 / 7 | 本家 | どこに湧くか（松明の意味） |

## 空腹

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `vitals.ts` `EXHAUST_LIMIT` ほか消耗の表 | 4 / 歩き 0.01 / 走り 0.1 | 本家 | 腹の減る速さ全体 |
| `vitals.ts` `REGEN_HUNGER` | 18 | 本家 | 自然回復に要る満腹度 |
| `vitals.ts` `STARVE_FLOOR` / `POISON_FLOOR` | 1 / 1 | **ユーザーと決めた**（ノーマル相当） | 餓死・毒死するかどうか |
| `vitals.ts` `SPRINT_HUNGER` | 6 | 本家 | 走れなくなる線 |
| `vitals.ts` `EAT_SECONDS` | 1.6 | 本家 | 食べる長さ |

## 世界・探索

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `fortress.ts` `FORTRESS_SPACING` / `FORTRESS_CHANCE` | 6 列 / 0.6 | 目分量 | ネザー要塞の見つけやすさ（いま原点から 25〜34 マス） |
| `portaltravel.ts` `PORTAL_DELAY` | 0.9 秒 | 本家 | ポータルに立ってから移るまでの間 |
| `portaltravel.ts` `SEARCH_RADIUS` | 12 | 目分量 | 戻ったときに元の枠へ出る範囲 |
| `portaltravel.ts` `PORTAL_SCALE` | 8 | 本家 | ネザーの 1 歩が地上の何歩か |
| `drops.ts` `DESPAWN_AGE` / `MAX_DROPS` | 300 秒 / 128 | 本家 / 目分量 | 死んだ場所に戻る猶予 |
| `items.ts` 砂利 → 火打石 | 10% | 本家 | 火打石と打ち金までの手間 |
| `worldgen.ts` `VEINS` の `veinChance` | 鉱石ごと | 目分量 | 鉱石の見つけやすさ（ダイヤ 0.003） |
| `daynight.ts` `SKY_STYLES.nether` の `brightness` | 1.0 | 目分量 | ネザーの地形の明るさ全体。**モブの湧きの明るさも同じ値を見る**ので、下げると暗所が増える |
| `daynight.ts` `SKY_STYLES.nether` の色 3 つ | 天頂 #2a0906 / 地平 #5a1a12 / 地面 #180504 | 目分量 | ネザーの空と**フォグ**の赤黒さ（フォグは地平線に追従） |
| `daynight.ts` `SKY_STYLES.end` の `brightness` と色 | 0.7 / 天頂 #0a0716・地平 #1d1236・地面 #06040e | 目分量 | エンドの暗さと紫の濃さ。**まだ行けない**ので、2-10 で実際に立ってから決める |

## モブ（ブレイズ）

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `mobs.ts` `BLAZE.maxHealth` | 20 | 本家 | 何回殴れば倒れるか（ダイヤの斧 5 で 4 回） |
| `mobs.ts` `BLAZE.speed` | 3.6 | 目分量 | **プレイヤーの歩き 5.2 より遅いこと。** 壁も崖も関係なく飛んでくるので、ゾンビ（4.6）に揃えると振り切れない |
| `mobs.ts` `BLAZE.drop.chance` | 0.5 | 本家 | ブレイズロッドの集まり方（エンダーアイまでの手間） |
| `mobs.ts` `FLY_HOVER` / `FLY_ABOVE` | 2.5 / 1.2 | 目分量 | 飛ぶモブが床から浮く高さ／追うときプレイヤーのどれだけ上に居るか。**1 以下にしないこと**（床の手すりに引っかかる） |
| `mobs.ts` `FLY_RISE` / `FLY_ACCEL` | 3 / 12 | 目分量 | 上下の動きの機敏さ。上げると壁をすぐ越えるが、ふわふわ感が消える |
| `mobs.ts` `BLAZE.damage` | 6 | 本家 | 近づかれたときの一撃（ゾンビの 3 倍）。**めったに届きません** —— 床から 2.5 浮くので、平地のプレイヤーとは `ATTACK_HEIGHT`(1.5) 以上離れる |
| `mobs.ts` `BLAZE.ranged.damage` | 5 | 本家 | 火球 1 発の重み（体力 20 なので 4 発で死ぬ） |
| `mobs.ts` `BLAZE.ranged.range` / `near` | 16 / 3 | 本家 / 目分量 | 撃ってくる間合い。`near` は「近すぎると撃たない」線（0 にすると足元へ撃って近接と二重取り） |
| `mobs.ts` `BLAZE.ranged.cooldown` | 3 秒 | 目分量 | 火球の間隔。**本家の 3 連射は入れていません**（1 発ずつ）。詰めると避けきれなくなる |
| `mobs.ts` `BLAZE.ranged.height` | 1.1 | 目分量 | 火球が出る高さ（足元から）。下げると自分の足場に当たる |

**近接ダメージは種類ごとに持つようになりました**（`MobDef.damage`。2-4b）。
`MOB_DAMAGE` は `ZOMBIE.damage` の別名なので、**新しいモブをそこへ繋がないこと。**

## モブ（エンダーマン）

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `mobs.ts` `ENDERMAN.maxHealth` | 40 | 本家 | 何回殴れば倒れるか（ダイヤの斧 5 で 8 回）。**跳んで逃げるので、実際にはもっと長く感じます** |
| `mobs.ts` `ENDERMAN.damage` | 7 | 本家 | 近接の一撃（ゾンビ 2 / ブレイズ 6 より重い）。**3 発で死にます** |
| `mobs.ts` `ENDERMAN.speed` | 5 | 目分量 | **本家の比なら 6.0** ですが、`ZOMBIE` の「プレイヤーの歩き 5.2 より遅く」の線に揃えました。跳んで詰めてくるので、速さまで上回ると走っても逃げ切れません |
| `mobs.ts` `ENDERMAN.drop.chance` | 0.5 | 本家 | エンダーパールの集まり方（エンドポータルに 12 個要る） |
| `mobs.ts` `ENDERMAN.spawnWeight` | 10 | 本家 | ゾンビ (100) との出やすさの比。上げると夜がずっと危なくなります |
| `mobs.ts` `ENDERMAN.teleport.range` | 16 | 目分量 | 殴られて逃げる距離。広げると見失いやすく、狭めるとすぐ殴り直せます |
| `mobs.ts` `ENDERMAN.teleport.closeIn` | 5 | 目分量 | 追うときにプレイヤーのどれだけ近くへ出るか。**0 にしないこと**（目の前に湧いて即殴られます） |
| `mobs.ts` `ENDERMAN.teleport.vertical` | 8 | 目分量 | 上下に探す幅。広げると屋根の上や穴の底へ跳びます |
| `mobs.ts` `ENDERMAN.teleport.tries` | 8 | 目分量 | 行き先を探す回数。**費用はここだけ**（1 回につき地面 1 本の走査）。減らすと狭い所で跳ばなくなります |
| `mobs.ts` `ENDERMAN.teleport.cooldown` | 1.5 秒 | 目分量 | 跳ぶ間隔。詰めると追い付けず、空けると殴り放題になります |
| `mobs.ts` `ENDERMAN.teleport.hurtChance` | 0.5 | 目分量 | 殴られて跳ぶ確率。**1 にしないこと**（一発も当てられなくなります） |
| `mobs.ts` `ENDERMAN.teleport.chaseAt` | 8 | 目分量 | これより遠い相手には跳んで詰める。`HOSTILE_SIGHT`(18) より小さいこと |

**本家と違って中立ではありません**（見つめると怒る、が無い）。ゾンビと同じ
「暗い所に湧いて襲ってくる」側です。中立にするなら `hostile` の使われ方
（湧きの枠・逃げ方・日光・ベッド）をまとめて考え直すことになるので、
**手触りの調整の周ではなく、別のタスクとして決めてください。**

## クリア導線の集まり具合（2-6）

**倒す数ではなく「何回そこへ通うか」を決める数値です。** 手触りの中でも
**一括調整のときに真っ先に見るべき側**で、ここを触るとネザー要塞に通う回数が直に変わります。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `crafting.ts` ブレイズパウダーの `count` | 2 | 本家 | ロッド 1 本から何個できるか。**枠 12 個ぶんがロッド 6 本で足りるのはこの 2 のおかげ**。1 にすると要塞に通う回数が倍になります |
| `crafting.ts` エンダーアイの `count` | 1 | 本家 | パウダー 1 + パール 1 で 1 個。**増やすとパール（＝エンダーマンを倒す数）が丸ごと減ります** |
| `items.ts` `ENDER_EYE` の `stack` | 64 | 本家 | 1 枠に持てるアイの数。**12 未満にしないこと**（エンドポータルぶんが 1 枠に収まらなくなります。`test/crafting.test.ts` が見張っています） |
| `items.ts` `BLAZE_POWDER` の `stack` | 64 | 本家 | 1 枠に持てるパウダーの数 |
| `items.ts` `ENDER_EYE` の色 | `0x3fbf8c`（明るい青緑） | 目分量 | 持ち物欄での見え方。**エンダーパール（`0x11726b`）と見分けが付く必要があります**（並ぶので） |
| `items.ts` `BLAZE_POWDER` の色 | `0xe8a33d`（橙） | 目分量 | 同上。ブレイズロッド（`0xf2c033`）より少し赤寄りにしてあります |

**ロッドの落ちる確率（0.5）とパールの落ちる確率（0.5）は「戦闘・ドロップ」の節**にあります。
通う回数を決めているのはこの 2 つとの掛け算なので、**片方だけ触らないこと。**

## 要塞の遠さとエンダーアイ（2-7）

**「どれだけ歩かされるか」を決める数値です。** ここを触ると、エンドへ行くまでの
旅の長さがそのまま変わります。**要塞そのものを建てるのは 2-8** なので、
いまはまだ「アイがそちらを向く」だけです。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `stronghold.ts` `STRONGHOLD_SPACING` | 24 列（= 384 マス四方に 1 個） | 目分量 | **要塞までの遠さ。** 実測で 34〜372 マス（中央 153）。**本家の 1408 マス以遠にはしていません** —— 移動手段が歩きと泳ぎだけなので、片道 20 分の旅になります。ネザー要塞（オーバーワールド換算 200〜270 マス相当）と釣り合う距離にしてあります |
| `stronghold.ts` `STRONGHOLD_CHANCE` | 1（必ず建つ） | 目分量 | **下げないこと。** 要塞はクリア導線の唯一の通り道なので、「近くのマスに建たなかった」が起きるとアイの指す先が 2 マス向こうのグリッドへ飛びます。遠さは `STRONGHOLD_SPACING` で決めてください |
| `stronghold.ts` `EYE_RISE` | 0.35（水平 1 に対して） | 目分量 | 投げたアイが上へ向く量。**0 にすると地面すれすれを飛ぶ**ので、丘の向こうへ行った先が見えません。大きくすると真上へ抜けて、方角が読み取りにくくなります |
| `projectiles.ts` `eye` の `speed` / `life` | 8 m/s / 12 秒（＝ 96 マス飛ぶ） | 目分量（1-8 で置いた） | 投げてから消えるまで。**本家のように「12 マスほど飛んで地面に落ちる」形にはしていません** —— 落ちたアイを拾い直す仕掛けが要ります |
| **アイは投げても減らない** | 減らさない | **本家と違う** | 本家は 20% で壊れ、残りは地面に落ちて拾い直せます。**減らす形にすると、枠 12 個ぶんを集めきる前に探索でアイを使い切る詰みが作れます**（拾い直す仕掛けが無いため）。壊れる確率を入れるなら、落ちたアイを拾える形と一緒に決めてください |

**`STRONGHOLD_SPACING` を触ったら `test/stronghold.test.ts` の出力を見ること。**
「要塞まで 〜 マス（中央 〜）」を毎回出します。

## 要塞の部屋（2-8）

**「掘り当てるまでの手間」と「見つけたときの手応え」を決める数値です。**
場所の数値（間隔・割合）は上の「要塞の遠さとエンダーアイ（2-7）」にあります。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `stronghold.ts` `STRONGHOLD_DEPTH` | 18（地面から床までのマス数） | 目分量 | **掘り下げる手間。** 天井までは 13 マスなので、ダイヤのツルハシで 20 秒ほど。**24 以上にしないこと** —— 地形のいちばん低い所は高さ 25 前後なので、海の底の要塞が岩盤（y=0）を突き抜けて床が欠けます（`test/stronghold.test.ts` が最低値を毎回出します）。浅くすると土かぶりが薄くなり、少し掘っただけで露出します |
| `stronghold.ts` `STRONGHOLD_HALF` | 6（外側 13 マス四方 / 内側 11 マス四方） | 目分量 | **掘り当てる的の大きさ。** 投げたアイは「その辺り」までしか案内しない（近づくと落ちる仕掛けが無い）ので、**狭くすると当てそこねて詰みに近づきます** |
| `stronghold.ts` `STRONGHOLD_HEIGHT` | 4（内側の高さ） | 目分量 | 部屋の広さの感じ。2 まで下げると天井が頭に付きます |
| `stronghold.ts` `STRONGHOLD_RING` | 2（＝ 5x5 の縁で枠 12 個） | **本家** | **触らないこと。** 12 個でなくなると、エンダーアイの必要数（＝ロッドの必要数）がまるごと変わります |
| `blocks.ts` `FRAME_HEIGHT` | 0.8125（13/16） | **本家** | 枠の高さ。`PLAYER_SIZE.step`(0.6) より高いので**歩いて乗り越えられず、輪に入るには跳ぶ**ことになります。0.6 以下にすると歩いて入れます |
| `blocks.ts` エンドポータル枠の `hardness` | 無限（壊せない） | **本家** | **下げないこと。** 掘れると、起動する前に枠を壊してクリアできなくなる詰みが作れます |
| `blocks.ts` `STONE_BRICK` の色 | `0x7d8288` | 目分量 | 石（`0x8a8f96`）と丸石（`0x767b82`）のあいだ。**模様が無いので、本家より見分けにくいはずです**（`REVIEW.md` の 2-8 で見てもらっています） |
| `blocks.ts` 枠の色 | 枠 `0x8f9b74` / アイ入りの上面 `0x3fbf8c` | 目分量 | アイが嵌まったかどうかは**色と、上面に乗る小さい箱**で分かります。アイ入りの色はアイテムのエンダーアイと同じにしてあります |
| **部屋の松明 4 本** | 壁の真ん中・床から 3 マス | 目分量 | 明かりが 1 つも無いと、掘り当てた部屋が真っ暗なうえ敵対モブが湧き続けます。**本家の要塞に松明はありません**（あちらは通路が長く、明かりを持って潜る前提） |

## エンドポータルの起動（2-9）

**「アイを何個集めるか」と「起動したときの手応え」の数値です。**
輪の形（枠 12 個）は上の「要塞の部屋（2-8）」の `STRONGHOLD_RING` にあります。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `blocks.ts` `END_PORTAL_LIGHT` | 15（溶岩・グロウストーンと同じ最大値） | **本家** | 起動した部屋の明るさ。松明（14）より明るいので、**点いたことが目で分かる**のはこの 1 行です。下げると、松明 4 本に埋もれて「点いたのかどうか」が分かりにくくなります |
| `blocks.ts` エンドポータルの色 | 上面 `0x2a1d52` / 側面 `0x1a1136` / 下面 `0x120b26` | 目分量 | 本家の「星空」は**テクスチャが無いと出せない**ので、暗い紫の無地にしてあります。自分で光る（`emission` 15）ぶん、真っ黒よりは見えるはずです（`REVIEW.md` の 2-9 で見てもらっています） |
| `blocks.ts` エンドポータルの高さ | `FRAME_HEIGHT`（13/16）と同じ | 目分量 | **枠の上面に合わせてあります。** 1 にすると、膝までの枠の輪から面だけが飛び出して見えます。**枠の値を写さずに読んでいる**ので、片方だけずれることはありません |
| `blocks.ts` エンドポータルの `hardness` | 無限（壊せない） | **本家** | **下げないこと。** 起動したあとに消せると、アイを 12 個使い切ったのに入れない詰みが作れます |
| `blocks.ts` エンドポータルの `solid` | false（通り抜ける） | **本家** | 面の上に立てず、輪に跳び込むと落ちます。true にすると床になって、2-10 の「踏んだら移る」が書けなくなります |
| **輪の内側にブロックがあっても起動する** | 上書きする | 目分量 | 枠が壊せない以上、輪の中にブロックを置いて起動を止められると詰みます。**止める側に倒さないこと**（`endportal.ts` の `activate()`） |
| **投げるより嵌めるが勝つ** | 枠を狙っているときだけ嵌める | 目分量 | `main.ts` の順番。逆にすると、枠を狙ってもアイが飛んでいって永久に嵌まりません |

## エンドの島と、エンドへの移動（2-10）

**「島がどれくらいの大きさか」と「どこに出るか」の数値です。**
本家に合わせられるのは形だけで、**大きさはこの世界の描画距離に合わせて縮めてあります。**

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `endgen.ts` `ISLAND_RADIUS` | 48 | 目分量（本家は 55〜60） | 島の広さ。**描画距離が短いので、大きくすると「島の上に居る」ことが見て分かりません**（端が霧の向こう）。小さくするとドラゴンの戦場（2-13）が狭くなります |
| `endgen.ts` `ISLAND_SURFACE` | 48 | 目分量 | 島の上面。オーバーワールドの海面（40）より少し上。**下げすぎると島の下面が世界の底（0）に刺さります**（テストが「いちばん深い下面」を毎回出します。いまは 31） |
| `endgen.ts` `SURFACE_WOBBLE` | ±3 | 目分量 | 上面のうねり。0 にすると鏡のように平らな円盤になります。大きくすると、出た所から歩き出すのに段差を登ることになります |
| `endgen.ts` `THICKNESS_CENTRE` / `THICKNESS_EDGE` | 18 / 3 | 目分量 | 島の厚み。**ふちを 1 にしないこと** —— 掘り抜きやすくなり、下は虚空です |
| `endgen.ts` `EDGE_WOBBLE` | 0.25（半径の ±25%） | 目分量 | ふちの揺れ。実測でふちの半径は 39.1〜57.4。**大きくすると島の中心まで食い込みます**（テストが「いちばん狭い所 > `LANDING_RADIUS * 2`」を見ています） |
| `endgen.ts` `LANDING_RADIUS` / `LANDING_RAMP` | 5 / 10 | 目分量 | 出る所のまわりを平らにする半径と、うねりへ繋ぐ距離。**本家に無い仕掛けです** —— 崖の途中に出ると出た瞬間に落ちるので入れました。`LANDING_RAMP` を 0 にすると 3 マスの段差が輪になって出ます |
| `endgen.ts` **岩盤を置かない** | 底も天井も無し | **本家** | 島から落ちたら `VOID_Y`(-5) まで落ちて死にます。**塞ぐと虚空の意味が消えます** |
| `portaltravel.ts` `END_SEARCH` | ±8 マス | 目分量 | 出る所に地面が無かったときに探す広さ。**`primeAround(_, _, 1)` の 1 列ぶんに収めること**（外は未読み込みで、地面があっても見えません） |
| **エンドへは座標を掛けない** | 島の中心に固定 | 目分量（本家は固定の黒曜石の台） | 掛けると、要塞が原点から遠い人（＝ほぼ全員）が虚空に出て即死します |
| **エンドからは戻れない** | 出口ポータルはまだ無い | **本家**（ドラゴンを倒すまで開かない） | 落ちて死ねばリスポーン地点へ戻れるので詰みにはなりません。出口を建てるのは 2-13 |
| `blocks.ts` `END_STONE` の硬さ | 3.0 / `pickaxe` / `TIER_WOOD` | **本家** | 石（1.5）の倍。**虚空に浮いた島の床**なので、掘り抜いて落ちるまでに手間が掛かるほうがよい |
| `blocks.ts` `END_STONE` の色 | 上面 `0xdfe1a4` / 側面 `0xd6d89b` / 下面 `0xc8ca8d` | 目分量 | 本家の淡い黄白色。**砂（`0xd8c99a`）とかなり近い**ので、並べたときに見分けが付くか見てもらっています（`REVIEW.md` の 2-10） |

## エンドの黒曜石の柱（2-11a）

**「戦場をどう区切るか」の数値です。** 位置も高さも**種に依りません**（誰のワールドでも
同じ配置）。上に載るエンドクリスタル（2-11b）とドラゴン（2-13）が、この表を読みます。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `endgen.ts` `PILLAR_COUNT` | 10 本 | **本家** | クリスタルの数 = ドラゴンが回復する回数（2-13）。減らすと戦いが短くなります |
| `endgen.ts` `PILLAR_RING` | 28 | 目分量（本家は 42.5） | 輪の半径。**`PILLAR_RING + PILLAR_MAX_RADIUS < EDGE_MIN_REACH`(36) を保つこと** —— 超えると、ふちが内側へ揺れた種で柱が虚空に浮きます。小さくすると出る所（`LANDING_RADIUS` 5）に近づきます |
| `endgen.ts` `PILLAR_HEIGHTS` | 12〜39（10 段階） | **本家**（上面 y76〜103 / 島 y64） | 柱の高さ。**高い・低いを交互に並べてあること** —— 昇順にすると輪が一周する坂に見えます（テストが見ています） |
| `endgen.ts` `PILLAR_MAX_RADIUS` / `PILLAR_THICK_FROM` | 3 / 高さ 27 以上 | **本家**（高い柱ほど太い） | 柱の太さ（低い 5 本は半径 2 = 直径 5、高い 5 本は半径 3 = 直径 7）。太くすると足元の日陰が広がります |
| `endgen.ts` **上面を絶対の高さで止める** | `ISLAND_SURFACE + height` | 目分量 | 柱の上面が平らになり、島のうねり（±3）は根元で吸収されます。**列ごとの上面から測る形にすると上面が凸凹になり、クリスタルが傾いて載ります** |
| `endgen.ts` **柱の上を 2 マス空ける** | `top + 1` / `top + 2` が空気 | 目分量 | エンドクリスタル（2-11b）の置き場。テストが 5 種 x 10 本で見ています |
| `endgen.ts` `EDGE_MIN_REACH` | 36（= `ISLAND_RADIUS * (1 - EDGE_WOBBLE)`） | 目分量（式） | **柱を立ててよい半径の限界。** 実測した最小のふち（ある種で 39.1）で線を引かないこと —— 別の種で虚空に柱が浮きます |

## エンドクリスタル（2-11b）

**「どれくらい壊しやすいか」の数値です。** ドラゴンの回復（2-13）が
「生きているクリスタルの数」に直に効くので、ここを触ると戦いの長さが変わります。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `blocks.ts` `END_CRYSTAL` の `hardness` | 0.2（素手で 1 秒） | 目分量（本家は**当たれば必ず壊れる**） | 壊しやすさ。**0 にしないこと** —— 掠っただけで壊れると、柱に登る途中で当たって消えます。大きくすると、矢 1 本では壊れなくなります（いまは 1 本で壊れます） |
| `blocks.ts` `END_CRYSTAL` の `emission` | 15（`END_PORTAL_LIGHT`） | 目分量（本家も光る） | 柱の上の見つけやすさ。エンドは薄暗い（空の明るさ 0.7 固定）ので、下げると遠くの柱の上が見えません |
| `blocks.ts` `END_CRYSTAL` の `tool` / `minTier` | なし / 素手 | 目分量 | 道具を要ると、柱の上まで登ってツルハシを持っていない人が詰みます |
| `blocks.ts` `END_CRYSTAL_BOX` | 3 段（0.25 → 0.625 → 0.25 幅） | 目分量 | 見た目と当たり判定（同じ箱です）。**細くすると矢が当たりにくくなります** |
| `items.ts` `DROPS[END_CRYSTAL]` | 何も落ちない | **本家**（爆発して消える） | **拾えるようにしないこと** —— 柱の上へ運び直して、ドラゴンの回復を復活させられます |
| **爆発しない** | ダメージ 0 | 本家と違う（本家は爆発してプレイヤーも吹き飛ぶ） | 手で壊しても無傷です。爆発を入れるなら、火・爆発の仕組みから作ることになります |

## 弓と矢（2-12）

**「どれくらい引けば、どれだけ痛いか」の数値です。** 弓はエンドクリスタルを
離れた所から壊す手段なので、ここを触るとドラゴン戦（2-13）の組み立てが変わります。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `bow.ts` `DRAW_SECONDS` | 1.0 秒 | **本家** | 満引きまでの長さ。短くすると連射になり、矢の消費が増えます |
| `bow.ts` `MAX_ARROW_DAMAGE` | 9 | **本家**（満引きの弓） | 満引きで当たったときの減り。ゾンビ（体力 12）は 2 発、ブレイズ（20）とエンダーマン（40）は 3 発・5 発 |
| `bow.ts` `MIN_ARROW_DAMAGE` | 1 | 目分量 | 放てる最小の重み。**0 にしないこと** —— 当たっても何も起きない矢は、外したのと区別が付きません |
| `bow.ts` `MIN_POWER` | 0.2（＝ 0.2 秒） | 目分量（**本家は浅くても飛ぶ**） | ここ未満では放たず、矢も減りません。本家は浅いほど矢が遅く飛びますが、**こちらは速さが `PROJECTILE_KINDS` の固定値**（矢 40m/s）なので、真似ると「浅く連打するほど強い」になります。速さを撃つ側から渡す形にするなら、この下限は外せます |
| `bow.ts` `SHOOT_HEIGHT` | 1.5（足元から） | 目分量（目の高さ） | 撃ち出す高さ。0 にすると、放った矢が自分の足元に刺さります（テストが見ています） |
| `bow.ts` `FULL_DRAW_PITCH` | 1.6 倍 | 目分量 | 満引きの合図の音程（放つ音を高くして鳴らす）。**この環境では引く動きを描けないので、満引きは音でしか分かりません** |
| `sfx.ts` `EVENTS.bow` | 520Hz / 0.22 秒 / 下がる | 目分量 | 弦の音。**耳で確かめてください** |
| `crafting.ts` 弓の材料 | 棒 3 + **羊毛** 3 | 本家と違う（本家は糸 3） | 蜘蛛がまだ居ないので羊毛で代用。糸を出す敵を足したら差し替えてください（**羊毛はベッドと取り合いになります**） |
| `crafting.ts` 矢の材料 | 火打石 + 棒 → **4 本** | 本家と違う（本家は羽根も要る） | 鶏がまだ居ないので 2 つで作れます。**砂利から火打石が出る**ので、弓が作れる頃には材料が揃っています |
| `items.ts` 矢のスタック | 64（弓は 1） | **本家** | 1 本ずつ枠を食うと使い物になりません |
| **矢は拾い直せない** | 刺さった矢は 60 秒で消える | 本家と違う（本家は拾える） | 撃ちっぱなしなので、矢の作り足しが要ります。拾えるようにするなら `projectiles.onExpire` から `drops` へ渡すところから |
| **クリエイティブでは矢が減らない** | 減らさない | **本家** | ブロックを置くのと同じ扱い |

## エンダードラゴン（2-13a）

**ボスの数値。** クリア導線の最後なので、**ここを触ると「クリアできるかどうか」が変わります。**
とくに**回復（`regen`）と矢の重み（`MAX_ARROW_DAMAGE` 9）の釣り合い**が、
「クリスタルを先に落とす」という導線そのものです。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `mobs.ts` `DRAGON.maxHealth` | 200 | **本家** | 倒すのに要る矢の数（満引き 9 なら 23 本）。ダイヤの斧なら 40 回 |
| `mobs.ts` `DRAGON.damage` | 10 | **本家**（ノーマル） | 降りてきたときの一撃。体力 20 に対して**2 発で半分以下**。3 発耐えられません |
| `mobs.ts` `DRAGON.speed` | 7 | 目分量 | 詰めてくる速さ。**プレイヤーの走り（8.4）より遅いこと** —— 速いと壁も崖も関係なく飛んでくるので、絶対に振り切れません |
| `mobs.ts` `DRAGON.regen` | 2（もと 1 個につき毎秒） | 目分量（本家はビームで速く回復） | **クリスタル 10 個で毎秒 20。** 矢（9 / 約 1.5 秒）でも斧（5 / 0.5 秒 = 毎秒 10）でも削り切れません。**下げすぎると柱に登らずに倒せます** |
| `mobs.ts` `DRAGON.orbit.radius` | 20 | 目分量 | 回る輪の半径。**柱の輪（`endgen.ts` の `PILLAR_RING` 28）より内側にすること** —— 外だと柱に体当たりし続けます |
| `mobs.ts` `DRAGON.orbit.height` | 12 | 目分量 | 輪の高さ（島の地面から）。**低い柱（12）と同じくらい**にしてあります。上げると矢が届きません |
| `mobs.ts` `DRAGON.orbit.diveAt` | 16 | 目分量 | ここまで近づかれると輪を離れて詰めてきます。**半径 20 より小さいこと** —— 大きいと中心に立つだけで always 突っ込んできて、輪が 1 度も見えません |
| `mobs.ts` `DRAGON.orbit.turn` | 0.35 rad / 判断 | 目分量 | 1 周 30 秒ほど。上げると内側へ切り込み、下げると外へ膨らみます（速さとの釣り合い） |
| `mobs.ts` `DRAGON.hover` | 1.2 | 目分量 | 詰めてきたときに床から浮く高さ。**`ATTACK_HEIGHT`(1.5) より低いこと** —— ブレイズと同じ 2.5 にすると、目の前まで来ても**一度も殴れないボス**になります |
| `mobs.ts` `DRAGON.size` | 4 x 1.8 | 目分量（本家はもっと大きい） | 当たり判定。矢の当てやすさがそのまま変わります |
| `mobs.ts` `BOSSES.end` | 中心 (0, 0) | 島の中心（`endgen.ts` の `END_SPAWN`） | 回る中心。**湧くのは輪の上**（中心 + 半径）で、中心に湧かせるとポータルから降りた人の真上に出ます |
| `mobs.ts` `DRAGON.phases` | 近接 10 秒 → ブレス 12 秒 → 旋回 8 秒（1 周 30 秒） | 目分量（本家の「突っ込む・ブレス・旋回」に倣った 3 つ） | 攻め方の回り方。**「何も仕掛けない番（旋回）」を消さないこと** —— 柱のクリスタルを割りに行ける唯一の隙です。近接を長くすると輪が見えず、ブレスを長くすると地上に居られません |
| `mobs.ts` `DRAGON.phases` ブレスの `above` | プレイヤーの 10 マス上 | **ユーザーの指定**（`REVIEW.md` 2-14） | 撃ち下ろす高さ。**中心の地面ではなくプレイヤー基準**なので、柱の上に登っても頭を取られます。下げると矢は当てやすくなりますが、ブレスも避けにくくなります |
| `mobs.ts` `DRAGON.ranged.cooldown` | 1.5 秒に 1 発 | **ユーザーの指定**（`REVIEW.md` 2-14） | ブレスの発射レート。番 12 秒で 7〜8 発。上げると避ける暇がなくなります |
| `mobs.ts` `DRAGON.ranged.damage` | 6 | 目分量（近接 10 とブレイズの火球 5 のあいだ） | ブレス 1 発の重み。**体力 20 に対して 3 発**。近接（10）より軽いのは、避けようのない高さから撃つためです |
| `mobs.ts` `DRAGON.ranged.range` | 4〜40m | 目分量 | 撃つ間合い。**輪の半径（20）より長いこと** —— 短いと、中心に立つ相手にすら 1 発も届かない番ができます。ブレイズの「`HOSTILE_SIGHT` より短く」は当てはまりません（あちらは自分から寄っていくモブ） |
| `projectiles.ts` `breath` の弾道 | 落ちない・抵抗なし・30m/s（火球 12 の 2.5 倍） | **ユーザーの指定**（`REVIEW.md` 2-14） | ブレスの飛び方。**火球とまったく同じ形**にしてあります。山なりに戻すと、10 マス上から撃ち下ろしたときに狙った所より手前へ落ちます |
| **何も落とさない** | ドロップ無し | 本家と違う（本家は経験値と卵） | 経験値も卵もまだありません。倒した合図は体力バーとクリア画面（2-13b）の仕事です |

## エンドの出口ポータル（2-13b-1）

**倒した印であり、帰り道でもあります。** 数値は 2 つだけで、どちらも
「降りた人が着いた瞬間に持っていかれない」ことのために置いてあります。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `endgen.ts` `EXIT_PORTAL_SPOT` | 島の中心から +X へ 3 マス（y は地面の 1 つ上） | 目分量（本家は島の中心ちょうどで、降りる所のほうが 100 マス離れている） | **降りる場所（`END_SPAWN` = 原点）と重ねないこと** —— 重なると、着いた瞬間に面の中に立って引き返されます。**3x3 が `LANDING_RADIUS`(5) の内側に収まること**（外はうねるので、面の下に段差ができます）。テストが両方を見ています |
| `exitportal.ts` `EXIT_PORTAL_RADIUS` | 1（＝3x3） | 要塞のエンドポータル（`stronghold.ts` の `RING_INSIDE`）と同じ | 面の大きさ。行きと帰りで違うと、片方だけ踏み外します。広げるときは `LANDING_RADIUS` に収まるかを見ること |
| **出口は 1 つだけ** | 島の中心の脇 | 本家と違う（本家は倒すと出口ポータルと**ゲートウェイ**が開く） | 外側の島がまだ無いので、ゲートウェイは行き先がありません |
| **戻る先はいまの座標** | オーバーワールドの原点あたり | 本家と違う（本家はリスポーン地点） | `portaltravel.ts` の `planTravel()`。エンドの座標をそのまま使うので、原点のあたりに出ます。ベッドへ戻す形にするなら `beds.ts` を引き込むことになります |

## ボスの体力バーとクリア画面（2-13b-2）

**手触りというより「読めるかどうか」の数値です。** 帯の大きさと位置は
ブラウザでしか確かめられないので、**気に入らなければここを見て直してください**
（判断は `boss.ts`、見た目は `style.css`。数値をどちらにも写していません）。

| 場所 | いま | 出典 | 触ると |
| --- | --- | --- | --- |
| `boss.ts` `bossBarState()` のラベル | `エンダードラゴン　132 / 200`（残りは**切り上げ**） | 目分量（本家は名前だけで数字を出さない） | **残りの数を消さないこと** —— 棒だけだと、クリスタルの回復と矢のダメージが釣り合っているのかが目で分かりません。切り上げをやめると、残り 0.4 が「0」と出て倒したように見えます |
| `boss.ts` `victoryMessage()` | 「〜を倒しました　出口ポータルが開いています（踏むと帰れます）」 | 目分量 | **帰り道に触れておくこと** —— 出口ポータルは音も通知もなく島の中心の脇に建つので、書かないと「倒したのに何も起きていない」ように見えます |
| `style.css` `#bosstrack` の幅 | `min(46vw, 360px)` / 高さ 9px | 目分量（本家はもっと横長） | 帯の大きさ。広げると F3 のデバッグ表示（左上）に近づきます |
| `style.css` `#bossbar` の位置 | 画面上端から 14px・中央 | 目分量（本家も上端中央） | 下げるとクロスヘアに近づきます |
| `style.css` `#bossfill` の色 | 紫のグラデーション（`#7b4bd0` → `#c98bff`） | 目分量（エンドの空とドラゴンの目の色に寄せた） | **減った側（`#bosstrack` の地）と明るさを近づけないこと** —— どこまで削れたのか分からなくなります |
| `#victory` の暗幕 | `rgba(26, 12, 48, 0.6)` | 死亡画面（`rgba(74, 12, 12, 0.55)`）と同じ濃さで色だけ変えた | 濃くすると後ろの島が見えなくなります |
