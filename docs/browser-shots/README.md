# ブラウザで撮った絵（2026-09-02）

**このプロジェクトを本物のブラウザで動かして撮った 5 枚**です。撮り方は
`tools/browsershot.mjs`（`node tools/browsershot.mjs`）。`docs/` は誰も読まない
置き場なので、ここに置いてあります。

**`CLAUDE.md` の「この環境では WebGL が動かない」は手元の devcontainer の話で、
Claude Code のクラウドのサンドボックスには当てはまりません。** あちらには
Chromium 141 と playwright が入っていて、SwiftShader のソフト描画で普通に動きます
（GPU は無い。`/dev/dri` が無いので 20fps 前後）。

| 撮った条件 | 値 |
| --- | --- |
| 種 | **4242**（`npm run shot` と同じ） |
| 時刻 | 07:32（メニューの既定 07:12 から少し進んだところ） |
| 立ち位置 | xyz 0.5 46.0 0.5・バイオーム 平原・オーバーワールド |
| 大きさ | 960x600 |
| WebGL | `WebGL 2.0 (OpenGL ES 3.0 Chromium)` / `gl.getError()` = 0 |
| ポインタロック | **効いた**（ヘッドレスでも `#hud` と `#crosshair` が出た） |

| ファイル | 写っているもの | バイト |
| --- | --- | --- |
| `menu.png` | 最初のメニュー（種 4242 を入れたところ） | 277,645 |
| `game.png` | 地形だけ（`#viewport`。HUD を退けて撮った） | 44,901 |
| `hud.png` | 体力・空腹・ホットバー・十字（F3 の文字は消してある） | 49,220 |
| `debug.png` | F3 のデバッグ表示つき | 89,066 |
| `inventory.png` | `E` で開いたインベントリ | 59,451 |

**真っ黒でないことの裏取り**（`browsershot.mjs` が撮るたびに出します）:
`game.png` は色 **1,147 種**、上 15%（空）が rgb(47,97,181)、下 15%（地面）が
rgb(103,165,75)。三角形は 323,266 枚・チャンク 1,704 個。

**日本語は 1 文字も欠けていません**（漢字・かな・全角の括弧・`→` まで出ています）。

## `npm run shot` の絵との食い違い —— sRGB の伝達関数 1 回ぶん

**同じ種・同じ時刻で、CPU 側の絵が systematically 暗い**（`npm run shot -- ground --time 0.064`）。
草の上面をぶつけると:

| | 赤 | 緑 | 青 |
| --- | --- | --- | --- |
| `tools/raster.ts`（CPU） | 37 | 100 | 20 |
| ブラウザ（three） | 106 | 168 | 79 |
| CPU の値に sRGB の符号化を掛けた値 | 106.3 | 168.1 | 79.1 |

`1.055 * x^(1/2.4) - 0.055` を通すと **3 チャンネルとも ±1 で一致**します。
three の `WebGLRenderer` は `outputColorSpace = SRGBColorSpace` が既定で線形 → sRGB を
掛けますが、`raster.ts` は線形の値をそのまま 8 ビットへ丸めています（`raster.ts:243`）。

**この食い違いは直しました**（`raster.ts` の `srgb()`。混ぜるのも three と同じく
符号化したあとにしてあります）。直したあとに同じ絵を撮り直して突き合わせると、
地面の代表色は **両方とも rgb(106,168,79) と rgb(94,156,65)** で一致します。
判定は `test/shot.test.ts` が画素の値で持っています（正面 239 / 上面 255 /
底面 188 / 半透明 119）。

空は別経路（ブラウザは `sky.ts` の天球 GLSL、CPU は `daynight.ts` の 2 色の勾配）なので、
そもそも一致しません（CPU rgb(16,57,146) / ブラウザ rgb(46,96,181)）。

## ついでに見つかったもの

**通知（`#status`）がインベントリの説明文とメニューのモード行に重なります。**
`#status` は `bottom: 118px` 固定（`style.css:674`）で、960x600 だと帯は y 452〜482。
そこに来るのは**インベントリのパネル末尾の操作説明（`<p class="lead">` の 1 行目）**と、
**メニューのモード行**です（`#recipehint` ではありません —— あちらは作業台の出力の真下で、
250px 離れています）。オートセーブは 15 秒ごと・通知は 3 秒出るので、**インベントリを
開いている時間の 2 割ほどで説明文が読めません**（`menu.png` では「シード 4242 で
作り直しました」が `#mode` のボタンを丸ごと覆っています）。

**`REVIEW.md` に積んであります**（この周は撮るだけなので直していません）。

## 6 枚目: `creative-swords.png`（2026-09-02・剣 4 本を入れた周）

**クリエイティブの一覧を末尾までスクロールして撮ったもの**です（上の 5 枚と違い、
メニューで `#mode` をクリックしてクリエイティブで始め、`E` を押してから
`#creative` の `scrollTop` を最後まで送っています）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **82 個**（`allItemIds()` そのまま。剣を入れる前は 78） |
| 末尾に並んだもの | 弓 → 矢 → **木の剣 / 石の剣 / 鉄の剣 / ダイヤの剣** |
| 色 | 道具と同じ階層の色（茶 / 灰 / 白 / 水色。`TIER_COLORS`） |
| 名前 | **4 つとも枠の下に出ています**（欠けも溢れもなし） |

**`MAX_ITEM_ID` が 94（矢）から 114（ダイヤの剣）へ飛んでも、95..110 の穴は
一覧に出ません**（`allItemIds()` が `ITEMS[id]` の無い番号を飛ばす）。
**この絵はそれを目で確かめたものです。**

## 7 枚目: `creative-shears.png`（2026-09-02・シアーズを入れた周）

**6 枚目と同じ手**（メニューで `#mode` をクリックしてクリエイティブで始め、`E` を押してから
`#creative` の `scrollTop` を最後まで送る）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **83 個**（剣を入れた周の 82 から +1） |
| 末尾に並んだもの | 矢 → 木の剣 / 石の剣 / 鉄の剣 / ダイヤの剣 → **シアーズ** |
| 色 | `0xa8b8c0`（青みの灰色）。**石の剣（`0x8a8f96`）より明るく、ダイヤの剣の水色とは別物** |
| 名前 | **枠の下に「シアーズ」がそのまま出ています**（欠けも溢れもなし） |
| console のエラー | **なし** |

**`MAX_ITEM_ID` が 115 になっても、95..110 の穴は一覧に出ません**（`allItemIds()` が
`ITEMS[id]` の無い番号を飛ばす）。**6 枚目と同じことを、番号が 1 つ増えた状態で
もう一度目で確かめたものです。**

**まだ見えないもの**: **刈られた羊の見た目**（`AUTODEV-QUEUE.md` の 11 番）。
いまは刈っても姿が変わらないので、**「その羊がもう刈れるかどうか」が画面から分かりません。**

## 8 枚目: `creative-hoes.png`（2026-09-02・クワ 4 本と耕地を入れた周）

**6・7 枚目と同じ手**（メニューで `#mode` をクリックしてクリエイティブで始め、`E` を押してから
`#creative` の `scrollTop` を最後まで送る）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **87 個**（シアーズの周の 83 から +4） |
| 末尾に並んだもの | ダイヤの剣 → シアーズ → **木のクワ / 石のクワ / 鉄のクワ / ダイヤのクワ** |
| 色 | 道具と同じ階層の色（茶 / 灰 / 白 / 水色。`TIER_COLORS`。剣と同じ並び） |
| 名前 | **4 つとも枠の下に出ています**（「ダイヤの / クワ」は 2 行に折れますが、ほかの長い名前と同じ扱い） |
| **耕地（116）** | **一覧に出ません**（`variantOf: DIRT` が効いている証拠） |
| console のエラー | **なし** |

## 9 枚目: `farmland.png`（2026-09-02・**実際に耕したところ**）

**絵を撮るだけでなく、本物のブラウザで機能を動かした最初の 1 枚**です。クリエイティブで
一覧から木のクワをホットバーへ入れ、草むらを 2 回刈ってから右クリックしました。

| 見たこと | 値 |
| --- | --- |
| `#debug` の `hand` | **木のクワ** |
| `#debug` の `target` | 耕す前 **`草 (2, 45, -4)`** → 耕したあと **`耕地 (2, 45, -4)`** |
| `edits` | **3**（草むら 2 + 耕地 1） |
| 見た目 | 緑の草原の中に**暗い茶色の 1 マス**（上面 `0x59422d`）。**十字の下にはっきり出ます** |
| console のエラー | **なし** |

**横から見ると土と見分けが付きません**（横・下は土のまま `0x6b533a`）。**上面だけが 2 割暗い**
ので、**平らな地面に並べたときに「耕した」と分かるかどうかは人の目でしか決められません**
（`TUNING.md` の「クワと耕地」）。

### 撮り方で踏んだもの（次に動かす人へ）

- **`page.mouse.click()` を使わないこと。** あれは先にポインタを動かすので、**ポインタロック中は
  視点が飛んで狙いが外れます**（`target -` になって `edits 0`。2 回落としました）。
  **`mouse.down()` / `mouse.up()` で押して離すだけ**にすること。
- **どちらが「下」かを決め打ちしないこと。** `movementY` を増やす向きが下とは限りません。
  **`#debug` の `target` の行を読みながら、地面を狙えるまで動かす**ほうが速く着きます。
- **平原は草むらだらけです。** 先に左クリックで刈らないと耕せません（草むらは `tilled()` が
  `AIR` を返す —— **正しい挙動**で、不具合ではありません）。
- 撮る道具は使い捨てのスクリプトで、**リポジトリには置いていません**（`tools/browsershot.mjs` は
  5 枚の定番だけ。クリエイティブの一覧と耕すところは、そのつど書いたほうが安いという判断です）。

## 10 枚目: `creative-seeds.png`（2026-09-02・小麦の種と苗を入れた周）

**6〜8 枚目と同じ手**（メニューで `#mode` をクリックしてクリエイティブで始め、`E` を押してから
`#creative` の `scrollTop` を最後まで送る）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **88 個**（クワの周の 87 から +1） |
| 末尾に並んだもの | シアーズ → 木 / 石 / 鉄 / ダイヤのクワ → **小麦の種** |
| 色 | `0x9aa85a`（くすんだ黄緑）。**クワ 4 本のどの色とも別物**で、隣り合っても見分けが付きます |
| 名前 | **枠の下に「小麦の種」がそのまま出ています**（欠けも溢れもなし） |
| **小麦の苗（121）** | **一覧に出ません**（`variantOf` を自分自身に向けたのが効いている証拠） |
| console のエラー | **なし** |

## 11 枚目: `wheat-crop.png`（2026-09-02・**実際に植えたところ**）

**9 枚目（`farmland.png`）の続きで、耕してから植えるまでを本物のブラウザで通したもの**です。
クリエイティブで一覧から木のクワと小麦の種をホットバーへ入れ、草むらを刈り、耕し、植えました。

| 見たこと | 値 |
| --- | --- |
| `#debug` の `target` | 刈る前 **`草むら (0, 46, -4)`** → 刈って **`草 (0, 45, -5)`** → 耕して **`耕地 (0, 45, -5)`** → 植えて **`小麦の苗 (0, 46, -5)`** |
| `#debug` の `hand` | 木のクワ → **小麦の種**（`Digit2` で持ち替え） |
| `edits` | **4**（草むら 2 + 耕地 1 + 苗 1） |
| 見た目 | 茶色の耕地 1 マスの上に**黄みの緑の十字**。**耕地の茶色の上でははっきり見えます** |
| console のエラー | **なし** |

**緑の草原の中では紛れます。** 苗（`0x6f8f3f`）と草（`0x5e9c41`）の差は色相のわずかな
黄み寄りだけで、**遠目には草むらと見分けが付きません**（`TUNING.md` の「小麦の種と苗」）。
畑を作った本人はどこに植えたか覚えているので詰まりはしませんが、**育つ段階が入る周に
2 色まとめて決め直すのが早い**と思います。

## 12 枚目: `creative-bread.png`（2026-09-03・パンを入れた周）

**6〜8・10 枚目と同じ手**（メニューで `#mode` をクリックしてクリエイティブで始め、`E` を
押してから `#creative` の `scrollTop` を最後まで送る）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **90 個**（種の周の 88 から +2 = 小麦と**パン**） |
| 末尾に並んだもの | 木 / 石 / 鉄 / ダイヤのクワ → 小麦の種 → 小麦 → **パン** |
| 色 | `0xc49a5e` = `rgb(196,154,94)`。**小麦（`rgb(216,194,106)`）とは隣り合っても見分けが付きます**（小麦は黄色、パンは茶色） |
| 名前 | **枠の下に「パン」がそのまま出ています**（`title` は `パン x64`。欠けも溢れもなし） |
| console のエラー | **なし**（残るのは SwiftShader の warning だけ） |

**ただしパンの茶色は、板・原木・棒の茶色と近い系統です。** 隣の小麦とは分かれますが、
**一覧の中で目立つかどうかは人の目でしか決められません**（`TUNING.md` の「パン」）。

**畑の見た目は 1 画素も変わっていません**（`npm run shot -- crops` を撮り直して確認。
この周は描画を 1 行も触っていないので、変わっていないことのほうが正解です）。

### 撮り方で分かったこと（9 枚目の続き）

- **`page.mouse.move()` では視点が動きません**（ポインタロック中の `movementX/Y` が出ない）。
  `main.ts` が読むのは `mousemove` の `movementX/Y` だけなので、
  **`document.dispatchEvent(new MouseEvent("mousemove", { movementY: 20 }))` を直に投げる**のが確実です。
  9 枚目の README にある「`page.mouse.click()` を使わないこと」はそのままで、**押して離すのは
  `mouse.down()` / `mouse.up()`、視点は合成イベント**、という組み合わせになります。
- **一覧からホットバーへ入れるなら、クリックの往復ではなく `Shift`+クリック**にすること。
  掴んで枠に置くと**入れ替え**になって手に前の中身が残り、**次に一覧を押した 1 回が
  「捨てる」に化けます**（`pressCreative()` の規則。実際に 2 度落としました）。
  **先に枠を空にしてから `Shift`+クリック**すれば `inventory.add()` が空いた枠に直に入れます。
- **刈ると狙いが外れます**（草むらが消えたぶん光線が抜ける）。刈ったあとに
  `target` が `-` になったら、地面に当たるまでもう少し下を向くこと。

## 13 枚目 —— 鶏（AUTODEV 16・2026-09-03）

**`docs/browser-shots/mobs.png` を足しました**（1280x480）。ブラウザではなく
**`npm run shot -- mobs`**（CPU の描き。`tools/raster.ts`）で、**全 7 種類を 1 列**に
並べた画です。定番 5 枚は本物のブラウザで撮り直してあります（見た目は前の周と同じ ——
この周が触ったのはモブの表だけなので、**変わっていないことが正解**です）。

| 見たこと | 値 |
| --- | --- |
| 並び | 豚 / 羊 / **鶏** / ゾンビ / ブレイズ / エンダーマン / ドラゴン（`MOB_KINDS` の順） |
| 鶏の見え方 | **体と翼が白・くちばしと脚が橙・とさかが赤・目が黒。** 面の抜けも埋まりもなし |
| 羊と見分けが付くか | **付きます**（羊は一様な白。鶏はとさかの赤とくちばしの橙が乗る） |
| 三角形 | 108,612（メッシュ 245） |

**絵を見て 1 つ直しました**（この周の C-3 の成果）。**体の幅が翼と同じ ±3px だったので、
白い体に白い翼が埋まって、振れても 1 画素も動きませんでした。** 体積・巡回順・振る部位の
`y1 === 0`・当たり判定は**全部緑**です（箱としては正しいので）。**撮って見るまで
気付けない類の不具合**で、体を ±2.5px に狭めて翼を 0.6px ぶん外へ出しています。

### 小さいモブを見るときの切り出し

`mobs` の場は 7 体を 5 マス間隔で並べるので、640x400 のままでは**鶏が 20 画素**しか
ありません。**`--size 1920x700` で撮ってから切り出して拡大する**と読めます。
このリポジトリには PNG を読む依存が 1 つも無い（書くほうは `tools/raster.ts` が
`node:zlib` で自前）ので、**使い捨ての切り出し道具を書くのがいちばん早い**です
（IHDR と IDAT を読んで `inflateSync` → フィルタ 0..4 を戻す → 近傍で拡大 → 書き直す。
50 行ほど。`shots/` は `.gitignore` に入っているので、置き場は困りません）。

## 14 枚目: `creative-feather.png`（2026-09-03・羽根と矢を本家の形へ / AUTODEV 17）

**12 枚目と同じ手**（メニューで `#mode` をクリックしてクリエイティブで始め、`E` を
押してから `#creative` の `scrollTop` を最後まで送る）。定番 5 枚も撮り直してあり、
**見た目は前の周と同じ**です（この周が触ったのは表とレシピだけなので、
**変わっていないことが正解**）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **93 個**（鶏の周の 92 から +1 = **羽根**） |
| 末尾に並んだもの | 小麦の種 → 小麦 → パン → 生鶏肉 → 焼き鳥 → **羽根** |
| 色 | `0xe8e4dc` = ほぼ白。**隣の生鶏肉（桃）・焼き鳥（茶）とは離れています** |
| 名前 | **枠の下に「羽根」がそのまま出ています**（欠けも溢れもなし） |
| console のエラー | **0 件**（残るのは SwiftShader の warning だけ） |
| `inventory.png` | 壊れていません（枠・日本語の説明文・`→` まで前の周と同じ） |

**ただし羽根のほぼ白は、羊毛・雪・骨盤の白い枠と近い系統です。** 末尾に並ぶので
隣とは分かれますが、**一覧の中で見分けが付くかは人の目でしか決められません**
（`HANDOFF.md` の「ブラウザで見てほしいところ」）。

## 15 枚目: `creative-egg.png`（2026-09-03・鶏が卵を産む / AUTODEV 18）

**12・14 枚目と同じ手**（メニューで `#mode` をクリックしてクリエイティブで始め、`E` を
押してから `#creative` の `scrollTop` を最後まで送る）。定番 5 枚も撮り直してあり、
**見た目は前の周と同じ**です（この周が触ったのはモブの表とアイテムの表だけなので、
**変わっていないことが正解**）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **94 個**（羽根の周の 93 から +1 = **卵**） |
| 末尾に並んだもの | 小麦 → パン → 生鶏肉 → 焼き鳥 → 羽根 → **卵** |
| `title` | **`卵 x16`**（`stack: 16` が UI にそのまま出ています。ほかの食べ物は x64） |
| 色 | `0xf7f0e0` = ほぼ白でわずかに黄み |
| 名前 | **枠の下に「卵」がそのまま出ています**（欠けも溢れもなし） |
| console のエラー | **0 件**（残るのは SwiftShader の warning だけ） |
| `inventory.png` | 壊れていません（枠・日本語の説明文・`→` まで前の周と同じ） |
| `game.png` | 前の周と同じ（色 1,153 種・上 rgb(47,97,181)・下 rgb(103,165,75)） |

**絵を見て分かったこと**: **卵は羽根のちょうど隣に並び、どちらもほぼ白です**
（羽根 `0xe8e4dc` / 卵 `0xf7f0e0` で、差は黄みがわずかにあるだけ）。
14 枚目の「羽根の白が一覧の中で見分けが付くか」は**この周で 2 個ぶんの話になりました** ——
色の判断は人にしか決められないので、`TUNING.md` に 1 行足して先へ進んでいます。

**卵そのものは地面に落ちた姿をまだ撮っていません。** 鶏は 300〜600 秒ごとにしか産まず、
**モブは保存されず 72m で消える**ので、ブラウザで待って撮るのは現実的ではありません
（時計の正しさは `test/mobs.test.ts` が境目の 1 フレームで測っています）。

## 16 枚目: `egg.png`（2026-09-04・卵を投げる / AUTODEV 19）

**投げた卵が飛んでいるところ**です。定番 5 枚（`node tools/browsershot.mjs`）は
先に撮ってあり、**見た目は前の周と同じ**でした（色 1,149 種・上 rgb(47,97,181)・
下 rgb(102,164,75)。console のエラー **0 件**、残るのは SwiftShader の warning だけ）。

**撮り方**（この 1 枚だけは使い捨てのスクリプト。`tools/` には入れていません）:
定番 5 枚と同じ立ち上げのあと、`page.mouse.move()` で**上を向いてから** `N` を 5 回。
`N` は飛び道具を順ぐりに 1 つ撃つデバッグキーで、**5 回目が卵**（表の 5 行目）。

**地面と平行に投げると撮れません。** 20m/s で目線の高さから投げると **0.6 秒で
落ちて消える**うえ、SwiftShader では 1 枚撮るのに 300〜500ms 掛かるので、
**1 枚目に間に合いません**（実際、平行に投げた 4 枚には 1 個も写りませんでした）。
上を向けば滞空が数秒に伸びて、1 枚目で確実に捉えられます。

| 見たこと | 値 |
| --- | --- |
| 写ったもの | **空を上がっていく小さな白い立方体 1 個**（画面のほぼ中央・0.25m 角） |
| 色 | ほぼ白でわずかに黄み（表の `0xf7f0e0` = 持っている卵と同じ値） |
| 向き | **傾いて写っています**（`aims: false` = 向きを持たずに回る。矢のように尖らない） |
| 弾道 | 4 枚目には**もう画面外**（`gravityScale: 1` で山なりに落ちていく） |
| console のエラー | **0 件** |

**確かめられたのは「出る・色が合っている・回っている・落ちる」の 4 つ**です。
**当たった手ごたえ（当たっても何も起きない）は人にしか決められません**
（`HANDOFF.md` の「ブラウザで見てほしいところ」）。

## 17 枚目 —— 牛（AUTODEV 20・2026-09-04）

**この周は 3 通り撮っています**（定番 5 枚 + モブの並び + クリエイティブの一覧）。
定番 5 枚（`node tools/browsershot.mjs`）は**見た目が前の周と同じ**でした
（`game.png` は色 1,170 種・上 rgb(47,97,181)・下 rgb(102,164,75)。**console のエラー 0 件**、
残るのは SwiftShader の warning だけ。WebGL 2.0・ポインタロックも効いています）。

### `mobs.png` と `cow.png`（`npm run shot -- mobs`。CPU の描き）

**全 8 種類が 1 列**（`MOB_KINDS` の順）。`cow.png` は牛のところを切り出して 12 倍にしたものです。

| 見たこと | 値 |
| --- | --- |
| 並び | 豚 / 羊 / 鶏 / **牛** / ゾンビ / ブレイズ / エンダーマン / ドラゴン |
| 牛の見え方 | **体と脚が濃い茶・顔に白い筋・鼻が桃・角が骨色・目が黒。** 面の抜けも埋まりもなし |
| 鶏・羊と見分けが付くか | **付きます**（あの 2 つは白、牛だけ濃い茶で、遠目にもいちばん暗い） |
| 三角形 | 108,780（メッシュ 251） |

**絵を見て 2 回直しました**（この周の C-3 の成果。どちらも**撮るまで全部緑**でした）。

1. **頭と体が 1 枚の板に見えました。** どちらも同じ茶色で、**頭は体より 1.1px しか前に
   出ておらず、面の向きも同じ**なので陰でも分かれません。頭は体へ差し込む部位なので
   **鶏のように輪郭を外へ出す手が使えず**、**顔に白い筋**（目とまったく同じ、前面から
   0.1px 出した薄い箱）を置いて分けました。
2. **その筋を頭の幅いっぱい（±3.5px）に引いたら、同じ高さの角と 1 本の白い帯**になって
   **角が角に見えなくなりました。** 筋を ±1.5px に細めて、目（±1.8..3）にも角（±3.5..4.8）
   にも触れない幅に収めています。

どちらも `rules/mobs.md` に据えました。

### `creative-beef.png`（本物のブラウザ・クリエイティブの一覧）

**12・14・15 枚目と同じ手**（メニューで `#mode` をクリックしてクリエイティブで始め、
`E` を押してから `#creative` の `scrollTop` を最後まで送る）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **97 個**（卵の周の 94 から +3） |
| 末尾に並んだもの | 焼き鳥 → 羽根 → 卵 → **生牛肉 / ステーキ / 革** |
| 色（`getComputedStyle` で読んだ値） | 生牛肉 rgb(200,86,79) / ステーキ rgb(143,82,48) / **革 rgb(160,106,65)** |
| 名前 | **3 つとも枠の下に出ています**（欠けも溢れもなし。`title` は `x64`） |
| console のエラー | **0 件** |

**生牛肉の赤は、生豚肉（rgb(224,143,143)）とも生鶏肉（rgb(211,160,142)）ともはっきり
分かれます。** ただし**ステーキと革は隣り合う茶色どうし**で、焼き豚（rgb(196,118,63)）とも
系統が近いです。**見分けが付くかは人の目でしか決められません**（`TUNING.md` の「牛」）。

**横から見た形はまだ人に見てもらう必要があります。** この周は使い捨てのスクリプトで
横向き・斜めの牛も撮って（`tools/` には入れていません）、**胴の白い斑・脚の 4 本・
角の出方**を確かめましたが、**胴が本家の 18px ではなく 12px** なので
**寸詰まりに見えるかどうか**は手触りの話です（`HANDOFF.md`）。

### `creative-snowball.png` と `snowball-flight.png`（本物のブラウザ・雪玉の周）

**一覧のほうは 12・14・15・16 枚目と同じ手**（メニューで `#mode` をクリックして
クリエイティブで始め、`E` を押してから `#creative` の `scrollTop` を最後まで送る）。
**飛んでいるほうは `N` キー**（`debugspawn.ts` の `nextShot()` が表を順ぐりに 1 つ撃つ。
6 回目が雪玉）で、**空を向いてから撃っています** —— 地面へ撃つと草の上の小さな四角に
なって、撮った絵から何も読めません（1 度そうなって撮り直した）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **99 個**（牛の周の 97 から +2。糸と雪玉） |
| 末尾に並んだもの | 卵 → 生牛肉 → ステーキ → 革 → 糸 → **雪玉** |
| 色（`getComputedStyle` で読んだ値） | **雪玉 rgb(188,216,239)**（糸 rgb(184,188,200) / 卵 rgb(247,240,224)） |
| 飛んでいる雪玉 | **青白い立方体**。撃った向きへ飛び、**2 コマ後には画の下へ落ちています** |
| 落ち方 | エンダーアイ（`gravityScale: 0`）が高さを保つ横で、**雪玉だけが下がります** |
| console のエラー | **0 件** |

**糸（灰）と雪玉（青白）は絵の上でも分かれます** —— 白っぽいものが 4 つ
（羽根・卵・糸・雪玉）並びましたが、青みのあるのは雪玉だけです。
**それでも「一覧で拾いたいものが拾えるか」は人の目の話**です（`HANDOFF.md`）。

### `creative-stored.png` と `stored-blocks.png`（鉄・金・ダイヤのブロックの周）

**一覧のほうは今までと同じ手**（メニューで `#mode` をクリックしてクリエイティブで始め、
`E` を押してから `#creative` の `scrollTop` を最後まで送る）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **102 個**（雪玉の周の 99 から +3） |
| 末尾 6 枠（DOM の `title` から読んだ値） | 革 x64 / 糸 x64 / 雪玉 x16 / **鉄ブロック x64 / 金ブロック x64 / ダイヤブロック x64** |
| 色 | **3 つとも材料の山と同じ**（鉄 `0xd8d2c8` / 金 `0xf2d15c` / ダイヤ `0x4fe3d8`） |
| console のエラー | **0 件** |

**`stored-blocks.png` は本物のブラウザではなく CPU 描き**（`tools/raster.ts`）です。理由:

**ヘッドレスの Chromium ではポインタロックが掛からず、`page.mouse.move` を送っても
カメラが 1 度も向きを変えません。** 右クリックが足元にしか飛ばないので、**ブラウザで
ブロックを置いた画は撮れませんでした**（1 個も置けていない絵を 1 枚撮って気付いた）。
**置いた画が要るときは `world.setVoxel()` で直に置いて `raster.ts` で塗ること** ——
`tools/shot.ts` の `crops` の場面がそのやり方で、1〜2 秒で撮れます。
`browsershot.mjs` で置こうとして時間を溶かさないこと。

写っているもの: **平らな台に鉄（左）・金（中・2 段）・ダイヤ（右）を並べた画**。
**面の欠けも裏返りもなく、AO も他の立方体と同じに乗っています。**
（台の縁から下の洞窟が見えているのは、撮るために地形を削った跡です。）

### 革の防具 4 部位の周（2026-09-09・27b-1）

**`node tools/browsershot.mjs` の `inventory.png` には新しいアイテムが 1 つも写りません。**
あれは**サバイバルの空のインベントリ**なので、枠が 36 個とも空です
（前の周の `HANDOFF.md` が「一覧に出るだけの色は browsershot のインベントリの 1 枚で
見られる」と書いていましたが、**間違い**でした）。**一覧の色を見るのは上の
`creative-stored.png` と同じ手**です（`#mode` を押してクリエイティブにし、`E` を
押してから末尾までスクロール）。**使い捨てのスクリプトで撮ればよく、`browsershot.mjs`
そのものを書き換える必要はありません。**

**ヘッドレスでは `Escape` を送ってもメニューが開かないことがあります**（ポインタロックを
JS で偽装しているため）。**`document.getElementById("mode").click()` を直に呼ぶこと** ——
ボタンは隠れていても click ハンドラは効きます。

| 見たこと | 値 |
| --- | --- |
| 一覧に並んだか | **革 / 革の帽子 / 革の上着 / 革のズボン / 革の靴 の 5 つ**（`.label` から読んだ） |
| 4 色 | 帽子 `0xfca168` → 上着 `0xd88662` → ズボン `0xb16e51` → 靴 `0x644122` の**明るい順**。**4 つとも見分けが付きます** |
| console のエラー | **0 件** |

**⚠ 写った不具合: 「革のズボン」の名前が 2 行に折れて、枠の絵に被っています。**
`style.css` の `.slot .label` の既知の件（`HANDOFF.md` に前から積んである）で、
**5 文字の名前で出ます** —— 「革の帽子」「革の上着」「革の靴」の 4 文字は折れません。
**この周では直していません**（`style.css` は 27b-2 の持ち物で、仕様書の
「触らないファイル」に入っています）。

### `armor-empty.png` と `armor-worn.png`（防具枠の周・2026-09-11・27b-2）

**防具枠は画面に出るものなので、本物のブラウザで 2 枚撮りました**（使い捨ての
スクリプトで、`browsershot.mjs` は書き換えていません）。

| 見たこと | 値 |
| --- | --- |
| 防具枠の位置（`getBoundingClientRect`） | **4 枠とも 46x46・y 235 に横並び**（x 376 / 430 / 484 / 538）。盤面と収納のあいだ |
| サバイバルで `E`（`armor-empty.png`） | **頭・胴・脚・足の 4 枠が空で出ています**。ラベルは 1 文字なので折れません |
| クリエイティブでも出るか（`armor-worn.png`） | **出ます**（隠す分岐を作っていないので、一覧を開いていても同じ場所に残ります） |
| 着たあとの中身（DOM の `title` と `.swatch` の色） | 帽子 `rgb(252,161,104)` / 上着 `rgb(216,134,98)` / ズボン `rgb(177,110,81)` / 靴 `rgb(100,65,34)` の**4 つとも合う枠に入っています** |
| console のエラー | **0 件**（`GL Driver Message` の警告だけ） |

**⚠ 写った既知の件: 「革のズボン」の名前が 2 行に折れて枠の絵に被ります**（防具枠でも
一覧と同じに出ます）。`style.css` の `.slot .label` の件で、**5 文字の名前で出ます**。
**27b-2 の仕様書で「触らない」に入っているので直していません**（人の判断待ち）。

**スロットを名前で探すなら `.label` の文字を見ること。** `el.title` は
`paintSlot()` が **`"革の帽子 x1"` の形**（名前 + ` x` + 個数）で入れるので、
**名前そのものと `===` で比べると 1 つも見つかりません**（この周で 1 回撮り直しました）。

**クリエイティブの一覧からシフトクリックで取ったものは、ホットバーではなく
収納に入ることがあります** —— クリエイティブで始めるとホットバー 9 枠が最初から
埋まっているためです。**`#invhotbar` だけを探すと空振りします**（ここでも 1 回撮り直した）。
`#invhotbar .slot` と `#storage .slot` を**両方つないでから名前で探すこと**。

### `creative-charcoal.png`（木炭の周・2026-09-13・AUTODEV 77）

**一覧に色が 1 つ増えるだけの周でも撮ります**（`AUTODEV.md` の C-3）。使い捨ての
スクリプトで、`browsershot.mjs` は書き換えていません。**手順は上の
`creative-stored.png` とまったく同じ**（`#mode` を `click()` → `E` →
`#creative` の `scrollTop = scrollHeight`）。

| 見たこと | 値 |
| --- | --- |
| 一覧に並んだか | **末尾に「木炭」が 1 枠**（骨の隣・`.label` から読んだ）。`title` は `木炭 x64` |
| 色 | `.swatch` の `backgroundColor` が **`rgb(86,71,63)` = `0x56473f`**（指定どおり） |
| 石炭と並ぶか | **同じ画面には入りません**（石炭は 65 なのでずっと上。`rect` の y が −136）。**色の隔たり 64.0 は `test/items.test.ts` が数で見ています** |
| console のエラー | **0 件**（`GL Driver Message` の警告だけ） |

**⚠ 写った既知の件: 「革のズボン」の名前が 2 行に折れて枠の絵に被ります**（`style.css` の
`.slot .label`。**5 文字の名前で出ます**）。**この周の持ち物ではないので直していません。**

### `creative-sapling.png`（苗木の周・2026-09-13・AUTODEV 79）

**手順は `creative-charcoal.png` とまったく同じ**（使い捨てのスクリプトで、
`#mode` を `click()` → `E` → `#creative` の `scrollTop = scrollHeight`）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **127 個**（木炭の周の 125 から +2） |
| 末尾に並んだもの | 革の防具 4 部位 → 骨 → 木炭 → **オークの苗木 / トウヒの苗木** |
| 色 | `.swatch` が **`rgb(127,191,95)` = `0x7fbf5f`**（オーク）と **`rgb(47,127,90)` = `0x2f7f5a`**（トウヒ）。**隣り合っても明るい黄緑と暗い青緑ではっきり分かれます** |
| `title` | `オークの苗木 x64` / `トウヒの苗木 x64`（**ブロックなので 1 枠 64 個**） |
| console のエラー | **0 件**（`GL Driver Message` の警告だけ） |

**⚠ 写った既知の件: 6 文字の「オークの苗木」「トウヒの苗木」が 2 行に折れて枠の絵に
被ります**（`style.css` の `.slot .label`。「革のズボン」で前から積んである件で、
**5 文字から出ます** —— **6 文字は今までで一番長く、被りも一番大きい**です）。
**この周の持ち物ではないので直していません**（人の判断待ち）。

**苗木そのものの形は `npm run shot -- sapling`**（`tools/shot.ts` に場面を足しました）。
**自然には 1 本も生えない**ので、本棚・クモの巣・ケーキ・氷・フェンスと同じ扱いです。

### `creative-brick-slabs.png`（ハーフ 2 材質の周・2026-09-14・AUTODEV 83）

**⚠ ここまでの 2 枚（木炭・苗木）と手順が 1 つだけ違います。**
**55 / 56 は低帯なので、一覧の末尾ではなく真ん中に出ます**（`allItemIds()` は ID 順）。
**`#creative` の `scrollTop = scrollHeight` を送ると 1 枠も写りません** ——
この周で 1 度撮り直しました。**`.label` の文字列で枠を探して
`scrollIntoView({ block: "center" })` を呼ぶこと。**

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **129 個**（苗木の周の 127 から +2） |
| 並んだ場所 | **47 / 48 番目**。石レンガ → エンドクリスタル → **ネザーレンガハーフ → 石レンガハーフ** → 棒 |
| 色 | `.swatch` が **`rgb(57,34,41)` = `0x392229`**（ネザーレンガ）と **`rgb(125,130,136)` = `0x7d8288`**（石レンガ）。**どちらも元の材質と同じ値**で、石レンガハーフは 2 つ上の「石レンガ」と見分けが付きません（**仕様どおり**。色をずらすのは禁じ手） |
| `title` | `ネザーレンガハーフ x64` / `石レンガハーフ x64`（**ブロックなので 1 枠 64 個**） |
| console のエラー | **0 件**（`GL Driver Message` と SwiftShader の警告だけ） |

**⚠ 写った既知の件がここまでで一番ひどく出ます: 9 文字の「ネザーレンガハーフ」が
3 行に折れて、枠の絵を丸ごと覆います**（`style.css` の `.slot .label`。「革のズボン」= 5 文字で
前から積んである件で、**6 文字の苗木が「一番長い」だったのを更新しました**）。
**色を見るには `.swatch` の `backgroundColor` を読むしかありません**（上の表がそれです）。
**この周の持ち物ではないので直していません** —— `.slot .label` を触ると**全部の枠の
手ざわりが変わる**ので、人の判断待ちのままです。

**ハーフそのものの形は `npm run shot -- slabs`**（`tools/shot.ts` に場面を足しました）。
**自然には 1 マスも湧かない**ので、本棚・クモの巣・ケーキ・氷・フェンス・苗木と同じ扱いです。

### `creative-clay.png`（粘土の周・2026-09-15・AUTODEV 85）

**手順は `creative-charcoal.png` / `creative-sapling.png` と同じ**（使い捨ての
スクリプトで、`#mode` を `click()` → `E`）。**送り先は `.label` の文字列で枠を探して
`scrollIntoView({ block: "center" })`**（`creative-brick-slabs.png` で決めた形。
168 / 169 は共有帯の末尾なので `scrollTop = scrollHeight` でも写りますが、
**探し方を場面ごとに変えないため**そろえてあります）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **131 個**（ハーフの周の 129 から +2） |
| 末尾に並んだもの | 木炭 → オークの苗木 / トウヒの苗木 → **粘土 / 粘土玉**（130 / 131 番目） |
| 色 | `.swatch` が **`rgb(157,163,181)` = `0x9da3b5`**（粘土）と **`rgb(167,180,218)` = `0xa7b4da`**（粘土玉）。**指定どおり。隣り合っても、灰青と薄い青紫で分かれます** |
| `title` | `粘土 x64` / `粘土玉 x64`（**どちらも 1 枠 64 個**。粘土玉は雪玉と違って投げられないので 16 個ではありません） |
| console のエラー | **0 件**（`GL Driver Message` と SwiftShader の警告だけ） |

**⚠ 前から積んである「名前が折れて枠の絵に被る件」はこの 2 つでは出ません**
（`style.css` の `.slot .label`。**5 文字から出る**件で、「粘土」2 文字・「粘土玉」
3 文字はどちらも 1 行に収まります）。**直っていないので、次に長い名前を取る周では
また出ます。**

**海底の粘土そのものは `npm run shot -- clay`**（`tools/shot.ts` に場面を足しました）。
**⚠ この場面は原点のまわりを撮りません** —— 種 4242 は**±128 の海が全部凍っていて**、
氷の板が海底を覆います。**凍っていない海を `sourceOf()` の生成器で遠くまで探し、
見つけた所を中心に `makeWorld(…, at)` してから撮ります**（`rules/meshing-render.md`）。

### `creative-brick.png` / `creative-brickblock.png`（レンガの周・2026-09-15・AUTODEV 87）

**この周は 2 枚です** —— **アイテムの「レンガ」は一覧の末尾（170）・ブロックの
「レンガブロック」は真ん中（12）**なので、`scrollIntoView({ block: "center" })` を
2 回呼んで別々に撮りました（`creative-brick-slabs.png` で決めた形。**低帯の番号は
`scrollTop = scrollHeight` では 1 枠も写りません**）。手順はほかの
`creative-*.png` と同じ使い捨てのスクリプト（`#mode` を `click()` → `E`）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **132 個**（粘土の周の 131 から +1。ブロックは 1 つも増えていない） |
| 末尾に並んだもの | 苗木 2 種 → 粘土 / 粘土玉 → **レンガ**（132 番目） |
| レンガブロックの場所 | **11 番目**（低帯の 12 なので真ん中。ガラスの隣） |
| 色 | `.swatch` が **`rgb(188,93,57)` = `0xbc5d39`**（レンガ）と **`rgb(164,85,63)` = `0xa4553f`**（レンガブロック）。**指定どおり。並べると赤茶 2 つだが、明るさで分かれます**（測った隔たりは 26.0） |
| `title` | `レンガ x64` / `レンガブロック x64` |
| 名前の重複 | **0 件**（132 枠の `.label` を全部読んで突き合わせました。改名前は「レンガ」が 2 つ並ぶところでした） |
| console のエラー | **0 件** |

**⚠ 写った既知の件: 7 文字の「レンガブロック」が 2 行に折れて枠の絵に被ります**
（`style.css` の `.slot .label`。**5 文字から出る**件で、9 文字の「ネザーレンガハーフ」が
最長のまま）。**改名でこの件を 1 枠ぶん増やしました** —— 前は 3 文字の「レンガ」で
1 行に収まっていた枠です。**アイテムのほうの「レンガ」は 3 文字なので出ません。**
**この周の持ち物ではないので直していません**（`.slot .label` を触ると全部の枠の
手ざわりが変わるため。**人の判断待ち**）。

**レンガブロックそのものの形は撮っていません** —— **新しいブロックが 0 個**で、
`npm run shot` のどの場面にも 1 マスも増えないためです（`BRICK`(12) は前から
`blocks.ts` に居て、見た目は 1 ピクセルも変わっていません）。

### `creative-iron-armor.png`（鉄の防具の周・2026-09-16・AUTODEV 89）

**1 枚です** —— 鉄の防具 4 部位は **171..174 で一覧の末尾に並ぶ**ので、
`scrollIntoView({ block: "center" })` を 1 回呼べば 4 枠とも写ります
（レンガの周のように 2 枚要るのは、**低帯の番号を取ったものが混じるとき**だけ）。
手順はほかの `creative-*.png` と同じ使い捨てのスクリプト（`#mode` を `click()` → `E`）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **136 個**（レンガの周の 132 から +4。**ブロックは 1 つも増えていません**） |
| 末尾に並んだもの | 粘土 / 粘土玉 / レンガ → **鉄の帽子 / 鉄の上着 / 鉄のズボン / 鉄の靴** |
| 色 | `.swatch` が **`rgb(207,207,255)` = `0xcfcfff`** / **`rgb(146,167,206)` = `0x92a7ce`** / **`rgb(120,142,190)` = `0x788ebe`** / **`rgb(80,101,149)` = `0x506595``**。**指定どおり。4 段の明るさの階段が絵の上でも読めます** |
| 革との見分け | **革 4 部位（橙茶）のすぐ下の行に鉄 4 部位（青灰）が並びます。** 測った隔たり 118.8 のとおり、絵の上で取り違えません |
| `title` | `鉄の帽子 x1`（**`x1` です** —— 着る物は積めません） |
| 名前の重複 | **0 件**（136 枠の `.label` を全部読んで突き合わせました） |
| console のエラー | **0 件**（SwiftShader の警告だけ） |

**⚠ 写った既知の件: 5 文字の「鉄のズボン」が 2 行に折れて枠の絵に被ります**
（`.label` の高さが **26px**。他の 3 つは 13px）。**この周で新しく折れたのは
この 1 枠だけ**で、仕様書の見込みどおりでした。**⚠ 一覧全体では 51 枠が折れています**
（最長は 9 文字の「ネザーレンガハーフ」で **39px = 3 行**）。**人の判断待ちのまま**です
（`.slot .label` を触ると全部の枠の手ざわりが変わるため）。

**⚠ `.slot` で数えると 252 個になります** —— あれは**ホットバーとインベントリの
36 枠まで拾う**からで、一覧だけを数えるなら **`#creative .slot`** を使うこと
（この README の「枠 N 個」はどれも `#creative` の側の数です）。

**防具を着た姿は撮っていません** —— **一人称視点なので自分の体が写りません**
（防具枠に入れても画面に出るのは枠の中の絵だけです）。**装備したときの手ざわりは
人に見てもらうこと**（`HANDOFF.md`）。

### `creative-gold-diamond-armor.png`（金・ダイヤの防具の周・2026-09-16・AUTODEV 91）

**1 枚です** —— 金・ダイヤの防具 8 部位は **175..182 で一覧の末尾に並ぶ**ので、
`scrollIntoView({ block: "center" })` を 1 回呼べば 8 枠とも写ります（鉄の周と同じ形）。

**⚠ `#mode` はメニューの中のボタンです**（`index.html` の `seedrow`）。**`#play` のあとは
隠れるので、クリエイティブへの切り替えは開く前に押すこと** —— この周は
「`#play` → `#mode` を click」の順で書いて **30 秒待って `element is not visible` で落ちました。**
正しい順は **`#seed` を入れる → `#regen` → `#mode` → `#play` → `E`** です。

**⚠ Chromium を探すときに `chromium_headless_shell-*` を拾わないこと。**
`/opt/pw-browsers` には `chromium-1194` と `chromium_headless_shell-1194` の両方があり、
素直に `startsWith("chromium")` で `sort().pop()` すると**後者を掴んで
`executable doesn't exist` で落ちます**（`chrome-linux/chrome` が入っていない）。
`tools/browsershot.mjs` の `findChrome()` は **`startsWith("chromium-")`** で弾いています ——
**使い捨てのスクリプトを書くときも同じ絞り込みを写すこと。**

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **144 個**（鉄の周の 136 から +8。**ブロックは 1 つも増えていません**） |
| 末尾に並んだもの | 鉄のズボン / 鉄の靴 → **金の帽子 / 上着 / ズボン / 靴 → ダイヤの帽子 / 上着 / ズボン / 靴**（137..144 番目） |
| 色 | `.swatch` が **`rgb(254,227,77)` / `rgb(239,215,20)` / `rgb(248,179,23)` / `rgb(194,170,53)`**（金）と **`rgb(209,254,254)` / `rgb(164,248,245)` / `rgb(146,230,227)` / `rgb(128,212,209)`**（ダイヤ）。**指定どおり。黄 4 段と水色 4 段が絵の上でも 2 つの帯として読めます** |
| 鉄との見分け | **鉄 4 部位（青灰）のすぐ隣に金 4 部位（黄）、その次にダイヤ 4 部位（水色）**が並びます。測った隔たり（既存の防具 8 部位とは最小 47.1）のとおり、絵の上で取り違えません |
| `title` | `金の帽子 x1` … `ダイヤの靴 x1`（**どれも `x1`** —— 着る物は積めません） |
| 名前の重複 | **0 件**（144 枠の `.label` を全部読んで突き合わせました） |
| console のエラー | **0 件**（SwiftShader の警告だけ） |

**⚠ 写った既知の件: 新しく折れたのは「金のズボン」とダイヤ 4 部位の 5 枠です**
（`.label` の高さが **26px**。金の帽子・上着・靴は 4 文字以下で 13px）。
**仕様書の見込みどおりで、一覧全体では 51 → 56 枠が折れています**（最長は 9 文字の
「ネザーレンガハーフ」で 39px = 3 行）。**人の判断待ちのまま**です
（`.slot .label` を触ると全部の枠の手ざわりが変わるため）。

**防具を着た姿は撮っていません** —— **一人称視点なので自分の体が写りません**
（鉄の周と同じ。装備したときの手ざわりは人に見てもらうこと）。

### `creative-vine.png`（ツタの周・2026-09-17・AUTODEV 93）

**1 枚です** —— ツタは **183 で一覧の末尾**（145 枠目）に並ぶので、鉄・金の周と同じく
`.label` で枠を探して `scrollIntoView({ block: "center" })` を 1 回呼べば写ります。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **145 個**（金・ダイヤの周の 144 から **+1**。**ブロックは 4 つ増えましたが、184..186 は `variantOf: VINE` なのでアイテムを持ちません**） |
| 末尾に並んだもの | ダイヤのズボン / ダイヤの靴 → **ツタ**（145 枠目） |
| 色 | `.swatch` が **`rgb(48,109,24)`** = `0x306d18`。**指定どおり。** 一覧の緑の帯（オークの苗木の明るい黄緑・トウヒの苗木の青緑・草・葉）の中で、**いちばん暗く沈んだ緑**として読めます（測った隔たりはトウヒの葉 38.2 / 葉 39.4・判定は 20） |
| `title` | `ツタ x64`（**積める** —— 防具の `x1` とは違います） |
| 名前の重複 | **0 件**（145 枠の `.label` を全部読んで突き合わせました） |
| 折れた枠 | **56 個のまま ±0** —— **「ツタ」は 2 文字**なので折れません（`.slot .label` は 5 文字から折れます） |
| console のエラー | **0 件**（SwiftShader の警告だけ） |

**壁に掛けた姿はブラウザでは撮っていません** —— **`npm run shot -- vine` のほうが
安くて確か**です（石の柱の 4 面に掛けた絵。撮り方の落とし穴は `rules/meshing-render.md`）。

### `creative-glowstone-dust.png`（グロウストーンダストの周・2026-09-25・AUTODEV 126）

**手順は `creative-clay.png` と同じ**（使い捨てのスクリプトで `#mode` を `click()` → `E` →
`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })`）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **148 個**（末尾 = ツタ → ネザーレンガのフェンス → 石炭ブロック → **グロウストーンダスト**） |
| 色 | `.swatch` が **`rgb(255,242,122)` = `0xfff27a`**（指定どおり）。グロウストーン(47) は `rgb(246,216,136)` |
| `title` | `グロウストーンダスト x64` |
| console のエラー | **0 件** |

**⚠ 名前が 10 文字なので `.slot .label` が 3 行に折れて枠の絵に被ります**（前から積んである件。58 枠目）。

### `creative-spider-eye.png`（クモの目の周・2026-09-25・AUTODEV 128）

**手順は `creative-clay.png` と同じ**（使い捨てのスクリプトで `#mode` を `click()` → `E` →
`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })`）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **149 個**（末尾 = 石炭ブロック → グロウストーンダスト → **クモの目**） |
| 色 | `.swatch` が **`rgb(184,48,80)` = `0xb83050`**（指定どおり）。隣の段のリンゴ（赤）・本（くすんだ赤紫）と並べて見分けられる |
| `title` | `クモの目 x64` |
| 名前 | 4 文字で **`.slot .label` は 1 行**（高さ 13px。折れない） |
| console のエラー | **0 件** |

### `creative-gold-tools.png`（金の道具の周・2026-09-26・AUTODEV 130）

**手順は `creative-clay.png` と同じ**（使い捨てのスクリプトで `#mode` を `click()` → `E` →
`.label` の文字列で枠を探して `scrollIntoView({ block: "center" })`）。

| 見たこと | 値 |
| --- | --- |
| 一覧の枠 | **154 個**（末尾 = クモの目 → **金のツルハシ・斧・シャベル・剣・クワ**） |
| 色 | `.swatch` が 5 本とも **`rgb(242,209,92)` = `0xf2d15c`**（金インゴットと同じ。指定どおり） |
| `title` | `金のツルハシ x1` など（**1 枠 1 個**） |
| 名前 | **「金のツルハシ」「金のシャベル」は 6 文字で `.slot .label` が 2 行（26px）に折れる**（既知の件。60 枠）。斧・剣・クワは 1 行 |
| console のエラー | **0 件** |
