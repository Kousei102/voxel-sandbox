# 自動実装ループの記録（読まなくてよい）

**これは `AUTODEV.md` の「6. 記録」の退避先です。** キューではないので、ループは毎周
読む必要がありません。**必要になるのは 2 つのときだけ:**

- **同じものをまた検討しそうなとき** —— 「見送ったもの」に理由が 1 行あります
- **同じ形でまた落ちたとき** —— 「差し戻し」に、何を仕様書に書き忘れて落ちたかがあります

いま取るものは `AUTODEV-QUEUE.md`、手順は `AUTODEV.md`。

書き方は 1 周 1 節で、次の形にしてください（**要約しないこと**。次の周が読むのは
「なぜそう決めたか」で、結果だけなら `git log` にあります）。

```markdown
## 2026-09-01 — 12. 小麦と種（作物）

- **取ったもの**: 小麦（アイテム 111）/ 種（112）/ 耕地（ブロック 113）。ID は予約表から。
- **見送ったもの**: パン —— 育つブロックの仕組みが先に要る（次の周へ）。
  カボチャ —— 番号を 2 個食う割に、クリア導線に効かない。
- **差し戻し**: 1 回。育つ速さを `main.ts` に書いていた（仕様書の「判断をどこに置くか」に
  `crops.ts` と書いておらず、サブエージェントが `main.ts` に置いた）。
- **枠**: 111..255 の空き 145 → 142。`main.ts` 1373 → 1381 行。
```

---

## 2026-09-01 — 1. 砂と砂利が落ちる（重力ブロック）。C（実装）の周

- **取ったもの**: `src/gravity.ts`（新規）/ `blocks.ts` の `fallsDown()` と `falls` フラグ /
  `breaking.ts` と `placing.ts` から 1 行ずつ。**ID は 0 個**（新しいブロックもアイテムも足していない）。
- **なぜこの形にしたか（次の周が同じ道を通らないために）**:
  - **手本は `liquids.ts` と `quenched()` の対**。規則（**どの ID が**落ちるか）は
    `blocks.ts` の表 1 本、**どのマスに**効くかは `gravity.ts` 1 か所、呼ぶのは
    `breaking.ts` / `placing.ts` の 1 行ずつ。**`main.ts` と `world.ts` は 1 行も触っていない**
    —— 配線すら要らないのは、判断が全部 `gravity.ts` に閉じたからです。
  - **落下は即時（1 フレームで着地まで）。** 落下エンティティを作ると `droprender.ts` の
    ような描画が要り、**この環境では見た目を確かめられません**（`CLAUDE.md`）。
    本家と違うので `TUNING.md` に 1 行残してあります。
  - **`world.update()` と生成器からは呼びません。** 毎フレーム走らせると
    `test/world.test.ts` の p99 に乗り、生成直後の砂漠の砂と洞窟の天井の砂利が
    勝手に崩れて `test/worldgen.test.ts` の見え方まで変わります。
    **つまり「生成された浮いた砂」は掘るまで落ちません** —— これは仕様です。
  - **`breaking.ts` の `autoBreak()` からは呼びません。** あそこは `world.ts` が
    そのマスを消している**途中**でブロックがまだ残っており、空振りするだけ。
    **呼ばない理由をコメント 1 行で残してあります**（次の周が「呼び忘れ」と誤読しないため）。
  - **書く順は「先に落ちる先へ書き、成功したときだけ元のマスを消す」。** 逆にすると
    `setVoxel` が false を返したときに砂が消えます（`liquids.ts` の裏返し）。
    チェック役が**未読み込みの列を凍結して実際に確かめ**、`moved=0` で無変化＝
    砂が消えも増えもしないことを取っています。
- **見送ったもの**: 落下ダメージと落ちる速さ —— 落下エンティティが要り、見た目を
  確かめられない（`TUNING.md` 行き）。**松明を運ぶこと** —— 砂が下がると既存の
  `breakUnsupported` で壊れるのが本家と同じで、重力の側に支えの話を持ち込むと
  `supportFace` と二重管理になります。
- **差し戻し**: **0 回。** 仕様書の「2. 触るファイル / 触らないファイル」と
  「6. 禁じ手」がそのまま守られていました（B の周で 7 項目を埋めた効き目）。
- **チェック役が別に見つけたこと**: 実装側の報告にあった「2286 → 2307」は正しく、
  **仕様書の『2285 件』が 1 件古かった**（`test/docs.test.ts` が `AUTODEV-SPEC.md` の
  行数を見始めたぶん）。**件数は仕様書ではなく `npm test` から取ること。**
- **枠**: **変化なし。** 1..63 の空き 9 / 111..255 の空き 145 のまま（ID を使っていない）。
  `main.ts` 1373 行のまま（触っていない）。テスト 2286 → **2307 件**（+21）。

---

## 2026-09-01 — A（棚卸し）の周。`AUTODEV-QUEUE.md` を作った

- **基準**: 本家のバージョン順（Alpha → Beta → 正式版）で、まだ無いものを上から取る。
  **次の A の周でも変えないこと**（`AUTODEV.md` の A-1）。
- **取ったもの**: 10 件。並びは `AUTODEV-QUEUE.md`。
  上 3 件（重力ブロック・大きいチェスト・道具の耐久値）が**番号を 1 つも使わない**ので先頭で、
  4..6（剣・ハサミ・作物）が番号を使うぶん、7..10（鶏・牛・クモ・スケルトン）は
  **見た目がユーザーにしか確かめられない**ので最後。取りやすさで並べたのではありません。
- **数え直して分かったこと**: **剣が 1 本も無い。** 道具はツルハシ・斧・シャベルの 3 種だけで、
  攻撃力は `mobs.ts` の `TOOL_ATTACK`（斧 3 > ツルハシ 2 > シャベル 1・階層ごとに +0.5）。
  本家で最初期からある物がまるごと抜けていたので、`ROADMAP.md` にも載っていませんでした
  —— **「入れたいもの」の一覧より、コードを数えるほうが速く見つかる**という例です。
- **見送ったもの（理由 1 行ずつ）**:
  - **水流・溶岩流** —— 1 フレームの予算（生成 3ms / メッシュ化 6ms）に流体の更新を
    載せる設計がまだ無く、1 周では閉じない。**黒曜石の導線はバケツで足りている**
    （`rules/blocks-shapes.md` の `quenched()`）ので、急ぎでもない。
  - **防具** —— 番号を 12〜16 個食ううえ、インベントリ画面に枠を足す（DOM 側）ので 1 周に入らない。
    **革（牛・8 番）が先**に要る。
  - **経験値とエンチャント** —— 手触りの数値が多く、ユーザーと決めることが多い（`TUNING.md` 行き）。
  - **レッドストーン** —— 状態の伝播が丸ごと新しい仕組みで、1 周では閉じない。
  - **醸造とポーション** —— **効果（状態異常）の器が `vitals.ts` に無い**。毒しかないので、
    先に「効果」という枠を作る周が要る。
  - **ネザークォーツ** —— 番号を 2 個使うのに使い道がレッドストーン待ちで、いま入れても飾り。
  - **天候（雨・雪）** —— 描画とパーティクルが要り、この環境では確かめられない（`CLAUDE.md`）。
  - **村と村人** —— 構造物 + モブ + 取引の 3 つが同時に要り、1 周に入らない。
  - **TNT と爆発** —— 球状にブロックを消す処理がメッシュ化の予算に効く。重力ブロック（1 番）の
    あとなら形が見えるので、**次の A の周で数え直すこと。**
- **枠**: 変化なし（`src/**` と `test/**` に差分は無い）。1..63 の空き 9 / 111..255 の空き 145。

---

## 2026-09-01（C の周）— 大きいチェスト（隣り合った 2 個で 54 枠）

`AUTODEV-QUEUE.md` の 2 番。仕様は `AUTODEV-SPEC.md`（この周で `状態: 済` にしました）。
**サブエージェントは使わず、親がそのまま実装しました** —— この環境では同じ番の中で
結果が返る呼び方ができないためで、`AUTODEV.md` の C-1 が認めている抜け道です
（点検も親が `git diff` を読んで自分でやりました。C-2 の但し書き）。

- **取ったもの**: 横に隣り合った 2 個のチェストを、1 つの 54 枠として開ける。
  - **ブロック ID もアイテム ID も 0 個**。組かどうかは `chestPartner()` が**そのつど
    voxel から**決めます（水平 4 マスに `CHEST` がちょうど 1 個で、相手から見てもちょうど 1 個）。
    これで「相方の相方は自分」が必ず成り立ち、**3 個並びも 2x2 も全部 27 枠に戻ります。**
  - **54 枠は参照を並べた「見え方」**（`Chests.open()` がそのつど作る）。中身は 1 マス
    27 枠のまま `map` に残るので、**セーブは 1 バイトも変わりません**（`version` は 1 のまま、
    1 マスにつき 54 要素のキーが 2 つ）。片方を壊しても残った側は 27 枠で中身を保ちます。
  - `addToChest()` の枠数を `CHEST_SIZE` から `state.slots.length` に変えました。
    **ここを 27 のままにすると、組の後ろ 27 枠に 1 個も入りません**（シフトクリックも
    かき集めもここを通ります）。
  - 画面側は `craftscreen.ts` の `get chestSize()` 1 本だけ。`inventoryui.ts` は
    枠を 54 個作って `craft.chestSize` を超えたぶんに `hidden` を付けるだけで、
    **27 / 54 を書いていません**（`main.ts` は `chests.open(world, ...)` の 1 行だけ）。
- **見送ったもの**: **`placing.ts` に「3 個目を置けない」制限を足すこと** ——
  本家はそうしますが、規則が voxel だけで決まるという一点を崩すので入れませんでした
  （仕様書の禁じ手。代わりに `TUNING.md` に 1 行）。
  **`breaking.ts` に相方を消す経路**もベッドと違って要りません（片方だけ壊れてよい）。
- **差し戻し**: 0 回。
- **枠**: 変化なし。1..63 の空き 9 / 111..255 の空き 145 / `main.ts` 1375 行（上限 1500）。
  `npm test` は **2307 → 2332 件**（+25）で全部緑。

---

## 2026-09-01（C の周）— 道具の耐久値（掘るたびに 1 減り、0 で壊れる）

`AUTODEV-QUEUE.md` の 3 番の**前半**（後半 = 3b はキューに残してあります）。
仕様は `AUTODEV-SPEC.md`（この周で `状態: 済`）。**サブエージェントは使わず親が実装し、
点検（C-2）も親が `git diff` を読んでやりました** —— この環境では同じ番の中で結果が返る
呼び方ができないためで、`AUTODEV.md` の C-1 / C-2 が認めている抜け道です。

- **取ったもの**: 木 59 / 石 131 / 鉄 250 / ダイヤ 1561 回（**本家のまま**）。0 で消えます。
  - **ブロック ID もアイテム ID も 0 個。** 回数は新しい `src/durability.ts` の `TOOL_USES`
    で、**`items.ts` の `ToolDef` には 1 行も足していません**（あちらは速さの表）。
  - **判断は全部 `durability.ts`**（減るか・壊れたか・帯の割合・壊れたときの 1 行・
    セーブの形）。`ui.ts` は `wearBar()` を `style.width` に貼るだけ、`main.ts` は
    `wearSlot()` の戻り値を見るだけ（**回数も「壊れたか」の式も書いていません**）。
  - **セーブは `SaveData.wear`（36 要素・省略可）を 1 つ足しただけ**で、`inventory` の
    `[item, count]` は形を変えていません（`version` は 1 のまま）。**全部新品なら
    キーごと出ない**ので、道具を傷めていない人のセーブは 1 バイトも変わりません。
  - `Slot.damage` は**省略可**にしました。必須にすると `smelting.ts` / `craftscreen.ts` /
    `inventoryui.ts` の `Slot` を作る 4 か所に手が要り、**仕様書が「触らない」と決めた
    ファイル**でした（省略可なら 1 行も触らずに済みます）。
- **見送ったもの**（どれも仕様書の禁じ手。**次の周で拾わないこと**）:
  - **落とし物・チェスト・インベントリ画面での傷の持ち回り** —— セーブの形が 3 つ動くので
    3b の仕事です。**傷んだ道具を落として拾い直す／画面で掴んで動かすと新品に戻ります**
    （`TUNING.md` に 1 行）。
  - **壊れる音** —— `sfx.ts` は「確かめられないもの」の相方なので、ユーザーと決める周に。
  - **火打石と打ち金・弓の耐久値** —— 3b（減る条件が「掘る」ではないので別の判断が要ります）。
- **仕様書から変えた点が 2 つ**（どちらも偽陽性・型エラーを避けるため）:
  - 見張り 1 の語を `.damage` から **`damage =` / `damage +` / `damage ??`** にしました ——
    `main.ts` には矢の `shot.damage` が既にあり、**`.damage` では素で赤くなります**
    （`rules/testing.md` の「自分で書いた説明が引っかかる」と同じ罠の変種）。
  - `RestoreTargets.inventory` に `deserializeWear` を**必須**で足したので、
    `test/session.test.ts` の偽物 2 つに 1 行ずつ足しました（**判定は 1 つも緩めず、
    「傷は中身のあとで戻す」の順番の判定を 1 件足しています**）。
- **差し戻し**: 0 回。
- **枠**: **ID は 0 個**（1..63 の空き 9 / 111..255 の空き 145 のまま）。
  `main.ts` **1381 行**（上限 1500）。`npm test` は **2332 → 2382 件**（+50）で全部緑
  （**音の 1 件だけは前からの揺れ**。`HANDOFF.md` を見てください）。

## 2026-09-01（B の周）— 3b を 3 件に割って仕様書にした

- **取ったもの**: `AUTODEV-QUEUE.md` の先頭 3b。**`src/**` と `test/**` は 1 行も触っていません**
  （B の周の完了条件。`git diff -- src test` が空であることを確かめました）。
- **数え直して分かったこと**（B の決まり。キューは数周前の A が作ったもの）:
  - **傷が付く物は全部 `stack: 1`**（`items.ts` の道具 12 本）。**傷は必ず 1 個ずつ動き**、
    半端な山に混ざりません —— これで「半分だけ傷んだ山を割る」話が丸ごと消えました。
  - 傷が消える経路は **`craftscreen.ts` の 5 か所と `inventory.ts` の `addToSlots()`**。
    `gather()` は上限 1 の物で即戻るので触らなくてよく、`swap()` は 3 の周でもう入っています。
  - 道具は**燃料でも焼ける物でもない**ので、かまどの枠へはシフトクリックで入りません
    （手で置くことはできます。**セーブで消えるのは 3c**）。
- **2 件でなく 3 件に割りました**（`AUTODEV.md` の B は「2 件に割る」）。元の 3b が
  **「傷を運ぶ」と「火打石と打ち金・弓」という別の話を 1 行に持っていた**ためで、
  運ぶ側を 3b（画面の中）と 3c（世界の側）に割り、使ったときに減る話を 3d に出しました。
  **3c はセーブの省略可キーが 3 つ要るので、B の周でまた割れるかもしれません。**
- **見送ったもの**: 無し（キューの先頭をそのまま取りました）。
- **差し戻し**: 0 回（実装していないので該当なし）。
- **枠**: **ID は 0 個**。`main.ts` は触らず **1380 行**。`npm test` は **2382 件のまま全部緑**
  （`src/**` を触っていないので増えないのが正しい）。`AUTODEV-SPEC.md` は **119 行 / 上限 120**。

## 2026-09-01（C の周）— 掴んで動かしても傷が残る（インベントリ画面の中だけ）

- **取ったもの**: `AUTODEV-SPEC.md` の 3b（`AUTODEV-QUEUE.md` の先頭）。**ID は 0 個。**
- **判断の置き場**: `durability.ts` に **`damageOf()` と `carryWear()` の 2 本だけ**足し、
  運ぶ側（`inventory.ts` / `craftscreen.ts`）には `?? 0` も「道具かどうか」も書きませんでした。
  セーブは **`serializeWear()` / `deserializeWear()` を使い回し**（`Slot[]` を取るので
  盤面 9 + 手 1 の 10 枠にそのまま効きます）。**2 本目は書いていません。**
- **塞いだ経路 6 つ**: `transfer()`（掴む・置く・入れ替え）/ `swapHotbar()`（数字キー）/
  `release()`（ドラッグ配分）/ `returnSlot()`（閉じるときの返却）/ `quickMove()`（シフトクリック）/
  `addToSlots()`（`damage` を**既定 0 の省略可**で受ける ——
  必須にすると `chests.ts` に手が要る、と仕様書が禁じていました）。
  `pressCreative()` は逆向きで、**湧かせたものに `carryWear(held, 0)` を掛けて新品に固定**しました。
- **仕様書から 1 か所だけ外れました**（次の周が同じ判断をしなくて済むように書き残します）:
  - 仕様書は **`main.ts` を「1 行も触らない」**としていましたが、
    **`craftWear: craft.serializeWear(),` の 1 行だけ足しました。** これが無いと
    `SaveParts.craftWear` を誰も渡さず、**セーブの往復はテストの中だけで成り立って
    ゲームでは 1 度も書き出されません**（完了条件の「セーブの往復で傷が残る」が
    実物では嘘になります）。足したのは**値を貼るだけの配線**で、
    `test/durability.test.ts` の見張り（`main.ts` に `maxUses(` / `damage =` などが無い）は
    そのまま緑です。**`drops.ts` / `chests.ts` / `furnaces.ts` は 1 行も触っていません。**
  - `test/session.test.ts` は**判定を 1 つも書き換えず**、偽物に `deserializeWear` の
    スタブを 2 行と `parts()` に `craftWear: undefined` を 1 行足しただけです
    （`RestoreTargets.craft` に必須で足したため。読み込みの順の判定は
    `test/durability.test.ts` 側に **`applyRestore()` を実際に通す形**で足しました）。
- **見送ったもの**（仕様書の禁じ手。**次の周で拾わないこと**）:
  - **落とし物・チェスト・かまどの中身の傷** —— 3c。`quickMoveFromInventory()` の
    チェスト・かまど行きには傷を渡していません（受け側が持てないため）。
  - **火打石と打ち金・弓** —— 3d（減るのが「掘ったとき」ではないので別の判断）。
- **差し戻し**: 0 回（点検は親が `git diff` を読んで自分でやりました ——
  この環境ではサブエージェントの結果が同じ番の中で返らないため。`AUTODEV.md` の C-2）。
- **枠**: **ID は 0 個**（1..63 の空き 9 / 111..255 の空き 145 のまま）。
  `main.ts` **1381 行**（上限 1500）。`npm test` は **2382 → 2420 件**（+38）で全部緑、
  `npm run typecheck` と `npm run build` も通りました。

## AUTODEV 3c-1: 落とし物が傷を運ぶ（2026-09-01・C の周）

**取ったもの**: `AUTODEV-QUEUE.md` の先頭 `3c-1`（`AUTODEV-SPEC.md` の仕様どおり）。
落とした道具を拾い直しても・死んで拾い直しても・読み込み直しても・次元をまたいでも傷が残ります。

- **傷を載せる入口を 1 か所に絞りました**: `Drops.spawn()` の `options.damage` から
  `carryWear(drop, ...)` を呼ぶだけ。`burst()` / `throwOut()` / `deserialize()` は
  そこへ素通しするので、**`drop.damage` への直の代入はコードのどこにもありません。**
- **丸め方も 1 本にしました**: `durability.ts` に **`wornValue(item, raw)`** を足し、
  `deserializeWear()` はそれに委ねる 2 行になりました（判定は 1 つも変えていません ——
  `test/durability.test.ts` の「最大 - 1 に丸める」「長すぎる・負でも落ちない」が
  そのまま緑です）。落とし物の読み戻しも同じ 1 本を通ります。
- **セーブは `drops` の 5 要素を触らず、省略可キー `dropWear` を別に足しました**
  （1 山 1 要素。`wear` を `inventory` と分けたのと同じ理由）。全部新品なら
  `serializeWear()` が `undefined` を返してキーごと消えるので、**道具を落としていない人の
  セーブは 1 バイトも変わりません。** `version` は 1 のままです。
- **傷は平坦配列の位置（`i / 5`）から引いています。** 壊れた山を挟んだ配列で
  「飛ばしても傷がずれない」をテストにしてあります（`list` の並びから引くと、
  そこから先が 1 つずつずれます）。
- **仕様書から 1 か所だけ外れました**（次の周が同じ判断をしなくて済むように残します）:
  - 仕様書は `burst()` と `throwOut()` の**末尾**に `damage = 0` を足す、としていましたが、
    **`burst()` だけ `random` の前**（`burst(item, count, x, y, z, damage, random)`）にしました。
    末尾にすると `main.ts` の `dropOnDeath()` が乱数の差し替え口に `undefined` を渡すことに
    なり、**遊ぶ側が「テストのための引数」を知らないと傷を渡せません。**
    `random` を渡している呼び出しは `src/**` にも `test/**` にも 1 つも無いので、
    入れ替えても壊れるものはありません（`throwOut()` は仕様どおり末尾です）。
- **見送ったもの**（仕様書の禁じ手。**この周で拾わないこと**）:
  - **チェスト・かまどの中身の傷** —— 3c-2。`chests.ts` / `furnaces.ts` / `breaking.ts` は
    1 行も触っていません（`BreakDrop` に `damage` を足していません）。
  - **地面の山に耐久値の帯を出すこと** —— `droprender.ts` は 1 行も触っていません
    （見た目を増やすと、この環境では確かめられないものが増えます）。
  - **`mergeAll()` の「傷が違えば統合しない」** —— `stack: 1` なので起きません。
    起きないことを**値を出して**（2 山・傷 10 / 40）テストに固定しました。
- **差し戻し**: 0 回（点検は親が `git diff` を読んで自分でやりました ——
  この環境ではサブエージェントの結果が同じ番の中で返らないため。`AUTODEV.md` の C-2）。
- **枠**: **ID は 0 個**（1..63 の空き 9 / 111..255 の空き 145 のまま）。
  `main.ts` **1382 行**（上限 1500。足したのは配線 4 行）。`npm test` は
  **2420 → 2446 件**（+26）で全部緑、`npm run typecheck` と `npm run build` も通りました。

## AUTODEV 3c-2: チェストとかまどが傷を運ぶ（2026-09-01・C の周）

**取ったもの**: `AUTODEV-QUEUE.md` の先頭 `3c-2`（`AUTODEV-SPEC.md` の仕様どおり）。
**これで傷が消える経路は 1 つも残っていません** —— 器へ入れる・出す・器を壊す・
読み込み直す・次元をまたぐ、のどれを通っても傷が付いて回ります。

- **`durability.ts` は 1 行も触っていません**（仕様書の「触らないもの」）。
  `serializeWear` / `deserializeWear` / `damageOf` / `carryWear` の 4 本が
  `Slot[]` にそのまま効くので、器の側は**呼ぶだけ**になりました。
  丸め（`wornValue()`）も器へは写していません —— **壊れた値の判定
  （`"x"` / 負 / 最大以上 → `最大 - 1`）を `test/chests.test.ts` と
  `test/smelting.test.ts` の両方で値を出して確かめてあります。**
- **セーブは 54 要素と 9 要素を触らず、省略可キーを 2 つ足しました**
  （`chestWear` は 1 台 27 要素・`furnaceWear` は 3 要素。キーは `chests` / `furnaces` と
  同じ `"x,y,z"`）。**省く条件も `serialize()` と揃えました**（`isChestEmpty` / `isIdle`）——
  ずらすと別の台の傷が載ります。全部新品なら `undefined` を返してキーごと消えるので、
  **道具を器に入れていない人のセーブは 1 バイトも変わりません。** `version` は 1 のままです。
- **`deserialize(raw, wear)` は 2 本に分けていません**（`drops.deserialize()` と同じ形）。
  `map` を作り直すのがここなので、別呼び出しにすると順番を間違えた瞬間に傷だけ消えます。
- **`Burst` に `damage?` を足して素通しにしました。** `breaking.ts` は「道具かどうか」も
  「何回で尽きるか」も知りません。`test/breaking.test.ts` は**偽の器**に `damage` を
  持たせて確かめています（本物の `Chests` は持ち込んでいません）。
- **仕様書から外れた判断が 1 つ**（次の周が同じ所で迷わないように残します）:
  - 仕様書の完了条件にある「**シフトクリックでかまどの材料枠へ入れると傷が残る**」は、
    **いまの表では道具で届きません** —— 焼けるもの（`SMELTING` 5 本）にも
    燃料（`FUEL` 8 本）にも道具が 1 本も無いので、`quickMove` はかまどを素通りします。
    `moveInto()` に `carryWear(into, damageOf(from))` は仕様どおり入れたうえで、
    **「いま道具は焼けも燃えもしない」ことを値で出し、`moveInto()` の呼び出しの形を
    見張る**という 2 本立てにしました（`rules/testing.md` の「呼び出しの側も見ること」）。
    本家のように鉄の道具が焼けるようになった日に、黙って新品に戻らないための足場です。
- **見送ったもの**（仕様書の禁じ手。**この周で拾わないこと**）:
  - **`durability.ts` の先頭のコメント** —— 「チェストとかまどの中身はまだ
    `[item, count, ...]` のまま」と書いてありますが、**もう嘘です。**
    「触らないもの（1 行も）」に挙がっているので直していません。**`HANDOFF.md` の
    「人がやること」に回しました**（`TUNING.md` の 317 行目は書き直してあります）。
  - **器の中で耐久値が減る形**（焼いても入れても減りません。減るのは掘ったときだけ）。
  - **山（`count > 1`）に傷を載せる経路** —— `addToChest()` の `damage` は
    `addToSlots()` へ素通しするだけで、載るのは空き枠へ入れた 1 個ぶんです。
    石の山に傷が載らないことを値で出して固定しました。
- **差し戻し**: 0 回（点検は親が `git diff` を読んで自分でやりました ——
  この環境ではサブエージェントの結果が同じ番の中で返らないため。`AUTODEV.md` の C-2）。
- **枠**: **ID は 0 個**（1..63 の空き 9 / 111..255 の空き 145 のまま）。
  `main.ts` **1383 行**（上限 1500。**足したのは 0 行** —— 配線 3 行を書き換えただけ）。
  `npm test` は **2446 → 2488 件**（+42）で全部緑、`npm run typecheck` と
  `npm run build` も通りました。

## 3d. 火打石と打ち金・弓の耐久値（2026-09-02・C の周）

**取ったもの**: `AUTODEV-QUEUE.md` の先頭 3d。**火打石と打ち金は 64 回、弓は 384 回**で
壊れるようにしました（本家のまま）。**ID は 1 個も使っていません。**

- **`maxUses()` を 1 本のまま通しました。** `toolOf()` が null のときに `usedUp(item)` を
  返す形（`isFireStarter()` → 64 / `isBow()` → 384 / 他は 0）にしたので、
  **帯（`wearBar()`）もセーブの 5 つも自動で付いてきました** —— `serializeWear()` も
  `wornValue()` も `maxUses()` にしか聞いていないためです。**新しいキーは 1 つも足していません**
  （`SaveData.version` は 1 のまま。テストが `storage.ts` のキーを数えて 5 つのままだと出します）。
- **逆向きの守りを `wearForBreaking()` に 1 行足しました**（`toolOf(item) === null` なら 0）。
  これが無いと、**弓を持って石を掘るだけで弓が減ります**（`wearable()` が true になった瞬間に）。
- **`main.ts` は 12 行**（`wearHeld()` 5 行 + 配線 2 行 + コメント）。回数も「弓かどうか」も
  持っていません。`breakBlock()` の 2 行も同じ `wearHeld()` に寄せたので、
  **壊れたときの 1 行を出す場所は 1 か所**になりました。

**見送ったもの**（仕様書の禁じ手。**次の周でも拾わないこと**）:

- **殴って減る形**（剣が入る 4 の周の話。いまは殴っても 1 も減りません）。
- **火種・弓に `tool:` を付ける形** —— `ToolDef` は「掘る速さ」の表なので、
  付けると**弓で石が速く掘れます。**
- **`bow.ts` / `use.ts` / `placing.ts` / `portals.ts`** は 1 行も触っていません
  （引きの長さも右クリックの振り分けも点火の可否も、もうそれぞれ 1 か所にあります）。

**差し戻し**: 0 回（点検は親が `git diff` を読んで自分でやりました ——
この環境ではサブエージェントの結果が同じ番の中で返らないため。`AUTODEV.md` の C-2）。

**枠**: **ID は 0 個**（1..63 の空き 9 / 111..255 の空き 145 のまま）。
`main.ts` **1395 行**（上限 1500。**+12 行**）。`npm test` は **2488 → 2525 件**（+37）で
全部緑、`npm run typecheck` と `npm run build` も通りました。

## 4. 剣 4 本（2026-09-02・C の周）

**取ったもの**: `AUTODEV-QUEUE.md` の先頭 4。**木・石・鉄・ダイヤの剣**を足しました。
作業台で作れて、殴ると強く（4.5 / 5 / 5.5 / 6）、殴ると減ります（59 / 131 / 250 / 1561 回）。
**ID は 111..114 の 4 個**で、**共有帯 111 以降の最初の使用者**です。

- **`ToolKind` に `"sword"` を足すのがいちばん安い、は本当でした。** `tool:` を持たせた
  時点で `maxUses()` が `toolOf()` に聞くので、**耐久値の表もセーブの 5 キーも帯も
  1 行も書かずに付いてきました**（3d の周とまったく同じ形）。新しい表は 1 つも作っていません。
- **`95..110` の穴を通る道は値で確かめました。** `MAX_ITEM_ID` が 94 → 114 へ飛びましたが、
  `allItemIds()` は `ITEMS[id]` の無い番号を飛ばすので、**アイテムは 78 → 82 種**
  （+4 ちょうど）、**クリエイティブの一覧も 82 枠**、`111..255 の空き` は **145 → 141**。
  ブラウザで開いて一覧の末尾に剣 4 本が並ぶところも撮りました（`creative-swords.png`）。
- **`main.ts` は 5 行**（`mobs.attack()` の戻り値を受ける `if` とコメント）。**+5 行で 1400 行。**
  攻撃力も回数も「剣かどうか」も持っていません。
- **`wearForAttack()` を 3 本目として立てました。** `wearForUse()` に混ぜると
  **剣を右クリックしただけで減ります**。逆に `wearForBreaking()` は 1 行も触っていません ——
  剣は `toolOf()` が非 null なので「掘れば減る」（本家と同じ）が自動で付いてきました。
- **剣の形（材料 2 + 棒 1 の縦 3）はシャベル（材料 1 + 棒 2）と上下 1 マスしか違いません。**
  取り違えていないことを `test/crafting.test.ts` が両方向で見ています
  （上下を入れ替えるとシャベルになる、を判定に入れました）。

**見送ったもの**（仕様書の禁じ手。**次の周でも拾わないこと**）:

- **ツルハシ・斧・シャベルを「殴ると減る」に変えること**（本家はそうですが、
  **既存 12 本の寿命が黙って縮みます**）。
- **`TIER_ATTACK` を本家の +1 にすること** —— 表を 12 本と共有しているので、
  剣だけでなく既存の道具も一緒に強くなります（`TUNING.md` に 1 行残しました）。
- **ブロックに `tool: "sword"` を付けること**（剣が採掘道具になります。テストで見張り）。

**差し戻し**: 0 回（点検は親が `git diff` を読んで自分でやりました ——
この環境ではサブエージェントの結果が同じ番の中で返らないため。`AUTODEV.md` の C-2）。

**枠**: **ID 4 個**（1..63 の空き 9 のまま / **111..255 の空き 145 → 141**）。
`main.ts` **1400 行**（上限 1500。**+5 行**）。`npm test` は **2531 → 2575 件**（+44）で
全部緑、`npm run typecheck` と `npm run build` も通りました。

## 5. シアーズ（2026-09-02・C の周）

**取ったもの**: `AUTODEV-QUEUE.md` の 5 番（`AUTODEV-SPEC.md` の仕様どおり）。
**アイテム 115 を 1 個だけ**使い、ブロックは 1 個も取っていません。

- **仕様書の禁じ手 1（`ToolKind` に足さない）が正しかったことを、値で確かめました。**
  `tool:` を持たせずに `usedUp()` の 3 本目にしたので、**掘っても殴っても減りません**
  （`wearForBreaking(石) = 0` / `wearForAttack = 0` / `wearForUse = 1`）。
  剣（4 の周）は逆に `tool:` を持たせるのがいちばん安かったので、**「新しい道具は
  `ToolKind` に足す」でも「足さない」でもなく、減り方の 3 種類のどれかを先に決めること。**
- **`def.drop` を直に読む 2 か所を `dropFor(mob, def)` の 1 本に寄せました。**
  仕様書の警告どおり、`attack()` だけ直すと**弓で撃ったときだけ刈った羊から羊毛が出ます**。
  テストは殴った側と撃った側を**並べて**測っています（片方だけでは気付けません）。
- **右クリックがモブを見る道は新しく作らず、左クリックの式を `mobIsNearer()` として
  出しました。** `main.ts` に距離の比較を書くと、殴れる間合いと刈れる間合いが食い違い、
  「殴れるのに刈れない羊」がブラウザを開くまで分かりません。
  `test/ui.test.ts` の `routed` は **`useOrPlace(mobIsNearer(`** という形で見張っています
  （`useOrPlace();` のままだと、比較を書き直しても緑で通ります）。
- **`decideUse()` の先頭に置きました**（器より前）。あとにすると、作業台やチェストの
  前に立った羊だけ刈れません —— **そこまで追い込まないと気付けない形**の壊れ方です。
- **セーブは 1 バイトも増えていません**（`Mob.woolTimer` はモブの持ち物で、モブは保存しない）。
  `SaveData.version` は 1 のままで、キーも増えていません。
- **既存のテストを 2 つ書き直しました。どちらも緩めていません:**
  - `test/crafting.test.ts` の「2x2 ではバケツは作れない」が `=== null` を見ていましたが、
    **2x2 の斜めはシアーズが取った**ので `?.out !== BUCKET` に変えました（見たいのは
    「バケツにならない」ことそのもの）。**同じ形のレシピが増えたことに気付ける唯一の場所**でした
  - `test/durability.test.ts` の `main.ts` の `wearForUse(` を **2 → 3 回**（増やす側）
- **撮って見ました**（`docs/browser-shots/creative-shears.png`）。クリエイティブの一覧は
  **82 → 83 枠**、末尾は `… ダイヤの剣 → シアーズ`。名前は欠けも溢れもなく出ています。

**見送ったもの**（**次の周でも拾わないこと**）:

- **刈られた羊の見た目**（`AUTODEV-QUEUE.md` の 11 番）。`mobrender.ts` は形を
  `MobKind` ごとに 1 つ作って使い回すので、2 つ目の形と差し替えの仕掛けが要ります。
  **`Mob.woolTimer` はもう立っているので、描画側はそれを見るだけで済みます。**
- **刈る音を新しく足すこと**（`Sfx` は出来事 × 種類で膨らむ。既にある `"dig"` を鳴らす）。
- **`MobDef.drop` の表を書き換えること**（倒したときに何が出るかと、刈ったあとで
  出ないことは別の話。抑えるのは `dropFor()`）。

**差し戻し**: 0 回（点検は親が `git diff` を読んで自分でやりました。`AUTODEV.md` の C-2 ——
この環境ではサブエージェントの結果が同じ番の中で返らないため）。

**枠**: **ID 1 個**（1..63 の空き 9 のまま / **111..255 の空き 141 → 140**）。
アイテムは **82 → 83 種**、`MAX_ITEM_ID` 114 → **115**。
`main.ts` **1420 行**（上限 1500。**+20 行** —— 仕様書の「+20 行以内」ちょうど）。
`npm test` は **2575 → 2624 件**（+49）で全部緑、`npm run typecheck` と
`npm run build` も通りました。

## 6. クワと耕地（土を耕す）— 2026-09-02・C の周

**取ったもの**: `AUTODEV-QUEUE.md` の 6 番（`AUTODEV-SPEC.md` の全文）。**クワ 4 本と耕地。**
土か草を右クリックすると耕地になり、クワが 1 減る。耕地を掘ると土が 1 個落ちる。
**育つものはまだ植わりません**（割った後半 = 12 番）。

**仕様どおりに入りました**（差し戻し 0 回）。**`variantOf: DIRT` の 1 行で、アイテムも
ドロップ表も 1 行も書かずに「持てないブロック」になりました** —— `items.ts` の for が
`variantOf !== AIR` を飛ばし、`dropOf()` の既定が `baseBlock()` を返すためです
（点火中のかまど `FURNACE_LIT` と同じ仕掛け）。**共有帯に置いた最初のブロックで、
しかも立方体**です（`encodeFace` が 8 ビットなので置けます。`npm test` の「立方体 36」）。

**踏んだ罠**（仕様書の禁じ手 2 がそのまま当たりました）: `ToolKind` に `"hoe"` を足すと
`TOOL_NAMES` は `Record<ToolKind, …>` なので typecheck が抜けを教えますが、
**`mobs.ts` の `TOOL_ATTACK` は `Record<string, number>` なので教えません。**
足し忘れると `attackDamage()` が **NaN** を返し、クワで殴ったモブの体力が NaN になって
**二度と死にません。** `test/mobs.test.ts` に「クワで殴っても NaN にならない」を足して塞ぎました。

**既存のテストを 1 つ書き直しました。緩めていません**（**増やす側**）:
`test/blocks.test.ts` の「共有帯のアイテムは剣 4 本とシアーズの 5 個（115 まで）」を
**「剣 4 本・シアーズ・クワ 4 本の 9 個（120 まで）」**へ。`MAX_ITEM_ID === DIAMOND_HOE` も見ています。

**撮って見ました**（C-3）。**2 枚**:

- `docs/browser-shots/creative-hoes.png` —— クリエイティブの一覧は **83 → 87 枠**、
  末尾は `… シアーズ → 木のクワ / 石のクワ / 鉄のクワ / ダイヤのクワ`。色は道具と同じ
  階層の色（茶 / 灰 / 白 / 水色）で、**名前は 4 つとも枠の下に出ています。**
  **耕地（116）は一覧に出ません** —— `variantOf` が効いていることを目で確かめたものです
- `docs/browser-shots/farmland.png` —— **本物のブラウザで実際に耕した 1 枚**。クリエイティブで
  一覧から木のクワをホットバーへ入れ、草むらを刈ってから右クリック。`#debug` の
  **`target 耕地 (2, 45, -4)`** と `edits 3` が出ていて、緑の草原の中に**暗い茶色の 1 マス**が
  見えます。**console のエラーは 0 件**

**撮るときに分かったこと**（次の周のために `docs/browser-shots/README.md` にも書きました）:
**`page.mouse.click()` を使わないこと。** あれは先にポインタを動かすので、ポインタロック中は
**視点が飛んで狙いが外れます**（`target -` になって `edits 0`）。`mouse.down()` / `mouse.up()` で
押して離すだけにすること。**どちらが「下」かも決め打ちしないこと** —— `#debug` の `target` の
行を読みながら、地面を狙えるまで動かすほうが速く着きます。**平原は草むらだらけなので、
先に左クリックで刈らないと耕せません**（草むらは `tilled()` が `AIR` を返します。正しい挙動）。

**見送ったもの**（**次の周でも拾わないこと**）:

- **耕地が乾く・踏み荒らされる・何かが育つ**（`AUTODEV-QUEUE.md` の 12 番）。**毎フレーム
  進む仕掛けを `world.ts` に足すのはそちらの周**で、この周は 1 行も書いていません。
- **耕地の「乾き / 湿り」の 2 段階**（本家は水が近いと暗くなる）。**水を引く仕掛けが無い**ので、
  いま入れると ID を 1 個使って何も変わらないものが増えます。
- **クワで草むらを一発で刈ること**（本家の挙動）。**刈る道具はシアーズ**（5 の周）で、
  混ぜると「どの道具が何を取るか」が 2 か所に分かれます。
- **耕地の高さを 15/16 にすること**（本家）。**立方体のまま**にしてあります —— 非立方体に
  すると `shapeBoxes` と支えの判定に乗り、「立方体 36」の数え方も変わります。

**差し戻し**: 0 回。**実装はサブエージェント（sonnet）が同じ番の中で返しました**が、
**点検（C-2）は親が `git diff` を全部読んで自分でやりました**（`AUTODEV.md` の C-2 ——
書いた本人に確認させない）。`npm run typecheck` / `npm test` / `npm run build` も親が走らせ直しています。

**サブエージェントの起動について**（`AUTODEV.md` の C-1 に関わる実測）:
**`run_in_background: false` を渡しても背景で起動しました。** そこで番を閉じずに、
親が**転記ファイルに `"type":"result"` が出るまで前景で待つ**形にして、同じ番の中で
受け取りました（`sleep` は使えないので `timeout 5 tail -f /dev/null` で待つ）。**13 分 23 秒。**
**この待ち方なら C-1 をサブエージェントに出しても周を落としません。**

**枠**: **ID 5 個**（116 = 耕地 / 117..120 = クワ 4 本）。1..63 の空きは **9 のまま**、
**111..255 の空き 140 → 135**。アイテムは **83 → 87 種**、`MAX_ITEM_ID` 115 → **120**。
`main.ts` **1420 → 1433 行**（上限 1500。**+13 行** —— 仕様書の「+15 行以内」に収まりました）。
`npm test` は **2624 → 2672 件**（+48）で全部緑、`npm run typecheck` と `npm run build` も通りました。

## 12（前半）. 小麦の種と苗（耕地に植える）— 2026-09-02・C の周

**取ったもの**: `AUTODEV-SPEC.md` の「小麦の種と苗」（キューの 12 番を割った前半）。
草むらを壊すと **12.5% で小麦の種（アイテム 122）**、種を持って**耕地**を右クリックすると
その上に**小麦の苗（ブロック 121）**が立ちます。**まだ育ちません**（後半 = キューの 13 番）。

- `blocks.ts`: `WHEAT_CROP = 121`。草むらと同じ十字（`model: "cross"` / `CROSS_BOX` /
  `solid: false`）だが、**`replaceable` を付けず**、**`supportFace: FACE_YN`** を持ち、
  **`variantOf` は自分自身**
- `items.ts`: `WHEAT_SEEDS = 122`（`block: AIR` / `tool: null` / 積める 64）。`isSeed()` の表 1 本と
  `DROPS` の 2 行（草むら 12.5% + `otherwise` / 苗 → 種 1 個）
- `use.ts`: `plant` の注文を **`till` の次・バケツより前**に（14 通りめ）。可否は決めない
- `placing.ts`: `tryPlant()`（**`tryTill()` を写した形**。耕地でなければ `none`、
  上が塞がっていれば `blocked`）
- `main.ts`: `plantAt()` の配線だけ（**+12 行**。1433 → 1445 / 上限 1500）

**`variantOf` を自分自身に向けたのがこのタスクの肝です。** アイテムを作らせない入口は
`variantOf !== AIR` しか無いのに、**苗には大元にできる相手が居ません**（土でも耕地でもない）。
耕地（`variantOf: DIRT`）と違って `dropOf()` の既定が**アイテムの無い 121** を返すので、
**`DROPS` に「種 1 個」を書くのが必須**でした。書き忘れると地面に拾えない山が落ちます。

**見送ったもの**（**次の周でも拾わないこと**）:

- **育つ・乾く・踏み荒らす**（キューの 13 番）。`crops.ts` も `world.update()` の 1 行も
  書いていません。**セーブは 1 バイトも増えていません**（苗は `edits` に乗るただのブロック）
- **苗を段階ごとに低くすること**（本家）。段階そのものが 13 番の仕事なので、
  いま `boxes` を分けても比べる相手が居ません
- **種を食べられるようにすること**（本家でも食べられません）。`FOODS` は 1 行も触っていません
- **クワで草むらを刈ると種が出ること**（本家）。刈る道具はシアーズ（5 の周）で、
  混ぜると「どの道具が何を取るか」が 2 か所に分かれます

**差し戻し**: 0 回。**サブエージェントは使っていません** —— `AUTODEV.md` の C-1 は
「同じ番の中で返らないなら自分で実装すること」なので、前の周の実測（`run_in_background: false`
でも背景で起動し、転記ファイルを 13 分 23 秒待った）を踏まえて**親が直接書きました**。
点検（C-2）も親が `git diff` を全部読み、`npm run typecheck` / `npm test` / `npm run build` を
自分で走らせています。

**C-3（撮って見た）**: `docs/browser-shots/creative-seeds.png`（一覧 88 枠・末尾が小麦の種・
**苗は一覧に出ない**）と `wheat-crop.png`（**実際に刈って耕して植えたところ**。
`target` が `草むら → 草 → 耕地 → 小麦の苗` と変わり、`edits 4`）。**2 枚とも `Read` で開いて
見ました。** 写った気になる点は 1 つで、**苗の緑が草原の緑に紛れること**（`TUNING.md`）。
**不具合ではないので直していません。**

**枠**: **ID 2 個**（121 = 苗 / 122 = 種）。1..63 の空きは **9 のまま**、
**111..255 の空き 135 → 133**。アイテムは **87 → 88 種**、`MAX_ITEM_ID` 120 → **122**。
`npm test` は **2672 → 2707 件**（+35）で全部緑、進行（クリア導線）は **13 / 13** のまま。

## 2026-09-02 — 13. 育つ器（`crops.ts`）と実った小麦（C の周・無人）

- **取ったもの**: 実った小麦（**ブロック 123**・`variantOf: WHEAT_CROP`）/ 小麦（**アイテム 124**）/
  **`crops.ts`（新規）** —— 位置ごとに「育った秒数」を持つ 3 つ目の器。
  ID は `ROADMAP.md` の予約表の「123..255 予備」の先頭から詰めて 2 個。
- **仕様書**: `AUTODEV-SPEC.md`（B の周・2026-09-02）を**全文そのまま**実装。7 項目とも守っています。

**このタスクの肝は「段階をブロック ID で表さないこと」です。** 本家は 8 段階 = ブロック ID 8 個
ですが、番号は 1 つも戻せない（`localStorage` にそのまま入る）ので、**2 段階に絞ったうえで
育ち具合はワールドに書かず `crops.ts` が `"x,y,z"` → 秒数の表で持ちます。** かまど（`furnaces.ts`）・
チェスト（`chests.ts`）と同じ器の形で、**`SaveData` に増えたのは省略可キー `crops` の 1 つだけ**
（`version` は 1 のまま。畑を作っていない人のセーブは 1 バイトも増えません）。

**仕様書から 1 点だけ変えました**（`RULES-INBOX.md` にも書いてあります）。仕様の 5-3 は
「`baseBlock(getVoxel(...)) !== WHEAT_CROP` なら忘れる」でしたが、**実った小麦は
`variantOf: WHEAT_CROP` なので `baseBlock()` が苗を返し、「もう実っている」を見分けられません。**
そのまま残すと `World.setVoxel()` が「同じ値」で **false を返し続け**（`world.ts:181`）、
**二度と消えない印**が毎フレーム書き込みを試み続けます。**素の `getVoxel(...) !== WHEAT_CROP`
に直しました**（掘られた・上書きされた・もう実っている、の 3 つを 1 本で拾えます）。
`test/crops.test.ts` の「もう実っているマスの印は忘れる」がこれを押さえています。

**見送ったもの**（**次の周でも拾わないこと**）:

- **収穫で種も戻ること**（キューの 14 番）。`Drop` が 1 ブロックにつき 1 山しか持てないので、
  **器のほうを先に直すのが正しい**（`chance` で分けると平均で種が減るだけ）
- **パンと食べること**（15 番）。`crafting.ts` も `FOODS` も 1 行も触っていません
- **明るさ・水・踏み荒らし**（本家）。見ているのは「真下が耕地か」だけです。
  松明を置く理由が畑に無いのは意図どおりで、入れるなら光の判断をどこに置くかから決めること
- **段階ごとに `boxes` を低くすること**。2 段階しかないので、比べる相手が 1 つしかありません

**差し戻し**: 0 回。**サブエージェントは使っていません**（`AUTODEV.md` の C-1「同じ番の中で
返らないなら自分で実装すること」。前の 2 周の実測どおり背景起動になるため）。点検（C-2）も
親が `git diff` を全部読み、`npm run typecheck` / `npm test` / `npm run build` を自分で走らせました。

**C-3（撮って見た）**: `tools/shot.ts` に場面 `crops` を 1 つ足して `npm run shot -- crops`
（**待たずに苗と実りを並べられます**）。**撮った絵は `docs/browser-shots/wheat-ripe.png`**（左が
実り・右が苗）。**`Read` で開いて見ました。** 実った黄色（`0xd8c26a`）は一目で分かりますが、
**苗の緑は相変わらず草原に紛れます**（前の周からの持ち越し。`TUNING.md` に書いて人へ回しました）。
本物のブラウザ（`node tools/browsershot.mjs`）でも 5 枚撮り、**WebGL 2.0 / `gl.getError()` = 0 /
例外 0 件**を確かめています。

**撮るときに 1 つ踏みました**（`tools/shot.ts` にコメントで残してあります）:
**`world.setVoxel()` はメッシュ化のキューに積むだけで流しません。** 流すのは
`primeAround()`（と `world.update()`）なので、**書き換えたあとに呼ばないと編集前の地形が
そのまま写ります。** 1 枚目がまさにそれで、畑が 1 マスも写っていませんでした。

**枠**: **ID 2 個**（123 = 実った小麦 / 124 = 小麦）。1..63 の空きは **9 のまま**、
**111..255 の空き 133 → 131**。アイテムは **88 → 89 種**、`MAX_ITEM_ID` 122 → **124**。
`npm test` は **2707 → 2783 件**（+76）で全部緑、進行（クリア導線）は **13 / 13** のまま。
**`main.ts` は 1445 → 1450 行（`npm test` の数え方）で、`AUTODEV.md` の停止条件 3 の線に
ちょうど乗りました。次の周は 1 行でも足す前に判断を出すこと。**

## 2026-09-02 — 14. 1 回の採掘で 2 山落ちる器（収穫で種も戻る）（C の周・無人）

**取ったもの**: `AUTODEV-QUEUE.md` の先頭（14 番）を `AUTODEV-SPEC.md` の全文どおりに実装。
**`Drop.extra?: DropStack` を 1 行**足し、`items.ts` に **`rollDrops(blockId, roll): readonly DropStack[]`**
（0〜2 山）を新設して、`breaking.ts` の `harvest()` の戻りを **`Burst[]`** にしました。
`DROPS` で `extra` を持つのは**実った小麦の 1 行だけ**（小麦 1 + 種 1）。

**見送ったもの**: **種を 0〜3 個の範囲にすること**（本家の値）—— 流れてくる乱数が `roll` の
1 本しかないので、**個数を振ると 1 山目の当たり外れと必ず相関します**（砂利の火打石と
種の個数が連動する形）。乱数をもう 1 本流すのは `BreakOrder` と `autoBreak()` の引数と
`main.ts` に及ぶ別の話なので、**種は 1 個固定**にして `TUNING.md` に 1 行残しました。

**差し戻し**: 0 回。**サブエージェントは使っていません**（`AUTODEV.md` C-1 の
「同じ番の中で返らないなら自分で実装すること」。前の 5 周とも背景起動になったため）。
点検（C-2）も親が `git diff` を全部読み、`npm run typecheck` / `npm test` / `npm run build` を
自分で走らせました。

**踏んだもの**: **既存のテスト 2 件が「いまは種が戻らない」を根拠にしていました。**
`test/blocks.test.ts` のほうは条件（`rollDrop()` に種が出ない）がそのまま生きているので
**ラベルだけ**を「rollDrop() には種が出てこない（1 山目だけを答えるため）」に直し、
`test/breaking.test.ts` のほう（`out.drops.every(...)`）は**この周が変えた振る舞いそのもの**
なので 2 山の判定に置き換えました。**判定をゆるめてはいません**（両経路とも山の中身・個数・
出る場所まで見ています）。

**C-3（撮って見た）**: `npm run build` のあと **`npm run shot -- crops`** を撮り直して `Read` で
確認。描画は 1 行も触っていないので**苗と実りの見た目は前の周のまま**で、**苗の緑が草原に
紛れる件も変わっていません**（人へ回す持ち越し）。本物のブラウザ（`node tools/browsershot.mjs`）
でも 4 枚撮り、**WebGL 2.0 / 例外 0 件**・HUD と地形が正常に出ることを確かめました。

**枠**: **ID 0 個**（既存の 122 / 123 / 124 の組み合わせだけ）。1..63 の空き **9**・
111..255 の空き **131** とも動かず、`MAX_ITEM_ID` は **124** のまま。**`SaveData` も 1 バイトも
増えていません**（`version` は 1）。**`main.ts` は 1450 行のまま 1 行も触っていません**
（停止条件 3 の線に乗ったままなので、次の周も判断を出す前に足さないこと）。
`npm test` は **2783 → 2798 件**（+15）で全部緑、進行（クリア導線）は **13 / 13**。

## 2026-09-03 — 15. パン（小麦 3 → パン）と食べること（C の周・無人）

**取ったもの**: `AUTODEV-QUEUE.md` の 15 番。**小麦 3 個の横一列でパン 1 個**（3 幅なので
作業台が要る。本家と同じ）と、**食べると空腹 +5 / 満腹度 +6**（`FOODS` の 1 行）。
これで**小麦に使い道ができました** —— 前の周までは掘れるだけで、持っていても何にもなりません。

**触ったのは `src/` の 2 ファイル・6 行だけ**です:
`items.ts` に `BREAD = 125` の定数 + `MAX_ITEM_ID` の差し替え + `item({...})` 1 行 +
`FOODS` に 1 行、`crafting.ts` に `RECIPES` の 1 行と import 2 語。
**`main.ts` / `use.ts` / `vitals.ts` / `smelting.ts` / `blocks.ts` / `craftscreen.ts` /
`inventoryui.ts` は 1 行も触っていません**（仕様書の 2）。

**この周がいちばん確かめたかったのは「食べ物を足すのに何行要るか」です。**
答えは**表の 1 行**でした。右クリックの振り分け（`use.ts` の `decideUse()`）も、
食べ終わるまでの長さ（`vitals.ts` の `Eating`）も、クリエイティブ一覧（`allItemIds()`）も、
**もう `foodOf()` 1 本を通っている**ので、アイテムの名前をどこにも書かずに済みます。
`test/use.test.ts` に「パンも同じ経路で食べられる」を足したのは、**次に食べ物を足す人が
`use.ts` を触り始めていないか**を見るためです。

**見送ったもの**: **小麦を `FOODS` に足すこと**（本家と同じで、パンにしてから食べます。
足すとパンを作る理由が消えます）。**パンをかまどで焼く形**（本家にありません）。
**`stack: 1`**（傷が付く物ではないので 64 のまま）。

**差し戻し**: 0 回。**サブエージェントは使っていません**（`AUTODEV.md` C-1 の
「同じ番の中で返らないなら自分で実装すること」。前の 6 周とも背景起動になったため）。
点検（C-2）も親が `git diff` を全部読み、`npm run typecheck` / `npm test` / `npm run build` を
自分で走らせました。

**踏んだもの**: **1 つもありません**（この周は既存のテストを 1 件も書き換えていません）。
`test/blocks.test.ts` の共有帯の行だけは**数え直し**（11 個 → 12 個・ラベルも 125 まで）で、
**判定をゆるめてはいません**。`test/vitals.test.ts` にはもともと `bread` という名前の
仮の値（`{ hunger: 5, saturation: 6 }`）が別のブロックに居たので、**本物のほうは `loaf`**
という名前にしてあります（次に触る人へ）。

**C-3（撮って見た）**: `npm run build` のあと **`npm run shot -- crops`** を撮り直して `Read` で
確認 —— 描画は 1 行も触っていないので**畑の見た目は前の周のまま**（変わっていないことが正解）。
そのうえで**本物のブラウザ**（`/opt/pw-browsers` の Chromium・使い捨てスクリプト）で
**クリエイティブの一覧を末尾まで送って撮りました**（`docs/browser-shots/creative-bread.png`。
12 枚目）: **枠 90 個**（種の周の 88 から小麦とパンで +2）、末尾は **小麦 → パン**、
パンの色 `rgb(196,154,94)` は**小麦の `rgb(216,194,106)` と隣り合っても分かれます**。
**console のエラーは 0 件**（残るのは SwiftShader の warning だけ）。
**板・原木の茶色と近い系統**なのは人へ回しました（`TUNING.md` / `HANDOFF.md`）。

**枠**: **ID 1 個**（125 = パン）。111..255 の空きは **131 → 130**、1..63 の空きは **9** のまま、
`MAX_ITEM_ID` は **124 → 125**、アイテムは **89 → 90 種**。**`SaveData` は 1 バイトも
増えていません**（`version` は 1・キーも増えません）。**`main.ts` は 1450 行のまま
1 行も触っていません**（停止条件 3 の線に乗ったままなので、次の周も足す前に判断を出すこと）。
`npm test` は **2801 → 2818 件**（+17）で全部緑、進行（クリア導線）は **13 / 13**。

## 2026-09-03 — 16. 鶏（3 種類目の受動モブ）と生鶏肉・焼き鳥（C の周・無人）

**取ったもの**: `AUTODEV-QUEUE.md` の 7 番（前半）。**草地に鶏が湧いて、倒すと生鶏肉が
1 個出て、かまどで焼くと焼き鳥になる**。**受動モブが 3 種類目**になり、豚以外にも
肉の出どころができました。

**触ったのは `src/` の 3 ファイル**です: `mobs.ts`（`MobKind` に 1 語 + `CHICKEN` の
`MobDef` 1 つ + `MOBS` / `MOB_KINDS` / `PASSIVE_KINDS` に 1 語ずつ）/ `items.ts`
（定数 2 つ + `MAX_ITEM_ID` の差し替え + `item({...})` 2 行 + `FOODS` 2 行）/
`smelting.ts`（`SMELTING` 1 行と import 2 語）。
**`main.ts` / `mobmesh.ts` / `mobrender.ts` / `use.ts` / `vitals.ts` / `crafting.ts` /
`blocks.ts` / `drops.ts` / `craftscreen.ts` / `inventoryui.ts` / `storage.ts` は 1 行も
触っていません**（`git diff --numstat` で確かめました。仕様書の 2）。

**この周がいちばん確かめたかったのは「モブを 1 種類足すのに描画が何行要るか」です。**
答えは **0 行**でした（B の周の見込みどおり）。`buildMobMesh()` は `MobDef.groups` /
`boxes` を回すだけ、`MobRenderer` は `MOBS[kind]` から形を作るだけで、**どちらも
種類の名前を知りません。** 形の点検（体積・巡回順・振る部位・当たり判定に収まるか）も
`MOB_KINDS` を回す `test/mobs.test.ts` に**自動で乗りました**（鶏ぶん 11 件が
1 行も書かずに増えています）。

**見送ったもの**: **羽根**（`MobDef.drop` は 1 つしか落とせないので、2 つ目の落とし物を
足す話が先。7b へ）/ **卵**（7c へ）/ **生鶏肉の食中毒**（本家は 30% だが `FoodDef` に
確率が無い。足すと `vitals.ts` まで及ぶ。`TUNING.md`）/ **本家の「ゆっくり落ちる」**
（`MobDef.flying` は「重力を受けず自分で高さを決める」なので、付けると壁も崖も無視して
飛んでいきます。落下速度の上限を足す別の話）/ **刈られた羊のような 2 つ目の形**（11 番）。

**差し戻し**: 0 回。**サブエージェントは使っていません**（`AUTODEV.md` C-1 の
「同じ番の中で返らないなら自分で実装すること」）。点検（C-2）も親が `git diff` を
全部読み、`npm run typecheck` / `npm test` / `npm run build` を自分で走らせました。

**踏んだもの 2 つ**:

1. **`test/mobs.test.ts` の「両方の種類が湧く」が `=== 2` の決め打ちでした。** 受動が
   3 種類になった瞬間に赤くなります。**`=== 3` に書き換えるのは数え直しですが、
   それでは次に足す人がまた同じ行を踏みます。** 表から数える形
   （`MOB_KINDS.filter((k) => !MOBS[k].hostile).every(...)`）に替えたうえで、
   **`=== 2` が拾えていた「余計な種類が混じらない」を別の 1 件として足しました**
   （替えただけだと、敵対が混じっても緑になる形にゆるみます）。
2. **`items.ts` の `MAX_ITEM_ID` を差し替えると `npm run typecheck` が
   `test/blocks.test.ts` を止めます**（`'127' と '125' に重なりが無い`）。
   **これは良い止まり方です** —— 共有帯の数え直しを忘れたまま緑になりません。

**C-3（撮って見た）**: `npm run build` → `http-server` → **`node tools/browsershot.mjs`**
（本物のブラウザ・定番 5 枚。WebGL 2.0・`gl.getError()` = 0・console のエラー 0 件）と
**`npm run shot -- mobs`**（`docs/browser-shots/mobs.png` に 7 体を 1 列）。
**撮った絵を `Read` で開いて、鶏を 10 倍に切り出して見ました**（切り出しは使い捨ての
`node:zlib` だけの道具）。**面の抜けも埋まりもありません**。羊の隣に並べても、
とさかの赤とくちばし・脚の橙で見分けが付きます。

**絵を見て 1 つ直しました**: **体の幅が翼と同じ ±3px だったので、白い体に白い翼が
埋まって振れても見えませんでした。** 体を ±2.5px に狭めて翼を 0.6px ぶん外へ出しています
（判定の縁 ±3.2px には触れていないので、狭めるだけで済みました）。**テストでは
絶対に出ない類の不具合**です —— 体積も巡回順も当たり判定も、埋まったままで全部緑でした。

**枠**: **ID 2 個**（126 = 生鶏肉 / 127 = 焼き鳥）。111..255 の空きは **130 → 128**、
1..63 の空きは **9** のまま、`MAX_ITEM_ID` は **125 → 127**、アイテムは **90 → 92 種**、
モブは **6 → 7 種類**。**`SaveData` は 1 バイトも増えていません**（`version` は 1・
キーも増えません。**モブは保存しないため**）。**`main.ts` は 1450 行のまま 1 行も
触っていません。** `npm test` は **2818 → 2852 件**（+34）で全部緑、進行（クリア導線）は
**13 / 13**。

## AUTODEV 17: 羽根（鶏の 2 山目）と、矢を本家の形へ（2026-09-03・C の周・無人）

**取ったもの**: `AUTODEV-QUEUE.md` の **7b**（7 を 3 件に割った 2 つ目）。
**鶏を倒すと生鶏肉 1 個 + 羽根 1 個の 2 山が落ち、矢が「火打石 + 棒 + 羽根」で 4 本**
（本家の形。3 段なので**作業台が要る**ようになりました）。

**見送ったもの**: **羽根の 0〜2 個**（本家の個数）—— `MobDrop.extra` に個数の範囲
（`ShearRule` の `min` / `max` のような）を持たせない、という線引きです。範囲が要るなら
別の表の話で、この周の仕事ではありません（`TUNING.md` に 1 行）。

**差し戻し 0 回。** 実装も点検もこのセッションが自分でやりました（サブエージェントは
使っていません。`AUTODEV.md` の「返らないなら自分で実装する」の側）。

**形**: `items.ts` の `Drop.extra`（ブロックの 2 山目）をそのまま持ってきて、
**`MobDrop.extra`** にしました。`MobDropStack`（山ひとつ）を切り出して
`MobDrop extends MobDropStack` に `extra?` を足すだけなので、**既存 7 つの `drop:` は
1 行も書き換わっていません**。落ちる山を全部返すのは **`dropsFor(mob, def, random)`** の
1 本で、`rollDrop()` に対する `rollDrops()` とまったく同じ形です（**`dropFor()` は
残してあります** —— 既存テストの根拠で、`dropsFor()` の 1 山目もこれに作らせます）。

**ブロック側と 1 点だけ違います**: **`MobDrop.extra` は `chance` を持てます。**
`Drop.extra` が確率を持てないのは、壊す側に流れてくる乱数が `BreakOrder.roll` の
**1 本だけ**だからで、モブの側は `random()` という**関数**が流れてくるので 2 山目を
別に引けます。**この違いは決まりごとです**（`RULES-INBOX.md` に書きました）。

**踏まずに済んだ穴**（仕様書が先に書いていたもの）:

1. **`chance >= 1 || random() < chance` の短絡をそのまま使うこと。** `chance` が 1 の山では
   乱数を引きません。**引く形に変えると、種を固定した既存テストの目がずれて羽根と
   関係ない所が赤くなります。** 引いた回数を数える乱数でテストに固定しました（鶏 0 回）。
2. **`attack()` と `hitByProjectile()` の 2 か所とも差し替えること。** 片方だけだと
   **弓で撃ったときだけ羽根が出ません**（刈った羊の羊毛とまったく同じ話）。
   テストは殴った側と撃った側を**並べて**測っています。

**1 つだけ既存の判定を書き直しました**（ゆるめてはいません）:
`test/mobs.test.ts` の「ドロップは多くても 1 回」は、**表が持っている山の数ぶんだけ**
許す形に一般化しました（`drops.length <= 表の山の数` かつ **並び順のまま** かつ
`chance` 1 の山は必ず出る）。**既存 6 種のモブでは今までとまったく同じ判定**です。

**C-3（撮って見た）**: `npm run build` → `http-server` → `node tools/browsershot.mjs`
（定番 5 枚。**console のエラー 0 件**・WebGL 2.0・`inventory.png` は壊れていません）。
そのうえで**クリエイティブの一覧を末尾まで送って撮り直し**、
`docs/browser-shots/creative-feather.png` として置きました（`E` の一覧の末尾に
**「羽根」がほぼ白の枠で出ています**。名前の欠けも溢れもなし）。

**枠**: **ID 1 個**（128 = 羽根）。111..255 の空きは **128 → 127**、1..63 の空きは **9** のまま、
`MAX_ITEM_ID` は **127 → 128**、アイテムは **92 → 93 種**。**`SaveData` は 1 バイトも
増えていません**（`version` は 1・キーも増えません。**モブは保存しないため**）。
**`main.ts` は 1450 行のまま 1 行も触っていません。** `npm test` は **2852 → 2858 件**（+6）で
全部緑、進行（クリア導線）は **13 / 13**。

## AUTODEV 18: 鶏が卵を産む（`MobDef.laying`）（2026-09-03・C の周・無人）

**取ったもの**: `AUTODEV-QUEUE.md` の **7c-1**（7c を 2 件に割った前半。後半「投げる側」は
キューに残っています）。**鶏が 300〜600 秒ごとに足元へ卵を 1 個産みます**（本家の
6000〜12000 ティック）。**倒す必要はありません** —— 羊毛に続いて **倒さずに取れる 2 つ目**で、
違うのは**プレイヤーの操作ではなく時計が起こす**ところだけです。

**`main.ts` も `drops.ts` も `storage.ts` も `projectiles.ts` も 1 行も触っていません。**
出口は倒したとき・刈ったときと**同じ `onDrop`** で足りたので、足したのは
`mobs.ts` の `LayRule` / `MobDef.laying` / `Mob.layTimer` / `Mobs.lay()` と、
`items.ts` の 2 行（`EGG = 129` と `item({...})`）だけです。

**判断の置き場**（`CLAUDE.md` の対の表どおり）:

| 判断 | 置き場 |
| --- | --- |
| 誰が・何を・何個・どれだけの間隔で産むか | `MobDef.laying`（`ShearRule` とまったく同じ作法） |
| いつ産むか（時計を減らして 0 で鳴らす） | **`Mobs.lay()` の 1 本**（`update()` には条件を 1 つも足していません） |
| 産まれた物がどう散るか | **既にある `onDrop` → `main.ts` → `drops.burst()`**（0 行） |

**踏まずに済んだ穴**（仕様書が先に書いていたもの）:

1. **`layTimer` を 0 から始めないこと。** `spawn()` の初期値を `min..max` から引いています ——
   0 にすると、`populate()` のまとめ打ちで湧いた鶏が**全員その場で 1 個ずつ産みます**。
2. **`lay()` を判断（5Hz の `think()`）に置かないこと。** `step()` の直後・毎フレームです
   （`woolTimer` を `step()` で減らしているのと同じ理由。遠くて動かない個体でも時計が進む）。
3. **次の間隔を入れ直してから `onDrop` を鳴らすこと。** 逆にすると時計が 0 以下に留まります。

**踏んだもの 2 つ**（どちらもテストの側）:

1. **「2 秒 = 120 フレームぶん引けば 0 になる」は成り立ちません。** `1/60` を 120 回引くと
   **1e-15 ほど残って**、その 1 フレームでは鳴りません。**「鳴った最初のフレームを探して
   その 1 つ手前を見る」**形に直しました（フレーム数の決め打ちも別の 1 件として残しています）。
2. **10 分ぶん回すと、鶏が徘徊で試験場（±80）を出て `DESPAWN_DISTANCE`(72) で消えます。**
   「動かないから産まない」で緑になるので、**毎フレーム位置を貼り直して留め置き**、
   そのうえで**「回しているあいだ消えていない」を先に判定**しています（`rules/testing.md`）。

**見送ったもの**: **卵を投げること**（7c-2。`projectiles.ts` は 1 行も触っていません）/
**産卵の音**（出来事 × 種類で膨らむ。`onSound` は鳴らしません）/
**本家の「1/8 でヒヨコ」**（子モブの仕組みがまだありません）。

**差し戻し**: 0 回（この周はサブエージェントを立てず、親が実装して `git diff` を自分で点検）。

**C-3（撮って見た）**: `npm run build` → `http-server` → `node tools/browsershot.mjs`
（定番 5 枚。**console のエラー 0 件**・WebGL 2.0・`inventory.png` は壊れていません）。
そのうえで**クリエイティブの一覧を末尾まで送って撮り**、
`docs/browser-shots/creative-egg.png` として置きました（枠 **94 個**・末尾は
羽根 → **卵**、`title` は **`卵 x16`**）。**そこで 1 つ見えたこと**: **卵（`0xf7f0e0`）と
羽根（`0xe8e4dc`）はどちらもほぼ白で、隣り合っています。** 直すには色の判断が要るので
`TUNING.md` と `HANDOFF.md` に回しました（前の周の持ち越しと**同じ 1 件**になります）。

**枠**: **ID 1 個**（129 = 卵）。111..255 の空きは **127 → 126**、1..63 の空きは **9** のまま、
`MAX_ITEM_ID` は **128 → 129**、アイテムは **93 → 94 種**。**`SaveData` は 1 バイトも
増えていません**（`version` は 1・キーも増えません。**モブを保存しないので `layTimer` も
保存されません**）。**`main.ts` は 1449 行のまま 1 行も触っていません。**
`npm test` は **2884 → 2904 件**（+20）で全部緑、進行（クリア導線）は **13 / 13**。

## AUTODEV 19: 卵を投げる（`PROJECTILE_KINDS` に 1 行 + `use.ts` の `throw`）

**取ったもの**: `AUTODEV-QUEUE.md` の **7c-2**（7c を割った後半。前半「産む側」は
AUTODEV 18）。**卵を持って右クリックすると視線の向きへ 1 個飛び、落ちながら進んで
ブロックか相手に当たって消えます。** サバイバルでは 1 個減り、**ダメージは 0**、
**ヒヨコは孵りません**。仕様書は `AUTODEV-SPEC.md`（119 行）。

**`src/` は 4 ファイル・実質 5 か所**しか触っていません:

1. `projectiles.ts` — `ProjectileKind` に `"egg"` 1 語と、表に 1 行
   （半径 0.125 / 色 `0xf7f0e0` / 重力 1 / 抵抗 0 / 速さ 20 / 寿命 30 / `vanish` / 光らない / 向かない）
2. `items.ts` — **`THROWN` の表 1 本**（`EGG → "egg"`）と `thrownProjectile()`。
   **`import type` で運ぶこと**（値で取ると `items.ts` ↔ `projectiles.ts` が輪になります）
3. `use.ts` — `UseAction` に `throw` 1 行と、`decideUse()` の**アイの直後・火種の前**に 2 行
4. `main.ts` — `case "throw"` 1 行と `throwItem()` 1 本（**数値は 1 つも書いていません**）

**仕様から 1 か所だけ変えました**（記録として残します）: 仕様の（5-4）は
`throwItem(act.item, act.projectile)` でしたが、**`item` を使う所が無く**
`tsconfig.json` の **`noUnusedParameters: true`** で `npm run typecheck` が通りません。
`throwItem(act.projectile)` の 1 引数にしてあります（`UseAction.throw` の `item` は
テストが「何を投げたか」を見るのに使うので、型からは外していません）。

**踏んだもの 1 つ**（テストの側）:

- **卵の的の高さは、矢の的（19.2）を写すと当たりません。** 卵は 20m/s で
  `gravityScale: 1` なので、**6m 先までに 1.35m 落ちます**（矢は 40m/s で 8.5m 先まで
  0.76m）。写したままだと**当たり 0 回**で「相手に当たらない飛び道具」を緑で通します。
  **18.0 に下げて、落ちるぶんを見込んだ理由をコメントに残しました。**
  `rules/testing.md` の「先にちゃんと動いたことを確かめる」の、**弾道版**です
  （`RULES-INBOX.md` に本文を置いてあります）。

**見送ったもの**: **ヒヨコ**（本家の 1/8。子モブの仕組みが 1 つも無いので別件）/
**投げる音**（`sfx.ts` / `audio.ts` は 0 行。出来事 × 種類で膨らむため）/
**`projectilerender.ts` の手入れ**（**表の値だけで出ます** —— 実際に撮って確かめました）。

**差し戻し**: 0 回（この周もサブエージェントを立てず、親が実装して `git diff` を自分で点検。
背景で起動すると番を閉じてしまうため。`AUTODEV.md` の C-1 / C-2）。

**C-3（撮って見た）**: `npm run build` → `http-server` → `node tools/browsershot.mjs`
（定番 5 枚。**console のエラー 0 件**・WebGL 2.0・見た目は前の周と同じ）。そのうえで
**投げた卵そのものを撮りました**（`docs/browser-shots/egg.png`）——
**空を上がっていく小さな白い立方体**で、色も傾き（`aims: false`）も表のとおりです。
**地面と平行に投げると 1 枚も写りません**（0.6 秒で落ちて消えるのに、SwiftShader では
1 枚撮るのに 300〜500ms 掛かる）。**上を向いてから投げること**（撮り方は
`docs/browser-shots/README.md` の 16 枚目）。

**枠**: **ID は 0 個**（`"egg"` は `ProjectileKind` の文字列で番号ではありません）。
111..255 の空きは **126 のまま**、1..63 は **9** のまま、`MAX_ITEM_ID` は **129** のまま、
アイテムは **94 種**のまま。**`SaveData` は 1 バイトも増えていません**（飛び道具は
保存しません。`version` は 1）。`npm test` は **2904 → 2923 件**（+19）で全部緑、
進行（クリア導線）は **13 / 13**。**`main.ts` は 1449 → 1463 行**（上限 1500）——
**`AUTODEV.md` の「止まって人を呼ぶ条件」3 番（1450 行）を越えました。**
次に `main.ts` を触る周は、**先に判断を別ファイルへ出すこと。**

## AUTODEV 20: 牛（4 種類目の受動モブ）と生牛肉・ステーキ・革（2026-09-04・C の周・無人）

**取ったもの**: `AUTODEV-QUEUE.md` の 8 番（キューの先頭）。仕様書は前の周（B）が書いた
`AUTODEV-SPEC.md` で、**全文を根拠にしています**（要約していません）。

**形は鶏（16・17）とまったく同じ**で、違うのは **2 山目（`MobDrop.extra`）が最初から
付く**ところだけでした。触ったのは仕様書のとおり **`items.ts` / `mobs.ts` /
`smelting.ts` の 3 本**で、**`main.ts`・`mobmesh.ts`・`mobrender.ts`・`drops.ts`・
`crafting.ts` は 1 行も触っていません**（`git diff --stat` で確かめられます）。

**踏んだもの 2 つ（どちらも「テストは全部緑なのに、撮るまで気付けない」類）**:

- **頭と体が 1 枚の板に見えました。** 牛の頭は体と同じ茶色で、**体より 1.1px しか前に
  出ておらず面の向きも同じ**なので、陰でも分かれません。**鶏（白い体に白い翼）と同じ罠**
  ですが、**頭は体へ差し込む部位なので「輪郭を外へ出す」手が使えません** ——
  **顔に白い筋**（目とまったく同じ、前面から 0.1px 出した薄い箱）を置いて分けました。
- **その筋を頭の幅いっぱいに引いたら、同じ高さの角と 1 本の白い帯**になり、
  **角が角に見えなくなりました**（撮り直して気付いた）。筋を ±1.5px に細めています。

**`rules/mobs.md` に 2 件とも据えました**（「輪郭を外へ出せない部位は面に別の色を置く」と
「隣り合う淡い色どうしは間に地の色を残す」）。

**もう 1 つ、この周とは関係なく分かったこと**: **`test/audio.test.ts` の
「cutoff が低い材質ほどこもって鳴る（羊毛 < 雪 < 砂）」は揺れます。**
**15 回走らせて 2 回**赤くなり、**羊毛の明るさだけが 0.552 → 9.739 に跳びます**
（雪 0.716 / 砂 2.235 は動かない）。足音はノイズなので、短い窓に高い成分が偶然
固まると低い cutoff でも明るく出ます。**判定はゆるめていません** ——
`rules/testing.md` に「赤が出たらまず もう一度走らせる／直すなら測り方のほう」を据えました。

**見送ったもの**: **革の使い道**（防具も本も無いので別件。レシピを 1 本も書いていません）/
**牛乳とバケツ**（`FILLED_BUCKETS` は液体の表なので、液体でない中身を入れる話が先）/
**子牛と繁殖**（子モブの仕組みが 1 つも無い。卵のヒヨコと同じ理由）/
**革の 0〜2 個**（`MobDrop.extra` に個数の範囲を持たせない線引き。羽根と同じ）。

**差し戻し**: 0 回（この周もサブエージェントを立てず、親が実装して `git diff` を
自分で点検しました。背景で起動すると番を閉じてしまうため。`AUTODEV.md` の C-1 / C-2）。

**C-3（撮って見た）**: 定番 5 枚（`node tools/browsershot.mjs`。**console のエラー 0 件**・
WebGL 2.0・見た目は前の周と同じ）+ **`npm run shot -- mobs`**（`docs/browser-shots/mobs.png`
と切り出した `cow.png`）+ **クリエイティブの一覧**（`creative-beef.png`。枠 94 → **97 個**）。
横向きと斜めの牛も使い捨てのスクリプトで撮っています（`tools/` には入れていません）。

**枠**: **ID を 3 個**使いました（**130 = 生牛肉 / 131 = ステーキ / 132 = 革**）。
111..255 の空きは **126 → 123**、1..63 は **9** のまま、`MAX_ITEM_ID` は **129 → 132**、
アイテムは **94 → 97 種**。**`SaveData` は 1 バイトも増えていません**（モブは保存しません。
`version` は 1）。`npm test` は **2907 → 2934 件**（+27）で全部緑、進行（クリア導線）は
**13 / 13**。**`main.ts` は 1463 行のまま**（1 行も触っていないので、
**「止まって人を呼ぶ条件」3 番（1450 行）は越えたまま持ち越し**です）。

## AUTODEV 21（C の周・2026-09-04）: クモと糸

**取ったもの**: `AUTODEV-QUEUE.md` の 9「クモ（夜の敵対 2 種類目）と糸」。
**`AUTODEV-SPEC.md` のとおり、`mobs.ts` の `SPIDER`（`MobDef` 1 つ）と
`items.ts` の 2 行だけ**で閉じました。**`main.ts` / `mobmesh.ts` / `mobrender.ts` /
`crafting.ts` / `drops.ts` / `blocks.ts` は 1 行も触っていません** ——
モブを 1 種類足すのに描画側が 0 行なのは `rules/mobs.md` のとおりで、
**新しい「確かめられないもの」は 1 つも増えていません**（`unverifiable-pair` は不要）。

**踏んだところ**（次の周が同じ道を通らないために）:

- **`spawnWeight: 100` はゾンビと同じ重み**なので、**夜の「どこでも」の敵対が
  ゾンビとクモで半々**になりました（石の上で 2000 回抽選して
  **ゾンビ 49.2% / クモ 46.2% / エンダーマン 4.7%**）。**赤くなったのは仕様書の
  読みどおり 2 件**で、どちらも**ゆるめずに数え直しています**:
  - 「ゾンビがいちばん出やすい」→ **「ゾンビとクモがほぼ同数で、エンダーマンはその 1/10」**
    （3 つの実測値を出してから判定。差が 5% 未満・両方がエンダーマンの 5 倍超）
  - 「敵対の上限を超えない」→ **ゾンビ + クモの合計**で見る形に。**夜の草地で
    ちょうど 13 + 11 = 24 / 24** なので、ゾンビだけを数えていたら**上限が 2 倍に
    ゆるんでも緑のまま**でした
- **`census()` の `counts` と `show()` に `クモ` を足すこと**（忘れると `counts[name]++`
  が `NaN`）。仕様書が名指ししていたので踏まずに済んでいます
- **形は「脚を胴より外へ出す」だけでは足りず、太ももと すね の 2 箱に割る**必要が
  ありました。`swing` の箱は **`y1 === 0`**（軸からぶら下げる）なので、1 箱だと
  「外へ伸びる棒」か「下へ伸びる棒」のどちらかにしかなりません。**2 箱とも
  `y1 === 0` にして、外端で下ろす**と Γ の形になり、X 軸まわりの `swing` で
  前後に振れます（足先 ±10.6px・判定の縁 ±11.2px には触れていない）
- **当たり判定 1.4 x 0.9 は自然に湧くモブでいちばん横に広い**（次は豚・羊・牛の 0.9）。
  **`longBody` にも足さず `size` も広げず**、形のほうを削って収めました
  （胴は本家の 12x8x10 + 8x8x8 より小さめ）

**見送ったもの**: **糸の使い道**（**弓を「棒 3 + 糸 3」に戻すのは次の 10 番**。
この周は `crafting.ts` を 0 行にしました）/ **糸 0〜2 個**（`MobDrop` に個数の範囲を
持たせない線引き。羽根・革と同じ）/ **クモを日光で燃えなくすること**（`fireproof` が
溶岩と日光の両方を止める 1 つの印なので、分けると `MobDef` が 1 列増えて 9 種類ぜんぶに
手が要る。`TUNING.md` に 1 行残して**朝に燃えるまま**にしています）/ **壁を登ること**
（本家のクモの特徴ですが、`physics.ts` に「登る」が 1 つも無く、飛ぶモブ・跳ぶモブと
同じ「表の値で表す」形に落とすところから）/ **クモの巣**（ブロック 1 個ぶんの枠と、
「中を歩くと遅くなる」という新しい効果が要る）。

**差し戻し**: 0 回（この周もサブエージェントを立てず、親が実装して `git diff` を
自分で点検しました。`AUTODEV.md` の C-1 / C-2 のとおり）。

**C-3（撮って見た）**: 定番 5 枚（`node tools/browsershot.mjs`。**console のエラー 0 件**・
WebGL 2.0・`mobs 16` で 20fps）+ **`npm run shot -- mobs`**（9 体に増えた列）+
**使い捨てのスクリプトでクモを 4 向き・近くから**（`tools/` には入れていません）。
**赤い目 4 つがはっきり見え、脚 8 本が胴の外へ出ている**ことを確かめました。
**横から見ると胴と頭が 1 つの暗い塊**に見えますが、本家のクモもそうなので直していません。

**枠**: **ID を 1 個**使いました（**133 = 糸**）。111..255 の空きは **123 → 122**、
1..63 は **9** のまま、`MAX_ITEM_ID` は **132 → 133**、アイテムは **97 → 98 種**、
モブは **8 → 9 種類**（敵対はボスを除いて **3 → 4**）。**`SaveData` は 1 バイトも
増えていません**（モブは保存しません。`version` は 1）。`npm test` は
**2934 → 2962 件**（+28）で全部緑、進行（クリア導線）は **13 / 13**。
**`main.ts` は `npm test` の数え方で 1464 行のまま**（1 行も触っていないので、
**「止まって人を呼ぶ条件」2 番（1450 行）は越えたまま持ち越し**です）。

## AUTODEV 22（C の周・2026-09-04・無人）: 弓を本家の形に戻す（羊毛 3 → 糸 3）

**取ったもの**: `AUTODEV-QUEUE.md` の先頭の 10 番（**9 番を 2 件に割った後半**）。
`AUTODEV-SPEC.md` のとおり **`crafting.ts` の弓 1 行と、その周りのコメント・テストだけ**の周で、
**ID は 1 個も取っていません**。`src/**` に足したのは `crafting.ts` の `STRING` の import 1 語と
`key` の 1 語（`W: WOOL` → `W: STRING`）で、あとは全部コメントと文書です。

**これで弓は本家と同じ「棒 3 + 糸 3」になり、糸に初めて使い道が付きました。**
**弓を作るにはクモを 3 匹倒します**（`SPIDER.drop` が 1 個固定なので。本家は 0〜2 個なので
2 匹で済むこともあります）。**羊毛の使い道はベッド 1 本だけ**になりました。

**新しく足した判定は 3 件**（`test/crafting.test.ts`。既存 3 件は盤面の `L` → `G` の差し替えで、
件数は増えていません）:

- **「羊毛では弓が作れない」** —— 羊毛の盤面が `null`。**残っていたら差し替えが効いていません**
- **「ベッドは今までどおり羊毛 3 + 板 3」** —— **この周まで `test/crafting.test.ts` に
  ベッドの判定は 1 件もありませんでした**。羊毛の唯一の使い道になったので足しました
- **「糸を使うレシピはちょうど 1 本（弓）」** —— `RECIPES` を `key` と `ingredients` の
  両方から走査し、**本数と名前を出力してから**判定（`rules/testing.md` の形）

**見送ったもの**: **糸の 2 本目の使い道**（防具・本・釣り竿はどれも別件で、ブロック /
アイテムの枠と見た目が要ります）/ **クモの湧きの重み・糸の個数を触ること**（弓が遠くなった
ぶんを湧きで埋めると夜の顔ぶれごと変わります。**手触りの話は `TUNING.md` に 1 行**残して
進みました）/ **`SPIDER.drop` を本家の 0〜2 個にすること**（`MobDrop` に個数の範囲を
持たせない線引きは羽根・革と共通で、崩すなら 3 種類まとめて）。

**差し戻し**: 0 回（この周もサブエージェントを立てず、親が実装して `git diff` を自分で
点検しました。`AUTODEV.md` の C-1 / C-2 のとおり）。

**C-3（撮影）**: **要りません**。`crafting.ts` の 1 語とコメントだけで、`#recipehint` に出る
名前も「弓」のまま変わりません（**`AUTODEV.md` で撮らずに閉じてよい唯一の形**。仕様書の
10 節に理由が書いてあります）。`npm run build` は `src/**` を触ったので走らせました（緑）。

**踏んだ落とし穴（`rules/items-survival.md` に据えました）**: **「残っている代用は弓の糸
（羊毛 3）だけ」という決まりごとが、その代用を外した瞬間に嘘になります。** 仕様書の 8 節が
名指しで指していなければ、`rules/` に古い言い分が 1 件残ったまま次の周へ渡っていました
（**古くなった決まりごとを残さないこと** = `AUTODEV.md` の C-4）。ついでに
**「代用レシピは 1 本も残っていません」**と、**羊毛の行き場がベッド 1 本だけになったこと**も
同じ節に足しました。

**枠**: **ID は 0 個**。111..255 の空きは **122 のまま**、1..63 は **9** のまま、
`MAX_ITEM_ID` は **133**、アイテムは **98 種**、モブは **9 種類**のまま。
**`SaveData` は 1 バイトも増えていません**（レシピは保存しません。`version` は 1）。
`npm test` は **2962 → 2965 件**（+3）で全部緑、進行（クリア導線）は **13 / 13**。
**`main.ts` は 1464 行のまま**（1 行も触っていないので、**「止まって人を呼ぶ条件」2 番
（1450 行）は 5 周続けて越えたまま持ち越し**です）。

---

## 2026-09-04 — A（棚卸し）の周・無人。`AUTODEV-QUEUE.md` を数え直して 7 件足した

- **なぜ A だったか**: `AUTODEV.md` の判定表を上から当てて、**未着手 `- [ ]` が 2 件
  （3 件未満）**で最初に当たりました。`AUTODEV-SPEC.md` は `済` でしたが、判定表は
  上から当てるので **B より先に A** です（`HANDOFF.md` の指し示しどおり）。
- **基準は変えていません**: 本家のバージョン順（Alpha → Beta → 正式版）。
- **取ったもの**: 7 件（13..19）。**残っていた 11・12 は消さずに末尾へ回しました**
  —— どちらもコードで数え直して**まだ無い**ことを確かめています
  （`MOB_KINDS` にスケルトンは無く、`woolTimer` を読んでいるのは `mobs.ts` だけで
  `mobrender.ts` は 1 行も見ていない）。
- **数え直して分かったこと**（`docs/autodev-counting.md` の 8 ジャンルを全部）:
  - **鉄・金・ダイヤに「しまう形」がまるごと無い。** ブロックは 1..54 + 共有帯 3 個で、
    圧縮ブロックが 1 つもありません。**ダイヤは掘っても道具にするしか行き場がない**
    という、数えるまで誰も言っていなかった穴です（14 番）。
  - **`use.ts` の `throw` は `items.ts` の `thrownProjectile()` を見るだけの作り**なので、
    **投げるものを足すのに `main.ts` は 1 行も要りません**（卵の周が残した形）。
    雪玉を先頭に置けたのはこれが理由で、取りやすさで選んだのではありません（13 番）。
  - **前の表（2026-09-01）はもう使えませんでした** —— あれは「アイテム 78 種 /
    `MAX_ITEM_ID` 94 / モブ 6 種類 / クラフト 34 本」。いまは 98 / 133 / 9 / 44 です。
    **A の周でコードから数え直す決まりが、そのまま効いた回**でした。
- **並び（`AUTODEV.md` の A-2）**: 13・14 は**見た目にも `main.ts` にも触らない**ので先頭。
  15・16 は判断も番号も軽いのに、**`main.ts` が 1464 行（止まる目安 1450 行）**で
  振り分けの行を足せないため「依存が未解決」として後ろへ回しました（A-2 の 4）。
  17〜19 は生成・物理、11・12 は見た目なので最後です。
- **見送ったもの（理由 1 行ずつ）**: 前の A（2026-09-01）の 9 件はそのまま。
  **そのうち 2 件を、名指しで「次の A で見直す」と書いてあったので数え直しました**:
  - **TNT と爆発** —— また見送り。**重力ブロック（1 番）は済んだのに、道は開いていません。**
    あれは 1 マスずつ落とす形で、「球状にまとめて消す」書き込みは 1 行も無く、
    メッシュ化 6ms の予算に直に効きます。**次の A でまた数えること。**
  - **防具** —— また見送り。**革（132）で材料の依存は解けました**が、番号 12〜16 個と
    インベントリ画面の枠（DOM 側）が残っています。**次の A の周で「枠だけ」を
    先に割れないか見ること**（`craftscreen.ts` に枠を足す形は `add-stateful-block` にある）。
- **枠**: **変化なし。`src/**` と `test/**` に差分は 1 行もありません**（A の完了条件）。
  1..63 の空き 9 / 111..255 の空き 122 / `MAX_ITEM_ID` 133 / `main.ts` 1464 行 /
  `npm test` 2965 件すべて緑（**1 回目は「声色」で赤くなり、2 回目で緑**。
  `rules/testing.md` のとおり音の一群は揺れます）。

## AUTODEV 25（C の周・2026-09-04・無人）: 雪玉（掘って出て、投げられて、戻せる）

- **なぜ C だったか**: 判定表を上から当てて、`AUTODEV-QUEUE.md` の未着手が **8 件
  （3 件以上）**、`AUTODEV-SPEC.md` が **`未着手`** だったので 3 行目に当たりました。
- **取ったもの**: キューの 13 番（先頭）。**飛ばしていません。**
- **サブエージェントは立てていません。** `AUTODEV.md` の C-1 の「同じ番の中で結果が
  返らないなら自分で実装する」に従い、実装も点検（C-2）も親が持ちました
  —— **周を閉じずに終わるほうがずっと高くつきます。**
- **枠**: **アイテム 134 を 1 個だけ**（`ROADMAP.md` の予約表どおり）。
  111..255 の空きは **122 → 121**、`MAX_ITEM_ID` は **133 → 134**。
  **`main.ts` は 1464 行のまま**（触ったのは `throwItem()` の見出しコメント 1 行だけで、
  `git diff --stat` の `main.ts` は **1 行**）。`npm test` は **2965 → 2977 件すべて緑**。
- **仕様書のとおり `use.ts` は 1 行も触っていません。** 投げるものは `items.ts` の
  `THROWN` の 1 行を足すだけで、`decideUse()` にも `main.ts` にも `SNOWBALL` の名前は
  出てきません（`rules/projectiles.md` の「`held === EGG` と書かないこと」がそのまま効いた）。
- **差し戻し 0 回。** `npm run typecheck` / `npm test` / `npm run build` は 1 回で緑でした
  （音の一群も 2 回とも緑）。

### 撮って見たこと（C-3）

**本物のブラウザで 2 枚**（`docs/browser-shots/creative-snowball.png` /
`snowball-flight.png`。撮り方と数字はあちらの README）。

- **一覧の末尾に青白い「雪玉」が並びました**（枠 99 個・`getComputedStyle` で
  rgb(188,216,239)）。**糸 rgb(184,188,200) と隣り合いますが、青みで分かれます。**
- **`N` キーで撃った雪玉が飛んで落ちました。** 空を背にして撮ると、
  高さを保つエンダーアイ（`gravityScale: 0`）の横で**雪玉だけが 2 コマで画の下へ
  下がります** —— `gravityScale: 1` が絵に出た形です。**console のエラーは 0 件。**
- **撮り直しを 2 回しています**: (1) クリエイティブの一覧は**送らないと末尾が写りません**
  （雪玉は 134 = 最後）。(2) **地面へ向けて撃った 1 枚目は何も読めませんでした** ——
  草の上の 0.25m の四角は絵にならないので、**空を向いてから撃つこと**。
  どちらも `docs/browser-shots/README.md` に書き足しました。

### 決まりごと（`rules/`）

`rules/projectiles.md` の「5 種類」を **6 種類**にし、**雪玉が卵の値を写したものである
こと**（片方だけ触ると弾道が食い違う）を 1 行足しました。**新しい落とし穴は踏んでいません**
—— 卵の周（AUTODEV 19）が作った形に 2 行足すだけで閉じたためです。
