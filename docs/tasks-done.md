# 済んだタスクの記録（読まなくてよい）

**これは `TASKS.md` から退避した完了分です。** キューではないので、ループは毎周読む必要が
ありません。**必要になるのは「前に似たものをどう足したか」を調べるときだけ**です
（節の中の `<!-- 着手したときの見取り図 -->` が、次に同じ形のものを足すときの手本になります）。

いま取るタスクは `TASKS.md`。手順は `LOOP.md`。

---

## Phase 1 — 土台

### [x] 1-1b. 溶岩の直し（フォグと狙う判定）**見** — 確認済み

1-1 の確認で出た 2 点。**`liquid: true` と `isLiquid()` / `liquidFog()` に寄せた**ので、
液体を足すときに 3 か所（狙う・置く・フォグ）を直し忘れなくなった。
決まりごとは `.claude/rules/blocks-shapes.md` の液体の項。

### [x] 1-1. 溶岩ブロック **見** — 確認済み（7 項目中 5 つ OK、2 つは 1-1b で直した）

- ID 42 (`LAVA`)。水と同じ半透明の液体で、違うのは `emission: LAVA_LIGHT`(15) だけ
- `worldgen.ts` の `LAVA_LEVEL`(10) 以下の洞窟に溜まる。**海面の水とまったく同じ形**
  （掘り抜いた空間を高さで液体にする 1 行）
- ダメージはまだ無い（1-2）。`generateChunk` は 1.08〜1.16ms で退行なし

### [x] 1-2. 溶岩のダメージと炎上 **見** — 確認済み

**黒曜石は 1-2b に切り出しました**（下）。1 コミットに収まらないのと、
**入手導線（バケツ）にユーザーの判断が要る**ため。

- 判断は `vitals.ts` の `updateFire`（浸かっているあいだ + 出てからの炎上）。
  数値は Minecraft のまま。`main.ts` は `inLiquid` / `inLava` を渡すだけ
- **`isHotLiquid()` を足した**ので、焼けるかどうかも表 1 本。モブも同じものを見る
- ついでに `isLiquid()` を残り 4 か所（`player.ts` / `mobs.ts` / `drops.ts` / `beds.ts`）へ
  広げた。**溶岩にモブが湧く・溶岩の上にベッドが置ける・「溶岩」というアイテムが
  1 個増えている**の 3 つが直った
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていない。
  モブ 40 体の中央値 0.048ms（上限 0.5ms）

### [x] 1-2b-1. バケツ **見** — 確認済み

アイテム 84/85/86。鉄 3 個の V 字。**積めるのは 1 個まで。**
汲む／流すの判断は `items.ts` の `bucketUse()`（純粋）で、対応の表は `FILLED_BUCKETS` 1 か所。
バケツを持っているときだけ光線が液体に当たる（`raycastVoxels` の `hitLiquid`）。

### [x] 1-2b-2. 黒曜石 **見** — 確認済み（6 項目すべて OK）

ID 43 (`OBSIDIAN`)。硬さ 50 / `minTier: TIER_DIAMOND`（ダイヤで 9.4 秒）。
規則は `blocks.ts` の `quenched()`（純粋。**熱い液体 + 熱くない液体**で決めるので
液体を足しても 1 行のまま）、どのマスに効くかは新しい `liquids.ts` の `quenchAround()`。
`main.ts` は流した直後に 1 行呼ぶだけ。**進行テストが「達成 2 / 13」になった**
（probe も本物に差し替え済み）。

### [x] 1-3. 砂利 → 火打石 → 火打石と打ち金 **見** — 確認済み（7 項目すべて OK）

ID 44 (`GRAVEL`)、アイテム 87（火打石）/ 88（火打石と打ち金）。
**`Drop` に `otherwise` を足した** —— 「10% で火打石、外したら砂利」は
今までの「確率を外したら何も落ちない」では書けず、外れが 90% で消えるブロックになる。
**何が落ちるかを決めるのは `items.ts` の `rollDrop(blockId, roll)`**（乱数は呼ぶ側が作る）で、
`main.ts` からは確率の比較が消えた（`test/ui.test.ts` が見張っている）。

砂利は `worldgen.ts` の `VEINS`（旧 `ORES`）に相乗り。**`shift` を足して塊の粗さを持たせた** ——
鉱石は 2x2x2 のままで砂利だけ 4x4x4。1 にすると石に胡椒を撒いたような散り方になる。
**進行テストが「達成 3 / 13」になった**（probe も本物に差し替え済み）。
`generateChunk` は交互に 5 回ずつ測って 1.31 → 1.33ms（誤差の中）。

### [x] 1-4. `portals.ts` — 枠の検出と点火の可否 **見** — 確認済み（5 項目すべて OK）

ID 49（X 向き）/ 103（Z 向き）。`portals.ts` は **`getVoxel` / `setVoxel` の 2 つだけ**を
受ける判断ファイルで、`findFrame()` が枠を読み、`ignite()` が内側を埋める。
**枠は Minecraft と同じ 内側 2..21 x 3..21 で、四隅は要らない。**
向きの違いは `mapper()` 1 か所に閉じ込めてあるので、走査は向きを知らずに書ける。

**何が火種かは `items.ts` の `isFireStarter()`**（表 1 本）。`portals.ts` は
アイテムを名指ししない（テストが `FLINT` の混入を見張っている）。

**まだ `main.ts` に配線していない**（右クリックで点く操作は無い）。次元をまたぐ移動と
一緒に 1-6 でやる。**進行テストが「達成 4 / 13」になった**（probe も本物）。

**踏んだ穴:** 立方体でないブロックは `buildProps()` が**必ず不透明側へ積む**ので、
`translucent: true` は黙って無視される（`rules/meshing-render.md` に追記）。
ポータルは不透明の紫板にしてある。

### [x] 1-5. `World` の生成器を注入できるようにする

`World` は `ChunkSource`（`seed` と `generateChunk` の 2 つだけ）を外から受け取る形になり、
**`world.ts` から `worldgen.ts` の import が消えた**（テストが見張っている）。
作るのは `main.ts` の `startWorld()`。

**バイオームは `ChunkSource` に載せていない** —— オーバーワールドにしか無い話なので、
F3 の表示用に `main.ts` が `WorldGen` を自分で持つ形にした。
`new World(scene, seed)` の呼び出し 14 か所（ほぼテスト）は `new WorldGen(seed)` を渡す形へ。

ついでに、見張りのテストが使うソース読み込みを **`arena.ts` の `sourceOf()` に 1 本化**した
（コメントを落として読む。写しが 5 か所に増えていた）。
`generateChunk` 1.08〜1.22ms / `world.update` 平均 3.8ms・最悪 16〜19ms で退行なし。

### [x] 1-7. `structures.ts` — 構造物の器

`placementsFor()`（列挙）と `stampPlacements()`（書き込み）の 2 段。
**列挙は列のキャッシュ（`ColumnData.structures`）に持たせた** —— 1 つの列は 8 段ぶん
生成されるので、毎回列挙すると地面を測る回数が 8 倍になる。
**地面を測るのは「建つと決まった 1 個につき 1 回」だけ。**

`worldgen.ts` の `STRUCTURES` は**まだ空**（器だけ通してある）。最初の 1 個は 2-3 のネザー要塞。
`generateChunk` 1.10〜1.40ms / `world.update` 平均 3.8ms で退行なし。

**進行テストの仮の判定が「ネザー要塞が生成される」を勝手に達成にしていた**
（`structures.ts` があるかを見ていたため）。要塞は 1 個も建っていないので、
**建つものの名前**で見る形に直した（`anySourceHas("ネザー要塞")`、コメントは落として読む）。

**タスク番号は順番どおりではありません**（1-7 を 1-6 の前に出してあります）。
番号は振り直さない決まりなので、**上から順に**という規則のほうが優先です。

### [x] 1-6. `dimensions.ts` — 次元の器とセーブ **見** — 確認済み（5 項目すべて OK）

**2 つに割った。** 元は「切り替えとセーブ」＋「`portals.ts` の配線（点火 → 通ると移動）」
だったが、**移動には行き先が要る**。ここで仮のネザーを作ると 2-1 の先食いになって捨てる
ことになるので、**配線は 2-2 へ移した**（下）。

- `SaveData` に `dim` と `dims` を足した。**オーバーワールドは上の階層のまま**
  （`edits` / `drops` / `furnaces` / `chests`）で、`dims` に入るのはそれ以外だけ。
  **`version` は 1 のまま**で、オーバーワールドに居る限り**保存の形は今までと同じ**
  （`dim` も `dims` も出ない）
- **預けるのと取り出すのが `switchTo()` の 1 回の呼び出し**になっている（分けると
  「預け忘れて、戻ったら改変が消えていた」が起こせる）。保存も `forSave(live)` が兼ねる
- **表に無い次元へは移らない**（生成器が無い場所に立つと世界が空になる）。
  セーブが知らない次元を指していたらオーバーワールドに落とし、**預かり物は捨てない**
- 決まりごとは `.claude/rules/dimensions.md`。**登録されているのはオーバーワールドだけ**
- 進行テストの仮の判定を直した。前は「`dimensions.ts` に `export` があるか」で、
  **器を作った瞬間に達成へ化ける**形だった（1-7 で踏んだのと同じ罠）

### [x] 1-8. `projectiles.ts` + `projectilerender.ts` **見** — ブラウザ確認待ち（`REVIEW.md`）

- **`drops.ts` / `droprender.ts` とまったく同じ対の形**にすること（判断は全部
  `projectiles.ts`、three は render 側だけ）。火球・矢・投げたエンダーアイ・
  ブレスが全部これに乗る
- 重力・終端速度・水の扱いは自前で持つ（`drops.ts` と同じ）。
  **未生成の列では動かさない**（`world.hasColumn(columnOf(x), columnOf(z))`）
- `update()` は `world.update()` の**外**で回すこと（`test/world.test.ts` の p99 に混ぜない）
- 形は `mobmesh.ts` の `buildBoxMesh()` を使い回す（巡回順の発行点を増やさない）
- **`projectilerender.ts` に判断が漏れていないかの見張りを同時に足すこと**

**やったこと**（`.claude/rules/projectiles.md`）:

- 4 種類の表（火球・矢・エンダーアイ・ブレス）。違いは**表の値だけ**で飛び方の実装は 1 本
- **当たったときに何が起きるかは書いていない。** ダメージの数値は手応えの判断なので
  （停止条件 4）、当たったマスを `onHitBlock` で外へ渡すだけ。誰に当たったかも未実装 ——
  ブレイズ（2-4）と弓矢（2-12）の周に、ユーザーと数値を決めてから足すこと
- 当たり判定は **0.2m ごとに刻む**（矢は 40m/s なので、刻まないと重いフレームで壁を抜ける）
- `N` キーで 4 種類を順ぐりに 1 つ撃てる（`M` のモブ出しと同じデバッグ用）

## Phase 2 — 導線を通す

**それぞれ「クリアできる最小」で止めること。** 着手する前に、その周でタスクを
細かく割り直してください（下は見取り図です）。

- [x] 2-1. ネザーの地形（ネザーラック 45・ソウルサンド 46・グロウストーン 47・溶岩海・天井の岩盤）
      — ブラウザ確認待ち（`REVIEW.md`。**ブロック 3 つだけ** —— ネザーへ行けるのは 2-2 から）

  `nethergen.ts` は `worldgen.ts` と同じ `ChunkSource`。**床と天井の 2 枚**を別のノイズで作り、
  あいだを空にして 31 以下を溶岩で埋める。天井があるので `blocksSky` が効き、
  **昼夜に関わらず暗い**（明かりは溶岩とグロウストーンだけ）。決まりごとは
  `.claude/rules/worldgen.md` の「ネザー」。**`DIMENSIONS` にネザーを登録済み**（`salt` は 0 以外）。
- [x] 2-2. ネザーポータルで往復 — ブラウザ確認待ち（`REVIEW.md`）。
      **進行テストの「ネザーへ行って戻れる」が本物になり、達成 5 / 13**

  判断は **`portaltravel.ts`**（次元の対応・1:8・待ち時間 0.9 秒と掛け金・出る場所・
  向こうに枠が無ければ建てる）。決まりごとは `.claude/rules/dimensions.md` の
  「ポータルを通る」。`main.ts` は `travelThrough()` で値を集めて貼るだけ。
  上の 4 つは全部ヘッドレスで固定した（`test/portaltravel.test.ts`）。

  **この周で分かったこと 2 つ:**

  - **`world.seed` をセーブに使わないこと。** ネザーでは塩を混ぜたあとの値なので、
    保存すると次に開いた地形が別物になる。種は `main.ts` の `worldSeed`
  - **`startWorld()` で `beds.clear()` しないこと。** 次元を移るたびに
    リスポーン地点が消える（消してよいのは新しいワールドを始める 2 か所だけ）

  `main.ts` が 1500 行に届いたので、**置く／火を点ける判断を `placing.ts` へ出した**
  （`.claude/rules/blocks-shapes.md`）。**上限は上げていない。**

### [x] 2-3c. main.ts の余白を作る — 済み（**1494 → 1349 行 / 上限 1500**）

**上限は上げていない。** 出したのは全部「判断」なので、どれもヘッドレスで確かめられる
ようになった（新しいファイルには全部テストを付けた。`npm test` は 1630 件）。

| 出したもの | 出し先 | 見張り |
| --- | --- | --- |
| セーブの組み立て・読み戻し | **`session.ts`（新規）** | `test/session.test.ts` |
| F3 のデバッグ文字列 | **`debugtext.ts`（新規）** | `test/debugtext.test.ts` |
| `M` / `N` のデバッグ湧かせ | **`debugspawn.ts`（新規）** | `test/debugspawn.test.ts` |
| 足音・着地・水しぶき（前フレームの状態ごと） | `sfx.ts` の `Footsteps` | `test/sfx.test.ts` |
| 空とフォグ（頭の位置のブロック → 色と濃さ） | `daynight.ts` の `environmentFor()` | `test/daynight.test.ts` |
| バケツで汲む／流す | `placing.ts` の `tryBucket()` | `test/placing.test.ts` |
| 食べ進み（中断する条件ごと） | `vitals.ts` の `Eating` | `test/vitals.test.ts` |
| 死亡画面の 1 行 / まとめ捨ての判定 | `vitals.ts` / `inventory.ts` | 同上 |

**`test/ui.test.ts` に「出した判断を呼び直しているか」の見張りを足した** ——
行数の上限だけでは「出したのに `main.ts` にも書き戻した」を止められない
（両方あっても行は増えない）。

**残りのタスクに要る配線は見積もり 90〜100 行**なので、これで 2-13 まで届く。
足りなくなったら**同じやり方で出すこと**（上限を上げない）。

### [x] 2-3b. 次元ごとの空 **見** — ブラウザ確認待ち（`REVIEW.md`）

`daynight.ts` の **`SKY_STYLES`**（次元 → 固定の空 3 色 + tint と明るさ / 太陽・月・星を
出すか）。ネザーは赤黒くて天体無し・明るさは時刻に依らず 1、エンドは紫で星だけ・0.7。
**エンドの行も置いた**ので、2-10 は表を触らずに済む。

- **`sky.ts` は 1 行も変えていない。** 出さない天体は**不透明度 0** として伝わり、
  もともとある `place()` と `stars.visible` がそのまま隠す。**GLSL も触っていない**
- **`main.ts` は `startWorld()` で `dayNight.setDimension(dimension)` を 1 行**
  （次元の分岐は増えていない。`test/ui.test.ts` の routed に足して見張っている）
- **`daynight.ts` は `dimensions.ts` を import しない**（生成器を引き連れてくるので）。
  キーの綴りは `test/daynight.test.ts` が `DIMENSIONS` と突き合わせている
- 決まりごとは `.claude/rules/lighting.md` の「次元ごとの空」と `rules/dimensions.md` の 1 行。
  数値は `TUNING.md`（3 行・全部「目分量」）
- **わざと壊して 2 つとも赤くなることを確かめた**: `main.ts` の 1 行を消す →
  「出した判断を呼び直している」が NG / 星の出し分けを外す → ネザーとエンドが NG。
  **最初に書いた星の判定は最小値しか見ておらず、この壊し方を見逃した**（最大も見る形に直した）

- [x] 2-3. ネザー要塞（ネザーレンガ 48 の通路だけ）— ブラウザ確認待ち（`REVIEW.md`）。
      **進行テストの「ネザー要塞が原点から近くに生成される」が本物になり、達成 6 / 13**

  `fortress.ts` = **十字の通路 1 つ**（`structures.ts` の器に乗った最初の建物）。
  決まりごとは `.claude/rules/worldgen.md` の「ネザー要塞」。
  **部屋もブレイズの湧き場もまだ無い**（2-4 でブレイズと一緒に足す）。
  湧き方の数値（6 列ごと・0.6）は手触りなので、変えるならユーザーと決めること。
### [x] 2-4a. ブレイズ本体（飛ぶモブ・要塞に湧く・ブレイズロッド）**見** — ブラウザ確認待ち（`REVIEW.md`）

**2-4 を 2 つに割りました。** 1 コミットに収まらないのと、**当たり判定（誰に当たったか）が
まだ `projectiles.ts` に無い**ので、火球は「飛ばす」だけでなく「当てる」から作ることになるためです。
**進行テストの「ブレイズがブレイズロッドを落とす」が本物になり、達成 7 / 13**。

- アイテム 89（ブレイズロッド）。**50% で落ちる**（本家と同じ）
- **`MobDef` に 3 つ足した**: `flying` / `fireproof` / `spawnOn`。
  **飛び方の違いは全部表の値**で、`step()` に「ブレイズなら」は 1 行も書いていない
  （決まりごとは `.claude/rules/mobs.md` の「飛ぶモブ」「火に強いモブ」）
- 高さは **`flyTarget`**（`think` が 5Hz で決めて、`step` が毎フレーム寄せる）。
  床から 2.5 浮き、追うときはプレイヤーの少し上。**床が 8 以内に無ければその場を保つ**
  （溶岩の海の上がこれ）
- **どの敵対モブが湧くかは地面が決める**（`hostileFor()`）。
  **地面を指定したモブが勝つ**ので、ネザーレンガ（要塞の床）ではブレイズだけが湧く
- **わざと壊して 6 件とも赤くなることを確かめた**: `flying`/`fireproof` を false →
  浮く・落ちない・溝を越える・溶岩で焼けないの 4 件 / `hostileFor` を素朴な抽選に →
  湧く種類の 2 件
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていない

### [x] 2-4b. ブレイズの火球（当たり判定とダメージ）**見** — ブラウザ確認待ち（`REVIEW.md`）

**`onHitMob` / `onHitPlayer` の 2 本ではなく `onHitTarget` 1 本**にした。飛び道具の側は
「重なったか」という形の話しか持たず（`ProjectileTarget` = 印・足元・大きさ）、
**誰に何が起きるかは `mobs.ts` の `hitByProjectile()`** にまとまっている。
だから 2-12 の弓矢は、撃つ側を足すだけで当たる側が要らない。

- **`Projectile` に載ったのは 2 つだけ**: `owner`（撃った本人。**この相手には当たらない**）と
  `damage`（**撃った側が決めた値を運ぶだけ**）。**表にダメージは書いていない**
- **`mobs.ts` は `projectiles.ts` を import しない**（型だけ）。撃つのは
  `MobContext.shoot`（注文 `Shot` を渡す）で、`onDrop` が `drops.ts` を知らないのと同じ形
- `MobDef` に **`damage`（種類ごとの近接。ゾンビ 2 / ブレイズ 6 / 受動 0）** と
  **`ranged`（撃つもの・重み・間合い・間隔・高さ）** を足した。`MOB_DAMAGE` は
  `ZOMBIE.damage` の別名に変えてある
- **壁越しには撃たない**（`clearShot()` = `raycastVoxels` 1 本）。**近すぎても撃たない**（3m）
- **わざと壊して 4 通りとも赤くなることを確かめた**: 撃ち手の印を外す → 3 件 /
  見通しの判定を外す → 1 件 / 近接を 1 つの定数に戻す → 2 件 / 0.2m 刻みを外す → 2 件
- 決まりごとは `RULES-INBOX.md`（層 2 は無人の周が触れないため）。数値は `TUNING.md` に 5 行、
  確認項目は `REVIEW.md` の「2-4b」に 7 個。**進行は 7 / 13 のまま**（クリア導線の項目ではない）
### [x] 2-4c. 死んだら寝た次元へ戻る **見** — ブラウザ確認待ち（`REVIEW.md`）

**ユーザーがブラウザで見つけた不具合**（ネザーで死ぬと天井の岩盤の上に湧く）の直し。
**根はリスポーン地点が次元を持っていなかったこと**で、`surfaceY()` が上から降りてくるので
天井の岩盤の上面が返っていた。**進行は 7 / 13 のまま**（クリア導線の項目ではない）。

- **`BedPoint` に `dim` を足した**（`beds.ts`）。**地点はいまも世界に 1 つだけ**で、
  増えたのは「どの次元の 1 点か」だけ。**`beds.ts` は `dimensions.ts` を import しない**
  （`daynight.ts` の `setDimension()` と同じで文字列で受け、綴りはテストが `DIMENSIONS` と
  突き合わせる）。記録が無いときに戻る次元は `new Beds(OVERWORLD)` で渡す
- **どの次元へ戻るかは `beds.ts` の判断 2 本**: `respawnDimension()`（戻る次元）と
  `respawnPlan(world)`（戻ったあとの行き先。**ベッドが壊されていたら既定の次元へ落とす**）。
  **2 段になっているのは避けられない** —— 次元を移らないとベッドのマスが読めない。
  `main.ts` は `returnToDimension()` を挟んで並べるだけで、**次元の分岐は 1 つも増えていない**
- セーブは **`SaveData.bedDim`（省略可）**。**オーバーワールドなら書かない**ので、
  オーバーワールドだけで遊んでいる人のセーブは今までと 1 バイトも変わらない。
  **`bed` の `[x, y, z]` も `version` も変えていない**。無ければオーバーワールド
  （ネザーが入る前のセーブは、寝た場所が必ずオーバーワールドだった）
- **次元を移る経路が 2 つになった**（ポータル / リスポーン）ので、**`switchTo()` に
  `liveState()` を渡しているか**を `test/dimensions.test.ts` が数えるようにした ——
  渡し忘れると、残してきた改変・落とし物・かまど・チェストが黙って消える（型では防げない）
- **わざと壊して 4 通りとも赤くなることを確かめた**: 次元を戻す 1 行を消す → 1 件 /
  `liveState()` を `emptyState()` に → 1 件 / 戻り先を常にオーバーワールドに → 4 件 /
  `bedDim` を書かない → 1 件
- **テストの偽陽性を 1 つ踏んだ**: 2 つの次元の**同じマス**にベッドを置いていたので、
  行き先の次元を間違えても見つかってしまい、(3)(4) が緑のまま通った（別のマスに直した）
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていない。
  決まりごとは `RULES-INBOX.md` に 3 件（`rules/dimensions.md` 2 件・`rules/beds.md` 1 件）

### [x] 2-5. エンダーマン（テレポート + エンダーパール）**見** — ブラウザ確認待ち（`REVIEW.md`）

**進行テストの「エンダーマンがエンダーパールを落とす」が本物になり、達成 8 / 13。**
アイテム 90（エンダーパール。**積めるのは 16 個まで**）。

- **`MobDef` に 2 つ足した**: `teleport`（`TeleportRule`）と `spawnWeight`。
  **飛ぶモブ（`flying`）とまったく同じ形**で、`update()` に「エンダーマンなら」は 1 行も無い
- **判断は 2 つに割れている**: 「いつ跳ぶか」は `Mobs.teleport()`（`world` が要る）、
  **「どこへ跳べるか」は `teleportSpot()` 1 本**（壁・液体・未生成の列の保険はそこだけ）。
  跳ぶ理由は 2 つ —— **殴られて逃げる**（確率つき）と、**遠い相手へ詰める**
- **殴られたときは印（`teleportUrge`）を立てるだけ。** `wound()` は殴られた・撃たれた・
  焼けたの 3 か所から呼ばれるので、印にすれば 3 通りとも同じ経路に乗る
- **湧きの抽選に重みを入れた**（`pickWeighted()`。受動と敵対で共有）。
  均等割りのままエンダーマンを足すと、**夜の敵対の半分がエンダーマン**（体力 40・一撃 7）になる
- **わざと壊して 5 通りとも赤くなることを確かめた**: 液体の保険を外す → 1 件 /
  未生成の列の保険を外す → 1 件 / 当たり判定の保険を外す → 1 件 /
  重みを均等に戻す → 2 件（敵対の比と受動の 2 種類）/ cooldown を 0 に → 1 件
- **本家と違って中立ではない**（見つめると怒る、が無い）。`hostile` の使われ方を
  まとめて考え直すことになるので、別のタスクにしてある
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていない。
  **`main.ts` は 1 行も変えていない**（1400 行のまま）。決まりごとは `RULES-INBOX.md` に 2 件、
  数値は `TUNING.md` に 12 行、確認項目は `REVIEW.md` の「2-5」に 9 個

### [x] 2-6. エンダーアイ（ブレイズパウダー + エンダーパール）**見** — ブラウザ確認待ち（`REVIEW.md`）

**クリア導線の 2 本がここで合流します**（ネザー要塞のロッド / 夜のエンダーマンのパール）。
**進行テストの「エンダーアイが作れる」が本物になり、達成 9 / 13**。

- アイテム 91（ブレイズパウダー）/ 92（エンダーアイ）。**レシピは 2 つとも形なし**なので
  **手持ちの 2x2 で作れます** —— ネザーや夜の野外で作業台を探しに戻らずに済みます
- **`items.ts` と `crafting.ts` に行を足しただけ**で、新しいファイル・新しい判断・
  新しい「確かめられないもの」はありません（`main.ts` は 1 行も触っていません）
- **エンダーアイのスタック上限は 64、エンダーパールは 16** と食い違わせてあります。
  理由は一点で、**エンドポータルの枠 12 個ぶんが 1 枠に収まること**
  （`test/crafting.test.ts` が 12 個を数えて見張っています）
- **わざと壊して 3 通りとも赤くなることを確かめた**: アイの材料をロッドに直結 → 4 件
  （進行の後戻りを含む）/ アイのスタックを 8 に → 1 件 / ブレイズの落とし物を
  腐った肉に → 進行が 2 件（ロッドとアイ）未達に落ちて後戻りが NG
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていない。
  決まりごとは `RULES-INBOX.md` に 1 件、数値は `TUNING.md` に 6 行、
  確認項目は `REVIEW.md` の「2-6」に 6 個

### [x] 2-7. 投げたエンダーアイが要塞の方へ飛ぶ **見** — ブラウザ確認待ち（`REVIEW.md`）

**進行テストの「投げたエンダーアイが要塞の方を向く」が本物になり、達成 10 / 13。**
**要塞はまだ建ちません**（建てるのは 2-8）。この周で決めたのは**どこに建つかの規則**だけです。

- 判断は **`stronghold.ts`**（新規）。`nearestStronghold` / `strongholdDirection` /
  `eyeShot`。**three も DOM も乱数も出てきません**（`portals.ts` / `beds.ts` と同じ形）
- **`structures.ts` から `SiteRule` と `siteAt()` を切り出した**のが肝心です ——
  **建つ場所の出どころを 1 か所にする**ため。写して 2 か所に持つと、
  **アイの指す方角に何も無い**という形で静かに壊れます（しかも地面の下なので掘るまで
  気付けません）。2-8 は `STRONGHOLD_SITE` を広げて `StructureDef` にすること
- **アイは投げても減りません**（本家は 20% で壊れる）。**手触りの数値**なので
  `TUNING.md` に置いて進めました —— 減らす形にすると、枠 12 個ぶんを集めきる前に
  探索でアイを使い切る詰みが作れます
- **種は `worldSeed`。** `world.seed` はネザーだと塩を混ぜたあとの値なので、
  渡すと**ネザーで投げたアイだけ別の場所を指します**（`rules/dimensions.md`）
- **テストの偽陽性を 1 つ踏んだ**: 探す広さ（±2 マス）を 1 に落としても、
  手で選んだ 6 箇所でも面舐め 1600 箇所でも**総当りと同じ答えが返って緑のまま通りました**
  （取りこぼすのはマスの角のごく狭い所だけ）。**総当りとの突き合わせでは守れない**ので、
  「探す広さがマスの対角の最悪（`√2 × マス`）を超えている」という**理屈のほうを判定**に
  置き直しました
- **わざと壊して 5 通りとも赤くなることを確かめた**: 探す広さを 1 に → 1 件 /
  投げる向きを固定に → 3 件（進行の後戻りを含む）/ 場所を別の種で持つ（＝写した状態）→
  4 件 / 上向きを 0 に → 1 件 / `main.ts` の配線を外す → 2 件
- `structures.ts` はチャンク生成の中なので `npm run bench` を**交互に 3 回ずつ**測った:
  `generateChunk` 1.46 / 1.45 / 1.46 → 1.43 / 1.42 / 1.40ms（退行なし）。
  `main.ts` は 1400 → 1413 行（上限 1500）

### [x] 2-8. 要塞（石レンガ 53 + エンドポータル部屋 + 枠 51）**見** — ブラウザ確認待ち（`REVIEW.md`）

**`stronghold.ts` が「どこに建つか」と「どんな形か」の両方を持つようになりました。**
`STRONGHOLD` は `STRONGHOLD_SITE` を広げただけ（写していない）なので、
**投げたアイの指す先と実際に建つ場所が食い違いようがありません。**
**進行は 10 / 13 のまま**（起動は 2-9 の項目です）。

- ブロック 2 つ: **石レンガ 53** と **エンドポータル枠 51**（向き 4 x アイの有無 2 = 8 通り。
  向き違いは 104..110）。**枠は壊せません**（岩盤と同じ扱い）——
  掘れると、起動する前に壊して詰められます
- 形は**エンドポータルの部屋 1 つだけ**（通路も階段も部屋も無い）。内側 11 マス四方 x 高さ 4、
  石レンガの殻、真ん中に枠 12 個の輪（**中の 3x3 は空**）、壁に松明 4 本。
  地面から **18 マス下**（`lift: -18`）に床を置くので、掘って降りることになります
- **`stampPlacements()` に横の飛ばしを足した** —— 列挙は 1 チャンクにつきグリッド 3x3 マス
  ぶん出てくるので、飛ばさないと**捨てられるだけの `put` を 9000 回**呼びます。
  結果は 1 マスも変わらないことをテストで固定（`test/structures.test.ts`）
- **わざと壊して 6 通りとも赤くなることを確かめた**: 枠の向きを外向きに → 1 件 /
  横の飛ばしを外す → 1 件 / `extent` を小さく申告 → 2 件 / 深さを 30 に → 1 件 /
  最初からアイを嵌める → 1 件 / `STRUCTURES` から外す → 1 件
- `structures.ts` はチャンク生成の中なので `npm run bench` を**交互に 3 回ずつ**測った:
  `generateChunk` 0.89 / 0.89 / 0.90ms（要塞なし）→ **0.91 / 0.88 / 0.93ms**（誤差の中）。
  `main.ts` は **1 行も触っていない**（1413 行のまま）
- 決まりごとは `RULES-INBOX.md` に 2 件、数値は `TUNING.md` の「要塞の部屋（2-8）」に 9 行、
  確認項目は `REVIEW.md` の「2-8」に 7 個

### [x] 2-9. エンドポータルの起動（枠 12 個にエンダーアイ）**見** — ブラウザ確認待ち（`REVIEW.md`）

**枠にアイが嵌まり、12 個そろうと輪の内側 3x3 がポータルの面になります。**
**進行テストの「エンドポータルが 12 個の枠で起動する」が本物になり、達成 11 / 13**。
**まだエンドへは行けません**（面を踏んでも何も起きない。移動は 2-10）。

- 判断は **`endportal.ts`**（新規。`ringAt` / `activate` / `fitEye` / `eyeMessage`）。
  three も DOM も乱数も出てこない（`portals.ts` / `beds.ts` / `stronghold.ts` と同じ形）
- **`portals.ts` に混ぜていません。** あちらは「黒曜石の枠を探して火を点ける」、
  こちらは「もう建っている輪の状態を進める」で、枠の探し方も点き方も共通点がありません
- **輪の中心は枠の向きからは決まりません。** 1 辺の 3 個は同じ向きを向いているので、
  「向きへ 2 マス進んだ先が中心」だと**角寄りの 8 個が別の場所を指します**。
  代わりに**「自分が輪のどの位置に居るか」を 12 通りとも試します**
- **輪の形は `stronghold.ts` の `RING_OFFSETS` / `RING_INSIDE` 1 か所**（写していない）。
  `test/endportal.test.ts` は**手で輪を並べず、`STRONGHOLD.build()` が実際に建てた輪**に
  アイを嵌めます —— 手で並べると、建てる側と起動する側が食い違っても両方とも緑になります
- ブロック **エンドポータル 52**（寝ているので向きは 1 つ。**壊せない**・通り抜ける・
  `emission` 15）。`FRAME_HEIGHT` を**読んで**高さを合わせているので、枠の輪から飛び出さない
- **わざと壊して 6 通りとも赤くなることを確かめた**: 中心を軸上の枠だけから探す →
  10 件 / 11 個で起動 → 4 件 + 進行の後戻り / 嵌め直しを許す → 1 件 /
  輪の内側を 5x5 に → 4 件 / 嵌めるついでに向きを決め直す → 1 件 /
  `main.ts` の配線を外す → 進行の後戻り + 「出した判断を呼び直している」
- `stronghold.ts` はチャンク生成の中なので `npm run bench` を 3 回:
  `generateChunk` 1.08 / 0.99 / 1.00ms（中央 1.00。退行なし）。
  `main.ts` は 1413 → 1441 行（上限 1500）
- 決まりごとは `RULES-INBOX.md` に 2 件、数値は `TUNING.md` の
  「エンドポータルの起動（2-9）」に 7 行、確認項目は `REVIEW.md` の「2-9」に 7 個

### [x] 2-10. エンド次元（エンドストーン 50 の島）**見** — ブラウザ確認待ち（`REVIEW.md`）

**起動したエンドポータルの面を踏むと、エンドの島に降ります。**
**進行テストの「エンドへ行ける」が本物になり、達成 12 / 13**（仮は残り 1 件）。

- ブロック **エンドストーン 50**（普通の立方体。硬さ 3 / ツルハシ）。
  生成器は **`endgen.ts`**（新規。`nethergen.ts` と同じ `ChunkSource`）で、
  **原点のまわりの島 1 つだけが地面、あとは全部虚空。岩盤は 1 マスも置いていない** ——
  塞ぐと、落ちた人が世界の底に立って「虚空の意味」が消えます
- **`DIMENSIONS` にエンドを足した**（`salt` はネザーとも別の値）。
  空の行は 2-3b で置いてあったので、**`daynight.ts` は 1 行も触っていません**
- **守っている不変条件は 1 つだけ: 出る場所（`END_SPAWN`）が必ず地面の上にあること。**
  **点は `endgen.ts` が持ちます**（そこが地面だと保証しているのがあのファイルなので）。
  `LANDING_RADIUS`(5) の内側は上面が平ら、`LANDING_RAMP`(10) マス掛けてうねりに繋ぐ
- **ポータルが 2 種類になった。** 見分けは **`portalAt()` 1 か所**、行き先と目指すマスは
  **`planTravel()` 1 本**、出る場所は **`arriveThrough()` 1 本**。種類の違いは
  **型 1 つ（`PortalHere`）に載せて運ぶ**ので、**`main.ts` の次元の分岐は 0 件のまま**
  （上限 2）。`main.ts` は 1440 → 1449 行（上限 1500）
- **エンドへは座標を掛け算で移さない。** 島は原点のまわりにしかないので、ネザーの 1:8 と
  同じ形にすると**要塞が遠い人（＝ほぼ全員）が虚空に出て即死します**
- **エンドで `arrive()` を使い回さない。** あちらは「枠を探して、無ければ建てる」なので、
  **島の真ん中に黒曜石の枠が生えてネザーへ抜けられます。** `landOnGround()` は何も置きません
- **エンドからは戻れません**（本家と同じ。出口は 2-13 でドラゴンを倒すと開く）。
  落ちて死ねばリスポーン地点へ戻るので**詰みにはなりません**
- **テストの偽陽性を 1 つ踏んだ**: ふちの揺れを「半径 48 の円の面積の何 %」で見ていたが、
  **真円でも 100% なのでそのまま通る**（実測 108%）。**ふちの半径そのもの**を
  360 方向で測る形に置き直した（実測 39.1〜57.4）
- **わざと壊して 6 通りとも赤くなることを確かめた**: エンドも座標を掛け算で移す → 5 件 /
  底に岩盤を敷く → 6 件 / 出る所の平らを外す → 2 件 / エンドの面を見分けない → 2 件 /
  エンドでも `arrive()` で枠を建てる → 4 件 / `portalTarget` が面の種類を無視する → 8 件
  （どれも進行の後戻りを含む）
- 熱い場所（`worldgen` / `mesher` / `World.update`）は触っていないが、次元の表を触ったので
  `npm run bench` を**前後 3 回ずつ**測った: `generateChunk` **1.49 / 1.66 / 1.48ms**（前）→
  **1.48 / 1.42 / 1.41ms**（後）。中央 1.49 → 1.42（誤差の中）。
  エンドの生成そのものは **0.116ms**（オーバーワールド 2.87ms と同じ土俵で測って）
- 決まりごとは `RULES-INBOX.md` に 3 件、数値は `TUNING.md` の
  「エンドの島と、エンドへの移動（2-10）」に 13 行、確認項目は `REVIEW.md` の「2-10」に 7 個

### [x] 2-11a. 黒曜石の柱（エンドの島に 10 本）**見** — ブラウザ確認待ち（`REVIEW.md`）

**2-11 を 2 つに割りました。** 柱は地形（`endgen.ts` に 1 本足すだけ）ですが、
**エンドクリスタルは「湧かない・歩かない・消えない個体」**という新しい形が要るので、
1 コミットには収まりません（`LOOP.md` の「2」）。**進行は 12 / 13 のまま**
（クリア導線の項目ではありません）。

- **`structures.ts` の器には乗せませんでした。** あちらは `SiteRule`（グリッドに確率で撒く）で、
  **エンドの柱は「原点のまわりの決まった場所に必ず 10 本」**という別の性質のものです。
  島の中心が原点だと分かっているので、`endgen.ts` の中で直に建てるほうが素直でした
  （`HANDOFF.md` が「どちらにするか先に決めること」と書いていた点）
- **新しいブロックは 1 つも足していません**（黒曜石 43 をそのまま使います）。
  **`main.ts` は 1 行も触っていません**（1449 行のまま）
- **位置も高さも種に依りません**（`PILLARS` の表）。ドラゴンの戦場（2-13）は
  誰のワールドでも同じ形になります。輪の半径 28 / 10 本 / 高さ 12〜39 / 太さ 2〜3
- **柱を立ててよい場所は式で決めてあります**（`EDGE_MIN_REACH` = `ISLAND_RADIUS * (1 - EDGE_WOBBLE)`
  = 36）。**実測した最小のふち（ある種で 39.1）で線を引かないこと** —— 別の種で虚空に柱が浮きます
- **上面は絶対の高さで止めるので平ら**です（島のうねりは根元で吸収）。
  上に 2 マス空けてあるので、クリスタル（2-11b）はそのまま載ります
- **わざと壊して 7 通りとも赤くなることを確かめた**: 走査の帯を `PILLAR_RING ± 半径` の
  決め打ちに → 3 件 / 虚空の列にも柱を置く → 1 件 / 輪を島のふちの外（40）へ → 3 件 /
  高さを列ごとの上面から測る → 4 件 / `generateChunk` が柱を渡さない → 1 件 /
  高さを昇順に並べる → 1 件 / 輪を出る所（4）に重ねる → 6 件
- `endgen.ts` はチャンク生成の中なので `npm run bench` を 3 回: `generateChunk`
  **1.54 / 1.44 / 1.43ms**（中央 1.44。2-10 の中央 1.42 と誤差の中。オーバーワールドの
  生成器は 1 行も触っていません）。**エンドの生成そのものは 0.115ms**
  （2-10 の 0.116ms から動いていません。`test/endgen.test.ts` が毎回出します）
- 決まりごとは `RULES-INBOX.md` に 1 件、数値は `TUNING.md` の
  「エンドの黒曜石の柱（2-11a）」に 7 行、確認項目は `REVIEW.md` の「2-11a」に 6 個

### [x] 2-11b. エンドクリスタル（柱の上に載る・壊せる）**見** — ブラウザ確認待ち（`REVIEW.md`）

**柱 10 本の上に 1 個ずつ載り、素手でも矢でも壊せます。**
**進行は 12 / 13 のまま**（クリア導線の項目ではありません）。

- **「モブ側か、新しい器か」の問いに、3 つ目の答えを出しました: ブロック（54）。**
  どちらの弱点も無く、**新しい `*render.ts` が 1 つも増えません**
  （＝「確かめられないもの」が 1 つも増えない）:
  - **壊した記録が `edits` に乗る**ので、読み込み直しても戻りません
    （モブ側の最大の弱点。テストが本物の `World` で往復して押さえています）
  - **当たり判定が要りません** —— 掘るのも飛び道具（`projectiles.onHitBlock`）も
    もともとブロックに当たります（新しい器の最大の弱点）
- **`solid: true` でないと飛び道具が素通りします**（`collisionBoxes()` が `solid` で
  ないブロックに空の箱を返す）。**矢で壊せることがクリア導線**なので、ここは崩せません
- 判断は **`crystals.ts`**（新規。`crystalStates` / `liveCrystals` / `shatterCrystal`）。
  **居場所は `endgen.ts` の `CRYSTAL_SPOTS`**（`PILLARS` から `top + 1` で引く。写していない）
- **生き死には `hasColumn` を見ます**（`alive` / `gone` / `unknown` の 3 つ）。
  **`unknown` は生きているに数えません** —— 数えると、エンドに降りた直後に
  砕いたはずのクリスタルからドラゴンが回復し続けます
- **`projectiles.remove()` を足しました。** 砕いた矢を消さないと、
  **相手だけが消えて矢が空中に浮いたまま残ります**
- **わざと壊して 9 通りとも赤くなることを確かめた**: 柱の上面に埋める → 10 件 /
  `solid` を外す → 1 件 / 虚空の判定より先に置く → 1 件 / `hasColumn` を見ない → 3 件 /
  何のブロックでも砕く → 2 件 / 自分自身を落とす → 1 件 / 砕いた弾を消さない → 1 件 /
  居場所を写す → 9 件 / `main.ts` が自分でブロック ID を見る → 1 件
- `endgen.ts` はチャンク生成の中なので `npm run bench` を 3 回:
  `generateChunk` **0.89 / 1.02 / 1.12ms**（中央 1.02）。**エンドの生成そのものは 0.072ms**
  （`test/endgen.test.ts` が毎回オーバーワールドと同じ土俵で測って出します）。
  `main.ts` は 1449 → 1465 行（上限 1500）
- 決まりごとは `RULES-INBOX.md` に 3 件（**うち 1 件は新しいファイル
  `.claude/rules/crystals.md`。`paths` の行も書いてあります**）、
  数値は `TUNING.md` の「エンドクリスタル（2-11b）」に 6 行、
  確認項目は `REVIEW.md` の「2-11b」に 7 個

### [x] 2-12. 弓と矢（クリスタルを壊す手段）**見** — ブラウザ確認待ち（`REVIEW.md`）

**右クリック長押しで引き、離すと矢が飛びます。** アイテム 93（弓）/ 94（矢）。
**進行は 12 / 13 のまま**（クリア導線の項目ではありません）。

- **足したのは「撃つ側」だけ。** 当たる側は 1 行も要りませんでした（`onHitTarget` →
  `mobs.hitByProjectile()` も、`onHitBlock` → `shatterCrystal()` も、もう繋がっている）——
  2-4b で「表にダメージを書かず、撃った側が `Shot.damage` に載せる」形にしておいた成果です
- 判断は **`bow.ts`**（新規。引きの長さ・放てる下限・ダメージ・中断する条件・撃ち出す高さ）。
  **`Eating` とまったく同じ形**で、three も DOM も乱数も持ち物も出てきません
  （テストが `items` / `inventory` / `Projectiles` の混入を見張っています）
- **材料は本家の 2 つとも代用**: 弓は糸の代わりに**羊毛 3**（蜘蛛が居ない）、
  矢は羽根抜きの**火打石 + 棒で 4 本**（鶏が居ない）。`TUNING.md` に 12 行
- **浅い引き（0.2 秒未満）では放ちません。** 本家は浅いほど遅く飛びますが、
  **矢の速さは `PROJECTILE_KINDS` の固定値**なので、真似ると「浅く連打するほど強い」になります
- **満引きの合図は音だけ**（`sfx.ts` に `bow` を 1 行足し、`FULL_DRAW_PITCH` 倍で鳴らす）。
  引く動きは描けないので、これが唯一の手ごたえです。**`audio.ts` は 1 行も触っていません**
- **テストの偽陽性を 1 つ踏んだ**: `test/ui.test.ts` の `routed` に `drawing.release(` を
  並べたが、**その語は呼ぶ関数（`loose()`）の定義の中にもある** ——
  マウスの配線を外しても**全部緑のまま**でした。**呼び出しの形**（`loose();` /
  `updateDrawing(dt)`）も並べる形に直しています（`RULES-INBOX.md`）
- **わざと壊して 10 通りとも赤くなることを確かめた**: 放てる下限を 0 に → 2 件 /
  中断の条件を落とす → 2 件 / `release()` が引きを止めない → 2 件 /
  満引きの合図を毎フレーム → 1 件 / 離しても放たない（配線を外す）→ 1 件（**直したあと**）/
  毎フレーム引き進めない → 1 件 / `bow.ts` が持ち物を知る → 1 件 /
  足元から撃つ → 2 件 / 矢が積めない → 2 件 / 矢と松明を同じ形に → 2 件 /
  弓を 2 個で作れるように → 2 件
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていない。
  `main.ts` は 1465 → **1499 行（上限 1500。余白 1 行）**。次は下の 2-12b から取ること

### [x] 2-12b. `main.ts` の余白を作る（2-3c と同じやり方）— 済み（**1499 → 1445 行 / 上限 1500**）

**上限は上げていません。** 出したのは `useOrPlace()` の振り分け（右クリックで何が起きるかの
11 通り）で、**`placing.ts` に寄せるのではなく `use.ts` を新しく作りました** ——
あちらは「置ける／点けられるか」の**可否**を持つファイルで、こちらが持つのは
**そもそも置く話に来るかどうか**（器・バケツ・アイ・火種・弓・食べ物が先に取る）。
混ぜると、`tryPlace()` が作業台やかまどを知ることになります。

- **`decideUse(aim, facts)` は純粋**（`UseFacts` → `UseAction`）。three も DOM も
  `World` も持ち物も出てこないので、11 通りを丸ごとヘッドレスで確かめられます
  （`test/use.test.ts` に 28 件。`npm test` は 2005 → 2033 件）
- **並び順そのものが判断**です。壊れると現物を狙うまで気付けないものが 3 つあり、
  それぞれに判定を置きました: 器が先（作業台の上に立って置けない）/
  **嵌めるが投げるより先**（枠を狙ってもアイが飛んでいく。枠は地下 18 マス）/
  バケツが食べ物より先
- **置ける／置けないは決めません。** 置けないアイテムも `place`（`base` が `AIR`）で返し、
  可否は `placing.ts` のまま。**2 か所で弾かないこと**
- **`main.ts` に振り分けが戻っていないか**を `test/use.test.ts` が語で見張り
  （`isBucket(` / `isBow(` / `isFireStarter(` / `isEndPortalFrame(` / `ENDER_EYE`）、
  `test/ui.test.ts` の `routed` に **`decideUse(` と `useOrPlace();` の両方**を並べました
  （2-12 で踏んだ偽陽性。**実際、配線だけ外したときに反応したのは呼び出しの形のほう**でした）
- **わざと壊して 7 通りとも赤くなることを確かめた**（**コミットしてから**）:
  かまどを `baseBlock` で見ない → 1 件 / 嵌めると投げるを入れ替え → 2 件 /
  矢が無いときに黙る → 2 件 / クリエイティブでも食べる → 1 件 /
  バケツに狙う先を要求 → 1 件 / `useOrPlace();` の配線を外す → 1 件 /
  `main.ts` に `isBucket(` を書き戻す → 1 件
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていない。
  **見た目・音・GLSL も 1 行も触っていない**ので `REVIEW.md` に追記はありません
  （挙動は 11 通りとも 2-12 までと同じ）。決まりごとは `RULES-INBOX.md` に 2 件
  （**うち 1 件は新しいファイル `.claude/rules/use.md`。`paths` の行も書いてあります**）

### [x] 2-13a. エンダードラゴン本体（ボス・輪を回る飛行・クリスタルで回復）**見** — ブラウザ確認待ち（`REVIEW.md`）

**2-13 を 2 つに割りました。** 1 コミットに収まらないのと、**体力バーとクリア画面は DOM**
（＝ここでは確かめられない側）で、本体の AI とは確かめ方がまるで違うためです。
**進行テストの「エンダードラゴンを倒せる」が本物になり、達成 13 / 13（仮の判定 0 件）。**

- **ボスの違いは 3 つとも表の値**（`MobDef` に足したのは `boss` / `orbit` / `regen` /
  `hover` の 4 つ）。**`update()` にも `step()` にも「ドラゴンなら」は 1 行もありません**
  —— 飛ぶモブ（`flying`）・跳ぶモブ（`teleport`）とまったく同じ形です
- **`boss` は 1 つの印で 3 か所に効きます**（湧きの抽選に出ない / 遠くても消えない /
  数の上限で間引かれない）。**別々の印にしないこと** —— 「湧かない」だけを足すと、
  輪の反対側へ回った拍子にデスポーンして戦いが終わります
- **置くのは `ensureBoss(次元, world)` だけ**（毎フレーム呼んでよい）。どの次元に何が
  居るかは **`BOSSES` の表**（キーは次元の名前。`mobs.ts` は `dimensions.ts` を
  import しません）なので、**`main.ts` の次元の分岐は 0 件のまま**（上限 2）
- **回復のもとを数えるのは `main.ts`**（`liveCrystals(world).length` → `MobContext.healers`）で、
  **1 個につきどれだけ戻るかは表**（`regen` = 2）。クリスタル 10 個で毎秒 20 なので、
  **矢でも斧でも削り切れません**（＝先に柱を落とすことになる）
- **`hover` を種類ごとに持たせました。** ブレイズの 2.5 をそのまま使っていたときは、
  目の前まで降りてきても `ATTACK_HEIGHT`(1.5) に届かず**一度も殴れないボス**でした。
  ついでに**「追うときの高さに『その場の高さ』を混ぜない」**直しも入っています ——
  混ざっていると、`FLY_SCAN`(8) より高い所に居る飛ぶモブは**そもそも降りてこられません**
- **わざと壊して 9 通りとも赤くなることを確かめた**（**コミットしてから**）:
  抽選にボスを残す → 2 件 / 遠いボスを消す → 6 件 / 倒しても湧き直す → 2 件 /
  中心を貼り直さない → 2 件 / もとの数を数えない → 3 件（進行の後戻りを含む）/
  高い所から降りてこない → 2 件 / 浮きをブレイズと同じにする → 1 件 /
  `main.ts` の召喚を外す → 2 件 / `main.ts` が `healers` を渡さない → 2 件
- **テストの偽陽性を 2 つ踏んだ**: 詰めてくる判定でプレイヤーをドラゴンの真下に
  置いていたので**フレーム 0 から距離 0**で通り、降りてくる判定も**湧いた直後は
  地面に居る**ので最初から満たしていました（**先に 12 秒回して輪へ上げてから**測る形に直した）
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていない。
  `main.ts` は 1445 → **1453 行（上限 1500）**。決まりごとは `RULES-INBOX.md` に 1 件
  （`rules/mobs.md` の新しい節 3 つ）、数値は `TUNING.md` に 13 行、
  確認項目は `REVIEW.md` の「2-13a」に 7 個

### [x] 2-13b-1. 出口ポータル（倒した印）**見** — ブラウザ確認待ち（`REVIEW.md`）

**2-13b を 2 つに割りました**（`LOOP.md` の「2」）。1 コミットに収まらないのと、
**体力バーとクリア画面は DOM**（＝ここでは確かめられない側）で、確かめ方がまるで違うためです。
**進行は 13 / 13 のまま**（クリア導線の項目ではありません）。

- **倒すと島の中心の脇（+X へ 3 マス）に 3x3 のエンドポータルの面が建ち、踏むと
  オーバーワールドへ戻れます。** `portalTarget(END, "end")` はもとから定義してあったのに
  **エンドに面が 1 枚も無かった**ので通っていませんでした（`rules/dimensions.md`）
- **建った面がそのまま「倒した印」**です。`edits` に乗る（＝読み込み直しても残る）ので、
  **エンドに入り直してもドラゴンは湧き直しません**。**新しいセーブのキーは 1 つも
  足していません**（`SaveData.version` は 1 のまま）—— クリスタルをブロックにしたのと同じ筋
- 判断は **`exitportal.ts`**（新規。`exitPortalState` / `buildExitPortal` / `syncExitPortal`）。
  three も DOM も乱数も出てこない（`crystals.ts` / `endportal.ts` と同じ形）。
  **居場所は `endgen.ts` の `EXIT_PORTAL_SPOT`** —— そこが平らな地面だと保証しているのが
  あのファイルなので、`END_SPAWN` とまったく同じ理由で点を向こうに置いています
- **倒したかどうかは 2 段。** この読み込みのあいだは **`Mobs.bossDefeated()`**
  （「召喚したのに、もう居ない」で決める。ボスは遠くても消えず上限でも間引かれないので、
  居なくなる理由は倒された以外に無い）、**世界をまたぐのは出口ポータルのほう**です。
  **`ensureBoss()` の第 3 引数に既定値を付けていません** —— 付けると渡し忘れが型で通り、
  「倒しても湧き直すエンド」が黙って戻ります（必須にしたので 10 か所とも
  コンパイルで止まりました）
- **降りる場所（`END_SPAWN`）と重ねないこと。** 重ねると、着いた瞬間に面の中に立って
  引き返され、島に立てません（テストが `endgen.ts` 側と `exitportal.ts` 側の両方で見ています）
- **9 マスとも見て「建っている」を決めること。** 中心 1 マスだけで決めると、
  未読み込みの列で書き込みが落ちた**穴あきの面が二度と埋まりません**
- **わざと壊して 8 通りとも赤くなることを確かめた**（**コミットしてから**）:
  中心 1 マスだけで決める → 1 件 / `hasColumn` を見ない → 2 件 / 床を置かない → 1 件 /
  倒していなくても建てる → 2 件 / `ensureBoss` が印を無視する → 2 件 /
  `bossDefeated` が返さない → 1 件 / 降りる場所に重ねる → 3 件 /
  `main.ts` の配線を外す → 1 件
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていない
  （`endgen.ts` は表を 1 つ足しただけで、`generateChunk` の中は 1 行も変えていません）。
  `main.ts` は 1453 → **1462 行（上限 1500）**。決まりごとは `RULES-INBOX.md` に 3 件
  （**うち 1 件は新しいファイル `.claude/rules/exitportal.md`。`paths` の行も書いてあります**）、
  数値は `TUNING.md` に 4 行、確認項目は `REVIEW.md` の「2-13b-1」に 7 個

### [x] 2-13b-2. 体力バーとクリア画面 **見** — ブラウザ確認待ち（`REVIEW.md`）

**倒したことが画面で伝わるようになりました。** 進行は **13 / 13 のまま**
（クリア導線の項目ではありません）。**ドラゴンの動きも数値も 1 つも変えていません。**

- 判断は **`boss.ts`**（新規。`bossBarState` / `victoryMessage` / `VictoryWatch`）。
  three も DOM も乱数も出てこない（`crystals.ts` / `exitportal.ts` と同じ形）。
  **`mobs.ts` を import しません** —— 受け取るのは器ではなく**必要な値**（`BossFacts`）で、
  `Mobs.activeBoss()` が返すものと**構造で合わせて**あります（`vitals.ts` の `FoodValue` と
  `items.ts` の `FoodDef` と同じ作法。食い違えばテストの型検査で止まります）
- **`mobs.ts` に足したのは 2 本だけ**: `activeBoss(次元)`（いま生きているボスの名前と体力）と
  `bossName(次元)`（**倒したあとにも要る** —— 倒れたモブはもう `list` に居ないので、
  クリア画面の 1 行に出す名前が `activeBoss()` では取れません）。
  **1 体だけを前提にしていません**（`BOSSES` は次元ごとの表）
- **置き場所そのものが判断でした。** 体力バー（`#bossbar`）は **`#hud` の中**、
  **クリア画面（`#victory`）は `#hud` の外** —— `#hud` はロックが外れると `hidden` に
  なるので、クリア画面をあの中に置くと**倒しても永久に出ません**（クリア画面は
  ロックを外して出すものなので）。`test/boss.test.ts` が `index.html` の並びで見ています
- **クリア画面は「倒した瞬間」だけ**（`VictoryWatch` が `mobs.bossDefeated()` の
  false → true を拾う）。**「印が立っているか」で出しません** —— 出口ポータルは
  `edits` に乗って残るので、入り直すたびにクリア画面が出ます
- **見張りに戻し方（`reset()`）を足していません。** 次元を移る・作り直すと
  `mobs.clear()` が走って `bossDefeated()` が false に戻るので、同じ経路で戻ります
- **わざと壊して 9 通りとも赤くなることを確かめた**（**コミットしてから**）:
  体力 0 でも帯を出す → 1 件 / 割合を頭打ちにしない → 1 件 / 残りを切り捨てる → 1 件 /
  立ち上がりを見ずに毎フレーム出す → 1 件 / `activeBoss` が次元を見ない → 2 件 /
  クリア画面を `#hud` の中へ → 1 件 / 帯の配線を外す → 1 件 /
  **倒した印で出す（`victory.update()` に `syncExitPortal()` の返り値を渡す）→ 0 件** /
  **メニューを出さない条件から `hud.victoryOpen` を外す → 0 件**
- **最後の 2 つは、最初は見逃していました**（`main.ts` はヘッドレスで import できないので、
  中で何を**渡している**かは誰も見ていなかった）。`test/ui.test.ts` の `routed` を
  **呼び出しの形**（`victory.update(defeated)` / `const defeated = mobs.bossDefeated(` /
  `!hud.victoryOpen`）まで見る形にして、2 つとも 1 件ずつ赤くなることを確かめ直しました
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていないので `bench` は取っていない。
  `main.ts` は 1462 → **1485 行（上限 1500。余白 15 行）**。決まりごとは
  `RULES-INBOX.md` に 2 件（**`.claude/rules/dom-ui.md` の新しい節は `paths` に
  `src/boss.ts` と `test/boss.test.ts` の 2 行が要ります**）、
  数値は `TUNING.md` の「ボスの体力バーとクリア画面（2-13b-2）」に 6 行、
  確認項目は `REVIEW.md` の「2-13b-2」に 7 個

<!-- 着手したときの見取り図（残しておく。次に似たものを足すときの手本）

  **導線は 13 / 13 で通っていて、倒した印（出口ポータル）も立つようになりました。**
  ここは「倒したことが画面で伝わる」ぶんの肉付けで、進行テストの件数は動きません。
  **いまの合図は「島の中心の脇に面が建つ」ことだけ**です。

  1. **体力バー。** ボスが居るあいだだけ画面の上に出す。**判断（いつ出す・いつ消す・
     何割か）は `mobs.ts` か新しい判断のファイルに置き、`ui.ts` は貼るだけ**にすること
     （`hud.flash()` と同じ形。`#hud` の中に置くと、隠れるパネルの罠に掛かります）。
     **ボスが 1 体だけであることを前提にしないほうが楽です** —— `BOSSES` は次元ごとの表なので、
     「いま居るボス」を返す 1 本を `mobs.ts` に足すのが素直
  2. **クリア画面。** 死亡画面（`#death`）と同じ作りで文言だけ違うもの。
     **1 行の組み立ては `vitals.ts` の `deathMessage()` と同じ形**で、判断の側に置くこと。
     **出す条件は「倒した瞬間」**（`bossDefeated()` が false → true に変わったところ）で、
     **印が立っているだけで出さないこと** —— 出すと、倒したあとエンドへ入り直すたびに
     クリア画面が出ます

  **`main.ts` の余白は 38 行**（1462 / 上限 1500）。**足りなくなったら上限を上げず、
  2-12b と同じやり方でもう 1 つ出すこと** —— 次に出しやすいのは**左クリックの振り分け**
  （`mousedown` の「モブとブロックのどちらが手前か」とスポイト。
  `use.ts` に `decideAttack()` を足す形）。
-->

**この周のあと、`main.ts` の余白は 15 行です**（1485 / 上限 1500）。
次に何かを足す周は、**まず上の見取り図が言っている減量から取ること**
（左クリックの振り分けを `use.ts` の `decideAttack()` へ）。**上限は上げないこと。**

---
