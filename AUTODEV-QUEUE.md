# 次に足すもの（AUTODEV のキュー）

**上から順に取ります。** 1 件取るごとに `AUTODEV-SPEC.md` を書き（B の周）、
実装してからこの行を消します（C の周）。手順は `AUTODEV.md`、見送った理由は
`docs/autodev-log.md`。**足すのは A（棚卸し）の周だけ** —— 実装の周で継ぎ足さないこと。

基準: 本家のバージョン順（Alpha → Beta → 正式版）で、まだ無いものを上から取る

**この基準は次の A の周でも変えないこと。** 変えると取りやすいものだけを拾って穴が残ります。
並べ直すのは `AUTODEV.md` の A-2（確かめられるものが先 → 枠を食わないものが先 →
ジャンルの順 → 依存が済んでいるもの）だけです。

**行の番号は付けた順で、取る順ではありません。** 取る順は上からです。

## 数えた結果（2026-09-19・コードから数え直したもの）

| ジャンル | いま何があるか（前の A = 2026-09-12 との差） |
| --- | --- |
| ブロック | 立方体 **42**（+1）/ 非立方体 **89**（+10。うち向き違いの帯 47）。**空き: 1..63 が 7（57..63）・111..255 が 69（187..255）** |
| アイテム | **145 種**（124 → +21）・`MAX_ITEM_ID` **186**（162 → +24）。道具 20（5 種 x 4 階層）+ シアーズ / 食べ物 **11**（±0）/ **防具 16 部位**（4 → +12。革・鉄・金・ダイヤ） |
| レシピ・精錬 | クラフト **82 本**（65 → +17）/ 精錬 **10 本**（7 → +3）/ 燃料 **9 種**（8 → +1） |
| モブ | **10 種類**（±0）。受動 4（豚・羊・鶏・牛）/ 敵対 5（ゾンビ・クモ・スケルトン・ブレイズ・エンダーマン）/ ボス 1 |
| バイオーム・地形 | **11 種類**（±0） |
| 構造物 | **2 つ**（`FORTRESS` / `STRONGHOLD`）。エンドの柱は `endgen.ts` が直に建てる |
| 効果 | `DamageCause` **9 種**（±0。落下・溺れ・奈落・モンスター・空腹・毒・溶岩・炎上・サボテン） |
| 次元 | **3 つ**（±0） |

**数え方**: `npm test` の「ブロック ID の枠」の行と、`src/**` を esbuild で束ねて
`RECIPES.length` / `SMELTING.size` / `FUEL.size` / `MOBS` / `BIOMES` / `itemName()` /
`toolOf()` / `armorOf()` / `foodOf()` を直に数えました（**README も `ROADMAP.md` も
根拠にしていません**）。**6 周で 24 番号・17 レシピ**（防具 12 部位・粘土・粘土玉・
レンガ・苗木 2 種・木炭・上付きハーフ 2 つ・ツタ 4 向き）。

**数え直して見つかった穴**（どれも下のキューに入れました）:

- **クモは壁を登らず、明るくても襲ってきます**（38 / 39）。本家のクモの 2 つの性質が
  どちらも `mobs.ts` に 1 行もありません
- **エンダーマンは水に入っても平気です**（40）。**モブは溺れも傷つきもしません**
  （`mobs.ts` の 1468 行の注記どおり、水面に浮くところまで）
- **サボテンは育ちません**（37）。`crops.ts` の道を持つのは小麦・サトウキビ・苗木の 3 つだけ
- **投げた卵（129）は当たっても何も起きません**（43）。ダメージ 0 でヒヨコも湧かない
- **フェンスは 1 材質だけ**（157。本家は木とネザーレンガ）（41）
- **骨（162）の使い道はまだ 0 本**（35 が残っているため）

## 取るもの

**ID は取るときに `ROADMAP.md` の予約表から次の空きを取ること。** 下の「ID: N 個」は
個数の見積もりで、番号を予約するものではありません（順番が変わると番号がずれるので、
**先に番号を書き込まないこと**）。**共有帯の次の空きは 187・低帯は 57 から。**

- [ ] 36. **`test/audio.test.ts` の揺れを測り方で直す**（**本家の機能ではありません**）— **ID 0 個** / **`src/**` 0 行の見込み**（触るのは `test/**` だけ）/ 依存: なし。**15 回に 2 回ほど赤く、赤い周は公開サイトに載りません** —— 費用がいちばん大きいのでここに置きました。**直すのは測り方**（窓を長く取る・複数回鳴らして中央値）で、**判定は 1 文字もゆるめないこと**（`rules/testing.md`）
- [ ] 37. **サボテンが育つ**（Alpha）— **ID 0 個** / **`main.ts` 0 行の見込み**（`crops.update(dt, world)` はもう配線済み）/ 依存: なし。`crops.ts` の **4 つ目の道**（小麦・サトウキビ・苗木に続く）。**置いたぶんだけ伸びます** —— 自然生成ぶんが伸びないのはサトウキビと同じ
- [ ] 38. **クモが壁を登る**（Alpha からある性質）— **ID 0 個** / **`main.ts` 0 行の見込み**（判断は `mobs.ts` だけ）/ 依存: なし。段差 0.5 の当たり判定はそのままで、**壁に当たっているあいだだけ上へ動く**形
- [ ] 39. **クモは明るいところでは襲ってこない**（Beta 1.8）— **ID 0 個** / **`main.ts` 0 行の見込み** / 依存: なし。**明るさの読み方は `spawnBrightness()` がもう持っています**（湧きの判定と同じ式を使うこと）
- [ ] 40. **エンダーマンが水に触れると痛い**（Beta 1.8）— **ID 0 個** / **`main.ts` 0 行の見込み** / 依存: なし。**`mobs.ts` の溶岩の道（`isHotLiquid`）とそっくりな形**で、`DamageCause` は増えません（モブ側の体力なので）
- [ ] 41. **ネザーレンガのフェンス**（Beta 1.9）— **ID 1 個** / **`main.ts` 0 行の見込み** / 依存: なし。**`model: "fence"` と `fenceConnects()` の表がもうあります**（`mesher.ts` の `case "fence"` は 1 文字も変えない見込み）。**⚠ 色: ネザーレンガ（48）とレンガ（12・170）に近いので、一覧で 20 以上離すこと**（ずらした値は `TUNING.md` へ）
- [ ] 42. **石炭ブロック**（正式版 1.6）— **ID 1 個** / **`main.ts` 0 行の見込み** / 依存: なし。**鉄・金・ダイヤの立方体（135..137）とまったく同じ形**（9 個でしまう / 形なしで戻す）に、**`FUEL` の 1 行**（本家は 800 秒 = 石炭 10 個ぶん）が付くだけ。**⚠ 色: 石炭（65）と石炭鉱石から 20 以上離すこと**
- [ ] 43. **投げた卵からヒヨコが湧く**（Alpha 1.0.14）— **ID 0 個** / 依存: **卵（129）も鶏も湧きの道も済み**。ただし **`main.ts` の `onHitBlock`（255 行）に 1〜3 行増えます**（下の ⚠ の判断待ち）。本家は 1/8
- [ ] 35. **骨粉**（骨 1 → 骨粉 3。苗にかけると実る）— **ID 1 個** / 依存: **骨（162）も小麦の苗（121）も済み**。ただし **`use.ts` に 19 通り目が要り、`main.ts` の `switch` に `case` が 1 行増えます**（下の ⚠ の判断待ち）
- [ ] 24b. **ケーキをかじる（7 回）** — 位置ごとの状態（`add-stateful-block`）と `use.ts` の 18 通り目 / **ID 0 個** / 依存: **24a は済み（2026-09-08）。残るは `main.ts` を割ってよいかの人の判断だけ**（下の ⚠）

**未着手 10 件**（**12 刈られた羊は 2026-09-18 の C の周で済み**・**34a / 34b / 34c のツタも
2026-09-17 と 2026-09-18 に済み**・**33a / 33b の防具 12 部位も 2026-09-16 に済み**）。
並べたのは `AUTODEV.md` の A-2 の順（確かめられるもの → 枠を食わないもの → ジャンル →
依存が済んだもの）で、**取りやすさで並べてはいません**。**36 だけが本家の機能ではなく、
公開サイトに載らない周を減らすための持ち越しです**（`HANDOFF.md` の「人がやること」）。
**37..40 の 4 件は ID も `main.ts` も 0 の見込み**なので、**下の ⚠ の判断が下りなくても
6 周ぶんは進みます**（41 / 42 は番号を 1 個ずつ使うだけ）。
**下 3 件（43 / 35 / 24b）が後ろに居るのは `main.ts` の判断待ちだから**です。

## ⚠ `main.ts` の行数がもう効いています

**いま 1450 行**（テストの数え方。`wc -l` では 1449。上限 1500・**止まる目安 1450 に並んでいます**）。
**次に 1 行でも配線が要る周は、その前に人の判断が要ります**（`AUTODEV.md` の停止条件 2）。
**ただし「判断を出せば減る」なら止まりません** —— 27b-1 がそのとおりになりました。

**⚠ 「配線が要る」と「行が増える」は別です。** 23 は `updateVitals()` に渡す 1 行の
**名前を書き換えただけ**で通り、27b-1 は**セーブのキーを `session.ts` に決めさせて −1 行**に
なりました。**何行要るかは、取るときに B の周が数えること。**

2026-09-05 に `test/ui.test.ts` を読んで分かったこと: あの一覧は **`main.ts` に残って
いるべき呼び出しを 40 件、文字列で見張っています**（`decideUse(` `tryBucket(` `fitEye(` …）。
だから**大きい塊はどれも動かせません** —— 動かすなら見張りの側を新しいファイルへ
付け替える必要があり、**それは人が決めることです**（`HANDOFF.md` の「人がやること」）。

## ⚠ 一覧の色はもう総当たりで測ること

**無彩色（灰）と赤茶と黄色と水色は埋まっています**（33a / 33b で 12 色使いました。
`rules/items-survival.md`）。**素直な値はたいてい判定の 20 を割ります** ——
**選ぶ前に総当たりで測り、割ったらずらして、ずらした値を `TUNING.md` に書くこと**
（判定はゆるめない）。**41 のネザーレンガ色と 42 の石炭色はここに当たります。**

## 見送ったもの

**理由は `docs/autodev-log.md` に 1 行ずつ書いてあります。** 同じものを毎周
検討し直さないために、ここには名前だけ置きます:
水流・溶岩流 / 経験値とエンチャント / レッドストーン / 醸造とポーション /
ネザークォーツ / 天候（雨・雪）/ 村と村人 / TNT と爆発 / 釣り竿と魚 / ドアと看板 /
カボチャとジャック・オ・ランタン / 苔石 / スライム / 染料と 16 色の羊毛 /
クリーパー / ハーフでない階段（ネザーレンガ・石レンガ）/ ダンジョン /
**ブロックに埋まると窒息する** / **落ちたアイテムが溶岩で燃える** /
**動物の繁殖（小麦で増える）** / **鉄格子とガラス板**。

**TNT と爆発**は 5 周続けて見送りです —— 「球状にまとめて消す」道が 1 行も無く、
メッシュ化 6ms の予算に効くので、取るなら**それだけで 1 周**になります。
**クリーパー**は爆発そのものなので、TNT と同じ扱いです。
**窒息**と**繁殖**は**どちらも `main.ts` が増える側**なので、43 / 35 / 24b の
判断が下りてから見ること（窒息は `VitalsContext` に 1 つ、繁殖は `use.ts` の 1 通り）。
**溶岩でアイテムが燃える**のは、**いまの「液体にはどれでも浮かせる」が人の選んだ判断**
（`drops.ts` の 347 行）なので、変えてよいかを先に聞くこと。
