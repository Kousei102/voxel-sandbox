# 次に足すもの（AUTODEV のキュー）

**上から順に取ります。** 1 件取るごとに `AUTODEV-SPEC.md` を書き（B の周）、
実装してからこの行を消します（C の周）。手順は `AUTODEV.md`、見送った理由は
`docs/autodev-log.md`。**足すのは A（棚卸し）の周だけ** —— 実装の周で継ぎ足さないこと。

基準: 本家のバージョン順（Alpha → Beta → 正式版）で、まだ無いものを上から取る

**この基準は次の A の周でも変えないこと。** 変えると取りやすいものだけを拾って穴が残ります。
並べ直すのは `AUTODEV.md` の A-2（確かめられるものが先 → 枠を食わないものが先 →
ジャンルの順 → 依存が済んでいるもの）だけです。

**行の番号は付けた順で、取る順ではありません。** 取る順は上からです。

## 数えた結果（2026-09-23・コードから数え直したもの）

| ジャンル | いま何があるか（前の A = 2026-09-19 との差） |
| --- | --- |
| ブロック | 立方体 **43**（+1）/ 非立方体 **90**（+1。うち向き違いの帯 47）。**空き: 1..63 が 7（57..63）・111..255 が 67（189..255）** |
| アイテム | **147 種**（145 → +2）・`MAX_ITEM_ID` **188**（186 → +2）。道具 20（5 種 x 4 階層。**金の階層が無い**）+ シアーズ / 食べ物 **11**（±0）/ 防具 **16 部位**（±0） |
| レシピ・精錬 | クラフト **85 本**（82 → +3）/ 精錬 **10 本**（±0）/ 燃料 **10 種**（9 → +1。石炭ブロック） |
| モブ | **10 種類**（±0）。受動 4（豚・羊・鶏・牛）/ 敵対 5（ゾンビ・クモ・スケルトン・ブレイズ・エンダーマン）/ ボス 1 |
| バイオーム・地形 | **11 種類**（±0） |
| 構造物 | **2 つ**（`FORTRESS` / `STRONGHOLD`）。エンドの柱は `endgen.ts` が直に建てる |
| 効果 | `DamageCause` **9 種**（±0。落下・溺れ・奈落・モンスター・空腹・毒・溶岩・炎上・サボテン） |
| 次元 | **3 つ**（±0） |

**数え方**: `npm test` の「ブロック ID の枠」の行と、`src/**` を esbuild で束ねて
`allItemIds()` / `RECIPES.length` / `SMELTING.size` / `FUEL.size` / `MOB_KINDS` / `BIOMES` /
`DIMENSIONS` / `toolOf()` / `armorOf()` / `foodOf()` を直に数えました（**README も
`ROADMAP.md` も根拠にしていません**）。前の A からの差は 41 / 42 の 2 番号と 3 レシピだけ。

**数え直して見つかった穴**（どれも下のキューに入れました）:

- **金の道具が 1 本もありません**（49）。`TIER_NAMES` が「木・石・鉄・ダイヤ」の 4 つきりで、
  **金インゴット（67）の使い道は防具・金のリンゴ・金ブロックだけ**。本家では Indev から在る
- **グロウストーンはそれ自体を落とします**（47）。本家 Alpha 1.2 からは粉 2〜4 個
- **クモは糸しか落としません**（48）。`SPIDER.drop` は `extra` を持っていない
- **サボテンはどこにでも置けます**（44）。`def(CACTUS)` に `needsSoil` の類が無い。
  **サトウキビも水の隣でなくても置けます**（45）
- **キノコは置いても増えません**（46）。`crops.ts` の `notePlaced()` が覚えるのは
  サトウキビ・サボテン・苗木 2 種だけ
- **木はオークとトウヒの 2 種だけ**（50）。`TreeKind` は `"oak" | "spruce" | "cactus"`

## 取るもの

**ID は取るときに `ROADMAP.md` の予約表から次の空きを取ること。** 下の「ID: N 個」は
個数の見積もりで、番号を予約するものではありません（順番が変わると番号がずれるので、
**先に番号を書き込まないこと**）。**共有帯の次の空きは 189・低帯は 57 から。**

- [ ] 47. **グロウストーンダスト**（本家 Alpha 1.2。壊すと粉 2〜4 個・粉 4 個でブロック）— **ID 1 個** / `items.ts` の `DROPS` 1 行・`crafting.ts` 1 行 / 依存: なし。**⚠ 一覧の色は黄色が埋まっている**ので総当たりで測ること（`rules/items-survival.md`）
- [ ] 48. **クモの目**（本家 Beta 1.9 → 正式 1.0。クモが 1/3 で落とす・食べると毒）— **ID 1 個** / `SPIDER.drop.extra` 1 つ・`FOODS` に `poison: true` 1 行 / 依存: **毒（`vitals.ts`）は済み**
- [ ] 49. **金の道具 5 種**（ツルハシ・斧・シャベル・剣・クワ。本家 Indev）— **ID 5 個** / **⚠ 本家の金は「掘れる階層は木と同じ・速さは 12 で最速」**で、いまの `tier` は「掘れる階層」と「速さの添字」を 1 つの数で兼ねています。**B の周でどう割るかを先に数えること**（`WOOD_PICKAXE + (tier-1)*3 + k` のループに混ぜない。剣・クワの注記と同じ）/ 耐久 32 回（`durability.ts`）/ 依存: なし
- [ ] 50. **シラカバの木**（原木・葉・苗木。本家 Beta 1.2）— **ID 3 個** / 生成（`worldgen.ts` の森）を触るので **`npm run bench` が要る** / 見た目に出る（撮ること）/ 依存: **苗木が木になる道（30b）は済み**
- [ ] 35. **骨粉**（骨 1 → 骨粉 3。苗にかけると実る）— **ID 1 個** / 依存: **骨（162）も小麦の苗（121）も済み**。ただし **`use.ts` に 19 通り目が要り、`main.ts` の `switch` に `case` が 1 行増えます**（下の ⚠ の人の判断待ち）
- [ ] 24b. **ケーキをかじる（7 回）** — 位置ごとの状態（`add-stateful-block`）と `use.ts` の 18 通り目 / **ID 0 個** / 依存: **24a は済み（2026-09-08）。残るは `main.ts` を割ってよいかの人の判断だけ**（下の ⚠）

**未着手 6 件**（新しく 7 件 + 持ち越し 2 件。**44 は AUTODEV 120・45 は AUTODEV 122・46 は AUTODEV 124 で済み**）。並べたのは `AUTODEV.md` の A-2 の順で、
**取りやすさで並べてはいません**: 44..46 が ID 0 個 → 47 / 48 が 1 個 → 49 が 5 個 →
50 は見た目と生成に出るので確かめにくい側 → **35 / 24b は依存（人の判断）が
済んでいないので後ろへ回しました**（A-2 の 4）。**全部取っても共有帯は 67 → 56。**

## ⚠ `main.ts` の行数がもう効いています

**いま 1450 行**（テストの数え方。`wc -l` では 1449。上限 1500・**止まる目安 1450 に並んでいます**）。
**次に 1 行でも配線が要る周は、その前に人の判断が要ります**（`AUTODEV.md` の停止条件 2）。
**ただし「判断を出せば減る」なら止まりません** —— 27b-1 がそのとおりになりました。
**44..50 の 7 件は `main.ts` 0 行の見込み**で選びました（**見込みです。B の周が数えること**）。

**⚠ 「配線が要る」と「行が増える」は別です。** 23 は `updateVitals()` に渡す 1 行の
**名前を書き換えただけ**で通り、27b-1 は**セーブのキーを `session.ts` に決めさせて −1 行**に
なりました。**何行要るかは、取るときに B の周が数えること。**

2026-09-05 に `test/ui.test.ts` を読んで分かったこと: あの一覧は **`main.ts` に残って
いるべき呼び出しを 40 件、文字列で見張っています**（`decideUse(` `tryBucket(` `fitEye(` …）。
だから**大きい塊はどれも動かせません** —— 動かすなら見張りの側を新しいファイルへ
付け替える必要があり、**それは人が決めることです**（`HANDOFF.md` の「人がやること」）。

## ⚠ 一覧の色はもう総当たりで測ること

**無彩色（灰）と赤茶と黄色と水色は埋まっています**（33a / 33b で 12 色使いました。
`rules/items-survival.md`）。**素直な値はたいてい判定の 20 を割ります** ——
**選ぶ前に総当たりで測り、割ったらずらして、ずらした値を `TUNING.md` に書くこと**
（判定はゆるめない）。**47 のグロウストーンダスト（黄）と 49 の金の道具はここに当たります。**

## 見送ったもの

**理由は `docs/autodev-log.md` に 1 行ずつ書いてあります。** 同じものを毎周
検討し直さないために、ここには名前だけ置きます:
水流・溶岩流 / 経験値とエンチャント / レッドストーン / 醸造とポーション /
ネザークォーツ / 天候（雨・雪）/ 村と村人 / TNT と爆発 / 釣り竿と魚 / ドアと看板 /
カボチャとジャック・オ・ランタン / 苔石 / スライム / 染料と 16 色の羊毛 /
クリーパー / ハーフでない階段（ネザーレンガ・石レンガ）/ ダンジョン /
**ブロックに埋まると窒息する** / **落ちたアイテムが溶岩で燃える** /
**動物の繁殖（小麦で増える）** / **鉄格子とガラス板** /
**ラピスラズリ** / **ゾンビピッグマン** / **ガスト** / **クリティカルヒット** /
**耕地が乾く・踏み荒らされる**。

**TNT と爆発**は 6 周続けて見送りです —— 「球状にまとめて消す」道が 1 行も無く、
メッシュ化 6ms の予算に効くので、取るなら**それだけで 1 周**になります。
**クリーパー**と**ガスト**は爆発そのものなので、TNT と同じ扱いです。
**窒息**と**繁殖**は**どちらも `main.ts` が増える側**なので、35 / 24b の
判断が下りてから見ること（窒息は `VitalsContext` に 1 つ、繁殖は `use.ts` の 1 通り）。
**溶岩でアイテムが燃える**のは、**いまの「液体にはどれでも浮かせる」が人の選んだ判断**
（`drops.ts` の 347 行）なので、変えてよいかを先に聞くこと。
