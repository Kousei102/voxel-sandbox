# 次に足すもの（AUTODEV のキュー）

**上から順に取ります。** 1 件取るごとに `AUTODEV-SPEC.md` を書き（B の周）、
実装してからこの行を消します（C の周）。手順は `AUTODEV.md`、見送った理由は
`docs/autodev-log.md`。**足すのは A（棚卸し）の周だけ** —— 実装の周で継ぎ足さないこと。

基準: 本家のバージョン順（Alpha → Beta → 正式版）で、まだ無いものを上から取る

**この基準は次の A の周でも変えないこと。** 変えると取りやすいものだけを拾って穴が残ります。
並べ直すのは `AUTODEV.md` の A-2（確かめられるものが先 → 枠を食わないものが先 →
ジャンルの順 → 依存が済んでいるもの）だけです。

**行の番号は付けた順で、取る順ではありません。** 取る順は上からです。

## 数えた結果（2026-09-04・コードから数え直したもの）

| ジャンル | いま何があるか |
| --- | --- |
| ブロック | 名前つき 1..54 と共有帯 3 個（116 耕地 / 121 苗 / 123 実った小麦）。立方体 36 / 非立方体 69（うち向き違いの帯 47）。**空き: 1..63 が 9・111..255 が 122** |
| アイテム | 一覧 98 種類（`MAX_ITEM_ID` 133）。道具は**ツルハシ・斧・シャベル・剣・クワの 5 種 x 4 階層**とシアーズ。食べ物 8 種 |
| レシピ・精錬 | クラフト **44 本**（道具 20・ハーフ 4・階段 4 を含む）/ 精錬 **7 本**（鉄・金・砂・丸石・生豚肉・生鶏肉・生牛肉）/ 燃料 8 種 |
| モブ | **9 種類**。受動 4（豚・羊・鶏・牛）/ 敵対 4（ゾンビ・クモ・ブレイズ・エンダーマン）/ ボス 1（ドラゴン） |
| バイオーム・地形 | 10 種類（海・浜・砂漠・平原・森・針葉樹林・雪原・高山・岩山・雪の浜） |
| 構造物 | 2 つ（`FORTRESS` / `STRONGHOLD`）。エンドの柱は `endgen.ts` が直に建てる |
| 効果 | `DamageCause` 8 種（落下・溺れ・奈落・モンスター・空腹・毒・溶岩・炎上）。**餓死はしない**（`STARVE_FLOOR` 1） |
| 次元 | 3 つ（オーバーワールド・ネザー・エンド） |

**クリア導線は 13 / 13 で終わっています**（`test/progression.test.ts`）。
ここから先は「本家にあってここに無い、サバイバルの地の部分」を埋めていきます。

**数え直して分かったこと**: 前の表（2026-09-01）は「アイテム 78 種 / `MAX_ITEM_ID` 94 /
モブ 6 種類 / クラフト 34 本」でした。**5 周で 20 番号・3 モブ・10 レシピ増えています。**
**鉄・金・ダイヤに「しまう形」がまるごと無い**のもこの数え直しで出ました（下の 14 番）。

## 取るもの

**ID は取るときに `ROADMAP.md` の予約表（111..255 の共有帯）から次の空きを取ること。**
下の「ID: N 個」は個数の見積もりで、番号を予約するものではありません
（順番が変わると番号がずれるので、**先に番号を書き込まないこと**）。**次の空きは 134 から。**

- [ ] 13. 雪玉（雪ブロックを掘ると出て、右クリックで投げられる）— **`main.ts` に 1 行も要らない**（`use.ts` の `throw` は `items.ts` の `thrownProjectile()` を見るだけ。卵と同じ道）/ 触るのは `items.ts` の `DROPS` と `projectiles.ts` の `PROJECTILE_KINDS` 1 行（ダメージ 0）/ **戻すレシピ（雪玉 4 → 雪ブロック）も同じ周で書くこと** —— 落とし物を差し替えると**雪が置けなくなります** / ID: 1 個 / 依存: なし
- [ ] 14. 鉄・金・ダイヤのブロック（9 個 → 1 個、戻すと 9 個）— **立方体 3 つを共有帯に置くだけ**。`main.ts` にも `*render.ts` にも 1 行も要らず、判定は `crafting.ts` のレシピ 6 本で全部出せる / **ダイヤに初めて「持ち物以外の行き場」ができます** / ID: 3 個 / 依存: なし
- [ ] 15. ミルクバケツ（牛を右クリック）と、飲むと毒が消えること — 牛（20 番）とバケツ（84）はもうある。`use.ts` に `milk` を 1 つ足し、`vitals.ts` に「毒を消す」を置く / ID: 1 個 / 依存: **`main.ts` の行数**（羊を刈るのと同じ形で振り分けが 3〜5 行要る。**いま 1464 行**なので、先に判断を別ファイルへ出すこと）
- [ ] 16. サボテンに触るとダメージ — **ID を 1 個も使いません。** `player.ts` が「サボテンに触っている」を出し、`vitals.ts` が `DamageCause` に 1 つ足して間隔と量を持つ（溶岩とまったく同じ形）/ ID: 0 個 / 依存: **`main.ts` の行数**（`VitalsContext` へ渡す配線が 1 行。15 番と同じ理由で先に出すこと）
- [ ] 17. キノコ 2 種（赤・茶）とボウル・キノコシチュー — 草むらと同じ `model: "cross"` の生えもの。暗い所に生える条件は `worldgen.ts` / 食べ物は `FOODS` の 1 行 / ID: 4 個 / 依存: なし（**ボウルは板 3 個で、木の行き場が 1 つ増えます**）
- [ ] 18. サトウキビと砂糖 — 水辺に生えて**上に伸びる**。伸ばす仕掛けは `crops.ts` にあるが、**あれは「その場で ID が変わる」形で、上へ 1 マス置く形はまだ無い** / ID: 2 個 / 依存: 伸び方の仕掛け（ケーキ・紙・本はこの後）
- [ ] 19. はしご（登れる）— 向き 4 つぶんの番号と、`physics.ts` の「掴まっているあいだ重力が効かない」が要る / ID: 4 個 / 依存: **向き違いの帯（64..110）は満杯**なので 4 個とも共有帯から取ること
- [ ] 11. スケルトンと骨 — 弓を撃ってくる敵。飛び道具の器（`projectiles.ts`）はもうある / ID: 2 個 / 依存: 見た目
- [ ] 12. 刈られた羊が見て分かること — **5 を 2 件に割った後半**（B の周・2026-09-02）。`mobrender.ts` は形を `MobKind` ごとに 1 つ作って使い回すので、2 つ目の形と差し替えの仕掛けが要る / ID: 0 個 / 依存: **5 は済み**（`Mob.woolTimer` がもう立つので、描画側はそれを見るだけ）

**未着手 9 件。** 上 2 件（13・14）は**見た目にも `main.ts` にも触らずに閉じられる**ので先頭で、
15・16 は**`main.ts` の行数が解けるまで**その次、17〜19 は生成と物理が絡むぶん後、
11・12 は**見た目がユーザーにしか確かめられない**ので最後です。取りやすさで並べてはいません。

## ⚠ 15 番より前に要ること: `main.ts` が 1464 行

**`AUTODEV.md` の「止まって人を呼ぶ条件」2 番（1450 行）を越えたままです**（上限 1500）。
13・14 は 1 行も触らないので先に取れますが、**15 番を取る周は、先に判断を別ファイルへ
出して行数を空けること。** これは本家の機能ではないので、キューの行にはしていません。

## 見送ったもの

**理由は `docs/autodev-log.md` に 1 行ずつ書いてあります。** 同じものを毎周
検討し直さないために、ここには名前だけ置きます:
水流・溶岩流 / 防具 / 経験値とエンチャント / レッドストーン / 醸造とポーション /
ネザークォーツ / 天候（雨・雪）/ 村と村人 / TNT と爆発。

**この周で数え直した 2 件**（2026-09-01 の A が「次の A で見直す」と書いたもの）:

- **TNT と爆発** —— **また見送り。** 重力ブロックは済みましたが、あれは**1 マスずつ**動かす
  形なので、「球状にまとめて消す」道は 1 行も出来ていません（メッシュ化 6ms の予算に効く）。
- **防具** —— **また見送り。** 革（132）が入って材料の依存は解けましたが、**番号 12〜16 個と
  インベントリ画面の枠（DOM）**が残っています。**次の A の周で「枠だけ」に割れないか見ること。**
