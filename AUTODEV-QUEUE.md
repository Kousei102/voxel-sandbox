# 次に足すもの（AUTODEV のキュー）

**上から順に取ります。** 1 件取るごとに `AUTODEV-SPEC.md` を書き（B の周）、
実装してからこの行を消します（C の周）。手順は `AUTODEV.md`、見送った理由は
`docs/autodev-log.md`。**足すのは A（棚卸し）の周だけ** —— 実装の周で継ぎ足さないこと。

基準: 本家のバージョン順（Alpha → Beta → 正式版）で、まだ無いものを上から取る

**この基準は次の A の周でも変えないこと。** 変えると取りやすいものだけを拾って穴が残ります。
並べ直すのは `AUTODEV.md` の A-2（確かめられるものが先 → 枠を食わないものが先 →
ジャンルの順 → 依存が済んでいるもの）だけです。

**行の番号は付けた順で、取る順ではありません。** 取る順は上からです。

## 数えた結果（2026-09-06・コードから数え直したもの）

| ジャンル | いま何があるか |
| --- | --- |
| ブロック | 名前つき 1..54 と共有帯 8 個（116 耕地 / 121 苗 / 123 実った小麦 / 135..137 鉱物ブロック / 139 赤キノコ / 140 茶キノコ / 143 サトウキビ / 145..148 はしご 4 向き）。立方体 39 / 非立方体 76（うち向き違いの帯 47）。**空き: 1..63 が 9・111..255 が 107** |
| アイテム | 一覧 110 種類（`MAX_ITEM_ID` 145）。道具 20 個（ツルハシ・斧・シャベル・剣・クワの 5 種 x 4 階層）とシアーズ。食べ物 9 種 |
| レシピ・精錬 | クラフト **55 本** / 精錬 **7 本**（鉄・金・砂・丸石・生豚肉・生鶏肉・生牛肉）/ 燃料 8 種 |
| モブ | **9 種類**。受動 4（豚・羊・鶏・牛）/ 敵対 4（ゾンビ・クモ・ブレイズ・エンダーマン）/ ボス 1（ドラゴン。`BOSSES` は `end` の 1 件） |
| バイオーム・地形 | 10 種類（海・浜・砂漠・平原・森・針葉樹林・雪原・高山・岩山・雪の浜） |
| 構造物 | 2 つ（`FORTRESS` / `STRONGHOLD`）。エンドの柱は `endgen.ts` が直に建てる |
| 効果 | `DamageCause` 9 種（落下・溺れ・奈落・モンスター・空腹・毒・溶岩・炎上・**サボテン**） |
| 次元 | 3 つ（オーバーワールド・ネザー・エンド） |

**前の表（2026-09-04）との差**: アイテム 98 → **110**・`MAX_ITEM_ID` 133 → **145**・
レシピ 44 → **55**・食べ物 8 → **9**・立方体 36 → **39**・`DamageCause` 8 → **9**。
**5 周で 12 番号・11 レシピ増えています**（雪玉・鉱物ブロック 3 つ・ミルクバケツ・
キノコ 2 種・ボウル・シチュー・サトウキビ・砂糖・はしご）。

**20（リンゴ）を実装したので直しました**（2026-09-07）: **`Drop.extra` は前からあり**、
足りなかったのは**その確率と、それを引く 2 本目の乱数**でした。いまは
`extra.chance` と `rollDrops(id, roll, extraRoll)` があるので、**「2 山目を確率つきで
足す」は表に 1 行**です（`extraRoll` は `BreakOrder` と `autoBreak()` を通ります）。

## 取るもの

**ID は取るときに `ROADMAP.md` の予約表（111..255 の共有帯）から次の空きを取ること。**
下の「ID: N 個」は個数の見積もりで、番号を予約するものではありません
（順番が変わると番号がずれるので、**先に番号を書き込まないこと**）。**次の空きは 155 から**（154 = クモの巣が埋まりました）。

- [ ] 24. ケーキ（Beta 1.2）— 小麦 3 + 砂糖 2 + 卵 1 + ミルクバケツ 3。**置いて 7 回かじる**ので `furnaces.ts` と同じ「位置ごとの状態」（`add-stateful-block`）/ ID: 2 個 / 依存: 砂糖（144）・卵（129）・ミルクバケツ（138）・小麦（124）とも済み
- [ ] 25. 氷（Alpha 1.0.14）— 雪原と雪の浜の水面が凍る。**滑る**（`player.ts` の摩擦）・壊すと水に戻る / ID: 1 個 / 依存: 見た目（半透明の立方体）と `worldgen.ts`
- [ ] 26. フェンス（Alpha 1.0.1）— 棒 6 → 2 個。**跳び越えられない**（当たり判定だけ 1.5 マス）/ ID: 1 個 / 依存: 見た目（隣と繋がる形）と `physics.ts`
- [ ] 27b. 革の防具 4 部位 — 着る経路・DOM の枠 4 つ・**防具のセーブ**（下の「防具を割った結果」）/ ID: 4 個 / 依存: **27a も革（132）も済み**。**`main.ts` に 1 行要ります**（セーブ）
- [ ] 11. スケルトンと骨 — 弓を撃ってくる敵。飛び道具の器（`projectiles.ts`）はもうある / ID: 2 個 / 依存: 見た目
- [ ] 12. 刈られた羊が見て分かること — **5 を 2 件に割った後半**（B の周・2026-09-02）。`mobrender.ts` は形を `MobKind` ごとに 1 つ作って使い回すので、2 つ目の形と差し替えの仕掛けが要る / ID: 0 個 / 依存: **5 は済み**（`Mob.woolTimer` がもう立つので、描画側はそれを見るだけ）

**未着手 6 件**（**23 は 2026-09-08 の C の周で実装済み**。予告どおり ID 1 個・
`main.ts` に **±0 行**でした）。11・12 は**見た目がユーザーにしか確かめられない**ので最後です。
取りやすさで並べてはいません（A-2 の順: 確かめられるもの → 枠を食わないもの → 依存が済んだもの）。

**⚠ 「残る 7 件はどれも `main.ts` に配線が要る」は行き過ぎでした**（2026-09-07 の B の周で
23 をコードで追い直した結果）。**「配線が要る」と「行が増える」は別**です —— 23 は
`updateVitals()` に渡す 1 行の**名前を書き換えただけ**で通りました（2026-09-08 に実測。
**判断は `player.ts` の `clinging` ゲッター側**に置いてあります）。
**何行要るかは、取るときに B の周が数えること。**

## 防具を割った結果（2026-09-04 の A が「次の A で見ること」と書いたもの）

**割れます。** 2026-09-05 に `src/panels.ts` が出来たので、DOM の枠は前ほど重くありません。
**27 は B の周で次の 2 件にすること**（1 周に畳まないこと。ID 4 個 + DOM + 被ダメージの
計算を 1 周でやると 120 行の仕様書に収まりません）:

- **27a. 防具の枠 4 つと防御の計算** —— **済み**（2026-09-07 の C の周）。
  `inventory.ts` の `armor` 4 枠と `armorPoints` / `items.ts` の空の `ARMORS` /
  `vitals.ts` の `armorReduced()`。**ID 0 個・`main.ts` 0 行**で通りました
  - **セーブは 27b へ回してあります。** 書き出しは `main.ts` の
    `currentSave()` が `buildSave()` へ値を並べる形なので、**キーを 1 本足すと
    `main.ts` に 1 行要ります** —— いま 1450 行で、それは人の判断待ちです。
    **`inventory` のキーに 8 要素を継ぎ足して済ませないこと** ——
    `wear` を別キーにしたのと同じ理由（`rules/inventory-screen.md` の末尾）
- **27b. 革の防具 4 部位** —— **ID 4 個**（革の帽子・上着・ズボン・靴）。**革（132）だけ。
  鉄・金・ダイヤは取らない**（12 個の番号は後回し）。着る経路と `inventoryui.ts` の枠 4 つ

**元の見積もり「番号 12〜16 個」は階層を 4 つ全部取ったときの値**で、
**革だけなら 4 個**です。2026-09-04 に見送った理由はこれで消えました。

## ⚠ `main.ts` の行数がもう効いています

**いま 1449 行**（`wc -l`。テストの数え方では 1450。上限 1500・**止まる目安 1450 に届きました**）。
**19b は予告どおり +1 行**（`updateVitals()` の `onLadder:` 1 つ。2026-09-06 の C の周で実測）。

**次に 1 行でも配線が要る周は、その前に人の判断が要ります**（`AUTODEV.md` の停止条件 2）。
**20・21・22・27a は予告どおり ±0 行で通りました**（どれも 2026-09-07 の C の周で実測。
**22 は 2 行を `vitals.canEat` → `vitals.canEatFood(...)` に書き換えただけ**、
**27a は `main.ts` を 1 文字も開いていません**）。
**23 も ±0 行で通る見込みです**（上の ⚠）—— 判断を `player.ts` へ置き、`main.ts` は
渡す値の名前を書き換えるだけ。**残りも取るときに数えること。**

2026-09-05 に `test/ui.test.ts` を読んで分かったこと: あの一覧は **`main.ts` に残って
いるべき呼び出しを 40 件、文字列で見張っています**（`decideUse(` `tryBucket(` `fitEye(` …）。
だから**大きい塊はどれも動かせません** —— 動かすなら見張りの側を新しいファイルへ
付け替える必要があり、**それは人が決めることです**（`HANDOFF.md` の「人がやること」）。

## 見送ったもの

**理由は `docs/autodev-log.md` に 1 行ずつ書いてあります。** 同じものを毎周
検討し直さないために、ここには名前だけ置きます:
水流・溶岩流 / 経験値とエンチャント / レッドストーン / 醸造とポーション /
ネザークォーツ / 天候（雨・雪）/ 村と村人 / TNT と爆発 /
**釣り竿と魚** / **ドアと看板** / **カボチャとジャック・オ・ランタン** / **ツタ** /
**苔石** / **スライム** / **染料と 16 色の羊毛**。

**防具は見送りから外しました**（上の「防具を割った結果」）。
**TNT と爆発**は 3 周続けて見送りです —— 「球状にまとめて消す」道が 1 行も無く、
メッシュ化 6ms の予算に効くので、取るなら**それだけで 1 周**になります。
