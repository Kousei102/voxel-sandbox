# 自動実装ループ（AUTODEV）の手順書

**本家 Minecraft にあってここに無いものを、1 個ずつ足していくループです。**
クリア導線のループ（`LOOP.md` / `loop/endgame`）とは別物で、**作業は `loop/devgame`**。

このファイルは**手順と禁じ手**だけです。恒久的な決まりごとは `CLAUDE.md` と
`.claude/rules/`（触ったファイルに応じて自動で読まれます）、手順書は `.claude/skills/`。
**`LOOP.md` の「3. 完了条件」「6. 禁じ手」「7. ブランチ」もそのまま掛かります**
（`master` へ push しない・ID を振り直さない・テストの判定をゆるめない）。

---

## 1. いまはどの周か — 先に決める

**1 周で全部をやりません。** 周は 3 種類あり、**どれをやるかはファイルの有無だけで決まります**
（人に聞かないこと）。上から順に当てはめて、**最初に当たったものだけ**をやります。

| 見るもの（上から順に） | 周 | 生む物 | `src/**` |
| --- | --- | --- | --- |
| `AUTODEV-QUEUE.md` が無い / 未着手 `- [ ]` が 3 件未満 | **A 棚卸し** — 数えて並べる | `AUTODEV-QUEUE.md`（150 行以内） | **触らない** |
| `AUTODEV-SPEC.md` が無い / `状態: 済` | **B 仕様** — 先頭の 1 件を仕様書に | `AUTODEV-SPEC.md`（120 行以内） | **触らない** |
| それ以外（未着手の仕様がある） | **C 実装** — 実装 → チェック → 記録 | コミット 1 つ / ログ / `HANDOFF.md` | 触る |

**1 タスク = B + C の 2 周。** A はキューが尽きたときだけ（5〜8 タスクに 1 回）。
**どの周も、終わりに `HANDOFF.md` へ「次はどの周か」を 1 行残すこと。**

## 2. 予算 — どの周でも先に見る

**先に尽きるのは時間ではなく枠です。** `npm test` が出す数字を読むこと（推測しないこと）。

| 枠 | 上限 | いまの値の出どころ | 尽きたら |
| --- | --- | --- | --- |
| 1..63 の帯（**凍結**） | 63 まで | `npm test` の「1..63 の空き」 | **111 以降へ移る**（止まらなくてよい） |
| **111..255 の共有帯** | 255（ボクセルが `Uint8Array`） | `npm test` の「111..255 の空き」 | **人を呼ぶ**（`Uint16Array` の話になる） |
| `main.ts` | **1500 行** | `npm test` の「main.ts N 行」 | 判断を別ファイルへ出す |
| 毎周読む文書 | `CLAUDE.md` / `LOOP.md` / `AUTODEV.md` 200・`TASKS.md` 150・`REVIEW.md` 200 | `npm test` の「毎周読まれる文書の大きさ」 | `docs/` へ退避（**上限を上げない**） |

**ID は使ったら戻せません**（セーブが化けます）。**111 以降はブロックとアイテムで 1 本の
番号列**なので**片側だけ見て取らないこと。** 唯一の根拠は `ROADMAP.md` の予約表です。

---

## A. 棚卸しの周 — 数えて、並べる

### A-1. 実装済みを「数える」

**実装済みリストをファイルに持たないこと。** 腐って、同じものを 2 回足します。
**この周でコードから作り直す**のが決まりです。**どのファイルを数えるかの表は
`docs/autodev-counting.md`**（8 ジャンル。A の周でだけ開けばよい）。

外を見るのは **「本家に何があるか」を思い出すため**だけです（Minecraft Wiki など）。
**実装済みかどうかはコードが唯一の根拠**で、README や `ROADMAP.md` は古いことがあります。

- **基準を 1 つ選んで、未実装を 5〜10 個取る。** 例: 本家のバージョン順 /
  ディメンション順（オーバーワールド → ネザー → エンド）/ ジャンル 1 つの深掘り
- **基準は `AUTODEV-QUEUE.md` の先頭に 1 行で書き、次の A の周でも変えないこと。**
  変えると、取りやすいものだけを拾って穴が残ります
- **見送ったものは理由を 1 行**（`docs/autodev-log.md`）。同じものを毎周検討しないため

### A-2. 並べる

ジャンルの順（ブロック → バイオーム → 構造物 → モブ → アイテム → 効果）**より先に**、
このプロジェクト固有の 2 つが来ます。逆にすると、確かめられないものと枠の消費が先に来ます。

1. **確かめられるものが先。** 判断だけで足せるもの（レシピ・ドロップ・効果・湧きの条件）＞
   描画や音が要るもの（新しいモブの見た目・パーティクル・シェーダ）。
   後者も**撮って自分で見られます**（C-3）が、手触りの良し悪しは人にしか決められません
2. **枠を食わないものが先。** 既存 ID の組み合わせ（レシピ・ドロップ・湧き）で足せるもの ＞
   番号を 1 個使うもの。**位置ごとの状態で済むなら ID は要りません**（`furnaces.ts` の形）
3. ジャンルの順（上）
4. **依存が解決済みのものだけ。** 未解決なら「先に要るもの」を書いて後ろへ回す

### A-3. キューに書く

`AUTODEV-QUEUE.md`（無ければ作る。150 行以内）に、先頭へ基準を 1 行、あとは 1 件 1 行で:

```
基準: ディメンション順（オーバーワールド → ネザー → エンド）
- [ ] 12. 小麦と種（作物）— 育つブロックの仕組みが要る / ID: 111 から 3 個 / 依存: なし
```

**完了条件**: 未着手が 5 件以上 / 先頭に基準が 1 行 / **`src/**` と `test/**` に差分が無い。**

## B. 仕様の周 — サブエージェントに渡す 1 枚

**`AUTODEV-QUEUE.md` の先頭の 1 件だけ**を `AUTODEV-SPEC.md` にします。**ここもまだ
`src/**` を触りません。**

**取る前に、その 1 件だけコードで数え直すこと。** キューは数周前の A が作ったもので、
もう入っているかもしれません（入っていたら消して次の行へ）。**全部を数え直すのは A の仕事**です。

仕様書は次の 7 つを必ず書くこと。**1 つでも欠けたら書き直す**（欠けたぶんは、そのまま
サブエージェントの勝手な判断になって返ってきます）。

1. **何を足すか 1 行と、完了の判定**（「`npm test` に〜という項目が増えて緑」まで書く）
2. **触るファイルと、触らないファイル**（`main.ts` は配線だけ。判断を書かせない）
3. **使う ID**（`ROADMAP.md` の予約表から。無ければ**人を呼ぶ**。勝手に取らせない）
4. **判断をどのファイルに置くか**（`CLAUDE.md` の対の表。新しく確かめられないものを
   足すなら `unverifiable-pair` スキルを名指しで渡す）
5. **書くテスト**（値を出力してから判定する形。`.claude/rules/testing.md`）
6. **このタスク固有の禁じ手**（例:「既存のドロップ表を書き換えない」）
7. **終了条件**: `npm run typecheck` と `npm test` が緑 / コミット 1 つ /
   手触りの数値を置いたら `TUNING.md` に 1 行

- 先頭に **`状態: 未着手` と `差し戻し: 0 回`** の 2 行を置くこと。**周をまたぐと回数が
  消えるので、ここに無いと「3 回目で止まる」が効きません**
- 使えるスキルは名前で渡すこと（`add-block` / `add-stateful-block` / `unverifiable-pair`）
- **120 行に収まらない仕様は、1 周で閉じない信号です。** その場で 2 件に割って
  `AUTODEV-QUEUE.md` に書き戻し、**前半だけ**を仕様書にすること

**完了条件**: 7 項目が揃っている / 120 行以内 / **`src/**` に差分が無い。**

## C. 実装の周 — 実装 → チェック → 記録

**1 タスク = 1 コミット。3 つとも同じ周でやること。** 分けると、未コミットの差分と
古い `HANDOFF.md` を持ったまま周が終わります。

### C-1. 実装（サブエージェント / sonnet・medium）

- **背景（async）で起動しないこと** —— 結果が**同じ番の中で返る**呼び方をすること。背景に
  投げると親は番を閉じ、**起き直せなければ実装ごと捨てて次の発火がやり直します**
  （2026-09-01 に実際に起きかけた）。**返らないなら自分で実装すること**
- 仕様書は**全文を渡す**（要約しない。要約すると B の（2）と（6）が真っ先に落ちます）
- **「`src/**` と `test/**` は `Read` / `Edit` で開くこと」を毎回書く。**
  `cat` や `sed` で読むと `.claude/rules/` が読み込まれません（実測）
- **1 エージェントに 1 タスク。並行して走らせないこと** —— 2 つ渡すとコミットが混ざり、
  同じファイル（`blocks.ts` / `items.ts` / `crafting.ts`）を取り合います

### C-2. チェック（**別の**サブエージェント）

**書いた本人に確認させないこと。ここも背景で起動しないこと**（C-1 と同じ理由。**別を立てられ
ないなら親が `git diff` を読んで自分で点検する** —— 飛ばすより確実）。見るのは次の 5 つ:

- [ ] `npm run typecheck` と `npm test` を**自分で走らせて**緑（報告を信じない）
- [ ] **判断が漏れていない**（`main.ts` / `*render.ts` / `ui.ts` / `inventoryui.ts` に数値や条件が
      書かれていないか。`test/ui.test.ts` の見張りに引っかからなくても、目で見ること）
- [ ] **ID が予約表どおり**（空きの数が予定どおり減ったか。111 以降で番号が二重でないか）
- [ ] **テストが「値を出してから判定」になっている**。緑にするために既存の判定を
      ゆるめていないか（`git diff test/` を読むこと）
- [ ] 行数の上限に当たっていない（`main.ts` と毎周読む文書）

**差し戻しはこの周の中で最大 2 回**（そのつど `AUTODEV-SPEC.md` の「差し戻し」を +1）。
3 回目は止めて人を呼び、`docs/autodev-log.md` に「何を・なぜ 3 回落としたか」を残すこと。

### C-3. 撮って、自分の目で見る（見た目に出るものを触った周だけ）

**クラウドのサンドボックスでは本物のブラウザが動きます**（`docs/browser-shots/README.md`）。
`npm run build` → `(npx --no-install http-server dist -p 8080 --silent &)` →
**`node tools/browsershot.mjs`**（`docs/browser-shots/` へ 5 枚・30 秒。上書きしてよい）。
ブラウザが無い環境なら **`npm run shot -- mobs terrain`** だけでよく（GPU 不要・1 場面
1〜2 秒）、**そこで Playwright を入れようとしないこと。**
**撮ったら `Read` で開いて見ること** —— 撮っただけでは 1 つも確かめたことになりません。
**直せる不具合が写っていたらこの周で直す**（写ったのに放って閉じないこと）。

### C-4. 記録 — 次の周が同じ道を通らないために

- **`docs/autodev-log.md` に 1 タスク 1 節**（**毎周は読まない置き場**）:
  取ったもの / 見送ったもの（理由 1 行）/ 差し戻し（何を・なぜ・どう直したか）/ 枠
- `AUTODEV-QUEUE.md` の済んだ行を消し、**`AUTODEV-SPEC.md` の `状態:` を `済` にする**
  （次の周が B に入れなくなります）
- **使った ID を `ROADMAP.md` の予約表に「実装済み」と書く**
- **`HANDOFF.md` を丸ごと書き直す。** 見た目に出るものは 2〜3 行（**撮った絵で何を
  確かめたか**と、それでも人に見てほしいところ）
- 決まりごと（`.claude/rules/`）は **`npm run rules:install` で据える**（`Edit` で直接
  触ると確認待ちで止まります。手順は `LOOP.md` の 4.5。スキルは `RULES-INBOX.md` へ）

**完了条件**: コミット 1 つ / `npm test` 緑 / キューの行を消した / 仕様書が `済` /
**見た目に出るものを触ったなら撮って見た** / `HANDOFF.md` を書き直した。

---

## 止まって人を呼ぶ条件

**迷ったら止まること。** 止まるのは失敗ではありません。`HANDOFF.md` に理由を書いて終えます。

1. **`ROADMAP.md` の予約表に無い ID を使いたくなった**
2. **111..255 の空きが 10 個を切った**（`Uint16Array` の話。**1..63 が尽きただけなら止まらず 111 以降へ**）
3. **`main.ts` が 1450 行を超えた**（判断を出しても収まらないとき）
4. **同じタスクで差し戻しが 3 回目**
5. **`SaveData` の形を変えたくなった**（省略可のキーを足すのは可。`version` は 1 のまま）
6. **2 周続けてテストが赤い**

## 禁じ手

- **1 周で 2 種類の周をやること**（A でキューを作ってそのまま実装を始める、が典型）。
  **B と C を畳むと、ユーザーが安く口を出せる場所が消えます**（次に見るのが差分になる）
- **本家にあるからという理由だけで、テクスチャ・パーティクル・新しいシェーダが要るものを
  取ること**（外部アセットを使わない決まりに当たります。`CLAUDE.md`）
- **見た目に出るものを触ったのに、撮らずに周を閉じること**（C-3）
- **実装済みリストをファイルに持って、それを信じること**（A-1 のとおり数え直す）
- **数えずに `AUTODEV-QUEUE.md` を継ぎ足すこと**（足すのは A の周だけ）
- **サブエージェントを背景（async）で起動する／並行に走らせる／1 周で 2 つ以上取ること**
- **サブエージェントの「できました」を、テストを走らせずに信じること**
- **仕様書を要約して渡すこと**
- **`AUTODEV-QUEUE.md` を飛ばして「取りやすいもの」を取ること**（飛ばしたくなったら依存が
  書けていない合図。並びを直してから取る）
