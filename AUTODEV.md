# 自動実装ループ（AUTODEV）の手順書

**本家 Minecraft にあってここに無いものを、1 周 1 個ずつ足していくループです。**
クリア導線のループ（`LOOP.md` / `loop/endgame`）とは別物で、**作業は `loop/devgame`**。

このファイルは**手順と禁じ手**だけです。恒久的な決まりごとは `CLAUDE.md` と
`.claude/rules/`（触ったファイルに応じて自動で読まれます）、手順書は `.claude/skills/`。
**`LOOP.md` の「3. 完了条件」「6. 禁じ手」「7. ブランチ」はこのループにもそのまま掛かります**
（`master` へ push しない・ID を振り直さない・テストの判定をゆるめない）。

**このファイルも 200 行以内**（`test/docs.test.ts`）。増えたら `docs/` へ退避すること。

---

## 0. 予算を先に見る

**このプロジェクトで先に尽きるのは時間ではなく枠です。** 毎周、着手の前に `npm test` が
出す 3 つの数字を読むこと（推測しないこと）。

| 枠 | 上限 | いまの値の出どころ | 尽きたら |
| --- | --- | --- | --- |
| 1..63 の帯（**凍結**） | 63 まで | `npm test` の「1..63 の空き」 | **111 以降へ移る**（止まらなくてよい） |
| **111..255 の共有帯** | 255（ボクセルが `Uint8Array`） | `npm test` の「111..255 の空き」 | **人を呼ぶ**（`Uint16Array` の話になる） |
| `main.ts` | **1500 行** | `npm test` の「main.ts N 行」 | 判断を別ファイルへ出す |
| 毎周読む文書 | `CLAUDE.md` / `LOOP.md` / `AUTODEV.md` 200・`TASKS.md` 150・`REVIEW.md` 200 | `npm test` の「毎周読まれる文書の大きさ」 | `docs/` へ退避（**上限を上げない**） |

**ID は使ったら戻せません**（`localStorage` にブロック ID がそのまま入っているため）。
**111 以降はブロックとアイテムで 1 本の番号列**なので、**片側だけ見て空き番号を取らないこと。**
何に使うかは `ROADMAP.md` の予約表が唯一の根拠で、**そこに無い番号は使わないこと。**

## 1. リサーチ — 実装済みを「数える」

**実装済みリストをファイルに持たないこと。** 腐って、同じものを 2 回足します。
**毎周コードから作る**のが決まりです。出どころはこれだけ:

| ジャンル | 数える場所 |
| --- | --- |
| ブロック | `blocks.ts` の `BLOCKS`（`npm test` が種類と空きを出す） |
| アイテム・道具・食べ物 | `items.ts` の `ITEMS` / `FOODS` / `MAX_ITEM_ID` |
| レシピ・精錬 | `crafting.ts` の `RECIPES` / `smelting.ts` の表 |
| モブ | `mobs.ts` の `MobKind` と `MOBS`（ボスは `BOSSES`） |
| バイオーム・地形 | `biomes.ts` の表 / `worldgen.ts` / `nethergen.ts` / `endgen.ts` |
| 構造物 | `structures.ts` の `SiteRule` / `fortress.ts` / `stronghold.ts` |
| 効果・状態 | `vitals.ts`（体力・空腹・毒・炎上・溺れ） |
| 次元 | `dimensions.ts` の `DIMENSIONS` |

外を見るのは **「本家に何があるか」を思い出すため**だけです（Minecraft Wiki など）。
**何を実装済みと見なすかはコードが唯一の根拠**で、README や `ROADMAP.md` の記述は
古いことがあります。

- **基準を 1 つ選んで、未実装を 5〜10 個取る。** 例: 本家のバージョン順 /
  ディメンション順（オーバーワールド → ネザー → エンド）/ ジャンル 1 つの深掘り
- **基準を毎周変えないこと。** 変えると、取りやすいものだけを拾って穴が残ります
- **見送ったものは理由を 1 行**（`docs/autodev-log.md`）。同じものを毎周検討しないため

## 2. 優先順位 — この順に並べる

ジャンルの順（ブロック → バイオーム → 構造物 → モブ → アイテム → 効果）**より先に**、
このプロジェクト固有の 2 つが来ます。逆にすると、確かめられないものと枠の消費が先に来ます。

1. **確かめられるものが先。** 判断だけで足せるもの（レシピ・ドロップ・効果・湧きの条件）＞
   描画や音が要るもの（新しいモブの見た目・パーティクル・シェーダ）。
   後者は**ユーザーがブラウザで見るまで完了を確かめられません**（`CLAUDE.md`）
2. **枠を食わないものが先。** 既存 ID の組み合わせ（レシピ・ドロップ・湧き）で足せるもの ＞
   番号を 1 個使うもの。**位置ごとの状態で済むなら ID は要りません**（`furnaces.ts` の形）
3. ジャンルの順（上）
4. **依存が解決済みのものだけ。** 未解決なら「先に要るもの」を書いて後ろへ回す

並べた結果は **`AUTODEV-QUEUE.md`**（無ければ 1 周目が作る。150 行以内）に、1 件 1 行で:

```
- [ ] 12. 小麦と種（作物）— 育つブロックの仕組みが要る / ID: 111 から 3 個 / 依存: なし
```

## 3. 実装計画 — サブエージェントに渡す 1 枚

**1 周 = 1 タスク = 1 コミット。** 仕様書は次の 7 つを必ず書くこと。
**1 つでも欠けたら書き直してから渡す**（欠けたぶんは、そのままサブエージェントの
勝手な判断になって返ってきます）。

1. **何を足すか 1 行と、完了の判定**（「`npm test` に〜という項目が増えて緑」まで書く）
2. **触るファイルと、触らないファイル**（`main.ts` は配線だけ。判断を書かせない）
3. **使う ID**（`ROADMAP.md` の予約表から。無ければ**人を呼ぶ**。勝手に取らせない）
4. **判断をどのファイルに置くか**（`CLAUDE.md` の対の表。新しく確かめられないものを
   足すなら `unverifiable-pair` スキルを名指しで渡す）
5. **書くテスト**（値を出力してから判定する形。`.claude/rules/testing.md`）
6. **このタスク固有の禁じ手**（例:「既存のドロップ表を書き換えない」）
7. **終了条件**: `npm run typecheck` と `npm test` が緑 / コミット 1 つ /
   手触りの数値を置いたら `TUNING.md` に 1 行

- 使えるスキルは名前で渡すこと（`add-block` / `add-stateful-block` / `unverifiable-pair`）
- **1 周で閉じない**と分かったら、その場で分割して `AUTODEV-QUEUE.md` に書き戻す

## 4. 実装 — サブエージェント（sonnet / medium）

- 仕様書は**全文を渡す**（要約しない。要約すると 3.（2）と（6）が真っ先に落ちます）
- **「`src/**` と `test/**` は `Read` / `Edit` で開くこと」を毎回書く。**
  `cat` や `sed` で読むと `.claude/rules/` が読み込まれません（実測）
- **1 エージェントに 1 タスク。** 2 つ渡すとコミットが混ざります
- **並行して走らせないこと。** 同じファイル（`blocks.ts` / `items.ts` / `crafting.ts`）を
  取り合い、あとから直すほうが高くつきます

## 5. チェック — 別のサブエージェントに突き合わせさせる

**書いた本人に確認させないこと。** 見るのは仕様書との一致と、次の 5 つ:

- [ ] `npm run typecheck` と `npm test` を**自分で走らせて**緑（報告を信じない）
- [ ] **判断が漏れていない**（`main.ts` / `*render.ts` / `ui.ts` / `inventoryui.ts` に
      数値や条件が書かれていないか。`test/ui.test.ts` の見張りに引っかからなくても、
      目で見ること）
- [ ] **ID が予約表どおり**（立方体が 64 以降に居ないか。空きの数が予定どおり減ったか）
- [ ] **テストが「値を出してから判定」になっている**。緑にするために既存の判定を
      ゆるめていないか（`git diff test/` を読むこと）
- [ ] 行数の上限に当たっていない（`main.ts` と毎周読む文書）

**差し戻しは最大 2 回。** 3 回目は止めて人を呼び、`docs/autodev-log.md` に
「何を・なぜ 3 回落としたか」を残すこと（仕様書の書き方の問題であることが多いです）。

## 6. 記録 — 次の周が同じ道を通らないために

- **`docs/autodev-log.md` に 1 周 1 節**（無ければ作る。**毎周は読まない置き場**）:
  取ったもの / 見送ったもの（理由 1 行）/ 差し戻し（何を・なぜ・どう直したか）
- `AUTODEV-QUEUE.md` の済んだ行を消す
- **使った ID を `ROADMAP.md` の予約表に「実装済み」と書く**
- **`HANDOFF.md` を丸ごと書き直す。** 見た目に出るものは 2〜3 行（次にユーザーが
  ブラウザで見るときの見どころ）
- 決まりごと（`.claude/rules/`）を直したくなったら **`RULES-INBOX.md` へ**
  （無人の周は直接編集すると確認待ちで止まります。`LOOP.md` の 4.5）

## 7. 止まって人を呼ぶ条件

**迷ったら止まること。** 止まるのは失敗ではありません。`HANDOFF.md` に理由を書いて終えます。

1. **`ROADMAP.md` の予約表に無い ID を使いたくなった**
2. **111..255 の空きが 10 個を切った**（次は `Uint16Array` の話で、人と決めることです。
   **1..63 が尽きただけなら止まりません** —— 111 以降へ移って続けること）
3. **`main.ts` が 1450 行を超えた**（判断を出しても収まらないとき）
4. **同じタスクで差し戻しが 3 回目**
5. **`SaveData` の形を変えたくなった**（省略可のキーを足すのは可。`version` は 1 のまま）
6. **2 周続けてテストが赤い**

## 8. 禁じ手

- **本家にあるからという理由だけで、テクスチャ・パーティクル・新しいシェーダが要るものを
  取ること。** この環境では確かめられません（`CLAUDE.md`）。取るなら判断の側だけを先に入れ、
  見た目はユーザーに見てもらう周に回すこと
- **実装済みリストをファイルに持って、それを信じること**（1. のとおり毎周数え直す）
- **サブエージェントを並行に走らせること／1 周で 2 つ以上のタスクを取ること**
- **サブエージェントの「できました」を、テストを走らせずに信じること**
- **仕様書を要約して渡すこと**
- **`AUTODEV-QUEUE.md` を飛ばして「取りやすいもの」を取ること**
  （飛ばしたくなったら依存が書けていないので、並びを直してから取る）
