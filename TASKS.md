# タスクキュー

**上から順に取ること。** 手順は `LOOP.md`、進み具合は `npm test` の「進行（クリア導線）」。

記号:

- `[ ]` 未着手 / `[x]` 済み / `[!]` 止まっている（理由を書くこと）
- **見** = 見た目に触れる
- **要** = 依存。そのタスクが済むまで取らない

**番号順ではなく、上から順に取ること。** 番号は振り直さない決まりなので、並びのほうが優先です。

**進め方は「薄い縦切り」。** それぞれ**クリアできる最小**で止めて、肉付けは
導線が端から端まで通ってからにすること。

**手触りの数値で止まらないこと。** 本家の値を暫定で置き、`TUNING.md` に記録して進みます
（`LOOP.md` の「3.5」）。

## このファイルは短く保つこと

**上限 150 行**（`test/docs.test.ts` が見ています）。ここは**キューであって記録ではありません。**

- タスクを `[x]` にしたら、**その節の本文を丸ごと `docs/tasks-done.md` へ移し**、
  ここには下の一覧の 1 行だけを残すこと
- 上限に当たったら、上限を上げずに退避すること（`main.ts` の 1500 行と同じ扱い）

---

## 未着手

**いま取れるタスクはありません。** クリア導線は 13 / 13 で通っています。
新しいタスクはユーザーが足します。**無人の周は下の「取るタスクが無くなったら」に従うこと。**

## 済み（本文は `docs/tasks-done.md`）

**Phase 1 — 土台**

- [x] 1-1b. 溶岩の直し（フォグと狙う判定）**見**
- [x] 1-1. 溶岩ブロック **見**
- [x] 1-2. 溶岩のダメージと炎上 **見**
- [x] 1-2b-1. バケツ **見**
- [x] 1-2b-2. 黒曜石 **見**
- [x] 1-3. 砂利 → 火打石 → 火打石と打ち金 **見**
- [x] 1-4. `portals.ts` — 枠の検出と点火の可否 **見**
- [x] 1-5. `World` の生成器を注入できるようにする
- [x] 1-7. `structures.ts` — 構造物の器
- [x] 1-6. `dimensions.ts` — 次元の器とセーブ **見**
- [x] 1-8. `projectiles.ts` + `projectilerender.ts` **見**

**Phase 2 — 導線を通す**

- [x] 2-1. ネザーのブロック 3 つ **見**
- [x] 2-2. ネザーへ行って帰る **見**
- [x] 2-3. ネザー要塞（通路）**見**
- [x] 2-3c. main.ts の余白を作る（1494 → 1349 行 / 上限 1500）
- [x] 2-3b. 次元ごとの空 **見**
- [x] 2-4a. ブレイズ本体（飛ぶモブ・要塞に湧く・ブレイズロッド）**見**
- [x] 2-4b. ブレイズの火球（当たり判定とダメージ）**見**
- [x] 2-4c. 死んだら寝た次元へ戻る **見**
- [x] 2-5. エンダーマン（テレポート + エンダーパール）**見**
- [x] 2-6. エンダーアイ（ブレイズパウダー + エンダーパール）**見**
- [x] 2-7. 投げたエンダーアイが要塞の方へ飛ぶ **見**
- [x] 2-8. 要塞（石レンガ 53 + エンドポータル部屋 + 枠 51）**見**
- [x] 2-9. エンドポータルの起動（枠 12 個にエンダーアイ）**見**
- [x] 2-10. エンド次元（エンドストーン 50 の島）**見**
- [x] 2-11a. 黒曜石の柱（エンドの島に 10 本）**見**
- [x] 2-11b. エンドクリスタル（柱の上に載る・壊せる）**見**
- [x] 2-12. 弓と矢（クリスタルを壊す手段）**見**
- [x] 2-12b. `main.ts` の余白を作る（1499 → 1445 行 / 上限 1500）
- [x] 2-13a. エンダードラゴン本体（ボス・輪を回る飛行・クリスタルで回復）**見**
- [x] 2-13b-1. 出口ポータル（倒した印）**見**
- [x] 2-13b-2. 体力バーとクリア画面 **見**

**`main.ts` の余白は 15 行です**（1485 / 上限 1500）。次に何かを足す周は、
**まず減量から取ること**（左クリックの振り分けを `use.ts` の `decideAttack()` へ。
見取り図は `docs/tasks-done.md` の 2-13b-2 の節）。**上限は上げないこと。**

---

## ループは取らないもの（人がやること）

**この 2 つはループの仕事ではありません。**

- [ ] **通しのテストプレイ** —— ユーザーがブラウザで端から端まで遊び、
      見つけた不具合を `REVIEW.md` に足します（`REVIEW.md` の書き方を読むこと）
- [ ] **`TUNING.md` の一括調整** —— 手触りの数値を実際に遊んで決める

## 取るタスクが無くなったら

取るタスクが無いとき、無人の周がここへ来たら、以下に従ってください。

1. **何も実装しないこと。** `TASKS.md` に無い作業を思いついても、それは今回の仕事ではありません
   （見た目の作り込み・手触りの調整・見つけた気になる点の直しも含む）
2. **`REVIEW.md` に `[ ]` の不具合があれば、それは取ってよい**（上から 1 つ）。
   ユーザーが通しで遊んで見つけたものなので、直したら `[x]` にして本文はそのまま残すこと
3. どちらも無ければ `HANDOFF.md` に「取るタスクが無い。13 / 13 達成済み。
   通しのテストプレイと `TUNING.md` の一括調整はユーザーの仕事」と書く。
   すでに書かれていた場合は何もしない
4. **コミットは作らない**（何も変えていない場合）。そのまま終える

**クラウドの定期実行（3 時間ごと）はこの状態を知らないので、止めるまで発火し続けます。**
止めるのはユーザーの仕事です。
