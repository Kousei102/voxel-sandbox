# タスクキュー

**上から順に取ること。** 手順は `LOOP.md`、進み具合は `npm test` の「進行（クリア導線）」。

記号:

- `[ ]` 未着手 / `[x]` 済み / `[!]` 止まっている（理由を書くこと）
- **見** = 見た目に触れる。終わったら `REVIEW.md` に確認項目を書く
- **要** = 依存。そのタスクが済むまで取らない

**進め方は「薄い縦切り」。** それぞれ**クリアできる最小**で止めて、肉付けは
導線が端から端まで通ってからにすること。ネザーを本家並みに作り込んでから次へ、
という順にすると、クリアできるかどうかが最後まで分かりません。

---

## Phase 1 — 土台

### [x] 1-1b. 溶岩の直し（フォグと狙う判定）**見** — ブラウザ確認待ち（`REVIEW.md`）

1-1 の確認で出た 2 点。**`liquid: true` と `isLiquid()` / `liquidFog()` に寄せた**ので、
液体を足すときに 3 か所（狙う・置く・フォグ）を直し忘れなくなった。
決まりごとは `CLAUDE.md` の「立方体でないブロックの形」の液体の項。

### [x] 1-1. 溶岩ブロック **見** — 確認済み（7 項目中 5 つ OK、2 つは 1-1b で直した）

- ID 42 (`LAVA`)。水と同じ半透明の液体で、違うのは `emission: LAVA_LIGHT`(15) だけ
- `worldgen.ts` の `LAVA_LEVEL`(10) 以下の洞窟に溜まる。**海面の水とまったく同じ形**
  （掘り抜いた空間を高さで液体にする 1 行）
- ダメージはまだ無い（1-2）。`generateChunk` は 1.08〜1.16ms で退行なし

### [x] 1-2. 溶岩のダメージと炎上 **見** — ブラウザ確認待ち（`REVIEW.md`）

**黒曜石は 1-2b に切り出しました**（下）。1 コミットに収まらないのと、
**入手導線（バケツ）にユーザーの判断が要る**ため。

- 判断は `vitals.ts` の `updateFire`（浸かっているあいだ + 出てからの炎上）。
  数値は Minecraft のまま。`main.ts` は `inLiquid` / `inLava` を渡すだけ
- **`isHotLiquid()` を足した**ので、焼けるかどうかも表 1 本。モブも同じものを見る
- ついでに `isLiquid()` を残り 4 か所（`player.ts` / `mobs.ts` / `drops.ts` / `beds.ts`）へ
  広げた。**溶岩にモブが湧く・溶岩の上にベッドが置ける・「溶岩」というアイテムが
  1 個増えている**の 3 つが直った
- 熱い場所（`worldgen` / `mesher` / `world.update`）は触っていない。
  モブ 40 体の中央値 0.048ms（上限 0.5ms）

### [x] 1-2b-1. バケツ **見** — ブラウザ確認待ち（`REVIEW.md`）

アイテム 84/85/86。鉄 3 個の V 字。**積めるのは 1 個まで。**
汲む／流すの判断は `items.ts` の `bucketUse()`（純粋）で、対応の表は `FILLED_BUCKETS` 1 か所。
バケツを持っているときだけ光線が液体に当たる（`raycastVoxels` の `hitLiquid`）。

### [x] 1-2b-2. 黒曜石 **見** — ブラウザ確認待ち（`REVIEW.md`）

ID 43 (`OBSIDIAN`)。硬さ 50 / `minTier: TIER_DIAMOND`（ダイヤで 9.4 秒）。
規則は `blocks.ts` の `quenched()`（純粋。**熱い液体 + 熱くない液体**で決めるので
液体を足しても 1 行のまま）、どのマスに効くかは新しい `liquids.ts` の `quenchAround()`。
`main.ts` は流した直後に 1 行呼ぶだけ。**進行テストが「達成 2 / 13」になった**
（probe も本物に差し替え済み）。

### [ ] 1-3. 砂利 → 火打石 → 火打石と打ち金

- ID 44 (`GRAVEL`)。アイテムは 84 から
- 砂利のドロップは確率つき（`items.ts` の既存の仕組み）。**乱数のテストは 1 本を回し続けること**
  （`seeded()` を 1 件ずつ作ると線形合同法の 1 個目に引きずられて偏ります）
- レシピ「火打石と打ち金」= 鉄インゴット + 火打石

### [ ] 1-4. `portals.ts` — 枠の検出と点火の可否 **要 1-2b**

- **判断だけのファイル。** `World` を丸ごと受け取らず、`getVoxel` / `setVoxel` の
  必要な入口だけを受ける（`beds.ts` が手本）
- 黒曜石の枠（内側 2x3）を検出し、**ポータル面のマスを列挙して返す**
- ID 49 (`NETHER_PORTAL`)。非立方体（`isProp`）。向き 2 種は 64 以降
- **点火の可否と枠の形だけ。** 次元をまたぐ移動は 1-6
- テスト: `test/portals.test.ts`。欠けた枠・大きすぎる枠・向きが 2 通りあること

### [ ] 1-5. `World` の生成器を注入できるようにする

- `constructor(scene, seed, edits?)` の中の `new WorldGen(seed)` をやめ、
  **生成器を外から渡す**形にする。既存の呼び出し 1 か所（`main.ts` の `startWorld`）を直す
- **このタスクでは次元を足さない。** 差し替えられる形にするだけ
- テスト: 別の生成器を渡したら、そのブロックが出ること

### [ ] 1-6. `dimensions.ts` — 次元の切り替えとセーブ **要 1-4, 1-5** **見**

- **ここが Phase 1 で一番大きい。閉じないと思ったら迷わず分割すること**
- `SaveData` に**省略可の新しいキー**（例 `dims`）。オーバーワールドは今の位置のまま
  （`edits` / `furnaces` / `chests` / `drops` を動かさない = 既存のセーブが読める）。
  **`version` は 1 のまま**。`test/storage.test.ts` の「知らないキーがあっても読める」が
  この形を守っている
- **`main.ts` に次元の分岐を散らさない**（`test/ui.test.ts` が上限 2 で見張っています）
- モブは保存しない（今のまま）。**次元を移ったら消すこと**
- テスト: 往復してもオーバーワールドの改変が消えないこと・古いセーブが読めること

### [ ] 1-7. `structures.ts` — 構造物の器 **要 1-5**

- 「このチャンクに重なりうる構造物」を**決定的に**列挙してスタンプする器。
  `worldgen.ts` の `applyTrees()`（隣接 8 列を毎回スタンプし直す）の考え方の一般化
- **純粋。three も DOM も出てこない。** ループが一番得意な仕事
- **費用に注意。** `heightAt()` は 1 列 256 回呼ばれます。`npm run bench` を数回まわして中央値で見ること
- テスト: 同じシードで何度生成しても同じ・列をまたいで欠けない・生成が遅くなっていない

### [ ] 1-8. `projectiles.ts` + `projectilerender.ts` **見**

- **`drops.ts` / `droprender.ts` とまったく同じ対の形**にすること（判断は全部
  `projectiles.ts`、three は render 側だけ）。火球・矢・投げたエンダーアイ・
  ブレスが全部これに乗る
- 重力・終端速度・水の扱いは自前で持つ（`drops.ts` と同じ）。
  **未生成の列では動かさない**（`world.hasColumn(columnOf(x), columnOf(z))`）
- `update()` は `world.update()` の**外**で回すこと（`test/world.test.ts` の p99 に混ぜない）
- 形は `mobmesh.ts` の `buildBoxMesh()` を使い回す（巡回順の発行点を増やさない）
- **`projectilerender.ts` に判断が漏れていないかの見張りを同時に足すこと**

## Phase 2 — 導線を通す

**それぞれ「クリアできる最小」で止めること。** 着手する前に、その周でタスクを
細かく割り直してください（下は見取り図です）。

- [ ] 2-1. ネザーの地形（ネザーラック 45・ソウルサンド 46・グロウストーン 47・溶岩海・天井の岩盤）**要 1-6**
- [ ] 2-2. ネザーポータルで往復（座標の対応・入ってからの間・戻り先）**要 1-6** **見**
- [ ] 2-3. ネザー要塞（ネザーレンガ 48 の通路だけ）**要 1-7, 2-1**
- [ ] 2-4. ブレイズ（飛行 + 火球 + ブレイズロッド）**要 1-8, 2-3** **見**
- [ ] 2-5. エンダーマン（テレポート + エンダーパール）**要 1-8** **見**
- [ ] 2-6. エンダーアイ（ブレイズパウダー + エンダーパール）**要 2-4, 2-5**
- [ ] 2-7. 投げたエンダーアイが要塞の方へ飛ぶ **要 1-8, 2-6**
- [ ] 2-8. 要塞（石レンガ 53 + エンドポータル部屋 + 枠 51）**要 1-7**
- [ ] 2-9. エンドポータルの起動（枠 12 個にエンダーアイ）**要 2-8**
- [ ] 2-10. エンド次元（エンドストーン 50 の島）**要 1-6, 2-9**
- [ ] 2-11. 黒曜石の柱 + エンドクリスタル **要 2-10** **見**
- [ ] 2-12. 弓と矢（クリスタルを壊す手段）**要 1-8**
- [ ] 2-13. エンダードラゴン（飛行 AI・体力バー・クリスタルで回復・撃破でクリア画面）**要 2-11, 2-12** **見**

---

## 止まっているもの

（`[!]` にしたタスクと理由をここに書くこと）
