# 仕様: 金・ダイヤの防具 8 部位（キューの 33b・**共有帯 8 個**）

状態: 済
差し戻し: 0 回

**33「防具 12 部位」を割った後半**（前半 33a の鉄 4 部位は 2026-09-16 に済み）。**33a が
`crafting.ts` を `armorRecipes()` 1 本に畳み、`test/items.test.ts` が「材質ごとに 4 部位」の数え方を
覚えたので、この周は材質の列を 2 つ増やすだけです。**

## 1. 何を足すか と 完了の判定

`npm test` に金とダイヤの防具が増えて、少なくとも次が緑:

- 「**着られるのは 16 種**（革 + 鉄 + 金 + ダイヤ）」・「**金もダイヤも頭・胴・脚・足が 1 つずつ**」・「**16 種は 4 部位に 4 つずつ**」
- 「**点数は本家のまま 金 2 / 5 / 3 / 1 = 11・ダイヤ 3 / 8 / 6 / 3 = 20**」（内訳と合計の両方を出してから）
- 「**金一式 11 点で 44% 減**」「**ダイヤ一式 20 点はちょうど `ARMOR_CAP` で 80% 減**」
  （**革 7 点 = 28% 減・鉄 15 点 = 60% 減が 1 つも動かないこと**）
- 「**レシピ 8 本**（材料 5 / 8 / 7 / 4 枚・**8 本とも 3 幅で作業台が要る**）」・「**サバイバルで
  一式に届く**（**金鉱石 → 焼く → 金インゴット 24 枚** / **ダイヤ鉱石を掘るとダイヤ 24 個**。
  **ダイヤは焼きません**）」
- 「**16 部位が既存のどのアイテムとも・互いとも一覧で見分けられる（RGB 20 以上）**」・
  「金・ダイヤの 8 部位は**置けず・掘る道具でも食べ物でもなく・1 枠 1 個**」
- **既存の判定を 1 つもゆるめずに緑のまま**（数え直しは下の 5-6）

## 2. 触るファイルと、触らないファイル

| ファイル | 何をするか | 見込み |
| --- | --- | --- |
| `src/items.ts` | ID 8 つ ＋ `item()` 8 行 ＋ `ARMORS` に 8 行 ＋ `MAX_ITEM_ID` ＋ 表の説明 | +45 行 |
| `src/crafting.ts` | **`armorRecipes()` の呼び出しを 2 行足すだけ**（**関数の中身は 1 行も変えない**） | **+2 行** |
| `test/items.test.ts` / `test/crafting.test.ts` / `test/blocks.test.ts` / `test/vitals.test.ts` | 下の 5 | +90 行 |
| `ROADMAP.md` / `TUNING.md` | 予約表に 1 行（**実装済み**・次は 183）/ 色を 1 節 | +8 行 |
| **`main.ts` / `inventory.ts` / `vitals.ts` / `craftscreen.ts` / `durability.ts`** | **どれも触らない** —— `main.ts` は**1 文字も開かないこと**（1450 行。**停止条件 2 に並んでいます**）、残り 4 つは 33a で ±0 行が実証済み | **0 行** |

**先に読むこと（自動では読み込まれません）**: `rules/items-survival.md`・`rules/vitals.md`・
`rules/inventory-screen.md`・`rules/testing.md`。使うスキルは **`add-block`**。

## 3. 使う ID

**8 個。共有帯の次の空き 175..182**（`ROADMAP.md` の予約表。**低帯 57..63 は階段の予備なので触らない**）。
**並びはどちらも 帽子(head) → 上着(chest) → ズボン(legs) → 靴(feet)**（革・鉄と同じ順）:

| 材質 | ID | 定数 | 点数 | 色（上ほど明るい） |
| --- | --- | --- | --- | --- |
| **金** | **175..178** | `GOLD_HELMET` / `GOLD_CHESTPLATE` / `GOLD_LEGGINGS` / `GOLD_BOOTS` | **2 / 5 / 3 / 1 = 11** | `0xfee34d` / `0xefd714` / `0xf8b317` / `0xc2aa35` |
| **ダイヤ** | **179..182** | `DIAMOND_HELMET` / `DIAMOND_CHESTPLATE` / `DIAMOND_LEGGINGS` / `DIAMOND_BOOTS` | **3 / 8 / 6 / 3 = 20** | `0xd1fefe` / `0xa4f8f5` / `0x92e6e3` / `0x80d4d1` |

- 表示名は**「金の帽子」…「ダイヤの靴」**。**`GOLD_INGOT`(67) / `GOLD_BLOCK` / `DIAMOND`(68) /
  `DIAMOND_BLOCK` / `DIAMOND_PICKAXE` … と衝突する定数名を作らないこと**（両方 import した瞬間に `typecheck` が落ちる。型で止まる安全な罠）
- **この周のあと: 111..255 の空きは 81 → 73・次の空きは 183**（低帯は 7 のまま）。
  **`MAX_ITEM_ID` は `IRON_BOOTS`(174) → `DIAMOND_BOOTS`(182)**（伸ばし忘れると一覧に出ません）

## 4. 判断をどのファイルに置くか

- **点数は `items.ts` の `ARMORS` に 8 行だけ**（本家のまま）。**`inventory.ts` にも `vitals.ts` にも
  アイテムの名前を書かないこと** —— 33a で「材質が増えても `armorPoints` は ±0 行」が実証済み。
- **⚠ ダイヤ一式 20 点は `ARMOR_CAP`(20) にちょうど当たります。それでも `ARMOR_CAP` も
  `ARMOR_DENOM`(25) も触らないこと。本家がそういう設計です** —— 「早く頭打ちになった」のではなく
  **上限に届く一式が初めて入る**だけ。`test/vitals.test.ts` の「防具点 20 で 2（8 割減で頭打ち）」は
  **もう緑**で、この周が足すのは「**その 20 点がダイヤ一式で届く**」1 件です。
- **⚠ 金は鉄より弱いのに同じ 24 枚です**（11 点 < 15 点）。**本家のまま**にし、「安くする」
  「点を足す」のどちらもしないこと。
- **⚠ 色はこの周が測り直しました**（2026-09-16・一覧 136 色に対して）。**8 色とも通ります**:
  金 **26.3（金インゴット）/ 38.7（ブレイズロッド）/ 26.7（金のリンゴ）/ 32.7（金鉱石）**・
  ダイヤ **28.4（ミルクバケツ）/ 34.9（ガラス）/ 27.4（ガラス）/ 39.6（氷）**。
  **互いは最小 31.2**（ダイヤの上着 ↔ ズボン）・**既存の防具 8 部位とは最小 47.1**。
  **取るときにもう一度測り、20 を割ったらずらして `TUNING.md` に書くこと**（判定はゆるめない）。
- **レシピは呼び出し 2 行**（形も枚数も革・鉄とまったく同じ）。**新しく「確かめられないもの」は
  増えません**（`unverifiable-pair` は不要）。

## 5. 書くテスト

**値を出してから判定すること**（`rules/testing.md`）。**33a が作った表に材質の列を 2 つ足すだけで、
新しい入口は 1 つも作らないこと。**

1. **表（`test/items.test.ts`）**: `MATERIALS` に金とダイヤの 2 行。「**材質の中で頭・胴・脚・足が
   1 つずつ**」を一覧を出力してから判定（2 件増えます）。**`armors.length === 8` → `16`**・
   **`bySlot.every((n) => n === 2)` → `n === 4`**・**`wearable.length === 8` → `16`**
   （**ゆるめたのではなく、材質が 4 つになったので数え直した**と件名に書くこと）
2. **点数**（同上）: `TOTALS` に **金 `[2,5,3,1]` 合計 11**・**ダイヤ `[3,8,6,3]` 合計 20**。
   **内訳と合計の両方を出力してから**（合計だけだと内訳を入れ替えても緑）
3. **減り方**（`test/vitals.test.ts`）: `armorReduced(10, "モンスター", 11)` と `…, 20)` を
   **出力してから** **5.6（44% 減）**と **2.0（8 割減・上限ちょうど）**。**同じ 1 件で革 7 点の 7.2 と
   鉄 15 点の 4.0 が動いていないこと**も見る。**既存の「防具点 20 で 2」はそのまま残すこと**
4. **置けず・道具でも食べ物でもない・1 枠 1 個**: 既存のループが `allArmorIds()` を回すので
   **そのまま 16 種に伸びます**（出力だけ確かめること）
5. **レシピ**（`test/crafting.test.ts`）: `armorTable` に**金 4 行とダイヤ 4 行**。**枚数・3 幅・
   2x2 では作れないこと**を出力してから判定。さらに一式の道を **2 本**出力してから:
   **金鉱石 → 焼く → 金インゴット 1 個 → 金一式 24 枚**と**ダイヤ鉱石を掘る → ダイヤ 1 個 →
   ダイヤ一式 24 個**（**⚠ ダイヤは `SMELTING` にありません** —— 鉄・金と同じ形を書くと
   `smeltResultOf(DIAMOND_ORE)` が null で落ちます。**`DROPS` の側を見ること**）
6. **数え直し（ゆるめるのではありません。件名に理由を書くこと）**: `blocks.test.ts` の「**共有帯の
   アイテムは … 56 個**」→ **64 個**（件名の末尾に「金・ダイヤの防具 8 部位」を足し、
   `sharedItems[56..63]` の 8 件を足す。**[52..55] の鉄は動きません**）・「**空きは 81**」→ **73**・
   **`MAX_ITEM_ID === IRON_BOOTS` → `DIAMOND_BOOTS`**（`blocks` と `items` の 2 か所。**古いまま残すと
   `tsc` が TS2367 で落ちる** —— 33a の罠）・`crafting.test.ts` の「**レシピは 74 本**」→ **82 本**

## 6. このタスク固有の禁じ手

- **`ARMOR_CAP` / `ARMOR_DENOM` / `ARMOR_APPLIES` / `ARMOR_SLOTS` / `armorPoints` を触らないこと**（上の 4 の ⚠）
- **防具に耐久値を足さないこと**（`durability.ts` に 1 行も書かない。**人の判断待ち**）/
  **金の道具を足さないこと**（`toolRecipes()` に 1 行も足さない）
- **`armorRecipes()` の中身を 1 行も変えないこと**（革・鉄の 8 本は名前・形・枚数・出目がそのまま）
- **`GOLD_INGOT` / `DIAMOND` の `DROPS` / `SMELTING` / `FUEL` に 1 行も足さないこと**（もう通っています）
- **既存 ID を振り直さない・`SaveData.version` は 1 のまま・`main.ts` を開かない**・
  **8 個より多く番号を取らないこと**（183 以降は空けたまま）

## 7. 終了条件

`npm run typecheck` 緑 / `npm test` **すべて緑**（音の 1 件が跳ねたら走り直す。2 回続けて赤いときだけ
退行）/ `npm run build` 緑 / **`npm run bench` は不要** / コミット 1 つを `master` へ push /
**撮って `Read` で開いて見る**（C-3。見た目に出るのは**クリエイティブ一覧**なので
`node tools/browsershot.mjs` で **`#mode` を `click()` → `E` → `.label` が「金の帽子」の枠を
`scrollIntoView({ block: "center" })`** して 1 枚。**枠を数えるのは `#creative .slot`**。**`npm run
shot` は不要**。**⚠ 絵で `.label` の折れも数えること** —— 5 文字から折れ、**新しく折れるのは
金のズボンとダイヤ 4 部位の 5 枠**の見込み）/
`AUTODEV-QUEUE.md` の 33b を消す / **`ROADMAP.md` の予約表に 1 行**（175..182・次は 183）/
**`TUNING.md` に 1 節**（測った 8 色と**ダイヤが上限にちょうど当たる件**）/ このファイルを `状態: 済`
に / `docs/autodev-log.md` に 1 節 / `HANDOFF.md` を書き直す。
