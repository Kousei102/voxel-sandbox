# 仕様: クモの目（キューの 48・**ID 1 個 = 190**）

状態: 済
差し戻し: 0 回

**この 1 件だけコードで数え直しました**（B の周の決まり）: 入っていません。`src/**` に「クモの目」の
アイテムは 0 個（`SPIDER_EYE` は `mobs.ts` 884 行の**見た目の色の定数**で、アイテムではない）。
`SPIDER.drop`（`mobs.ts` 945 行）は `{ item: STRING, count: 1, chance: 1 }` で **`extra` を持たず**、
コメントも「`extra` は持たない」。食べ物は `allFoodIds()` で **11 種**、毒つきは腐った肉 1 行だけ。

**キューの注記の確かめ**（前の周の `HANDOFF.md` が頼んだ 2 つ）:

- **モブの `extra` はブロックの `Drop.extra` とは別の型**（`mobs.ts` 116 行の `MobDrop extends MobDropStack`・
  `extra?: MobDropStack`）。**`test/blocks.test.ts` の「extra は 1 山目と別のアイテム」はブロックだけを見る**が、
  糸 ≠ クモの目なのでどちらにしても当たらない。**2 山目は 1 山目と無関係に別に引く**（`dropsFor()` 3245 行）。
  **`chance < 1` の山は乱数を 1 本引く**（スケルトンの矢 0.5 が前例）
- **毒は済み**: `FoodDef.poison`（`items.ts` 1012 行）→ `Vitals.eat()` が `POISON_TICKS`(4) 回・
  `POISON_INTERVAL`(1.5 秒) ごとに痛む（`vitals.ts` 466 / 711 行）。**確率の毒は持てない**（生鶏肉のコメント）が、
  本家のクモの目は**必ず毒**なので `poison: true` の 1 行で本家どおり

**本家の規則**（Beta 1.9pre → 正式 1.0）: クモを倒すと **1/3 で目 1 個**（糸 0〜2 とは別に引く）/
食べると **空腹 2・満腹度 3.2・必ず毒**。醸造の材料は見送り済み（キューの「醸造とポーション」）。

## 1. 何を足すか / 完了の判定

**アイテム 190「クモの目」を足し、クモを倒すと糸 1 個に加えて 1/3 の確率で目が 1 個落ち、
食べると空腹 2・満腹度 3.2 が戻って毒になるようにする。**
**完了**: `npm test` に**「クモの目（48）」の件**（`test/items.test.ts`・`test/blocks.test.ts`・`test/mobs.test.ts`・
`test/vitals.test.ts`）が増えて**すべて緑**（**+8〜14 件**）。ブロック ID の枠の行が**「111..255 の空き 65」**になる。

## 2. 触るファイル / 触らないファイル

**触る**: `src/items.ts`（定数 `SPIDER_EYE = 190` と説明のコメント・`item({...})` 1 行・`MAX_ITEM_ID` の付け替え・
`FOODS` 1 行）/ `src/mobs.ts`（**`SPIDER.drop` に `extra` を 1 つ**と、その上の「`extra` は持たない」のコメントの
書き換え・import 1 つ）/ `test/items.test.ts` / `test/blocks.test.ts` / `test/mobs.test.ts` / `test/vitals.test.ts` /
`ROADMAP.md`（予約表の 190 に 1 行・「190..255 予備 66 個」を「191..255・予備 65 個」に・213 行あたりの「使用済み」）/
`TUNING.md` / `AUTODEV-QUEUE.md` / `docs/autodev-log.md` / `HANDOFF.md` / 当たった `rules/*.md`。

**触らない**: **`src/main.ts`（0 行。落とし物は `onDrop` が、食べるのは `foodOf()` → `vitals.eat()` が既に通す）** /
`src/use.ts`（`foodOf(held)` の既存の 1 本で食べられる）/ `src/vitals.ts`（毒の長さ・強さを変えない）/
`src/mobrender.ts` / `src/mobmesh.ts`（**`mobs.ts` の定数 `SPIDER_EYE`（見た目の色）を消さない・名前を変えない**）/
`src/blocks.ts` / `src/crafting.ts`（**レシピは足さない**。発酵したクモの目は醸造で見送り）/ `SaveData`。

**⚠ 名前がぶつかります**: `mobs.ts` には既に `const SPIDER_EYE = 0xd03a2a`（目の箱の色）があります。
`items.ts` の新しい定数を `SPIDER_EYE` にすると **`mobs.ts` で import した瞬間に重複**になるので、
**`mobs.ts` 側は `import { SPIDER_EYE as SPIDER_EYE_ITEM }` のように別名で取ること**（色の定数を
改名すると `mobmesh` の絵の見張りの文字列に触る恐れがある。改名しないこと）。

**先に引いて読むこと**: `grep -l '"src/items.ts"' rules/*.md`（**`items-survival.md` の `MAX_ITEM_ID` と TS2367・
一覧の色**）/ `grep -l '"src/mobs.ts"' rules/*.md`（**`mobs.md` の「殴った側と撃った側を並べて測る」**）/
`rules/vitals.md`（食べ物の強さの並び 85 行）/ `rules/testing.md`（**乱数は 1 本を回し続ける**）/
**`add-block` スキル**（アイテムを足す手順。**ブロックは足さないので `blocks.ts` の節は飛ばす**）。

## 3. 使う ID

**190 を 1 個**（`ROADMAP.md` の予約表「190..255 予備 66 個」の先頭）。**アイテムだけ**で、ブロック 190 は作らない
（111 以降は 1 本の番号列。`test/blocks.test.ts` が両側を突き合わせる）。**次に取るのは 191 になる。**

## 4. 判断をどこに置くか

**判断は `items.ts`（何か・食べるとどうなるか）と `mobs.ts`（誰が・どれだけの確率で落とすか）。**
新しい「確かめられないもの」は 0 個（`unverifiable-pair` 不要）。

- **`item({ id: SPIDER_EYE, name: "クモの目", block: AIR, stack: MAX_STACK, color: 0xb83050, tool: null })`**
  を `GLOWSTONE_DUST` の `item()` の後に。**64 個積める**（本家どおり）
- **`MAX_ITEM_ID = SPIDER_EYE`**（いまは `GLOWSTONE_DUST`）
- **`FOODS`**: `[SPIDER_EYE, { hunger: 2, saturation: 3.2, poison: true }]` を `ROTTEN_FLESH` の後ろではなく
  **表の末尾（金のリンゴの後）**に、腐った肉と同じ「弱い代わりに毒」のコメント付きで。
  **`heal` / `alwaysEdible` を付けないこと**（満腹なら食べない今までどおり）
- **`mobs.ts` の `SPIDER.drop`**: `{ item: STRING, count: 1, chance: 1, extra: { item: SPIDER_EYE_ITEM, count: 1, chance: 1 / 3 } }`。
  **糸は 1 個固定のまま**（羽根・革と同じ線引き）。コメントを「2 山目にクモの目を 1/3（本家どおり）」へ
- **色 `0xb83050` は B の周で総当たりで測った値**（`TUNING.md` に 1 行・`items.ts` のコメントに 4 つ）:
  - **選んだ値** `0xb83050`（くすんだ深紅。目の箱 `0xd03a2a` より暗く、ピンクに寄せた）
  - **いちばん近い相手**: **赤キノコ(139) で 32.1**。次がベッド(41) 39.4・生牛肉(130) 41.2
  - **割った候補**: 絵の目の色そのまま `0xd03a2a`（**リンゴと 17.2**）/ `0x9e2a3a`（**ベッドと 19.0**）
  - **赤の帯の総当たりの最大は `0xba105a` の 59.6** だが**赤紫に寄る**ので採らない（「材質らしく見える範囲で遠いもの」）
  - **C の周で測り直すこと**（`HANDOFF.md` の「色を総当たりで測る」の入口がそのまま使える）

## 5. 書くテスト（**値を出力してから判定する**）

- **`test/items.test.ts`**（グロウストーンダストの件の形）: 名前・色・置ける/道具・1 枠の数を 1 行に出してから、
  **置けない・道具でない・64 個**。**色の隔たり**: 全アイテムでいちばん近い相手を出してから **`>= 20`**。
  **`allItemIds()` に 190**（`MAX_ITEM_ID` の突き合わせを新しい番号へ。**古い `=== GLOWSTONE_DUST` を残すと TS2367**）
- **`test/blocks.test.ts`**:
  - 共有帯の一覧に **`sharedItems[68] === SPIDER_EYE && MAX_ITEM_ID === SPIDER_EYE`**・「共有帯 N 個」を数え直す
  - **空きの件を「111..255 の空きは 65（クモの目 190 で 1 個減った）」に**（数え直し。ゆるめではない）
  - **食べ物の数の 2 件（2590 / 2792 行の「11 種」）を 12 種に数え直す**。**名前も「11 種から 12 種になった」へ**
    （判定の形は `===` のまま。**`>= 11` にしないこと**）
  - **ブロック 190 は存在しない**
- **`test/mobs.test.ts`**（2476 行のクモの件を**書き換える**。**仕様が変わったので、ゆるめではない**）:
  - 表: **糸 1 個・必ず + 2 山目クモの目 1 個・1/3** を出してから `===`（`chance === 1 / 3`）
  - **スケルトンの件（2528 行）の形で 200 体ずつ**、殴って / 矢で倒して、**糸 100%・目の割合**を出してから
    **糸は必ず 1 山目**・**目は 2 つとも 20〜47%**（1/3 の二項の 200 体で ±4σ 程度。**出した値を見て幅を決め、
    幅を決めてから種を変えないこと**）。**乱数は 1 本を回し続けること**
- **`test/vitals.test.ts`**（腐った肉の毒の件の形）: クモの目を食べて**空腹 +2・毒になる**・
  **満腹なら食べられない**（`canEatFood`）。**強さの並び**: 生鶏肉 2 / 1.2 と同じ空腹 2 で満腹度は上（3.2）、を 1 行出す

## 6. このタスク固有の禁じ手

- **`dropsFor()` / `MobDrop` の型を変えないこと**（3 山目を足す・個数の幅を足す、はしない）/
  **糸を 0〜2 個にしないこと** / **ほかのモブの `drop` を触らないこと**
- **`mobs.ts` の色の定数 `SPIDER_EYE` を改名・削除しないこと**（上の ⚠）/ **`vitals.ts` の毒の数値を触らないこと**
- **`main.ts` に 1 行も書かないこと** / **発酵したクモの目・醸造のレシピを足さないこと**（見送り済み）
- **既存の ID を振り直さないこと**・`SaveData.version` は 1 のまま / **色の判定 `>= 20` と割合の判定をゆるめないこと**
  （**割合が外れたら種ではなく実装を疑うこと**）/ **49 以降に手を出さないこと**

## 7. 終了条件

- `npm run typecheck` / **`npm test`**（すべて緑・**3981 → 3989〜3995 あたり**）/ `npm run build` 緑。
  **`bench` は不要**（生成もメッシュ化も触らない）
- **C-3**: 一覧に 1 枠増える（見た目に出る）。**使い捨てのスクリプトでクリエイティブの一覧を撮って `Read` で見ること**
  （`docs/browser-shots/README.md` の `creative-clay.png` の手順）。名前は 4 文字なので `.slot .label` は折れない見込み
  （**折れたら 59 枠目**として `HANDOFF.md` に書く）。**モブの絵は変わらない**（`npm run shot -- mobs` は不要）
- **コミット 1 つを `master` へ push** / キューの 48 を消す / この仕様書を **`状態: 済`** /
  **`ROADMAP.md` の予約表に 190 を「実装済み」** / `TUNING.md` に 1 節（1/3・2 / 3.2・毒・色）/
  `docs/autodev-log.md` に 1 節 / 踏んだ落とし穴を `rules/` へ / **`HANDOFF.md` を書き直す**
