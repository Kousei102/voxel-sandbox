# 仕様: 赤キノコ・茶キノコ（生えものブロック 2 つ）

状態: 済
差し戻し: 0 回

**`AUTODEV-QUEUE.md` の先頭 17 番を 2 つに割った前半（17a）。** 元の 17 番は
「キノコ 2 種 + ボウル + キノコシチュー」の 4 個で、**ブロックと生成・アイテムとレシピと
食べ物**の 3 方向あり、**120 行に収まりませんでした**（`AUTODEV.md` の B の「割る」）。
後半 **17b（ボウル・キノコシチュー）はキューの 2 番目**に書き戻してあります。

2026-09-05 に数え直して **`mushroom` / キノコは `src/**` にも `test/**` にも 0 件**
（`grep -rn -i "mushroom\|キノコ\|BOWL\|STEW" src/ test/` が 1 行も出しません）。

**`main.ts` は 0 行**（いま 1444 行のまま。止まる目安 1450 に当たりません）。

## 1. 何を足すか / 完了の判定

**森と針葉樹林の地表に、草むらと同じ十字の板でまれに生えるキノコ 2 種。**
**壊すと自分が 1 個落ち、持ち歩いて置ける**（草むらとまったく同じ経路）。

完了の判定（`npm test` が**全部緑のまま**、次が増えていること。いま 3044 件）:

- `test/blocks.test.ts` —— **2 つの色と形を出してから**「`cross` で `variantOf` が無く、
  同じ番号のアイテムとして持てて、置くと自分に戻る」（鉱物の立方体の件と同じ書き方）
- `test/worldgen.test.ts` —— **生えた本数を出してから**「森・針葉樹林にだけ生える」
  「必ず地表の 1 つ上」「赤と茶が両方出る」
- **数え直し**（ゆるめるのではない。`AUTODEV.md` の C-2）: 共有帯のアイテム **25 → 27 個**・
  **111..255 の空き 117 → 115**・**非立方体 69 → 71**

## 2. 触るファイル / 触らないファイル

| ファイル | 何を書くか |
| --- | --- |
| `src/blocks.ts` | `RED_MUSHROOM = 139` / `BROWN_MUSHROOM = 140` と `def()` 2 つ（**草むらの定義をそのまま写す**） |
| `src/biomes.ts` | `BiomeDef.mushroom`（地表 1 マスごとの確率）と 10 バイオームぶんの値 |
| `src/worldgen.ts` | `tuft` を決める 3 行に「**キノコを先に引き、外したら草むら**」を足す |
| `test/blocks.test.ts` `test/worldgen.test.ts` | 上の件 + 数え直し |
| `ROADMAP.md` | 予約表に 139 / 140 の行（**実装済み**と書く） |

**触らないこと**: `src/main.ts`（**0 行**）/ `src/items.ts`（**1 行も要りません** ——
`variantOf === AIR` のブロックには for が同じ番号のアイテムを自動で作ります）/
`src/mesher.ts`（`cross` はもうある）/ `src/crafting.ts` / `src/vitals.ts` /
`src/drops.ts` / `items.ts` の `DROPS`（**既定が「自分が 1 個」**なので行が要りません）。

**先に読むこと**（`rules/*.md` は自動では読まれません）:
`rules/blocks-shapes.md`・`rules/worldgen.md`・`rules/items-survival.md`・`rules/testing.md`。
**スキルは `add-block`**（ID の取り方とクリエイティブ一覧までの道筋）。

## 3. 使う ID

**139 = 赤キノコ / 140 = 茶キノコ** の 2 個（`ROADMAP.md` の予約表「139..255 予備 117 個」の
先頭 2 つ）。**共有帯なので 1 つの番号をブロックとアイテムで共有します**（135..137 の
鉱物の立方体と同じ）。**アイテム側の番号を別に取らないこと。**
判定は **`npm test` の「111..255 の空き」が 117 → 115** に減ること。

## 4. 判断をどのファイルに置くか

- **どのバイオームにどれだけ生えるかは `biomes.ts` の表**（`BiomeDef.mushroom`）。
  **`worldgen.ts` に「森なら」と書かないこと**（`rules/worldgen.md` の「地表のブロックや
  木の生えやすさを `worldgen.ts` 側に書かない」）。**0 より大きいのは森（0.015）と
  針葉樹林（0.01）だけ** —— 砂・雪・岩肌の上に生えると見た瞬間におかしいと分かります。
  **暫定の数値なので `TUNING.md` に 1 行**。
- **赤か茶かは `worldgen.ts` の 2 本目のハッシュで半々**（`hash3` / `hash2` の既存の形。
  塩は他と重ねないこと —— 重ねると草むらと同じマスに寄ります）。
- **キノコを草むらより先に引くこと。** あとに回すと、草むらの生えない所
  （確率 0.15 の外）にしか出ず、**森の密度が biomes.ts の値と食い違います。**
- **形と色は `blocks.ts`**。`model: "cross"` / `boxes: CROSS_BOX` / `supportFace: FACE_YN`
  （**床が要ることと、床が消えたら壊れることはこれだけで済みます**）/ `hardness: 0` /
  `sound: "grass"` / `opaque: false` / `solid: false`。

## 5. 書くテスト

**値を出してから判定すること**（`rules/testing.md`）。

- `test/blocks.test.ts` に 2 件（**`model` / `variantOf` / 色 / 落ちるもの / アイテム名を
  1 行に出してから**判定。鉱物の立方体の件が手本）＋ **色の件 1 つ**
  （**赤・茶・草むらの 3 色を並べて出してから**、どの 2 つも RGB で十分離れていること
  —— 3 つとも `cross` の板 1 枚なので、**色が近いと絵で見分けが付きません**）
- `test/worldgen.test.ts` に 3 件（**16x16 列ぶん生成して、バイオームごとの本数と
  赤 / 茶の内訳を出してから**判定）:
  1. **森と針葉樹林にだけ立つ**（砂漠・雪原・浜・海に 1 本も無い）
  2. **必ず地表の 1 つ上**（下が地表のブロック・上が空気。**浮いていない**）
  3. **赤も茶も出る**（半々のハッシュが片側に寄っていない）

## 6. このタスク固有の禁じ手

- **`replaceable: true` を必ず付けること**（草むらと同じ）。**付けないと木の葉が
  キノコに弾かれて、森の木に穴が空きます**（`worldgen.ts` の `stampTree` の
  `put(..., overwrite=false)` が `isReplaceable()` で見ています）
- **`items.ts` に `item({...})` を書き足さないこと**（二重に作ると番号が食い違います）
- **`DROPS` に行を足さないこと**（既定で自分が 1 個。足すと草むらの形に寄せたくなります）
- **既存の ID を振り直さないこと**・**`SaveData.version` は 1 のまま**
  （**セーブは 1 バイトも増えません** —— 位置ごとの状態を持たないブロックです）
- **`main.ts` に 1 行も書かないこと**（1444 行のまま。次の 17b で 1 行使います）
- **既存の判定をゆるめて緑にしないこと** —— 上の 3 つは**数え直し**です
- **洞窟の床には生やさないこと**（本家にはありますが、下のマスが別チャンクになる
  段の底が抜けるうえ、`npm run shot` に**写らない**ので確かめられません。
  見送りの理由を `docs/autodev-log.md` に 1 行残すこと）

## 7. 終了条件

`npm run typecheck` 緑 / `npm test` **全部緑** / `npm run build` 緑 / **コミット 1 つ** /
`AUTODEV-QUEUE.md` の 17a の行を消す / この仕様書を `状態: 済` に /
**`ROADMAP.md` の予約表に 139・140 を「実装済み」で書く** / **`docs/autodev-log.md` に 1 節** /
**`TUNING.md` に 1 行**（森 0.015・針葉樹林 0.01 は暫定）/ **`HANDOFF.md` を丸ごと書き直す** /
**`master` へ push**。

**C-3（撮る）**: **地表に新しい形が出るので必ず撮ること。**
`npm run shot -- terrain ground` を撮って **`Read` で開き**、**面の欠け・裏返りが無いこと**と
**キノコが草むらと見分けられること**を見ます（森が画に入らずキノコが 1 本も写らなかったら、
**その旨を `HANDOFF.md` に書くこと** —— 見えたことにしない）。
