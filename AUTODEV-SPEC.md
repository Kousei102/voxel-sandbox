# 仕様: 刈られた羊が見て分かること（キューの 12・**ID 0 個**）

状態: 未着手
差し戻し: 0 回

**先に読むこと**: `rules/mobs.md`（全部）/ `rules/meshing-render.md`（撮る所）/ `rules/testing.md`。
**自動では読み込まれません。** スキルは **`unverifiable-pair`**（`mobs.ts` ↔ `mobrender.ts` の対は
**もう出来ています** —— この周は**対を新しく作るのではなく、崩さずに 1 本だけ枝を通す**周です）。

## 1. 何を足すか / 完了の判定

**刈った羊が、羊毛の戻るまで「刈られた姿」で描かれる。** いま `Mob.woolTimer` は立つのに
（`mobs.ts` の `shear()`）、**`mobrender.ts` は種類ごとに形を 1 つ作って使い回すので見た目が 1 画素も変わりません。**

完了の判定は `npm test` に次の項目が増えて緑になること（数字は実装が測って出す）:

- 「刈ると姿が変わり、羊毛が戻ると元へ戻る」— `mobVariant()` が **0 → 1 → 0** と動くのを値で出してから判定
- 「刈られた姿ももこもこと同じ検査を通る」— 体積・巡回順・頂点数・`y1 === 0`・当たり判定（下の 5）
- 「刈られた姿の体は、もこもこより細い」— **体（`group 0`）の外形を両方出して**、3 軸とも変種 < 既定
  （**モデル全体で比べないこと** —— 外形は脚と頭が決めていて、**刈っても 0.625 x 1.125 x 1.444 のまま**です）
- 「羊以外は姿が変わらない」— `MOB_KINDS` を回して変種を持つ数を出す（羊だけ 1）
- 「`mobrender.ts` に種類の名前が直書きされていない」— 新しい見張り（下の 5）

## 2. 触るファイル / 触らないファイル

| ファイル | 何をするか |
| --- | --- |
| `src/mobs.ts` | `MobDef.shornBoxes`（`readonly MobBox[] \| null`）を足し、**10 種すべてに書く**（羊以外 `null`）。羊に刈られた姿の箱。`mobVariant(mob)` と `variantBoxes(def, variant)` を export |
| `src/mobmesh.ts` | `buildMobMesh(def, rgbOf, variant = 0)` —— 箱の取り出しを `variantBoxes()` に替えるだけ（**three は今までどおり import しない**） |
| `src/mobrender.ts` | 形の控えの鍵を `kind` → `` `${kind}:${variant}` `` に。`Rig` に `variant` を持たせ、**変わったら作り直す** |
| `tools/shot.ts` | 場面 `sheep` を足す（もこもこと刈られたのを並べる。C-3） |
| `test/mobs.test.ts` | 上の 5 件 |

**触らないこと**: `src/main.ts`（**0 行。1450 行のまま**）/ `src/blocks.ts` / `src/items.ts` /
`src/use.ts` / `src/crafting.ts` / `ROADMAP.md` の予約表 / **`MobDef.shearing` の値**（1〜3 個・60 秒）。

## 3. 使う ID

**0 個。** ブロックもアイテムも 1 つも増えません（見た目だけの周）。
空きは **1..63 が 7・111..255 が 69 のまま**で、**次に取るのは 187**。
**新しい番号を取らないこと**（取りたくなったら設計が違う信号）。

## 4. 判断をどこに置くか

- **「いつ刈られた姿か」は `mobs.ts` の `mobVariant(mob)` 1 か所だけ。**
  `MOBS[mob.kind].shornBoxes !== null && mob.woolTimer > 0 ? 1 : 0`。
  **`kind === "sheep"` と書かないこと**（`shearing` / `milkable` / `orbit` と同じ作法）
- **`shornBoxes` を `?:`（省略可）にしないこと** —— `BiomeDef.vine` と同じで、足し忘れが黙って通ります
- **`mobrender.ts` が見るのは `mobVariant()` の返す数だけ。** `woolTimer` / `shearing` / `shorn` /
  種類の名前を 1 つも書かないこと（**既存の見張りに `shearing` と `woolTimer` はもう入っています**。
  `"shorn"` と種類名を足すのがこの周の仕事）
- **箱の取り出しは `variantBoxes(def, variant)`**（`mobs.ts`）。`mobmesh.ts` も `mobrender.ts` も
  `def.shornBoxes` を直に読まないこと —— 読むと「1 は刈られた姿」がファイル 3 つに散ります
- **形は `mobs.ts` の表に置くこと**（`groups` は**もこもこと同じ骨組みを使い回す** ——
  `pivot` と `motion` を変えないこと。変えると `mobrender.ts` に部位の数の分岐が生えます）

### 刈られた羊の箱（`px()` 単位。**この値をそのまま書くこと**）

```
SHEEP_SKIN = 0xe3b09c        // 刈られた地肌。羊毛 0xe8e4dc から 82.6・顔と脚 0xd6c8b4 から 36.3 離れている
group 0 体: [-3.5, 9.5, -3] .. [3.5, 16.5, 14]   SHEEP_SKIN   // もこもこ [-5,9,-4]..[5,18,16] より細く・低く・短い
group 1 頭: [-3, -3, -4] .. [3, 3, 0]            SHEEP_SKIN   // 箱はもこもこと同じ。色だけ地肌へ
group 1 顔・目 2 つ: もこもこと同じ箱・同じ色（`SHEEP_FACE` / `SHEEP_EYE`）
group 2..5 脚 4 本: もこもこと同じ箱・同じ色（`SHEEP_FACE`。**y1 === 0 を崩さない**）
```

**体を低くする（18 → 16.5）のが要点**です —— 頭（上端 18）が体から出て、**横から見た輪郭が変わります。**
体の外形は **0.625 x 0.563 x 1.250 → 0.438 x 0.438 x 1.063**（測って確かめた値。3 軸とも縮みます）。
**脚の上端 9 と体の下端 9.5 の間の 0.5px の隙間を埋めないこと**（`rules/mobs.md` の
「隣り合う面が同じ色なら、隙間か別の色のどちらかが要る」。**ここは隙間と色の両方**）。

## 5. 書くテスト（`rules/testing.md`・**値を出してから判定する**）

- **既存の「モブの形」のループの中身を `checkShape(label, def, boxes)` に括り出し**、
  変種 0（10 種）と変種 1（羊だけ）の**両方に同じ 5 件を掛けること**。
  **`boxVolume(def, group)` も箱を受け取る形に替える**（`def.boxes` 直読みのままだと変種を見ません）。
  **既存の判定名・しきい値を 1 文字も変えないこと**（変種 0 は今までどおりの名前で通ること）
- **`longBody` の例外（豚と羊）はそのまま。** 刈られた羊の後ろ脚は今までと同じ 16px なので、
  **例外を広げる必要はありません**（広げたくなったら箱を書き間違えています）
- 刈る試験は**既存の `shearArena()` を使い回すこと**。`pack.shear()` → `mobVariant()` が 1、
  `pack.update()` を 61 秒ぶん回して 0 に戻る（`woolTimer` の値も一緒に出す）
- **色の見分けを数で押さえること**（`test/items.test.ts` の `dist` と同じ式を手で書く）:
  地肌 ↔ 羊毛 **40 以上**・地肌 ↔ 脚 **20 以上**。**両方の実測値を出してから判定**
- **`mobrender.ts` の見張りに 2 つ足すこと**: `decisions` に **`"shorn"`**、
  および **`MOB_KINDS.filter((k) => renderSource.includes(`"${k}"`))` が 0 件**（別の 1 件）

## 6. このタスク固有の禁じ手

- **もこもこの箱（`SHEEP` の `boxes`）を 1 つも変えないこと。** 変えると既存の形の検査が全部動き、
  この周で何が変わったのか分からなくなります
- **`MobMotion` を増やさないこと・`mobrender.ts` に部位ごとの分岐を書かないこと**
  （`rules/mobs.md`: ここに手が要ったら表で表せないものを足そうとしている合図）
- **形の控え（`shapes`）を種類だけの鍵のままにしないこと** —— もこもこと刈られたのが
  同じ形を共有して、**先に湧いたほうの姿で全部が描かれます**
- **変種が変わったとき `Rig` を作り直したら `sky` / `block` を `-1` に戻すこと**（`createRig()` が
  そうしています）。引き継ぐと、**刈った瞬間だけ光が古いまま**になります
- **`Mob` に新しい状態を足さないこと**（`woolTimer` で足ります。モブは保存しないので
  `SaveData` にも 1 行も要りません）
- **`main.ts` に 1 行も書かないこと**（`MobRenderer.sync()` の呼び方は変わりません）

## 7. 終了条件

- `npm run typecheck` 緑 / `npm test` **すべて緑**（判定を 1 件もゆるめない）/ `npm run build` 緑
- **`npm run bench` は要りません**（生成もメッシュ化も触らないため）
- **撮って自分の目で見る**（C-3・**この周は見た目そのものが本題**）。
  `npm run shot -- sheep mobs` と `node tools/browsershot.mjs`。**`Read` で開いて、
  もこもこと刈られたのが並べて見分けられるか・面の欠けと裏返りが 0 件か**を確かめること
- コミット 1 つ / `AUTODEV-QUEUE.md` の 12 の行を消す / この仕様書を `状態: 済` に
- **手触りの数値（地肌の色・細さ）は `TUNING.md` に 1 行**
- `docs/autodev-log.md` に 1 節 / `HANDOFF.md` を丸ごと書き直す / `master` へ push
