# 仕様: 鉄の防具 4 部位（キューの 33a・**共有帯 4 個**）

状態: 済
差し戻し: 0 回

**キューの 33「鉄・金・ダイヤの防具 12 部位」を 2 件に割った前半です**（後半 33b は
金・ダイヤの 8 部位。`AUTODEV-QUEUE.md` に書き戻し済み）。**割った理由は色で、この周が
測りました** —— 明るい灰色の帯は既存 46 個で埋まっていて、**素直な銀灰はどう選んでも
既存といちばん近くて 20.1〜22.1**（判定は 20）。**まず鉄 4 色だけ**入れます（下の 4）。

## 1. 何を足すか と 完了の判定

`npm test` に「鉄の防具」が増えて、少なくとも次が緑:

- 「**着られるのは 8 種**（革 4 + 鉄 4）」・「**鉄の 4 部位も頭・胴・脚・足が 1 つずつ**」
- 「**鉄の点数は本家のまま 2 / 6 / 5 / 2 = 合計 15**」（内訳と合計の両方を出してから）
- 「**鉄一式 15 点で 60% 減**」（`armorReduced()`。革一式 7 点 = 28% 減は動かないこと）
- 「**レシピ 4 本**（鉄インゴット 5 / 8 / 7 / 4 枚・**4 本とも 3 幅で作業台が要る**）」
- 「**8 部位が既存のどのアイテムとも・互いとも一覧で見分けられる（RGB 20 以上）**」
- 「鉄の 4 部位は**置けず・掘る道具でも食べ物でもなく・1 枠 1 個**」
- **既存の判定を 1 つもゆるめずに緑のまま**（数え直しは下の 5-6）

## 2. 触るファイルと、触らないファイル

| ファイル | 何をするか | 見込み |
| --- | --- | --- |
| `src/items.ts` | ID 4 つ ＋ `item()` 4 行 ＋ `ARMORS` に 4 行 ＋ `MAX_ITEM_ID` ＋ 表の説明を直す | +30 行 |
| `src/crafting.ts` | **`toolRecipes()` と同じ形の `armorRecipes(材質名, 材料, 帽子, 上着, ズボン, 靴)` を 1 本立て**、革と鉄の 2 行にする（**革の 4 本をベタ書きから移す**。出来上がるレシピは 1 本も変わらない） | +20 行 / −27 行 |
| `test/items.test.ts` / `test/crafting.test.ts` / `test/blocks.test.ts` / `test/vitals.test.ts` | 下の 5 | +120 行 |
| `ROADMAP.md` / `TUNING.md` | 予約表に 1 行（**実装済み**・次は 175）/ 色を 1 節 | +8 行 |
| **`src/main.ts`** | **1 文字も開かないこと**（1449 行。停止条件 2 に並んでいる） | **0 行** |
| **`src/inventory.ts` / `src/vitals.ts` / `src/craftscreen.ts` / `src/durability.ts`** | **触らない** —— `armorPoints` は `armorOf()` に聞くだけで材質を知らず、防具枠の配線は 27b で済んでいます | **0 行** |

**先に読むこと（自動では読み込まれません）**: `rules/items-survival.md`・`rules/vitals.md`・
`rules/inventory-screen.md`・`rules/testing.md`。使うスキルは **`add-block`**。

## 3. 使う ID

**4 個。共有帯の次の空き 171..174**（`ROADMAP.md` の予約表。**低帯 57..63 は階段の予備なので触らない**）:

| ID | 定数 | 名前 | 部位 / 点数 | 色（下の 4） |
| --- | --- | --- | --- | --- |
| **171** | `IRON_HELMET` | 鉄の帽子 | head / **2** | `0xcfcfff` |
| **172** | `IRON_CHESTPLATE` | 鉄の上着 | chest / **6** | `0x92a7ce` |
| **173** | `IRON_LEGGINGS` | 鉄のズボン | legs / **5** | `0x788ebe` |
| **174** | `IRON_BOOTS` | 鉄の靴 | feet / **2** | `0x506595` |

- **名前は革と同じ並べ方**（帽子 / 上着 / ズボン / 靴）にすること。本家の「ヘルメット」は
  6 文字で、**一覧の `.label` が折れて絵に被ります**（`HANDOFF.md` の持ち越し）。この 4 つで
  **新しく折れるのは 5 文字の「鉄のズボン」1 枠だけ**です。
- **`IRON_INGOT`(6) と衝突する定数名を作らないこと**（`IRON_BLOCK` も既にあります）。
- **この周のあと: 111..255 の空きは 85 → 81・次の空きは 175。低帯の空きは 7 のまま。**
  **`MAX_ITEM_ID` は `BRICK_ITEM`(170) → `IRON_BOOTS`(174)**（伸ばし忘れると一覧に出ません）。

## 4. 判断をどのファイルに置くか

- **点数は `items.ts` の `ARMORS` に 4 行だけ**（本家のまま 2 / 6 / 5 / 2 = 15）。
  **`inventory.ts` にも `vitals.ts` にもアイテムの名前を書かないこと** —— `armorPoints` は
  `armorOf()` に聞くだけで、材質を 1 つも知りません（**だから鉄でそのまま効きます**）。
  **`ARMOR_CAP`(20) と `ARMOR_DENOM`(25) を触らないこと**（鉄一式 15 は上限に当たりません）。
- **⚠ 色はこの周が測って選んだ「鋼の青」です**（`TUNING.md` に 1 節）。**素直な銀灰は
  通りません** —— 明るさの帯ごとにいちばん遠い無彩色を総当たりで探して **20.3 / 20.1 /
  22.1 / 21.4**（判定 20 のすぐ上）でした。石・丸石・鉄インゴット・バケツ・糸・粘土・
  かまど・羊毛・紙・骨が同じ帯に居ます。**青を 18〜48 足した上の 4 段**なら**いちばん
  近い相手で 26.0 / 27.5 / 43.5 / 47.8**（革 4 部位とは最小 118.8・互いは最小 39.5・
  **明るさ 212 > 165 > 141 > 100 で部位の順**）。**測り直して 20 を割ったらずらしてよく、
  ずらした値を `TUNING.md` に書くこと。**
- **レシピは `crafting.ts` の `armorRecipes()` 1 本**（`toolRecipes()` と同じ作法）。
  **形も枚数も本家のまま**: 帽子 `["III","I.I"]` 5 枚 / 上着 `["I.I","III","III"]` 8 枚 /
  ズボン `["III","I.I","I.I"]` 7 枚 / 靴 `["I.I","I.I"]` 4 枚。**4 本とも 3 幅**です。
  **新しく「確かめられないもの」は増えません**（`unverifiable-pair` は不要）。

## 5. 書くテスト

**値を出してから判定すること**（`rules/testing.md`）。**既存の形をそのまま使い、新しい入口は
1 つも作らないこと。**

1. **表（`test/items.test.ts` の「防具の表」）**: いまの `armors.length === 4` と
   `new Set(slots).size === 4` は **8 種になると落ちます**。**材質ごとに 4 部位を並べた表を
   1 本作り**、「**材質の中で頭・胴・脚・足が 1 つずつ**」「**着られるのは 8 種**」を
   **一覧を出力してから**判定（**「4 つとも別」をゆるめたのではなく、材質が 2 つに
   増えたので数え方を変えた**と件名に書くこと）
2. **点数**（`test/items.test.ts`）: 革 1 / 3 / 2 / 1 = 7 と**鉄 2 / 6 / 5 / 2 = 15** を
   **内訳と合計の両方を出力してから**（合計だけだと帽子 6 / 上着 2 でも緑になります）
3. **減り方**（`test/vitals.test.ts`）: `armorReduced(10, "monster", 15)` の値を**出力してから**
   **4.0**（15 / 25 = 60% 減）を判定。**革一式 7 点の 28% 減が動いていないこと**も同じ 1 件で
4. **置けず・道具でも食べ物でもない・1 枠 1 個**: 既存のループが `allArmorIds()` を回しているので**そのまま 8 種に伸びます**（出力だけ確かめること）
5. **レシピ**（`test/crafting.test.ts`）: 既存の `armorRecipes` の表に**鉄 4 行を足す**（材質の列を 1 つ増やす）。**枚数・3 幅・2x2 では作れないこと**を出力してから判定。
   さらに **鉄鉱石 → 焼く → 鉄インゴット → 鉄一式 24 枚**の道を**1 本として出力してから**「サバイバルで鉄一式に届く」を判定
6. **数え直し（ゆるめるのではありません。件名に理由を書くこと）**: `test/blocks.test.ts` の
   「111..255 の空きは 85」→ **81**・**共有帯のアイテム 52 個 → 56 個**（件名の並びの末尾に
   「鉄の防具 4 部位」を足す。**`sharedItems[41..44]` の革の位置は動きません**）・
   `MAX_ITEM_ID === BRICK_ITEM` → **`IRON_BOOTS`**、`test/items.test.ts` の
   **アイテム 132 種 → 136 種**・`MAX_ITEM_ID === BRICK_ITEM` → **`IRON_BOOTS`**、
   `test/crafting.test.ts` の「レシピは 70 本」→ **74 本**

## 6. このタスク固有の禁じ手

- **金・ダイヤの防具を足さないこと**（33b です。**ID を先取りしない** —— 175 以降は空けたまま）
- **防具に耐久値を足さないこと**（`durability.ts` に 1 行も書かない。**人の判断待ち**で、足す
  ならそれだけで 1 周。`HANDOFF.md`）/ **`ARMOR_CAP` / `ARMOR_DENOM` / `ARMOR_SLOTS` /
  `armorPoints` を触らないこと**
- **`IRON_INGOT`(6) の `DROPS` / `SMELTING` / `FUEL` に 1 行も足さないこと**（もう通っています）
- **既存 ID を振り直さない・`SaveData.version` は 1 のまま・`main.ts` を開かない**
- **`crafting.ts` の `armorRecipes()` 化で、出来上がるレシピを 1 本も変えないこと**
  （革 4 本は名前・形・枚数・出目がそのまま。変わるのは書き方だけ）

## 7. 終了条件

`npm run typecheck` 緑 / `npm test` **すべて緑**（音の 1 件が跳ねたら走り直す。2 回続けて
赤いときだけ退行）/ `npm run build` 緑 / **`npm run bench` は不要**（生成もメッシュ化も触りません）/
コミット 1 つを `master` へ push / **撮って `Read` で開いて見る**（C-3。**見た目に出るのは
クリエイティブ一覧と防具枠**なので、`node tools/browsershot.mjs` で **`#mode` を `click()` →
`E` → `.label` が「鉄の帽子」の枠を `scrollIntoView({ block: "center" })`** して 1 枚。
**`npm run shot` は不要** —— 新しいブロックが 0 個で、どの場面にも 1 マスも増えません）/
`AUTODEV-QUEUE.md` の 33a を消す / **`ROADMAP.md` の予約表に 1 行**（171..174 と「次は 175」）/
**`TUNING.md` に 1 節**（鋼の青。**測った 4 つの隔たり**）/ このファイルを `状態: 済` にする /
`docs/autodev-log.md` に 1 節 / `HANDOFF.md` を書き直す。
