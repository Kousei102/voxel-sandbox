# 仕様: 苗木 2 種が葉から落ちて、土か草に立つ（キューの 30a・ブロック ID 2 個 = 164 / 165）

状態: 未着手
差し戻し: 0 回

**使えるスキル: `add-block`。`unverifiable-pair` は要りません** —— `cross` の板は既にある
`model` で、three にも DOM にも音にも新しい対を作りません。
**キューの 30 を 2 件に割った前半です**（120 行に収まらないため。後半は `30b`）。

## 0. 取る前に数え直した結果（B の周・2026-09-13）

- **`SAPLING` も「苗木」も `src/**` `test/**` に 1 件もありません**（未実装。`items.ts` の
  `DROPS` の「苗木がまだ無いので、葉から出るのは棒とリンゴだけ」がそれ）。
  **`npm test` は 3506 件すべて緑**・**111..255 の空きは 92**（次に取るのは 164）
- **⚠ 山は 2 つまでで、オークの葉は 2 つとも埋まっています**（1 山目 = 棒 10% を `roll` で、
  2 山目 = リンゴ 0.5% を `extraRoll` で）。**苗木は 3 山目になり、そのままでは載りません。**
  **乱数をもう 1 本流すと `breaking.ts` と `main.ts` に及ぶ**ので、下の 4 の形で載せます
- **植える経路はもう通っています** —— 苗木はブロックなので `use.ts` → `placing.ts` の
  `tryPlace()` がそのまま置き、**`main.ts` は 0 行**（種専用の `tryPlant()` は触りません）。
  ただし**土の上だけに立てる仕掛けはありません** —— いまの `supportFace: FACE_YN` は
  「真下が平らな固体」までなので、石でも板でも立ちます（下の 4）
- **`MAX_ITEM_ID` は 163（木炭）** —— **共有帯にブロックを足したら伸ばすこと**（忘れると
  クリエイティブの一覧にだけ出てきません）。**育つのは 30b**（`worldgen.ts` の `stampTree()` は
  チャンクの `Uint8Array` に直に書くので、**`setVoxel` の側からは呼べません**）

## 1. 何を足すか（1 行）と、完了の判定

**オークとトウヒの苗木が葉から 5% で落ち、土・草・耕地の上にだけ立って、掘れば戻る。**
（**育ちません** —— 木になるのは 30b）完了の判定 —— `npm test` に次が増えて**すべて緑**:

- 「オークの葉から苗木が落ちる（`extraRoll` の帯）」「リンゴは今までどおり落ちる」
  「トウヒの葉からトウヒの苗木が落ちる」「111..255 の空きが 92 → 90」（`test/blocks.test.ts`）
- 「苗木は土・草・耕地の上にだけ立つ（石・板・砂の上には立たない）」
  「真下の土を掘ると苗木が壊れて落ちる」（`test/placing.test.ts`）
- 「苗木 2 種がクリエイティブの一覧に出る（`MAX_ITEM_ID` が 165）」
  「2 種とも既存のどのアイテムとも一覧で見分けられる（隔たり 20 以上）」（`test/items.test.ts`）

## 2. 触るファイルと、触らないファイル

| ファイル | 足すもの |
| --- | --- |
| `src/blocks.ts` | `SAPLING = 164` / `SPRUCE_SAPLING = 165` と `def()` 2 つ（下の 4）・`needsSoil` の表 1 本と `supportsBlock()` の 1 行・`supportHint()` の分岐 1 つ |
| `src/items.ts` | `MAX_ITEM_ID` を苗木へ伸ばす（`item()` の行は**要りません** —— ブロックは自動でアイテムになります）・`DROPS` の葉 2 行・`Drop.extra` を帯で 2 件まで（下の 4）・`rollDrops()` の抽選 |
| `test/blocks.test.ts` / `test/placing.test.ts` / `test/items.test.ts` | 上の 1 の項目 |
| `ROADMAP.md` / `AUTODEV-QUEUE.md` / `TUNING.md` / `docs/autodev-log.md` / `HANDOFF.md` | C-4 の記録 |

**触らない**: `src/main.ts`（**±0 行。1 文字も開かないこと** —— いま 1450 行で停止条件に
並んでいます）/ `crops.ts`（**育てるのは 30b**）/ `worldgen.ts`（自然の木を変えない）/
`breaking.ts`（乱数は 2 本のまま）/ `use.ts` / `placing.ts` / `craftscreen.ts` / `inventoryui.ts` /
`ui.ts` / `crafting.ts` / `smelting.ts` / `storage.ts` / `session.ts`。**`SaveData` は 1 バイトも
増えません**（置いた苗木は `edits` に乗るだけ）。

## 3. 使う ID

**ブロック 164（オークの苗木）と 165（トウヒの苗木）の 2 個だけ。** `ROADMAP.md` の予約表の
「次に取るのは 164」から取り、**実装後にその行を「164..165 = 苗木 2 種・実装済み」へ直すこと。**
**アイテムの番号は取りません**（`variantOf` の無いブロックは同じ番号で自動的にアイテムに
なります）。**共有帯の空きは 92 → 90。**

## 4. 判断をどのファイルに置くか

- **苗木が何か**（色・`cross` の板・通り抜けられる・`hardness: 0`・`sound: "grass"`・
  **`replaceable` は付けない**）→ `blocks.ts` の `def()` 2 つ。**小麦の苗（`WHEAT_CROP`）の定義を
  そのまま写し、違うのは色と「`variantOf` を書かない」の 2 点**（書くと掘っても戻りません）
- **土の上だけに立つ** → `blocks.ts` に `needsSoil` の表 1 本（`STACKS_ON_SELF` と同じ形の
  `Uint8Array`）と、`supportsBlock()` に **`needsSoil(id) && !isSoil(supporter)` なら false** の
  1 行（土は**土・草・耕地の 3 つ**）。**`canSupport()` をゆるめないこと** —— あちらは壁掛けの
  松明とベッドの足場で、触ると松明が草むらに刺さります（`rules/blocks-shapes.md`）。
  **`supportsBlock()` は置く側（`canPlaceAt`）と壊す側（`breakUnsupported`）の両方が通る**ので、
  真下の土を掘れば苗木が勝手に壊れて落ちます（サトウキビの例外と同じ置き場所）
- **置けない理由の文** → `blocks.ts` の `supportHint()`。**「床か壁」のままにしないこと**
  （嘘になります。はしごに「壁」を返しているのと同じ扱いで**「土か草の上」**を返す）
- **葉から何が落ちるか** → `items.ts` の `DROPS`。**3 山目を作らず、2 山目を `extraRoll` の
  帯で分けること**: `Drop.extra` が **1 件または 2 件まで**を取れるようにし、`rollDrops()` が
  **上から順に帯を切って 1 件だけ**選びます（オークの葉は**リンゴ 0..0.005 → 苗木
  0.005..0.055**、トウヒの葉は苗木だけ）。**乱数は 2 本のまま**なので `breaking.ts` も `main.ts`
  も ±0 行。**リンゴと苗木は同時に落ちません**（本家は独立。`TUNING.md` に 1 行）

**先に読むこと**: `rules/blocks-shapes.md` / `rules/items-survival.md` / `rules/drops.md` / `rules/worldgen.md` / `rules/testing.md`（`grep -l '"src/blocks.ts"' rules/*.md` + `test/**` のぶん）。

## 5. 書くテスト（値を出してから判定する形）

- **色は `console.log` でいちばん近い相手と隔たりを出してから `>= 20`**（骨・木炭と同じ形）。
  **緑は一覧でいちばん混んでいる帯**です —— 葉 `0x3f7a3a`・トウヒの葉 `0x2c5c3a`・草むら
  `0x5e9c41`・小麦の苗 `0x6f8f3f`・サボテン `0x5c9b47`・サトウキビ `0x9ad14f`。**2 種どうしの隔たりも出すこと**（並ぶので、近いと見分けられません）
- **ドロップは `rollDrops()` に目を渡して山を出してから判定**: `(LEAVES, 0.5, 0.001)` →
  リンゴ 1 山 / `(LEAVES, 0.5, 0.03)` → 苗木 1 山 / `(LEAVES, 0.05, 0.03)` → 棒 + 苗木の 2 山 /
  `(LEAVES, 0.5, 0.9)` → 0 山 / `(SPRUCE_LEAVES, 0.5, 0.03)` → トウヒの苗木。
  **帯の合計が 1 を超えていないことも 1 件**（超えると後ろの候補が永久に出ません）
- **支えは表を一覧で出してから判定**: 土・草・耕地で true、石・板・砂・ガラス・葉で false。
  **`canPlaceAt` と `breakUnsupported` の両方**を通すこと（片方だけだと、置けないのに
  下を掘っても残る／置けるのに勝手に消えるが起きます）
- **数え直す判定（ゆるめるのではなく数字を新しい値に直す）**: `111..255 の空き` 92 → **90** /
  `MAX_ITEM_ID === CHARCOAL` → **苗木**（比べる相手を直さないと `tsc` が TS2367 で落ちます。
  `rules/testing.md`）/ 非立方体と全ブロックの数え上げ。
  **「4 通りの山の数が 1,2,0,1」は消さないこと** —— 目が 0.001 と 0.9 のままなら緑です

## 6. このタスク固有の禁じ手

- **棒 10% とリンゴ 0.5% の数値を書き換えないこと**（山の**並べ方**だけ変える）。
  **乱数を 3 本目に増やさないこと**（`breaking.ts` と `main.ts` に及びます）
- **`DROPS` に苗木の行を足さないこと** —— 掘った苗木は `baseBlock()` が自分を落とします。
  **`tryPlant()`（種の経路）にも足さないこと** —— 苗木はブロックとして置きます
- **育てないこと。`crops.ts` を 1 行も触らないこと**（30b の仕事。`notePlaced()` にも足さない）。
  **`worldgen.ts` も触らないこと**（自然に苗木を生やさない・木の形を変えない）
- **`canSupport()` をゆるめないこと**（上の 4）。**`id === SAPLING` と書かないこと**（表 1 本）
- **ID を 3 個以上取らないこと**（葉の色違いも、育った木も番号を使いません）
- **`main.ts` を開かないこと**（1 行でも配線が要るなら**止まって人を呼ぶ**）。
  **判定をゆるめて緑にしないこと**（数え直すのは可。5 のとおり）

## 7. 終了条件

- `npm run typecheck` 緑 / `npm test` **すべて緑**（いま 3506 件 + 上の項目）/ `npm run build`
  緑。**`npm run bench` は不要。コミット 1 つ**を `master` へ push
- **見た目に出ます**（一覧の色 2 つと `cross` の板）—— **撮って `Read` で見ること**（C-3）。
  一覧は **`#mode` → `E` → 末尾までスクロール**（`docs/browser-shots/README.md`）
- **色 2 つと落ちる確率 5%、リンゴと同時に落ちないことを `TUNING.md` に 1 行**
- `AUTODEV-QUEUE.md` の 30a の行を消し（**30b は残す**）、この仕様書の `状態:` を `済` に
- `ROADMAP.md` の予約表に「164..165 = 苗木 2 種・実装済み」と、`docs/autodev-log.md` に 1 節
