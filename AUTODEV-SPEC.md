# 仕様: レンガ（アイテム）と、焼いて・組んで戻す道（キューの 32b・**共有帯 1 個**）

状態: 未着手
差し戻し: 0 回

**キューの 32 を 2 件に割った後半です**（前半 32a の粘土 168 / 粘土玉 169 は 2026-09-15 に済み）。
**粘土玉をかまどで焼くとレンガ 1 個**、**レンガ 4 個（2x2）で既にある `BRICK`(12)** ——
これで**置けるのに作れなかった `BRICK`(12) が、サバイバルで初めて手に入ります。**

## 1. 何を足すか と 完了の判定

`npm test` に「レンガ（アイテム）」の節が増えて、少なくとも次が緑:

- 「**粘土玉（169）→ 焼く → レンガ（170）1 個**」（`smelting.ts` の 1 行。**燃料ではない**）
- 「**レンガ 4 個（2x2）→ `BRICK`(12) 1 個**・**作業台が要らない**」（粘土・雪ブロックと同じ形）
- 「**粘土 1 個 → 粘土玉 4 → レンガ 4 → `BRICK` 1 個**の道が通っている」（下の 5-5）
- 「レンガ（170）は**置けず・道具でも食べ物でもなく・1 枠 64 個**」
- 「レンガ（170）が**既存のどのアイテムとも一覧で見分けられる（RGB 20 以上）**。
  **とくに `BRICK`(12) と**（下の 4 の名前の話。**並んで出ます**）」
- **既存の判定を 1 つもゆるめずに緑のまま**（数え直しは下の 5-7）

## 2. 触るファイルと、触らないファイル

| ファイル | 何をするか | 見込み |
| --- | --- | --- |
| `src/items.ts` | `BRICK_ITEM = 170` の定数と `item()` 1 行 ＋ `MAX_ITEM_ID` | +16 行 |
| `src/smelting.ts` | `SMELTING` に 1 行（粘土玉 → レンガ）＋ import 2 語 | +5 行 |
| `src/crafting.ts` | `RECIPES` に 1 行（2x2）＋ import 2 語 | +5 行 |
| `src/blocks.ts` | **`def(BRICK, …)` の表示名を「レンガ」→「レンガブロック」に直すだけ**（下の 4） | +1 行 / −1 行 |
| `test/smelting.test.ts` / `test/crafting.test.ts` / `test/items.test.ts` / `test/blocks.test.ts` | 下の 5 | +110 行 |
| `ROADMAP.md` / `TUNING.md` | 予約表に 1 行（**実装済み**・次は 171）/ 色を 1 節 | +6 行 |
| **`src/main.ts`** | **1 文字も開かないこと**（1449 行。停止条件 2 に並んでいる） | **0 行** |
| **`src/worldgen.ts` / `src/biomes.ts`**（湧かせるのは 32a で済み）/ **`src/use.ts` / `src/drops.ts` / `src/placing.ts` / `src/furnaces.ts` / `src/craftscreen.ts`**（かまども 2x2 も既存の道をそのまま通ります） | **触らない** | **0 行** |

**先に読むこと（自動では読み込まれません）**: `rules/items-survival.md`・`rules/blocks-shapes.md`・
`rules/inventory-screen.md`・`rules/testing.md`。使うスキルは **`add-block`**。

## 3. 使う ID

**1 個。共有帯の次の空き 170**（`ROADMAP.md` の予約表。**低帯 57..63 は階段の予備なので触らない**）:

| ID | 名前（下の 4） | 何 |
| --- | --- | --- |
| **170** | `BRICK_ITEM`（"レンガ"） | アイテムだけ（`block: AIR` / `tool: null`） |

- **定数名を `BRICK` にしないこと** —— `blocks.ts` の `BRICK`(12) と衝突して、
  `crafting.ts` が両方 import した瞬間に `typecheck` が落ちます（**ここは型が止めてくれる罠**）。
- **この周のあと: 111..255 の空きは 86 → 85・次の空きは 171。低帯の空きは 7 のまま。**
  **`MAX_ITEM_ID` は `CLAY_BALL`(169) → `BRICK_ITEM`(170)**（伸ばし忘れると一覧に出ません）。

## 4. 判断をどのファイルに置くか

- **⚠ 名前は「ブロック = レンガブロック / アイテム = レンガ」にすること**（本家の日本語と同じ）。
  いま `BRICK`(12) の表示名が「レンガ」なので、**そのままだと一覧に「レンガ」が 2 つ並びます。**
  **直すのは `blocks.ts` の `def()` の文字列 1 つだけ**で、**ID は 12 のまま**です ——
  セーブに入るのは番号なので、**名前を変えても既存のセーブは 1 バイトも動きません**
  （**`BRICK` の名前を見ているテストは 1 本もありません**。この周で `grep` 済み）。
- **焼けるかどうかは `smelting.ts` の `SMELTING`**（`[CLAY_BALL, { out: BRICK_ITEM, count: 1 }]`）。
  **`FUEL` には 1 行も足さないこと**（粘土玉もレンガも燃料ではありません。革・粘土と同じ）。
  **`SMELT_TIME` を触らないこと**（10 秒のまま。本家と同じ）。
- **組み直すのは `crafting.ts` の 1 行**（`shape: ["BB","BB"]` / `key: { B: BRICK_ITEM }` /
  `out: BRICK` / `count: 1`）。**2x2 なので作業台が要りません**（粘土・雪ブロックと同じ形）。
- 新しく「確かめられないもの」は増えません（`unverifiable-pair` は不要）。
  **`TUNING.md` に 1 節**: **色 `0xbc5d39`** —— **このループが測って選んだ値**です。
  素直な `0x9c5a3c` は**`BRICK`(12) の `0xa4553f` と 9.9 しか離れません**（判定は 20）。
  赤茶はこの一覧でとても混んでいて（**革 `0xa06a41`・ステーキ `0x8f5230`・作業台 `0x9a6f3e`・
  棒 `0x9a7549`・腐った肉 `0x8a6b4f`・革のズボン `0xb16e51`**）、`0xbc5d39` で**いちばん近い
  相手は `BRICK`(12) 本人で 26.0** です。**測り直して 20 を割ったらずらしてよく、
  ずらした値を `TUNING.md` に書くこと。**

## 5. 書くテスト

**値を出してから判定すること**（`rules/testing.md`）。**既存の形をそのまま使い、新しい入口は
1 つも作らないこと。**

1. **ID と帯**（`test/blocks.test.ts`）: 170 と `itemName(170)`・**空きの数**（低帯 7・共有帯 85）を**出力してから**判定
2. **名前の対**（`test/blocks.test.ts`）: **`blockName(BRICK)` と `itemName(BRICK_ITEM)` を
   並べて出力してから**、「ブロックが**レンガブロック**・アイテムが**レンガ**・**2 つが別の
   文字列**」を判定（**ここが崩れると一覧に同じ名前が 2 つ並びます**）
3. **焼ける**（`test/smelting.test.ts`）: 木炭の節とまったく同じ形で、**`smeltResultOf(CLAY_BALL)`
   の出目と個数を出力してから** `BRICK_ITEM` x1 を判定（**`>= 1` のような数え方をしないこと**）。
   あわせて **`isFuel(CLAY_BALL) === false` と `isFuel(BRICK_ITEM) === false`**
   （燃料の表に紛れ込んでいない。**`fuelTimeOf()` の値を出してから**）
4. **置けず・道具でも食べ物でもない**（`test/items.test.ts`）: 木炭・粘土玉の節と同じ形で
   （`placedBlock` / `toolOf` / `foodOf` / `itemStackLimit` を出力してから）
5. **レシピと道**（`test/crafting.test.ts`）: 盤面 `["BB","BB"]` を **2x2 の枠**で組んで
   `findRecipe()` の**出目と個数を出力してから**判定（`BRICK` 1 個）。さらに
   **粘土 1 個 → 掘って粘土玉 4（`dropOf`）→ 焼いてレンガ 4（`smeltResultOf`）→ 2x2 で
   `BRICK` 1 個**を**1 本の道として出力してから**「サバイバルで `BRICK` に届く」を判定
6. **色の隔たり**（`test/items.test.ts`）: **いちばん近い相手と隔たりを出してから** `>= 20`
   （木炭・粘土玉の節と同じ形）。**`BRICK`(12) との隔たりは別の 1 件で見ること** ——
   一覧で隣り合うのはこの 2 つで、まとめると**どちらが詰まったのか出力から読めません**
7. **数え直し（ゆるめるのではありません。件名に理由を書くこと）**: `test/blocks.test.ts` の
   「111..255 の空きは 86」→ **85**・**共有帯のアイテム 51 個（169 まで）→ 52 個（170 まで）**
   （件名の並びの末尾に「レンガ」を足す）、`test/items.test.ts` の
   `MAX_ITEM_ID === CLAY_BALL` → **`BRICK_ITEM`**・**アイテム 131 種 → 132 種**、
   `test/crafting.test.ts` の「レシピは 69 本」→ **70 本**

## 6. このタスク固有の禁じ手

- **`BRICK`(12) の `DROPS` に 1 行も書かないこと**（いまは行が 0 本で、既定どおり自分が 1 個
  落ちます。書くと**積んだレンガブロックが壊しても戻りません**）。**`FUEL` にも足さない**（上の 4）
- **`BRICK`(12) の ID を動かさない**（変えるのは表示名だけ）。**新しいレンガのブロックを
  作らないこと** —— 12 が既に居ます（**ハーフも階段も取らない**。低帯 57..63 は予備）。
  **粘土・`floorPatch`・`VEINS` にも 1 行も触らないこと**（湧かせるのは 32a で済み）
- **3 列のレシピにしないこと**（2x2 で作れないと、作業台の前でしかレンガブロックに戻せません）
- **既存 ID を振り直さない・`SaveData.version` は 1 のまま・`main.ts` を開かない**

## 7. 終了条件

`npm run typecheck` 緑 / `npm test` **すべて緑**（音の 1 件が跳ねたら走り直す。2 回続けて
赤いときだけ退行）/ `npm run build` 緑 / **`npm run bench` は不要**（生成もメッシュ化も触りません）/
コミット 1 つを `master` へ push / **撮って `Read` で開いて見る**（C-3。
**見た目に出るのはクリエイティブ一覧だけ**なので、`node tools/browsershot.mjs` で
**`#mode` を `click()` → `E` → `.label` が「レンガ」と「レンガブロック」の枠を
`scrollIntoView({ block: "center" })`** して 1 枚。**`npm run shot` は不要** ——
新しいブロックが 0 個で、どの場面にも 1 マスも増えません）/ `AUTODEV-QUEUE.md` の 32b を消す /
**`ROADMAP.md` の予約表に 1 行**（170 と「次に取るのは 171」）/ **`TUNING.md` に 1 節**（色）/
このファイルを `状態: 済` にする / `docs/autodev-log.md` に 1 節 / `HANDOFF.md` を書き直す。
