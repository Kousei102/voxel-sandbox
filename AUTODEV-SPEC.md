# 仕様: 音のテストの揺れを測り方で直す（キューの 36・**ID 0 個**）

状態: 済
差し戻し: 0 回

**先に読むこと**: `rules/testing.md`（とくに 164..197 行の「揺れる一群」）/ `rules/audio.md`（全部）。
**スキルは使いません**（`add-block` も `add-stateful-block` も `unverifiable-pair` も当たらない ——
足すものが無く、対の形も変わらないため）。

## この周の前に分かったこと（2026-09-19 の B の周で実測。**推測ではありません**）

**揺れの正体は「ノイズがたまたま明るく出る」ではありませんでした。**
`test/audio.test.ts` が**借り物の配列を持ち回っている**のが原因です。

- `bench().wave()` は `buffer.getChannelData(0)` を**そのまま**返します。
  `node-web-audio-api` のこれは**ネイティブ側のメモリを指す借り物**で、
  `AudioBuffer` を誰も持っていないので、**次の描画がその領域を上書きします**
- だから**測るのを後回しにした波形だけが化けます**。化け方は 3 通りで、どれも実測しました:
  **爆発**（ピーク 1e7〜1e21）/ **無音**（明るさ 0.000）/ **混線**（地上と水中が同じ 0.461）
- **表の 15 本は 225 回撮って 1 度も化けませんでした** —— あそこは撮ってすぐ測り、
  配列を持ち回らないからです。**化けるのは後で比べる 9 本だけ**（羊毛・雪・砂・
  地上・水中・石・咀嚼・低い声・高い声・音量 2 本）という偏りが、この診断の裏付けです
- **中央値も撮り直しも効きません**（両方やって実測）。化けは独立ではなく、
  **撮り直すほど描画が増えて悪くなります**（中央値 3 発で 6 回中 1 回赤、
  「同じ波形が 2 回出るまで」で 16 回中 4 回赤 —— **素の 15 回中 2 回より悪い**）
- **写しを取るだけで消えました**: `return new Float32Array(buffer.getChannelData(0))` の
  1 行で、**50 回連続で緑**・値は毎回ビット同一（羊毛 0.552 / 雪 0.716 / 砂 2.235 /
  低い声 0.162 / 高い声 0.329 / 音量 5.00 倍 / 地上 2.224 / 水中 0.461 / 咀嚼 0.555）

## 1. 何をするか / 完了の判定

**`test/audio.test.ts` の `wave()` が、描画した波形の写しを返すようにする。**

完了の判定:

- `npm test` の「音（実際に鳴らして波形で見る）」が **11 件から 12 件に増えて緑**
  （足す 1 件は下の 5.）
- **その場で 30 回続けて走らせて 30 回とも緑**（下の「確かめ方」）。
  **1 回でも赤いなら直っていません** —— 判定をゆるめるのではなく、原因を見直すこと
- 上に並べた 9 つの値が**毎回同じ**であること（`npm test` の出力に出ます）

## 2. 触るファイル / 触らないファイル

| 触る | 何をするか |
| --- | --- |
| `test/audio.test.ts` | `wave()` で写しを取る（+ 5. の 1 件を足す） |
| `rules/testing.md` | 164..197 行を書き直す（**「15 回に 2 回・原因不明」はもう嘘になります**） |
| `rules/audio.md` | 26..28 行の「1 度だけ落ちた」の段を、原因が割れた形に直す |
| `docs/autodev-log.md` | 1 節（C-4） |
| `AUTODEV-QUEUE.md` / `HANDOFF.md` | 済んだ行を消す / 丸ごと書き直す |

**触らないこと**: **`src/**` は 1 行も触りません**（`src/audio.ts` も `src/sfx.ts` も
正しく、化けているのはテスト側の持ち方だけ）。**`npm run build` も `npm run bench` も要りません。**
`test/` の他のファイル・`test/harness.ts`・`test/arena.ts` も触らないこと。

## 3. 使う ID

**0 個。** ブロックもアイテムも足しません（`ROADMAP.md` の予約表は変えない）。

## 4. 判断をどこに置くか

**変わりません。** 確かめられないもの（`audio.ts`）と判断（`sfx.ts`）の対はそのままで、
この周が触るのは**測る側だけ**です。**`wave()` に判断（閾値・材質・音の種類）を
一切書かないこと** —— 写しを取る以外のことをさせない。

## 5. 書くテスト（**足すのは 1 件だけ**）

**既存の 11 件は 1 文字も変えないこと**（判定も、比べる相手も、出力の書式も）。

足すのは **「同じ呼び方なら同じ波形が出る」** の 1 件です。`step`/`stone` を 2 発撮り、
**ビット同一か**を見て、**違ったサンプルの数と最初の位置を出してから**判定します。

- これが**今回の不具合をそのまま捕まえる見張り**になります（借り物に戻すと、
  1 発目が 2 発目の描画で上書きされて必ず割れます）
- **この 1 件も 30 回連続で緑でなければ入れないこと。** 緑にならないなら
  「正常な描画はビット同一」という前提が崩れているので、**判定をゆるめず、
  その事実を `docs/autodev-log.md` と `rules/testing.md` に書いて 1 件足さずに閉じること**

## 6. このタスク固有の禁じ手

- **判定をゆるめない。** 不等号・係数・比べる相手・`AUDIBLE` を動かさない
- **外れ値を閾値で捨てない**（爆発は捕まりますが、**無音と混線は閾値をすり抜けます**）
- **中央値・平均・窓の長さで均さない**（`AUTODEV-QUEUE.md` の 36 行目が挙げていた
  手ですが、**実測で効かないと分かりました**。悪くなります）
- **撮り直しを足さない**（描画が増えるほど化けます）
- **`src/audio.ts` を「安全側に」直さない。** あちらに非はありません
- **`skip` で黙らせない・`test/run.ts` から外さない**

## 7. 終了条件

- `npm run typecheck` 緑 / `npm test` 緑（**音は 12 件**・全体は **3837 件 + 1**）
- **30 回連続で緑**を確かめて、その回数を `docs/autodev-log.md` に書く
- **コミット 1 つ**を `master` へ push
- `rules/testing.md` と `rules/audio.md` を上の 2. のとおり直す（**C-4 の「据える」はこれ**）
- **`TUNING.md` は触らない**（手触りの数値ではありません）
- **絵は撮りません**（見た目に出るものを 1 つも触らないため。C-3 は当たりません）

## 確かめ方（そのまま使えます）

```bash
npm test                      # まず 1 回
for i in $(seq 1 30); do npm test >/dev/null 2>&1 || echo "run $i RED"; done
```

**30 回が重いなら、音のテストだけを束ねて回してよい**（B の周はそうしました）——
`test/audio.test.ts` の `run()` と `test/harness.ts` の `summary()` を呼ぶだけの
入口を**スクラッチパッドに**書き、`esbuild --external:node-web-audio-api` で
**`node_modules/.cache/` へ**出して走らせます（**出力先がそこでないと
`node-web-audio-api` を見つけられずスキップになります**）。1 回 0.25 秒です。
