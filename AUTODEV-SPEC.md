# 仕様: 道具の修理（キューの 28・ID 0 個）

状態: 済
差し戻し: 0 回

**取る前にコードで数え直しました**（B の周の決まり）: `src/**` に **`repair` も `修理` も
0 件** / `RECIPES` は **65 本で、形なしは 11 本**（**2 枠で成立する既存レシピは棒と
シアーズの 2 本だけ**で、材料は板と鉄インゴット —— **どちらも傷を持たないので
修理とはぶつかりません**）/ `findRecipe()` の呼び先は **`craftscreen.ts` の 3 か所だけ**
（`takeResult()` 725 / `quickCraft()` 748 / `result()` 814）/ **傷を持てるのは
`wearable()` が真のものだけ** = **実測 23 種類**（道具 20 本・火種 64 回・弓 384 回・
シアーズ 238 回）。

**⚠ 防具は傷を持ちません。** `maxUses()` は `toolOf()` と火種・弓・シアーズしか見ないので、
革の防具 4 部位は `wearable()` が偽です。**キューの行名は「道具と防具の修理」ですが、
この周では防具に耐久値を足しません**（`ARMORS` に回数を持たせる別の話で、
120 行に収まりません）。**`wearable()` で書けば、防具が傷を持った日に 0 行で効きます。**

## 1. 何を足すか / 完了の判定

**同じ道具を 2 つ盤面に並べると 1 つになり、傷が減る**（本家のクラフト修理）。
**2x2（手持ち）でも 3x3 でも作れる**（2 枠しか使わないので作業台が要りません）。

完了 = `npm test` に次の項目が増えて**全部緑**（いま **3472 件**）:

- 「修理で戻る傷の量」を **7 種類ぶん実数で出してから**判定（木 59 / 石 131 / 鉄 250 /
  ダイヤ 1561 / 弓 384 / シアーズ 238 / 火種 64。**ボーナスは切り捨てで 2/6/12/78/19/11/3**）
- 「新品 2 本を合わせても最大で頭打ち（＝1 本まるごと損）」「傷まない物（棒）は 0」
- 「盤面が修理の形か」を **6 通り**で: 同じ傷んだ道具 2 つ ○ / 違う道具 2 つ × /
  棒 2 個 × / 同じ道具 3 つ × / 1 つだけ × / **2x2 でも 3x3 でも ○**
- **「既存 65 本が今までどおり返る」**（`findCraft()` と `findRecipe()` を**並べて**測り、
  65 本すべてで `out` / `count` が一致し `damage` が 0 であること）
- 「盤面から受け取ると傷が載り、盤面が空になる」「シフトクリックでも傷ごと入る」
  「掴んでいる山が同じ道具なら受け取れない」（`stack: 1` なので積めない）

## 2. 触るファイルと、触らないファイル

| ファイル | やること |
| --- | --- |
| `src/durability.ts` | `REPAIR_BONUS = 0.05` と `repairedDamage(item, a, b)` の**純関数 1 本** |
| `src/crafting.ts` | `Craft` 型 1 つ / `findCraft(grid, size)` 1 本（中で修理を見る）。**`import { damageOf, maxUses } from "./durability"`**（輪はできません） |
| `src/craftscreen.ts` | `findRecipe` → `findCraft` に **3 か所**差し替え / 傷を載せる **3 行** |
| `test/durability.test.ts` / `test/crafting.test.ts` / `test/craftscreen.test.ts` | 上の項目 |

**触らないファイル**（1 文字も開かないこと）: **`src/main.ts`**（**±0 行**。作る経路は
`craftscreen.ts` 1 本で、`main.ts` は画面を開け閉めするだけ。**いま 1450 行で止まる目安に
並んでいます**）/ **`src/inventoryui.ts` / `src/ui.ts` / `src/style.css`**（**±0 行** ——
`paintSlot()` は `Slot` を取るので、`result()` が `damage` を返せば**帯は勝手に出ます**）/
`src/items.ts` / `src/inventory.ts` / `src/storage.ts` / `src/smelting.ts`。

## 3. 使う ID

**0 個。** ブロックもアイテムも 1 つも足しません（**`npm test` の「111..255 の空き」が
**93 のまま**・「1..63 の空き」が **9 のまま**であること。減っていたら番号を取った合図）。
**`RECIPES` も 65 本のまま**です（`test/crafting.test.ts:711` が数えています）。

## 4. 判断をどのファイルに置くか

**2 つに割ること。**「**何回ぶん戻るか**」は `durability.ts`（傷の持ち主）、
「**盤面が修理の形か**」は `crafting.ts`（盤面の持ち主）。**片方に寄せないこと** ——
`crafting.ts` に割合を書くと傷の丸め方が 2 か所に分かれ、`durability.ts` に `Slot[]` を
読ませると `crafting.ts` → `durability.ts` の向きが逆流します。

**本家のまま**の値（手触りは人が決めるので `TUNING.md` に 1 行）:

| もの | 値 | なぜ |
| --- | --- | --- |
| 戻る量 | `残りA + 残りB + floor(最大 × 0.05)` を最大で頭打ち | 本家のクラフト修理の式 |
| 出来る数 | 1 個（`count: 1`） | 2 本が 1 本になる |
| 名前 | `itemName(item)` そのまま | 「（修理）」を付けないこと（結果の行が伸びます） |
| 要る盤面 | 2 枠だけ。**2x2 でも成立** | 本家と同じ（作業台が要りません） |

- **新品 2 本でも成立します**（本家のまま。1 本が丸損になるだけ）。**止めないこと** ——
  止めると「傷んでいるか」の判断が `crafting.ts` に増えます
- **`findRecipe()` を先に見て、当たらなかったときだけ修理を見ること。** 形ありレシピと
  ぶつからないのは数え済みですが、**順番を逆にすると将来ぶつかったときに静かに壊れます**
- **`consumeGrid()` はそのまま使えます**（1 スロット 1 個。道具は `stack: 1` なので
  2 枠とも空になります）。**書き換えないこと**

## 5. 書くテスト

**値を出してから判定する形**（`rules/testing.md`）。

1. `repairedDamage()` の表を **7 種類 x 3 通り**（新品 2 本 / 半分 2 本 / 満身創痍 2 本）
   **印字してから**判定。**ボーナスの実数も出すこと**（木 2 / ダイヤ 78）
2. `findCraft()` を **既存 65 本すべてに掛けて** `findRecipe()` と突き合わせ、
   **食い違い 0 件**を数字で出す（1 本でもずれたら退行）
3. 修理の 6 通り（上の 1.）。**null になる側も理由ごとに 1 件ずつ**
4. `CraftScreen` で受け取る 3 通り。**受け取る前と後の `damage` を両方印字すること**
5. **既存の判定を 1 つも書き換えないこと** —— `findRecipe()` を直接呼ぶ既存 20 件以上は
   そのまま緑であるはずです（落ちたら `findRecipe()` の振る舞いを変えた合図）

## 6. このタスク固有の禁じ手

- **`main.ts` を 1 文字も開かないこと**（±0 行。1 行でも要るなら止まる合図）
- **`inventoryui.ts` / `ui.ts` / `style.css` を 1 行も触らないこと**（帯は勝手に出ます）
- **`findRecipe()` の戻り値・引数・振る舞いを変えないこと**（既存テストが直に呼びます）
- **`Recipe` に列を足さないこと**（レシピ表は静的な表で、傷はその場ごとの値です。
  ミルクバケツの残りかすで同じ判断をしています。`crafting.ts` の `consumeGrid()` の上）
- **`RECIPES` に行を足さないこと**（65 本のまま。修理は表に書ける形ではありません）
- **`TOOL_USES` / `maxUses()` / `wearSlot()` / `wornValue()` を書き換えないこと**
- **5% を別の値にしないこと**（本家のまま。変えたくなったら `TUNING.md` へ）
- **防具に耐久値を足さないこと**（この周の外。上の ⚠）
- **`SaveData` は 1 バイトも増えません**（`version` は 1 のまま。傷の 5 つの入れ物は既にあります）
- **既存の判定をゆるめて緑にしないこと**（とくに `test/world.test.ts` の p99）

## 7. 終了条件

- `npm run typecheck` 緑 / `npm test` **全部緑**（音の一群が赤いときはもう一度走らせる）
- `npm run build` 緑（`src/**` を触るので必須）。`npm run bench` は不要（生成もメッシュ化も触らない）
- **撮ること**: 見た目のファイルは ±0 行ですが、`node tools/browsershot.mjs` を 1 回走らせ、
  **`inventory.png` を `Read` で開いて今までどおり出ること**だけ見ること（**差分が出たら
  `paintSlot()` に何か流れ込んでいる合図**）。`npm run shot` は不要（DOM は写りません）
- `TUNING.md` に 1 行（**ボーナス 5%・新品 2 本は 1 本が丸損・修理済みの印は付かない**）
- コミット 1 つ / `AUTODEV-QUEUE.md` の 28 の行を消す / この仕様書を `状態: 済` に /
  **`ROADMAP.md` は触らない**（ID を 1 つも使いません）
- 使えるスキル: **なし**（ID 0 個なので `add-block` は要りません）。読む `rules/`:
  **`inventory-screen.md` / `items-survival.md` / `drops.md` / `testing.md`**
