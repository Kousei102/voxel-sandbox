# 仕様: ツタの自然生成（キューの 34c・**ID 0 個**）

状態: 未着手
差し戻し: 0 回

**先に読むこと**: `rules/worldgen.md`（全部）/ `rules/blocks-shapes.md` の支えの節 /
`rules/testing.md` / `rules/meshing-render.md`（場面を撮る所だけ）。
**自動では読み込まれません。** スキルは使いません（ID を 1 個も取らないので `add-block` は要りません）。

## 1. 何を足すか / 完了の判定

**森（`FOREST`）の木の葉に、ツタ（183..186）が自然に掛かって下へ垂れる。**
いまツタは手で置くしか出どころがありません（`worldgen.ts` にも `biomes.ts` にも 0 行）。

完了の判定は `npm test` に次の項目が増えて緑になること（数字は実装が測って出す）:

- 「森にツタが生えている」— まとまった森を探して**本数と、木 1 本あたりの割合**を出してから判定
- 「**生えたツタは 1 本残らず支えを持っている**」— 0 件を出してから判定（いちばん強い見張り）
- 「ツタの向きが壁の側と合っている」— `vineVariant()` の答えと突き合わせる
- 「森でないバイオームには 1 本も生えない」— 平原・砂漠・海で 0 件
- 「生成の順に依らない」— 隣の列を先に作っても後に作っても同じ（`applyTrees` と同じ形）

## 2. 触るファイル / 触らないファイル

| ファイル | 何をするか |
| --- | --- |
| `src/treeshape.ts` | **`vineCells(kind, height, x, z)` を 1 本足す**（純関数。既存の `treeCells()` は 1 文字も触らない） |
| `src/biomes.ts` | `BiomeDef.vine`（0..1）を足し、**11 行すべてに値を書く**。森だけ 0 より大きい |
| `src/worldgen.ts` | `Tree` に旗を 1 つ持たせ、`stampTree()` の末尾で `vineCells()` を書き込む |
| `tools/shot.ts` | 場面 `vine` を「森の木から垂れているところ」に差し替える（C-3） |
| `test/worldgen.test.ts` | 上の 5 件 |
| `test/treeshape.test.ts` | `vineCells()` の形（純関数として直に呼ぶ） |

**触らないこと**: `src/main.ts`（**0 行。1450 行のまま**）/ `src/blocks.ts` / `src/items.ts` /
`src/crafting.ts` / `src/world.ts` / `src/crops.ts` / `src/placing.ts` / `ROADMAP.md` の予約表。

## 3. 使う ID

**0 個。** 既にある `VINE`(183) / `VINE_XN`(184) / `VINE_ZP`(185) / `VINE_ZN`(186) を置くだけです。
**新しい番号を取らないこと**（取りたくなったら設計が違う信号）。空きは 111..255 が 69・1..63 が 7 のまま。

## 4. 判断をどこに置くか

- **「どれだけ生えるか」は `biomes.ts`**（`BiomeDef.vine`）。`worldgen.ts` に確率の数値を書かないこと。
  **`?:`（省略可）にしないこと** —— `seaSurface` / `floorPatch` と同じで、足し忘れが黙って通ります
- **「どのマスに何を置くか」は `treeshape.ts`**（`vineCells()`）。**形を `worldgen.ts` に写さないこと** ——
  葉の形は `treeCells()` だけが持つ決まりです（`rules/worldgen.md`）
- **`vineCells()` は `treeCells(kind, height)` の答えから導くこと。** 葉の座標を手で書き直すと、
  葉の形を変えた日にツタだけが取り残されます。**置けるのは「`treeCells()` に無いマスで、
  横 4 方向のどれかが葉のマス」**（オークなら落とした 4 隅、トウヒなら細い段の横）
- **向きは `vineVariant(face)` に聞くこと**（`blocks.ts`）。`VINE_XN` などを直に書かないこと
- **`Math.random()` を使わないこと。** `treeshape.ts` は既存の `hash2(x, z, salt)`、
  `worldgen.ts` は `hash2(x, z, seed ^ 塩)`。**塩は既存 7 本と重ねないこと**
  （`0x7c39` / `0x4d17` / `0x2f8b` / `0x6a55` / `0x5b27` / `0x3d15` / `0x6e83`）

## 5. 書くテスト（`rules/testing.md`・**値を出してから判定する**）

- **森は原点のまわりに偏って出ません。** `gen.biomeAt()` で**まとまった森（64x64）を先に探し、
  そこを 1 マスも飛ばさずに数えること**（キノコの数え方が手本。`rules/worldgen.md`）。
  **走査を広げて数を稼がないこと** —— 点数がそのまま `npm test` の秒数になります
- **支えの見張りが本体です。** 見つけたツタ 1 本ごとに、**横 4 方向に `canSupport()` の支えがあるか、
  真上が同じ大元のツタか**を確かめる（`World.canPlaceAt()` と同じ理屈を手で書く）。
  **0 件を出してから判定すること**（「1 本も見つからなかった」でも緑になる形にしないこと ——
  必ず「本数 > 0」を別の 1 件に置く）
- `vineCells()` は純関数なので、`test/treeshape.test.ts` から**直に呼んで**形を見る
  （オークとトウヒの両方。**`|dx|` と `|dz|` が `TREE_RADIUS` を超えない**ことも）
- **既存の「木の形」の見張りをゆるめないこと。** `treeCells()` の答えが 1 マスも動いていないことを、
  いまあるテストがそのまま通るかで確かめる

## 6. このタスク固有の禁じ手

- **`treeCells()` の並び・`overwrite` の真偽・葉の座標を 1 つも変えないこと。** 変えると
  **既存のワールドの木が動き**、セーブの差分が別の場所を指します（`rules/worldgen.md`）
- **`TREE_RADIUS`(2) を超えた所にツタを書かないこと。** `applyTrees()` は自分と隣接 8 列しか
  スタンプし直さないので、超えたぶんは**隣の列の生成時に黙って欠けます**
- **木の根元（`tree.y`）より下へ垂らさないこと。** 地面にぶつかったぶんは `put()` の
  `overwrite: false` に弾かれて**宙に浮いた切れ端**になります。長さは上限を決めて、
  **`dy - 長さ >= 0` のマスからしか垂らさないこと**
- **`stampTree()` の中で `heightAt()` や `getVoxel()` を呼ばないこと**（列のキャッシュの外を見ない）
- **`crops.ts`（苗木から育つ木）に手を入れないこと。** この周では**植えた木にツタは掛かりません**
  —— 意図した違いなので `HANDOFF.md` に 1 行残すこと
- **`biomes.ts` に `VINE` を import しないこと**（`ICE` / `CLAY` と同じ決まり。確率だけを持つ）

## 7. 終了条件

- `npm run typecheck` 緑 / `npm test` **すべて緑**（判定を 1 件もゆるめない）/ `npm run build` 緑
- **`npm run bench` を 3 回まわして中央値を見る**（`stampTree` に仕事が増えるため。
  `generateChunk` はこの箱で 0.75〜1.40ms に振れるので、1 回の値で判断しないこと）
- **撮って自分の目で見る**（C-3）。`npm run shot -- vine terrain` と
  `node tools/browsershot.mjs`。**`Read` で開いて、垂れたツタが森の木に掛かっているか・
  面の欠けと裏返りが 0 件かを確かめること**
- コミット 1 つ / `AUTODEV-QUEUE.md` の 34c の行を消す / この仕様書を `状態: 済` に
- **手触りの数値（生える割合・垂れる長さ）は `TUNING.md` に 1 行**。本家の値が分からない所は
  **暫定で入れて止まらずに進むこと**
- `docs/autodev-log.md` に 1 節 / `HANDOFF.md` を丸ごと書き直す / `master` へ push

## ⚠ キューの 34c の書き方は 1 か所間違っています

キューは「**葉の下に 1 マス置いて、そこから下へ伸ばす**」と書いていますが、
**葉の真下にツタは置けません。** `supportsBlock()` が真上を支えと認めるのは
**`baseBlock(supporter) === baseBlock(id)`（＝真上も同じツタ）のときだけ**で、葉は通りません
（`blocks.ts` の `hangsBelow` の行）。**1 本目は必ず「横が葉」の壁掛け**で、
**2 本目から下が「真上のツタ」にぶら下がるぶん**です。ここを読み違えると、
**生成では置けているのに、隣を 1 つ壊した瞬間に列ごと消えるツタ**になります
（`World.breakUnsupported()` が `canPlaceAt()` に聞き直すため）。
