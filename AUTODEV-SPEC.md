# 仕様: フェンス（キューの 26a・置ける・跳び越えられない）

状態: 済
差し戻し: 0 回

**キューの 26 を 2 件に割った前半です**（120 行に収まらないため。取りやすさの話ではありません）。
後半 **26b は「隣に何も無い側の腕を描かない」**（`mesher.ts` だけ）で、キューへ書き戻しました。
**この周の見た目は「柱 1 本 + 4 方向の腕」を常に描く形**で、`mesher.ts` は 1 行も触りません。
**`main.ts` も ±0 行**（この周に数えました。置く経路も壊す経路も一覧も既にあるものを通ります）。

## 1. 何を足すか と 完了の判定

**フェンス（ブロック + アイテム・157）。棒 6 本で 2 個。歩いて通り抜けられず、跳んでも
越えられない**（当たり判定だけ**マスいっぱい × 高さ 1.5**。見た目と狙いは高さ 1.0）。

`npm test` が緑のまま（いま **3332 件**）、次の 7 つが**値を出してから**増えていること:

- **置いて壊せる**（壊すと自分が 1 個。`DROPS` は 0 行 —— 既定が `baseBlock()`）
- **形が 2 本立ちしている**: `shapeBoxes(FENCE)` が **9 個**（柱 1 + 腕 8）で**上端 1.0**、
  `collisionBoxes(FENCE)` が **`[[0,0,0,1,1.5,1]]` の 1 個**
- **跳んでも越えられない**: 本物の `Player` を `JUMP_SPEED`（9.2）で跳ばせ、
  **足の y の最大**を出してから `< 1.5` を判定（`GRAVITY` 30 で **1.41 m**。余裕は 0.09）
- **横から歩いて通り抜けられない**（フェンスの列へ 1 秒歩かせて、向こう側へ出ないこと）
- **上に立ってもガタつかない**: フェンスの真上に落として `onGround` と y が
  **2 フレーム続けて 1.5 のまま**（1 段下を見ないと 0.5 落ちて跳ね上がる。下の 4.）
- **レシピが 61 本目**（棒 6 → フェンス 2）・**枠**: `111..255` の空きが **99 → 98**・
  `MAX_ITEM_ID` **156 → 157**・アイテム **118 → 119 種**・非立方体 **78 → 79**（立方体 41）

## 2. 触るファイルと、触らないファイル

**触る**: `src/blocks.ts` / `src/physics.ts` / `src/items.ts`（`MAX_ITEM_ID` の 1 行だけ）/
`src/crafting.ts`（レシピ 1 行）/ `test/blocks.test.ts` / `test/physics.test.ts` /
`tools/shot.ts`（場面 1 つ）/ `ROADMAP.md` / `TUNING.md` / `rules/blocks-shapes.md` /
キューとこの仕様書 / `docs/autodev-log.md` / `HANDOFF.md`

**触らない**: **`src/main.ts`（1 行も開かないこと）** / **`src/mesher.ts`（26b の持ち物）** /
`player.ts` / `mobs.ts` / `world.ts` / `worldgen.ts` / `placing.ts` / `breaking.ts` / `drops.ts`

**先に `rules/` を引いて読むこと**（自動では読み込まれません）。触るファイルごとに
`grep -l '"src/physics.ts"' rules/*.md` を引き、**当たったものを全部**。確実に当たるのは
**`blocks-shapes.md`**（形・箱・ID の帯）/ **`items-survival.md`** / **`meshing-render.md`**
（`tools/shot.ts`）/ **`testing.md`**。**スキルは `add-block`**（`unverifiable-pair` は不要）。

## 3. 使う ID

**157 を 1 つ**（`ROADMAP.md` の予約表「157..255 予備」の先頭）。**111 以降はブロックと
アイテムで 1 本の番号列**なので、この 157 が**ブロックでもありアイテムでもあります**。
アイテムは `items.ts` に 1 行も書かずに付いてきます（`variantOf` が `AIR`）が、
**`MAX_ITEM_ID` だけは手で `= FENCE` に伸ばすこと**（氷と同じ。忘れると一覧に出ません）。
**予約表のこの行を「実装済み」に書き換えること**（次に取るのは 158）。

## 4. 判断をどのファイルに置くか

| 何を | どこに |
| --- | --- |
| 見た目と狙いの形（柱 + 腕 8 本） | `blocks.ts` の `FENCE_BOXES`（`model: "boxes"`） |
| **当たり判定だけ別の形** | `blocks.ts` の**新しい `BlockDef.collision`** |
| 1 マスより高い箱が下から届くこと | `physics.ts` の `collides()` が**1 段下も見る** |
| どのブロックが高いか | `blocks.ts` の `isTallCollision()`（表 1 本） / レシピは `crafting.ts` |

- **`collision` を `BlockDef` の必須の項目にし、`def()` の既定を `boxes` そのものにすること**
  （`const boxes = opts.boxes ?? FULL_BOX;` → `collision: opts.collision ?? boxes`）。
  `?:` にすると足し忘れが黙って `undefined` になります。**`collisionBoxes()` だけが
  `collision` を引き**、`shapeBoxes()` と `shapeBounds()` は今までどおり `boxes`。
  **これは「形は 3 つの用途を兼ねる」の唯一の例外です**（本家も見た目 1.0・当たり 1.5）
- **当たり判定を柱の太さ（0.25）にしないこと** —— `collisionBoxes()` は座標を知らないので
  腕を隣で出し分けられず、柱だけだと**フェンスの列のあいだを歩いて抜けられます**
- **`isTallCollision()` は手で旗を書かず、`collision` の最大 y > 1 から表を立てること**
  （`BY_ID` の for の中。`SLIPPERY` などと同じ形。2 か所に書くと必ず食い違います）
- **`collides()` の直し方**: `by` の下限を `Math.floor(py0) - 1` にし、**その 1 層だけは
  `isTallCollision(id)` でないマスを飛ばす**こと（`world.getVoxel()` を 1 回に減らしてから
  `collisionBoxes(id)` を回す）。**押し戻しの式・EPS・評価順は 1 文字も変えないこと**
- **形の数値**（本家と同じ 16 分の 1 刻み）: 柱 `[0.375, 0, 0.375, 0.625, 1, 0.625]` / 腕は
  4 方向 x 2 段（高さ `0.375..0.5625` と `0.75..0.9375`・幅 `0.4375..0.5625`）で**柱の外側から
  マスの端まで**。**当たり判定は `[[0, 0, 0, 1, 1.5, 1]]` の 1 個だけ**
- **色は `0x988a5e`**（1 色）。**板の `0xb18a56` は使えません** —— 一覧の「既存のどの
  アイテムからも 20 以上」に落ちます（板から 7.1）。`0x988a5e` は板から **26.2**
- `hardness: 2` / `tool: "axe"` / `sound: "wood"` / `opaque: false` / `model: "boxes"`。
  **`blocksSky` は書かない**（既定で false。本家どおり下は暗くなりません）

## 5. 書くテスト

**どれも値を出してから判定すること**（`rules/testing.md`）。

- `test/blocks.test.ts` に**フェンスの節を 1 つ**（氷・ケーキの節を写す）: 形の箱 2 種類
  （**個数と上端を出す**）/ 置いて壊すと自分が 1 個 / 硬さ・道具・音・`solid` / **一覧の色が
  どのアイテムからも 20 以上**（距離を出してから）/ **`collision` が違うのはフェンスだけ**
- `test/physics.test.ts` に **3 件**: 跳んでも越えられない（**足の y の最大を出す**）/
  横から歩いて通り抜けられない（**到達した x を出す**）/ **上に立ってガタつかない**
  （**2 フレームぶんの y と `onGround` を出す**）。**本物の `World`**（`test/arena.ts`）で
- **既存の判定を 1 つもゆるめないこと**（軌跡の値も色の 20 もそのまま。`git diff test/` を読む）

## 6. このタスク固有の禁じ手

- **`main.ts` と `mesher.ts` を開かないこと**（腕を隣で出し分けるのは 26b）
- **`boxes` のほうを 1.5 にしないこと**（狙う判定も選択枠も 1.5 になり、**空中を狙って
  いるのにフェンスに当たります**）・**`collision` をフェンス以外に書かないこと**
- **1 段下の走査を全部のブロックへ広げないこと**（`collides()` は毎フレーム体ごとに 3 回。
  **`isTallCollision()` で弾いてから**箱を回すこと）
- **`supportFace` を書かない**（本家どおり宙に浮きます）・**`stacksOnSelf` /
  `replaceable` / `variantOf` / `spiky` / `sticky` を書かない**
- **ゲート（開く扉）・材質違い（石・ネザー）を足さないこと**（別の周）・**`DROPS` にも
  `FOODS` にも精錬にも燃料にも自然生成にも 1 行も足さないこと**
- **`SaveData` の形を変えないこと**（`version` は 1・キーも増やさない）
- **`JUMP_SPEED` / `GRAVITY` / `STEP_HEIGHT` を触らないこと**（余裕は 0.09 だけ。跳躍を
  上げるならフェンスの 1.5 も一緒に見直す話で、この周ではやりません）

## 7. 終了条件

`npm run typecheck` と `npm test`（3332 件 + 増えたぶん）が**すべて緑** / `npm run build` 緑 /
**撮って `Read` で見た** —— **フェンスは自然生成しないので `browsershot` には 1 マスも
写りません**。**`tools/shot.ts` に場面を 1 つ足して `npm run shot -- fence`**（`cake` /
`ice` の場面を写す。平らな台に**直線・角・1 本だけ**の 3 通り。見るのは**腕が 4 方向に
出ていること**と**面の欠け・裏返り**）/ `TUNING.md` に 1 行（色 `0x988a5e` と、当たり判定
1.5 と跳躍 1.41 の余裕 0.09）/ `ROADMAP.md` の 157 を「実装済み」に /
`AUTODEV-QUEUE.md` の 26a を消す / このファイルの `状態:` を `済` に /
**例外を `rules/blocks-shapes.md` へ据える** / `docs/autodev-log.md` に 1 節 /
`HANDOFF.md` を丸ごと書き直す / **コミット 1 つを `master` へ push**。
