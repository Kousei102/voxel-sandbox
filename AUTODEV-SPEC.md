# 仕様: スケルトンと骨（キューの 11・ID 1 個）

状態: 済
差し戻し: 0 回

**取る前にコードで数え直しました**（B の周の決まり）: `MobKind` は **9 種類**で
`"skeleton"` はありません / `src/**` に **`骨` も `BONE` も 0 件**（`grep` で確認）/
一方 **`projectiles.ts` の `"arrow"`（速さ 40・重力 1・`onBlock: "stick"`）と
`MobDef.ranged` の仕掛け（`fire()` / `clearShot()` / `shootTimer`）はブレイズで
全部通っています**。つまり**残っているのは表 2 つ（`MobDef` 1 件・`item()` 1 行）だけ**で、
**新しい飛び方も新しい AI も 1 つも要りません。**

## 1. 何を足すか / 完了の判定

**暗い所に湧いて矢を撃ってくる敵対モブ「スケルトン」と、倒すと落ちる「骨」を足す。**
**矢が 2 山目で落ちるので、弓の矢がここで初めて「稼げる」ようになります。**

完了 = `npm test` に次の項目が増えて**全部緑**（いま **3424 件**）:

- 「スケルトンは `MOB_KINDS` に居て、敵対で、`ranged.kind` が `"arrow"`」
- 「10m から矢を撃つ（重み 3）」— ブレイズの火球の判定と**並べて**測ること
- 「壁越しには撃たない」「湧いた直後の初弾は 1 回ぶん待つ」
- 「倒すと骨が必ず 1 個、矢が半分の確率で 2 山目」——
  **殴った側（`attack()`）と撃った側（`hitByProjectile()`）を並べて**測ること
- 「骨は既存のどのアイテムとも一覧で見分けられる（RGB で 20 以上）」
- 「骨は置けず・道具でもなく・食べ物でもない」
- **既存の直し 4 件**（下の 5.）が、ゆるめずに数え直されて緑

## 2. 触るファイルと、触らないファイル

| ファイル | やること |
| --- | --- |
| `src/mobs.ts` | `MobKind` に `"skeleton"` / `SKELETON: MobDef` 1 件 / `MOBS` と `MOB_KINDS` に 1 語ずつ |
| `src/items.ts` | `BONE = 162` と `item()` 1 行、**`MAX_ITEM_ID` を `BONE` へ** |
| `test/mobs.test.ts` / `test/items.test.ts` | 上の項目と、既存 4 件の数え直し |

**触らないファイル**（1 文字も開かないこと）: **`src/main.ts`**（**±0 行**。撃つ受け口も
当たり先もブレイズで繋がっています。**いま 1450 行で止まる目安に並んでいます**）/
`src/mobrender.ts` / `src/mobmesh.ts`（**モブ 1 種類に 0 行**。`rules/mobs.md`）/
`src/projectiles.ts` / `src/bow.ts` / `src/crafting.ts` / `src/drops.ts` / `src/storage.ts`。

## 3. 使う ID

**1 個。162 = 骨**（`ROADMAP.md` の予約表の次の空き）。**`npm test` の「111..255 の空き」が
94 → 93 になること**（それ以外の減り方をしたら番号を余分に取った合図）。
**スケルトンそのものは番号を使いません**（`MobKind` は文字列）。

## 4. 判断をどのファイルに置くか

**全部 `mobs.ts` の表 1 件です。**`update()` にも `step()` にも `fire()` にも
**「スケルトンなら」を 1 行も書かないこと**（飛ぶ・跳ぶ・刈る・搾るとまったく同じ作法）。

暫定の値（本家に寄せたもの。**手触りは人が決めるので `TUNING.md` に 1 行**）:

| 列 | 値 | なぜ |
| --- | --- | --- |
| `size` | `{ half: 0.3, height: 1.99, step: 0.6 }` | 本家と同じ。段差はゾンビと同じ（登れる所には付いてくる） |
| `maxHealth` / `damage` / `speed` | 20 / 2 / 4.6 | 近接はゾンビ並み。**歩き 5.2 より遅い**（振り切れる） |
| `ranged` | `{ kind: "arrow", damage: 3, range: 10, near: 3, cooldown: 2, height: 1.5 }` | 下の ⚠ |
| `spawnWeight` | 100 | 本家と同じ（ゾンビ・クモと並ぶ 3 つ目） |
| `drop` | 骨 1 個 `chance: 1` / `extra` に矢 1 個 `chance: 0.5` | 個数の範囲は持たせない（羽根・革と同じ線） |
| `voice` | 1.3 | ゾンビ 0.7 より乾いた高さ |
| 残りの列 | `teleport` / `spawnOn` / `orbit` / `phases` / `shearing` / `laying` = `null`、`flying` / `fireproof` / `boss` / `milkable` = false、`hover` / `regen` = 0 | **`fireproof: false` がそのまま「朝に燃える」**です |

- **⚠ `range` を 10 より伸ばさないこと。** 矢は**重力を受けます**（`gravityScale: 1`・
  `GRAVITY` 30・速さ 40）。狙いは `PLAYER_AIM`(1.4) の高さへ**まっすぐ**なので、
  落ちる量は `0.5 × 30 × (距離/40)²` —— **10m で 0.94m**（足元から 0.46m に当たる）、
  **16m だと 2.4m でプレイヤーの下を抜けます。** 当たらない飛び道具になります。
  **`fire()` に先読みや山なりを足して伸ばさないこと**（`rules/mobs.md` の
  「狙うのは、いまプレイヤーが居る所」）。
- **本家と違って間合いを取りません**（近づいて殴りもします）。下がりながら撃つのは
  `think` に新しい動き方を足す話なので、**この周では入れません**（`TUNING.md` に 1 行）。
- **骨の色は `0xcdc8b0`**（`0xd8cfae` などは砂と 20.9 しか離れません。**この値でいちばん
  近いのは砂で 24.6**）。**`tool:` を持たせないこと**（羽根・革・糸と同じ罠 ——
  `ToolKind` が増えると `mobs.ts` の `TOOL_ATTACK` に無い種類が入って NaN が黙って通る）。
  `block: AIR` / `stack: MAX_STACK` / `FOODS` にも `SMELTING` にも `crafting.ts` にも 0 行。
- **形の罠 2 つ**（`rules/mobs.md`。**撮るまで気付けません**）: **頭も体も同じ骨色**なので、
  **牛の頭と同じ「前から見ると 1 枚の板」**になります —— **胴を頭より細くして輪郭を出し**、
  顔には暗い目（前面から 0.1px 出した薄い箱）を置くこと。もう 1 つは**当たり判定** ——
  `half: 0.3` は **±4.8px** なので、**腕の外端がここを越えないこと**
  （越えたら**`longBody` に足すのでも `size` を広げるのでもなく、形を削る**）。
  振る部位（腕・脚）の箱は **`y1 === 0`**。

## 5. 書くテスト

**値を出してから判定する形**（`rules/testing.md`）。**既存 4 件は必ず数え直すこと**
（ゆるめない・消さない。`rules/mobs.md` の「敵対を足すと 2 件必ず赤くなる」）:

1. `census()` の `counts` に **`スケルトン: 0` の 1 語**（無いと `counts[name]++` が
   **NaN** になり、数えているつもりで何も見ていない状態で緑になります）
2. 湧きの上限は**敵対の合計**で見ること（`ゾンビ + クモ + スケルトン <= MAX_HOSTILE`）
3. 「ゾンビとクモがほぼ同数で、エンダーマンはその 1/10」→ **3 つが同じ重み 100** に
   なります。**3 つの実測値を出してから**書き直すこと
4. 「敵対はゾンビ・クモ・ブレイズ・エンダーマンの 4 種類」→ **5 種類**へ

## 6. このタスク固有の禁じ手

- **`main.ts` を 1 文字も開かないこと**（±0 行。1 行でも足したくなったら止まる合図）
- **`mobrender.ts` / `mobmesh.ts` / `projectiles.ts` を 1 行も触らないこと**
  （触ったら「表で表せないものを足そうとしている」合図。`unverifiable-pair` は要りません）
- **`MobDef` に列を足さないこと**（`fireproof` を `sunproof` に割らない ——
  9 種類ぜんぶに手が要ります）
- **既存 9 種類の `drop:` / `spawnWeight` / `voice` を書き換えないこと**
- **`projectiles.ts` の `arrow` の行を書き換えないこと**（弓の手触りが変わります）
- **骨にレシピ・精錬・食べ物を付けないこと**（骨粉は別のキューの行です。革・糸と同じ扱い）
- **`SaveData` は 1 バイトも増えません**（モブは保存しない。**`version` は 1 のまま**）
- **既存の判定をゆるめて緑にしないこと**（とくに上の 4 件と `test/world.test.ts` の p99）

## 7. 終了条件

- `npm run typecheck` 緑 / `npm test` **全部緑**（音の一群が赤いときはもう一度走らせる）
- `npm run build` 緑（`src/**` を触るので必須）。`npm run bench` は不要（生成もメッシュ化も触らない）
- **撮ること**（`AUTODEV.md` の C-3）。**`npm run shot -- mobs` で骨の形**を撮って `Read` で
  開き、**頭と胴が分かれて見えるか**を自分の目で見ること（上の「形の罠」）
- `TUNING.md` に 1 行（**`range` 10 / `cooldown` 2 / 矢の重み 3 / 間合いを取らないこと**）
- コミット 1 つ / `AUTODEV-QUEUE.md` の 11 の行を消す / この仕様書を `状態: 済` に /
  **`ROADMAP.md` の予約表の 162 を「実装済み」に**
- 使えるスキル: **`add-block`**（アイテム 1 個）。読む `rules/`: **`mobs.md` /
  `projectiles.md` / `items-survival.md` / `drops.md` / `bow.md` / `testing.md`**
