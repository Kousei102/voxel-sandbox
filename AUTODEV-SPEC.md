# 仕様: クモ（2 種類目の地表の敵対モブ）と糸

状態: 済
差し戻し: 0 回

**キューの 9 番。数え直し済み**（2026-09-04・コードが根拠）: `src/**` と `test/**` に
`spider` / `クモ` / `蜘蛛` / 糸のアイテムは **1 つもなく**（`crafting.ts` と `items.ts` の
「蜘蛛がまだ居ないので羊毛で代用」というコメントだけ）、`MobKind` は **8 語**・`HOSTILE_KINDS`
は表から作られて **3 語**。実装前: **2934 件緑** / **空き 123** / `main.ts` **1463 行**。
**この 1 件は 120 行に収まらなかったので 2 件に割りました**（`AUTODEV.md` の B）。**弓を
「棒 3 + 糸 3」に戻すのは次の 10 番**で、**この周は `crafting.ts` を 1 行も触りません。**

## 1. 何を足すか / 完了の判定

**夜の地表にクモが湧いて、倒すと糸が 1 個落ちる。表の作りはゾンビ（`ZOMBIE`）とまったく
同じ**で、違うのは**横に広い当たり判定（1.4 x 0.9）と脚 8 本**だけ。完了の判定 ——
**`npm test` に次が増えて全部緑**（**2934 件から減らさないこと**）:

- 「**敵対が 4 種類**」「**夜の草地にクモが湧く**」「昼の草地には湧かない」
  「**敵対の上限はゾンビとクモの合計で見る**」
- 「**殴って倒しても撃って倒しても糸 1 個**」（並べて測る）
- 「糸は**置けず・道具でもなく・食べ物でもない**」
- 「共有帯のアイテムが **20 個・133 まで**」「**111..255 の空きが 122**」
- 「**クモの脚の先が胴より外へ出ている**」（鶏の翼が体に埋まった罠よけ。`rules/mobs.md`）

## 2. 触るファイル / 触らないファイル

| 触る | 何を |
| --- | --- |
| `src/items.ts` | 定数 **1 つ**（133）+ `MAX_ITEM_ID` の差し替え + `item({...})` **1 行**（`FOODS` は 0 行） |
| `src/mobs.ts` | `MobKind` に **`"spider"` 1 語** + `SPIDER` の `MobDef` **1 つ** + `MOBS` / `MOB_KINDS` に **1 語ずつ**（**`HOSTILE_KINDS` は表から作られるので 0 語**） |
| `test/mobs.test.ts` / `test/blocks.test.ts` | 下の 6 |
| `TUNING.md` / `ROADMAP.md` / `AUTODEV-QUEUE.md` / `docs/autodev-log.md` / `HANDOFF.md` | 下の 8 |

**1 行も書かないこと**: `main.ts`（**1463 行。触れば停止条件 2 番に当たります**）/
**`crafting.ts`（10 番の仕事）** / `mobmesh.ts` / `mobrender.ts`（**モブを 1 種類足すのに 0 行**。
`rules/mobs.md`）/ `bow.ts` / `projectiles.ts` / `drops.ts` / `smelting.ts` / `blocks.ts` /
`use.ts` / `vitals.ts` / `physics.ts` / `inventoryui.ts` / `.claude/**`。**先に読む `rules/`**:
`mobs.md` / `items-survival.md` / `vitals.md` / `testing.md`。（`crafting.ts` は 10 番で読む）

## 3. 使う ID

**1 個だけ。`ROADMAP.md` の予約表の「133..255 予備」の先頭から: 133 = 糸。モブは ID を
取りません**（`MobKind` は文字列）。**`MAX_ITEM_ID` 132 → 133 / 111..255 の空き 123 → 122 /
アイテム 97 → 98 種。`SaveData.version` は 1 のまま**（**モブは保存しない**。`rules/mobs.md`）。

## 4. 判断をどこに置くか

体力・速さ・湧きの重み・段差・形・声は **`mobs.ts` の `SPIDER`（`MobDef` 1 つ）**、何を落とすかは
**`SPIDER.drop`**（1 山だけ。`extra` は使わない）。**確かめられないものは 0 です** ——
`buildMobMesh()` も `MobRenderer` も種類の名前を知らないので **`unverifiable-pair` は
要りません**（`rules/mobs.md`）。**スキルは `add-block` だけ。**

## 5. 実装の要点（この順で）

1. `items.ts`: `STRING = 133`、`MAX_ITEM_ID = STRING`、`item({ id: STRING, name: "糸",
   block: AIR, stack: MAX_STACK, color: 0xb8bcc8, tool: null })`。**`tool:` を持たせない**
   （`TOOL_ATTACK` に無い種類が入って **NaN** が黙って通ります）。**色は羽根 `0xe8e4dc` と
   卵 `0xf7f0e0` から離した冷たい灰**にすること。**`FOODS` には入れない**（羽根・革と同じ）
2. `mobs.ts`: `MobKind` に `"spider"`、`MOBS` / `MOB_KINDS` に 1 語ずつ
   （**`PASSIVE_KINDS` には足さない**。`HOSTILE_KINDS` は `MOB_KINDS` から作られるので 0 語）。
   `SPIDER` の値 —— **`size: { half: 0.7, height: 0.9, step: 0.6 }`**（本家と同じ 1.4 x 0.9。
   段差はゾンビと同じ 0.6 で、**プレイヤーが登れる所には付いてくる**）/ **`maxHealth: 16`** /
   **`speed: 6.0`**（本家の 0.3。ゾンビ 4.6 の元が 0.23 なので同じ比。**歩き 5.2 より速く走り
   8.4 より遅い** = 走れば振り切れる）/ **`damage: 2`** / **`spawnWeight: 100`**（本家でゾンビと
   同じ重み。**夜の敵対はゾンビとクモが半々になります**）/ **`voice: 1.5`** / **`hostile: true`**、
   **`ranged` / `teleport` / `spawnOn` / `orbit` / `phases` / `shearing` / `laying` は `null`**、
   **`flying` / `fireproof` / `boss` は false・`hover` / `regen` は 0**、**`drop: { item: STRING,
   count: 1, chance: 1 }`**（本家の 0〜2 個ではなく **1 個固定**。`extra` は使わない ＝ 羽根と同じ）
3. 形（`groups` / `boxes`）: **0 = 胴（`fixed`）/ 1 = 頭（`head`）/ 2..9 = 脚 8 本（`swing`）**。
   **振る部位の箱は `box[4] === 0`**（軸からぶら下げる）。**当たり判定に全部収めること** ——
   **前後左右 ±11.2px（`px(11.2)`）・上は 14.4px**。`longBody` の例外は**豚と羊だけ。そこへ
   クモを足さない**（本家の脚は判定の外まで届きますが、形のほうを削る）
4. **脚は胴より外へ出すこと**（`swing` の箱を胴の `maxX` より外まで伸ばす）。**目は胴・脚と
   違う色**（赤系）で**頭の前面から 0.1px 出す**こと —— 同じ色の箱を中に置くと 1 画素も見え
   ません（鶏の翼・牛の顔で 2 度。`rules/mobs.md`）。**胴・脚・目は濃さの違う 3 色**にし、
   **真っ黒にしないこと**（エンダーマン `0x14121a` が既に居ます）

## 6. 書くテスト（**値を出力してから判定すること**。`rules/testing.md`）

- `test/mobs.test.ts`:
  - **`census()` の `counts` に `クモ: 0` を足し、`show()` にも足すこと** —— **忘れると
    `counts[name]++` が `NaN` になり、数えているつもりで緑になります**（`rules/mobs.md`）
  - **夜の草地にクモが湧く** / **昼の草地には湧かない** / **敵対が 4 種類**
  - **「敵対の上限を超えない」を `ゾンビ + クモ` の合計に数え直す**（**ゆるめるのではなく
    数え直す**。いまゾンビだけを見ているので、半分になったぶんが素通りします）
  - **「ゾンビがいちばん出やすい」を数え直す** —— 重みの表が **3 種類（ゾンビ 100 / クモ 100 /
    エンダーマン 10）**になるので、**3 つの実測値を出力してから**「ほぼ同数 + 1/10」で判定する
  - **殴って倒したときと撃って倒したときを並べて**、どちらも **糸 1 個**
    （片方だけ直すと弓のときだけ落ちない形が戻ります。`rules/mobs.md`）
  - **脚の箱の `|x|` の最大が胴の箱の `|x|` の最大より大きい**（値を両方出力してから）
- `test/blocks.test.ts`: **共有帯の一覧を「20 個・133 まで」に数え直す**（`sharedItems[19]` を
  突き合わせる）/ **`111..255 の空きは 122`** / **糸を `block === AIR` / `tool === null` /
  `foodOf(糸) === null`**（羽根・革と同じ行に足す）

## 7. このタスク固有の禁じ手

1. **糸に使い道を足さないこと**（**弓は次の 10 番**。`crafting.ts` は 0 行）
2. **既存のドロップ表・`FOODS` の既存行を書き換えないこと**（足すだけ）
3. **`longBody` に `"spider"` を足さないこと / `size` を広げないこと** —— 形のほうを削る
4. **`fireproof: true` にしないこと** —— 本家のクモは日光で燃えませんが、ここでは
   **`fireproof` が溶岩と日光の両方を止める 1 つの印**です。**新しい印（`sunproof` など）を
   足すのもこの周では禁止。クモは夜に湧いて朝に燃えます** —— `TUNING.md` に 1 行残すこと
5. **`spawnOn` を付けないこと**（付けると「どこでも」の敵対に勝ってしまいます）
6. **`mobrender.ts` / `mobmesh.ts` / `main.ts` を 1 行も触らないこと**
7. **テストの判定をゆるめないこと**（とくに `test/blocks.test.ts` の共有帯の一覧、
   `test/mobs.test.ts` の重みの表、`test/progression.test.ts` の 13 / 13）

## 8. 終了条件

`npm run typecheck` と `npm test` が緑（**2934 件から増えていること**）/ `npm run build` /
**コミット 1 つ** / `TUNING.md` に「**クモ（AUTODEV 21）**」の節を 1 つ（**速さ 6.0・体力 16・
重み 100・声 1.5・糸 1 個固定・朝に燃えること・糸に使い道がまだ無いこと**）/ `ROADMAP.md` の
予約表に **133 を「実装済み」で足す**（予備は **134..255**）/ **C-3（`AUTODEV.md` の手順）で
`node tools/browsershot.mjs` と `npm run shot -- mobs` を撮って `Read` で開き、クモの形
（脚 8 本が胴に埋まっていないか・目が見えるか）と console のエラー 0 件を自分の目で見る**
（写った不具合はこの周で直す）/
9 番をキューから消す / 仕様書を `済` に / **踏んだ落とし穴を `rules/` へ `Edit` で据える**
（無ければ「決まりごと 0 件」）/ `HANDOFF.md` を丸ごと書き直す。
