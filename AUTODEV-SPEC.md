# 仕様: 弓を本家の形に戻す（羊毛 3 → 糸 3）

状態: 済
差し戻し: 0 回

**キューの 10 番。数え直し済み**（2026-09-04・コードが根拠）: `src/crafting.ts:147` の弓は
まだ `key: { S: STICK, W: WOOL }` で**羊毛のまま**、`src/items.ts:319` の `STRING = 133` は
**もうある**（依存は解けています）。`crafting.ts` に `STRING` の import は 0 行、`SMELTING`
にも 0 行 ＝ **糸の使い道はいまだに 1 つもありません**。実装前: **2962 件緑** /
**111..255 の空き 122** / `main.ts` **1464 行**。**ID を 1 個も取らない周**です。

## 1. 何を足すか / 完了の判定

**弓の材料を羊毛 3 → 糸 3 に差し替える。形と並び（`.SW` / `S.W` / `.SW`）は動かさない。**
これで弓は本家と同じ「棒 3 + 糸 3」になり、**弓を作るにはクモを 3 匹倒す**ことになります
（羊を探す道のりは無くなり、ベッドとの取り合いも消えます）。完了の判定 ——
**`npm test` に次が増えて全部緑**（**2962 件から減らさないこと**）:

- 「**棒 3 + 糸 3 → 弓 1 本**」「左右どちらの向きでも作れる」「2x2 では作れない」
  （**既存 3 件の材料を差し替える**。増える件数ではありません）
- 「**羊毛では弓が作れなくなった**」（新規。羊毛の盤面が `null`）
- 「**ベッドは今までどおり羊毛 3 + 板 3**」（新規。**羊毛の使い道が消えていないこと**）
- 「**糸を使うレシピはちょうど 1 本（弓）**」（新規。本数と名前を出力してから判定）

## 2. 触るファイル / 触らないファイル

| 触る | 何を |
| --- | --- |
| `src/crafting.ts` | `./items` の import に **`STRING` 1 語** + 弓の **1 行**（`W: WOOL` → `W: STRING`）+ その上のコメント。**`./blocks` の `WOOL` はベッドが使うので残すこと** |
| `src/items.ts` | **`BOW` の説明コメント（129..136）だけ。** 定数・表・`FOODS`・`MAX_ITEM_ID` は 1 行も動かさない |
| `test/crafting.test.ts` | 下の 6 |
| `test/blocks.test.ts` | **糸のコメント（235..237）だけ。`check` は 1 つも動かさない** |
| `TUNING.md` / `ROADMAP.md` / `rules/items-survival.md` / `AUTODEV-QUEUE.md` / `docs/autodev-log.md` / `HANDOFF.md` | 下の 8 |

**1 行も書かないこと**: `main.ts`（**1464 行。触れば停止条件 2 番に当たります**）/
`mobs.ts`（**クモの湧きも糸のドロップも動かさない**）/ `bow.ts` / `projectiles.ts` /
`durability.ts`（弓 384 回はそのまま）/ `drops.ts` / `smelting.ts` / `blocks.ts` /
`inventory.ts` / `craftscreen.ts` / `inventoryui.ts` / `.claude/**`。
**先に読む `rules/`**: `items-survival.md` / `inventory-screen.md` / `testing.md` /
`blocks-shapes.md`（`test/blocks.test.ts` のぶん）。

## 3. 使う ID

**0 個。** `STRING`(133) も `BOW`(93) も既にあります。**`MAX_ITEM_ID` は 133 のまま /
111..255 の空きは 122 のまま / アイテムは 98 種のまま / `SaveData.version` は 1 のまま**
（**レシピは保存しません**）。**`ROADMAP.md` の予約表から新しい番号を取らないこと。**

## 4. 判断をどこに置くか

材料は **`crafting.ts` の `RECIPES` の 1 行**だけ。**確かめられないものは 0 です** ——
描画も音も DOM も 0 行なので **`unverifiable-pair` は要りません**。**スキルも使いません**
（`add-block` は ID を取る周のものです）。

## 5. 実装の要点（この順で）

1. `crafting.ts` の `./items` の import に `STRING` を足す。**`./blocks` の `WOOL` は
   ベッド（106 行）が使うので消さないこと**（消すと typecheck が落ちます）
2. 弓の 1 行を `key: { S: STICK, W: STRING }` に。**`shape` / `count` / `out` / `name` は
   動かさない** —— 動かすと「同じ形のレシピが重複していない」（`test/crafting.test.ts:374`。
   `sig` は `shape.join("/") + JSON.stringify(key)`）の当たり方まで変わります
3. 弓の上のコメント（144..146 の「糸の代わりが羊毛」）を書き直す ——
   **「本家と同じ棒 3 + 糸 3。糸はクモが落とす」**の言い分に
4. `items.ts` の `BOW` の説明（132..133 の「Minecraft の糸の代わりに羊毛」）も同じに直す。
   **`ARROW`(94) の説明も「羽根は要らない」のまま古い**ので、**コメント 1 行だけなら
   同じ周で直してよい**（表・材料・順番は 1 つも動かさないこと）

## 6. 書くテスト（**値を出力してから判定すること**。`rules/testing.md`）

- `test/crafting.test.ts`:
  - 文字の表 `P`（68..72 行）に **`G: STRING` を足す**。**`L: WOOL` は消さないこと** ——
    下の 2 件が使います（消すと `WOOL` の import が余って typecheck が落ちます）
  - **既存 3 件**（192..197）の盤面を `L` → `G` に差し替え、ラベルも
    「**棒 3 + 糸 3 → 弓 1 本**」に直す。**判定の形（`out === BOW && count === 1`）は
    ゆるめない**
  - **新規「羊毛では弓が作れない」**: `grid(3, [".SL", "S.L", ".SL"], P)` が **`null`**
    （`?.name ?? "無し"` を補足に出すこと。**残っていたら羊毛の道が消えていません**）
  - **新規「ベッドは今までどおり羊毛 3 + 板 3」**: `grid(3, ["LLL", "PPP"], P)` が
    **`BED` 1 個**。**いま `test/crafting.test.ts` にベッドの判定は 1 件もありません** ——
    羊毛の唯一の使い道なので、ここで足しておくこと
  - **新規「糸を使うレシピはちょうど 1 本（弓）」**: `RECIPES` を走査して
    `key` の値と `ingredients` の両方から数え、**本数と名前を出力してから** 1 本と判定
  - 「同じ形のレシピが重複していない」（374）は**そのまま緑であること**（判定を触らない）
- `test/blocks.test.ts`: **`check` を 1 つも動かさないこと**（糸は置けず・道具でもなく・
  食べ物でもないまま）。235..237 のコメントだけ現状に合わせる

## 7. このタスク固有の禁じ手

1. **ベッドの羊毛（`WWW` / `PPP`）を書き換えないこと** —— 羊毛の使い道はベッドだけになります
2. **糸に 2 本目の使い道を足さないこと**（防具・本・釣り竿はどれも別件）
3. **`items.ts` の定数・`FOODS`・`MAX_ITEM_ID` を動かさないこと**（コメントだけ）
4. **弓の耐久 384 回・スタック 1・`isBow()` の表を触らないこと**
5. **テストの判定をゆるめないこと**（とくに「同じ形のレシピが重複していない」と
   `test/progression.test.ts` の 13 / 13）
6. **クモの重み・糸のドロップ（`mobs.ts`）を動かさないこと** —— 弓が遠くなったからといって
   湧きやすくしない。**手触りの話は `TUNING.md` に 1 行**残して進むこと
7. **`main.ts` を 1 行も触らないこと**（1464 行。停止条件 2 番）

## 8. 終了条件

`npm run typecheck` と `npm test` が緑（**2962 件から増えていること**）/ `npm run build`
（`src/**` を触るため）/ **コミット 1 つ** / `TUNING.md` の「弓の材料」（241 行）を
**棒 3 + 糸 3 ＝ 本家**に書き直し、**「クモを倒すまで弓が作れない」手触りを 1 行**足す
（443 行の `SPIDER.drop` の「糸の使い道はまだ 1 つもありません」も直す）/ `ROADMAP.md`
156 行の糸の同じ言い分を直す / **`rules/items-survival.md` の 178..183（「残っている代用は
弓の糸（羊毛 3）だけ」）を `Edit` で直す** —— **古くなった決まりごとを残さないこと**（C-4）/
10 番をキューから消す / 仕様書を `済` に / `docs/autodev-log.md` に 1 節 /
`HANDOFF.md` を丸ごと書き直す。

**見た目には出ない周です**（`crafting.ts` の 1 行とコメントだけ。`#recipehint` に出る名前は
「弓」のまま）。**`AUTODEV.md` の C-3 の撮影は要りません** —— 撮らずに閉じてよい唯一の形です。
